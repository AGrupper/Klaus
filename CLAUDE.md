# Klaus Runtime Notes

See `AGENTS.md` for the current runtime contract. Claude Project provides
reasoning through MCP; Cloud Run retains data, authorization, deterministic
alerts, routines, Hub APIs, and Web Push. Gemini is embedding-only.
