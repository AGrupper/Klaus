# =====================================================================
# Klaus — Personal Hybrid Agent
# Python 3.11+
# =====================================================================

# --- Google OAuth & APIs (Phase 1: Auth & scaffolding) ---
google-auth>=2.30
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
google-api-python-client>=2.140    # Gmail + Calendar service builders

# --- Cloud state (Firestore: Things 3 queue + user profile) ---
google-cloud-firestore>=2.18
google-cloud-secret-manager>=2.20  # OAuth token persistence on Cloud Run
google-cloud-storage>=2.18         # GCS: chat-log staging bucket (Phase 12)
google-cloud-monitoring>=2.20      # Cloud Monitoring: heartbeat 5xx metrics
google-cloud-run>=0.10             # Cloud Run revision checks (Phase 14)
google-cloud-tasks>=2.16           # Telegram update dispatch → full-CPU in-request processing

# --- Vector memory (Pinecone: unstructured RAG) ---
# NOTE: the legacy 'pinecone-client' package is deprecated; the new SDK is 'pinecone'.
pinecone>=5.0

# --- LLM SDKs (dual-model architecture + universal fallback) ---
anthropic>=0.99,<1.0                # Main "Smart" agent (claude-sonnet-5) — floor bump for typed adaptive-thinking/Sonnet-5 support (30.5-03)
google-genai>=1.0                  # Gemini Flash router (fast/cheap) — new official SDK (google-generativeai is deprecated)
openai>=1.50                       # OpenAI-compatible protocol = lingua franca for self-hosted models (e.g. Hermes)

# --- Interface ---
python-telegram-bot>=21.0
fastapi>=0.115                     # Cloud Run webhook server
uvicorn[standard]>=0.30            # ASGI server for FastAPI

# --- Runtime utilities ---
python-dotenv>=1.0
python-dateutil>=2.8.2             # NL datetime parse for schedule_followup (D-12)
requests>=2.32
tzdata>=2024.1                     # Timezone database for zoneinfo (needed on Linux/Cloud Run)
itsdangerous>=2.2                  # HMAC-signed session cookies for hub auth (Phase 26, HUB-01; RESEARCH Open Question 3)

# --- External data connections (Phase 7) ---
garminconnect>=0.2          # Garmin Connect health data (sleep, HRV, body battery)
psycopg2-binary>=2.9        # PostgreSQL database adapter

# --- Web Push notifications (Phase 29) ---
pywebpush>=2.3              # Web Push sends (VAPID-signed); py-vapid arrives transitively

# --- Groq token-budget measurement (Phase 32) ---
tiktoken==0.13.0            # o200k_harmony tokenizer for the MEM-05 Groq budget-guard test

# --- Dev / testing ---
pytest>=8.0
