# =====================================================================
# Klaus — Personal Hybrid Agent
# Python 3.11+
# =====================================================================

# --- Google OAuth & APIs (Phase 1: Auth & scaffolding) ---
google-auth>=2.30
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
google-api-python-client>=2.140    # Gmail + Calendar service builders

# --- Cloud state (Firestore: Things 3 queue + user profile) ---
google-cloud-firestore>=2.18
google-cloud-secret-manager>=2.20  # OAuth token persistence on Cloud Run

# --- Vector memory (Pinecone: unstructured RAG) ---
# NOTE: the legacy 'pinecone-client' package is deprecated; the new SDK is 'pinecone'.
pinecone>=5.0

# --- LLM SDKs (dual-model architecture + universal fallback) ---
anthropic>=0.40                    # Likely main "Smart" agent (Claude)
google-genai>=1.0                  # Gemini Flash router (fast/cheap) — new official SDK (google-generativeai is deprecated)
openai>=1.50                       # OpenAI-compatible protocol = lingua franca for self-hosted models (e.g. Hermes)

# --- Interface ---
python-telegram-bot>=21.0
fastapi>=0.115                     # Cloud Run webhook server
uvicorn[standard]>=0.30            # ASGI server for FastAPI

# --- Runtime utilities ---
python-dotenv>=1.0
requests>=2.32
tzdata>=2024.1                     # Timezone database for zoneinfo (needed on Linux/Cloud Run)

# --- External data connections (Phase 7) ---
garminconnect>=0.2          # Garmin Connect health data (sleep, HRV, body battery)

# --- Dev / testing ---
pytest>=8.0
