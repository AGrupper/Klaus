---
phase: 14-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/main.py
  - docs/TECHNICAL_PLAN.md
autonomous: true
requirements:
  - LLM-01
  - LLM-02

must_haves:
  truths:
    - "core/main.py contains no 'JARVIS' string anywhere"
    - "core/main.py contains no 'Claude' in agent-description comments (module docstring, inline loop comments)"
    - "docs/TECHNICAL_PLAN.md contains an LLM-per-purpose map section listing tick-brain, brain, worker, fallback, embeddings"
  artifacts:
    - path: "core/main.py"
      provides: "corrected module docstring and inline comments referencing Gemini"
      contains: "Gemini 3 Flash"
    - path: "docs/TECHNICAL_PLAN.md"
      provides: "LLM-per-purpose map"
      contains: "tick-brain"
  key_links:
    - from: "core/main.py module docstring"
      to: "actual brain model"
      via: "comment text"
      pattern: "Gemini.*Smart Agent"
---

<objective>
Fix stale "Claude"/"JARVIS-style" model references in core/main.py and document the
full LLM-per-purpose map in docs/TECHNICAL_PLAN.md.

Purpose: The codebase was originally built with Claude as the brain. Gemini 3 Flash
replaced it, but comments were never updated. This creates confusion for future phases.
The TECHNICAL_PLAN.md LLM map is the authoritative reference for Phase 15+ self-knowledge.

Output:
- core/main.py — corrected module docstring + inline comments (no code logic changes)
- docs/TECHNICAL_PLAN.md — new "LLM Strategy" section with per-purpose map
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@core/main.py
@docs/TECHNICAL_PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale comments in core/main.py</name>
  <files>core/main.py</files>
  <read_first>core/main.py (read in full — all stale references must be found and fixed)</read_first>
  <action>
Read core/main.py in full. Make ONLY comment/docstring changes — zero logic changes.

Replace every stale model-name reference with accurate descriptions. Exact replacements:

LINE 1-15 (module docstring) — replace entire docstring with:
```python
"""Agent orchestrator — dual-model coordination logic.

Gemini 3 Flash (Smart Agent) receives every user message, makes all judgment
calls, and crafts every response. Gemini 2.5 Flash (Worker Agent) executes
tools and gathers data on the Smart Agent's behalf via two delegation paths:

  Path A — Delegate + Review:
    Smart Agent delegates via delegate_to_worker (respond_directly=false)
    → Worker executes tools → result returned to Smart Agent → Smart Agent responds.

  Path B — Worker Solo:
    Smart Agent delegates via delegate_to_worker (respond_directly=true)
    → Worker handles the request end-to-end → response goes directly to user.

Conversation history storage is pluggable via the ConversationStore protocol:
"""
```

LINE 228: `# Run Claude's orchestration loop.`
→ `# Run the Smart Agent orchestration loop.`

LINE 233: `# Persist Claude's final text response.`
→ `# Persist the Smart Agent's final text response.`

LINE 238 (section header comment): `# Smart Agent loop (Claude)                                          #`
→ `# Smart Agent loop (Gemini 3 Flash)                                   #`

LINE 243 (def _run_smart_loop docstring): Replace `"""Run Claude's tool-use loop until it produces a final text response.\n\n        Claude may call delegate_to_worker...`
→ `"""Run the Smart Agent tool-use loop until it produces a final text response.\n\n        The Smart Agent may call delegate_to_worker...`

Any remaining `# Claude` references in inline comments within _run_smart_loop and _run_worker_loop — replace "Claude" with "Smart Agent".

LINE 334 area: `# Path A: feed Flash's result back to Claude for review + crafting.`
→ `# Path A: feed Flash's result back to the Smart Agent for review + crafting.`

LINE 343-348: `# WHY: remember/recall are always called directly by Claude —` area
→ `# WHY: remember/recall are always called directly by the Smart Agent —`

Do NOT change any variable names, function names, string literals passed to APIs,
or any executable code. Comments and docstrings only.
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && grep -n "JARVIS\|Claude (Smart\|Claude's orchestration\|Claude's final\|Claude's tool-use\|called directly by Claude\|Smart Agent loop (Claude)" core/main.py | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "JARVIS" core/main.py == 0
    - grep -in "claude" core/main.py shows ONLY: import lines, variable names (smart_agent_fallback, etc.), string content (e.g. "I'm afraid..." response text), and the fallback model env var comment. NO "Claude" in agent-description prose comments.
    - grep -n "Gemini 3 Flash" core/main.py returns at least 1 match (module docstring)
    - python -c "import ast; ast.parse(open('core/main.py').read()); print('syntax OK')" exits 0
  </acceptance_criteria>
  <done>
    - Module docstring says "Gemini 3 Flash (Smart Agent)" not "Claude (Smart Agent)"
    - No "JARVIS-style" anywhere in file
    - No "Claude" in agent-description comments (only in variable names and string literals is acceptable)
    - File syntax valid (ast.parse passes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add LLM-per-purpose map to docs/TECHNICAL_PLAN.md</name>
  <files>docs/TECHNICAL_PLAN.md</files>
  <read_first>docs/TECHNICAL_PLAN.md (read Section 2 — Model Architecture — to understand current state before appending)</read_first>
  <action>
Read docs/TECHNICAL_PLAN.md in full. Append a new top-level section after the existing
content (after Phase 13 section, before any trailing newline). The section must be titled
"## LLM Strategy — Per-Purpose Model Map" and contain the following table and notes
verbatim (adjust if docs already contain a newer version):

```markdown
## LLM Strategy — Per-Purpose Model Map

Added Phase 14. This table is the authoritative reference for which model handles
each purpose. Phase 15 (self-knowledge) and Phase 16 (SELF.md) ingest this table.

| Purpose | Backend | Model | Env Vars | Notes |
|---------|---------|-------|----------|-------|
| Smart Agent (brain) | gemini | gemini-3-flash-preview | SMART_AGENT_BACKEND / MODEL / API_KEY | Orchestration, judgment, crafting responses |
| Worker Agent (hands) | gemini | gemini-2.5-flash | WORKER_AGENT_BACKEND / MODEL / API_KEY | Tool execution, data gathering, structured JSON |
| Smart Agent fallback | anthropic | claude-haiku-4-5 | SMART_AGENT_FALLBACK_BACKEND / MODEL / API_KEY | Activated inline on LLMError from brain |
| Tick-brain | openai (Groq-compat) | qwen3-32b (default) | TICK_BRAIN_BACKEND / MODEL / API_KEY / BASE_URL | Free Groq tier; falls back to Smart Agent on error |
| Embeddings | gemini | gemini-embedding-2 | (uses WORKER_AGENT_API_KEY) | AI Studio only — NOT Vertex AI |

### Model Selection Rationale

- **Gemini 3 Flash** as brain: lower cost than Claude at scale, native tool-use, Google ecosystem
- **Gemini 2.5 Flash** as worker: fastest structured JSON output, shared API key with brain
- **Claude Haiku** as fallback: diversity hedge — different provider means different failure modes
- **Groq/Qwen3-32B** for tick-brain: free tier, 0.5s latency, does not train on data, OpenAI-compatible
- **gemini-embedding-2** for embeddings: available via AI Studio (NOT Vertex AI — embedding model is AI Studio only)

### Cost Model

Measure everything, cap nothing (explicit user preference). Cost is recorded per call via
`LLMUsageStore` (Phase 14) and surfaced in `get_self_status` (Phase 16).
Free models (Groq) return cost=0.0 by design — see `core/pricing.py::compute_cost()`.
```
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && grep -n "LLM Strategy" docs/TECHNICAL_PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "## LLM Strategy" docs/TECHNICAL_PLAN.md returns a match
    - grep -n "tick-brain" docs/TECHNICAL_PLAN.md returns at least 1 match
    - grep -n "gemini-embedding-2" docs/TECHNICAL_PLAN.md returns at least 1 match (may already exist; verify it's in the new table)
    - grep -n "Groq" docs/TECHNICAL_PLAN.md returns at least 1 match
    - grep -n "qwen3-32b" docs/TECHNICAL_PLAN.md returns a match
    - grep -n "LLMUsageStore" docs/TECHNICAL_PLAN.md returns a match
  </acceptance_criteria>
  <done>
    - docs/TECHNICAL_PLAN.md contains "## LLM Strategy — Per-Purpose Model Map" section
    - Table lists all 5 purposes: Smart Agent, Worker Agent, fallback, tick-brain, embeddings
    - Each row has correct backend, model name, and env var prefix
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/TECHNICAL_PLAN.md | Internal documentation; no execution surface |
| core/main.py comments | No executable impact; comment-only edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | core/main.py | accept | Comment-only edits; syntax validation (ast.parse) prevents accidental logic breakage |
| T-14-05 | Information Disclosure | docs/TECHNICAL_PLAN.md | accept | Internal docs in private repo; no sensitive credentials in the table (env var names only, not values) |
</threat_model>

<verification>
1. `grep -c "JARVIS" core/main.py` returns 0
2. `grep -n "Gemini 3 Flash" core/main.py` returns at least 1 match
3. `python -c "import ast; ast.parse(open('core/main.py').read()); print('syntax OK')"` exits 0
4. `grep -n "## LLM Strategy" docs/TECHNICAL_PLAN.md` returns a match
5. `grep -n "tick-brain" docs/TECHNICAL_PLAN.md` returns a match
</verification>

<success_criteria>
- core/main.py: zero "JARVIS" references; zero "Claude" in agent-description comments; file syntax valid
- docs/TECHNICAL_PLAN.md: new LLM Strategy section present with 5-row purpose table
</success_criteria>

<output>
After completion, create .planning/phases/14-foundation/14-02-SUMMARY.md
</output>
