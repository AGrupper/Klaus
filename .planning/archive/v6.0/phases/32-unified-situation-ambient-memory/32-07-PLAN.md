---
phase: 32-unified-situation-ambient-memory
plan: 07
type: execute
wave: 2
depends_on: ["32-01", "32-02", "32-04"]
files_modified:
  - core/autonomous.py
  - prompts/autonomous_triage.md
  - prompts/autonomous.md
  - tests/test_autonomous.py
  - tests/test_token_budget.py
autonomous: true
requirements: [MEM-04, MEM-05]
user_setup: []

must_haves:
  truths:
    - "The cascade sees a conversation_tail gather (triage: 24h/≤15 msgs/240-char cap; paid compose: 48h/≤40 msgs) and a reconciled training_reality window (today-3d..tomorrow)"
    - "Triage renders training_reality as tight terminal-status strings; paid Layer-2 compose renders the wider, evidence-rich window"
    - "conversation_tail and training_reality are context-only in _is_empty_signals — neither flips an otherwise-empty tick to non-empty"
    - "The token-budget guard stays green with the new triage render slots actually populated at their caps"
  artifacts:
    - path: "core/autonomous.py"
      provides: "_gather_conversation_tail + _gather_training_reality gathers, jobs registration, context-only exclusion comments, triage render slots"
      contains: "_gather_training_reality"
    - path: "prompts/autonomous_triage.md"
      provides: "{conversation_tail} + {training_reality} tight render slots"
      contains: "{training_reality}"
    - path: "prompts/autonomous.md"
      provides: "wider training_reality render for paid compose"
      contains: "training_reality"
  key_links:
    - from: "core/autonomous.py::_gather_training_reality"
      to: "core/training_checkin.py::build_training_reality"
      via: "gather reads stores + calls the pure reconciler"
      pattern: "build_training_reality"
    - from: "core/autonomous.py::_is_empty_signals"
      to: "conversation_tail / training_reality keys"
      via: "deliberately NOT referenced (context-only exclusion comment)"
      pattern: "context.only|CONTEXT only"
---

<objective>
Wire the conversation tail and reconciled `training_reality` into the autonomous cascade as
context-only gathers, render them (tight for triage, wide for paid compose), enforce the
context-only invariant in `_is_empty_signals`, and prove the token budget still holds with
the new triage slots populated.

Purpose: MEM-04 gives triage and paid compose a real view of what actually happened
(so a completed/moved session is never re-asked) plus recent conversation context. MEM-05's
non-negotiable invariant: neither gather may wake the free tier — the empty gate is Klaus's
entire cost model.
Output: two new gathers + jobs registration + exclusion comments + two prompt render slots
(triage tight, compose wide) + per-gather context-only tests + the tightened budget guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md
@.planning/phases/32-unified-situation-ambient-memory/32-02-SUMMARY.md
@.planning/phases/32-unified-situation-ambient-memory/32-04-SUMMARY.md

<interfaces>
From core/autonomous.py: sentinel-on-failure gather shape (lines 338-355 _gather_standing_directives
is the exact clone target); `gather_situation` jobs dict (lines 615-647); `_is_empty_signals`
(lines 175-227) with the training_status/acwr (192-195) and standing_directives (220-226)
context-only exclusion comments to mirror; `_build_triage_prompt` (line 718) is where triage
render slots attach; `_compose_layer2` (line 885) / `_compose_followup_layer2` (line 955) are
the paid-compose render sites; `_gather_training_evidence` (504-579, single-date) reused across dates.
From core/training_checkin.py (Plan 04): `planned_sessions_for(date_iso)`, `build_training_reality(...)`.
From memory/firestore_conversation.py: `get_recent_window(user_id, hours, max_messages)`.
Windows (MEM-04): triage tail 24h/≤15 msgs/240-char cap; paid compose tail 48h/≤40 msgs;
reconciliation window today-3d..tomorrow (5 dates). Research Open Question 2: triage
training_reality = terminal status strings only (no evidence detail); paid = rich detail.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _gather_conversation_tail + _gather_training_reality (sentinel-on-failure) + jobs registration</name>
  <read_first>core/autonomous.py (lines 338-355 gather clone target, 504-579 _gather_training_evidence, 615-647 jobs dict); core/training_checkin.py (build_training_reality + planned_sessions_for from Plan 04); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 1, §Pattern 4)</read_first>
  <files>core/autonomous.py</files>
  <behavior>
    - `_gather_conversation_tail(...)` returns the recent tail via `get_recent_window` (widest window needed, 48h/≤40 msgs; triage rendering trims to 24h/≤15/240-char). Sentinel `[]` on failure, never raises.
    - `_gather_training_reality(now, project_id, database)` reads planned (`planned_sessions_for`), calendar (reuse the existing `calendar` gather output where possible), evidence (loop `_gather_training_evidence` across today-3d..tomorrow), self-report (`TrainingLogStore.get_range`), calls `build_training_reality(...)`, returns the per-date dict. Sentinel `{}` on failure, never raises.
    - Both registered in `gather_situation`'s jobs dict with a CONTEXT-only comment.
  </behavior>
  <action>Add both `_gather_*` functions cloning the `_gather_standing_directives` sentinel-on-failure shape. For `_gather_training_reality`, reuse `_gather_training_evidence` across the 5-date window (loop dates) and pass the assembled planned/calendar/evidence/self-report data into `build_training_reality`. Register both in the jobs dict (lines 615-647) with a `# CONTEXT only, never a trigger (see _is_empty_signals)` comment mirroring standing_directives. Do NOT add a second Calendar API call — reuse the existing `calendar` gather result.</action>
  <verify>
    <automated>pytest tests/test_autonomous.py -k "conversation_tail or training_reality" -x</automated>
  </verify>
  <acceptance_criteria>Tests assert: (a) both gathers return their sentinel on a forced store failure without raising; (b) `_gather_training_reality` produces the reconciled per-date dict from injected/mocked stores; (c) both keys appear in `gather_situation`'s output.</acceptance_criteria>
  <done>Two context-only gathers added, sentinel-safe, registered in the jobs dict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Context-only invariant in _is_empty_signals + per-gather assertions (MEM-05)</name>
  <read_first>core/autonomous.py (lines 175-227 _is_empty_signals with the 192-195 + 220-226 exclusion comments); tests/test_autonomous.py (existing is_empty_signals tests); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pitfall 3)</read_first>
  <files>core/autonomous.py, tests/test_autonomous.py</files>
  <behavior>
    - `_is_empty_signals` gains explicit docstring/comment exclusions for `conversation_tail` and `training_reality` (mirroring standing_directives wording) with a one-line rationale each.
    - A situation carrying ONLY a non-trivial `conversation_tail` (or ONLY a non-trivial `training_reality`) and no other trigger returns `True` (empty) — the gather does not wake the free tier.
    - The standing_directives and location gathers are also asserted context-only (all four new/existing MEM gathers covered).
  </behavior>
  <action>Extend `_is_empty_signals`'s exclusion comment block to name `conversation_tail` and `training_reality` explicitly (do NOT add any `if situation.get(...)` check for them). Add one unit assertion per gather in `tests/test_autonomous.py`: `_is_empty_signals({..., "<gather>": <non-trivial value>})` returns `True` when no other trigger is present. Cover conversation_tail, training_reality, standing_directives (regression), and leave a placeholder assertion for `location` that Plan 08 completes (or coordinate so Plan 08 adds the location assertion).</action>
  <verify>
    <automated>pytest tests/test_autonomous.py -k is_empty_signals -x</automated>
  </verify>
  <acceptance_criteria>One passing assertion per new/existing MEM gather that a non-trivial value alone keeps `_is_empty_signals` True; the exclusion comment names conversation_tail + training_reality with rationale; no code path references them in the gate.</acceptance_criteria>
  <done>Context-only invariant enforced and tested for the tail + reality gathers (location covered by Plan 08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Triage (tight) + paid-compose (wide) training_reality/tail renders + tightened budget guard</name>
  <read_first>core/autonomous.py (line 718 _build_triage_prompt, 885/955 compose sites); prompts/autonomous_triage.md, prompts/autonomous.md; tests/test_token_budget.py (Plan 01); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 4 two-variant render, §Open Question 2)</read_first>
  <files>core/autonomous.py, prompts/autonomous_triage.md, prompts/autonomous.md, tests/test_autonomous.py, tests/test_token_budget.py</files>
  <behavior>
    - Triage render: `conversation_tail` trimmed to 24h/≤15 msgs/240-char-per-message; `training_reality` as tight terminal-status strings for today+tomorrow only (no evidence detail) — within the triage char discipline.
    - Paid Layer-2 compose render: wider `training_reality` (full today-3d..tomorrow window with evidence detail — session titles/volumes/pace) + 48h/≤40-msg tail.
    - The token-budget guard test now exercises the REAL new triage slots (the maximal fixture from Plan 01 already carries them at cap) and stays ≤ 8000.
  </behavior>
  <action>Add `{conversation_tail}` and `{training_reality}` render slots to `prompts/autonomous_triage.md` (tight) and a wider `training_reality`/tail render to `prompts/autonomous.md` (compose). Wire the tight render through `_build_triage_prompt` (line 718) and the wide render through `_compose_layer2`/`_compose_followup_layer2`. Enforce the 240-char-per-message triage cap and the 15/40-msg limits in the render helpers. Update `tests/test_token_budget.py` only if the maximal fixture needs to match the final render slot names — then confirm it stays green with the slots actually populated.</action>
  <verify>
    <automated>pytest tests/test_autonomous.py -k "triage_render or training_reality" -x && pytest tests/test_token_budget.py -x</automated>
  </verify>
  <acceptance_criteria>Tests assert: (a) triage render of training_reality is terminal-status strings only, within the char cap, and the tail is trimmed to 24h/≤15/240-char; (b) paid compose render includes the wider window with evidence detail; (c) `test_token_budget.py` passes with the new triage slots populated at their caps (total + max_tokens ≤ 8000).</acceptance_criteria>
  <done>Two-variant renders wired; triage stays within caps; budget guard green with real slots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| context gathers → free-tier cost gate | a context signal must never wake the paid layers |
| rendered triage prompt → Groq 8K ceiling | prompt bloat can 413 → silent metered reroute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-14 | Elevation of Privilege (cost) | conversation_tail/training_reality reachable from _is_empty_signals → every tick fires | mitigate | Exclusion comments + one per-gather assertion that a non-trivial value alone keeps the gate empty (Pitfall 3) |
| T-32-15 | Denial of Service | Triage prompt bloat exceeding Groq 8K per-request ceiling | mitigate | 240-char/15-msg triage caps + the budget-guard test exercised with the new slots populated (Plan 01 + this task) |
</threat_model>

<verification>
- `pytest tests/test_autonomous.py -k "is_empty_signals or conversation_tail or training_reality or triage_render" -x` green.
- `pytest tests/test_token_budget.py -x` green with slots populated.
- `grep -nE "conversation_tail|training_reality" core/autonomous.py` shows jobs registration + exclusion comment, NOT an `if situation.get()` trigger.
- Per-wave: run every `tests/test_*.py` per-file; baseline ≥1775 holds.
</verification>

<success_criteria>
The cascade sees the conversation tail and reconciled training_reality (tight for triage,
wide for paid compose), both provably context-only in `_is_empty_signals`, and the
token-budget guard holds with the new triage slots populated at their caps.
</success_criteria>

<output>
Create `.planning/phases/32-unified-situation-ambient-memory/32-07-SUMMARY.md` when done.
</output>
