---
phase: 32-unified-situation-ambient-memory
plan: 06
type: execute
wave: 2
depends_on: ["32-02"]
files_modified:
  - memory/pinecone_db.py
  - core/main.py
  - prompts/smart_agent.md
  - tests/test_pinecone_db.py
  - tests/test_main.py
autonomous: true
requirements: [MEM-01, MEM-02]
user_setup: []

must_haves:
  truths:
    - "Every chat turn auto-injects a 'Things you remember' block from Pinecone (score-thresholded, recency-weighted, k≈5) rendered in the volatile half"
    - "Auto-recall is best-effort and timeout-guarded — a slow or failing recall yields an empty block and NEVER blocks the turn"
    - "A weak-match (below the score floor) memory does not surface in the ambient block"
    - "After a 6h+ idle gap into a fresh/empty session, the recent conversation tail is prepended as real history with a synthetic time-gap boundary marker so the brain doesn't re-act on the stale last message"
  artifacts:
    - path: "memory/pinecone_db.py"
      provides: "score-thresholded ambient recall path"
      contains: "min_score"
    - path: "core/main.py"
      provides: "ambient recall pre-loop + continuity tail prepend in handle_message"
      contains: "things_you_remember"
    - path: "prompts/smart_agent.md"
      provides: "{things_you_remember} placeholder in the VOLATILE (post-cache) half"
      contains: "{things_you_remember}"
  key_links:
    - from: "core/main.py::handle_message"
      to: "memory/pinecone_db.py ambient recall"
      via: "timeout-guarded recall on the live message, empty-fallback on any exception"
      pattern: "things_you_remember"
    - from: "core/main.py::handle_message"
      to: "FirestoreConversationStore.get_recent_window"
      via: "hours=6 tail prepend + boundary marker when session is idle-empty"
      pattern: "get_recent_window"
---

<objective>
Deliver ambient auto-recall (MEM-01) and conversation continuity (MEM-02): every chat turn
injects a score-thresholded "Things you remember" block, and a fresh session after a 6h+
idle gap rehydrates the recent tail as real history with a time-gap boundary marker — both
best-effort, both off the blocking path, both rendered in the volatile (post-cache) half.

Purpose: Klaus should remember involuntarily and not greet a returning Amit as an amnesiac.
The two hard constraints are safety (Pitfall 4: a slow Pinecone/embedding call must never
stall a chat turn) and cache-hygiene (Pitfall 1: these blocks go in the volatile half from
Plan 02, never in the cached stable prefix).
Output: a score floor on the ambient recall path; `handle_message` recall + tail wiring;
the two placeholders in `smart_agent.md`'s volatile section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md
@.planning/phases/32-unified-situation-ambient-memory/32-02-SUMMARY.md

<interfaces>
From memory/pinecone_db.py (lines 32-62): `_blend_recency(cosine, ts)` already implements
recency-weighting (90-day half-life, 30%-max influence). `MemoryStore.recall` (lines 129-168)
over-fetches, re-sorts by blended score, returns top-k. GAP: no score floor exists.
From core/main.py: `render_smart_system` now returns `(stable, volatile)` (Plan 02);
`handle_message` (line 551) sets `tool_registry.set_current_user_id`, renders the system,
appends the user message, calls `_run_smart_loop`. The ambient block must render into the
VOLATILE half.
From memory/firestore_conversation.py: `get_recent_window(user_id, hours=24, max_messages=60)`
(lines 205-248) windows by per-message `ts`, tolerating legacy no-ts messages. `SESSION_TIMEOUT_HOURS=6`
(line 135) is the same constant `get()` uses to decide a session went idle.
Discretion (research [ASSUMED], tunable): ambient score floor `min_score=0.5` on the BLENDED
score; recall timeout ~2.5s; continuity marker wording (Anthropic messages only support
user/assistant roles — use a `role:"user"` synthetic marker, mirroring _compose_layer2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score-thresholded ambient recall path in pinecone_db</name>
  <read_first>memory/pinecone_db.py (lines 32-62 _blend_recency, 129-168 recall — the $eq user_id filter at 155); tests/test_pinecone_db.py; .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 2, §Assumptions A1)</read_first>
  <files>memory/pinecone_db.py, tests/test_pinecone_db.py</files>
  <behavior>
    - A new ambient path (`recall_ambient(user_id, query, k=5, min_score=0.5)` OR a `min_score` param) filters out candidates whose BLENDED (post-recency-decay) score is below the floor.
    - The existing tool-facing `recall()` stays UNTHRESHOLDED so a deliberate `recall` tool call can still see a marginal match (D-03: two different needs).
    - Filtering is on the blended score already exposed to callers, not raw cosine.
  </behavior>
  <action>Add a score-floor to the auto-inject path only — prefer a thin `recall_ambient(...)` wrapper (leaving `recall()` unchanged) with `min_score` as a module constant (default 0.5, tagged [ASSUMED]/tunable). Log the raw `(score, was_injected)` pairs at debug so the threshold can be re-tuned from real data in the first weeks. Keep the existing `$eq` user_id scoping (line 155) intact.</action>
  <verify>
    <automated>pytest tests/test_pinecone_db.py -k score_threshold -x</automated>
  </verify>
  <acceptance_criteria>Tests assert: (a) a candidate with blended score below min_score is excluded from the ambient result; (b) a candidate at/above the floor is included; (c) the unthresholded `recall()` still returns a marginal match (behavior unchanged); (d) user_id scoping preserved.</acceptance_criteria>
  <done>Ambient recall applies a blended-score floor; deliberate recall stays unthresholded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ambient "Things you remember" block in handle_message (timeout-guarded, never blocks)</name>
  <read_first>core/main.py (lines 551-610 handle_message; the render_smart_system tuple from Plan 02); prompts/smart_agent.md (the volatile section seam introduced in Plan 02); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 2 timeout-guard, §Pitfall 4)</read_first>
  <files>core/main.py, prompts/smart_agent.md, tests/test_main.py</files>
  <behavior>
    - `handle_message` runs ambient `recall_ambient` on the LIVE user message (k≈5) before the smart loop, wrapped in a ~2.5s timeout (the Gemini embed call is synchronous — use run_in_executor / a thread with timeout).
    - ANY exception OR timeout yields an EMPTY block; recall NEVER raises into handle_message and never delays the turn beyond the timeout budget.
    - The formatted memories render into the `{things_you_remember}` placeholder in the VOLATILE half of render_smart_system's output (never the cached stable half).
  </behavior>
  <action>Add `{things_you_remember}` to `prompts/smart_agent.md` in the VOLATILE (post-cache-seam) section. In `handle_message`, call `recall_ambient` on the live message with a hard timeout and a guaranteed empty-block fallback on any exception/timeout, format the results into the block, and substitute it into the volatile half of the rendered system tuple. Do NOT place it before the cache seam (Pitfall 1). Keep the deliberate `recall` tool untouched (D-03 coexistence).</action>
  <verify>
    <automated>pytest tests/test_main.py -k ambient_recall -x</automated>
  </verify>
  <acceptance_criteria>Tests assert: (a) a normal recall injects a non-empty "Things you remember" block into the volatile half; (b) a forced-hang/forced-raise recall yields an empty block and `handle_message` still returns within the normal latency budget (no exception propagates); (c) the block renders after the cache seam, never in the stable half.</acceptance_criteria>
  <done>Ambient block injected best-effort/timeout-guarded into the volatile half; failure path proven non-blocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Continuity tail + time-gap boundary marker for idle-fresh sessions</name>
  <read_first>memory/firestore_conversation.py (lines 135 SESSION_TIMEOUT_HOURS, 205-248 get_recent_window, and get() idle-empty semantics); core/main.py (handle_message message assembly at line 597-600); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 3, §D-05)</read_first>
  <files>core/main.py, tests/test_main.py</files>
  <behavior>
    - When `get(user_id)` returns empty BUT the stored `messages` array is non-empty (a prior session exists, idle-expired), `handle_message` prepends the recent tail (`get_recent_window(user_id, hours=6)`) as real `{"role","content"}` history.
    - A single synthetic `role:"user"` boundary marker is inserted at the seam: `"[~{N}h elapsed since the messages above — a new conversation begins here.]"` (N from the last message's `ts`, generic phrase if `ts` absent).
    - Best-effort: a failed `get_recent_window` yields no tail + no marker (falls through to today's amnesiac-but-safe behavior), never raises.
  </behavior>
  <action>In `handle_message`, detect the idle-fresh condition and prepend the tail + one boundary marker message (role "user", wording per D-05, computing elapsed hours from the last message `ts` with a generic fallback). Insert the tail into the local message list passed to `_run_smart_loop` (do NOT re-persist the tail as new history — it is already stored). Keep it best-effort with a no-tail fallback on any failure.</action>
  <verify>
    <automated>pytest tests/test_main.py -k continuity_tail -x</automated>
  </verify>
  <acceptance_criteria>Tests assert: (a) an idle-fresh session (empty current, non-empty stored, last ts ~8h ago) prepends the tail AND exactly one boundary marker with the elapsed-hours wording; (b) an active (non-idle) session gets NO tail prepend; (c) a raising get_recent_window yields no tail/marker and does not break the turn.</acceptance_criteria>
  <done>Idle-fresh sessions rehydrate the tail with a time-gap marker; active sessions and failures are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pinecone/embedding → chat critical path | a degraded recall now runs on EVERY turn |
| stored memory → agent belief state | weak/stale facts auto-surfaced (ASI06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-11 | Denial of Service (self-inflicted) | Slow Pinecone/Gemini-embed stalling every chat turn | mitigate | ~2.5s timeout + guaranteed empty-block fallback on any exception; forced-hang test proves handle_message stays within latency budget (Pitfall 4) |
| T-32-12 | Tampering (belief state) | Ambient poisoning — weak match surfaced as truth | mitigate | Blended-score floor (Task 1) filters weak matches; correction path (forget_memory + contradiction flag) shipped in Plan 03 |
| T-32-13 | Tampering | Volatile ambient block leaking into the cached prefix | mitigate | Block rendered only in the volatile half (Plan 02 seam); test asserts placement |
</threat_model>

<verification>
- `pytest tests/test_pinecone_db.py -k score_threshold -x`, `pytest tests/test_main.py -k ambient_recall -x`, `pytest tests/test_main.py -k continuity_tail -x` green.
- `grep -n "{things_you_remember}" prompts/smart_agent.md` matches in the post-cache section.
- Per-wave: run every `tests/test_*.py` per-file; baseline ≥1775 holds.
</verification>

<success_criteria>
Every chat turn best-effort injects a score-thresholded "Things you remember" block, a 6h+
idle session rehydrates the tail with a boundary marker, and both live safely in the
volatile half — a slow or failing recall never blocks the turn.
</success_criteria>

<output>
Create `.planning/phases/32-unified-situation-ambient-memory/32-06-SUMMARY.md` when done.
</output>
