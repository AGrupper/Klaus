---
phase: 32-unified-situation-ambient-memory
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - core/training_checkin.py
  - core/nightly_review.py
  - tests/test_training_checkin.py
autonomous: true
requirements: [MEM-04]
user_setup: []

must_haves:
  truths:
    - "A pure build_training_reality(dates) reconciles planned vs self-report vs Garmin/Hevy evidence vs calendar with precedence: evidence > self-report > intent (D-01)"
    - "A real activity of the right modality on the planned day satisfies the slot without pace/distance/duration matching (D-02)"
    - "Once evidence/completion lands, a slot's status is terminal 'done' — a later re-read of the same date+slot returns 'done', never a fresh 'missed' (SC-4 invariant)"
    - "core/autonomous.py never imports core/nightly_review.py — planned_sessions_for lives in the neutral training_checkin module"
  artifacts:
    - path: "core/training_checkin.py"
      provides: "planned_sessions_for(date_iso) + build_training_reality(dates)"
      contains: "def build_training_reality"
    - path: "core/nightly_review.py"
      provides: "re-import of planned_sessions_for from training_checkin"
      contains: "from core.training_checkin import"
  key_links:
    - from: "core/training_checkin.py::build_training_reality"
      to: "TrainingLogStore / StrengthSessionStore / RunDetailStore"
      via: "injected already-fetched evidence dicts (pure function, no I/O of its own)"
      pattern: "def build_training_reality"
    - from: "core/nightly_review.py"
      to: "core/training_checkin.py::planned_sessions_for"
      via: "import (acyclic: autonomous → training_checkin ← nightly_review)"
      pattern: "planned_sessions_for"
---

<objective>
Build the reconciled `training_reality` engine as a pure function in the neutral
`core/training_checkin.py` module, and move `_planned_workouts_for` there so the
autonomous path can consume planned intent without importing `nightly_review.py` (a locked
project decision). This plan delivers the reconciliation LOGIC + tests; Plan 07 wires it
into the autonomous gathers and prompt renders.

Purpose: MEM-04 — one training-reality window that reconciles four sources under the
D-01/D-02 precedence rule, with the SC-4 "done is never re-asked" determinism. Evidence
(Garmin/Hevy) is ground truth: a logged run satisfies the day's run slot even at a
different time, coached on quality separately.
Output: `planned_sessions_for(date_iso)` moved to training_checkin (nightly re-imports it);
`build_training_reality(dates)` pure reconciler with a stale-fact idempotency test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md

<interfaces>
From core/nightly_review.py (lines 130-162): `_planned_workouts_for(date_iso)` reads
`UserProfileStore.load()["weekly_split"]` keyed by weekday name (a recurring template, NOT
a per-date store), returns `{"weekday", "am": {label,modality,priority}, "pm": {...}}`.
From core/autonomous.py (lines 504-579): `_gather_training_evidence(now, project_id, database)`
returns `{"training_log_today", "strength_today", "runs_today"}` for a SINGLE date — reuse
across the window (loop dates or extend to a range).
From memory/firestore_db.py: `TrainingLogStore.get_by_date(date_iso)` / `.get_range(start,end)`
(lines 1172-1223) rows carry `type`, `planned`, `completed`, `skipped_reason`, `source`.
From core/training_checkin.py: `compute_recovery_concern` is already imported by BOTH
nightly_review and morning_briefing (line 147) — the acyclic direction to preserve.
LOCKED (STATE.md §Decisions): `core/autonomous.py` must NEVER import `core/nightly_review.py`.
Precedence (D-01): evidence (Hevy/Garmin) > self-report (TrainingLogStore.completed) >
calendar intent > weekly_split intent. Match (D-02): same date + modality only. Window: today-3d..tomorrow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move planned_sessions_for into training_checkin.py; nightly_review re-imports it</name>
  <read_first>core/nightly_review.py (lines 130-162 _planned_workouts_for, and every call site of it in that file); core/training_checkin.py (module structure + the compute_recovery_concern acyclic-import precedent, line 147); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 4 point 1)</read_first>
  <files>core/training_checkin.py, core/nightly_review.py</files>
  <action>Move `_planned_workouts_for(date_iso)` from `core/nightly_review.py` into `core/training_checkin.py` as public `planned_sessions_for(date_iso)` (same weekly_split-keyed-by-weekday logic, same return shape). In `core/nightly_review.py`, replace the local definition with `from core.training_checkin import planned_sessions_for` and update every internal call site. Do NOT change the import direction — `training_checkin` must not import `nightly_review`. Verify no new cyclic import (`autonomous → training_checkin ← nightly_review` must hold).</action>
  <verify>
    <automated>python -c "import core.autonomous, core.nightly_review, core.training_checkin; from core.training_checkin import planned_sessions_for; print('import graph OK')"</automated>
  </verify>
  <acceptance_criteria>`planned_sessions_for` importable from `core.training_checkin`; `core/nightly_review.py` re-imports it; importing all three modules together raises no ImportError (no cycle); nightly_review behavior unchanged (existing nightly tests still green).</acceptance_criteria>
  <done>planned_sessions_for relocated; acyclic import direction preserved; nightly_review re-imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_training_reality(dates) reconciler with the SC-4 idempotency invariant</name>
  <read_first>core/autonomous.py (lines 504-579 _gather_training_evidence evidence shape); memory/firestore_db.py (lines 1172-1223 TrainingLogStore row fields); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 4 reconciliation algorithm + Code Examples)</read_first>
  <files>core/training_checkin.py, tests/test_training_checkin.py</files>
  <behavior>
    - `build_training_reality(dates)` is a PURE function: callers inject already-fetched planned/calendar/evidence/self-report data (no store I/O inside it — mirrors compute_recovery_concern's pure shape). Returns per-date `{"planned": {...}, "calendar": [...], "evidence": {...}, "slots": {"am": <status>, "pm": <status>}}` where status ∈ {"done","missed","skipped:<reason>","planned"}.
    - Precedence per slot (D-01): evidence (Hevy/Garmin) > self-report (TrainingLogStore.completed) > calendar intent > weekly_split intent.
    - Matching (D-02): same date + modality match only (run satisfies run slot, lift satisfies lift slot) — NO pace/distance/duration comparison.
    - SC-4 invariant: once evidence OR a completed self-report exists for a slot's modality on that date, the status is terminal "done" — a re-read of the SAME date+slot after evidence lands returns "done", NOT a fresh "missed".
  </behavior>
  <action>Add `build_training_reality(dates: list[str], planned_by_date, calendar_by_date, evidence_by_date, self_report_by_date) -> dict[str, dict]` to `core/training_checkin.py`. Implement the D-01 precedence and D-02 same-day+modality matching from Pattern 4 as explicit deterministic rules (NOT a fuzzy confidence score — the locked rule guarantees SC-4 determinism). Keep the function pure; the actual store reads happen in the Plan 07 gather that calls this. Do NOT reach across into autonomous.py's private `_gather_training_evidence` from here — accept its output shape as an injected argument.</action>
  <verify>
    <automated>pytest tests/test_training_checkin.py -k training_reality -x</automated>
  </verify>
  <acceptance_criteria>Tests cover: (a) evidence-wins — a Garmin run with no matching training_log still marks the run slot "done"; (b) modality match — a run does NOT satisfy a lift slot; (c) time-of-day irrelevance — an evening run satisfies a morning-planned run slot; (d) STALE-FACT idempotency — the same date+slot re-read after evidence lands returns "done", never re-derived as "missed"; (e) empty evidence → "planned"/"missed" per whether the date is past.</acceptance_criteria>
  <done>build_training_reality is a pure, deterministic reconciler honoring D-01/D-02 with a passing SC-4 stale-fact idempotency test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| multiple training stores → reconciled view | conflicting sources merged into one status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Tampering (data integrity) | Non-deterministic reconciliation re-asking a done session | mitigate | Explicit D-01/D-02 precedence rules (not fuzzy scoring); terminal-status idempotency proven by the stale-fact test (SC-4) |
| T-32-08 | Repudiation | Import cycle silently reintroducing nightly→autonomous coupling | mitigate | planned_sessions_for lives in neutral training_checkin; import-graph smoke assertion in Task 1 |
</threat_model>

<verification>
- `pytest tests/test_training_checkin.py -k training_reality -x` green.
- `python -c "import core.autonomous, core.nightly_review, core.training_checkin"` no ImportError.
- `grep -n "import nightly_review" core/autonomous.py` returns nothing (invariant holds).
</verification>

<success_criteria>
A pure, deterministic `build_training_reality` reconciles the four sources under D-01/D-02
with terminal "done" idempotency (SC-4), and `planned_sessions_for` lives in the neutral
module so the autonomous path can consume planned intent without importing nightly_review.
</success_criteria>

<output>
Create `.planning/phases/32-unified-situation-ambient-memory/32-04-SUMMARY.md` when done.
</output>
