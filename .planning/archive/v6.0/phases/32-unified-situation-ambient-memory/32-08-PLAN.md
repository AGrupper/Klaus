---
phase: 32-unified-situation-ambient-memory
plan: 08
type: execute
wave: 3
depends_on: ["32-04", "32-07"]
files_modified:
  - core/autonomous.py
  - core/nightly_review.py
  - core/morning_briefing.py
  - prompts/nightly_review.md
  - tests/test_autonomous.py
autonomous: true
requirements: [MEM-07, MEM-05]
user_setup: []

must_haves:
  truths:
    - "current_location is derived from calendar travel events + standing directives; default is Tel Aviv silently (no chatter on a normal day)"
    - "A positive travel signal (calendar location or directive place-name) overrides the home default"
    - "When signals conflict or a trip window is unclear, Klaus asks via Phase 31's nightly-ask pattern rather than guessing (no new prompt surface)"
    - "Weather and travel-time gathers consume the derived current_location — no more Tel Aviv forecast delivered to Paris"
    - "The location gather is context-only in _is_empty_signals — deriving a location never wakes the free tier"
  artifacts:
    - path: "core/autonomous.py"
      provides: "_gather_location + current_location derivation heuristic, context-only"
      contains: "_gather_location"
    - path: "core/nightly_review.py"
      provides: "fetch_weather consumes derived current_location (not hardcoded Tel Aviv)"
      contains: "fetch_weather"
    - path: "core/morning_briefing.py"
      provides: "fetch_weather consumes derived current_location"
      contains: "fetch_weather"
    - path: "prompts/nightly_review.md"
      provides: "location_ask payload doc + weaving instruction so the compose LLM phrases the 'still in <city>, Sir?' ask"
      contains: "location_ask"
  key_links:
    - from: "core/nightly_review.py"
      to: "mcp_tools/weather_tool.py::fetch_weather"
      via: "passes derived current_location string instead of the 'Tel Aviv' literal"
      pattern: "fetch_weather\\("
    - from: "core/autonomous.py::_gather_location"
      to: "StandingDirectiveStore + calendar gather"
      via: "conservative heuristic reading only already-scoped directives + calendar location field"
      pattern: "_gather_location"
---

<objective>
Derive `current_location` (MEM-07) from calendar travel events + standing directives so
weather and travel-time stop hardcoding "Tel Aviv" — fixing the felt bug of Paris getting a
Tel Aviv forecast — while staying silent on the 99% home case and ASKING (via Phase 31's
nightly-ask) only when genuinely ambiguous.

Purpose: the correct location answer over a quiet one for conflict cases, but no location
chatter on a normal day (D-06). The heuristic reads only already-Amit-scoped sources
(StandingDirectiveStore, the existing calendar gather), inheriting the existing
directive-capture security scoping — no new spoofing surface.
Output: `_gather_location` + a conservative derivation heuristic (context-only), the
nightly-ask on ambiguity, and the weather/routes call-site repoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md
@.planning/phases/32-unified-situation-ambient-memory/32-07-SUMMARY.md
@.planning/phases/31-standing-directives/31-CONTEXT.md

<interfaces>
From core/autonomous.py: `_gather_calendar` (lines 240-255) already returns each event's
`location` field; `_gather_standing_directives` (338-355) returns active directives; jobs
dict (615-647); `_is_empty_signals` exclusion pattern (192-226). Clone the sentinel-on-failure shape.
From memory/firestore_db.py (lines 1844-1853): StandingDirectiveStore exposes directive
`text`/`condition_text` as free text (no structured location field).
Hardcoded weather call sites (VERIFIED): `core/nightly_review.py:217` `fetch_weather("Tel Aviv")`,
`core/morning_briefing.py:302` `fetch_weather("Tel Aviv")`. `mcp_tools/routes_tool.py::get_travel_time`
already takes plain string origin/dest (no default) — only its caller needs the derived value.
Security (RESEARCH §Security Domain): the heuristic reads ONLY StandingDirectiveStore
(already scoped to live Amit turns per smart_agent.md:373) + calendar — inherits existing protection.
Heuristic (research [ASSUMED], D-06): home default "Tel Aviv" silent; calendar location not
containing "Tel Aviv" is a travel candidate; directive place-name regex ("while I'm in X",
"back from X"); conflict OR unclear trip-end → nightly-ask (reuse Phase 31 surface), never guess.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _gather_location + current_location derivation heuristic (context-only)</name>
  <read_first>core/autonomous.py (lines 240-255 _gather_calendar location field, 338-355 gather clone shape, 615-647 jobs, 175-227 _is_empty_signals exclusions); memory/firestore_db.py (lines 1844-1853 directive text/condition_text); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 7, §Assumptions A2/A3)</read_first>
  <files>core/autonomous.py, tests/test_autonomous.py</files>
  <behavior>
    - `_gather_location(situation)` derives current_location: (1) default "Tel Aviv" silently; (2) calendar signal — a today event with a non-empty `location` not containing "Tel Aviv" is the travel candidate; (3) directive signal — a conservative place-name regex over active directive text/condition_text; (4) ambiguity — calendar/directive conflict OR unclear trip-end → return an "ask" sentinel (no guess).
    - Reuses the already-gathered `calendar` + `standing_directives` situation values (no new Calendar/Firestore call). Sentinel/never-raise.
    - Registered in jobs dict with a CONTEXT-only comment; NOT referenced in `_is_empty_signals` (completes the Plan 07 location placeholder assertion).
  </behavior>
  <action>Add a module-level PURE helper `derive_current_location(calendar_events, active_directives) -> dict` in `core/autonomous.py` implementing the Pattern 7 heuristic (under-detect rather than over-detect travel, per D-06); it returns a small dict distinguishing `{"location": <str>}` (resolved) vs `{"ambiguous": True, "candidate": <str|None>}` (ask). Add `_gather_location(situation)` that wraps the helper over the already-gathered `calendar` + `standing_directives` situation values (no new API call), cloning the sentinel-on-failure shape. Register in the jobs dict with a context-only comment and add the location exclusion line to `_is_empty_signals`'s comment block. The helper MUST be importable by `core/nightly_review.py` / `core/morning_briefing.py` (Task 2) — confirm no import cycle (autonomous must never import nightly_review/morning_briefing; if a cycle would arise, relocate the pure helper to a neutral module and import it from all three). Add the per-gather context-only assertion for `location` in `tests/test_autonomous.py` (completing the four-gather MEM-05 coverage).</action>
  <verify>
    <automated>pytest tests/test_autonomous.py -k current_location -x</automated>
  </verify>
  <acceptance_criteria>Tests (3+ fixtures) assert: (a) no travel signal → "Tel Aviv" silently; (b) a calendar/directive travel signal → that location overrides; (c) conflicting/unclear signals → the "ask" sentinel, NEVER a guessed location; (d) `_is_empty_signals({..., "location": <non-home value>})` returns True (context-only).</acceptance_criteria>
  <done>current_location derived conservatively, context-only, with a never-guess ambiguity path and full four-gather MEM-05 coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint weather to derived current_location + wire the D-06 location-ambiguity ask through the nightly compose payload+prompt</name>
  <read_first>core/nightly_review.py (`_gather_tomorrow` lines 165-228 incl. the `data["weather"]=fetch_weather("Tel Aviv")` at 217 and the `calendar`/`standing_directives` it already gathers; `_compose_nightly` payload assembly lines 239-281 that passes the whole `tomorrow` dict as `payload["tomorrow"]`); prompts/nightly_review.md (§"What you're given" lines 13-34 incl. the hardcoded "`weather` (Tel Aviv)" at line 19; §"Directive housekeeping" lines 64-82 as the LLM-weaving analog — THIS is the real Phase-31 ask mechanism, a payload key phrased by the compose LLM, NOT a code function named "nightly-ask"); core/morning_briefing.py (line 302 fetch_weather); mcp_tools/routes_tool.py (get_travel_time signature line 23); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Pattern 7 call sites, §D-06)</read_first>
  <files>core/nightly_review.py, core/morning_briefing.py, prompts/nightly_review.md, tests/test_autonomous.py</files>
  <behavior>
    - In `_gather_tomorrow`, call `derive_current_location(tomorrow.calendar, tomorrow.standing_directives)` (Task 1 helper). On a RESOLVED location, set `data["weather"] = fetch_weather(<derived location>)` (defaults to "Tel Aviv" at home → byte-identical home behavior) and set no ask field. On an AMBIGUOUS result, SUPPRESS the location-dependent weather (omit or blank it) and set `data["location_ask"] = {"candidate": <city>}` on the `tomorrow` dict so it rides into `payload["tomorrow"]`.
    - `prompts/nightly_review.md`: (a) update the line-19 "`weather` (Tel Aviv)" description to say the weather reflects Klaus's derived `current_location` (may be elsewhere when travelling); (b) document the new `tomorrow.location_ask` field in §"What you're given"; (c) add a weaving instruction (analogous to §"Directive housekeeping", NO generic "surface any anomaly" clause exists so it MUST be explicit) telling the model: when `location_ask` is present, ask "still in <candidate>, Sir?" before serving any location-dependent content, and do not state a forecast for a possibly-wrong city.
    - `core/morning_briefing.py:302` also consumes the derived current_location (resolved → that city; ambiguous → suppress location-dependent output; the ASK itself is nightly-only per D-06). Any routes/travel-time caller passes the derived string; `get_travel_time` signature unchanged.
  </behavior>
  <action>Wire `derive_current_location` into `_gather_tomorrow`: resolved → `fetch_weather(<derived>)`; ambiguous → suppress weather + set `data["location_ask"]={"candidate":<city>}` (rides through the EXISTING `payload["tomorrow"]` passthrough — do NOT add a new payload plumb in `_compose_nightly`, the whole `tomorrow` dict already flows). Edit `prompts/nightly_review.md`: fix the line-19 weather description to the derived-location wording, document `tomorrow.location_ask` in "What you're given", and add an explicit weaving bullet so the compose LLM phrases "still in <candidate>, Sir?" and withholds a possibly-wrong forecast. Repoint `core/morning_briefing.py:302` to the derived location (suppress on ambiguous). Update any routes/travel caller to pass the derived value; routes_tool signature unchanged. Do NOT invent a code "nightly-ask" surface — the Phase-31 mechanism IS the payload-key-phrased-by-compose-LLM pattern.</action>
  <verify>
    <automated>pytest tests/test_autonomous.py -k current_location -x</automated>
  </verify>
  <acceptance_criteria>Tests assert: (a) a resolved travel location flows into `fetch_weather` (not the hardcoded literal); (b) the home case still yields "Tel Aviv" (no behavior change, no chatter, no `location_ask`); (c) an AMBIGUOUS derivation sets `tomorrow.location_ask={"candidate":<city>}` AND suppresses the location-dependent weather. A `grep` asserts `prompts/nightly_review.md` documents the `tomorrow.location_ask` field AND contains the "still in <city>, Sir?" weaving instruction — proving the ask can actually fire and be phrased. Manual (VALIDATION MEM-07 row): seed a conflicting travel signal, run the nightly, confirm the "still in <city>, Sir?" ask appears in the sent message.</acceptance_criteria>
  <done>Weather/travel consume the derived location; home path unchanged; the ambiguity ASK is fully wired end-to-end (payload field + compose-prompt phrasing) — both halves of D-06 ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| directive/calendar text → location signal | free text drives a location decision |
| location gather → free-tier cost gate | deriving a location must not wake the tick |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-16 | Spoofing | Non-Amit text ("while I'm in Paris" from a Notion/Gmail body) misread as a location signal | mitigate | Heuristic reads ONLY StandingDirectiveStore (already scoped to live Amit turns, smart_agent.md:373) + calendar — inherits existing capture scoping; no raw tool output parsed |
| T-32-17 | Elevation of Privilege (cost) | location gather reachable from _is_empty_signals | mitigate | Context-only exclusion comment + per-gather assertion (completes MEM-05 four-gather coverage) |
| T-32-18 | Tampering (wrong output) | Guessing a location and serving a wrong-city forecast | mitigate | Conservative under-detect heuristic; conflict/unclear → nightly-ask, never a guess (D-06) |
</threat_model>

<verification>
- `pytest tests/test_autonomous.py -k current_location -x` green.
- `grep -n "fetch_weather" core/nightly_review.py core/morning_briefing.py` shows the derived value, not `"Tel Aviv"` literals.
- `grep -nE "location" core/autonomous.py` shows jobs registration + exclusion comment, NOT an `if situation.get("location")` trigger.
- Per-wave: run every `tests/test_*.py` per-file; baseline ≥1775 holds.
</verification>

<success_criteria>
`current_location` is derived from calendar + directives, defaults home silently, overrides
on real travel, asks (never guesses) on ambiguity via the existing nightly surface, stays
context-only, and drives weather/travel-time — closing the Paris-gets-Tel-Aviv-weather bug.
</success_criteria>

<output>
Create `.planning/phases/32-unified-situation-ambient-memory/32-08-SUMMARY.md` when done.
</output>
