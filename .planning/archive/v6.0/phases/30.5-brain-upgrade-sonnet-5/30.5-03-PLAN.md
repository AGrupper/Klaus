---
phase: 30.5-brain-upgrade-sonnet-5
plan: 03
type: execute
wave: 2
depends_on: ["30.5-02"]
files_modified:
  - core/pricing.py
  - core/llm_client.py
  - requirements.txt
  - tests/test_pricing.py
  - tests/test_llm_client.py
autonomous: true
requirements: [BRAIN-02, BRAIN-05]
user_setup: []

must_haves:
  truths:
    - "The Anthropic system prompt is sent as a cache_control content block with 1h TTL"
    - "cache-read/cache-write tokens are extracted from the Anthropic response and threaded into cost + LLMUsage"
    - "compute_cost prices cache reads at 0.1x input and 1h cache writes at 2x input, correctly for claude-sonnet-5"
    - "No temperature/top_p/top_k/manual-thinking is ever sent on the Anthropic path; max_tokens default has thinking headroom"
    - "D-09: Adaptive thinking is the default on the Anthropic path — no effort-level tuning, no per-path thinking config"
    - "D-10: LLM_TIMEOUT_SECONDS (120s) remains the only latency backstop — no thinking-motivated latency constraints"
    - "D-11: Anthropic-path default max_tokens raised to ~16000; the tick-brain 2048 Groq admission ceiling is untouched"
  artifacts:
    - path: core/pricing.py
      provides: "claude-sonnet-5 pricing (dated intro), cache multipliers, extended compute_cost signature"
      contains: "claude-sonnet-5"
    - path: core/llm_client.py
      provides: "cache_control system block + cache-token usage extraction + metering thread-through + raised MAX_TOKENS"
      contains: "cache_control"
    - path: requirements.txt
      provides: "anthropic floor bump for Sonnet-5 typed support"
      contains: "anthropic>=0.99"
  key_links:
    - from: "core/llm_client.py _AnthropicBackend.chat"
      to: "compute_cost cache args"
      via: "usage dict cache_read_tokens/cache_write_tokens"
      pattern: "cache_read_tokens"
    - from: "core/llm_client.py metering block"
      to: "LLMUsageStore.record cache kwargs"
      via: "record(... cache_read_tokens=..., cache_write_tokens=...)"
      pattern: "cache_write_tokens="
---

<objective>
Activate Anthropic prompt caching (1h TTL) on the smart-brain path, extract the resulting
cache-read/cache-write token counts, price them correctly (0.1x reads, 2x 1h-TTL writes) for
`claude-sonnet-5` with dated intro pricing, thread the cache tokens through the cost-metering
block into `LLMUsageStore.record`, audit the Anthropic path so no sampling/manual-thinking
params are ever sent (Sonnet-5 hard-400s on them), and raise the module `MAX_TOKENS` default so
default-on adaptive thinking never truncates the reply. Ships cache_control + extraction + pricing
in ONE change (Pitfall 2 — never split across deploys).

Purpose: Truthful cache-aware metering (BRAIN-02) + Sonnet-5 request compatibility (BRAIN-05).
Output: Priced cache tokens, cached system block, param-safe Anthropic backend, raised max_tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-02-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 02 (memory/firestore_db.py) — consume directly, no exploration -->
LLMUsageStore.record(model, purpose, in_tokens, out_tokens, cost,
                     cache_read_tokens=0, cache_write_tokens=0)  # cache kwargs now accepted
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Price claude-sonnet-5 + cache multipliers, extend compute_cost, bump anthropic floor</name>
  <files>core/pricing.py, requirements.txt, tests/test_pricing.py</files>
  <behavior>
    - compute_cost("claude-sonnet-5", 1_000_000, 1_000_000) == input_rate + output_rate
    - compute_cost(..., cache_read_tokens=1_000_000) adds input_rate * 0.1
    - compute_cost(..., cache_write_tokens=1_000_000) adds input_rate * 2.0 (1h TTL)
    - Unknown model still returns 0.0 and never raises
  </behavior>
  <read_first>
    - core/pricing.py (full file; MODEL_PRICING at line 15; compute_cost at 31; _logged_unknown dedup discipline)
    - tests/test_pricing.py (full file; test_haiku_known_price template; the entry-count assertion that says "four" but asserts the real count — bump it)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"core/pricing.py::compute_cost"
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md §"Standard Stack" (pricing $3/$15 standard, $2/$10 intro through 2026-08-31; cache 0.1x read / 2x 1h write) and §"Assumptions Log" A1/A2 (re-confirm live pricing before implementing)
  </read_first>
  <action>
    Re-confirm current claude-sonnet-5 pricing against platform.claude.com before coding (A1/A2).
    Add a `claude-sonnet-5` row to `MODEL_PRICING`. Because intro pricing ($2/$10) applies only
    through 2026-08-31 and standard ($3/$15) after, implement a dated mechanism rather than a
    static number (Claude's Discretion on shape — e.g. compute the active input/output rate from
    a small dated-tier structure, or a helper that picks intro vs standard by today's date).
    Extend `compute_cost` with `cache_read_tokens: int = 0, cache_write_tokens: int = 0` kwargs
    (defaults 0 keep all existing callers source-compatible). Add cache-read cost as
    `(cache_read_tokens / 1_000_000) * pricing_input * 0.1` and cache-write as
    `(cache_write_tokens / 1_000_000) * pricing_input * 2.0` (1h-TTL write multiplier). Preserve
    the "unknown model returns 0.0, logs once via _logged_unknown, never raises" invariant.
    Bump requirements.txt `anthropic>=0.40` to `anthropic>=0.99,<1.0` with a comment noting
    Sonnet-5 typed adaptive-thinking support (no new package; existing official dependency —
    audited/approved in RESEARCH §Package Legitimacy Audit).
    Add tests mirroring the explicit-arithmetic-assertion style: a sonnet-5 base-price test, a
    cache-read-price test, a cache-write-price test; bump the model-entry-count assertion.
  </action>
  <verify>
    <automated>python -m pytest tests/test_pricing.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - core/pricing.py MODEL_PRICING contains a `claude-sonnet-5` entry
    - core/pricing.py has a dated intro-vs-standard pricing mechanism (a 2026-08-31 boundary is referenced in code or comment)
    - `compute_cost` signature contains `cache_read_tokens` and `cache_write_tokens`
    - tests/test_pricing.py has explicit-arithmetic tests for sonnet-5 base, cache-read (0.1x), and cache-write (2.0x) pricing
    - requirements.txt contains `anthropic>=0.99,<1.0`
    - `python -m pytest tests/test_pricing.py -x -q` exits 0
  </acceptance_criteria>
  <done>claude-sonnet-5 priced with dated intro handling; cache multipliers correct; compute_cost extended source-compatibly; anthropic floor bumped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anthropic cache_control + cache-token extraction + metering + param audit + max_tokens</name>
  <files>core/llm_client.py, tests/test_llm_client.py</files>
  <behavior>
    - When system is non-empty, the Anthropic request sends system as a content-block list carrying cache_control {"type":"ephemeral","ttl":"1h"}
    - The returned usage dict includes cache_read_tokens and cache_write_tokens (0 when absent)
    - The LLMClient.chat metering block passes cache tokens into compute_cost AND LLMUsageStore.record
    - No temperature/top_p/top_k/thinking is present in the Anthropic kwargs unless explicitly passed (and none is on the smart path)
    - MAX_TOKENS default gives adaptive-thinking headroom (raised from 4096)
  </behavior>
  <read_first>
    - core/llm_client.py (_AnthropicBackend.chat 183-248: kwargs build 203-214, system wiring 211, error handling 216-225, usage extraction 240-248; LLMClient.chat metering block 128-148; MAX_TOKENS at line 36; Gemini usage_meta getattr style ~344-346)
    - tests/test_llm_client.py (test_anthropic_backend_sets_timeout_and_capped_retries at ~line 343 — the analog test)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"core/llm_client.py::_AnthropicBackend.chat"
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md §"Pattern 1", §"Pattern 2", §"Common Pitfalls" 2 & 4, §"Anti-Patterns to Avoid"
  </read_first>
  <action>
    In `_AnthropicBackend.chat`, replace the plain `kwargs["system"] = system` (line 211) with a
    single content-block list: one text block containing `system` plus
    `cache_control={"type": "ephemeral", "ttl": "1h"}` — only when `system` is non-empty. Do NOT
    add temperature/top_p/top_k; keep the existing `if temperature is not None` guard as the sole
    param-gate (no smart-path caller passes temperature — verify it stays that way). Extend the
    returned `usage` dict (lines 240-248) with `cache_read_tokens` and `cache_write_tokens` using
    the defensive `getattr(response.usage, "cache_read_input_tokens", 0) or 0` /
    `getattr(response.usage, "cache_creation_input_tokens", 0) or 0` style already used for the
    Gemini backend. In the `LLMClient.chat` cost-metering block (128-148), read the two new usage
    fields (default 0) and pass them into BOTH `compute_cost(...)` and `LLMUsageStore.record(...)`
    as `cache_read_tokens=`/`cache_write_tokens=`. Keep the entire metering block inside its bare
    `try/except Exception: logger.debug(...)` — metering must never raise. Raise the module-level
    `MAX_TOKENS` default from 4096 to a generous ceiling (~16000) per D-11 so default-on adaptive
    thinking plus the visible reply never hit stop_reason "max_tokens"; leave every deliberately
    small per-call ceiling (e.g. tick-brain's 2048, passed explicitly) untouched.
    Add tests: assert the Anthropic request's `system` is a list with a cache_control block; assert
    cache tokens are extracted from a mocked usage object; assert no temperature/top_p/top_k/thinking
    key is present in the built kwargs on the smart path; assert the module MAX_TOKENS is >= 16000.
  </action>
  <verify>
    <automated>python -m pytest tests/test_llm_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - core/llm_client.py sends `system` as a list containing `cache_control` with `"ttl": "1h"`
    - The Anthropic usage return dict contains `cache_read_tokens` and `cache_write_tokens`
    - The LLMClient.chat metering block passes `cache_read_tokens=`/`cache_write_tokens=` to both compute_cost and LLMUsageStore.record
    - `grep -n "top_p\|top_k" core/llm_client.py` returns no Anthropic-path additions; temperature remains guarded by `if temperature is not None`
    - MAX_TOKENS is >= 16000
    - A test asserts no temperature/top_p/top_k/thinking is sent on the Anthropic smart path
    - `python -m pytest tests/test_llm_client.py -x -q` exits 0
  </acceptance_criteria>
  <done>Anthropic path caches the system prefix (1h TTL), extracts+meters cache tokens, sends no forbidden params, and has thinking-safe max_tokens headroom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Anthropic API | Request shape (params, cache_control) must match Sonnet-5 or hard-400 |
| Anthropic response → cost meter | usage fields drive billing-visibility; misread = wrong dashboard |
| requirements.txt → build | Dependency version pin (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30.5-07 | Information Disclosure / Integrity | Cache-read tokens mispriced as full input | mitigate | 0.1x read multiplier in compute_cost; explicit-arithmetic pricing tests; ship extraction with cache_control (Pitfall 2) |
| T-30.5-08 | Denial of Service | Sonnet-5 400 on temperature/top_p/top_k/manual-thinking | mitigate | Param audit; test asserts none sent on the Anthropic smart path; documented invariant |
| T-30.5-09 | Denial of Service | max_tokens truncation under default adaptive thinking | mitigate | Raise MAX_TOKENS to ~16K (D-11); assert >=16000 |
| T-30.5-SC | Tampering | anthropic floor bump (supply chain) | mitigate | Existing official dependency; RESEARCH §Package Legitimacy Audit disposition = Approved (floor bump, not new install) — no blocking checkpoint required |
</threat_model>

<verification>
- `python -m pytest tests/test_pricing.py tests/test_llm_client.py -x -q` green
- `grep -n "cache_control\|ttl.*1h" core/llm_client.py` present
- `grep -n "claude-sonnet-5" core/pricing.py` present
- requirements.txt shows `anthropic>=0.99,<1.0`
</verification>

<success_criteria>
BRAIN-02 (caching + truthful cache-token metering) and BRAIN-05 (no forbidden params, thinking-safe max_tokens) satisfied at the code level, ready for the Plan 06 live flip + console comparison.
</success_criteria>

<output>
Create `.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-03-SUMMARY.md` when done.
</output>
