---
phase: 30.5-brain-upgrade-sonnet-5
plan: 06
type: execute
wave: 4
depends_on: ["30.5-01", "30.5-03", "30.5-04", "30.5-05"]
files_modified:
  - core/main.py
  - core/morning_briefing.py
  - core/nightly_review.py
  - core/self_manifest.py
  - .github/workflows/deploy.yml
  - tests/test_main.py
  - tests/test_morning_briefing.py
  - tests/test_nightly_review.py
autonomous: false
requirements: [BRAIN-01]
user_setup:
  - service: anthropic
    why: "claude-sonnet-5 must be enabled on the Anthropic account backing klaus-anthropic-key"
    env_vars:
      - name: SMART_AGENT_API_KEY
        source: "Secret Manager klaus-anthropic-key (already provisions claude-haiku-4-5; confirm Sonnet-5 access before flip)"

must_haves:
  truths:
    - "Every conversation turn and every paid proactive compose runs on claude-sonnet-5"
    - "D-16: A forced Anthropic outage falls back to gemini-3.5-flash, then claude-haiku-4-5 as tertiary only — no cheap-task Haiku routing this phase"
    - "Every paid proactive compose (morning briefing, nightly review) falls back to gemini-3.5-flash on Anthropic failure BEFORE dropping to the plain-text template"
    - "D-12: When the Gemini fallback serves a turn, Klaus appends a subtle Klaus-voiced backup-reasoning disclosure line"
    - "D-13: The tick-brain fallback decoupling (Plan 01) is already deployed and verified before this flip"
    - "D-14: Post-flip validation is the live production checklist — Telegram turn, hub turn, tool-heavy coaching question, forced Anthropic failure, next-day LLMUsage-vs-console compare"
    - "D-15: Rollback stays an env-var flip back to Gemini at Amit's judgment — no predefined auto-rollback thresholds"
  artifacts:
    - path: core/main.py
      provides: "3-tier fallback chain (Sonnet -> Gemini -> Haiku) + D-12 fallback disclosure on the Gemini branch"
      contains: "smart_fallback_2"
    - path: core/morning_briefing.py
      provides: "2-tier Sonnet->Gemini fallback in _compose_briefing before _plain_text_fallback"
      contains: "SMART_AGENT_FALLBACK_BACKEND"
    - path: core/nightly_review.py
      provides: "2-tier Sonnet->Gemini fallback in _compose_nightly before _plain_text_fallback"
      contains: "SMART_AGENT_FALLBACK_BACKEND"
    - path: .github/workflows/deploy.yml
      provides: "SMART_AGENT_* flip to Sonnet, SMART_AGENT_FALLBACK_* -> Gemini, tertiary -> Haiku, KLAUS_DAILY_COST_ALERT=5"
      contains: "SMART_AGENT_MODEL=claude-sonnet-5"
    - path: core/self_manifest.py
      provides: "Model Map default strings updated to post-flip truth"
  key_links:
    - from: "core/main.py _run_smart_loop"
      to: "smart_agent_tertiary.chat"
      via: "nested except after Gemini fallback fails"
      pattern: "smart_fallback_2"
    - from: "core/morning_briefing.py _compose_briefing"
      to: "SMART_AGENT_FALLBACK_* LLMClient"
      via: "second try/except before _plain_text_fallback"
      pattern: "SMART_AGENT_FALLBACK_MODEL"
    - from: ".github/workflows/deploy.yml"
      to: "klaus-anthropic-key / klaus-gemini-key"
      via: "--update-secrets remapping"
      pattern: "SMART_AGENT_API_KEY=klaus-anthropic-key"
---

<objective>
Flip Klaus's smart brain to `claude-sonnet-5` (BRAIN-01), the culminating change of the phase.
Extend `AgentOrchestrator` from a 2-tier to a 3-tier fallback chain (Sonnet -> Gemini -> Haiku,
D-16), append the D-12 Klaus-voiced fallback-disclosure line when the Gemini tier serves a turn,
extend the SAME `SMART_AGENT_FALLBACK_*` Gemini fallback (already present in `core/reflection.py`)
to the two remaining paid scheduled composes — `core/morning_briefing.py` and
`core/nightly_review.py` — so BRAIN-01's "every paid proactive compose falls back cleanly to
gemini-3.5-flash" holds, remap `deploy.yml` env + secrets (SMART_AGENT_* -> Sonnet/klaus-anthropic-key;
SMART_AGENT_FALLBACK_* -> Gemini/klaus-gemini-key; a new tertiary set -> Haiku/klaus-anthropic-key;
add KLAUS_DAILY_COST_ALERT=5), update the `self_manifest.py` Model Map default strings to post-flip
truth, and run the D-14 live validation checklist. This ships ONLY after Plan 01's decoupling is
deployed and verified (hard-ordering per D-13), and after metering (03), slimming (04), and the
tripwire (05) are in place.

Purpose: Sonnet-5 becomes the brain, safely, with a self-aware fallback chain on both conversation
turns and every paid compose.
Output: 3-tier chain + disclosure, compose-path Gemini fallback, deploy.yml flip, updated manifest
defaults, live checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-01-SUMMARY.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-03-SUMMARY.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-04-SUMMARY.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AgentOrchestrator to a 3-tier fallback chain + D-12 disclosure</name>
  <files>core/main.py, tests/test_main.py</files>
  <behavior>
    - On smart_agent (Sonnet) LLMError, the Gemini fallback is tried (purpose="smart_fallback"); on its success the response text gains a subtle Klaus-voiced backup-reasoning disclosure line
    - On the Gemini fallback also raising LLMError, the tertiary Haiku client is tried (purpose="smart_fallback_2") before returning CONNECTIVITY_ERROR_TEXT
    - The tertiary client is None-safe (absent tertiary env => skip straight to CONNECTIVITY_ERROR_TEXT)
    - increment_fallback_counter() stays wrapped in its own silent try/except
  </behavior>
  <read_first>
    - core/main.py (AgentOrchestrator.__init__ 183-254: existing smart_agent + smart_agent_fallback optional-client construction 186-212; _run_smart_loop except-block chain 589-624; CONNECTIVITY_ERROR_TEXT; where response text is finalized for the D-12 hook)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md section "core/main.py::AgentOrchestrator" (3-tier extension + D-12 hook placement) and "Optional-client-from-env, 3x-repeated shape"
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md D-12 (fallback disclosure, Klaus voice, also logged in LLMUsage), D-16 (tertiary Haiku, config-level)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md "Pattern 4" + "Open Questions" #1 (env-var naming; heartbeat fallback-rate label becomes stale)
  </read_first>
  <action>
    In `AgentOrchestrator.__init__`, add a third optional client `self.smart_agent_tertiary`
    using the IDENTICAL `if backend and model and key: LLMClient(...) else: None` shape already
    used for `smart_agent_fallback`, reading a new tertiary env set (name your choice per Open
    Question 1 - e.g. `SMART_AGENT_TERTIARY_BACKEND/MODEL/API_KEY/BASE_URL`; whatever you pick
    must match the deploy.yml wiring in Task 2). In `_run_smart_loop`, nest one more
    `try/except LLMError` inside the existing `except LLMError as fallback_exc` branch: attempt
    `self.smart_agent_tertiary.chat(..., purpose="smart_fallback_2")` before returning
    `CONNECTIVITY_ERROR_TEXT`, and if tertiary is None, return `CONNECTIVITY_ERROR_TEXT` as today.
    Preserve the `increment_fallback_counter()` call in its own silent try/except and the
    purpose-label layering (`smart` -> `smart_fallback` -> `smart_fallback_2`). Add the D-12
    fallback-disclosure hook at the point the Gemini fallback (`smart_fallback`) succeeds: append
    a subtle, Klaus-voiced one-liner to the response text conveying "running on backup reasoning
    at the moment, Sir" in his register (not that literal string) - ONLY on the Gemini branch, not
    the tertiary Haiku branch. Keep the existing behavior when no fallback is configured.
    Add tests to tests/test_main.py: Sonnet error -> Gemini used + disclosure line appended;
    Sonnet+Gemini error -> Haiku (smart_fallback_2) used; all three error -> CONNECTIVITY_ERROR_TEXT;
    tertiary None -> CONNECTIVITY_ERROR_TEXT after Gemini fails.
  </action>
  <verify>
    <automated>python -m pytest tests/test_main.py -x -q -k "fallback or tertiary or disclosure"</automated>
  </verify>
  <acceptance_criteria>
    - core/main.py constructs a third optional client (smart_agent_tertiary) with the same env-optional None-safe shape
    - _run_smart_loop attempts `purpose="smart_fallback_2"` before returning CONNECTIVITY_ERROR_TEXT
    - The Gemini-fallback success path appends a Klaus-voiced disclosure line to the response text; the Haiku branch does not
    - A test asserts Sonnet->Gemini->Haiku ordering and the disclosure-on-Gemini behavior
    - `python -m pytest tests/test_main.py -x -q -k "fallback or tertiary or disclosure"` exits 0
  </acceptance_criteria>
  <done>3-tier Sonnet->Gemini->Haiku chain with the D-12 disclosure on the Gemini tier; None-safe tertiary; purpose labels layered for LLMUsage visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the SMART_AGENT_FALLBACK_* Gemini fallback to the paid morning/nightly composes</name>
  <files>core/morning_briefing.py, core/nightly_review.py, tests/test_morning_briefing.py, tests/test_nightly_review.py</files>
  <behavior>
    - _compose_briefing: on the SMART_AGENT_* brain call raising or returning empty, a SMART_AGENT_FALLBACK_* Gemini compose is attempted (same 2-tier shape as core/reflection.py) BEFORE dropping to _plain_text_fallback
    - _compose_nightly: identical 2-tier brain -> Gemini-fallback -> plain-text ordering, preserving purpose="nightly_review"
    - Only when BOTH the brain and the Gemini fallback fail/return empty does the compose fall back to the deterministic _plain_text_fallback template
    - A Gemini-fallback failure never raises out of the cron (silent try/except, exactly like core/reflection.py)
  </behavior>
  <read_first>
    - core/reflection.py 305-349 (the canonical 2-tier SMART_AGENT_* -> SMART_AGENT_FALLBACK_* pattern to replicate in shape: brain try/except, then fallback try/except, purpose label preserved, silent-on-failure)
    - core/morning_briefing.py 471-528 (_compose_briefing; the single brain LLMClient call at 511-528 that currently drops STRAIGHT to _plain_text_fallback on failure — the gap)
    - core/nightly_review.py 223-269 (_compose_nightly; same missing-fallback compose path at 251-269)
    - tests/test_reflection.py 469-475, 666-691 (SMART_AGENT_FALLBACK_* env fixture + brain+fallback-fail -> template assertion; mirror this for the two composes)
  </read_first>
  <action>
    In core/morning_briefing.py `_compose_briefing`, after the existing brain `client.chat(...)`
    try/except (511-528) and BEFORE the final `return _plain_text_fallback(...)`, insert a second
    `try/except` that builds an LLMClient from `SMART_AGENT_FALLBACK_BACKEND`/`SMART_AGENT_FALLBACK_MODEL`/
    `SMART_AGENT_FALLBACK_API_KEY` (and `SMART_AGENT_FALLBACK_BASE_URL` if the reflection pattern
    reads it) and calls `.chat(...)` with the SAME `system=system_prompt` and `user_message` the
    brain call used, plus the same purpose; return its stripped text when non-empty; on any
    Exception log a warning and fall through to `_plain_text_fallback` exactly as today. Replicate
    the identical structure in core/nightly_review.py `_compose_nightly` (251-269), preserving
    `purpose="nightly_review"`. Do NOT add a tertiary Haiku tier in the composes — they only need
    Sonnet->Gemini per BRAIN-01's success criterion (the tertiary Haiku hop is a conversation-turn
    nicety owned by Task 1). Match core/reflection.py's env-var names and its silent try/except
    discipline so a fallback failure can never crash the scheduled cron. Add tests to
    tests/test_morning_briefing.py and tests/test_nightly_review.py: brain raises -> Gemini
    fallback (SMART_AGENT_FALLBACK_*) composes and its text is returned (NOT the plain template);
    brain + fallback both raise -> deterministic _plain_text_fallback returned. Reuse the
    SMART_AGENT_FALLBACK_* env-fixture shape from tests/test_reflection.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_morning_briefing.py tests/test_nightly_review.py -x -q -k "fallback or compose"</automated>
  </verify>
  <acceptance_criteria>
    - core/morning_briefing.py `_compose_briefing` attempts a SMART_AGENT_FALLBACK_* Gemini compose before `_plain_text_fallback`
    - core/nightly_review.py `_compose_nightly` attempts the same SMART_AGENT_FALLBACK_* Gemini compose before `_plain_text_fallback`
    - Both preserve their brain `purpose` label and never raise out of the cron (silent try/except like core/reflection.py)
    - A test in each file asserts brain-fail -> Gemini-fallback text (not the plain template), and brain+fallback-fail -> plain template
    - `python -m pytest tests/test_morning_briefing.py tests/test_nightly_review.py -x -q -k "fallback or compose"` exits 0
  </acceptance_criteria>
  <done>Both paid scheduled composes gain the same 2-tier Sonnet->Gemini fallback as core/reflection.py, satisfying BRAIN-01's "every paid proactive compose ... falls back cleanly to gemini-3.5-flash"; the plain-text template fires only when both tiers fail.</done>
</task>

<task type="auto">
  <name>Task 3: Flip deploy.yml env/secrets + update self_manifest Model Map defaults</name>
  <files>.github/workflows/deploy.yml, core/self_manifest.py</files>
  <read_first>
    - .github/workflows/deploy.yml (line 100 --set-env-vars; line 101 --update-secrets; current values SMART_AGENT_BACKEND=gemini/MODEL=gemini-3.5-flash, SMART_AGENT_FALLBACK_BACKEND=anthropic/MODEL=claude-haiku-4-5, SMART_AGENT_API_KEY=klaus-gemini-key, SMART_AGENT_FALLBACK_API_KEY=klaus-anthropic-key; TICK_BRAIN_FALLBACK_* already added by Plan 01)
    - core/self_manifest.py Model Map section (~349-362): literal default strings os.getenv("SMART_AGENT_MODEL","gemini-3.5-flash") etc. - the defaults that must become post-flip truth (Plan 04 already compacted this file - read current state first)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md section ".github/workflows/deploy.yml" (required changes 1-4) and "core/self_manifest.py::_render_manifest" (Model Map defaults note)
    - $HOME/.claude/projects/-Users-amitgrupper-Desktop-Klaus/memory/klaus_web_push_gotchas.md (deploy.yml --set-env-vars clobber; verify live revision post-deploy)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-05-SUMMARY.md (KLAUS_DAILY_COST_ALERT=5 flagged for this plan)
  </read_first>
  <action>
    In deploy.yml `--set-env-vars` (line 100): set `SMART_AGENT_BACKEND=anthropic`,
    `SMART_AGENT_MODEL=claude-sonnet-5`; set `SMART_AGENT_FALLBACK_BACKEND=gemini`,
    `SMART_AGENT_FALLBACK_MODEL=gemini-3.5-flash`; add the tertiary set matching Task 1's chosen
    names, e.g. `SMART_AGENT_TERTIARY_BACKEND=anthropic,SMART_AGENT_TERTIARY_MODEL=claude-haiku-4-5`;
    add `KLAUS_DAILY_COST_ALERT=5`. In `--update-secrets` (line 101): set
    `SMART_AGENT_API_KEY=klaus-anthropic-key:latest` (brain now Anthropic),
    `SMART_AGENT_FALLBACK_API_KEY=klaus-gemini-key:latest` (fallback now Gemini), and add
    `SMART_AGENT_TERTIARY_API_KEY=klaus-anthropic-key:latest` (Haiku on the Anthropic key). All
    secret names must be existing live secrets (klaus-anthropic-key, klaus-gemini-key,
    klaus-deepseek-key). Every var lands in these two strings only - never console-only (clobber
    gotcha). In core/self_manifest.py Model Map, update the literal default fallbacks so a
    missing-env edge still renders truth: `os.getenv("SMART_AGENT_MODEL", "claude-sonnet-5")` and
    the fallback-row default `"gemini-3.5-flash"` (change only the Model Map defaults; the Plan 04
    compaction elsewhere in this file stays). Note the now-stale heartbeat "Gemini->Haiku fallback
    fired Nx" label (Open Question 1) in the SUMMARY as a follow-up; do not block on it.
  </action>
  <verify>
    <automated>grep -c "SMART_AGENT_MODEL=claude-sonnet-5" .github/workflows/deploy.yml && grep -c "SMART_AGENT_FALLBACK_MODEL=gemini-3.5-flash" .github/workflows/deploy.yml && grep -c "KLAUS_DAILY_COST_ALERT=5" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - deploy.yml --set-env-vars contains `SMART_AGENT_BACKEND=anthropic`, `SMART_AGENT_MODEL=claude-sonnet-5`
    - deploy.yml --set-env-vars contains `SMART_AGENT_FALLBACK_BACKEND=gemini`, `SMART_AGENT_FALLBACK_MODEL=gemini-3.5-flash`, a tertiary Haiku set, and `KLAUS_DAILY_COST_ALERT=5`
    - deploy.yml --update-secrets sets `SMART_AGENT_API_KEY=klaus-anthropic-key:latest`, `SMART_AGENT_FALLBACK_API_KEY=klaus-gemini-key:latest`, and `SMART_AGENT_TERTIARY_API_KEY=klaus-anthropic-key:latest`
    - core/self_manifest.py Model Map default for SMART_AGENT_MODEL is `"claude-sonnet-5"` and the fallback-row default is `"gemini-3.5-flash"`
    - All three grep counts output 1
  </acceptance_criteria>
  <done>deploy.yml flips the brain to Sonnet, remaps fallback->Gemini and tertiary->Haiku with correct existing secrets, adds the cost threshold; manifest defaults reflect post-flip truth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-14 live validation checklist on production</name>
  <what-built>Sonnet-5 brain flip: 3-tier Sonnet->Gemini->Haiku chain with D-12 disclosure in main.py, SMART_AGENT_FALLBACK_* Gemini fallback added to the morning/nightly composes, deploy.yml env/secret remap, manifest Model Map defaults updated. Requires Plan 01's tick-brain decoupling already deployed + verified (D-13 ordering).</what-built>
  <how-to-verify>
    Pre-check: confirm Plan 01's decoupling is deployed and its forced-Groq-failure verification passed. Confirm claude-sonnet-5 access is enabled on the Anthropic account backing klaus-anthropic-key (a single scratch messages.create against claude-sonnet-5 returning 200). If access is missing, STOP - this blocks the flip.
    Deploy this plan (push -> workflow -> new klaus-agent-00NNN revision). Confirm the live Cloud Run revision Variables show SMART_AGENT_MODEL=claude-sonnet-5 and the remapped secrets (not just the YAML).
    Then run the D-14 live checklist on production:
    1. One Telegram turn - Klaus answers on Sonnet (LLMUsage shows purpose=smart on claude-sonnet-5).
    2. One hub turn - same, via /internal/process-hub-message.
    3. One tool-heavy coaching question - tools fire, no temperature/thinking 400 errors, no max_tokens truncation. RECORD the observed end-to-end turn latency for this tool-heavy turn and confirm it stays under the LLM_TIMEOUT_SECONDS=120s ceiling (D-10 / BRAIN-05 live re-validation of the timeout).
    4. One forced Anthropic failure (e.g. staging bad key) - confirm the Gemini fallback answers a conversation turn AND the D-12 backup-reasoning disclosure line appears.
    5. Next paid compose window (morning briefing / nightly review) with the same forced-Anthropic-failure condition - confirm the compose is served by the Gemini fallback (LLMUsage shows gemini-3.5-flash on that purpose), NOT the deterministic plain-text template.
    6. Next day: compare LLMUsage day totals against the Anthropic console - within ~10% (BRAIN-02), and confirm cache_read tokens are non-zero on turn 2+ of a conversation.
    Rollback if needed is an env-var flip back to Gemini (D-15, Amit's judgment).
  </how-to-verify>
  <resume-signal>Type "approved" once the live checklist passes (Sonnet answers on chat+hub, tool-heavy turn latency recorded under 120s, forced-failure shows Gemini + disclosure on a turn AND Gemini on a paid compose, next-day cost within ~10% of console), or describe what surfaced.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.yml -> Cloud Run env/secrets | Controls which model+key backs brain/fallback/tertiary; a wrong secret sends traffic to the wrong model |
| Anthropic account -> brain | Sonnet-5 must be enabled or the whole brain path 404/403s |
| Fallback chain -> billing | A misconfigured fallback tier can silently bill the wrong (expensive) model |
| Paid compose crons -> Gemini fallback | A compose that silently drops to the plain-text template hides an Anthropic outage from Amit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30.5-17 | Tampering / Information Disclosure | Wrong secret backing a fallback tier post-rename | mitigate | Explicit secret map (klaus-anthropic-key for brain+tertiary, klaus-gemini-key for fallback); verify live Cloud Run revision config post-deploy, not just YAML (clobber gotcha) |
| T-30.5-18 | Denial of Service (cost) | Misconfigured fallback billing an expensive model on every turn | mitigate | 3-tier chain tested; Plan 01 decoupling verified first; D-14 next-day console comparison catches cost inversion within ~10% |
| T-30.5-19 | Denial of Service | Sonnet-5 not enabled on the account -> brain path fails live | mitigate | Pre-flip access check (scratch messages.create) in the checkpoint; blocks if missing; env-flip rollback (D-15) |
| T-30.5-20 | Information Disclosure | Silent fallback hiding degraded reasoning from Amit | mitigate | D-12 disclosure line on the Gemini branch surfaces backup-reasoning state; compose paths now log gemini-3.5-flash usage instead of silently templating |
| T-30.5-21 | Availability / Repudiation | Paid compose (morning/nightly) drops straight to plain-text on Anthropic failure, losing the LLM-quality briefing | mitigate | Task 2 adds the 2-tier Gemini fallback before _plain_text_fallback; D-14 item 5 verifies the compose is Gemini-served under forced failure |
| T-30.5-SC | Tampering | pip installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_main.py -x -q` green
- `python -m pytest tests/test_morning_briefing.py tests/test_nightly_review.py -x -q` green
- `grep -c "SMART_AGENT_MODEL=claude-sonnet-5" .github/workflows/deploy.yml` == 1
- Live D-14 checklist passes (checkpoint): Sonnet on chat+hub, tool-heavy turn latency under 120s, forced-failure -> Gemini + disclosure on a turn AND Gemini on a paid compose, next-day cost within ~10% of console
</verification>

<success_criteria>
BRAIN-01 satisfied: claude-sonnet-5 is the brain for every conversation turn and paid proactive compose, with a verified Sonnet->Gemini->Haiku fallback chain for turns, a Sonnet->Gemini fallback for the morning/nightly composes, and the D-12 disclosure, shipped after the tick-brain decoupling was verified.
</success_criteria>

<output>
Create `.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-06-SUMMARY.md` when done (record D-14 checklist results, observed tool-heavy turn latency vs 120s, and next-day cost comparison).
</output>
</output>
