---
phase: 31-standing-directives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/firestore_db.py
  - tests/test_firestore_db.py
autonomous: true
requirements: [DIR-02, DIR-05]
must_haves:
  truths:
    - "A standing directive is persisted verbatim with origin + triggering-context quote and never hard-deleted"
    - "A directive with an explicit end date carries expires_at; an event-based one carries condition_text; a conditionless one persists indefinitely (D-05 hybrid expiry fields)"
    - "A refined directive supersedes an old one, writing superseded_by on the old doc (status='superseded') — D-16 persona-conflict resolution mechanics"
    - "list_active() is read-cached and every write invalidates the cache"
  artifacts:
    - path: "memory/firestore_db.py"
      provides: "StandingDirectiveStore class (add/list_active/list_all/cancel/supersede/expire)"
      contains: "class StandingDirectiveStore"
    - path: "tests/test_firestore_db.py"
      provides: "TestStandingDirectiveStore coverage"
      contains: "class TestStandingDirectiveStore"
  key_links:
    - from: "StandingDirectiveStore write methods"
      to: "_READ_CACHE"
      via: "_cache_invalidate_prefix(('standing_directives',))"
      pattern: "_cache_invalidate_prefix"
    - from: "StandingDirectiveStore.list_active"
      to: "_READ_CACHE"
      via: "_cache_get/_cache_put keyed ('standing_directives','active')"
      pattern: "_cache_(get|put)"
---

<objective>
Create `StandingDirectiveStore` — the durable, verbatim persistence layer for Amit's lasting
behavioral wishes — modeled 1:1 on the existing `FollowupStore`. This is the foundation every
other Phase 31 plan reads from and writes to.

Purpose: A directive stated once must outlive the conversation it was stated in and be injected
into every reasoning path indefinitely (or until it expires/is cancelled). That requires a
dedicated, race-safe Firestore collection with a status lifecycle and no hard-delete, so every
mutation is auditable. Directly realizes DIR-02 (hybrid expiry field shape) and DIR-05
(supersede chain for persona-conflict resolution).

Output: `StandingDirectiveStore` in `memory/firestore_db.py` + `TestStandingDirectiveStore` in
`tests/test_firestore_db.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standing-directives/31-CONTEXT.md
@.planning/phases/31-standing-directives/31-PATTERNS.md

<interfaces>
<!-- The exact sibling to replicate. Executor must NOT explore — copy the FollowupStore shape. -->
FollowupStore lives at memory/firestore_db.py:1656-1815. Its shape:
- class attr `_COLLECTION = "followups"`; __init__(project_id, database="(default)") builds `self._col`
- add(...) -> dict: mints `uuid.uuid4().hex`, builds a doc, `self._col.document(id).set(doc)`,
  re-raises after logging on failure
- list_due/list_pending -> list[dict]: FieldFilter query, returns [] on any Exception (never raises)
- cancel(id) -> bool: get-then-update status; re-raises on error

Module-level read cache (memory/firestore_db.py:61-86) — reuse, do not reinvent:
- _cache_get(key) / _cache_put(key, value) / _cache_invalidate_prefix(prefix)
- _READ_CACHE_TTL_SEC = 600 (10 min)

FieldFilter import: `from google.cloud.firestore_v1.base_query import FieldFilter`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing TestStandingDirectiveStore tests</name>
  <files>tests/test_firestore_db.py</files>
  <read_first>
    - tests/test_firestore_db.py (read the existing TestFollowupStore class — mirror its Firestore-mock/fixture convention exactly; PATTERNS.md notes it at lines 189-278+)
    - .planning/phases/31-standing-directives/31-PATTERNS.md (§ "memory/firestore_db.py — add StandingDirectiveStore" for schema + method shapes)
  </read_first>
  <behavior>
    - add(text, origin, context_quote, expires_at, condition_text) persists a doc with status="active", a uuid `id`, ISO `created_at`, and superseded_by=None
    - add with explicit expires_at stores that ISO date; add with condition_text stores that text; add with neither leaves both None (conditionless → persists indefinitely, DIR-02)
    - list_active() returns only status=="active" docs; list_all() returns every status
    - list_active() second call within TTL does NOT re-hit Firestore (served from _READ_CACHE); a write between the two calls DOES invalidate (fresh read)
    - cancel(id) sets status="cancelled" and returns True; cancel(unknown_id) returns False; the doc is NOT deleted (still present via list_all)
    - supersede(old_id, new_doc) writes superseded_by=new_doc["id"] and status="superseded" on the OLD doc (DIR-05)
    - expire(id) sets status="expired" (DIR-05/D-08 nightly-judged expiry path)
    - a Firestore error on a read method yields [] (never raises); a Firestore error on a write method re-raises after logging
  </behavior>
  <action>
    Add a `TestStandingDirectiveStore` class mirroring `TestFollowupStore`'s existing
    Firestore-mock setup in this file (same monkeypatch/`isolated_modules` fixtures, same
    fake-collection double). Cover every bullet in <behavior>. Assert the cache behavior by
    monkeypatching or spying the underlying collection read count across two `list_active()`
    calls with a write in between. These tests MUST fail initially (class does not yet exist).
  </action>
  <verify>
    <automated>pytest tests/test_firestore_db.py -k StandingDirectiveStore -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_firestore_db.py contains `class TestStandingDirectiveStore`
    - The named test methods reference add / list_active / list_all / cancel / supersede / expire
    - `pytest tests/test_firestore_db.py -k StandingDirectiveStore -x` fails with ImportError/AttributeError (store not yet implemented) — RED confirmed
  </acceptance_criteria>
  <done>Failing tests exist encoding all DIR-02/DIR-05 storage behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement StandingDirectiveStore</name>
  <files>memory/firestore_db.py</files>
  <read_first>
    - memory/firestore_db.py lines 61-86 (_READ_CACHE / _cache_get / _cache_put / _cache_invalidate_prefix)
    - memory/firestore_db.py lines 1656-1815 (FollowupStore — the exact template: __init__, add, list_due, cancel)
    - .planning/phases/31-standing-directives/31-RESEARCH.md (§ Code Examples "StandingDirectiveStore" for the verbatim schema docstring + add() body)
    - .planning/phases/31-standing-directives/31-PATTERNS.md (§ memory/firestore_db.py assignments)
  </read_first>
  <action>
    Add class `StandingDirectiveStore` with `_COLLECTION = "standing_directives"` (lowercase per
    the GCP-casing invariant). Schema fields: `id` (uuid4 hex), `text`, `origin`
    ('user_chat'|'klaus_self'), `context_quote`, `created_at` (ISO-8601 UTC), `status`
    ('active'|'expired'|'cancelled'|'superseded'|'vetoed'), `expires_at` (str|None),
    `condition_text` (str|None), `superseded_by` (str|None).
    Methods:
    - `add(text, origin="user_chat", context_quote="", expires_at=None, condition_text=None) -> dict`
      — mint uuid, set status="active", `.set(doc)`, re-raise after `logger.error` on failure, then
      call `_cache_invalidate_prefix(("standing_directives",))`, return the doc.
    - `list_active() -> list[dict]` — cache key `("standing_directives", "active")` via `_cache_get`;
      on miss, `FieldFilter("status","==","active")` stream, `[]` on any Exception (never raises),
      `_cache_put` on success.
    - `list_all() -> list[dict]` — stream the whole collection, `[]` on error (uncached; history reads are rare).
    - `cancel(id) -> bool` — get-then-update status="cancelled"; return False if not found; re-raise on
      error; `_cache_invalidate_prefix(("standing_directives",))` after.
    - `supersede(old_id, new_directive_id) -> bool` — update old doc `status="superseded"`,
      `superseded_by=new_directive_id` (DIR-05, D-16 persona-conflict supersede chain); same invalidate.
    - `expire(id) -> bool` — update status="expired" (D-05/D-08); same invalidate.
    NEVER hard-delete. Reads never raise; writes re-raise after logging. Place the class as a sibling
    of `FollowupStore` in the same file.
  </action>
  <verify>
    <automated>pytest tests/test_firestore_db.py -k StandingDirectiveStore -x</automated>
  </verify>
  <acceptance_criteria>
    - memory/firestore_db.py contains `class StandingDirectiveStore` and `_COLLECTION = "standing_directives"`
    - Source contains `_cache_invalidate_prefix(("standing_directives",))` in add/cancel/supersede/expire
    - Source contains `superseded_by` written by `supersede(`
    - `pytest tests/test_firestore_db.py -k StandingDirectiveStore -x` exits 0 (GREEN)
    - `pytest tests/test_firestore_db.py -x` exits 0 (no regression to FollowupStore/other store tests)
  </acceptance_criteria>
  <done>StandingDirectiveStore passes all Task-1 tests; existing firestore_db tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → StandingDirectiveStore | Directive text originates from user chat (captured upstream in Plan 03) or reflection (Plan 06); this store only persists it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Tampering | Stored directive influences every downstream reasoning path indefinitely | mitigate | Status-transition-only mutation (cancel/supersede/expire); never hard-delete — every state change is auditable via Firestore doc history, matching FollowupStore discipline |
| T-31-SC | Tampering | Package installs | accept | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `pytest tests/test_firestore_db.py -x` green.
- Grep confirms the store never calls `.delete(` on a directive doc.
</verification>

<success_criteria>
- StandingDirectiveStore exists with the full lifecycle (add/list_active/list_all/cancel/supersede/expire), read-cached list_active, invalidate-on-write, and never hard-deletes.
- DIR-02 storage shape (expires_at vs condition_text vs neither) and DIR-05 superseded_by chain are covered by passing tests.
</success_criteria>

<output>
Create `.planning/phases/31-standing-directives/31-01-SUMMARY.md` when done.
</output>
