---
phase: 29-web-push-transition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - docs/DEPLOYMENT.md
autonomous: false
requirements: [PUSH-01, PUSH-02]
user_setup:
  - service: web-push-vapid
    why: "VAPID keypair signs every Web Push send; private key must live in Secret Manager, public key drives the frontend subscribe call"
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: "Output of `vapid --applicationServerKey` (base64url) — set on Cloud Run service klaus-agent and in local .env"
    dashboard_config:
      - task: "Create Secret Manager secret klaus-vapid-private-key from private_key.pem"
        location: "gcloud secrets create klaus-vapid-private-key --data-file=private_key.pem (project klaus-agent)"

must_haves:
  truths:
    - "pywebpush is importable in the project venv"
    - "The VAPID private key is retrievable from Secret Manager secret klaus-vapid-private-key"
    - "VAPID_PUBLIC_KEY is documented as a required env var"
  artifacts:
    - path: requirements.txt
      provides: "pywebpush dependency pin"
      contains: "pywebpush"
    - path: .env.example
      provides: "VAPID_PUBLIC_KEY placeholder"
      contains: "VAPID_PUBLIC_KEY"
  key_links:
    - from: core/push_sender.py
      to: Secret Manager klaus-vapid-private-key
      via: "access_secret_version on latest"
      pattern: "klaus-vapid-private-key"
---

<objective>
Install the backend Web Push dependency (`pywebpush`) behind a package-legitimacy gate and complete the one-time VAPID key operator setup (keypair generation, Secret Manager storage, env var documentation). This unblocks every push-sending plan.

Purpose: Web Push sends require a signed VAPID keypair and the `pywebpush` library; both are prerequisites for `core/push_sender.py` (Plan 04) and the send paths (Plan 08).
Output: `pywebpush` in requirements.txt + installed, `klaus-vapid-private-key` secret created, `VAPID_PUBLIC_KEY` documented in `.env.example` and `docs/DEPLOYMENT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify pywebpush + py-vapid package legitimacy before install</name>
  <read_first>
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (§ Package Legitimacy Audit — both packages tagged [ASSUMED], slopcheck unavailable)
  </read_first>
  <what-built>Research tagged `pywebpush` and `py-vapid` as [ASSUMED] because slopcheck could not run. Registry metadata is clean (web-push-libs / mozilla-services orgs, ~9yr history, no postinstall) but a human must confirm legitimacy before installing.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pywebpush/ — confirm version 2.3.0, project URL github.com/web-push-libs/pywebpush, healthy download counts.
    2. Open https://pypi.org/project/py-vapid/ — confirm mozilla-services/vapid source repo.
    3. Confirm neither page shows a yank/security warning.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms pywebpush 2.3.x is published by web-push-libs at github.com/web-push-libs/pywebpush
    - Human confirms py-vapid is published by mozilla-services
    - Approval recorded before Task 2 runs
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin and install pywebpush</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (see current pin style + ordering)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (§ Standard Stack — pin `pywebpush>=2.3`; `py-vapid` arrives transitively)
  </read_first>
  <action>Add the line `pywebpush>=2.3` to requirements.txt following the existing alphabetical/grouped placement convention. Then install it into the project venv (use the Python 3.13 `.venv`, never system Python 3.14 — grpc GC segfault per STATE.md). `py-vapid` installs transitively; do not add a second explicit pin unless the venv import fails without it.</action>
  <verify>
    <automated>python -c "import pywebpush, py_vapid; print(pywebpush.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains `pywebpush`
    - `python -c "import pywebpush"` exits 0 in the .venv
    - `python -c "from pywebpush import webpush, WebPushException"` exits 0
  </acceptance_criteria>
  <done>pywebpush is pinned in requirements.txt and importable in the venv.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Generate VAPID keypair, store private key in Secret Manager, set VAPID_PUBLIC_KEY, document</name>
  <files>.env.example, docs/DEPLOYMENT.md</files>
  <read_first>
    - .env.example (current variable layout + comment style)
    - docs/DEPLOYMENT.md (§ secrets/runbook format for adding a new secret)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 3 — VAPID key generation commands; § Invariants — lowercase `klaus-` secret name)
  </read_first>
  <what-built>Claude adds the `VAPID_PUBLIC_KEY` documentation to `.env.example` (placeholder value + comment) and a DEPLOYMENT.md runbook entry describing the secret. The operator then runs the key-gen + Secret Manager + Cloud Run env steps, which touch the live GCP project.</what-built>
  <how-to-verify>
    Operator steps (documented in the DEPLOYMENT.md entry Claude writes):
    1. `pip install py-vapid && vapid --gen` (writes private_key.pem + public_key.pem).
    2. `vapid --applicationServerKey` — copy the base64url public key.
    3. `gcloud secrets create klaus-vapid-private-key --data-file=private_key.pem --project klaus-agent` (lowercase klaus- — uppercase = silent 404).
    4. Set `VAPID_PUBLIC_KEY=<base64url key>` on the klaus-agent Cloud Run service and in local `.env`.
    5. Confirm `gcloud secrets versions access latest --secret klaus-vapid-private-key` returns the PEM.
  </how-to-verify>
  <acceptance_criteria>
    - .env.example contains a line beginning `VAPID_PUBLIC_KEY=` with an explanatory comment
    - docs/DEPLOYMENT.md contains the string `klaus-vapid-private-key` and the 5-step key-gen runbook
    - Operator confirms the secret exists (`gcloud secrets describe klaus-vapid-private-key` succeeds) and VAPID_PUBLIC_KEY is set on Cloud Run
  </acceptance_criteria>
  <resume-signal>Type "vapid ready" once the secret exists and VAPID_PUBLIC_KEY is set, or report blockers.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → build | untrusted third-party package pulled into the runtime |
| Operator → Secret Manager | VAPID private key material crosses into cloud secret storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-SC | Tampering | pip install pywebpush/py-vapid | mitigate | Blocking human-verify checkpoint (Task 1) confirms registry legitimacy before install; [ASSUMED] packages never auto-approved |
| T-29-01 | Information disclosure | VAPID private key | mitigate | Private key stored ONLY in Secret Manager (`klaus-vapid-private-key`), never committed, never in .env.example/logs; only public key is env-exposed |
| T-29-02 | Spoofing | Push payloads | accept | Payload spoofing is impossible without the VAPID private key + per-subscription keys (end-to-end encrypted); key custody handled above |
</threat_model>

<verification>
- `python -c "import pywebpush"` exits 0
- `grep -q pywebpush requirements.txt`
- `grep -q VAPID_PUBLIC_KEY .env.example`
- `gcloud secrets describe klaus-vapid-private-key` succeeds (operator-confirmed)
</verification>

<success_criteria>
pywebpush is installed and pinned; the VAPID keypair exists with the private key in Secret Manager and the public key set as VAPID_PUBLIC_KEY; both are documented.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-01-SUMMARY.md` when done.
</output>
