---
phase: 29-web-push-transition
plan: 05
type: execute
wave: 2
depends_on: ["29-03"]
files_modified:
  - core/tools.py
  - core/heartbeat.py
  - tests/test_tools.py
  - tests/test_heartbeat.py
autonomous: true
requirements: [PUSH-03]

must_haves:
  truths:
    - "Klaus can flip the Telegram-mirror flag with a single brain-direct tool call ('kill the mirror') — the D-11 retirement path is conversational; code removal stays a future cleanup"
    - "Klaus can read push health (subscriptions, last success, mirror state) via a brain-direct tool — the two D-13 self-awareness tools"
    - "The heartbeat surfaces push-failure signals, downgraded in severity while the mirror is ON"
  artifacts:
    - path: core/tools.py
      provides: "toggle_telegram_mirror + get_push_health brain-direct tools"
      contains: "toggle_telegram_mirror"
    - path: core/heartbeat.py
      provides: "_check_push_health checker registered in _collect_signals"
      contains: "_check_push_health"
  key_links:
    - from: core/tools.py
      to: HubSettingsStore.set
      via: "toggle_telegram_mirror handler"
      pattern: "telegram_mirror_enabled"
    - from: core/heartbeat.py
      to: PushSubscriptionStore.list_all
      via: "_check_push_health"
      pattern: "_check_push_health"
---

<objective>
Give Klaus self-awareness of the push channel: two brain-direct tools (`toggle_telegram_mirror` to execute the D-11 retirement conversationally, `get_push_health` to report subscription/mirror status) and a heartbeat checker (`_check_push_health`) that alerts on push failures — self-validating during the mirror week (D-13/D-14).

Purpose: Amit executes "kill the mirror" in chat; push failures surface via the existing alert machinery so retirement is just a flag flip with no follow-on build.
Output: 2 tools in `core/tools.py`, 1 checker in `core/heartbeat.py`, + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
@.planning/phases/29-web-push-transition/29-PATTERNS.md

<interfaces>
<!-- From Plan 03 -->
HubSettingsStore.get() -> {"telegram_mirror_enabled": bool, "push_enabled_at"}
HubSettingsStore.set(patch)
PushSubscriptionStore.list_all() -> list[dict]  # {endpoint, keys, user_agent, last_success_at, failure_count, ...}
<!-- Existing tool-registration touch points in core/tools.py -->
SMART_AGENT_DIRECT_TOOLS frozenset (~40-78); schema block pattern (~768-783, get_self_status);
_handle_get_self_status (~1788-1827); _HANDLERS dict tail (~2606-2646); brain-direct filter get_smart_schemas (~2674).
<!-- Existing heartbeat touch points -->
Signal dataclass (~38-54): fingerprint, severity, area, title, detail, remediation.
check_tokens (~203-222) checker shape; _collect_signals checker tuple (~621-634).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: toggle_telegram_mirror + get_push_health brain-direct tools</name>
  <files>core/tools.py, tests/test_tools.py</files>
  <read_first>
    - core/tools.py (SMART_AGENT_DIRECT_TOOLS ~40-78; get_self_status schema ~768-783; _handle_get_self_status ~1788-1827; _HANDLERS tail ~2606-2646; get_smart_schemas ~2674)
    - tests/test_tools.py (existing tool-handler test conventions)
    - .planning/phases/29-web-push-transition/29-PATTERNS.md (core/tools.py section — 3-part registration; Shared Patterns "Brain-direct tool registration")
  </read_first>
  <behavior>
    - toggle_telegram_mirror(enabled: bool) calls HubSettingsStore.set({"telegram_mirror_enabled": enabled}) and returns JSON with the new state
    - get_push_health() returns JSON: subscription count + per-device {user_agent, last_success_at, failure_count}, telegram_mirror_enabled, push_enabled_at — NO chat_visible_until field (see action)
    - both are registered in all three places (SMART_AGENT_DIRECT_TOOLS frozenset, schema list, _HANDLERS) so get_smart_schemas surfaces them to the brain
    - both reads pass through _jsonsafe_doc before json.dumps
  </behavior>
  <action>Add two tool schema blocks modeled on get_self_status, each with the "Call this directly — do NOT delegate to the worker." description convention. `toggle_telegram_mirror` input_schema has a required boolean property `enabled`; `get_push_health` takes no args (empty object). Add both names to the `SMART_AGENT_DIRECT_TOOLS` frozenset. Add handlers `_handle_toggle_telegram_mirror(enabled)` (calls `HubSettingsStore.set({"telegram_mirror_enabled": enabled})`, returns `json.dumps({"telegram_mirror_enabled": enabled})`) and `_handle_get_push_health()` (reads `PushSubscriptionStore.list_all()` + `HubSettingsStore.get()`, wraps with `_jsonsafe_doc`, returns combined json.dumps). Register in `_HANDLERS`: `"toggle_telegram_mirror": lambda args: _handle_toggle_telegram_mirror(**args)` and `"get_push_health": lambda args: _handle_get_push_health()`. Lazy-import the stores inside the handlers. Do NOT report a chat_visible_until field in get_push_health output: the D-02 visibility gate is an in-process variable in core/scheduled_message.py (Plan 08), so a Firestore-sourced field would always read null and mislead Klaus's self-awareness (D-13); report only subscription data, mirror state, and push_enabled_at. Tests assert both tool names are in SMART_AGENT_DIRECT_TOOLS, appear in get_smart_schemas output, and that the handlers call the right store methods (mock the stores).</action>
  <verify>
    <automated>python -m pytest tests/test_tools.py -x -q -k "mirror or push_health"</automated>
  </verify>
  <acceptance_criteria>
    - core/tools.py contains schema entries for `toggle_telegram_mirror` and `get_push_health`
    - Both names appear in the SMART_AGENT_DIRECT_TOOLS frozenset (grep)
    - Both keys appear in the _HANDLERS dict (grep)
    - `_handle_toggle_telegram_mirror(True)` results in HubSettingsStore.set({"telegram_mirror_enabled": True}) (mocked test)
    - `pytest tests/test_tools.py -x -k "mirror or push_health"` exits 0
  </acceptance_criteria>
  <done>Both brain-direct push tools are registered in all three places and reach the brain schema; handlers mutate/read the correct stores.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _check_push_health heartbeat checker</name>
  <files>core/heartbeat.py, tests/test_heartbeat.py</files>
  <read_first>
    - core/heartbeat.py (Signal dataclass ~38-54; check_tokens ~203-222; _collect_signals checker tuple ~621-634)
    - tests/test_heartbeat.py (existing checker test conventions + Signal assertions)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 9 severity table)
  </read_first>
  <behavior>
    - _check_push_health() returns list[Signal] for three conditions:
      (1) any subscription failure_count >= 3 -> fingerprint "push:failure-streak:{endpoint_hash}", severity critical
      (2) push_enabled_at set but zero subscriptions -> fingerprint "push:no-subscription", severity critical if mirror OFF else warning
      (3) no last_success_at across all subs in > 48h while sends occurred -> fingerprint "push:delivery-stale", severity warning
    - severity of (2) is downgraded to warning while telegram_mirror_enabled is True (Telegram covers delivery — D-14 self-validation)
    - registered in the _collect_signals checker tuple (runs every tick, not weekly-only)
  </behavior>
  <action>Add `def _check_push_health() -> list[Signal]` following the check_tokens shape, lazy-importing PushSubscriptionStore + HubSettingsStore. Read the subscription list + mirror flag; emit Signals for the three conditions in the Pattern 9 table with the exact fingerprints above and the fields fingerprint/severity/area/title/detail/remediation. Use `area="push"`. Downgrade the no-subscription severity to warning when `telegram_mirror_enabled` is True. Add `_check_push_health` to the checker tuple in `_collect_signals` (the every-tick tuple, NOT the weekly-only branch). Tests: mock stores to produce failure_count>=3 (assert critical failure-streak signal), zero-subs-with-mirror-on (assert warning) vs mirror-off (assert critical).</action>
  <verify>
    <automated>python -m pytest tests/test_heartbeat.py -x -q -k push</automated>
  </verify>
  <acceptance_criteria>
    - core/heartbeat.py contains `def _check_push_health`
    - `_check_push_health` appears in the _collect_signals checker tuple (grep)
    - Test: failure_count>=3 yields a Signal with fingerprint starting "push:failure-streak:" and severity critical
    - Test: push_enabled_at set + zero subs yields warning when mirror ON, critical when mirror OFF
    - `pytest tests/test_heartbeat.py -x -k push` exits 0
  </acceptance_criteria>
  <done>The heartbeat surfaces push-failure signals with mirror-aware severity, registered to run every tick.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brain (LLM) → mirror flag | a tool call can disable the Telegram safety net |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering | toggle_telegram_mirror abuse | accept | The tool is reachable only through Klaus's session (single-user allowlist upstream); flipping the mirror is a reversible runtime toggle, never destructive — matches the intended conversational-retirement design (D-13) |
| T-29-09 | Information disclosure | get_push_health output | mitigate | Returns subscription metadata (user_agent, timestamps, counts) but NEVER the p256dh/auth keys or the VAPID private key; handler selects fields explicitly |
</threat_model>

<verification>
- `pytest tests/test_tools.py -x -k "mirror or push_health"` exits 0
- `pytest tests/test_heartbeat.py -x -k push` exits 0
- `grep -c "toggle_telegram_mirror\|get_push_health" core/tools.py` >= 3 (schema + frozenset + handler references)
</verification>

<success_criteria>
Klaus can toggle the mirror and report push health conversationally; the heartbeat alerts on push failures with mirror-aware severity — all covered by per-file tests.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-05-SUMMARY.md` when done.
</output>
