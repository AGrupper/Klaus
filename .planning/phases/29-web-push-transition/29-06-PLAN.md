---
phase: 29-web-push-transition
plan: 06
type: execute
wave: 2
depends_on: ["29-01", "29-03"]
files_modified:
  - interfaces/web_server.py
  - tests/test_push_api.py
autonomous: true
requirements: [PUSH-01, PUSH-03]

must_haves:
  truths:
    - "The frontend can fetch the VAPID public key over a session-authed endpoint"
    - "The frontend can register a push subscription; the server validates it (https endpoint + keys present) and upserts it"
    - "The frontend can read and PATCH the hub settings (Telegram-mirror flag)"
    - "The first successful subscribe stamps push_enabled_at in hub settings (the D-14 heartbeat anchor)"
    - "All new routes require a hub session; none touch OIDC cron/internal/trigger auth"
  artifacts:
    - path: interfaces/web_server.py
      provides: "/api/push/subscribe, /api/push/vapid-public-key, /api/settings (GET+PATCH)"
      contains: "/api/push/subscribe"
    - path: tests/test_push_api.py
      provides: "route tests: validation, auth, upsert"
      exports: []
  key_links:
    - from: interfaces/web_server.py
      to: PushSubscriptionStore.upsert
      via: "POST /api/push/subscribe via run_in_executor"
      pattern: "/api/push/subscribe"
    - from: interfaces/web_server.py
      to: HubSettingsStore
      via: "GET/PATCH /api/settings"
      pattern: "/api/settings"
---

<objective>
Add the session-authed hub API surface for push: `POST /api/push/subscribe` (validate + upsert a subscription), `GET /api/push/vapid-public-key` (serve the public key), and `GET/PATCH /api/settings` (read/toggle the mirror flag from the Settings page). These are the endpoints the frontend hooks (Plan 09) and Settings page (Plan 10) consume.

Purpose: PUSH-01 requires storing subscriptions and re-validating on open; D-09 requires a hub-toggleable mirror flag. All new routes sit behind the existing `require_hub_session` and must NOT weaken the OIDC boundary (HUB-04).
Output: 3 route groups in `interfaces/web_server.py` + `tests/test_push_api.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
@.planning/phases/29-web-push-transition/29-PATTERNS.md

<interfaces>
<!-- Existing web_server.py analogs -->
api_today (~1415-1478): GET + run_in_executor + _jsonsafe_doc response shape.
api_chat_send (~1554-1617): POST + Depends(require_hub_session) + body validation + run_in_executor.
SPA mount warning at ~1727 — register /api/* routes BEFORE the SPA mount.
require_hub_session (imported ~41) — the session-cookie auth dependency for ALL /api/* routes.
_verify_cron_request / OIDC (~1688) — used by /internal/process-hub-message; NEVER touch (HUB-04).
<!-- From Plan 03 -->
PushSubscriptionStore.upsert(sub_json, user_agent); HubSettingsStore.get()/set(patch)
<!-- From Plan 01 -->
env VAPID_PUBLIC_KEY (base64url application server key)
<!-- Ready-to-use route sketches: 29-RESEARCH.md Code Examples ~706-731 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /api/push/subscribe + /api/push/vapid-public-key</name>
  <files>interfaces/web_server.py, tests/test_push_api.py</files>
  <read_first>
    - interfaces/web_server.py (api_chat_send ~1554-1617 for POST-validate shape; api_today ~1415-1478 for run_in_executor; SPA mount comment ~1727)
    - tests/test_hub_chat.py (lines 1-80 — `_ENV`, `_stub_web_server_imports`, dependency_overrides harness — reuse verbatim)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Code Examples ~706-731; § Security Domain V5 input validation)
  </read_first>
  <behavior>
    - POST /api/push/subscribe requires a hub session (401 without); body {subscription:{endpoint,keys:{p256dh,auth}}, user_agent}
    - rejects with 400 when endpoint does not start with "https://" OR keys.p256dh / keys.auth missing
    - on valid input calls PushSubscriptionStore.upsert(sub, user_agent) via run_in_executor, returns {"ok": true}
    - on the FIRST successful upsert (HubSettingsStore.get()["push_enabled_at"] is unset/None) the handler stamps HubSettingsStore.set({"push_enabled_at": firestore.SERVER_TIMESTAMP}); later subscribes leave it untouched — this makes D-14's 'push_enabled_at set but zero subscriptions' checker condition reachable
    - GET /api/push/vapid-public-key requires a hub session, returns {"key": VAPID_PUBLIC_KEY}
  </behavior>
  <action>Register `POST /api/push/subscribe` and `GET /api/push/vapid-public-key` BEFORE the SPA mount, both with `_email: str = Depends(require_hub_session)`. For subscribe: read the JSON body, extract `subscription` + `user_agent`, validate `endpoint.startswith("https://")` and presence of `keys["p256dh"]`/`keys["auth"]` (raise HTTPException 400 otherwise), then `await loop.run_in_executor(None, _get_push_store().upsert, sub, user_agent)`. For vapid-public-key: return `{"key": os.environ["VAPID_PUBLIC_KEY"]}`. Add a lazy `_get_push_store()` singleton accessor. After a successful upsert, read `HubSettingsStore.get()` via run_in_executor; if `push_enabled_at` is unset/None, call `HubSettingsStore.set({"push_enabled_at": firestore.SERVER_TIMESTAMP})` (also via executor) — stamping the D-14 heartbeat checker's 'push was ever enabled' anchor exactly once. Add the lazy `_get_hub_settings_store()` accessor here (Task 2 reuses it). Tests (model on test_hub_chat.py harness, reuse `_ENV` + add `VAPID_PUBLIC_KEY`): valid subscribe → 200 + upsert called; non-https endpoint → 400; missing keys → 400; no session (clear dependency_overrides) → 401; vapid-public-key → 200 with the env key.</action>
  <verify>
    <automated>python -m pytest tests/test_push_api.py -x -q -k "Subscribe or vapid"</automated>
  </verify>
  <acceptance_criteria>
    - interfaces/web_server.py contains `/api/push/subscribe` and `/api/push/vapid-public-key` with `Depends(require_hub_session)`
    - Test: POST with a non-https endpoint returns 400
    - Test: POST with missing keys returns 400
    - Test: POST without a session returns 401
    - Test: valid POST calls PushSubscriptionStore.upsert
    - Test: first valid subscribe (push_enabled_at unset) calls HubSettingsStore.set with a push_enabled_at key; a second subscribe with push_enabled_at already set does NOT re-stamp
    - `pytest tests/test_push_api.py -x -k "Subscribe or vapid"` exits 0
  </acceptance_criteria>
  <done>Subscribe validates + upserts behind session auth and stamps push_enabled_at on first success; the VAPID public key is served over a session-authed endpoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET/PATCH /api/settings (mirror flag)</name>
  <files>interfaces/web_server.py, tests/test_push_api.py</files>
  <read_first>
    - interfaces/web_server.py (api_today ~1415-1478 for GET+jsonsafe; api_chat_send for PATCH body validation)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Code Examples api_get_settings ~725-731)
    - .planning/phases/29-web-push-transition/29-CONTEXT.md (D-09 runtime toggle, effective immediately)
  </read_first>
  <behavior>
    - GET /api/settings (session-authed) returns HubSettingsStore.get() passed through _jsonsafe_doc (includes telegram_mirror_enabled)
    - PATCH /api/settings (session-authed) accepts {"telegram_mirror_enabled": bool}, validates it is a bool, calls HubSettingsStore.set, returns the updated settings
    - only telegram_mirror_enabled is accepted from the PATCH body (ignore/reject other keys) — the Settings page is a skeleton (D-15)
  </behavior>
  <action>Register `GET /api/settings` and `PATCH /api/settings` before the SPA mount, both `Depends(require_hub_session)`. GET: `await loop.run_in_executor(None, _get_hub_settings_store().get)` then return `_jsonsafe_doc(settings)`. PATCH: read body, if `telegram_mirror_enabled` present validate it is a bool (400 otherwise), `run_in_executor(None, _get_hub_settings_store().set, {"telegram_mirror_enabled": value})`, return the re-read settings. Add lazy `_get_hub_settings_store()` singleton. Tests: GET returns default mirror True (no session → 401); PATCH False then GET returns False; PATCH with a non-bool → 400.</action>
  <verify>
    <automated>python -m pytest tests/test_push_api.py -x -q -k settings</automated>
  </verify>
  <acceptance_criteria>
    - interfaces/web_server.py contains GET and PATCH handlers for `/api/settings` with `Depends(require_hub_session)`
    - Test: GET without session → 401
    - Test: PATCH {"telegram_mirror_enabled": false} then GET returns telegram_mirror_enabled false
    - Test: PATCH with a non-bool value → 400
    - `pytest tests/test_push_api.py -x -k settings` exits 0
  </acceptance_criteria>
  <done>The mirror flag is readable and toggleable over session-authed settings routes, effective immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → /api/* | untrusted subscription JSON + settings PATCH cross the hub-session boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Spoofing / EoP | /api/push/*, /api/settings without auth | mitigate | Every new route uses Depends(require_hub_session); tests assert 401 without a session; OIDC cron/internal/trigger routes untouched (HUB-04) |
| T-29-11 | Tampering (SSRF-ish) | attacker-controlled subscribe endpoint | mitigate | Validate endpoint startswith "https://" + keys present before upsert; auth-gate means only Amit can register (ASVS V5) |
| T-29-12 | Tampering | arbitrary settings keys via PATCH | mitigate | PATCH accepts only telegram_mirror_enabled with a bool type-check; other keys rejected/ignored |
</threat_model>

<verification>
- `pytest tests/test_push_api.py -x` exits 0 (all groups)
- `grep -c "require_hub_session" interfaces/web_server.py` increased (all new routes gated)
- No new use of `_verify_cron_request` in the diff (HUB-04 preserved)
</verification>

<success_criteria>
The subscribe, vapid-public-key, and settings routes work behind session auth with input validation and immediate mirror-flag effect — verified by per-file tests.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-06-SUMMARY.md` when done.
</output>
