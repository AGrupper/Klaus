---
phase: 29-web-push-transition
plan: 08
type: execute
wave: 3
depends_on: ["29-04", "29-06"]
files_modified:
  - core/scheduled_message.py
  - interfaces/_router.py
  - interfaces/web_server.py
  - tests/test_scheduled_message.py
autonomous: true
requirements: [PUSH-02, PUSH-03]

must_haves:
  truths:
    - "Every proactive send fans out a push (unless the chat view is visible) and mirrors to Telegram while the flag is ON"
    - "Hub chat replies and Telegram-turn replies also push (D-01), all three send paths covered"
    - "Push is skipped only when the chat view was reported visible within the last few seconds (server-side D-02 gate)"
    - "Push fan-out never blocks the event loop (run_in_executor)"
  artifacts:
    - path: core/scheduled_message.py
      provides: "send_and_inject push+mirror+visibility gate; lazy-Bot deliver helper; visibility helpers"
      contains: "send_push_to_all"
    - path: interfaces/_router.py
      provides: "push after Telegram-turn reply_text"
      contains: "send_push_to_all"
  key_links:
    - from: core/scheduled_message.py
      to: core/push_sender.send_push_to_all
      via: "run_in_executor push fan-out"
      pattern: "run_in_executor"
    - from: interfaces/web_server.py
      to: core/push_sender.send_push_to_all
      via: "hub-reply push hook + chat_visible param"
      pattern: "chat_visible"
---

<objective>
Route all three Klaus send paths through push delivery (D-01): (1) proactive crons via `send_and_inject` (add server-side D-02 visibility gate → push fan-out → mirror-gated Telegram → inject), (2) hub chat replies in `/internal/process-hub-message`, (3) Telegram-turn replies in `_router.py`. Add the server-side chat-visibility mechanism (the messages-poll route reports visibility; the send gate reads it) so a visible chat suppresses push without ever creating a silent push.

Purpose: This wires the push_sender (Plan 04) into every outbound message while honoring the mirror flag (D-08/D-10) and the tight-scope suppression (D-02).
Output: extended `send_and_inject` + visibility helpers in `core/scheduled_message.py`; push hooks in `_router.py` and `web_server.py`; tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
@.planning/phases/29-web-push-transition/29-PATTERNS.md

<interfaces>
<!-- From Plan 04 -->
core.push_sender.send_push_to_all(text, message_class="default") -> dict  # SYNC, call via run_in_executor
<!-- From Plan 03 -->
HubSettingsStore.get() -> {"telegram_mirror_enabled": bool, "chat_visible_until": ...}
<!-- Current send_and_inject (core/scheduled_message.py:26-71) -->
async def send_and_inject(bot, text, *, inject_into_conversation=False, reply_markup=None) -> telegram.Message
  # currently: bot.send_message -> optional FirestoreConversationStore.append
<!-- Send paths -->
interfaces/_router.py ~362: await update.message.reply_text(orchestrator_response)  # Telegram native; add push
interfaces/web_server.py internal_process_hub_message (~1668-1721): appends assistant reply, no Telegram; add push + mirror
interfaces/web_server.py: the existing GET /api/chat/messages poll route (CHAT-03) — add ?chat_visible=1 handling
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend send_and_inject with push + mirror gate + visibility gate</name>
  <files>core/scheduled_message.py, tests/test_scheduled_message.py</files>
  <read_first>
    - core/scheduled_message.py (whole file, 71 lines — current send_and_inject body + lazy-import convention at line 59)
    - tests/test_scheduled_message.py (existing send_and_inject tests to extend without breaking)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 1 sketch ~336-358; Pattern 7 visibility ~597-605; D-10 OutreachLog gate note)
    - .planning/phases/29-web-push-transition/29-PATTERNS.md (scheduled_message.py section — backward-compat new kwargs)
  </read_first>
  <behavior>
    - send_and_inject signature gains keyword-only `message_class="default"` and `push=True` (backward compatible; existing callers unchanged)
    - order: read HubSettingsStore.get() -> if push and NOT chat-visible: run_in_executor(None, send_push_to_all, text, message_class) -> if telegram_mirror_enabled: bot.send_message (full volume, NO disable_notification, D-10) -> conversation inject (unchanged)
    - push errors are swallowed (logged, never raised) — messages never lost (D-04); Telegram send keeps its existing raise-on-failure contract (total-failure semantics preserved for OutreachLog callers during the mirror-on period)
    - module-level helpers: mark_chat_visible(seconds=8) sets an in-process _chat_visible_until; is_chat_visible() returns now < _chat_visible_until
    - when is_chat_visible() is True, push is skipped but mirror + inject still run (D-02)
    - a lazy module-level Bot accessor _get_bot() lets callers without a bot (hub replies) reuse delivery
  </behavior>
  <action>Extend `send_and_inject`: add keyword-only `message_class="default"` and `push=True`. Before the Telegram send, lazily import HubSettingsStore + `from core.push_sender import send_push_to_all`; read the settings. If `push and not is_chat_visible()`: `await loop.run_in_executor(None, send_push_to_all, text, message_class)` inside a try/except that logs and swallows push errors. Gate the existing `bot.send_message` on `settings["telegram_mirror_enabled"]` (default True) — do NOT pass disable_notification (D-10 full volume). Keep the conversation-inject block unchanged. Add module-level `_chat_visible_until` (float, default 0), `mark_chat_visible(seconds=8)`, and `is_chat_visible()`. Add a lazy `_get_bot()` that builds a module-level `telegram.Bot(token)` from env for callers without a bot instance (Open Question 2). Follow the existing inline-import discipline. Extend tests: mirror ON → both push and Telegram; mirror OFF → push+inject only, no Telegram; is_chat_visible True → push skipped, mirror still sends; existing callers (positional bot/text) still pass.</action>
  <verify>
    <automated>python -m pytest tests/test_scheduled_message.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - core/scheduled_message.py send_and_inject accepts `message_class` and `push` keyword-only params
    - core/scheduled_message.py contains `mark_chat_visible` and `is_chat_visible`
    - Test: mirror ON → send_push_to_all called (via executor) AND bot.send_message called
    - Test: mirror OFF → bot.send_message NOT called; push + inject still run
    - Test: is_chat_visible True → send_push_to_all NOT called; bot.send_message still called
    - Test: existing call `send_and_inject(bot, text)` still works (no push kwargs) 
    - `pytest tests/test_scheduled_message.py -x` exits 0
  </acceptance_criteria>
  <done>send_and_inject fans out push (visibility-gated), mirrors Telegram behind the flag at full volume, and never blocks the loop or raises on push failure.</done>
</task>

<task type="auto">
  <name>Task 2: Wire hub-reply + Telegram-turn push paths + chat-visibility reporting</name>
  <files>interfaces/_router.py, interfaces/web_server.py</files>
  <read_first>
    - interfaces/_router.py (handle_message ~330-362 — the reply_text block)
    - interfaces/web_server.py (internal_process_hub_message ~1668-1721; the existing GET /api/chat/messages poll route)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 7 chat_visible param ~597-605; Open Question 1 — push Telegram-turn replies too; Open Question 2 — lazy Bot for hub replies)
    - .planning/phases/29-web-push-transition/29-PATTERNS.md (_router.py section; hub-reply hook section)
  </read_first>
  <action>In `interfaces/_router.py`, immediately after the successful `await update.message.reply_text(orchestrator_response)`, add `await loop.run_in_executor(None, send_push_to_all, orchestrator_response, "chat_reply")` (import send_push_to_all from core.push_sender; grab the running loop). No mirror gate here — Telegram already sent natively; the D-02 visibility gate does not apply (the chat view is not the source). This is the deliberate double-buzz (D-10 / Open Question 1). In `interfaces/web_server.py` `internal_process_hub_message`, after the assistant reply is produced, capture the reply text and deliver it via the Plan-1 lazy-Bot path: `await send_and_inject(None-or-_get_bot(), reply_text, message_class="chat_reply", inject_into_conversation=False)` so hub replies push AND mirror to Telegram while the flag is ON (D-08), without double-injecting (the route already appended the reply). Also modify the existing `GET /api/chat/messages` poll route: when the query param `chat_visible=1` is present, call `mark_chat_visible()` (import from core.scheduled_message) so the server-side D-02 gate knows the chat view is open. Keep OIDC/session auth on these routes exactly as-is.</action>
  <verify>
    <automated>python -m pytest tests/test_push_api.py -x -q -k "visible or hub_reply" ; grep -q "send_push_to_all" interfaces/_router.py && grep -q "mark_chat_visible" interfaces/web_server.py && echo wired</automated>
  </verify>
  <acceptance_criteria>
    - interfaces/_router.py calls `send_push_to_all(..., "chat_reply")` via run_in_executor after reply_text
    - interfaces/web_server.py internal_process_hub_message pushes + mirrors the hub reply (grep send_and_inject or send_push_to_all in that function)
    - interfaces/web_server.py GET /api/chat/messages calls `mark_chat_visible()` when `chat_visible=1`
    - the messages poll route does NOT push (it is a read path); only the send paths push
    - grep confirms both wirings present
  </acceptance_criteria>
  <done>All three send paths push; the chat-visibility signal is reported server-side on the existing poll with no new polling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/async handler → push service | push fan-out runs inside request/cron handlers |
| client poll → visibility state | client asserts chat visibility to suppress push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-16 | Denial of service | blocking the event loop on push fan-out | mitigate | Every send_push_to_all call goes through run_in_executor; webpush timeout=10 — weekly-review-500 class regression prevented |
| T-29-17 | Tampering | forged chat_visible=1 to suppress push | accept | Only Amit's session can hit the poll route; worst case is a self-inflicted suppressed banner (message still in Firestore + Telegram mirror) — UX blemish, not a security issue |
| T-29-18 | Repudiation | message not delivered/logged | mitigate | Telegram mirror (D-08) + Firestore conversation inject remain the audit trail; push failures are logged, never silently lost (D-04) |
</threat_model>

<verification>
- `pytest tests/test_scheduled_message.py -x` exits 0
- `grep -q send_push_to_all interfaces/_router.py`
- `grep -q mark_chat_visible interfaces/web_server.py`
- 1153+ baseline holds (per-file)
</verification>

<success_criteria>
Proactive, hub, and Telegram-turn sends all push; the mirror flag gates Telegram at full volume; a visible chat suppresses push server-side; nothing blocks the event loop.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-08-SUMMARY.md` when done. Note the post-retirement OutreachLog append-gate redefinition (≥1 channel = success) as a tracked follow-up for the mirror-off decision (D-21), not built this phase.
</output>
