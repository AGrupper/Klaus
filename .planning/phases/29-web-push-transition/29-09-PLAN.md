---
phase: 29-web-push-transition
plan: 09
type: execute
wave: 3
depends_on: ["29-06", "29-07"]
files_modified:
  - frontend/src/hooks/usePush.ts
  - frontend/src/hooks/usePush.test.ts
  - frontend/src/hooks/useAppBadge.ts
  - frontend/src/hooks/useAppBadge.test.ts
autonomous: true
requirements: [PUSH-01, PUSH-04]

must_haves:
  truths:
    - "A user gesture can subscribe to push (fetch VAPID key → pushManager.subscribe → POST /api/push/subscribe)"
    - "On hub open, a granted-but-missing subscription silently re-subscribes; a denied permission surfaces the re-enable banner state"
    - "The icon badge reconciles to the true unread count on open and clears at zero"
  artifacts:
    - path: frontend/src/hooks/usePush.ts
      provides: "enablePush gesture + revalidate + permission state"
      contains: "pushManager.subscribe"
    - path: frontend/src/hooks/useAppBadge.ts
      provides: "setAppBadge/clearAppBadge reconciliation with useUnread"
      contains: "setAppBadge"
  key_links:
    - from: frontend/src/hooks/usePush.ts
      to: /api/push/subscribe
      via: "POST after subscribe"
      pattern: "/api/push/subscribe"
    - from: frontend/src/hooks/useAppBadge.ts
      to: navigator.setAppBadge
      via: "reconcile on unreadCount change"
      pattern: "setAppBadge"
---

<objective>
Build the two frontend hooks: `usePush` (feature-detect + user-gesture subscribe + re-validate-on-open per D-19) and `useAppBadge` (reconcile the icon badge to the true unread count and keep the SW's IDB counter honest, D-18).

Purpose: PUSH-01 needs a gesture-driven subscribe + silent recovery; PUSH-04 needs the icon badge to mirror the in-app unread counter. These hooks are consumed by the Settings page, enable banner, and ChatWindow in Plan 10.
Output: `usePush.ts` + `useAppBadge.ts` + their vitest tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
@.planning/phases/29-web-push-transition/29-PATTERNS.md

<interfaces>
<!-- Analog: frontend/src/hooks/useInstallBanner.ts (feature-detect + localStorage-gated + {state, action}) -->
<!-- Analog: frontend/src/hooks/useUnread.ts — useUnread(messageCount) -> {unreadCount, markAllSeen} (localStorage last_seen_seq) -->
<!-- From Plan 06 -->
GET  /api/push/vapid-public-key -> {key: base64url}
POST /api/push/subscribe {subscription: sub.toJSON(), user_agent}
<!-- iOS requirements: subscribe MUST be from a user gesture; userVisibleOnly:true mandatory (29-RESEARCH.md Pattern 3) -->
<!-- Re-validation flow: 29-RESEARCH.md Pattern 8 (~607-623); badge reconcile: Code Examples ~745-755 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usePush hook (subscribe gesture + revalidate)</name>
  <files>frontend/src/hooks/usePush.ts, frontend/src/hooks/usePush.test.ts</files>
  <read_first>
    - frontend/src/hooks/useInstallBanner.ts (whole file — feature-detect + defensive try/catch + {state, action} shape to replicate)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 3 enablePush ~397-413; Pattern 8 re-validation branches ~607-623; urlBase64ToUint8Array requirement)
    - .planning/phases/29-web-push-transition/29-PATTERNS.md (usePush.ts section)
  </read_first>
  <behavior>
    - feature-detect: 'serviceWorker' in navigator && 'PushManager' in window && 'Notification' in window; if absent, permission state = 'unsupported'
    - enablePush(): (called from a user gesture) await serviceWorker.ready → GET /api/push/vapid-public-key → pushManager.subscribe({userVisibleOnly:true, applicationServerKey: urlBase64ToUint8Array(key)}) → POST /api/push/subscribe {subscription: sub.toJSON(), user_agent}
    - revalidate() (on mount/visibility, standalone only): if Notification.permission==='granted' → getSubscription(); exists → POST subscribe (idempotent upsert); null → silently re-subscribe (no gesture) → POST subscribe. If 'denied' AND push-was-enabled flag → expose needsReenable=true. Else 'default' → expose neverAsked=true
    - exposes: { permission, enablePush, needsReenable, neverAsked, isSubscribed }
    - all browser-API access wrapped in try/catch (SSR/undefined-safe like useInstallBanner)
  </behavior>
  <action>Create `usePush.ts` modeled on useInstallBanner's defensive structure. Implement `urlBase64ToUint8Array` (standard base64url → Uint8Array). Implement `enablePush` per Pattern 3 (gesture-triggered) and `revalidate` per Pattern 8's three branches (granted+sub → upsert; granted+null → silent resubscribe; denied+wasEnabled → needsReenable; default → neverAsked). Persist a `push_was_enabled` localStorage flag on first successful subscribe (drives the denied-branch banner). Log revocation-recovery events (silent resubscribe) loudly per Pitfall 1 so the 3-strikes root cause isn't masked. Tests (vitest, mock navigator.serviceWorker/PushManager/Notification + fetch): enablePush calls subscribe with userVisibleOnly:true and POSTs to /api/push/subscribe; revalidate with granted+null-sub triggers a resubscribe; denied+wasEnabled sets needsReenable; default sets neverAsked.</action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/usePush.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - usePush.ts contains `pushManager.subscribe` with `userVisibleOnly: true`
    - usePush.ts POSTs to `/api/push/subscribe` after subscribing
    - Test: revalidate with permission granted + getSubscription()→null triggers a silent re-subscribe (no gesture) + POST
    - Test: permission denied + push_was_enabled flag → needsReenable true
    - Test: permission default → neverAsked true
    - `cd frontend && npx vitest run src/hooks/usePush.test.ts` exits 0
  </acceptance_criteria>
  <done>usePush subscribes from a gesture and silently recovers stale subscriptions on open, surfacing the denied state for the re-enable banner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useAppBadge hook (reconcile icon badge with unread)</name>
  <files>frontend/src/hooks/useAppBadge.ts, frontend/src/hooks/useAppBadge.test.ts</files>
  <read_first>
    - frontend/src/hooks/useUnread.ts (whole file — the counter useAppBadge mirrors; markAllSeen)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 6 badge sync ~579-591; Code Examples badge reconciliation ~745-755)
    - .planning/phases/29-web-push-transition/29-PATTERNS.md (useAppBadge.ts section)
  </read_first>
  <behavior>
    - useAppBadge(unreadCount): on unreadCount change, if 'setAppBadge' in navigator: unreadCount>0 → navigator.setAppBadge(unreadCount); else navigator.clearAppBadge()
    - also posts {type:'RESET_BADGE', count: unreadCount} to navigator.serviceWorker.controller to resync the SW IDB counter
    - no-op (guarded) when setAppBadge is unavailable
  </behavior>
  <action>Create `useAppBadge.ts` with a `useEffect` keyed on `unreadCount` implementing the Code-Examples reconciliation: guard on `'setAppBadge' in navigator`, set/clear the badge, and postMessage RESET_BADGE with the count to `navigator.serviceWorker?.controller`. Tests (mock navigator.setAppBadge/clearAppBadge + serviceWorker.controller.postMessage): unreadCount 3 → setAppBadge(3) + postMessage {RESET_BADGE,3}; unreadCount 0 → clearAppBadge + postMessage {RESET_BADGE,0}; missing setAppBadge → no throw.</action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useAppBadge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useAppBadge.ts calls `navigator.setAppBadge` when unreadCount>0 and `clearAppBadge` at 0
    - useAppBadge.ts posts `{type:'RESET_BADGE', count}` to the SW controller
    - Test: unreadCount=3 → setAppBadge(3); unreadCount=0 → clearAppBadge()
    - Test: absent setAppBadge does not throw
    - `cd frontend && npx vitest run src/hooks/useAppBadge.test.ts` exits 0
  </acceptance_criteria>
  <done>The icon badge reconciles to the true unread count on open and keeps the SW counter in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → push permission | permission prompt must originate from a user gesture (iOS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-19 | Information disclosure | subscription keys in the client | accept | The subscription's p256dh/auth are generated in-browser and sent only to the same-origin auth-gated /api/push/subscribe; no third-party exposure |
| T-29-20 | Spoofing | fake enablePush without a gesture | mitigate | iOS enforces the user-gesture requirement at the platform level; enablePush is only wired to a real click handler (Plan 10) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/hooks/usePush.test.ts` exits 0
- `cd frontend && npx vitest run src/hooks/useAppBadge.test.ts` exits 0
- `cd frontend && npx vitest run` full suite green
</verification>

<success_criteria>
usePush subscribes from a gesture and silently recovers; useAppBadge mirrors the unread counter to the icon badge — both covered by vitest tests.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-09-SUMMARY.md` when done.
</output>
