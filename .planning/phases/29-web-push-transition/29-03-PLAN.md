---
phase: 29-web-push-transition
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/firestore_db.py
  - tests/test_push_subscription_store.py
  - tests/test_hub_settings_store.py
autonomous: true
requirements: [PUSH-01, PUSH-03]

must_haves:
  truths:
    - "A push subscription can be upserted (idempotent), listed, deleted, and marked success/failure"
    - "The Telegram-mirror flag and push_enabled_at are stored in a single hub-settings doc with a default mirror = ON"
    - "Every read that feeds json.dumps is passed through _jsonsafe_doc"
  artifacts:
    - path: memory/firestore_db.py
      provides: "PushSubscriptionStore + HubSettingsStore"
      contains: "class PushSubscriptionStore"
    - path: tests/test_push_subscription_store.py
      provides: "store CRUD + failure-path tests"
      exports: []
    - path: tests/test_hub_settings_store.py
      provides: "settings get/set + default tests"
      exports: []
  key_links:
    - from: core/push_sender.py
      to: PushSubscriptionStore.list_all
      via: "fan-out iteration"
      pattern: "PushSubscriptionStore"
    - from: core/scheduled_message.py
      to: HubSettingsStore.get
      via: "mirror gate"
      pattern: "HubSettingsStore"
---

<objective>
Add the two new Firestore stores this phase depends on: `PushSubscriptionStore` (fan-out subscription list, multi-device from day one per D-17) and `HubSettingsStore` (runtime Telegram-mirror flag + push_enabled_at per D-08/D-09/D-14 — chat visibility for D-02 is tracked in-process in core/scheduled_message.py per RESEARCH A5, NOT persisted here).

Purpose: Every downstream plan reads or writes these stores — the sender fans out over subscriptions, the delivery path reads the mirror flag, the settings routes and brain tools mutate the flag.
Output: two store classes in `memory/firestore_db.py` + their unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
@.planning/phases/29-web-push-transition/29-PATTERNS.md

<interfaces>
<!-- Existing analogs in memory/firestore_db.py the executor MUST replicate -->
RunDetailStore (lines ~1226-1334): upsert(dict) keyed on derived id (re-raise on write failure), delete (re-raise), get/list reads that never raise + return _jsonsafe_doc.
HeartbeatConfigStore (lines ~287-320): _COLLECTION="config"; get() merges {**DEFAULTS, **stored}; set(patch) merges patch + SERVER_TIMESTAMP updated_at, re-raise on failure.
IncidentStore.record_open (~353-380): doc_ref.set(payload, merge=True) small-merge-write pattern for record_success/record_failure.
_jsonsafe_doc (~885-913): converts DatetimeWithNanoseconds → isoformat before json.dumps.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PushSubscriptionStore (collection push_subscriptions)</name>
  <files>memory/firestore_db.py, tests/test_push_subscription_store.py</files>
  <read_first>
    - memory/firestore_db.py (RunDetailStore lines ~1226-1334 for CRUD shape; IncidentStore.record_open ~353-380 for merge-write; _jsonsafe_doc ~885-913)
    - tests/fakes.py (FakeCollection, FakeQuery, make_snap, FailingCollection — the standard Firestore store test fixtures)
    - .planning/phases/29-web-push-transition/29-PATTERNS.md (PushSubscriptionStore section — doc-id + method list)
  </read_first>
  <behavior>
    - upsert(sub_json, user_agent): writes doc keyed by sha256(endpoint).hexdigest()[:32] with endpoint, keys{p256dh,auth}, user_agent, created_at, last_validated_at (SERVER_TIMESTAMP); idempotent (merge=True); re-raises on write failure
    - list_all(): returns list of _jsonsafe_doc dicts; returns [] on read failure (never raises)
    - delete(endpoint): deletes the sha256-hashed doc; re-raises on failure
    - record_success(endpoint): merge-writes last_success_at + failure_count=0
    - record_failure(endpoint, error): merge-writes last_error + increments failure_count
    - reads pass through _jsonsafe_doc so datetimes serialize
  </behavior>
  <action>Add `class PushSubscriptionStore` with `_COLLECTION = "push_subscriptions"`, copying the RunDetailStore CRUD skeleton. Doc id = `hashlib.sha256(endpoint.encode()).hexdigest()[:32]` (endpoint is too long/unsafe as a raw doc id). Methods: `upsert(sub_json, user_agent="")`, `list_all()`, `delete(endpoint)`, `record_success(endpoint)`, `record_failure(endpoint, error)`. Follow the house rule: reads never raise (return []/None + logger.warning); writes re-raise after logger.error. All reads return `_jsonsafe_doc(...)`. Use doc-level `firestore.SERVER_TIMESTAMP` only. Write tests with FakeCollection/FailingCollection covering each method's happy path and one failure path.</action>
  <verify>
    <automated>python -m pytest tests/test_push_subscription_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - memory/firestore_db.py contains `class PushSubscriptionStore` with `_COLLECTION = "push_subscriptions"`
    - Contains methods upsert, list_all, delete, record_success, record_failure
    - Doc id derives from `sha256` of the endpoint (grep `sha256` near the class)
    - `pytest tests/test_push_subscription_store.py -x` exits 0
    - list_all returns [] (not raise) when the collection stream fails (FailingCollection test passes)
  </acceptance_criteria>
  <done>PushSubscriptionStore supports idempotent multi-device upsert, listing, deletion, and success/failure stamping with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HubSettingsStore (config/hub_settings)</name>
  <files>memory/firestore_db.py, tests/test_hub_settings_store.py</files>
  <read_first>
    - memory/firestore_db.py (HeartbeatConfigStore lines ~287-320 — copy verbatim, rename doc; _jsonsafe_doc ~885-913)
    - tests/fakes.py (FakeCollection/make_snap/FailingCollection)
    - .planning/phases/29-web-push-transition/29-PATTERNS.md (HubSettingsStore section — defaults dict)
  </read_first>
  <behavior>
    - get(): returns {**defaults, **stored}; defaults = {"telegram_mirror_enabled": True, "push_enabled_at": None}; on read failure returns defaults (never raises). No chat_visible_until field — D-02 visibility lives in an in-process variable (Plan 08), never Firestore
    - set(patch): merge-writes patch + updated_at SERVER_TIMESTAMP; re-raises on failure
    - default telegram_mirror_enabled is True (mirror ON, D-08/D-09)
    - get() output passes _jsonsafe_doc where it feeds json.dumps callers
  </behavior>
  <action>Add `class HubSettingsStore` with `_COLLECTION = "config"`, `_DOCUMENT = "hub_settings"`, copying HeartbeatConfigStore verbatim. Defaults dict `{"telegram_mirror_enabled": True, "push_enabled_at": None}`. Do NOT add a chat_visible_until field: the D-02 visibility gate is an in-process module variable in core/scheduled_message.py (RESEARCH A5, single Cloud Run instance) — a persisted copy would always be stale/misleading. `get()` merges defaults over stored and returns; `set(patch)` merge-writes with `updated_at`. Ensure callers that json.dumps the result (settings route, get_push_health tool) can wrap with `_jsonsafe_doc` — the store returns raw dicts, jsonsafe applied at the boundary per the PATTERNS note. Tests: default when doc absent (mirror True), set-then-get round trip, FailingCollection returns defaults on read and re-raises on write.</action>
  <verify>
    <automated>python -m pytest tests/test_hub_settings_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - memory/firestore_db.py contains `class HubSettingsStore` with `_DOCUMENT = "hub_settings"`
    - get() returns telegram_mirror_enabled=True when the doc is absent (test asserts default ON)
    - set({"telegram_mirror_enabled": False}) then get() returns False (round-trip test)
    - `pytest tests/test_hub_settings_store.py -x` exits 0
  </acceptance_criteria>
  <done>HubSettingsStore stores the mirror flag (default ON) and push_enabled_at with passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API routes → Firestore | subscription docs written from the auth-gated /api/push route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering | push_subscriptions docs | accept | Single-user allowlist enforced upstream at the /api/* session-auth boundary (Plan 06); store itself carries no per-user ACL need (V4) |
| T-29-04 | Information disclosure | subscription keys at rest | accept | Firestore access is IAM-scoped to the klaus-agent service account; keys are per-device push secrets, not user credentials |
</threat_model>

<verification>
- `pytest tests/test_push_subscription_store.py -x` exits 0
- `pytest tests/test_hub_settings_store.py -x` exits 0
- 1153+ baseline holds (run touched store tests per-file)
</verification>

<success_criteria>
Both stores exist with the correct collections/documents, mirror defaults ON, multi-device subscription support, and passing per-file tests.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-03-SUMMARY.md` when done.
</output>
