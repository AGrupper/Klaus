---
phase: 29-web-push-transition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - .planning/phases/29-web-push-transition/29-HUMAN-UAT.md
autonomous: false
requirements: [PUSH-02, PUSH-04]

must_haves:
  truths:
    - "workbox-precaching / routing / strategies / expiration / core are direct devDependencies at ^7.4.1"
    - "29-HUMAN-UAT.md exists with the D-20 device checklist and D-21 mirror-week tracking items"
  artifacts:
    - path: frontend/package.json
      provides: "workbox devDependencies for the custom SW"
      contains: "workbox-precaching"
    - path: .planning/phases/29-web-push-transition/29-HUMAN-UAT.md
      provides: "physical-device verification + mirror-week tracking"
      contains: "Mirror week"
  key_links:
    - from: frontend/src/sw.ts
      to: workbox-precaching
      via: "import precacheAndRoute"
      pattern: "workbox-precaching"
---

<objective>
Promote the workbox packages from transitive to direct devDependencies (behind a package-legitimacy gate) so the custom service worker in Plan 07 can import them, and create the `29-HUMAN-UAT.md` document that tracks the D-20 physical-device push verification and the D-21 one-week mirror observation.

Purpose: `injectManifest` requires the SW to import workbox modules directly; and the phase-closing verification (D-20) plus post-phase mirror-week tracking (D-21) need a durable checklist doc.
Output: workbox devDeps in `frontend/package.json`; `29-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify workbox package legitimacy before install</name>
  <read_first>
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (§ Package Legitimacy Audit — workbox-* tagged [ASSUMED], already in lockfile as transitive deps)
  </read_first>
  <what-built>The workbox packages are already present transitively at 7.4.1 (vite-plugin-pwa deps), but promoting them to direct devDependencies is a new declared install. slopcheck could not run; a human confirms legitimacy first.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/workbox-precaching — confirm publisher googlechrome / repo github.com/googlechrome/workbox, no install scripts.
    2. Spot-check https://www.npmjs.com/package/workbox-routing and workbox-strategies for the same org.
    3. Confirm version 7.4.1 matches the existing lockfile entry.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms workbox-* 7.4.1 is published by googlechrome (github.com/googlechrome/workbox)
    - Human confirms no postinstall scripts
    - Approval recorded before Task 2 runs
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install workbox devDependencies</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json (current devDependencies + vite-plugin-pwa version 1.3.0)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (§ Standard Stack Installation block — exact package list at ^7.4.1)
  </read_first>
  <action>From the `frontend/` directory run `npm install -D workbox-precaching@^7.4.1 workbox-routing@^7.4.1 workbox-strategies@^7.4.1 workbox-expiration@^7.4.1 workbox-core@^7.4.1`. Do NOT add the `idb` package — Plan 07 uses raw IndexedDB. These pin to the same 7.4.1 already in the lockfile as vite-plugin-pwa transitive deps.</action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json');['workbox-precaching','workbox-routing','workbox-strategies','workbox-expiration','workbox-core'].forEach(m=>{if(!p.devDependencies[m])throw new Error('missing '+m)});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json devDependencies contains workbox-precaching, workbox-routing, workbox-strategies, workbox-expiration, workbox-core
    - frontend/package.json devDependencies does NOT contain `idb`
    - `cd frontend && node -e "require('workbox-precaching/package.json')"` resolves (installed)
  </acceptance_criteria>
  <done>All five workbox devDependencies are declared and resolvable.</done>
</task>

<task type="auto">
  <name>Task 3: Create 29-HUMAN-UAT.md device + mirror-week checklist</name>
  <files>.planning/phases/29-web-push-transition/29-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-CONTEXT.md (on-device tracked-item pattern D-21 references — model the doc structure on Phase 26's device checklist)
    - .planning/phases/29-web-push-transition/29-CONTEXT.md (D-20 close condition, D-21 mirror-week tracking)
  </read_first>
  <action>Create the UAT doc with two sections. Section "D-20 Phase-Close Device Verification" lists the witnessed-on-iPhone steps: (a) enable-push flow from the Settings page / Today banner grants permission and stores a subscription; (b) one chat-reply push arrives with the app fully closed; (c) one real proactive push arrives (autonomous tick OR a manually-triggered cron) with the app closed; (d) the installed icon shows an unread badge after the closed-app push, and viewing chat clears it. Section "D-21 Post-Phase Mirror-Week Tracking" lists: mirror flag left ON, daily double-buzz audit (every push has a matching Telegram buzz), the ≥1-week observation window, and the final "flip mirror off" decision — all explicitly marked as tracked items the phase does NOT stay open for. Include a checkbox per item and a notes column.</action>
  <verify>
    <automated>test -f .planning/phases/29-web-push-transition/29-HUMAN-UAT.md && grep -q "Mirror" .planning/phases/29-web-push-transition/29-HUMAN-UAT.md && grep -qi "badge" .planning/phases/29-web-push-transition/29-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 29-HUMAN-UAT.md exists
    - Contains a "D-20" phase-close section with checkboxes for enable flow, chat-reply push, proactive push, and icon badge
    - Contains a "D-21" mirror-week section marked as tracked post-phase (not phase-blocking) items
  </acceptance_criteria>
  <done>The UAT doc captures both the phase-close device checks and the post-phase mirror-week tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | untrusted third-party packages pulled into the frontend build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-SC | Tampering | npm install workbox-* | mitigate | Blocking human-verify checkpoint (Task 1) confirms googlechrome publisher + no postinstall before install; [ASSUMED] never auto-approved |
</threat_model>

<verification>
- `cd frontend && node -e "require('workbox-precaching/package.json')"` resolves
- `grep -q workbox-precaching frontend/package.json`
- `test -f .planning/phases/29-web-push-transition/29-HUMAN-UAT.md`
</verification>

<success_criteria>
workbox devDependencies are installed and declared; the UAT tracking doc exists with D-20 and D-21 sections.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-02-SUMMARY.md` when done.
</output>
