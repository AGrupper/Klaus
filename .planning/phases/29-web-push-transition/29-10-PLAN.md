---
phase: 29-web-push-transition
plan: 10
type: execute
wave: 4
depends_on: ["29-08", "29-09"]
files_modified:
  - frontend/src/components/settings/SettingsPage.tsx
  - frontend/src/components/shared/PushEnableBanner.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/chat/ChatWindow.tsx
  - frontend/src/components/today/TodayPage.tsx
autonomous: false
requirements: [PUSH-01, PUSH-04]

must_haves:
  truths:
    - "Amit can open a Settings page, enable push from a button, and toggle the Telegram mirror"
    - "A first-run dismissible banner on Today prompts push enable (gesture-driven)"
    - "A notification tap opens the Today timeline; viewing chat clears both the tab badge and the icon badge"
    - "Push arrives on the physical iPhone with the app closed, and the icon badge appears (D-20)"
  artifacts:
    - path: frontend/src/components/settings/SettingsPage.tsx
      provides: "enable-push button + mirror toggle (D-15 skeleton)"
      contains: "enablePush"
    - path: frontend/src/components/shared/PushEnableBanner.tsx
      provides: "Today first-run enable banner (D-16)"
      contains: "enablePush"
  key_links:
    - from: frontend/src/App.tsx
      to: /settings route + NAVIGATE bridge
      via: "route element + serviceWorker message listener"
      pattern: "/settings"
    - from: frontend/src/components/chat/ChatWindow.tsx
      to: useAppBadge / clearAppBadge
      via: "markAllSeen clears both badges (D-18)"
      pattern: "clearAppBadge"
---

<objective>
Wire the frontend UI: a minimal Settings page (enable-push + mirror toggle, D-15), a first-run enable banner on Today (D-16), the `/settings` route + nav entry, the SW→router NAVIGATE bridge (tap opens Today, D-12), and the ChatWindow badge integration (viewing chat clears both badges, D-18). Then close the phase on the physically-verified device UAT (D-20).

Purpose: This is the user-facing surface that lets Amit enable push and see the badge, plus the phase-close verification that push actually lands on the iPhone.
Output: SettingsPage, PushEnableBanner, App route + NAVIGATE bridge, Sidebar nav, Today header gear, ChatWindow badge wiring, and a passed device UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
@.planning/phases/29-web-push-transition/29-PATTERNS.md

<interfaces>
<!-- From Plan 09 -->
usePush() -> { permission, enablePush, needsReenable, neverAsked, isSubscribed }
useAppBadge(unreadCount)  // reconciles icon badge; posts RESET_BADGE
<!-- From Plan 06 -->
GET /api/settings -> {telegram_mirror_enabled, ...}; PATCH /api/settings {telegram_mirror_enabled}
<!-- Analogs -->
App.tsx (~55-79 route wiring; ~123-128 useQuery fetchMe) — add /settings route + NAVIGATE listener
Sidebar.tsx NAV_ITEMS (~1-55) — append Settings (lucide Settings icon); do NOT add to BottomTabs (5 slots full)
InstallBanner.tsx (fixed-bottom banner shell) — template for PushEnableBanner
ChatWindow.tsx useUnread(allMessages.length) (~50) + markAllSeen (~42-44) — badge clear anchor
<!-- Responsive gotcha (frontend memory): do NOT use inline style display to hide/show; it overrides Tailwind md: classes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsPage + /settings route + nav entry + Today header gear</name>
  <files>frontend/src/components/settings/SettingsPage.tsx, frontend/src/App.tsx, frontend/src/components/layout/Sidebar.tsx, frontend/src/components/today/TodayPage.tsx</files>
  <read_first>
    - frontend/src/App.tsx (whole file — route wiring + useQuery/fetchMe idiom)
    - frontend/src/components/layout/Sidebar.tsx (NAV_ITEMS ~1-55)
    - frontend/src/components/today/TodayPage.tsx (header region for the gear button)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Nav placement note ~757-758 — gear in Today header, not a 6th bottom tab)
    - MEMORY: frontend_responsive_display_gotcha (no inline display styles)
  </read_first>
  <action>Create `SettingsPage.tsx`: a thin page composing (a) an enable-push control from `usePush()` — shows "Enable push" button when `neverAsked`/not subscribed (onClick=enablePush, a real user gesture), a re-enable hint when `needsReenable`, and an enabled state otherwise; and (b) a Telegram-mirror toggle backed by `GET/PATCH /api/settings` (useQuery to read, a mutation to PATCH `telegram_mirror_enabled`). Keep it a skeleton — no sign-out/preferences (D-15). Add `<Route path="/settings" element={<SettingsPage />} />` in App.tsx following the TasksPage wrapper pattern. Append `{ label: 'Settings', path: '/settings', icon: Settings }` (lucide `Settings`) to `NAV_ITEMS` in Sidebar.tsx (desktop). For phone, add a gear icon button in the TodayPage header that routes to `/settings` — do NOT add a 6th BottomTab. Use Tailwind classes for any responsive show/hide, never inline `display` (responsive-display gotcha).</action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc --noEmit -p tsconfig.app.json && grep -q "/settings" src/App.tsx && grep -q "Settings" src/components/layout/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPage.tsx renders an enable-push button wired to usePush().enablePush and a mirror toggle wired to PATCH /api/settings
    - App.tsx contains `<Route path="/settings"`
    - Sidebar.tsx NAV_ITEMS contains a Settings entry with path '/settings'
    - TodayPage.tsx has a gear button navigating to /settings (no new BottomTab)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0; `npx vitest run` green
  </acceptance_criteria>
  <done>The Settings skeleton exists with enable-push + mirror toggle, reachable via desktop nav and a Today header gear.</done>
</task>

<task type="auto">
  <name>Task 2: PushEnableBanner on Today + NAVIGATE bridge + ChatWindow badge clear</name>
  <files>frontend/src/components/shared/PushEnableBanner.tsx, frontend/src/App.tsx, frontend/src/components/chat/ChatWindow.tsx, frontend/src/components/today/TodayPage.tsx</files>
  <read_first>
    - frontend/src/components/shared/InstallBanner.tsx (whole file — fixed-bottom banner shell, dismiss X, 44px targets, safe-area)
    - frontend/src/components/chat/ChatWindow.tsx (useUnread + markAllSeen ~42-50 — the badge-clear anchor)
    - frontend/src/App.tsx (where to attach the serviceWorker message listener; useNavigate)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (NAVIGATE handling Code Example ~733-742; Pattern 8 neverAsked branch; badge reconciliation)
  </read_first>
  <action>Create `PushEnableBanner.tsx` by copying InstallBanner's fixed-bottom shell (role, z-index above BottomTabs, safe-area padding, dismiss X, 44px tap targets) and swapping the primary button's action to `usePush().enablePush` (real gesture). Gate rendering on `usePush().neverAsked` (first-run) and on standalone; also render the re-enable-in-iOS-Settings variant when `needsReenable` (D-19). Mount it on TodayPage. In App.tsx add the SW→router bridge: a `useEffect` adding a `navigator.serviceWorker` 'message' listener that calls `navigate(event.data.path ?? '/')` on `{type:'NAVIGATE'}` (D-12 tap→Today). In ChatWindow.tsx, call `useAppBadge(unreadCount)` alongside the existing `useUnread`, and extend `markAllSeen` so viewing chat also calls `navigator.clearAppBadge()` + posts `{type:'RESET_BADGE', count:0}` — clearing the tab badge and icon badge together (D-18). Avoid inline `display` styles for any responsive rendering.</action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc --noEmit -p tsconfig.app.json && grep -q "NAVIGATE" src/App.tsx && grep -q "clearAppBadge" src/components/chat/ChatWindow.tsx && grep -q "enablePush" src/components/shared/PushEnableBanner.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PushEnableBanner.tsx wires its primary button to usePush().enablePush and gates on neverAsked/standalone
    - App.tsx registers a serviceWorker 'message' listener that navigates on {type:'NAVIGATE'}
    - ChatWindow.tsx calls useAppBadge and clearAppBadge within/around markAllSeen
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0; `npx vitest run` green
  </acceptance_criteria>
  <done>The first-run enable banner prompts push, notification taps route to Today, and viewing chat clears both badges.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Physical-iPhone push verification (D-20 phase close)</name>
  <files>.planning/phases/29-web-push-transition/29-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/29-web-push-transition/29-HUMAN-UAT.md (the D-20 device checklist created in Plan 02)
    - .planning/phases/29-web-push-transition/29-CONTEXT.md (D-20 close condition, D-21 mirror-week is post-phase)
  </read_first>
  <what-built>Full push stack deployed: enable flow, SW push handler, send-path fan-out, mirror flag, icon badge. Automation cannot exercise APNs + iOS notification rendering, so the phase closes on a witnessed on-device test with the mirror ON. This must run against the DEPLOYED Cloud Run URL in the installed home-screen PWA (SPA/auth paths have no CI coverage — project memory).</what-built>
  <how-to-verify>
    On the physical iPhone with the app installed to the home screen (mirror flag left ON):
    1. Open the app → tap Enable push (Settings or Today banner) → grant permission. Confirm a subscription appears (ask Klaus "get push health" or check the store).
    2. Fully close the app. Send Klaus a hub/Telegram message that produces a reply → confirm a "Klaus" push notification arrives on the lock screen (and a matching Telegram buzz — the double-buzz).
    3. Trigger one real proactive push (autonomous tick or a manually-triggered cron) with the app closed → confirm it arrives as a push.
    4. Confirm the installed icon shows an unread badge after the closed-app push; open chat → confirm both the tab badge and icon badge clear.
    5. Tap a notification → confirm it opens the Today timeline (not chat).
    Record each result in 29-HUMAN-UAT.md.
  </how-to-verify>
  <acceptance_criteria>
    - Chat-reply push witnessed on the closed-app iPhone (D-20)
    - One proactive-class push witnessed on the closed-app iPhone (D-20)
    - Icon badge appears after a closed-app push and clears when chat is viewed
    - Notification tap opens Today
    - Results recorded in 29-HUMAN-UAT.md; mirror-week observation (D-21) noted as a tracked post-phase item (phase does NOT stay open for the week)
  </acceptance_criteria>
  <resume-signal>Type "verified" with the witnessed results, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → subscribe | the enable button is the only path that triggers the iOS permission prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-21 | Spoofing | mirror toggle from Settings without auth | mitigate | The mirror toggle calls PATCH /api/settings which is Depends(require_hub_session) (Plan 06); the page is only reachable inside the authed SPA |
| T-29-22 | Information disclosure | notification content on lock screen | accept | Full-text push is the intended design (D-12); same exposure as Telegram; user controls iOS previews |
</threat_model>

<verification>
- `cd frontend && npx vitest run` green
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0
- `cd frontend && npm run build` exits 0
- D-20 device checklist witnessed and recorded in 29-HUMAN-UAT.md
</verification>

<success_criteria>
The Settings page, enable banner, nav, NAVIGATE bridge, and badge wiring ship; push is physically verified on the iPhone with the mirror ON (D-20), closing the phase while the mirror-week observation continues as a tracked post-phase item.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-10-SUMMARY.md` when done.
</output>
