---
phase: 29-web-push-transition
plan: 07
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - frontend/vite.config.ts
  - frontend/src/sw.ts
  - frontend/src/sw.test.ts
  - frontend/tsconfig.app.json
  - frontend/vitest.config.ts
autonomous: true
requirements: [PUSH-02, PUSH-04]

must_haves:
  truths:
    - "The build produces a custom service worker (injectManifest) that still precaches, network-firsts index.html (5s), cache-firsts assets, and honors SKIP_WAITING"
    - "A push event always displays a notification inside event.waitUntil (never a silent push)"
    - "The push handler increments an IndexedDB badge counter and calls setAppBadge; notificationclick always opens the Today timeline, never the chat, no tag replacement (D-12)"
  artifacts:
    - path: frontend/src/sw.ts
      provides: "custom SW: precache + runtime caching + SKIP_WAITING + push + notificationclick"
      contains: "showNotification"
    - path: frontend/vite.config.ts
      provides: "injectManifest strategy pointing at src/sw.ts"
      contains: "injectManifest"
  key_links:
    - from: frontend/src/sw.ts
      to: navigator.setAppBadge
      via: "push handler badge increment"
      pattern: "setAppBadge"
    - from: frontend/vite.config.ts
      to: frontend/src/sw.ts
      via: "srcDir + filename"
      pattern: "sw.ts"
---

<objective>
Migrate the PWA from vite-plugin-pwa `generateSW` to `injectManifest` with a hand-written `src/sw.ts` that preserves the three load-bearing behaviors (precache + cleanupOutdatedCaches, NetworkFirst index.html 5s = HUB-03, SKIP_WAITING listener = UpdatePrompt flow) AND adds the push + notificationclick handlers (PUSH-02) with the IndexedDB badge counter (PUSH-04).

Purpose: iOS push requires a custom SW; the migration must not regress HUB-03 (stale-index protection) or the update-prompt flow. The push handler must ALWAYS show a notification (iOS 3-strikes rule).
Output: `vite.config.ts` change + `src/sw.ts` + `src/sw.test.ts` + tsconfig/vitest adjustments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-web-push-transition/29-CONTEXT.md
@.planning/phases/29-web-push-transition/29-RESEARCH.md
@.planning/phases/29-web-push-transition/29-PATTERNS.md

<interfaces>
<!-- Current vite.config.ts workbox block (to port verbatim into sw.ts) -->
runtimeCaching[0]: document destination -> NetworkFirst, cacheName 'html-cache', networkTimeoutSeconds 5, expiration {maxEntries 5, maxAgeSeconds 86400}
runtimeCaching[1]: /\/assets\/.+\.(js|css)$/ -> CacheFirst, cacheName 'assets-cache', expiration {maxEntries 50, maxAgeSeconds 31536000}
Keep: registerType 'prompt', injectRegister false.
<!-- sw.ts skeleton: 29-RESEARCH.md Pattern 4 (~447-518) is the canonical source -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip vite.config.ts to injectManifest</name>
  <files>frontend/vite.config.ts, frontend/tsconfig.app.json</files>
  <read_first>
    - frontend/vite.config.ts (whole file — current workbox block, registerType, injectRegister, manifest)
    - frontend/tsconfig.app.json (current lib array)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 4 vite.config changes ~429-445; Pitfall 2 — runtimeCaching is IGNORED under injectManifest)
  </read_first>
  <action>Change `strategies: 'generateSW'` to `strategies: 'injectManifest'`. Add `srcDir: 'src'`, `filename: 'sw.ts'`, and `injectManifest: { globPatterns: ['**/*.{js,css,html,ico,png,svg,webmanifest}'] }` (move the globPatterns value from the deleted `workbox` block). DELETE the entire `workbox` block — its `runtimeCaching` is silently ignored under injectManifest and is replicated in sw.ts (Task 2). Keep `registerType: 'prompt'` and `injectRegister: false` untouched (UpdatePrompt.tsx depends on both). In tsconfig.app.json add `"WebWorker"` to the `lib` array so sw.ts type-checks.</action>
  <verify>
    <automated>cd frontend && grep -q "injectManifest" vite.config.ts && ! grep -q "generateSW" vite.config.ts && ! grep -q "runtimeCaching" vite.config.ts && grep -q "WebWorker" tsconfig.app.json && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains `strategies: 'injectManifest'`, `filename: 'sw.ts'`, `srcDir: 'src'`
    - vite.config.ts no longer contains `generateSW` or `runtimeCaching`
    - vite.config.ts still contains `registerType: 'prompt'` and `injectRegister: false`
    - tsconfig.app.json lib array contains `WebWorker`
  </acceptance_criteria>
  <done>The plugin builds a custom SW from src/sw.ts; the ignored workbox block is removed; prompt-mode registration preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/sw.ts custom service worker + tests</name>
  <files>frontend/src/sw.ts, frontend/src/sw.test.ts, frontend/vitest.config.ts</files>
  <read_first>
    - frontend/vite.config.ts (the two runtimeCaching entries being ported — read the current values before deletion in Task 1, or from the interfaces block above)
    - frontend/src/components/shared/UpdatePrompt.tsx (useRegisterSW posts {type:'SKIP_WAITING'} — the listener must match)
    - frontend/vitest.config.ts (exclude/include patterns — sw.ts needs mocked SW globals or exclusion from the app tsc build)
    - .planning/phases/29-web-push-transition/29-RESEARCH.md (Pattern 4 full sw.ts skeleton ~447-518; Pattern 6 IndexedDB counter ~591; Pitfall 1 — waitUntil mandatory)
  </read_first>
  <behavior>
    - precacheAndRoute(self.__WB_MANIFEST) + cleanupOutdatedCaches() present
    - registerRoute document -> NetworkFirst 'html-cache' networkTimeoutSeconds 5, ExpirationPlugin maxEntries 5 maxAgeSeconds 86400 (HUB-03 parity)
    - registerRoute /assets/.(js|css) -> CacheFirst 'assets-cache' ExpirationPlugin maxEntries 50 maxAgeSeconds 31536000
    - message listener: {type:'SKIP_WAITING'} -> self.skipWaiting(); {type:'RESET_BADGE', count} -> write IDB counter + setAppBadge(count)/clearAppBadge
    - push handler: event.waitUntil ALWAYS resolves to showNotification("Klaus", {body, icon, data:{url:'/'}}) even if the badge/IDB step throws (try/catch around badge, notification unconditional); no tag
    - notificationclick: close, focus existing client + postMessage {type:'NAVIGATE', path:'/'} else openWindow('/')
    - increment/reset badge via raw IndexedDB (klaus-badge db, single store) — no idb package
  </behavior>
  <action>Create `src/sw.ts` from the Pattern 4 skeleton: workbox precache + the two `registerRoute` calls that byte-for-byte replicate the deleted vite.config runtimeCaching (html-cache NetworkFirst 5s; assets-cache CacheFirst), the `SKIP_WAITING` message listener, a `push` handler that wraps ALL work in `event.waitUntil` and calls `showNotification` UNCONDITIONALLY (badge/IDB increment in a try/catch so a badge failure can never skip the notification — Pitfall 1), and a `notificationclick` handler that focuses+postMessages NAVIGATE '/' or openWindow('/'). Add a raw-IndexedDB helper (~25 lines: open 'klaus-badge', one object store, `incrementBadgeCount()` + `resetBadgeCount(n)`). Add a RESET_BADGE message branch. Write `src/sw.test.ts` (vitest) mocking `self`/`self.registration`/`navigator`: assert push handler calls showNotification even when the badge step throws; assert notificationclick posts NAVIGATE '/'; assert SKIP_WAITING calls skipWaiting. In vitest.config.ts ensure sw.ts is testable (mock globals) but excluded from any jsdom collection that would choke on `self.__WB_MANIFEST`.</action>
  <verify>
    <automated>cd frontend && npx vitest run src/sw.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/sw.ts contains `precacheAndRoute(self.__WB_MANIFEST)`, `cleanupOutdatedCaches()`, `html-cache`, `assets-cache`, and a `SKIP_WAITING` message branch
    - src/sw.ts push handler calls `showNotification` inside `event.waitUntil`; badge work is inside a try/catch so it cannot skip the notification (test proves this)
    - src/sw.ts notificationclick posts `{type:'NAVIGATE', path:'/'}` or openWindow('/')
    - src/sw.ts does NOT import the `idb` package (raw IndexedDB)
    - `cd frontend && npx vitest run src/sw.test.ts` exits 0
  </acceptance_criteria>
  <done>The custom SW preserves HUB-03 + the update-prompt flow and adds an always-visible push handler with badge increment and Today-tap navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Build smoke — HUB-03 + SKIP_WAITING survive in dist/sw.js</name>
  <files>frontend/vite.config.ts</files>
  <read_first>
    - frontend/vite.config.ts (post-Task-1 state)
    - .planning/phases/29-web-push-transition/29-VALIDATION.md (HUB-03 regression row — grep dist/sw.js)
  </read_first>
  <action>Run the frontend production build and confirm the emitted `dist/sw.js` still contains the load-bearing cache names and the skip-waiting listener — proving the injectManifest migration did not silently drop HUB-03 (Pitfall 2) or the update flow (Pitfall 4). This task adds no new code; it is the build-output verification gate for the SW migration. If the grep fails, the SW is misconfigured — fix sw.ts/vite.config before proceeding. Do NOT verify by bundle-hash polling (project memory); grep the built file directly.</action>
  <verify>
    <automated>cd frontend && npm run build && grep -q "html-cache" dist/sw.js && grep -q "assets-cache" dist/sw.js && grep -q "SKIP_WAITING" dist/sw.js && echo hub03-ok</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/sw.js` contains `html-cache`
    - `dist/sw.js` contains `assets-cache`
    - `dist/sw.js` contains `SKIP_WAITING`
  </acceptance_criteria>
  <done>The built service worker provably retains network-first index caching, asset caching, and the skip-waiting update flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push service → service worker | encrypted push payload decrypted and rendered by the SW while the app is closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-13 | Elevation of privilege | silent-push background abuse | mitigate | Structurally prevented — the push handler ALWAYS shows a notification inside waitUntil; badge work is try/catch-isolated so it cannot suppress the notification (iOS 3-strikes) |
| T-29-14 | Denial of service | HUB-03 stale-index regression | mitigate | Build-smoke Task 3 greps dist/sw.js for html-cache/assets-cache/SKIP_WAITING before the change is trusted |
| T-29-15 | Information disclosure | notification body on lock screen | accept | Full-text notifications are the intended design (D-12), same exposure as Telegram today; iOS preview settings are the user-side control |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/sw.test.ts` exits 0
- `cd frontend && npm run build && grep -q html-cache dist/sw.js && grep -q SKIP_WAITING dist/sw.js`
- vite.config.ts has no `generateSW`/`runtimeCaching`
</verification>

<success_criteria>
The custom SW ships via injectManifest, preserves HUB-03 + the update-prompt flow (proven by build-output grep), and always displays a notification with badge increment and Today-tap navigation.
</success_criteria>

<output>
Create `.planning/phases/29-web-push-transition/29-07-SUMMARY.md` when done.
</output>
