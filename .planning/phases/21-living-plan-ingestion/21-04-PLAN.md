---
phase: 21-living-plan-ingestion
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - core/main.py
  - prompts/smart_agent.md
  - tests/test_main_render_smart_system.py
autonomous: true
requirements: [PLAN-02, PLAN-03]
must_haves:
  truths:
    - "The training_profile section in the smart-agent system prompt renders the structured blueprint fields as a coaching-reference guide, not a rigid k:v dump"
    - "weekly_split renders as a day-by-day AM/PM summary (label + modality + priority), never as attendance flags — Klaus never nags about a single missed session"
    - "The prompt frames weekly_split as a template/guide and preserves the 'if profile empty, do not invent' discipline extended to the new structured fields"
    - "v3.0 crons (morning_briefing, 21:30 proactive_alerts, Sunday weekly review) still render cleanly with the expanded profile"
  artifacts:
    - path: "core/main.py"
      provides: "Coaching-reference prose rendering of structured profile in render_smart_system"
      contains: "weekly_split"
    - path: "prompts/smart_agent.md"
      provides: "Reframed TRAINING & ATHLETIC COACHING section describing the structured keys + template discipline"
      contains: "template"
    - path: "tests/test_main_render_smart_system.py"
      provides: "Tests for structured-field prose rendering + no-attendance-flag guard"
      contains: "weekly_split"
  key_links:
    - from: "core/main.py render_smart_system"
      to: "memory/firestore_db.py UserProfileStore.load"
      via: "fresh per-turn profile load → prose snippet"
      pattern: "_user_profile_store.load"
    - from: "prompts/smart_agent.md {training_profile}"
      to: "core/main.py training_profile_snippet"
      via: "placeholder substitution"
      pattern: "\\{training_profile\\}"
---

<objective>
Reframe how the structured profile is presented to the brain, and verify no v3.0 cron regressed. Two production edits + one regression check:

1. `core/main.py render_smart_system` (lines 287-298): replace the generic `for k, v: "- {k}: {v}"` dump with coaching-reference prose. Header `**Coaching reference — Amit's training plan:**`; `dated_goals` as bulleted target lines; `weekly_split` as a day-by-day AM/PM summary (label + modality + priority — never attendance); `nutrition_targets` as macro targets + fueling slot sequence; `supplement_schedule`/`fueling_timeline` as ordered slot lists; `plan_start_date` as "Block anchor: YYYY-MM-DD (Block Week 1)". Preserve the `non_empty` guard + the `updated_at/bootstrapped_at/schema_version` exclusion filter, and fall back to the generic `k: v` line for any unknown key (forward-compat). Follow the `self_state` field-by-field conditional-append pattern (lines 248-268).

2. `prompts/smart_agent.md` TRAINING & ATHLETIC COACHING section (lines 77-99): describe what each structured key means (`dated_goals` = Tier A peak targets; `weekly_split` = flexible template, NOT attendance contract; `nutrition_targets` = daily macro targets; `plan_start_date` = block anchor); add explicit discipline "weekly_split is a template, not a contract — never nag about a single missed session"; extend the recognized-keys list for the update tool (now also `update_plan`); KEEP the "if profile empty, do NOT invent thresholds" discipline and extend it to the new structured fields; add the Tier A (profile targets) vs Tier B (Garmin/TrainingLogStore measured actuals) note.

3. v3.0 cron-regression check: `morning_briefing`, the 21:30 `proactive_alerts`, and the Sunday weekly review must still render with the expanded profile. The weekly review reads `profile.get("athletic_goals")` (weekly_training_review.py:188) — confirm that key still exists (Plan 01 retained it) and the review test still passes; confirm morning_briefing/proactive_alerts either don't read the profile or render cleanly with the new keys.

Purpose: This is the user-visible reframe (SC-2 + SC-4) and the rigidity-drift guard, plus the gate that no consumer broke.
Output: prose renderer, reframed prompt, regression-verified crons, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-living-plan-ingestion/21-CONTEXT.md
@.planning/phases/21-living-plan-ingestion/21-PATTERNS.md

<interfaces>
<!-- Renderer + prompt + consumers. -->
From core/main.py render_smart_system (lines 287-307):
- profile = self._user_profile_store.load()   # fresh every turn — updates take effect next turn, no cache
- non_empty filter excludes updated_at/bootstrapped_at/schema_version and falsy values
- self_state rendering (lines 248-268) is the prose-append pattern to copy
- .replace("{training_profile}", training_profile_snippet)

From prompts/smart_agent.md:
- line 7: {training_profile} placeholder (no change)
- lines 77-99: TRAINING & ATHLETIC COACHING section (in-place reframe)

v3.0 profile consumers (regression surface):
- core/weekly_training_review.py:188 → profile.get("athletic_goals") or []  (athletic_goals retained in Plan 01)
- core/morning_briefing.py, core/proactive_alerts.py → confirm whether they read the profile; render cleanly if so
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coaching-reference prose rendering in render_smart_system</name>
  <files>core/main.py, tests/test_main_render_smart_system.py</files>
  <read_first>
    - core/main.py (lines 248-307 — self_state prose pattern + the training_profile dump to replace)
    - .planning/phases/21-living-plan-ingestion/21-PATTERNS.md (render_smart_system section, lines 270-318)
    - .planning/phases/21-living-plan-ingestion/21-CONTEXT.md (rendering discretion, lines 55; weekly_split as priorities, line 84)
    - tests/test_main_render_smart_system.py (existing training_profile test ~line 233-246)
  </read_first>
  <behavior>
    - When the profile has structured fields, the snippet header is "**Coaching reference — Amit's training plan:**" (not "**Training profile:**")
    - dated_goals renders as bulleted target lines including the metric values (e.g. "100kg bench")
    - weekly_split renders a per-day line listing AM and PM (label + modality + priority); the rendered string contains NO word "attendance"/"completed"/"missed" and renders no boolean — a missed session is structurally unrepresentable
    - plan_start_date renders as "Block anchor: 2026-06-21 (Block Week 1)"
    - nutrition_targets/supplement_schedule/fueling_timeline render as readable ordered lines (not raw dict repr)
    - An unknown/future key falls back to the generic "- key: value" line (forward-compat)
    - Empty profile → empty snippet (existing non_empty guard preserved)
    - updated_at/bootstrapped_at/schema_version never appear in the snippet
  </behavior>
  <action>
    Replace the `if non_empty:` block (lines 294-298) with a structured renderer following the `self_state` conditional-append pattern. Build `lines` starting with header `**Coaching reference — Amit's training plan:**`. For each structured key present and non-empty, append a formatted section: dated_goals → one bullet per goal ("{goal_label} ({target_date}): {metrics joined}"); weekly_split → one line per day ("{Day}: AM {am.label} [{am.modality}] · PM {pm.label} [{pm.modality}]") — read label/modality/priority defensively with .get; nutrition_targets → "Daily targets: {protein_g}g protein / {carbs_g}g carbs" + fueling slot labels if present; fueling_timeline → ordered "Slot N — {timing}: {food}"; supplement_schedule → "Supplements: ..."; plan_start_date → "Block anchor: {date} (Block Week 1)". Keep the `non_empty` dict and exclusion filter exactly (do not render updated_at/bootstrapped_at/schema_version). After the known-key sections, iterate any remaining keys in `non_empty` not handled above and append the generic "- {k}: {v}" fallback. Keep the empty-profile → empty-snippet guard. Then add tests to tests/test_main_render_smart_system.py asserting: header text, a dated_goals metric appears, weekly_split renders day lines with modality and contains no "attendance"/"completed", plan_start_date renders the "Block anchor" line, and an unknown key falls back to "- key:".
  </action>
  <verify>
    <automated>python -m pytest tests/test_main_render_smart_system.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_main_render_smart_system.py -x -q` exits 0
    - `grep -n "Coaching reference\|Block anchor" core/main.py` matches inside render_smart_system
    - A test feeds a weekly_split with am/pm objects and asserts the rendered snippet contains the modality but NOT the substring "attendance" or "completed"
    - `grep -n "schema_version\|bootstrapped_at\|updated_at" core/main.py` shows they remain in the exclusion filter
  </acceptance_criteria>
  <done>render_smart_system emits coaching-reference prose for structured fields with a forward-compat fallback; weekly_split renders as a template (no attendance flags); tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe the TRAINING & ATHLETIC COACHING prompt section</name>
  <files>prompts/smart_agent.md</files>
  <read_first>
    - prompts/smart_agent.md (lines 7 placeholder; lines 77-99 the section to reframe)
    - .planning/phases/21-living-plan-ingestion/21-PATTERNS.md (smart_agent.md section, lines 322-360 — including the Tier A/Tier B prompt block)
    - .planning/phases/21-living-plan-ingestion/21-CONTEXT.md (fabrication regression + Tier A/B discipline, lines 82; never silently rewrite, lines 45)
  </read_first>
  <behavior>
    - The section names the structured keys and what each means: dated_goals (Tier A peak targets), weekly_split (flexible template, NOT attendance contract), nutrition_targets (daily macro targets), plan_start_date (block anchor)
    - Explicit line: weekly_split is a template, not a contract — never nag about a single missed session
    - The "if profile empty, do NOT invent thresholds/targets/buffers" discipline is preserved and extended to the new structured fields
    - The Tier A (profile targets, citable) vs Tier B (measured actuals from Garmin/TrainingLogStore, derive at read time, never hand-seeded) distinction is stated
    - The update-tool reference lists the new structured keys and names update_plan as the user-facing update tool
    - Klaus recommends structural changes but never silently rewrites the plan (CONTEXT line 45)
    - {training_profile} placeholder at line 7 is unchanged
  </behavior>
  <action>
    Edit the TRAINING & ATHLETIC COACHING section (lines 77-99) in place. Add a paragraph describing the structured profile keys now rendered in {training_profile}: dated_goals = Tier A peak targets; weekly_split = flexible template (session label/modality/priority per AM/PM slot), explicitly "a template, not a contract — never nag about a single missed session"; nutrition_targets = daily macro targets + fueling slots; plan_start_date = block anchor. Add the Tier A vs Tier B block (transcribe the pattern from PATTERNS.md lines 353-359): targets live in the profile and are citable as "your target"; current pace/lifts/RPE are Tier B and must be derived from Garmin/TrainingLogStore at read time, never invented or hand-seeded. Keep the existing "if profile empty, do NOT invent" numbered discipline intact and extend it to apply to the structured fields. Update the tool reference to list the new recognized keys (dated_goals, weekly_split, nutrition_targets, supplement_schedule, fueling_timeline, plan_start_date) and name update_plan as the user-facing update tool (alongside update_training_profile). Add a line that Klaus recommends structural plan changes but never silently rewrites — Amit adopts changes via the update tool. Do NOT touch line 7's placeholder.
  </action>
  <verify>
    <automated>grep -niq "template" prompts/smart_agent.md && grep -niq "never nag\|missed session" prompts/smart_agent.md && grep -niq "tier b\|update_plan" prompts/smart_agent.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ni "template, not a contract\|never nag about a single missed session" prompts/smart_agent.md` matches
    - `grep -ni "tier a\|tier b" prompts/smart_agent.md` matches (Tier A/B discipline present)
    - `grep -n "update_plan" prompts/smart_agent.md` matches
    - `grep -n "weekly_split\|dated_goals\|plan_start_date" prompts/smart_agent.md` matches (structured keys described)
    - The "do NOT invent" discipline still present: `grep -ni "do not invent\|don.t invent\|never make up" prompts/smart_agent.md` matches
    - line 7 `{training_profile}` unchanged: `grep -n "{training_profile}" prompts/smart_agent.md` still matches
  </acceptance_criteria>
  <done>Prompt frames structured keys as a coaching reference, weekly_split as a template (no nagging), keeps anti-fabrication discipline, names update_plan + new keys, and the Tier A/B distinction.</done>
</task>

<task type="auto">
  <name>Task 3: v3.0 cron-regression verification (morning_briefing, proactive_alerts, weekly review)</name>
  <files>tests/test_weekly_training_review.py</files>
  <read_first>
    - core/weekly_training_review.py (lines 181-193 — profile.get("athletic_goals") fetch)
    - core/morning_briefing.py, core/proactive_alerts.py (grep for UserProfileStore / training_profile / render_smart_system usage)
    - tests/test_weekly_training_review.py (existing review tests)
    - memory/firestore_db.py (Plan 01 — confirm athletic_goals retained in _SCAFFOLD)
  </read_first>
  <behavior>
    - The Sunday weekly review still fetches athletic_goals without error when the profile has the expanded v4.0 schema (athletic_goals retained → no KeyError, defaults to [] if absent)
    - morning_briefing and proactive_alerts render/compose without error given an expanded profile (they either don't read the profile or tolerate the new keys)
    - No test in test_weekly_training_review.py regresses
  </behavior>
  <action>
    First, grep morning_briefing.py and proactive_alerts.py for any profile read. If neither reads UserProfileStore/training_profile, document that in the SUMMARY (no code change needed — they consume Garmin/weather, not the profile). If either reads the profile, add a defensive `.get(..., default)` only if it currently assumes a removed key — but Plan 01 retained athletic_goals, so this should be a no-op. For weekly_training_review: confirm `profile.get("athletic_goals") or []` (line 188) still resolves (athletic_goals retained). Add/extend a test in tests/test_weekly_training_review.py that runs the week-data gather (or the athletic_goals fetch path) against a mocked profile containing the FULL v4.0 schema (dated_goals, weekly_split, ..., athletic_goals) and asserts no exception and that data["athletic_goals"] resolves to the list. Do NOT change cron behavior beyond defensiveness — the goal is proving the expansion didn't break consumers.
  </action>
  <verify>
    <automated>python -m pytest tests/test_weekly_training_review.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_weekly_training_review.py -x -q` exits 0
    - `grep -rn "UserProfileStore\|training_profile" core/morning_briefing.py core/proactive_alerts.py` result is recorded in the SUMMARY (either "no profile read" or the specific read confirmed safe)
    - A test exercises weekly review gather against a full-v4.0-schema profile and asserts athletic_goals resolves without exception
  </acceptance_criteria>
  <done>Weekly review, morning briefing, and proactive alerts confirmed to render with the expanded profile (test-backed for weekly review; grep-documented for the other two); no consumer regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserProfileStore → system prompt → brain | Structured profile values are formatted into the brain's system prompt every turn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering / drift | Renderer emits weekly_split as attendance flags, re-introducing rigidity-drift | mitigate | Renderer formats only label/modality/priority (no boolean source field exists in the scaffold); a test asserts the snippet contains no "attendance"/"completed" substring (Task 1). |
| T-21-09 | Information Disclosure | Renderer leaks updated_at/bootstrapped_at into the prompt (cosmetic) or raises on DatetimeWithNanoseconds | mitigate | The non_empty exclusion filter (preserved in Task 1) drops updated_at/bootstrapped_at/schema_version before rendering. |
| T-21-10 | Repudiation/correctness | Prompt reframe weakens the anti-fabrication discipline, letting Klaus invent Tier B numbers | mitigate | Task 2 preserves the "do NOT invent" discipline and adds the explicit Tier A vs Tier B rule; grep gates assert both survive. |
</threat_model>

<verification>
- `python -m pytest tests/test_main_render_smart_system.py tests/test_weekly_training_review.py -x -q` (run per-file) both exit 0.
- Prompt grep gates confirm template/Tier-A-B/update_plan/do-not-invent language present.
- morning_briefing/proactive_alerts profile-read status documented in SUMMARY.
</verification>

<success_criteria>
- ROADMAP SC-4: the training_profile prompt section renders blueprint fields framed as a coaching reference guide, not a rigid contract.
- ROADMAP SC-2: weekly_split renders as a template (no attendance flags) → "did Klaus nag about a single missed session?" stays NO.
- ROADMAP SC-3 support: prompt names update_plan + the new keys so the brain knows the update path.
- v3.0 cron-regression gate cleared (weekly review test green; briefing/alerts confirmed unaffected).
</success_criteria>

<output>
Create `.planning/phases/21-living-plan-ingestion/21-04-SUMMARY.md` when done.
</output>
