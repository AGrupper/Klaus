---
phase: 21-living-plan-ingestion
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - core/tools.py
  - tests/test_tools.py
autonomous: true
requirements: [PLAN-03]
must_haves:
  truths:
    - "Amit can update any v4.0 structured field conversationally and Klaus reasons against it on the next turn"
    - "update_training_profile / update_plan schema advertises the six new structured keys to the brain"
    - "get_training_profile read path strips DatetimeWithNanoseconds timestamp fields before json.dumps so it never raises"
  artifacts:
    - path: "core/tools.py"
      provides: "Extended update schema + update_plan alias + JSON-safe get handler"
      contains: "update_plan"
    - path: "tests/test_tools.py"
      provides: "Tests for update_plan dispatch + JSON-safe get handler"
      contains: "update_plan"
  key_links:
    - from: "core/tools.py _handle_update_training_profile"
      to: "memory/firestore_db.py UserProfileStore.update"
      via: "merge=True patch write"
      pattern: "UserProfileStore.*update"
    - from: "core/tools.py _handle_get_training_profile"
      to: "memory/firestore_db.py _jsonsafe_doc"
      via: "timestamp stripping before json.dumps"
      pattern: "_jsonsafe_doc|isoformat|pop\\(.updated_at"
---

<objective>
Deliver the update path (PLAN-03) and harden the read path. Three changes in `core/tools.py`: (1) extend the `update_training_profile` schema description + `patch` description to advertise the six new structured keys so the brain knows they are valid; (2) add a user-facing `update_plan` alias to `_HANDLERS` that calls the same handler (roadmap + SC-3 name `update_plan`); (3) make `_handle_get_training_profile` JSON-safe by stripping `updated_at`/`bootstrapped_at` (DatetimeWithNanoseconds) before `json.dumps` — PATTERNS.md flags this as a latent bug already present. The update handler body needs no logic change: `UserProfileStore.update` uses `merge=True`, so any recognized key merges, and the brain re-reads the profile every turn (no cache invalidation).

Purpose: Without the schema advertisement the brain won't emit the new keys; without the JSON-safe fix the get tool raises once a real `updated_at` exists in the doc.
Output: Extended schema, `update_plan` alias, JSON-safe get handler, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-living-plan-ingestion/21-CONTEXT.md
@.planning/phases/21-living-plan-ingestion/21-PATTERNS.md

<interfaces>
<!-- Existing tool plumbing the executor extends. -->
From core/tools.py:
- update_training_profile schema (lines 665-685) — description lists recognized keys: athletic_goals, training_constraints, recovery_preferences
- get_training_profile schema (line ~653)
- _handle_get_training_profile() (line ~1293) — currently: return json.dumps(store.load())  ← UNSAFE: updated_at is DatetimeWithNanoseconds
- _handle_update_training_profile(patch) (lines 1296-1307) — store.update(patch); returns json.dumps({"ok": True})
- _HANDLERS dispatch (lines 1455-1456): "get_training_profile" and "update_training_profile" lambdas

From memory/firestore_db.py:
- _jsonsafe_doc(d: dict) -> dict (lines 699-718) — ISO-converts any value with .isoformat (use this for the get handler)
- UserProfileStore.update(patch) — merge=True, recognized keys are whatever is in patch (no allow-list enforcement)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend update schema, add update_plan alias, JSON-safe the get handler</name>
  <files>core/tools.py</files>
  <read_first>
    - core/tools.py (lines 640-740 for the two schemas; lines 1280-1471 for the handlers + _HANDLERS dispatch)
    - memory/firestore_db.py (lines 699-718 — _jsonsafe_doc helper to import/reuse)
    - .planning/phases/21-living-plan-ingestion/21-PATTERNS.md (tools.py section, lines 211-266; JSON serialization safety, lines 375-388)
    - .planning/phases/21-living-plan-ingestion/21-CONTEXT.md (update path decision, lines 43-45; discretion on update_plan naming, line 53)
  </read_first>
  <behavior>
    - update_training_profile schema description AND the patch property description both list: dated_goals (list), weekly_split (object), nutrition_targets (object), supplement_schedule (list), fueling_timeline (list), plan_start_date (string) — alongside the existing keys
    - A get_training_profile call returns valid JSON even when the stored doc contains updated_at as a DatetimeWithNanoseconds (no TypeError from json.dumps)
    - "update_plan" exists as a tool name in _HANDLERS and dispatches to the same merge-write handler as update_training_profile
    - update_plan and update_training_profile produce identical Firestore writes for the same patch
  </behavior>
  <action>
    In the `update_training_profile` schema (lines 665-685): append to the description string and the `patch` property description the six new recognized top-level keys — `dated_goals (list), weekly_split (object), nutrition_targets (object), supplement_schedule (list), fueling_timeline (list), plan_start_date (string)`. Keep the existing keys in the list. In `_handle_get_training_profile` (line ~1293): change `return json.dumps(store.load())` to strip timestamp fields first — `from memory.firestore_db import _jsonsafe_doc` then `return json.dumps(_jsonsafe_doc(store.load()))` (the helper ISO-converts updated_at/bootstrapped_at). Add a user-facing `update_plan` tool: register an `update_plan` entry in the tool schema list (clone the update_training_profile schema, name it `update_plan`, description "Update Sir's living training plan (goals, weekly split, nutrition targets, dates). Same structured keys as update_training_profile.") and add `_HANDLERS["update_plan"]` as a lambda that calls `_handle_update_training_profile(**args)` (same underlying handler — D from CONTEXT prefers extending the existing one). Do NOT change `_handle_update_training_profile`'s body — merge=True already accepts the new keys.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tools.py -x -q -k "profile or update_plan or get_training"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "update_plan" core/tools.py` matches in both the schema list and _HANDLERS
    - `grep -n "_jsonsafe_doc" core/tools.py` matches inside _handle_get_training_profile
    - `grep -n "dated_goals\|weekly_split\|plan_start_date" core/tools.py` matches inside the update_training_profile schema description block
    - `python -c "import core.tools as t; names=[s['name'] for s in t.SMART_AGENT_TOOLS] if hasattr(t,'SMART_AGENT_TOOLS') else []; print('update_plan' in str(t._HANDLERS))"` prints True (adapt to the actual tools registry symbol if different)
  </acceptance_criteria>
  <done>Schema advertises the six new keys; update_plan alias dispatches to the merge-write handler; get handler is JSON-safe via _jsonsafe_doc.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests for update_plan dispatch + JSON-safe get handler</name>
  <files>tests/test_tools.py</files>
  <read_first>
    - tests/test_tools.py (existing training-profile tool tests — find by grep "training_profile")
    - core/tools.py (handlers + _HANDLERS after Task 1)
    - .planning/phases/21-living-plan-ingestion/21-PATTERNS.md (JSON serialization safety section)
  </read_first>
  <behavior>
    - Test: dispatching the "update_plan" handler with a patch calls UserProfileStore.update with that patch (mock the store) — proves the alias wires to the merge-write
    - Test: a patch containing a new structured key (e.g. {"dated_goals": [...]}) is passed through to store.update unchanged (no allow-list rejection)
    - Test: _handle_get_training_profile returns parseable JSON when store.load() includes a DatetimeWithNanoseconds-like value (use a stub object with .isoformat or a datetime) — json.loads on the result succeeds and the timestamp is a string
  </behavior>
  <action>
    Add tests mirroring the existing training-profile tool test style (mock `UserProfileStore` via the same patching approach already used in test_tools.py). Cover: (1) `_HANDLERS["update_plan"]` invokes the update handler and `store.update` is called with the structured patch; (2) the get handler returns JSON-serializable output when `store.load()` returns a dict containing a `datetime`/DatetimeWithNanoseconds-like `updated_at` (assert `json.loads(result)` succeeds and `result`'s updated_at is a str). Run only this file (suite-wide pytest SIGKILLs at ~45% — per-file is the supported path).
  </action>
  <verify>
    <automated>python -m pytest tests/test_tools.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_tools.py -x -q` exits 0
    - A test references "update_plan" and asserts store.update is called
    - A test feeds a datetime-bearing dict to _handle_get_training_profile and asserts json.loads succeeds
  </acceptance_criteria>
  <done>tests/test_tools.py passes and covers update_plan dispatch + JSON-safe get serialization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram brain → UserProfileStore.update | The brain (authenticated single-user Telegram) emits a `patch` dict that is merge-written to Firestore users/amit with no allow-list. |
| Firestore → json.dumps (get handler) | Stored values (DatetimeWithNanoseconds) cross back into a JSON-serialized tool result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | update patch can write arbitrary top-level keys (merge=True, no allow-list) | accept | Single-user authenticated brain; merge=True cannot delete existing keys, only add/overwrite. CONTEXT D requires the brain to confirm with Sir before recording (prompt discipline in Plan 04). No PII beyond Amit's own training data; low-value target. |
| T-21-04 | Information Disclosure / DoS | get_training_profile raises on DatetimeWithNanoseconds → tool error leaks stack / breaks turn | mitigate | Strip timestamp fields via _jsonsafe_doc before json.dumps (Task 1) — verified by a datetime-bearing test (Task 2). |
| T-21-05 | Tampering | Brain emits an attendance-boolean under weekly_split, drifting toward a rigid contract | mitigate | Prompt discipline (Plan 04) frames weekly_split as a template; schema scaffold (Plan 01) seeds no boolean fields. Update path stores whatever the brain sends, so the guard is the prompt, addressed in Plan 04. |
</threat_model>

<verification>
- `python -m pytest tests/test_tools.py -x -q` exits 0.
- get handler returns JSON-safe output for datetime-bearing docs.
- update_plan dispatches identically to update_training_profile.
</verification>

<success_criteria>
- ROADMAP SC-3: "update my bench goal to 105kg" / "change Thursday to rest day" → brain emits a patch via update_plan/update_training_profile, merges to Firestore, and (because render reads fresh each turn) is reasoned against next turn.
- Latent get-handler serialization bug closed before a real updated_at exists in the doc.
</success_criteria>

<output>
Create `.planning/phases/21-living-plan-ingestion/21-02-SUMMARY.md` when done.
</output>
