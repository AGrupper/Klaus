---
phase: 21-living-plan-ingestion
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - scripts/ingest_blueprint.py
  - tests/test_ingest_blueprint.py
autonomous: true
requirements: [PLAN-01]
must_haves:
  truths:
    - "Running scripts/ingest_blueprint.py populates users/amit with dated_goals, weekly_split, nutrition_targets, supplement_schedule, fueling_timeline, plan_start_date"
    - "The 16-week aerobic table (blueprint Section 4) is NOT ingested as a tracked target structure — at most a loose directional reference"
    - "No current-performance baseline is written — the profile holds Tier A targets only"
    - "Re-running the script is idempotent (merge=True); --dry-run prints without writing; --force re-ingests over existing v4.0 fields"
  artifacts:
    - path: "scripts/ingest_blueprint.py"
      provides: "Idempotent blueprint → structured Firestore ingest with dry-run/force"
      contains: "load_dotenv"
    - path: "tests/test_ingest_blueprint.py"
      provides: "Tests for the blueprint→dict builder (pure function, no Firestore)"
      contains: "dated_goals"
  key_links:
    - from: "scripts/ingest_blueprint.py"
      to: "memory/firestore_db.py UserProfileStore.update"
      via: "merge=True patch write of the structured dict"
      pattern: "UserProfileStore.*update"
    - from: "scripts/ingest_blueprint.py"
      to: "docs/hybrid_athlete_blueprint.md"
      via: "parse repo blueprint as source of truth"
      pattern: "hybrid_athlete_blueprint"
---

<objective>
Create `scripts/ingest_blueprint.py` — a one-time idempotent seed script that builds the v4.0 structured profile dict from `docs/hybrid_athlete_blueprint.md` (the in-repo source of truth) and writes it to `users/amit` via `UserProfileStore.update` (merge=True). It populates: `dated_goals` (Oct + Nov peaks with target dates), `weekly_split` (Section 2 AM/PM shape as label/modality/priority — NOT attendance flags), `nutrition_targets` (150g protein / 350g carbs daily), `supplement_schedule` + `fueling_timeline` (the 6-slot fueling architecture, Section 6), and `plan_start_date` = "2026-06-21". The script supports `--dry-run` (print payload, no write) and `--force` (re-ingest over existing v4.0 fields).

LOCKED narrowings (CONTEXT.md): (1) the 16-week aerobic pace/volume table (Section 4) is NEVER ingested as a tracked target structure — store at most a single loose-reference note (e.g. `nutrition_targets`/a `notes` string), never per-week paces/volumes as contracts. (2) NEVER hand-seed current-performance baselines — `dated_goals` carries targets only; no "current bench" / "current pace" field.

Purpose: Populate the structured profile from the durable blueprint so re-ingest stays safe and the blueprint remains source-of-truth.
Output: The ingest script + a pure-function test for the blueprint→dict builder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-living-plan-ingestion/21-CONTEXT.md
@.planning/phases/21-living-plan-ingestion/21-PATTERNS.md
@docs/hybrid_athlete_blueprint.md

<interfaces>
<!-- Script structure analogs + the write target. -->
From scripts/backfill_notion_titles.py (structure analog):
- sys.path.insert(0, str(Path(__file__).parent.parent))
- load_dotenv(override=True)   # INVARIANT
- argparse with --dry-run; def main(); if __name__ == "__main__": main()

From memory/firestore_db.py:
- UserProfileStore(project_id, database).update(patch)  # merge=True, idempotent re-run safe
- _SCAFFOLD v4.0 keys (Plan 01): dated_goals, weekly_split, nutrition_targets, supplement_schedule, fueling_timeline, plan_start_date

Blueprint structured content (docs/hybrid_athlete_blueprint.md):
- Section 1: Oct peak (100kg bench, 120kg squat, 1:25 HM); Nov peak (125 push-ups, 35 pull-ups, 9:30 3k, 55s 400m)
- Section 2: 7-day AM/PM split (Sun..Sat), each with AM + PM protocol labels
- Section 6: 6-slot fueling architecture + supplements (D3+K2/Omega3, Beta-Alanine, Creatine, Mg Glycinate/Zinc/Copper); daily 150g protein / 350g carbs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure blueprint→structured-dict builder + its test</name>
  <files>scripts/ingest_blueprint.py, tests/test_ingest_blueprint.py</files>
  <read_first>
    - docs/hybrid_athlete_blueprint.md (full — Sections 1, 2, 6 are the durable content; Section 4 is loose-reference-only)
    - .planning/phases/21-living-plan-ingestion/21-PATTERNS.md (ingest_blueprint.py section, lines 139-207; load_dotenv invariant, lines 390-394)
    - .planning/phases/21-living-plan-ingestion/21-CONTEXT.md (two narrowings, lines 39-41; dated_goals content, lines 85; weekly_split as priorities/modalities, line 84)
    - memory/firestore_db.py (UserProfileStore._SCAFFOLD v4.0 key names — must match exactly)
  </read_first>
  <behavior>
    - build_profile_dict() (pure, no Firestore, no env) returns a dict with exactly the six structured keys
    - dated_goals is a list of dicts; includes an Oct-peak entry (metrics include bench 100kg, squat 120kg, HM 1:25) and a Nov-peak entry (125 push-ups, 35 pull-ups, 9:30 3k, 55s 400m), each with a target_date
    - weekly_split has 7 day keys (sunday..saturday); each day has "am" and "pm" objects with keys label, modality, priority; NO key named done/completed/attended/attendance anywhere
    - nutrition_targets includes protein_g 150 and carbs_g 350
    - fueling_timeline is a list of 6 ordered slot dicts (pre-am-run, post-am-run, mid-day, pm-pre-lift, pm-post-lift, pre-bed); supplement_schedule lists the supplements tied to their slots
    - plan_start_date == "2026-06-21"
    - The 16-week aerobic table is NOT present as per-week target rows — at most one loose-reference string; assert no key/list holds 16 weekly pace/volume entries
    - No current-performance/baseline key (e.g. current_bench, current_pace) exists
  </behavior>
  <action>
    Create `scripts/ingest_blueprint.py` with the script boilerplate from PATTERNS.md (sys.path bootstrap, `from dotenv import load_dotenv; load_dotenv(override=True)`, logging, argparse). Implement `build_profile_dict() -> dict` as a PURE function (no env, no Firestore) that returns the structured payload — hard-coding the durable blueprint content from Sections 1/2/6 (the blueprint is small and stable; parsing prose markdown into structured fields is more fragile than transcribing the known content, so transcribe it into Python literals and cite the blueprint as the source-of-truth comment). dated_goals = Oct + Nov peak entries (target_date, goal_label, metrics dict). weekly_split = 7 day keys each {am:{label,modality,priority}, pm:{label,modality,priority}} derived from the Section 2 table (modality ∈ run/lift/calisthenics/mobility/rest; priority a short string). nutrition_targets = {protein_g:150, carbs_g:350, fueling_slots: [...slot labels...]}. fueling_timeline = 6 ordered slot dicts (timing, food, optional supplements). supplement_schedule = list mapping supplements to slots. plan_start_date = "2026-06-21". Do NOT transcribe Section 4's 16-week table as target rows — at most a single `aerobic_reference_note` string under nutrition_targets or a top-level loose note, never the paces/volumes as tracked targets. Do NOT add any current-performance field. Then create `tests/test_ingest_blueprint.py` importing `build_profile_dict` and asserting the behaviors above (pure function → no mocking needed).
  </action>
  <verify>
    <automated>python -m pytest tests/test_ingest_blueprint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import sys; sys.path.insert(0,'.'); from scripts.ingest_blueprint import build_profile_dict as b; d=b(); assert set(['dated_goals','weekly_split','nutrition_targets','supplement_schedule','fueling_timeline','plan_start_date']).issubset(d); assert d['plan_start_date']=='2026-06-21'; assert len(d['weekly_split'])==7; assert len(d['fueling_timeline'])==6; print('OK')"` prints OK
    - `python -c "import sys; sys.path.insert(0,'.'); from scripts.ingest_blueprint import build_profile_dict as b; import json; s=json.dumps(b()).lower(); assert 'attendance' not in s and 'completed' not in s; print('no-attendance OK')"` prints no-attendance OK
    - `python -m pytest tests/test_ingest_blueprint.py -x -q` exits 0
  </acceptance_criteria>
  <done>build_profile_dict returns the six structured keys with Oct/Nov dated goals, 7-day template split (no attendance booleans), 6-slot fueling, plan_start_date 2026-06-21; no 16-week target table, no baselines; test passes.</done>
</task>

<task type="auto">
  <name>Task 2: CLI wiring — write to Firestore with --dry-run / --force idempotency</name>
  <files>scripts/ingest_blueprint.py</files>
  <read_first>
    - scripts/backfill_notion_titles.py (argparse + main() entry pattern, lines 85-91)
    - scripts/ingest_garmin_zip.py (idempotent main() + env setup, lines 1-16, 444-475)
    - memory/firestore_db.py (UserProfileStore.update merge=True; load() to detect existing v4.0 fields for the --force gate)
    - .planning/phases/21-living-plan-ingestion/21-PATTERNS.md (idempotent ingest main() + bootstrap idempotency gate, lines 172-207, 397-404)
  </read_first>
  <behavior>
    - `python scripts/ingest_blueprint.py --dry-run` prints the JSON payload and performs NO Firestore write
    - Without flags: if users/amit already has v4.0 fields (e.g. plan_start_date present) the script declines to overwrite and instructs to pass --force; otherwise it writes via UserProfileStore.update
    - `--force` writes (merge=True) regardless of existing v4.0 fields — safe re-ingest after blueprint edits
    - Re-running with --force is idempotent (merge=True produces the same document)
    - main() exits 1 with a logged error on failure (matches ingest_garmin_zip.py)
  </behavior>
  <action>
    Add `main()` with argparse flags `--dry-run` and `--force`. Build the payload via `build_profile_dict()`. On `--dry-run`: `print(json.dumps(payload, indent=2, ensure_ascii=False))` and return (no client construction needed beyond what dry-run requires — prefer NOT touching Firestore at all in dry-run). Otherwise construct `UserProfileStore(project_id=os.environ["GCP_PROJECT_ID"], database=os.environ.get("FIRESTORE_DATABASE", "(default)"))`, call `.load()`, and if `plan_start_date` is already set and `--force` was NOT passed, log a warning ("v4.0 fields present — pass --force to re-ingest") and exit 0 without writing; else call `.update(payload)`. Wrap in try/except → `logger.error(..., exc_info=True); sys.exit(1)` on failure. Add `if __name__ == "__main__": main()`. Keep `load_dotenv(override=True)`.
  </action>
  <verify>
    <automated>python scripts/ingest_blueprint.py --dry-run | python -c "import sys,json; d=json.load(sys.stdin); assert d['plan_start_date']=='2026-06-21'; assert len(d['weekly_split'])==7; print('dry-run OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/ingest_blueprint.py --dry-run` prints valid JSON with plan_start_date 2026-06-21 and writes nothing (no Firestore client error even without GCP creds in a dry run)
    - `grep -n "argparse\|--dry-run\|--force\|load_dotenv(override=True)" scripts/ingest_blueprint.py` matches all four
    - `grep -n "plan_start_date" scripts/ingest_blueprint.py` shows the --force existing-field gate
  </acceptance_criteria>
  <done>Script runs with --dry-run (no write), guards against accidental overwrite, re-ingests safely with --force, exits 1 on failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local operator → Firestore users/amit | The ingest script (run by Amit locally) writes the structured profile. Source data is an in-repo committed doc, not external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering / data loss | Re-running ingest overwrites good user-edited data (e.g. Amit updated a goal via update_plan, then a re-ingest clobbers it) | mitigate | Without --force the script declines to overwrite when v4.0 fields already exist (existing-field gate, Task 2); merge=True never deletes keys the payload omits. Re-ingest is intentional only via --force. |
| T-21-07 | Tampering | Dry-run accidentally writes | mitigate | --dry-run prints only and returns before any Firestore write (Task 2 behavior + verify). |
| T-21-SC | Tampering | No package-manager installs in this plan (reuses existing deps) | accept | No new pip installs — script imports only existing project modules + stdlib. No supply-chain surface introduced. |
</threat_model>

<verification>
- `python -m pytest tests/test_ingest_blueprint.py -x -q` exits 0.
- `python scripts/ingest_blueprint.py --dry-run` emits valid structured JSON, no write.
- No 16-week target table and no current-performance baseline in the payload (grep/json assertions).
</verification>

<success_criteria>
- ROADMAP SC-1: after `python scripts/ingest_blueprint.py` (or --force), `UserProfileStore.load()` returns non-empty dated_goals, weekly_split, nutrition_targets, supplement_schedule, fueling_timeline, plan_start_date.
- PLAN-01 narrowings honored: no tracked 16-week table, no hand-seeded baselines.
</success_criteria>

<output>
Create `.planning/phases/21-living-plan-ingestion/21-03-SUMMARY.md` when done.
</output>
