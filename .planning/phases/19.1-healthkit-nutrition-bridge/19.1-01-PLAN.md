---
phase: 19.1-healthkit-nutrition-bridge
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/fixtures/healthkit_payload_sample.json
  - tests/test_healthkit_payload_schema.py
  - tests/test_healthkit_tool.py
autonomous: false  # Task-1 requires operator-side iPhone work (real-device JSON capture)
requirements:
  - HEALTHKIT-01

must_haves:
  truths:
    - "tests/fixtures/healthkit_payload_sample.json exists and contains the literal top-level key 'samples' with at least one sample element."
    - "Each sample element contains the literal keys 'uuid', 'start_date', and 'samples_by_type' (and is therefore a valid HealthKitSample shape)."
    - "tests/test_healthkit_payload_schema.py runs (skipping the model-validate test until Plan 02 lands HealthKitPayload, but exercising the JSON shape asserts immediately)."
    - ".planning/REQUIREMENTS.md lists HEALTHKIT-01 through HEALTHKIT-08 with statuses 'Planned' and the coverage footer reads '53/53 requirements mapped (26 → Phase 19, 8 → Phase 19.1, 19 → Phase 20). No orphans.'"
    - ".planning/ROADMAP.md contains a Phase 19.1 details block (Goal, Depends on, Requirements, Success Criteria, Plans, UI hint) inserted between Phase 19 and Phase 20."
  artifacts:
    - path: "tests/fixtures/healthkit_payload_sample.json"
      provides: "Real-device-captured locked JSON fixture for the iOS Shortcut wire format"
      contains: "samples"
    - path: "tests/test_healthkit_payload_schema.py"
      provides: "Schema-lock harness mirroring tests/test_evals.py:23-60 (Pitfall-6 pattern)"
      contains: "_REQUIRED_TOP_KEYS"
    - path: "tests/test_healthkit_tool.py"
      provides: "Placeholder file so Plan 02 RED commits have a target; one trivial passing test"
    - path: ".planning/REQUIREMENTS.md"
      provides: "8 new HEALTHKIT-* IDs under a Phase 19.1 subsection + appended traceability rows + updated coverage footer"
      contains: "HEALTHKIT-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 19.1 row + details block + progress table row + bumped header counts"
      contains: "Phase 19.1: HealthKit Nutrition Bridge"
  key_links:
    - from: "tests/fixtures/healthkit_payload_sample.json"
      to: "tests/test_healthkit_payload_schema.py"
      via: "json.load + per-key assertions on _REQUIRED_TOP_KEYS / _REQUIRED_SAMPLE_KEYS"
      pattern: "with open\\(.*healthkit_payload_sample.json"
    - from: ".planning/ROADMAP.md Phase 19.1 details block"
      to: ".planning/REQUIREMENTS.md HEALTHKIT-01..08"
      via: "Requirements line in roadmap details references the 8 IDs"
      pattern: "HEALTHKIT-01, HEALTHKIT-02"
---

<objective>
Wave 0: lock the iOS Shortcut wire format with a REAL-DEVICE-captured JSON fixture, register the 8 HEALTHKIT-* requirement IDs in REQUIREMENTS.md, and insert Phase 19.1 in ROADMAP.md. This wave is the critical-path blocker for every downstream plan — if the fixture is hand-written from imagination it will lock the wrong shape and the first real iPhone push will 422 (per RESEARCH.md Q1/Q7).

Purpose: Without the real-device fixture, Plan 02's Pydantic model locks the wrong shape; without the requirement IDs registered, Plans 02-05 have no traceability anchors.

Output: 1 fixture file, 2 test files (one schema-lock, one placeholder), and 2 planning-document edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/CODING_STANDARDS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19.1-healthkit-nutrition-bridge/19.1-CONTEXT.md
@.planning/phases/19.1-healthkit-nutrition-bridge/19.1-RESEARCH.md
@.planning/phases/19.1-healthkit-nutrition-bridge/19.1-PATTERNS.md
@tests/test_evals.py
</context>

<interfaces>
<!-- Schema-lock test pattern verbatim from tests/test_evals.py:23-60 -->

From tests/test_evals.py (existing Pitfall-6 schema-lock pattern):
```python
_FIXTURE_GLOB = "evals/tick_brain/fixtures/*.json"
_REQUIRED_TOP_KEYS = {"id", "captured_at", "situation_snapshot", "trigger_type", "ground_truth"}
_REQUIRED_SNAPSHOT_KEYS = {"calendar", "ticktick_overdue", ...}

class TestFixtureSchema:
    def test_at_least_five_fixtures(self):
        assert len(_all_fixture_paths()) >= 5

    @pytest.mark.parametrize("path", _all_fixture_paths())
    def test_each_fixture_is_valid_json(self, path):
        with open(path) as f:
            json.loads(f.read())
```

For HealthKit (per 19.1-RESEARCH.md "Pydantic schema-lock test pattern"):
```python
_FIXTURE_PATH = "tests/fixtures/healthkit_payload_sample.json"
_REQUIRED_TOP_KEYS = {"samples"}
_REQUIRED_SAMPLE_KEYS = {"uuid", "start_date", "samples_by_type"}
```
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture real-device HealthKit fixture (OPERATOR — iPhone work)</name>
  <files>tests/fixtures/healthkit_payload_sample.json</files>
  <read_first>
    - .planning/phases/19.1-healthkit-nutrition-bridge/19.1-RESEARCH.md (sections Q1, Q7, Q8 — explain why this MUST be device-captured, not hand-written)
    - .planning/phases/19.1-healthkit-nutrition-bridge/19.1-CONTEXT.md (D-02, D-03, D-12, D-13, D-15 — the Shortcut shape this fixture must reflect)
  </read_first>
  <manual_intervention required="true">
    <what-built>This is the empirical-capture step. The Pydantic model in Plan 02 will be derived from this file, so the file MUST be the actual JSON the iOS Shortcut emits — not a fabrication.</what-built>
    <how-to-verify>
      Operator (Amit) does the following on the iPhone:
      1. Build the Lifesum-close Shortcut per the design in 19.1-CONTEXT.md `<specifics>` block (4× Find Health Samples — Dietary Energy/Protein/Carbs/Fat, last 2 hours; Repeat With Each; Dictionary builder per sample with keys `uuid`, `start_date`, `samples_by_type`, optional `metadata`/`food_item`; wrap as `{samples: [...]}`).
      2. Point Get Contents of URL at a debug catcher — recommended `webhook.site` (free, browser-based, no install). Use the random URL it gives you.
      3. Log a test meal in Lifesum (any food, any size). Close Lifesum to fire the trigger.
      4. Observe the POST body on webhook.site. Copy the raw JSON.
      5. mkdir -p tests/fixtures && save the raw JSON verbatim to tests/fixtures/healthkit_payload_sample.json.
      6. Verify the file with: `python -c "import json; data = json.load(open('tests/fixtures/healthkit_payload_sample.json')); assert 'samples' in data and len(data['samples']) > 0, 'fixture missing samples'; print(f'OK — {len(data[\"samples\"])} sample(s) captured')"`
    </how-to-verify>
    <resume-signal>Operator reports "captured" + paste the path/byte count, OR reports a snag (e.g. webhook.site rejected payload, Shortcut wouldn't run); if snag, Claude troubleshoots before resuming.</resume-signal>
  </manual_intervention>
  <action>
    Operator-led iPhone work. Claude's job here is to wait for the operator signal and then validate the captured file. After operator signals "captured":
    1. Run `python -c "import json; data = json.load(open('tests/fixtures/healthkit_payload_sample.json')); assert 'samples' in data and isinstance(data['samples'], list) and len(data['samples']) > 0; s = data['samples'][0]; assert 'uuid' in s and 'start_date' in s and 'samples_by_type' in s, f'sample missing required keys: {list(s.keys())}'; print('FIXTURE OK')"` and confirm exit 0.
    2. If the fixture is missing 'metadata' on every sample, that is acceptable (Lifesum may not write it; the normalizer's hour-bucket fallback handles this per D-13). Do NOT inject a fabricated metadata field.
    3. If samples_by_type values arrive as strings (per RESEARCH.md Q1), leave them as strings — Plan 02's `field_validator(mode='before')` will coerce. Do NOT pre-coerce in the fixture.
    4. Commit with message `test(19.1): capture real-device HealthKit fixture for schema lock` once operator has approved the captured shape.
  </action>
  <acceptance_criteria>
    - File exists at literal path `tests/fixtures/healthkit_payload_sample.json`.
    - `python -c "import json; d = json.load(open('tests/fixtures/healthkit_payload_sample.json')); assert 'samples' in d and isinstance(d['samples'], list) and len(d['samples']) > 0"` exits 0.
    - At least one sample in `d['samples']` contains the literal string keys `uuid`, `start_date`, and `samples_by_type` (grep-verifiable via `python -c "import json; s = json.load(open('tests/fixtures/healthkit_payload_sample.json'))['samples'][0]; assert {'uuid','start_date','samples_by_type'}.issubset(s.keys())"`).
    - File is NOT hand-written from this plan — Claude must NOT generate the JSON content itself. If the operator signals failure to capture, the plan PAUSES and is not unblocked until a real-device capture lands.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json; d = json.load(open('tests/fixtures/healthkit_payload_sample.json')); assert 'samples' in d and isinstance(d['samples'], list) and len(d['samples']) > 0; s = d['samples'][0]; assert {'uuid','start_date','samples_by_type'}.issubset(s.keys()); print('FIXTURE OK')"</automated>
  </verify>
  <done>The fixture file exists, parses as JSON, has at least one sample with the 3 required keys, and the operator has confirmed it came from a real on-device fire (NOT fabricated).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write schema-lock test + placeholder normalizer test file (RED, then partial GREEN)</name>
  <files>tests/test_healthkit_payload_schema.py, tests/test_healthkit_tool.py</files>
  <read_first>
    - tests/test_evals.py (lines 23-60 — _REQUIRED_SNAPSHOT_KEYS pattern, parametrized fixture loop, Pitfall-6 lineage)
    - tests/fixtures/healthkit_payload_sample.json (the file captured in Task 1 — drives what the schema-lock test will accept)
    - .planning/phases/19.1-healthkit-nutrition-bridge/19.1-RESEARCH.md "Pydantic schema-lock test pattern" and "Validation Architecture" sections
    - .planning/phases/19.1-healthkit-nutrition-bridge/19.1-PATTERNS.md "NEW tests/test_healthkit_payload_schema.py" section
  </read_first>
  <behavior>
    - Test 1: `test_fixture_exists_and_loads_as_json` — opens tests/fixtures/healthkit_payload_sample.json, json.loads it, asserts a dict result. PASSES immediately after Task 1.
    - Test 2: `test_fixture_has_required_top_keys` — asserts `_REQUIRED_TOP_KEYS == {"samples"}` and `set(data.keys()) >= _REQUIRED_TOP_KEYS`. PASSES immediately.
    - Test 3: `test_each_sample_has_required_keys` — parametrized over `data["samples"]`; for each sample asserts `_REQUIRED_SAMPLE_KEYS == {"uuid", "start_date", "samples_by_type"}` is a subset of sample.keys(). PASSES immediately.
    - Test 4: `test_fixture_loads_via_pydantic_model` — imports `HealthKitPayload` from `mcp_tools.healthkit_tool`; calls `HealthKitPayload.model_validate(data)`. EXPECTED RED in this plan (module doesn't exist yet); turns GREEN in Plan 02. Use `pytest.importorskip("mcp_tools.healthkit_tool")` so the test SKIPs cleanly while the module is absent, then unconditionally fails if it exists but doesn't validate.
    - Placeholder file `tests/test_healthkit_tool.py`: single trivial test `def test_module_will_be_created_in_plan_02(): pass` — Plan 02's TDD RED commits will replace this.
  </behavior>
  <action>
    Create `tests/test_healthkit_payload_schema.py` mirroring tests/test_evals.py:23-60 structure verbatim. Module-level constants:
    ```python
    _FIXTURE_PATH = "tests/fixtures/healthkit_payload_sample.json"
    _REQUIRED_TOP_KEYS = {"samples"}
    _REQUIRED_SAMPLE_KEYS = {"uuid", "start_date", "samples_by_type"}
    ```
    Helper: `def _load() -> dict: with open(_FIXTURE_PATH) as f: return json.load(f)`.

    Class `TestHealthKitFixtureSchema` with:
    - `test_fixture_exists_and_loads_as_json` — calls _load(), asserts isinstance(result, dict).
    - `test_fixture_has_required_top_keys` — asserts `_REQUIRED_TOP_KEYS.issubset(set(_load().keys()))`.
    - Parametrized `test_each_sample_has_required_keys` — generates ids from sample uuid prefix; asserts `_REQUIRED_SAMPLE_KEYS.issubset(set(sample.keys()))`.
    - `test_fixture_loads_via_pydantic_model` — uses `pytest.importorskip("mcp_tools.healthkit_tool")` to SKIP-not-fail while Plan 02 is pending; once module lands, calls `HealthKitPayload.model_validate(_load())` and expects no exception.

    Create `tests/test_healthkit_tool.py` with:
    ```python
    """Placeholder — Plan 19.1-02 fills this in (HEALTHKIT-02, HEALTHKIT-03)."""

    def test_module_will_be_created_in_plan_02():
        pass
    ```

    Commit: `test(19.1): add HealthKit payload schema-lock harness (Plan 01)`.
  </action>
  <acceptance_criteria>
    - File `tests/test_healthkit_payload_schema.py` contains the literal string `_REQUIRED_TOP_KEYS = {"samples"}` (grep: `grep -F '_REQUIRED_TOP_KEYS = {"samples"}' tests/test_healthkit_payload_schema.py`).
    - File contains the literal string `_REQUIRED_SAMPLE_KEYS = {"uuid", "start_date", "samples_by_type"}` (grep: `grep -F '"start_date", "samples_by_type"' tests/test_healthkit_payload_schema.py`).
    - File contains `pytest.importorskip("mcp_tools.healthkit_tool")` so test_fixture_loads_via_pydantic_model SKIPs cleanly until Plan 02 lands the module.
    - `pytest tests/test_healthkit_payload_schema.py -x` exits 0 (3 tests pass, 1 test skips with reason matching importorskip).
    - `pytest tests/test_healthkit_tool.py -x` exits 0 (placeholder passes).
    - Full suite `pytest tests/ -x` still green (no regression in 572 baseline).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_healthkit_payload_schema.py tests/test_healthkit_tool.py -x -v</automated>
  </verify>
  <done>Schema-lock harness file exists with the literal _REQUIRED_TOP_KEYS / _REQUIRED_SAMPLE_KEYS constants; 3 immediate-PASS tests + 1 SKIP-until-Plan-02 test; placeholder normalizer test file exists; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Register HEALTHKIT-01..08 in REQUIREMENTS.md and insert Phase 19.1 in ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current file — to find the v3.0 Phase 19 subsections; the new subsection goes after the "Smart-Agent Prompt Integration (Phase 19)" block and before "TrainingLogStore (Phase 20)")
    - .planning/ROADMAP.md (current file — to find the v3.0 header at line ~18, the phase list at lines 20-23, the Phase 19 details block at 27-44, and the Progress table around line 60)
    - .planning/phases/19.1-healthkit-nutrition-bridge/19.1-RESEARCH.md sections "Requirement ID Recommendations" and "ROADMAP.md Insertion Suggestion" — copy the wording VERBATIM from these sections.
  </read_first>
  <action>
    REQUIREMENTS.md edits (Edit tool, sequentially):
    1. Insert a new subsection `### HealthKit Nutrition Bridge (Phase 19.1)` between the existing `### Smart-Agent Prompt Integration (Phase 19)` block and `### TrainingLogStore (Phase 20)` block. Content: the 8-row HEALTHKIT-01..08 list with the exact descriptions from RESEARCH.md "Requirement ID Recommendations" table. Each item is a checkbox `- [ ] **HEALTHKIT-NN**: <description> (D-XX reference)`.
    2. In the Traceability table, append 8 new rows for HEALTHKIT-01..08 between the SCHEMA-* block (Phase 19) and the LOG-01 row (Phase 20). Each row: `| HEALTHKIT-NN | 19.1 | Planned |`.
    3. Replace the literal Coverage footer line `**Coverage:** 45/45 requirements mapped (26 → Phase 19, 19 → Phase 20). No orphans.` with `**Coverage:** 53/53 requirements mapped (26 → Phase 19, 8 → Phase 19.1, 19 → Phase 20). No orphans.`

    ROADMAP.md edits (Edit tool, sequentially — copy wording verbatim from RESEARCH.md "ROADMAP.md Insertion Suggestion"):
    1. Replace `**Phases:** 2 · **Plans:** 5 (Phase 19) + TBD (Phase 20) · **Requirements:** 45` with `**Phases:** 3 · **Plans:** 5 (Phase 19) + TBD (Phase 19.1) + TBD (Phase 20) · **Requirements:** 53`.
    2. In the `### Phases` summary list, append `*(SC #2 blocked pending Phase 19.1)*` to the Phase 19 line and insert a new Phase 19.1 row before Phase 20 with the wording from RESEARCH.md.
    3. Insert a full `### Phase 19.1: HealthKit Nutrition Bridge` details block (Goal, Depends on, Requirements, Success Criteria 1-6, Plans: TBD, UI hint: yes) between the Phase 19 details block and the Phase 20 details block. Use the VERBATIM text from RESEARCH.md "ROADMAP.md Insertion Suggestion" — the Requirements line MUST read `HEALTHKIT-01, HEALTHKIT-02, HEALTHKIT-03, HEALTHKIT-04, HEALTHKIT-05, HEALTHKIT-06, HEALTHKIT-07, HEALTHKIT-08`.
    4. In the Progress table, change the Phase 19 row Status to `Complete (SC #2 blocked pending 19.1)` and insert a new row `| 19.1. HealthKit Nutrition Bridge | 0/5 | In Progress | — |` before the Phase 20 row.

    Commit: `docs(19.1): register HEALTHKIT-01..08 + insert Phase 19.1 in ROADMAP`.
  </action>
  <acceptance_criteria>
    - `grep -c "HEALTHKIT-0[1-8]" .planning/REQUIREMENTS.md` returns at least 16 (8 requirements + 8 traceability rows).
    - `grep -F "**Coverage:** 53/53 requirements mapped (26 → Phase 19, 8 → Phase 19.1, 19 → Phase 20). No orphans." .planning/REQUIREMENTS.md` returns 1 line.
    - `grep -F "### Phase 19.1: HealthKit Nutrition Bridge" .planning/ROADMAP.md` returns 1 line.
    - `grep -F "HEALTHKIT-01, HEALTHKIT-02, HEALTHKIT-03, HEALTHKIT-04, HEALTHKIT-05, HEALTHKIT-06, HEALTHKIT-07, HEALTHKIT-08" .planning/ROADMAP.md` returns 1 line.
    - `grep -F "**Phases:** 3 · **Plans:** 5 (Phase 19) + TBD (Phase 19.1) + TBD (Phase 20) · **Requirements:** 53" .planning/ROADMAP.md` returns 1 line.
    - Phase 19 traceability row count unchanged (26); coverage footer math is consistent.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "HEALTHKIT-0[1-8]" .planning/REQUIREMENTS.md | awk "{ if (\$1 < 16) exit 1 }" && grep -F "53/53 requirements mapped" .planning/REQUIREMENTS.md && grep -F "### Phase 19.1: HealthKit Nutrition Bridge" .planning/ROADMAP.md && grep -F "HEALTHKIT-01, HEALTHKIT-02, HEALTHKIT-03, HEALTHKIT-04, HEALTHKIT-05, HEALTHKIT-06, HEALTHKIT-07, HEALTHKIT-08" .planning/ROADMAP.md'</automated>
  </verify>
  <done>REQUIREMENTS.md has 8 new HEALTHKIT-* entries + 8 traceability rows + updated coverage footer; ROADMAP.md has bumped header counts, summary-list row, details block, and progress-table row for Phase 19.1; all grep checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → repo | Operator pastes real-device JSON into a test fixture. Boundary: only test-realm data, no live secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01-01 | Information Disclosure | tests/fixtures/healthkit_payload_sample.json | mitigate | Operator confirms the captured JSON contains no PII beyond meal-size numerics and a HKObject UUID (which is not a personal identifier — it is per-sample, not per-user). Food-item strings may carry meal descriptions; operator reviews before commit. |
| T-19.1-01-02 | Tampering | .planning/REQUIREMENTS.md coverage footer | accept | Hand-edited docs; no automated reconciliation. Risk acceptable for solo-developer flow. |
| T-19.1-01-03 | Repudiation | Wave 0 fixture provenance | accept | Operator confirms via Telegram/chat that capture was real-device. No audit log beyond commit message. |
</threat_model>

<verification>
- `pytest tests/test_healthkit_payload_schema.py tests/test_healthkit_tool.py -x -v` → 4 tests pass + 1 skip (importorskip) on schema file; 1 pass on placeholder.
- `pytest tests/ -x` → no regression in 572-baseline suite.
- `grep` checks pass on REQUIREMENTS.md (HEALTHKIT IDs, coverage footer) and ROADMAP.md (Phase 19.1 block).
</verification>

<success_criteria>
- Real-device fixture captured (not fabricated) and committed.
- Schema-lock harness in place; will turn fully GREEN once Plan 02 lands HealthKitPayload.
- HEALTHKIT-01..08 registered in REQUIREMENTS.md; Phase 19.1 inserted in ROADMAP.md.
- Plans 02-05 can reference HEALTHKIT-* IDs in their `requirements` frontmatter field without orphan warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-healthkit-nutrition-bridge/19.1-01-SUMMARY.md` summarizing:
- Fixture file size and sample count
- Whether `metadata` was present on captured samples (informs Plan 02's normalizer test expectations)
- Whether `samples_by_type` values arrived as strings vs numbers (informs Plan 02's field_validator coverage)
- 8 new requirement IDs registered, ROADMAP.md insertion confirmed
</output>
