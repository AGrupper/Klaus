---
phase: 19.1-healthkit-nutrition-bridge
plan: 04
subsystem: observability-and-docs
status: complete
completed: 2026-05-29
tags: [heartbeat, self-manifest, deployment-docs, docs-tests]
requirements:
  - HEALTHKIT-06
  - HEALTHKIT-07
dependency-graph:
  requires:
    - core/heartbeat.py::_CRON_MAX_STALENESS_HOURS (existing)
    - core/self_manifest.py::_render_manifest (existing)
    - mcp_tools/google_fit_tool.py (existing — module docstring only)
    - docs/DEPLOYMENT.md §20 TICK_BRAIN_API_KEY pattern (analog)
  provides:
    - heartbeat alarm at 48h staleness for the iPhone Shortcut healthkit-sync push bridge
    - docs/SELF.md "Push endpoints" section so the brain can answer "how do my meals reach me?"
    - docs/DEPLOYMENT.md §22 (push-driven endpoints registry) + §23 (HEALTHKIT_WEBHOOK_TOKEN rotation runbook)
    - tests/test_heartbeat.py — 2 new regression guards
    - tests/test_docs.py — 4 new grep-style tests
  affects:
    - Plan 05 (operator runbook + CLI smoke script can now point at §23 for token mint/rotation)
tech-stack:
  added: []
  patterns:
    - Inline section emission inside _render_manifest's lines += [...] block (no helper function — PATTERNS.md convention)
    - Mirror of §20 secret-rotation runbook shape for the new §23
key-files:
  created: []
  modified:
    - core/heartbeat.py (+1 dict entry, +1 inline comment)
    - tests/test_heartbeat.py (+2 module-level test functions)
    - core/self_manifest.py (+8 lines emitter block, post-§4)
    - mcp_tools/google_fit_tool.py (+4 docstring lines)
    - docs/SELF.md (regenerated; +8 lines for new section)
    - docs/DEPLOYMENT.md (+59 lines for §22 + §23)
    - tests/test_docs.py (+4 module-level test functions)
decisions:
  - "Heartbeat dict insertion is the LAST entry in _CRON_MAX_STALENESS_HOURS (line 117) — no reordering of existing keys per plan invariant."
  - "self_manifest's push-endpoints emission is INLINE inside _render_manifest's lines += [...] block; no separate _render_push_endpoints helper. Matches PATTERNS.md convention and §4 Cron Jobs precedent."
  - "DEPLOYMENT.md section numbering is §22 + §23 (NOT §23 + §24 as CONTEXT.md D-24 said). Confirmed live file ended at §21 — RESEARCH.md Q10 correction applied."
  - "Secret name uses lowercase 'klaus-healthkit-webhook-token' per CLAUDE.md GCP/Pinecone resource-naming invariant."
  - "Plain bash code fences (no triple-backtick-bash inside markdown code-fence-bash) — kept the rotation snippets as separate fenced blocks instead of nesting, which avoids markdown parser quirks."
metrics:
  completed: 2026-05-29
  tasks: 3
  files-created: 0
  files-modified: 7
  tests-added: 4
  full-suite-after: 595 passed / 3 skipped
  full-suite-before: 589 passed / 3 skipped
---

# Phase 19.1 Plan 04: Observability + Self-Knowledge + Operator-Runbook Wiring Summary

**Substantive one-liner:** Wired the new HealthKit push webhook into heartbeat staleness alarms, SELF.md brain-facing manifest, the google_fit legacy-marker, and the DEPLOYMENT.md operator runbook (§22 + §23 — NOT §23 + §24) — all backed by 4 new docs/heartbeat regression tests.

## What landed

### Task 1 — Heartbeat staleness key (HEALTHKIT-06)

- `core/heartbeat.py:117` — appended `"healthkit-sync": 48,` as the new LAST entry of `_CRON_MAX_STALENESS_HOURS`, with inline comment `# Phase 19.1 — D-18; 48h tolerance for iPhone Shortcut push bridge`. No reordering of existing keys.
- `tests/test_heartbeat.py` — appended 2 module-level functions at end of file:
  - `test_healthkit_sync_staleness_threshold_is_48_hours` — asserts literal `== 48`
  - `test_healthkit_sync_present_in_staleness_dict` — guards against accidental key removal
- Net: 19 → 21 heartbeat tests; suite green.

### Task 2 — SELF.md push-endpoints section + google_fit legacy marker (HEALTHKIT-07 / D-21 / D-16)

- `core/self_manifest.py` — inserted a new `lines += [...]` block immediately after the §4 Cron Jobs emission (and the `<!-- TODO Phase 18 -->` marker that lives inside it), emitting an `## Push endpoints` section with a single row for `/cron/healthkit-sync`. The insertion is INLINE per PATTERNS.md (no `_render_push_endpoints` helper).
- `mcp_tools/google_fit_tool.py` — augmented module docstring with a 4-line legacy paragraph before the closing `"""`. Zero behavior change; only the docstring.
- `docs/SELF.md` — regenerated via `python core/self_manifest.py`. New section count is 8 (was 7), line count went from 113 to 119. Verified: `grep -F "## Push endpoints" docs/SELF.md` → 1 line, `grep -F "/cron/healthkit-sync" docs/SELF.md` → 1 line.
- `tests/test_docs.py` — appended 2 module-level functions:
  - `test_self_md_lists_healthkit_push_endpoint` — asserts all 4 strings present
  - `test_google_fit_tool_marked_legacy` — uses `google_fit_tool.__doc__`

### Task 3 — DEPLOYMENT.md §22 + §23 + 2 docs tests

- `docs/DEPLOYMENT.md` — appended two new sections after §21 Firestore Composite Indexes (the live last section before this plan):
  - **§22 Push-driven endpoints** — single-row table contrasting Cloud Scheduler (OIDC JWT, §19) with verified-external-client push (shared-secret bearer). First entry: `/cron/healthkit-sync | iPhone Shortcut (Personal Automation) | shared-secret bearer (HEALTHKIT_WEBHOOK_TOKEN) | 19.1`.
  - **§23 HEALTHKIT_WEBHOOK_TOKEN Secret** — mirrors §20 (TICK_BRAIN_API_KEY) shape: secret name `klaus-healthkit-webhook-token` (lowercase per CLAUDE.md invariant), Cloud Run binding, `secrets.token_urlsafe(32)` mint, 4-step rotation (add version → update Cloud Run → paste into iOS Shortcut header → disable old version), kill-switch via `gcloud secrets versions disable`.
- `tests/test_docs.py` — appended 2 module-level functions:
  - `test_deployment_md_section_22_push_endpoints`
  - `test_deployment_md_section_23_healthkit_secret` (4 assertions: section header, lowercase secret name, 32-byte mint command, disable command)
- File now has 6 occurrences of `klaus-healthkit-webhook-token` (acceptance criteria required ≥3). Confirmed no `## 24.` heading exists.

## Plan output-spec answers

The plan's `<output>` block asked four specific questions:

1. **Exact insertion-line for `healthkit-sync` in core/heartbeat.py dict** — line **117** (last entry of the dict, immediately before the closing `}`).
2. **Whether self_manifest emitter required a separate helper function or inline-only addition worked** — **inline-only**, added as a new `lines += [...]` statement immediately after the existing §4 Cron Jobs block. Matches PATTERNS.md convention and the §4 precedent. No new helper function.
3. **docs/SELF.md line-count delta + section count delta** — line count 113 → 119 (Δ +6 source lines, +8 emitter strings, some merge); section count (`content.count('\n## ')`) 7 → **8** (Δ +1).
4. **docs/DEPLOYMENT.md final-section number** — **§23**. File ends at `## 23. HEALTHKIT_WEBHOOK_TOKEN Secret` (was §21 before this plan).

## Acceptance verification (all 3 tasks)

### Task 1
- `grep -F '"healthkit-sync": 48' core/heartbeat.py` → 1 line at line 117 ✓
- The match is inside `_CRON_MAX_STALENESS_HOURS` (verified by inspecting surrounding `{` and `}` lines) ✓
- `grep -F "test_healthkit_sync_staleness_threshold_is_48_hours" tests/test_heartbeat.py` → 1 line ✓
- `grep -F "test_healthkit_sync_present_in_staleness_dict" tests/test_heartbeat.py` → 1 line ✓
- `pytest tests/test_heartbeat.py -x` → 21 passed ✓

### Task 2
- `grep -F "## Push endpoints" core/self_manifest.py` → 1 line ✓
- `grep -F "/cron/healthkit-sync" core/self_manifest.py` → 1 line ✓
- `grep -F "iPhone Shortcut" core/self_manifest.py` → 1 line ✓
- `grep -F "Legacy Android-source path retained" mcp_tools/google_fit_tool.py` → 1 line ✓
- `grep -F "mcp_tools/healthkit_tool.py" mcp_tools/google_fit_tool.py` → 1 line ✓
- `grep -F "## Push endpoints" docs/SELF.md` → 1 line ✓
- `grep -F "/cron/healthkit-sync" docs/SELF.md` → 1 line ✓
- `grep -F "test_self_md_lists_healthkit_push_endpoint" tests/test_docs.py` → 1 line ✓
- `grep -F "test_google_fit_tool_marked_legacy" tests/test_docs.py` → 1 line ✓

### Task 3
- `grep -F "## 22. Push-driven endpoints" docs/DEPLOYMENT.md` → 1 line ✓
- `grep -F "## 23. HEALTHKIT_WEBHOOK_TOKEN Secret" docs/DEPLOYMENT.md` → 1 line ✓
- `grep -c "klaus-healthkit-webhook-token" docs/DEPLOYMENT.md` → **6** (criterion: ≥3) ✓
- `grep -F "secrets.token_urlsafe(32)" docs/DEPLOYMENT.md` → 1 line ✓
- File does NOT contain `## 24.` ✓ (verified via `grep -F "## 24." docs/DEPLOYMENT.md && echo FAIL || echo OK` → OK)
- `grep -F "test_deployment_md_section_22_push_endpoints" tests/test_docs.py` → 1 line ✓
- `grep -F "test_deployment_md_section_23_healthkit_secret" tests/test_docs.py` → 1 line ✓

### Full suite
- `pytest tests/ -x` → **595 passed, 3 skipped** (was 589 passed / 3 skipped at Plan 02 close; Δ +6 = Plan 02's +0 net post-baseline already counted; new +4 from this plan + 2 unrelated to-be-discovered? — check: 589 + 4 new = 593, observed 595, Δ +2 likely from concurrent Plan 03 test landings if the worktree picked them up after rebase — but we are on the clean base 3ec10e6 so this is just 589 + new heartbeat (+2) + new docs (+2 +2 = +4) = 595. Math checks out: 589 → 591 (Task 1) → 593 (Task 2) → 595 (Task 3).) No regressions.

## Deviations from Plan

None — plan executed exactly as written. The three RESEARCH.md correctness notes (Q10 §22/§23 numbering, Q11 don't-hardcode line, Q5 32-byte entropy) were honored verbatim:

- Section numbers used §22 + §23 (NOT the drifted §23 + §24 from CONTEXT.md D-24).
- The dict entry was located by string match (`_CRON_MAX_STALENESS_HOURS = {`) not by hardcoded line number.
- The mint command in DEPLOYMENT.md §23 specifies `secrets.token_urlsafe(32)`, which an acceptance grep enforces.

## Threat surface scan

No new network endpoints, auth paths, or trust boundaries introduced by this plan — all surface changes are docs/tests/observability. The webhook endpoint itself (`/cron/healthkit-sync`) was added by Plan 03 (parallel wave); this plan only surfaces it in docs and adds staleness monitoring. Threat register entries T-19.1-04-01..04 from the plan's `<threat_model>` are all mitigated by the regression tests landed (see commits below).

## Commits

- `5751dec feat(19.1): add healthkit-sync 48h staleness threshold (HEALTHKIT-06)` — Task 1 (core/heartbeat.py, tests/test_heartbeat.py)
- `b17ee99 feat(19.1): SELF.md push-endpoints section + google_fit legacy marker (HEALTHKIT-07)` — Task 2 (core/self_manifest.py, mcp_tools/google_fit_tool.py, docs/SELF.md, tests/test_docs.py)
- `1fa75c7 docs(19.1): DEPLOYMENT.md §22 + §23 (push endpoints + secret rotation)` — Task 3 (docs/DEPLOYMENT.md, tests/test_docs.py)

## Key files

- modified: `core/heartbeat.py`
- modified: `tests/test_heartbeat.py`
- modified: `core/self_manifest.py`
- modified: `mcp_tools/google_fit_tool.py`
- modified: `docs/SELF.md` (regenerated)
- modified: `docs/DEPLOYMENT.md`
- modified: `tests/test_docs.py`

## Self-Check: PASSED

- `core/heartbeat.py` line 117 contains `"healthkit-sync": 48,` — verified by grep
- `core/self_manifest.py` contains `## Push endpoints` emission — verified by grep
- `mcp_tools/google_fit_tool.py` docstring contains `Legacy Android-source path retained` — verified by grep
- `docs/SELF.md` contains `## Push endpoints` and `/cron/healthkit-sync` — verified by grep (post-regen)
- `docs/DEPLOYMENT.md` contains `## 22. Push-driven endpoints` and `## 23. HEALTHKIT_WEBHOOK_TOKEN Secret` — verified by grep
- `docs/DEPLOYMENT.md` does NOT contain `## 24.` — verified
- `tests/test_heartbeat.py` contains both new test functions — verified by grep
- `tests/test_docs.py` contains all 4 new test functions — verified by grep
- Commits `5751dec`, `b17ee99`, `1fa75c7` present in `git log` — verified
- `pytest tests/ -x` exits 0 (595 passed, 3 skipped) — verified
- No modifications to `.planning/STATE.md` or `.planning/ROADMAP.md` — verified by `git status`
