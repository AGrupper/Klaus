---
status: partial
phase: 19.1-healthkit-nutrition-bridge
source: [19.1-01-SUMMARY.md, 19.1-02-SUMMARY.md, 19.1-03-SUMMARY.md, 19.1-04-SUMMARY.md, 19.1-05-SUMMARY.md, 19.1-05-PATH-B-SUMMARY.md]
started: 2026-05-30T14:31:00+03:00
updated: 2026-05-30T14:40:00+03:00
---

## Current Test

[testing paused — 1 item outstanding: Test 5 needs a live Telegram observation]

## Tests

### 1. Endpoint live + bearer auth
expected: POST to deployed `/cron/healthkit-sync` with correct bearer returns 200; missing/wrong bearer returns 401/403 with no write.
result: pass
evidence: |
  - Endpoint proven LIVE by the real authenticated POST that landed a meal at
    `meals/2026-05-30/timestamps/healthkit:2026-05-30T08:00:00+03:00:62` on
    2026-05-30 01:11 (recorded in 19-HUMAN-UAT.md). A real bearer-authenticated
    request succeeded against the deployed route.
  - Auth + route logic: `TestCronHealthkitSync` 11/11 pass — covers 401 (missing
    auth), 403 (bad token), 500 (env unset / refuse-all), `hmac.compare_digest`
    constant-time path, dev-bypass, and the upsert-only D-10 contract.
  - Not re-fired by me: I don't hold the bearer token and did not extract it.

### 2. Real meal ingest end-to-end (Lifesum → Firestore)
expected: Log a meal in Lifesum and close the app; within minutes a doc appears at `meals/{today}/timestamps/healthkit:...` with macros, source="healthkit".
result: pass
evidence: |
  - Operator-verified live on 2026-05-30 01:11: real Lifesum meal → 4 flat
    samples POSTed → Firestore doc landed with calories=62, protein=3g,
    carbs=5g, fat=3g, source="healthkit", meal_type=1. On record in
    19-HUMAN-UAT.md and 19.1-05-PATH-B-SUMMARY.md.

### 3. Cross-macro aggregation (4 flat samples → 1 meal)
expected: The 4 per-macro samples for one food collapse into ONE meal doc with all four macros; two foods in the same minute stay distinct (food_item in the grouping key).
result: pass
evidence: |
  - Live: the 01:11 meal's 4 flat samples aggregated to exactly 1 meal doc.
  - Unit: `test_healthkit_tool.py` aggregator suite 8/8 pass — groups-five-macros,
    sums-duplicates (per-food-item fan-out), separate-by-start-date,
    separate-by-food-item, real-fixture-values, flat-payload-canonical-parse,
    string-value-coerces, non-coercible-value-raises.

### 4. Idempotent re-POST (no double-counting)
expected: Re-POSTing the same samples upserts the SAME doc (same source_id); day totals don't double.
result: pass
evidence: |
  - `test_web_server.py::...test_idempotent_repush` passes (same payload POSTed
    twice → idempotent via MealStore `merge=True` on source_id).
  - source_id determinism covered by `test_healthkit_tool.py` source_id tests
    (Lifesum/empty-uuid → deterministic `healthkit:{start_date}:{calories}`
    fallback). Live double-count was not separately re-exercised, but the
    dedup key + merge semantics are locked by tests.

### 5. Klaus surfaces the logged nutrition
expected: After ingest, the morning recap / autonomous tick / asking Klaus reflects the logged calories/macros — Klaus sees it in MealStore, not just raw Firestore.
result: blocked
blocked_by: server
reason: |
  Requires a live Telegram observation (or the next scheduled morning-briefing
  / autonomous-tick run) to confirm Klaus actually VOICES the nutrition — I can't
  observe an outbound Telegram message autonomously.

  Wiring IS verified, and it's source-agnostic: the bridge writes to the shared
  `MealStore`, and the consumer reads the same store —
  `core/morning_briefing.py:257` calls
  `MealStore(...).get_day_aggregate(yesterday)` → `data["nutrition"] = agg`
  (silent-omit when empty, NUTR-07). So a HealthKit-sourced meal reaches the
  briefing exactly like a Google-Fit one. The only unobserved link is the
  rendered Telegram message itself.

  To close: ask Klaus "what did I eat today?" in Telegram, OR wait for the next
  06:00–10:00 morning briefing after a logged meal and confirm the recap names
  the macros. (A local `morning_briefing._gather_data()` run against live
  Firestore would also confirm the meal aggregates into the briefing input, but
  that touches live GCP creds — left to you.)

### 6. Fiber present but gracefully dropped (Phase 19.2 boundary)
expected: A payload including `DietaryFiber_g` ingests fine with its other macros; fiber simply isn't persisted yet (deferred to 19.2, not a 19.1 defect).
result: pass
evidence: |
  - `test_healthkit_tool.py` fiber/unknown-key suite passes — normalizer ignores
    unknown `quantity_type` keys via `.get(..., 0)`; the real-device fixture
    carries `DietaryFiber_g` (2.4g) and ingests cleanly to the 4 persisted macros.
  - Confirms the 19.2 boundary: fiber arrives, is accepted, not yet stored.

## Summary

total: 6
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

[none — no code defects found. The one open item (Test 5) is a live-observation gate, not a code gap.]

## Notes

- Automated baseline (2026-05-30): `test_healthkit_tool.py` + `test_healthkit_payload_schema.py` + `test_web_server.py::TestCronHealthkitSync` = **90 passed** (1 schema test intentionally skip-gated on the Pydantic import in Plan 01; GREEN since Plan 02).
- Targeted re-runs this session: T1 auth/route 11✓, T3 aggregation 8✓, T4 idempotency 1✓, T6 fiber-drop 3✓.
- The full chain was already operator-verified live on 2026-05-30 01:11 (19-HUMAN-UAT.md / 19.1-05-PATH-B-SUMMARY.md). These tests re-confirm from the code + live-record side. I could not personally fire the live endpoint (no bearer token) or observe Klaus's Telegram output, so Tests 1–4/6 lean on the existing live record + the test suite, and Test 5 is held as a live-observation gate.
