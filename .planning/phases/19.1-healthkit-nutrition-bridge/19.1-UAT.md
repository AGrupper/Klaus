---
status: complete
phase: 19.1-healthkit-nutrition-bridge
source: [19.1-01-SUMMARY.md, 19.1-02-SUMMARY.md, 19.1-03-SUMMARY.md, 19.1-04-SUMMARY.md, 19.1-05-SUMMARY.md, 19.1-05-PATH-B-SUMMARY.md]
started: 2026-05-30T14:31:00+03:00
updated: 2026-05-31T09:30:00+03:00
---

## Current Test

[testing complete]

## Tests

### 1. Endpoint live + bearer auth
expected: POST to deployed `/cron/healthkit-sync` with correct bearer returns 200; missing/wrong bearer returns 401/403 with no write.
result: pass
evidence: |
  - Endpoint proven LIVE by the real authenticated POST that landed a meal at
    `meals/2026-05-30/timestamps/healthkit:2026-05-30T08:00:00+03:00:62` on
    2026-05-30 01:11 (recorded in 19-HUMAN-UAT.md).
  - Auth + route logic: `TestCronHealthkitSync` 11/11 pass — 401/403/500/
    `hmac.compare_digest`/dev-bypass/upsert-only D-10.
  - Not re-fired by me: I don't hold the bearer token.

### 2. Real meal ingest end-to-end (Lifesum → Firestore)
expected: Log a meal in Lifesum and close the app; within minutes a doc appears at `meals/{today}/timestamps/healthkit:...` with macros, source="healthkit".
result: pass
evidence: |
  - Operator-verified live 2026-05-30 01:11: real Lifesum meal → 4 flat samples
    POSTed → Firestore doc with calories=62, protein=3g, carbs=5g, fat=3g,
    source="healthkit", meal_type=1. On record in 19-HUMAN-UAT.md and
    19.1-05-PATH-B-SUMMARY.md.

### 3. Cross-macro aggregation (4 flat samples → 1 meal)
expected: The 4 per-macro samples for one food collapse into ONE meal doc with all four macros; two foods in the same minute stay distinct.
result: pass
evidence: |
  - Live: the 01:11 meal's 4 flat samples aggregated to exactly 1 meal doc.
  - Unit: aggregator suite 8/8 pass (groups-five-macros, sums-duplicates,
    separate-by-start-date, separate-by-food-item, real-fixture-values, etc.).

### 4. Idempotent re-POST (no double-counting)
expected: Re-POSTing the same samples upserts the SAME doc (same source_id); day totals don't double.
result: pass
evidence: |
  - `test_idempotent_repush` passes (idempotent via MealStore `merge=True`).
  - Deterministic source_id fallback covered by source_id tests.

### 5. Klaus surfaces the logged nutrition
expected: After ingest, asking Klaus reflects the logged calories/macros — Klaus reads MealStore (where HealthKit writes), not the dead Google Fit source.
result: pass
verified: "2026-05-31 09:27 — after deploying the read-path repoint (0088aca) + the serialization fix (a46431f), asked Klaus 'What did I eat today?'. He returned the real HealthKit breakfast from MealStore: 1,113 kcal / 44g protein / 156g carbs / 34g fat / 19.7g fiber, and correctly noted no lunch/dinner logged yet. Confirms all three fixes live: (1) read path → MealStore, (2) updated_at serialization, (3) fiber surfaced. Two intermediate failures on the way: 2026-05-30 16:07 'no entries in Google Fit' (pre-fix), then a 'timestamp serialization anomaly' (the updated_at bug, fixed by a46431f)."
severity: major
analysis: |
  Root cause (confirmed in code before the tool environment began returning
  corrupted output):
  - The brain-direct `fetch_recent_meals` tool handler routes to
    `core/tools.py::_handle_fetch_recent_meals` → `get_google_fit().fetch_recent_meals(hours=...)`
    — i.e. it reads the DEPRECATED Google Fit source, which returns [] on iOS.
    It does NOT read `MealStore` (where `/cron/healthkit-sync` writes HealthKit meals).
  - This is the SAME class of defect as the autonomous tick
    (`core/autonomous.py:319` `sync_recent_meals()` from google_fit) — the meal
    READ paths were never repointed from Google Fit to MealStore when the iOS
    HealthKit bridge replaced the (dead-on-iOS) Google Fit ingest.
  - Klaus's own wording ("Google Fit or Lifesum") is the smoking gun: the tool
    he called names Google Fit.
  Scope correction: the iOS-source fix is broader than originally scoped for
  Phase 19.3. It must cover BOTH read paths:
    (a) the autonomous-tick gather (`core/autonomous.py:319`), AND
    (b) the brain-direct `fetch_recent_meals` tool (`core/tools.py` ~line 139-142).
  Morning briefing (`core/morning_briefing.py:254`) is the only path that already
  reads MealStore correctly.
  NOTE: this is NOT a Phase 19.1 regression — 19.1 correctly delivered the WRITE
  path (HealthKit → MealStore). The READ paths are Phase 19 carryover wired to
  Google Fit. Captured as Phase 19.3 (scope broadened below).

### 6. Fiber present but gracefully dropped (Phase 19.2 boundary)
expected: A payload including `DietaryFiber_g` ingests fine with its other macros; fiber simply isn't persisted yet (deferred to 19.2).
result: pass
evidence: |
  - Fiber/unknown-key suite 3/3 pass; real-device fixture carries 2.4g fiber,
    ingests cleanly to the 4 persisted macros. Confirms the 19.2 boundary.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Asking Klaus about today's food reflects HealthKit-logged meals (read from MealStore)"
  status: resolved
  reason: "User reported: Klaus said 'no nutrition entries in Google Fit or Lifesum' — the on-demand meal-query tool reads Google Fit, not MealStore, so iOS HealthKit meals are invisible."
  resolved_verified: "2026-05-31 09:27 live Telegram — Klaus returned real HealthKit breakfast (1113 kcal / 44P / 156C / 34F / 19.7 fiber) from MealStore. Fixes 0088aca + a46431f deployed green."
  severity: major
  test: 5
  root_cause: "core/tools.py::_handle_fetch_recent_meals → get_google_fit().fetch_recent_meals() reads the deprecated Google Fit source (returns [] on iOS) instead of MealStore.get_day(). Same defect class as core/autonomous.py:319."
  artifacts:
    - path: "core/tools.py (~line 139-142)"
      issue: "_handle_fetch_recent_meals routes to google_fit, not MealStore"
    - path: "core/autonomous.py:319"
      issue: "autonomous-tick gather calls sync_recent_meals() from google_fit"
  missing:
    - "Repoint fetch_recent_meals tool handler to MealStore.get_day()"
    - "Repoint autonomous-tick meal gather to MealStore.get_day()"
  resolution: |
    FIXED INLINE 2026-05-30 (Phase 19.3, done in this session at Amit's request):
    - core/tools.py::_handle_fetch_recent_meals now reads MealStore.get_day()
      (window-filtered to last N hours, midnight-straddle safe), source-agnostic.
    - core/autonomous.py gather now reads MealStore.get_day() filtered to the
      last hour (no more sync_recent_meals from dead Google Fit).
    - Tool schema + docs/SELF.md description updated (no longer claims Google Fit).
    - Tests: test_autonomous meal tests repointed to get_day; full suite 638 pass.
    LIVE RE-VERIFY PENDING DEPLOY: after push→deploy, re-ask Klaus "what did I eat
    today?" in Telegram — expect real macros, not "no entries in Google Fit".
    Also bundled here: Phase 19.2 fiber — fiber_g now threaded end-to-end
    (healthkit/google_fit normalizers → MealStore totals → meal_audit prompt).

## Notes

- Automated baseline (2026-05-30): `test_healthkit_tool.py` + `test_healthkit_payload_schema.py` + `test_web_server.py::TestCronHealthkitSync` = **90 passed**.
- The WRITE path (HealthKit → /cron/healthkit-sync → MealStore) is fully verified — live on 2026-05-30 01:11 and by the test suite. Phase 19.1's deliverable is sound.
- The one failure (Test 5) is a READ-path defect inherited from Phase 19 (meal reads still pointed at Google Fit), surfaced by the live Telegram check. Routed to Phase 19.3.
- Tool-environment caveat: while investigating Test 5, the Bash/Read tools began returning corrupted output. The root-cause code lines were captured from a clean grep BEFORE the corruption and cross-confirmed by Klaus's own "Google Fit or Lifesum" wording. Re-verify line numbers at 19.3 plan time.
