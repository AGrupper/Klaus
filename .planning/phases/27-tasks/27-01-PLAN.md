---
phase: 27-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/firestore_db.py
  - tests/test_task_store.py
  - tests/test_web_server.py
  - tests/test_tools.py
  - tests/test_autonomous.py
  - frontend/src/utils/parseTaskInput.test.ts
  - frontend/src/hooks/useTaskSummary.test.ts
  - docs/DEPLOYMENT.md
autonomous: true
requirements: [TASK-01, TASK-02, TASK-04, TASK-07]
user_setup:
  - service: gcp-firestore
    why: "Composite indexes for task overdue + list-scoped queries (Firestore does not auto-create composites)"
    dashboard_config:
      - task: "Create composite index (status ASC, due_date ASC) on collection-group 'tasks'"
        location: "gcloud firestore indexes composite create --project=klaus-agent --database=klaus-firestore --collection-group=tasks --query-scope=COLLECTION --field-config=field-path=status,order=ascending --field-config=field-path=due_date,order=ascending"
      - task: "Create composite index (list_id ASC, status ASC, due_date ASC) on collection-group 'tasks'"
        location: "gcloud firestore indexes composite create --project=klaus-agent --database=klaus-firestore --collection-group=tasks --query-scope=COLLECTION --field-config=field-path=list_id,order=ascending --field-config=field-path=status,order=ascending --field-config=field-path=due_date,order=ascending"

must_haves:
  truths:
    - "TaskStore.create/list/get/update/complete/undo_complete/delete operate against a flat Firestore 'tasks' collection — task fields are title/notes/due(optional date+time)/priority/list/recurrence with 4-level priority none/low/medium/high (D-01, D-03)"
    - "TaskListStore.create/list/rename/delete operate against a flat 'task_lists' collection; Inbox is implicit (list_id='inbox') — lists are user-creatable, no tag field this phase (D-02, D-04)"
    - "_next_due_date computes the correct next instance for daily/weekdays/weekly/monthly/every_n_days with both schedule and completion anchors (cadence + per-task anchor toggle, no RRULE — D-05)"
    - "A schedule-anchored next that lands in the past rolls forward to the next future occurrence (D-06)"
    - "complete() on a recurring task generates the next instance then sets the current to status='completing'; undo_complete reverses both (D-15)"
    - "get_summary returns {due_today, overdue} counts; get_overdue returns active tasks with due_date < today"
    - "All Wave 0 test files exist and define the test classes named in 27-VALIDATION.md"
  artifacts:
    - path: "memory/firestore_db.py"
      provides: "TaskStore + TaskListStore classes + _next_due_date helper"
      contains: "class TaskStore"
    - path: "tests/test_task_store.py"
      provides: "Recurrence + CRUD + soft-complete/undo + summary unit tests"
      contains: "class TestNextDueDate"
    - path: "frontend/src/utils/parseTaskInput.test.ts"
      provides: "Wave 0 stub asserting parseTaskInput contract (≥8 cases)"
    - path: "frontend/src/hooks/useTaskSummary.test.ts"
      provides: "Wave 0 stub for the summary hook"
  key_links:
    - from: "memory/firestore_db.py TaskStore"
      to: "Firestore 'tasks' collection"
      via: "_make_firestore_client + _where server-side filters"
      pattern: "_COLLECTION = \"tasks\""
    - from: "TaskStore.complete()"
      to: "_next_due_date helper"
      via: "recurring next-instance generation"
      pattern: "_next_due_date"
---

<objective>
Build the native task datastore — `TaskStore` + `TaskListStore` + a pure server-side recurrence engine (`_next_due_date`) in `memory/firestore_db.py` — and create every Wave 0 test file so all downstream plans have an automated verify target. This is the contract layer the entire phase builds on (routes, Klaus tools, autonomous gather, and frontend all read this shape).

Purpose: Eliminate the TickTick external dependency by giving Klaus a Firestore-native task store with correct recurrence math that the agent, the autonomous gather, and the hub all share as the single source of truth (RESEARCH Unknown 2/4).
Output: `TaskStore`, `TaskListStore`, `_next_due_date` in `memory/firestore_db.py`; six Wave 0 test files; two composite indexes created in production; DEPLOYMENT.md index table updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-tasks/27-CONTEXT.md
@.planning/phases/27-tasks/27-RESEARCH.md
@.planning/phases/27-tasks/27-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

Store-class conventions to mirror — memory/firestore_db.py:
- `_make_firestore_client(project_id: str, database: str) -> firestore.Client` (line 88)
- `_where(query, field: str, op: str, value)` — server-side filter helper (line 22)
- `_jsonsafe_doc(d: dict) -> dict` / `_jsonsafe_value(v)` — strip DatetimeWithNanoseconds before json.dumps (lines 885–913)
- FollowupStore (lines 1517–1614): `__init__(self, project_id, database="(default)")` sets `self._client` + `self._col`; `add()` uses `import uuid; uuid.uuid4().hex`, stamps `created_at` as `datetime.now(timezone.utc).isoformat()` (plain string), `status` field, write re-raises after `logger.error(..., exc_info=True)`; `list_due()` uses `from google.cloud.firestore_v1.base_query import FieldFilter` + `.where(filter=FieldFilter("status","==","pending")).where(filter=FieldFilter("due_at","<=",now_iso)).stream()`, returns `[]` on Exception with `logger.warning(..., exc_info=True)`.
- StrengthSessionStore (lines 1087–1223): same `__init__` shape; writes stamp `"updated_at": firestore.SERVER_TIMESTAMP`; reads strip it via `_jsonsafe_doc`.

Recurrence rule shape (RESEARCH Unknown 2/4):
```
recurrence: { "cadence": "daily"|"weekdays"|"weekly"|"monthly"|"every_n_days", "every_n_days": int|null, "anchor": "schedule"|"completion" } | null
```

Task document shape (RESEARCH Unknown 4):
```
tasks/{uuid}: id, title, notes|null, due_date|null ("YYYY-MM-DD" plain string), due_time|null ("HH:MM"),
priority ("none"|"low"|"medium"|"high"), list_id ("inbox" or list uuid), status ("active"|"completing"),
recurrence|null, series_id|null, created_at (ISO UTC string), updated_at (SERVER_TIMESTAMP)
task_lists/{uuid}: id, name, created_at (ISO UTC string), updated_at (SERVER_TIMESTAMP)
```

Test mock — tests/test_firestore_db.py:
- `_install_firestore_mock()` (line 29) installs the google.cloud.firestore mock BEFORE importing memory.firestore_db; force re-import via importlib (line 124). Mirror this fixture exactly in tests/test_task_store.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recurrence engine + _next_due_date with full test coverage</name>
  <read_first>
    - memory/firestore_db.py (lines 1–40 for _where, 1517–1614 for FollowupStore conventions)
    - .planning/phases/27-tasks/27-RESEARCH.md (Unknown 2 — _next_due_date reference implementation, month-end clamp, weekday wrap, D-06 roll-forward)
    - .planning/phases/27-tasks/27-VALIDATION.md (TASK-02 rows — exact test class names)
    - tests/test_firestore_db.py (lines 1–150 — _install_firestore_mock fixture to mirror)
  </read_first>
  <behavior>
    - daily: base + 1 day
    - weekdays: base + 1 day, then skip Sat(5)/Sun(6) forward to Monday
    - weekly: base + 7 days
    - monthly: same day next month, clamped via calendar.monthrange (Jan 31 → Feb 28; in a leap year → Feb 29)
    - every_n_days: base + every_n_days (default 1 if missing)
    - anchor="schedule": base = current_due; anchor="completion": base = completed_on
    - D-06: schedule-anchored candidate <= completed_on rolls forward (looping, not single-step) to the next future occurrence
    - D-06 multi-cycle: a weekly schedule-anchored task current_due=2026-05-01, completed_on=2026-06-18 → strictly > 2026-06-18 in ONE call (loop advances multiple weeks)
  </behavior>
  <action>
    Add a module-level pure helper `_next_due_date(current_due: date, completed_on: date, rule: dict) -> date` to `memory/firestore_db.py` near the other module helpers (after `_jsonsafe_value`). Implement the five cadences from RESEARCH Unknown 2 using `datetime.timedelta` and `calendar.monthrange` (stdlib — do NOT hand-roll month arithmetic per § Don't Hand-Roll). Define `_TZ = ZoneInfo("Asia/Jerusalem")`. Factor the single-cadence step into a `_advance_once(base, rule)` helper (as the revised RESEARCH Unknown 2 reference now shows) so the roll-forward can call it repeatedly. CRITICAL — the schedule-anchor roll-forward must be a real loop, NOT a single `break`-guarded step: implement it as `while anchor=="schedule" and candidate <= completed_on: candidate = _advance_once(candidate, rule)`. (An earlier revision of the RESEARCH reference code used a `while ...: candidate = _next_due_date(...); break` form — that `break` advances only ONE cadence and leaves a task several cadences in the past still past-due; the RESEARCH code is now corrected to the loop form, but if you copy from any cached version, use the loop, never the `break`.) Never materialize a past-due next (D-06). Then create `tests/test_task_store.py` mirroring the `_install_firestore_mock()` fixture from `tests/test_firestore_db.py`, with the test classes `TestNextDueDate`, `TestMonthEndClamping`, `TestWeekdayWrapping`, `TestPastDueRollForward` — each asserting concrete dates (e.g. monthly Jan 31 2026 schedule-anchor → 2026-02-28; weekdays Fri 2026-06-19 → Mon 2026-06-22; weekly past-due roll-forward lands strictly after completed_on). `TestPastDueRollForward` MUST include a multi-cycle case: a weekly schedule-anchored task with `current_due=2026-05-01` completed on `completed_on=2026-06-18` resolves in a SINGLE `_next_due_date` call to a date strictly > 2026-06-18 (i.e. 2026-06-19 or later) — a single-step `break` implementation would return a May/early-June date and fail this test.
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && python -m pytest tests/test_task_store.py::TestNextDueDate tests/test_task_store.py::TestMonthEndClamping tests/test_task_store.py::TestWeekdayWrapping tests/test_task_store.py::TestPastDueRollForward -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_task_store.py::TestNextDueDate -x` passes
    - `pytest tests/test_task_store.py::TestMonthEndClamping -x` passes with Jan 31 → Feb 28 assertion
    - `pytest tests/test_task_store.py::TestWeekdayWrapping -x` passes with Fri→Mon and Sat→Mon assertions
    - `pytest tests/test_task_store.py::TestPastDueRollForward -x` passes; asserts result is strictly > completed_on for a schedule-anchored weekly task whose naive next is in the past, INCLUDING a multi-cycle case (current_due=2026-05-01, completed_on=2026-06-18 → strictly > 2026-06-18 in one call) that a single-step `break` would fail
    - `_next_due_date` uses `calendar.monthrange` (grep `grep -n "monthrange" memory/firestore_db.py` returns a line)
  </acceptance_criteria>
  <done>Recurrence helper exists and all four recurrence test classes pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TaskStore + TaskListStore CRUD, soft-complete/undo, summary</name>
  <read_first>
    - memory/firestore_db.py (lines 1087–1223 StrengthSessionStore, 1517–1614 FollowupStore — clone these patterns)
    - .planning/phases/27-tasks/27-RESEARCH.md (Unknown 3 status mechanic, Unknown 4 doc shape, Pattern 1)
    - .planning/phases/27-tasks/27-VALIDATION.md (TASK-01/04/07 rows — test class names)
    - tests/test_task_store.py (the file created in Task 1)
  </read_first>
  <behavior>
    - create(task: dict) → stored dict with generated id (uuid4 hex); status defaults "active"; due_date stored as plain "YYYY-MM-DD" string (never Timestamp); created_at = datetime.now(timezone.utc).isoformat()
    - list(list_id=None) → active tasks (status=="active"), _jsonsafe_doc applied, server-side _where filter
    - get_overdue(today_iso) → active tasks with due_date < today_iso
    - get_summary(today_iso) → {"due_today": int, "overdue": int} (due_today = due_date == today_iso; overdue = due_date < today_iso)
    - complete(task_id, completed_on_iso): sets status="completing"; if recurrence present, creates next-instance doc (status="active", new id, same series_id, due_date from _next_due_date) and returns its id
    - undo_complete(task_id): sets status="active"; if a next instance was generated, deletes it
    - delete(task_id) hard: Firestore .delete()
    - TaskListStore.create/list/rename/delete on "task_lists"; Inbox is NOT a stored doc (implicit "inbox" constant)
    - Reads never raise (return []/{}/None + logger.warning); writes re-raise after logger.error
  </behavior>
  <action>
    Add `class TaskStore` (`_COLLECTION = "tasks"`) and `class TaskListStore` (`_COLLECTION = "task_lists"`) to `memory/firestore_db.py` after `FollowupStore`, cloning the `__init__`/`_col` shape and the read-never-raises / write-re-raises discipline. Store `due_date`/`due_time` as plain strings, `created_at` as `datetime.now(timezone.utc).isoformat()`, only `updated_at` as `firestore.SERVER_TIMESTAMP`; apply `_jsonsafe_doc` on every read result. Use `FieldFilter` server-side filters for `status`, `due_date`, `list_id` (per the composite index design in RESEARCH Unknown 4). `complete()` sets `status="completing"` and for recurring tasks computes the next due via `_next_due_date(date.fromisoformat(current_due), date.fromisoformat(completed_on_iso), rule)`, persists a fresh task doc sharing `series_id`, and returns `{"next_id": <id or None>}`. `undo_complete()` reverts `status="active"` and deletes the generated next instance if present. Reject hard `delete()` semantics live in the route layer (status check) — here `delete()` is the raw Firestore delete. Then extend `tests/test_task_store.py` with `TestTaskStoreCRUD`, `TestTaskListStore`, `TestSoftComplete`, `TestUndoComplete`, `TestRecurringComplete`, `TestGetSummary` per the VALIDATION map.
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && python -m pytest tests/test_task_store.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_task_store.py -x` passes (all classes: CRUD, ListStore, SoftComplete, UndoComplete, RecurringComplete, GetSummary, plus the recurrence classes from Task 1)
    - `grep -n "_COLLECTION = \"tasks\"" memory/firestore_db.py` returns a line
    - `grep -n "_COLLECTION = \"task_lists\"" memory/firestore_db.py` returns a line
    - TestSoftComplete asserts complete() leaves status="completing" (not deleted)
    - TestRecurringComplete asserts a new active doc is created with the same series_id and status="completing" on the original
    - TestUndoComplete asserts undo reverts status="active" AND deletes the generated next instance
    - No `due_date` field is ever set to `firestore.SERVER_TIMESTAMP` (grep the new classes — only `updated_at` uses SERVER_TIMESTAMP)
  </acceptance_criteria>
  <done>Both store classes exist; all test_task_store.py classes pass; due_date is a plain ISO string.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolds + composite indexes + DEPLOYMENT.md</name>
  <read_first>
    - .planning/phases/27-tasks/27-VALIDATION.md (§ Wave 0 Requirements — the 6 files + 2 indexes)
    - tests/test_tools.py (existing structure — where to add TestNativeTaskTools)
    - tests/test_autonomous.py (existing structure — where to add TestNativeOverdueGather/TestJobsDict)
    - frontend/src/hooks/useChat.test.tsx (renderHook pattern to mirror for useTaskSummary.test.ts)
    - docs/DEPLOYMENT.md (lines 1250–1265 — Firestore composite index table to extend)
  </read_first>
  <action>
    Create the remaining Wave 0 stub test files so every downstream plan has an automated target that fails meaningfully until implemented (RED scaffolds, marked with the exact class names from VALIDATION):
    (1) `tests/test_web_server.py` — add a `TestTaskRoutes` class with placeholder tests `@pytest.mark.skip(reason="implemented in 27-02")` referencing `/api/tasks` CRUD + `/api/tasks/summary` (TASK-01/07).
    (2) `tests/test_tools.py` — add `TestNativeTaskTools` (skip-marked, implemented in 27-03) asserting native task schemas registered + `add_task` removed.
    (3) `tests/test_autonomous.py` — add `TestNativeOverdueGather` + `TestJobsDict` (skip-marked, implemented in 27-03) for return shape + `"ticktick_overdue"` key retained.
    (4) `frontend/src/utils/parseTaskInput.test.ts` — ≥8 `it.skip` cases (implemented in 27-04) covering "Buy milk tomorrow", "meeting #work !high friday", near-midnight Asia/Jerusalem refDate stub.
    (5) `frontend/src/hooks/useTaskSummary.test.ts` — skip-marked renderHook stub mirroring useChat.test.tsx (implemented in 27-04).
    Then create the two composite indexes in production via the two `gcloud firestore indexes composite create` commands in this plan's `user_setup` (against `--project=klaus-agent --database=klaus-firestore`). Add both indexes to the DEPLOYMENT.md § Firestore Composite Indexes table with the FollowupStore-style note (FAILED_PRECONDITION on first query if missing).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && python -m pytest tests/test_web_server.py tests/test_tools.py tests/test_autonomous.py -x --co -q 2>&1 | grep -E "TestTaskRoutes|TestNativeTaskTools|TestNativeOverdueGather|TestJobsDict" && ls frontend/src/utils/parseTaskInput.test.ts frontend/src/hooks/useTaskSummary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six Wave 0 files listed in 27-VALIDATION.md § Wave 0 Requirements exist
    - Collected test names include TestTaskRoutes, TestNativeTaskTools, TestNativeOverdueGather, TestJobsDict
    - `frontend/src/utils/parseTaskInput.test.ts` contains ≥8 test cases (grep `it(` or `it.skip(` count ≥ 8)
    - `gcloud firestore indexes composite list --project=klaus-agent --database=klaus-firestore` shows the (status, due_date) and (list_id, status, due_date) indexes on collection 'tasks' (or operator-confirmed creating)
    - DEPLOYMENT.md composite-index table has two new 'tasks' rows
  </acceptance_criteria>
  <done>Six Wave 0 files exist with the named test scaffolds; two composite indexes created; DEPLOYMENT.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → TaskStore | Task data dicts reach Firestore; validation is enforced at the route/tool layer (27-02/27-03), but TaskStore must not store untrusted Timestamps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-IV | Tampering/Info | TaskStore.create due_date field | mitigate | Store due_date/due_time as plain ISO strings, never SERVER_TIMESTAMP — eliminates DatetimeWithNanoseconds JSON-serialization break (RESEARCH Pitfall 1); _jsonsafe_doc on all reads |
| T-27-REP | Repudiation | TaskStore.delete (hard) | mitigate | delete() is raw; the route layer (27-02) rejects hard-delete unless status="completing" — enforced where the HTTP request enters |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (chrono-node install is in 27-04 with its own checkpoint) |
</threat_model>

<verification>
- `pytest tests/test_task_store.py -x` fully green (recurrence + CRUD + soft-complete/undo + summary)
- The 1153+ baseline still holds: `pytest tests/test_firestore_db.py -x` passes (no regression in shared helpers)
- Both composite indexes exist (or are building) in klaus-firestore
</verification>

<success_criteria>
- TaskStore + TaskListStore + _next_due_date exist in memory/firestore_db.py following established store conventions
- All six Wave 0 test files exist; tests/test_task_store.py is fully green
- Recurrence math correct for all five cadences, both anchors, month-end clamp, weekday wrap, and D-06 past-due roll-forward
- Composite indexes created; DEPLOYMENT.md documents them
</success_criteria>

<output>
Create `.planning/phases/27-tasks/27-01-SUMMARY.md` when done.
</output>
