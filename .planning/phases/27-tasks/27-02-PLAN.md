---
phase: 27-tasks
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - interfaces/web_server.py
  - tests/test_web_server.py
autonomous: true
requirements: [TASK-01, TASK-02, TASK-04, TASK-07]

must_haves:
  truths:
    - "POST /api/tasks creates a task and returns it with id; GET /api/tasks lists active tasks (optionally by list_id)"
    - "PATCH /api/tasks/{id} edits fields; POST /api/tasks/{id}/complete soft-marks + generates recurring next; POST /api/tasks/{id}/undo reverts; POST /api/tasks/{id}/hard-delete removes"
    - "GET /api/tasks/summary returns {due_today, overdue} computed in Asia/Jerusalem"
    - "task-list CRUD: POST/GET/PATCH/DELETE /api/task-lists work; Inbox is implicit (list_id='inbox')"
    - "Every task route is behind Depends(require_hub_session); all responses pass through _jsonsafe_doc"
    - "CreateTaskInput Pydantic model validates title (1..500), notes (≤10000), due_date/due_time regex, priority enum, list_id"
    - "hard-delete is rejected (409/400) unless the doc is status='completing'"
  artifacts:
    - path: "interfaces/web_server.py"
      provides: "/api/tasks/* and /api/task-lists/* CRUD + summary routes + CreateTaskInput model"
      contains: "/api/tasks/summary"
    - path: "tests/test_web_server.py"
      provides: "TestTaskRoutes integration tests"
      contains: "class TestTaskRoutes"
  key_links:
    - from: "interfaces/web_server.py /api/tasks routes"
      to: "memory.firestore_db.TaskStore"
      via: "run_in_executor + lazy import"
      pattern: "TaskStore"
    - from: "/api/tasks/{id}/hard-delete"
      to: "status guard"
      via: "reject unless status=='completing'"
      pattern: "completing"
---

<objective>
Add the `/api/tasks/*` and `/api/task-lists/*` HTTP CRUD layer to `interfaces/web_server.py`, mirroring the Phase 26 `/api/today` and `/api/chat` route conventions (session-cookie auth, run_in_executor for sync Firestore calls, `_jsonsafe_doc` on all output, Pydantic input validation). This exposes the `TaskStore`/`TaskListStore` built in 27-01 to the hub frontend.

Purpose: Give the hub a session-authenticated CRUD API for tasks and lists with strict input validation (ASVS V5) and the soft-complete → undo → hard-delete mechanic (RESEARCH Unknown 3), without weakening any existing OIDC route (HUB-04 invariant).
Output: ~10 new routes + `CreateTaskInput`/`UpdateTaskInput`/`CreateListInput` Pydantic models in `interfaces/web_server.py`; `TestTaskRoutes` in `tests/test_web_server.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-tasks/27-CONTEXT.md
@.planning/phases/27-tasks/27-RESEARCH.md
@.planning/phases/27-tasks/27-VALIDATION.md
@CLAUDE.md
@.planning/phases/27-tasks/27-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

Route conventions — interfaces/web_server.py:
- `from interfaces.hub_auth import require_hub_session` (line 41) — used as `Depends(require_hub_session)`
- `/api/today` (line 1415): `async def api_today(_email: str = Depends(require_hub_session)) -> JSONResponse`; `loop = asyncio.get_running_loop()`; `await loop.run_in_executor(None, _fn, args)`; `from memory.firestore_db import _jsonsafe_doc` lazy import; `JSONResponse(content=_jsonsafe_doc(payload))`
- `/api/chat` (line 1554): input validation pattern — `body = await request.json()`; raise `HTTPException(status_code=400, detail={"error": ...})` on bad input; `_CHAT_CONTENT_MAX_LEN = 4000` (line 1502)
- Store instantiation: `project_id=os.environ.get("GCP_PROJECT_ID", "")`, `database=os.environ.get("FIRESTORE_DATABASE", "(default)")`
- Asia/Jerusalem today: `datetime.now(ZoneInfo("Asia/Jerusalem")).date().isoformat()`

TaskStore API (from 27-01, see 27-01-SUMMARY.md for final signatures):
- create(task: dict) -> dict; list(list_id=None) -> list[dict]; get(task_id) -> dict|None
- update(task_id, patch: dict) -> dict; complete(task_id, completed_on_iso) -> {"next_id": id|None}
- undo_complete(task_id) -> None; delete(task_id) -> None; get_overdue(today_iso) -> list[dict]; get_summary(today_iso) -> {"due_today": int, "overdue": int}
TaskListStore API: create(name) -> dict; list() -> list[dict]; rename(list_id, name) -> dict; delete(list_id) -> None

Validation model (RESEARCH § Security Domain V5):
```
class CreateTaskInput(BaseModel):
    title: str = Field(..., min_length=1, max_length=500)
    notes: str | None = Field(None, max_length=10_000)
    due_date: str | None = Field(None, pattern=r"^\d{4}-\d{2}-\d{2}$")
    due_time: str | None = Field(None, pattern=r"^([01]\d|2[0-3]):[0-5]\d$")
    priority: Literal["none","low","medium","high"] = "none"
    list_id: str | None = None   # None → "inbox"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Task + task-list CRUD routes with Pydantic validation</name>
  <read_first>
    - interfaces/web_server.py (lines 1415–1478 api_today; lines 1554–1660 api_chat for validation; lines 1500–1520 for helpers/constants)
    - .planning/phases/27-tasks/27-RESEARCH.md (Pattern 5 route, Unknown 3 endpoint flow, § Security Domain CreateTaskInput)
    - .planning/phases/27-tasks/27-01-SUMMARY.md (final TaskStore/TaskListStore signatures)
    - memory/firestore_db.py (TaskStore + TaskListStore from 27-01)
  </read_first>
  <behavior>
    - POST /api/tasks with valid CreateTaskInput → 200 + created task dict (list_id defaults "inbox")
    - POST /api/tasks with title="" → 422/400 (Pydantic rejects)
    - POST /api/tasks with due_date="2026/06/20" → 422/400 (regex reject)
    - GET /api/tasks → {tasks: [...]}; GET /api/tasks?list_id=X → filtered
    - GET /api/tasks/summary → {due_today, overdue}
    - PATCH /api/tasks/{id} → updated task
    - POST /api/tasks/{id}/complete → {next_id: ...}; POST /api/tasks/{id}/undo → ok
    - POST /api/tasks/{id}/hard-delete on a status="active" task → 409 (only "completing" may be hard-deleted, T-27-REP)
    - POST/GET/PATCH/DELETE /api/task-lists → list CRUD
    - all routes return 401 without a session cookie
  </behavior>
  <action>
    Add to `interfaces/web_server.py`: `CreateTaskInput`, `UpdateTaskInput` (all fields optional), and `CreateListInput` Pydantic `BaseModel`s with the exact field constraints from RESEARCH § Security Domain (use `from pydantic import BaseModel, Field` and `from typing import Literal` — check existing imports first). Add the routes below, each `Depends(require_hub_session)`, each using `loop.run_in_executor` for the sync TaskStore/TaskListStore calls, each wrapping output in `_jsonsafe_doc`:
    `POST /api/tasks` (validate via CreateTaskInput; default list_id="inbox"; pass dict to TaskStore.create),
    `GET /api/tasks` (optional `list_id` query param → TaskStore.list),
    `GET /api/tasks/summary` (Asia/Jerusalem today_iso → TaskStore.get_summary),
    `PATCH /api/tasks/{task_id}` (UpdateTaskInput → TaskStore.update),
    `POST /api/tasks/{task_id}/complete` (Asia/Jerusalem completed_on_iso → TaskStore.complete; for D-07 "this occurrence only" vs "this and following" accept an optional `scope` body field that the route forwards — default behavior matches the single-open-instance model),
    `POST /api/tasks/{task_id}/undo` (TaskStore.undo_complete),
    `POST /api/tasks/{task_id}/hard-delete` (fetch task via TaskStore.get; if status != "completing" raise HTTPException 409 detail {"error":"task not in completing state"} per T-27-REP; else TaskStore.delete),
    `POST /api/task-lists` (CreateListInput → TaskListStore.create),
    `GET /api/task-lists` (TaskListStore.list — prepend the implicit Inbox as {"id":"inbox","name":"Inbox"}),
    `PATCH /api/task-lists/{list_id}` (rename), `DELETE /api/task-lists/{list_id}` (delete; tasks in a deleted list fall back to Inbox — reassign in the route or document the behavior). Place all new routes AFTER the existing `/api/chat/messages` route; do NOT touch any `/cron/*`, `/internal/*`, `/trigger/*`, or `/telegram-webhook` route (HUB-04).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && python -m pytest tests/test_web_server.py::TestTaskRoutes -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_web_server.py::TestTaskRoutes -x` passes
    - `grep -n "/api/tasks/summary" interfaces/web_server.py` returns a line
    - `grep -n "class CreateTaskInput" interfaces/web_server.py` returns a line
    - Every new route declares `Depends(require_hub_session)` (grep the new route block: each `@app.(get|post|patch|delete)("/api/task...` is followed by a handler with `Depends(require_hub_session)`)
    - hard-delete route raises HTTPException 409 when status != "completing"
    - No existing OIDC route signature changed: `git diff interfaces/web_server.py` touches only additions after /api/chat/messages plus the new model classes
  </acceptance_criteria>
  <done>All task + task-list routes exist, validate input, are session-gated, and TestTaskRoutes passes.</done>
</task>

<task type="auto">
  <name>Task 2: TestTaskRoutes integration tests (CRUD + summary + hard-delete guard + auth)</name>
  <read_first>
    - tests/test_web_server.py (existing test client setup + auth-mock pattern + the TestTaskRoutes skip-stub from 27-01)
    - interfaces/web_server.py (the routes added in Task 1)
    - .planning/phases/27-tasks/27-VALIDATION.md (TASK-01/07 integration rows)
  </read_first>
  <action>
    Replace the skip-marked `TestTaskRoutes` stub (from 27-01) with real tests using the existing test-client + require_hub_session override pattern in `tests/test_web_server.py`. Cover: create→200 with returned id; create with empty title→422/400; create with malformed due_date→422/400; list returns created tasks; summary returns {due_today, overdue} shape; complete→{next_id}; undo→ok; hard-delete on active task→409; hard-delete on completing task→200; an unauthenticated request (no session override)→401; task-list create/list (Inbox prepended)/rename/delete. Mock TaskStore/TaskListStore at the boundary the existing tests use (monkeypatch the store or the Firestore client per the file's established approach).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && python -m pytest tests/test_web_server.py::TestTaskRoutes -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_web_server.py::TestTaskRoutes -x` passes with no skip markers remaining on the task tests
    - A test asserts 401 for an unauthenticated /api/tasks request
    - A test asserts 409 for hard-delete of a status="active" task
    - A test asserts 400/422 for an empty title and for a malformed due_date
  </acceptance_criteria>
  <done>TestTaskRoutes is fully implemented and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/tasks/* | Untrusted JSON bodies + path params cross here; session cookie is the only authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-AC | Elevation/Info | all /api/tasks* + /api/task-lists* routes | mitigate | every route uses `Depends(require_hub_session)`; session = owner check (single allowed Google account, no per-task owner_id needed per RESEARCH V4) |
| T-27-IV | Tampering | POST/PATCH task bodies | mitigate | Pydantic CreateTaskInput/UpdateTaskInput: title 1..500, notes ≤10000, due_date/due_time regex, priority enum, list_id; invalid → 422/400 |
| T-27-REP | Repudiation | POST /api/tasks/{id}/hard-delete | mitigate | server rejects (409) unless the doc is status="completing" — a replayed/forged hard-delete of an active task cannot succeed |
| T-27-SC | Tampering | npm/pip installs | accept | no package installs in this plan |
</threat_model>

<verification>
- `pytest tests/test_web_server.py::TestTaskRoutes -x` green
- Existing routes regression: `pytest tests/test_web_server.py -x` green (no break to /api/today, /api/chat, /api/auth)
- No CORS headers added; all routes same-origin session-cookie only
</verification>

<success_criteria>
- ~10 task/task-list routes exist, all session-gated, all _jsonsafe_doc-wrapped
- Pydantic input validation enforces ASVS V5 bounds
- hard-delete guarded on status="completing"
- TestTaskRoutes green; existing web_server tests unbroken
</success_criteria>

<output>
Create `.planning/phases/27-tasks/27-02-SUMMARY.md` when done.
</output>
