---
phase: 27-tasks
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - core/tools.py
  - core/autonomous.py
  - tests/test_tools.py
  - tests/test_autonomous.py
autonomous: true
requirements: [TASK-05]

must_haves:
  truths:
    - "core/tools.py exposes 6 native task tool schemas (task_create, task_list, task_complete, task_reschedule, task_edit, task_delete) backed by TaskStore — full native toolset replacing TickTick add_task/get_today_tasks (D-16)"
    - "The old add_task schema + _handle_add_task + _HANDLERS['add_task'] + the dead _ticktick_add_task reference are removed"
    - "core/autonomous.py _gather_native_overdue reads TaskStore.get_overdue and returns [{title, due}, ...] (TickTick-compatible shape)"
    - "The 'ticktick_overdue' situation key is UNCHANGED in the jobs dict and all triage/compose references (D-17 pure data-source swap)"
  artifacts:
    - path: "core/tools.py"
      provides: "6 native TaskStore-backed tool schemas + handlers; add_task removed"
      contains: "task_create"
    - path: "core/autonomous.py"
      provides: "_gather_native_overdue reading TaskStore"
      contains: "_gather_native_overdue"
  key_links:
    - from: "core/tools.py _handle_task_*"
      to: "memory.firestore_db.TaskStore"
      via: "direct import (not the dead _ticktick_add_task)"
      pattern: "from memory.firestore_db import TaskStore"
    - from: "core/autonomous.py jobs dict"
      to: "_gather_native_overdue"
      via: "key 'ticktick_overdue' unchanged"
      pattern: "\"ticktick_overdue\": _gather_native_overdue"
---

<objective>
Swap Klaus's task tooling from the single TickTick `add_task` tool to a full native TaskStore-backed toolset (create / list / complete / reschedule / edit / delete), and repoint the autonomous Layer-0 overdue gather from `ticktick_tool` to `TaskStore` — keeping the `ticktick_overdue` situation key name unchanged so triage/compose prompts and eval fixtures need zero changes (D-16/D-17, RESEARCH Unknown 5).

Purpose: Give Klaus full conversational task management (the UAT "ask Klaus to reschedule it" path) and make the autonomous tick read native overdue tasks — a pure data-source swap that satisfies TASK-05. The TickTick *files* are NOT removed here (D-09 gates that on native UAT — see 27-07).
Output: 6 new tool schemas + 6 `_handle_task_*` functions + 6 `_HANDLERS` entries in `core/tools.py` (add_task removed); `_gather_native_overdue` in `core/autonomous.py`; `TestNativeTaskTools`, `TestNativeOverdueGather`, `TestJobsDict` tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-tasks/27-CONTEXT.md
@.planning/phases/27-tasks/27-RESEARCH.md
@.planning/phases/27-tasks/27-VALIDATION.md
@CLAUDE.md
@.planning/phases/27-tasks/27-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

core/tools.py (exact locations to change, RESEARCH Unknown 5):
- add_task schema: lines 212–247 (in TOOL_SCHEMAS list) — REMOVE
- _handle_add_task: lines 1357–1364 — REMOVE (it references _ticktick_add_task, which is never imported/defined — dead code, A4/Pitfall 5)
- _HANDLERS["add_task"]: line 2258 — REMOVE
- Schema convention: each entry = {"name", "description", "input_schema": {"type":"object","properties":{...},"required":[...]}}
- Handler convention: `def _handle_<name>(**kwargs) -> str:` returning `json.dumps(result)`
- Dispatch convention: `"<name>": lambda args: _handle_<name>(**args),`

6 native tools (RESEARCH Unknown 5):
- task_create(title, notes?, due_date?, due_time?, priority?, list_id?)
- task_list(list_id?, date?, priority?, overdue?)  — query by list / date / priority / overdue
- task_complete(task_id)
- task_reschedule(task_id, due_date, due_time?)
- task_edit(task_id, title?, notes?, priority?, list_id?)
- task_delete(task_id)
All worker-delegatable (do NOT add to SMART_AGENT_DIRECT_TOOLS).

core/autonomous.py (RESEARCH Unknown 5):
- _gather_ticktick_overdue: lines 236–244 — RENAME to _gather_native_overdue, repoint to TaskStore.get_overdue, return [{"title": t["title"], "due": t.get("due_date","")} for t in tasks] (Pitfall 3 — exact shape)
- jobs dict line 433: `"ticktick_overdue": _gather_ticktick_overdue` → `"ticktick_overdue": _gather_native_overdue` (KEY NAME UNCHANGED)
- "ticktick_overdue" key referenced at lines 180, 463, 487–493, 516, 598, 684, 729 — ZERO changes (key preserved, A6)
- TaskStore instantiation: project_id=os.environ["GCP_PROJECT_ID"], database=os.getenv("FIRESTORE_DATABASE","(default)"); today_iso = datetime.now(ZoneInfo("Asia/Jerusalem")).date().isoformat()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Native task tools in core/tools.py; remove add_task</name>
  <read_first>
    - core/tools.py (lines 212–247 add_task schema; 1357–1364 _handle_add_task; 2248–2303 _HANDLERS block; the schema/handler/dispatch conventions around them)
    - .planning/phases/27-tasks/27-RESEARCH.md (Unknown 5 tool set + dead-code finding; Pattern 2)
    - .planning/phases/27-tasks/27-01-SUMMARY.md (TaskStore method signatures)
    - tests/test_tools.py (TestNativeTaskTools stub from 27-01)
  </read_first>
  <behavior>
    - TOOL_SCHEMAS contains task_create, task_list, task_complete, task_reschedule, task_edit, task_delete; does NOT contain add_task
    - _HANDLERS has the 6 task_* keys; does NOT have add_task
    - _handle_task_create imports TaskStore directly and returns json.dumps of the created task
    - importing core.tools does not raise NameError (the dead _ticktick_add_task reference is gone)
    - each handler instantiates TaskStore with env-driven project_id/database
  </behavior>
  <action>
    In `core/tools.py`: remove the `add_task` schema (lines 212–247), `_handle_add_task` (lines 1357–1364), and `_HANDLERS["add_task"]` (line 2258) — leave no path back to the dead `_ticktick_add_task` (Pitfall 5). Add 6 new schemas to `TOOL_SCHEMAS` matching the `<interfaces>` signatures, with clear descriptions (e.g. task_list description: "Query Amit's native tasks by list, date, priority, or overdue flag"). Add 6 `_handle_task_*(**kwargs) -> str` functions, each doing `from memory.firestore_db import TaskStore` + instantiating with `os.environ.get("GCP_PROJECT_ID","")` / `os.environ.get("FIRESTORE_DATABASE","(default)")`, calling the matching TaskStore method, and returning `json.dumps(result)`. task_complete passes the Asia/Jerusalem `date.today()`-equivalent completed_on. Add the 6 dispatch entries to `_HANDLERS`. Then implement `TestNativeTaskTools` in `tests/test_tools.py` (replace the 27-01 skip stub): assert all 6 names are in TOOL_SCHEMAS, assert `add_task` is NOT in TOOL_SCHEMAS, assert the 6 keys are in _HANDLERS and `add_task` is not, and assert `import core.tools` succeeds (regression guard for A4).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && python -m pytest tests/test_tools.py::TestNativeTaskTools -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_tools.py::TestNativeTaskTools -x` passes
    - `grep -c "_ticktick_add_task" core/tools.py` returns 0
    - `grep -n "\"add_task\"" core/tools.py` returns nothing (schema + handler key both gone)
    - `grep -n "task_create\|task_complete\|task_reschedule" core/tools.py` shows the new schemas + handlers
    - `python -c "import core.tools"` exits 0
  </acceptance_criteria>
  <done>Six native task tools registered, add_task fully removed, no dead reference, TestNativeTaskTools green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint autonomous overdue gather to TaskStore (key name unchanged)</name>
  <read_first>
    - core/autonomous.py (lines 236–244 _gather_ticktick_overdue; line 433 jobs dict; lines 180, 463, 487–493, 516, 598, 684, 729 — the ticktick_overdue key usages that must NOT change)
    - .planning/phases/27-tasks/27-RESEARCH.md (Unknown 5 autonomous repoint code; Pitfall 3 return shape)
    - tests/test_autonomous.py (TestNativeOverdueGather + TestJobsDict stubs from 27-01)
  </read_first>
  <behavior>
    - _gather_native_overdue() reads TaskStore.get_overdue(today_iso) and returns a list of {"title": str, "due": str}
    - on any exception it returns [] (never raises — autonomous tick must keep running)
    - jobs["ticktick_overdue"] now points to _gather_native_overdue
    - the string literal "ticktick_overdue" still appears in the jobs dict and is unchanged everywhere else
  </behavior>
  <action>
    In `core/autonomous.py`: rename `_gather_ticktick_overdue` → `_gather_native_overdue`, replacing the body with the TaskStore version from RESEARCH Unknown 5 — import `TaskStore` from `memory.firestore_db`, compute `today_iso` in Asia/Jerusalem, call `store.get_overdue(today_iso)`, and return `[{"title": t["title"], "due": t.get("due_date", "")} for t in tasks]` (Pitfall 3 — exact TickTick-compatible shape); wrap in try/except returning `[]` with `logger.warning(..., exc_info=True)`. Update the jobs dict entry at line 433 to `"ticktick_overdue": _gather_native_overdue` — KEY NAME UNCHANGED. Make ZERO changes to the key references at lines 180, 463, 487–493, 516, 598, 684, 729 (D-17 / A6). Then implement `TestNativeOverdueGather` (assert return shape is a list of {"title","due"} dicts given a mocked TaskStore.get_overdue) and `TestJobsDict` (assert `"ticktick_overdue"` is still a key in the jobs dict built by the gather entrypoint) in `tests/test_autonomous.py`, replacing the 27-01 skip stubs.
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && python -m pytest tests/test_autonomous.py::TestNativeOverdueGather tests/test_autonomous.py::TestJobsDict -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_autonomous.py::TestNativeOverdueGather tests/test_autonomous.py::TestJobsDict -x` passes
    - `grep -n "_gather_native_overdue" core/autonomous.py` shows the function def and the jobs entry
    - `grep -c "ticktick_overdue" core/autonomous.py` is unchanged from baseline (the situation key still present everywhere — confirm count matches pre-change; key not renamed)
    - `grep -n "_gather_ticktick_overdue" core/autonomous.py` returns nothing (function renamed)
    - TestNativeOverdueGather asserts the {"title","due"} shape (not raw TaskStore docs — Pitfall 3)
  </acceptance_criteria>
  <done>Autonomous gather reads TaskStore, returns the TickTick-compatible shape, key name preserved, both tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Klaus agent loop → TaskStore | Tool args (LLM-generated) reach TaskStore; the route-layer Pydantic validation does not apply here |
| autonomous tick → TaskStore | Layer-0 gather reads overdue tasks before tick-brain triage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-ID | Information Disclosure | autonomous gather during migration | mitigate | D-09 order — TickTick tools stay live until native is UAT-verified (27-07); this plan adds native gather but the file removal is gated, so there is no native/TickTick double-read window in production until cutover |
| T-27-IV | Tampering | _handle_task_* tool args | mitigate | TaskStore stores due_date/due_time as plain strings (27-01); handlers pass through TaskStore which never stores Timestamps; title is plain text |
| T-27-SC | Tampering | npm/pip installs | accept | no package installs in this plan |
</threat_model>

<verification>
- `pytest tests/test_tools.py::TestNativeTaskTools tests/test_autonomous.py::TestNativeOverdueGather tests/test_autonomous.py::TestJobsDict -x` green
- Tool-swap regression: `pytest tests/test_tools.py tests/test_autonomous.py -x` green
- `python -c "import core.tools; import core.autonomous"` exits 0 (no NameError from removed dead code)
</verification>

<success_criteria>
- 6 native task tools registered + add_task fully removed (no dead _ticktick_add_task)
- Autonomous gather reads TaskStore with the TickTick-compatible {title, due} shape
- "ticktick_overdue" situation key unchanged everywhere (D-17)
- All three new test classes green; no tool-swap regression
</success_criteria>

<output>
Create `.planning/phases/27-tasks/27-03-SUMMARY.md` when done.
</output>
