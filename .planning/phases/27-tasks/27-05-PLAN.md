---
phase: 27-tasks
plan: 05
type: execute
wave: 3
depends_on: ["27-04"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/tasks/TasksPage.tsx
  - frontend/src/components/tasks/TaskListView.tsx
  - frontend/src/components/tasks/TaskRow.tsx
  - frontend/src/components/tasks/TaskDetailSheet.tsx
  - frontend/src/components/tasks/TaskListSidebar.tsx
  - frontend/src/components/tasks/TaskListSelector.tsx
  - frontend/src/components/tasks/RecurrenceSelector.tsx
  - frontend/src/components/tasks/SortGroupControl.tsx
  - frontend/src/components/tasks/UndoToast.tsx
autonomous: true
requirements: [TASK-01, TASK-02, TASK-04]

must_haves:
  truths:
    - "The /tasks route renders the real TasksPage (ComingSoon placeholder removed)"
    - "TaskListView lists active tasks with the sort/group control; empty states match the copywriting contract"
    - "TaskRow shows checkbox + title + priority chip + due chip + list name; tapping the checkbox runs the completion micro-animation then the undo toast"
    - "TaskDetailSheet creates/edits all task fields with a context-aware CTA ('Add task' vs 'Save changes') and recurrence selector; recurring edits prompt 'this occurrence only' vs 'this and following'"
    - "TaskListSidebar (desktop) lists Inbox + user lists with accent active state; new-list inline input works"
    - "Completing/deleting a task soft-marks (optimistic remove), shows UndoToast (4s, last-action-wins), and hard-deletes on expiry"
  artifacts:
    - path: "frontend/src/components/tasks/TasksPage.tsx"
      provides: "Root /tasks page composing sidebar + list view"
      contains: "TaskListView"
    - path: "frontend/src/components/tasks/TaskRow.tsx"
      provides: "Task row + completion micro-animation"
      contains: "CheckCircle2"
    - path: "frontend/src/components/tasks/UndoToast.tsx"
      provides: "4s undo toast → hard-delete"
  key_links:
    - from: "frontend/src/App.tsx /tasks route"
      to: "TasksPage"
      via: "route element swap (ComingSoon → TasksPage)"
      pattern: "TasksPage"
    - from: "TaskRow completion"
      to: "undoStore + useCompleteTask + hardDeleteTask"
      via: "soft-mark → 4s timer → hard-delete"
      pattern: "hardDelete"
---

<objective>
Build the Tasks page UI: replace the `/tasks` ComingSoon placeholder with the real `TasksPage` and its component tree (list view, rows, detail sheet, list sidebar/selector, recurrence selector, sort/group control, undo toast). Wire the completion/delete → soft-mark → undo → hard-delete flow against the 27-04 data layer and undoStore. Render exactly to the 27-UI-SPEC contract.

Purpose: Deliver the real task-manager feel Amit asked for (user-creatable lists, recurrence, sort/group, satisfying completion animation + undo) — the core of TASK-01/02/04 on the hub surface.
Output: 9 new `components/tasks/*` components + the `/tasks` route swap in `App.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-tasks/27-CONTEXT.md
@.planning/phases/27-tasks/27-UI-SPEC.md
@.planning/phases/27-tasks/27-RESEARCH.md
@CLAUDE.md
@.planning/phases/27-tasks/27-04-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

Data layer (from 27-04 — see 27-04-SUMMARY.md):
- hooks: useTasks(listId), useTaskLists(), useTaskSummary(); api/tasks.ts createTask/updateTask/completeTask/undoTask/hardDeleteTask; api/task-lists.ts
- store/undoStore.ts: zustand single-active-item {id, action, listId}, show(item), clear()
- Task/TaskList interfaces exported from api/tasks.ts / api/task-lists.ts

Phase 26 component conventions:
- frontend/src/components/timeline/TimelineDay.tsx — inline-style components, color hex literals, `fontFamily` constant; loading/error/empty states; no CSS framework classes beyond Tailwind utility (`className="hidden md:block"`, `animate-pulse`)
- frontend/src/components/layout/DockChat.tsx — bottom-sheet translateY transition (250ms ease-out) pattern to mirror for TaskDetailSheet (phone)
- frontend/src/tokens.ts — dominant/secondary/accent/textPrimary/textSecondary/surface border/success tokens; typography map

App.tsx routing (line 153): `<Route path="/tasks" element={<TasksPage />} />`; TasksPage currently `return <ComingSoon label="Tasks" />` (line 57–59) — replace its body to render the real component.

UI-SPEC anchors (27-UI-SPEC.md):
- Colors: page bg #0A0A0A, cards #1A1A1A, border #2A2A2A, accent #6366F1, destructive #EF4444, success #22C55E, text #F9FAFB/#9CA3AF; priority chips High #F87171/Med #FBBF24/Low textSecondary text-only
- Touch targets ≥44px; row min-height 52px
- Completion animation: 150ms circle fill (#22C55E) + 150ms checkmark stroke-dashoffset + 200ms max-height:0 collapse
- Undo toast: 4s, single active, last-action-wins, copy "Task completed."/"Task deleted." + "Undo"
- CTA states: new task "Add task", existing "Save changes"
- Recurring edit sheet: "This occurrence only" / "This and following"
- Empty states + all copy in 27-UI-SPEC § Copywriting Contract (use verbatim)
- icons: Circle/CheckCircle2, Flag, CalendarDays, RotateCcw, AlertCircle, Plus, Trash2, MoreHorizontal (aria-label "Task options"), ChevronDown, GripHorizontal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TasksPage + TaskListView + TaskListSidebar/Selector + SortGroupControl + route swap</name>
  <read_first>
    - frontend/src/App.tsx (lines 34–80 ComingSoon + TasksPage + routing; line 153 route)
    - frontend/src/components/timeline/TimelineDay.tsx (inline-style + loading/error/empty pattern to mirror)
    - frontend/src/tokens.ts (token names + typography map)
    - .planning/phases/27-tasks/27-UI-SPEC.md (§ Component Inventory, § Responsive Layout, § Copywriting Contract, § Interaction Contracts — list navigation + sort/group)
    - .planning/phases/27-tasks/27-04-SUMMARY.md (useTasks/useTaskLists signatures)
  </read_first>
  <action>
    Create `components/tasks/TasksPage.tsx` as the root `/tasks` component: desktop = `TaskListSidebar` (200px) + `TaskListView` (flex-1); phone = current-list header (taps open `TaskListSelector` bottom sheet) + `TaskListView`. Manage the active list_id via `useState` (no per-list route). Create `TaskListView.tsx` (consumes `useTasks(listId)`; renders `SortGroupControl` header + a `TaskRow` per task; loading skeleton, error, and the exact empty states from § Copywriting — "Your Inbox is clear." / "This list is empty." with the body lines). Create `TaskListSidebar.tsx` (Inbox always first with accent active state per § Interaction Contracts; user lists from `useTaskLists`; "New list" inline input → createList; accent 4px left border on active). Create `TaskListSelector.tsx` (shared list-picker dropdown/sheet). Create `SortGroupControl.tsx` (two segmented groups: Sort Due date/Priority, Group On/Off; day-scoped views auto-sort by priority per D-18; local state only). Apply auto-sort (D-18: list views by due or priority via the control; no drag-reorder). Swap `App.tsx` so `TasksPage` returns `<TasksPage />` from the new component (remove the ComingSoon body for Tasks; keep Habits/Health ComingSoon untouched).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds (TypeScript + Vite compile)
    - `grep -n "ComingSoon label=\"Tasks\"" frontend/src/App.tsx` returns nothing (placeholder removed for Tasks)
    - `grep -n "import { TasksPage }" frontend/src/App.tsx` or equivalent import of the real component is present
    - TaskListView contains the verbatim empty-state strings "Your Inbox is clear." and "This list is empty."
    - SortGroupControl exposes Sort (Due date/Priority) and Group (On/Off); no drag-reorder code (grep no "drag"/"onDrop")
    - TaskListSidebar applies the accent #6366F1 active indicator
  </acceptance_criteria>
  <done>TasksPage + list view + sidebar/selector + sort control render; /tasks routes to the real page; build green.</done>
</task>

<task type="auto">
  <name>Task 2: TaskRow + completion micro-animation + UndoToast + soft-mark→hard-delete flow</name>
  <read_first>
    - .planning/phases/27-tasks/27-UI-SPEC.md (§ Interaction Contracts — task row, completion micro-animation, delete flow, undo toast)
    - .planning/phases/27-tasks/27-RESEARCH.md (Unknown 3 — soft-mark → 4s client timer → hard-delete; NEVER BackgroundTask; toast stacking last-wins)
    - frontend/src/store/undoStore.ts + frontend/src/hooks/useTasks.ts (from 27-04)
    - CLAUDE.md (§6 — the hard-delete must be a tracked client request, the 4s timer lives in the browser)
  </read_first>
  <behavior>
    - tap checkbox: circle fills success green (150ms) → checkmark draws (150ms) → row collapses max-height:0 (200ms); row removed from list optimistically
    - on tap: completeTask(id) fires (server soft-marks status="completing" + generates recurring next); undoStore.show({id, action:'complete'})
    - UndoToast shows for 4s; "Undo" → undoTask(id) + restore row + clear; expiry → hardDeleteTask(id)
    - delete (swipe phone / kebab desktop): same 4s soft-mark→hard-delete; toast copy "Task deleted."
    - second action before first toast expires: prior item's hard-delete fires immediately; new item gets fresh 4s (last-action-wins, single toast)
    - the 4s timer is a browser setTimeout — NOT a server BackgroundTask
  </behavior>
  <action>
    Create `components/tasks/TaskRow.tsx`: checkbox (left 44px tap target, Circle→CheckCircle2), title (body), priority chip (Flag, High #F87171 / Med #FBBF24 / Low text-only / None hidden), due chip (CalendarDays; overdue → destructive #EF4444 with "Nd overdue" via AlertCircle), recurrence indicator (RotateCcw if recurrence present), list name (textSecondary). Phone: swipe-left reveals 72px destructive Delete; desktop: kebab `MoreHorizontal` with `aria-label="Task options"` exposing Edit/Delete. Implement the completion micro-animation exactly per UI-SPEC (150ms fill + 150ms checkmark stroke-dashoffset + 200ms collapse). Create `components/tasks/UndoToast.tsx` driven by `undoStore`: positioned per UI-SPEC (phone above BottomTabs, desktop bottom-center), copy "Task completed."/"Task deleted." + accent "Undo", 4s auto-dismiss with 200ms fade. Wire the soft-mark → 4s `setTimeout` → `hardDeleteTask` flow in TaskRow/undoStore: on action call `completeTask`/(delete path), `undoStore.show`, start a browser timer; on Undo call `undoTask` (and for delete, re-insert into the query cache) + cancel timer; on expiry call `hardDeleteTask`. Last-action-wins: showing a new item immediately resolves the prior item's hard-delete. NEVER place the timer server-side (CLAUDE.md §6 / Pitfall 4).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds
    - TaskRow uses CheckCircle2 + the 150/150/200ms animation timings (grep the durations in TaskRow.tsx)
    - The kebab button carries `aria-label="Task options"` (grep returns a line)
    - UndoToast copy uses verbatim "Task completed." / "Task deleted." / "Undo"
    - The undo timer is a client setTimeout (grep `setTimeout` in TaskRow.tsx or undo logic); no fetch to a server-side delay endpoint
    - hardDeleteTask is called on timer expiry; undoTask on Undo
  </acceptance_criteria>
  <done>TaskRow + animation + UndoToast render; completion/delete soft-mark→undo→hard-delete works client-side; build green.</done>
</task>

<task type="auto">
  <name>Task 3: TaskDetailSheet (create/edit) + RecurrenceSelector + recurring-edit dialog</name>
  <read_first>
    - .planning/phases/27-tasks/27-UI-SPEC.md (§ Interaction Contracts — task detail sheet, recurring task edit dialog, optimistic create/edit; § Copywriting Contract — CTA states, recurrence options)
    - frontend/src/components/layout/DockChat.tsx (bottom-sheet translateY 250ms transition to mirror on phone)
    - frontend/src/hooks/useTasks.ts (useCreateTask/useUpdateTask optimistic mutations from 27-04)
    - .planning/phases/27-tasks/27-CONTEXT.md (D-05/06/07 recurrence semantics, D-01 fields)
  </read_first>
  <behavior>
    - new-task mode: CTA "Add task"; edit mode: CTA "Save changes"; both accent 44px
    - fields in order: Title (body single-line), Notes (multi-line 3 rows), Due date (native input[type=date]), "Add time" toggle → input[type=time], Priority (None/Low/Medium/High), List selector, RecurrenceSelector
    - RecurrenceSelector: cadence ("Does not repeat"/"Daily"/"Weekdays"/"Weekly"/"Monthly"/"Every N days") + anchor toggle ("Stick to schedule"/"From completion"); every-N exposes an N input
    - existing task: "Delete task" destructive text button (left) + "Cancel" link
    - saving an edit to a recurring task: 2-choice action sheet "This occurrence only" / "This and following" (heading "Edit recurring task") gates the save
    - create/edit are optimistic (row appears opacity-75 saving → full; rollback + error toast on failure)
    - dirty tracking: not saved until CTA tapped
  </behavior>
  <action>
    Create `components/tasks/TaskDetailSheet.tsx` (bottom sheet on phone via translateY transition mirroring DockChat; centered modal max-width 480px on desktop) rendering all fields in the UI-SPEC order with the context-aware CTA ("Add task" creating, "Save changes" editing). Wire create via `useCreateTask` and edit via `useUpdateTask` (optimistic per § Optimistic create/edit; error toasts "Couldn't save task — try again." / "Couldn't update task — try again."). Create `components/tasks/RecurrenceSelector.tsx` with the cadence select + anchor toggle + every-N input, mapping UI labels to the `{cadence, every_n_days, anchor}` rule shape from 27-01. For a recurring-task edit, show the 2-choice action sheet (heading "Edit recurring task", options "This occurrence only" / "This and following") and forward the choice as the `scope` field to PATCH/complete per D-07 (this-only = override current instance without touching the rule; this-and-following = update the rule). Delete in the sheet routes through the same undo-toast flow as TaskRow (no confirmation modal — D-14).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds
    - TaskDetailSheet CTA reads "Add task" in create mode and "Save changes" in edit mode (both strings present)
    - RecurrenceSelector contains all cadence labels verbatim ("Does not repeat", "Daily", "Weekdays", "Weekly", "Monthly", "Every N days") and both anchor labels ("Stick to schedule", "From completion")
    - The recurring-edit sheet contains "This occurrence only" and "This and following" under heading "Edit recurring task"
    - create/edit use the optimistic mutation hooks from 27-04 (grep useCreateTask / useUpdateTask)
    - No delete confirmation modal — delete flows to UndoToast
  </acceptance_criteria>
  <done>TaskDetailSheet creates/edits all fields with context-aware CTA; recurrence + recurring-edit dialog work; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task content → DOM | Task title/notes rendered in React components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-TI | Tampering (XSS) | TaskRow / TaskDetailSheet rendering title+notes | mitigate | React default text escaping; NEVER dangerouslySetInnerHTML for task content (same rule as MessageBubble) — task title/notes always rendered as plain text children |
| T-27-REP | Repudiation | client hard-delete timing | mitigate | hard-delete is a tracked client fetch (not a server BackgroundTask); server still rejects hard-delete of non-"completing" docs (27-02) |
| T-27-SC | Tampering | npm installs | accept | no new installs (chrono-node installed in 27-04) |
</threat_model>

<verification>
- `cd frontend && npm run build` green (whole task UI compiles)
- No `dangerouslySetInnerHTML` anywhere in components/tasks (grep returns nothing)
- Existing frontend tests still pass: `cd frontend && npm test -- --run` green
</verification>

<success_criteria>
- /tasks renders the real TasksPage; ComingSoon removed for Tasks
- Completion micro-animation + 4s undo toast + soft-mark→hard-delete all client-side per UI-SPEC
- TaskDetailSheet context-aware CTA, full field set, recurrence + recurring-edit dialog
- Build green; no XSS-prone rendering
</success_criteria>

<output>
Create `.planning/phases/27-tasks/27-05-SUMMARY.md` when done.
</output>
