---
phase: 27-tasks
plan: 07
type: execute
wave: 5
depends_on: ["27-02", "27-03", "27-04", "27-05", "27-06"]
files_modified:
  - core/tools.py
  - mcp_tools/ticktick_tool.py
  - mcp_tools/ticktick_auth.py
  - core/autonomous.py
  - docs/DEPLOYMENT.md
  - tests/test_tools.py
autonomous: false
requirements: [TASK-05, TASK-06]

must_haves:
  truths:
    - "Native tasks are UAT-verified working (create/edit/complete/recurrence + Klaus reschedule via chat) BEFORE any TickTick code is removed"
    - "Amit has manually re-created his open TickTick tasks in TaskStore before the cutover — no automated import script and no reconciliation report (D-08)"
    - "After UAT: mcp_tools/ticktick_tool.py and mcp_tools/ticktick_auth.py are deleted and all TickTick imports/references are gone from core/"
    - "The TickTick subscription cancellation + Secret Manager secret cleanup are flagged as operator actions AFTER code removal"
  artifacts:
    - path: "mcp_tools/ticktick_tool.py"
      provides: "DELETED — TickTick removed (D-09 cutover)"
    - path: "docs/DEPLOYMENT.md"
      provides: "TickTick retirement steps + secret cleanup operator note"
  key_links:
    - from: "core/autonomous.py"
      to: "TaskStore only"
      via: "no remaining ticktick_tool import"
      pattern: "ticktick"
---

<objective>
Execute the D-09 TickTick cutover IN ORDER: a blocking UAT checkpoint confirms native tasks work (including manual re-entry of open tasks) BEFORE any TickTick code is removed; then delete `mcp_tools/ticktick_tool.py` + `ticktick_auth.py` and purge all remaining TickTick references from `core/`; then flag subscription cancellation + Secret Manager cleanup as operator actions. This plan must NOT run until 27-03/27-05/27-06 are live and verified — it is gated last by design.

Purpose: Retire TickTick safely — Klaus keeps the TickTick fallback live until native is proven, preventing the migration information-disclosure / data-loss window (T-27-ID, D-09). Closes TASK-06 (manual migration order) and finalizes TASK-05 (TickTick fully removed).
Output: TickTick files deleted; core/ free of TickTick references; DEPLOYMENT.md retirement runbook; operator actions surfaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-tasks/27-CONTEXT.md
@.planning/phases/27-tasks/27-RESEARCH.md
@CLAUDE.md
@.planning/phases/27-tasks/27-03-SUMMARY.md
@.planning/phases/27-tasks/27-05-SUMMARY.md
@.planning/phases/27-tasks/27-06-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

TickTick removal targets (RESEARCH § State of the Art / Deprecated after Phase 27):
- mcp_tools/ticktick_tool.py — DELETE (the add_task / get_today_tasks tool removed in 27-03; this file is now unused by tools + autonomous after the 27-03 swap)
- mcp_tools/ticktick_auth.py — DELETE (TickTick OAuth token mgmt)
- core/tools.py — confirm no remaining `ticktick` import (27-03 removed add_task; check for any other reference)
- core/autonomous.py — 27-03 already repointed the gather to TaskStore; confirm no remaining `from mcp_tools import ticktick_tool`
- Env/Secret Manager (operator): TICKTICK_ACCESS_TOKEN, TICKTICK_REFRESH_TOKEN, TICKTICK_CLIENT_ID, TICKTICK_CLIENT_SECRET — remove AFTER subscription cancelled (Open Question 2)

CLAUDE.md / docs reference ticktick in prose (CLAUDE.md §4 layout, docs/*) — update docs but the directory-layout description in CLAUDE.md may be updated in the docs sweep; at minimum remove the deleted files from the live layout note.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Native tasks are fully built and deployed (27-01 through 27-06): TaskStore + recurrence, /api/tasks routes, Klaus native task tools, the hub Tasks page with quick-add + recurrence + undo, and timeline/glance-rail surfacing. TickTick is STILL LIVE (its tools were swapped out of the agent in 27-03, but the files remain and the subscription is active) so nothing is lost if native has a problem. This checkpoint gates the irreversible cutover.</what-built>
  <how-to-verify>
    Per D-09, verify native tasks work end-to-end BEFORE we remove TickTick:
    1. In the hub: create a task with a due date + priority in a user-created list; edit it; complete it (confirm the micro-animation + undo toast); delete one and undo within the window.
    2. Create a recurring task (e.g. weekly, stick-to-schedule), complete it, confirm the next instance appears with the correct date.
    3. Quick-add: "gym tomorrow #health !high" — confirm the chips resolve and the task lands correctly.
    4. Ask Klaus in chat to reschedule a task ("move my X task to friday") — confirm he uses the native tool and it updates in the hub.
    5. Confirm the Today timeline "Due today" band + glance rail counts reflect your tasks.
    6. CRITICAL — manual migration (TASK-06): re-create your few open TickTick tasks in the hub now. Confirm they all appear.
    Only after ALL of the above pass should TickTick be removed.
  </how-to-verify>
  <resume-signal>Type "approved — native verified, tasks re-entered, remove TickTick" to proceed with deletion, or describe what failed to halt the cutover.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Delete TickTick files + purge remaining references</name>
  <read_first>
    - mcp_tools/ticktick_tool.py + mcp_tools/ticktick_auth.py (confirm what's being deleted)
    - core/tools.py (confirm 27-03 left no ticktick import/handler)
    - core/autonomous.py (confirm 27-03 repoint left no `from mcp_tools import ticktick_tool`)
    - tests/test_tools.py + tests/test_ticktick_tool.py (existing TickTick tests to remove/adjust)
    - .planning/phases/27-tasks/27-03-SUMMARY.md (what was already swapped)
  </read_first>
  <behavior>
    - mcp_tools/ticktick_tool.py and mcp_tools/ticktick_auth.py no longer exist
    - no module under core/ imports ticktick_tool or ticktick_auth
    - importing core.tools, core.autonomous, interfaces.web_server all succeed
    - the TickTick-specific test file (tests/test_ticktick_tool.py) is removed or skipped; no test imports the deleted modules
  </behavior>
  <action>
    First grep the whole repo for remaining references: `grep -rn "ticktick" core/ interfaces/ mcp_tools/ scripts/` to enumerate every live import/usage. Confirm 27-03 already removed the `add_task` tool + repointed the autonomous gather (only the situation KEY name "ticktick_overdue" remains by design — leave it). Delete `mcp_tools/ticktick_tool.py` and `mcp_tools/ticktick_auth.py`. Remove any remaining `from mcp_tools import ticktick_tool` / `ticktick_auth` imports or references found by the grep in `core/` and `interfaces/`. Delete `tests/test_ticktick_tool.py` (and remove any TickTick-only tests in tests/test_tools.py that referenced the deleted add_task). Update any TickTick bootstrap/oauth helper scripts references if they break imports. Do NOT rename the `ticktick_overdue` situation key (D-17 / A6). Confirm the three entrypoint modules import cleanly.
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && ! test -f mcp_tools/ticktick_tool.py && ! test -f mcp_tools/ticktick_auth.py && python -c "import core.tools, core.autonomous, interfaces.web_server" && python -m pytest tests/test_tools.py tests/test_autonomous.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `mcp_tools/ticktick_tool.py` and `mcp_tools/ticktick_auth.py` do not exist
    - `grep -rn "import ticktick\|from mcp_tools import ticktick" core/ interfaces/` returns nothing
    - `python -c "import core.tools, core.autonomous, interfaces.web_server"` exits 0
    - `pytest tests/test_tools.py tests/test_autonomous.py -x` passes (TestNativeTaskTools + autonomous still green)
    - `grep -rn "ticktick_overdue" core/autonomous.py` still present (situation key intentionally preserved — count unchanged)
    - No test imports the deleted modules (`pytest --co -q` collects with no import errors)
  </acceptance_criteria>
  <done>TickTick files deleted, all live references purged, entrypoints import cleanly, tool/autonomous tests green, situation key preserved.</done>
</task>

<task type="auto">
  <name>Task 2: DEPLOYMENT.md retirement runbook + operator cleanup note</name>
  <read_first>
    - docs/DEPLOYMENT.md (operator runbook structure; secrets section; the composite-index table from 27-01)
    - .planning/phases/27-tasks/27-RESEARCH.md (Open Question 2 — secret cleanup timing; § State of the Art deprecated env vars)
    - CLAUDE.md (§4 live directory layout listing ticktick files; §5 secrets — to note the retirement)
  </read_first>
  <action>
    Add a "TickTick Retirement (Phase 27, v5.0)" subsection to `docs/DEPLOYMENT.md` documenting the completed cutover and the remaining OPERATOR actions (these are manual, post-code-removal, not Claude actions): (1) cancel the TickTick subscription; (2) AFTER cancellation, remove the four Secret Manager secrets `TICKTICK_ACCESS_TOKEN`, `TICKTICK_REFRESH_TOKEN`, `TICKTICK_CLIENT_ID`, `TICKTICK_CLIENT_SECRET` from the `klaus-agent` Cloud Run config + Secret Manager. Note that the migration was manual (no importer, D-08) and that native task tasks are stored in Firestore `tasks`/`task_lists` collections with the composite indexes created in 27-01. Update the CLAUDE.md §4 live directory layout to drop the two deleted `mcp_tools/ticktick_*.py` lines (minimal edit — full docs sweep is out of scope).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus && grep -n "TickTick Retirement\|TICKTICK_ACCESS_TOKEN" docs/DEPLOYMENT.md && ! grep -n "ticktick_tool.py" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - DEPLOYMENT.md contains a "TickTick Retirement" subsection listing subscription cancellation + the four secrets to remove
    - The runbook states secret removal happens AFTER subscription cancellation (Open Question 2 ordering)
    - CLAUDE.md §4 no longer lists `mcp_tools/ticktick_tool.py` / `ticktick_auth.py`
  </acceptance_criteria>
  <done>Retirement runbook documents the operator cleanup; CLAUDE.md layout updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration cutover | The window where TickTick is removed and native becomes the sole task source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-ID | Information Disclosure / data loss | TickTick→native cutover | mitigate | D-09 ordering enforced by the blocking-human UAT checkpoint: native verified + tasks manually re-entered BEFORE any TickTick code removed — no overlap window, no silent task loss |
| T-27-SC | Tampering | npm/pip installs | accept | no package installs in this plan (removal only) |
</threat_model>

<verification>
- UAT checkpoint approved before deletion (manual gate)
- `python -c "import core.tools, core.autonomous, interfaces.web_server"` exits 0 after deletion
- `pytest tests/test_tools.py tests/test_autonomous.py -x` green; full 1153+ baseline holds (run the regression set per VALIDATION § Phase gate)
- No live `import ticktick` reference remains in core/ or interfaces/
</verification>

<success_criteria>
- Native tasks UAT-verified + open tasks manually re-entered BEFORE removal (D-09)
- TickTick files deleted; core/interfaces free of TickTick imports; entrypoints clean
- ticktick_overdue situation key preserved (D-17)
- DEPLOYMENT.md retirement runbook + operator secret-cleanup note; CLAUDE.md layout updated
</success_criteria>

<output>
Create `.planning/phases/27-tasks/27-07-SUMMARY.md` when done.
</output>
