---
phase: 27-tasks
plan: 06
type: execute
wave: 4
depends_on: ["27-05"]
files_modified:
  - frontend/src/components/tasks/QuickAddBar.tsx
  - frontend/src/components/tasks/TaskFAB.tsx
  - frontend/src/components/tasks/TasksPage.tsx
  - frontend/src/components/timeline/DueTasksBand.tsx
  - frontend/src/components/timeline/TimelineDay.tsx
  - frontend/src/components/layout/GlanceRail.tsx
autonomous: true
requirements: [TASK-03, TASK-07]

must_haves:
  truths:
    - "Quick-add works: FAB on phone opens QuickAddBar as a bottom sheet; pressing N on desktop opens it inline above the list (D-10)"
    - "QuickAddBar live-resolves the input via parseTaskInput: date chip, list chip, priority chip update while typing; unparsed → Inbox (D-10)"
    - "The Today timeline shows a pinned 'Due today' band (DueTasksBand) above timed events when there are due/overdue tasks (D-11)"
    - "The glance rail shows a Tasks section: N due today + N overdue (overdue in destructive color, hidden when 0) (D-12)"
    - "No exact-time task reminders / OS notifications are built this phase — deferred to Phase 29 Web Push (D-19)"
    - "DueTasksBand renders actual due/overdue task rows (checkbox + title + overdue chip per UI-SPEC) via useTasks or equivalent, hidden when count==0; both DueTasksBand and GlanceRail use useTaskSummary for the count/guard (react-query dedupes the fetch)"
  artifacts:
    - path: "frontend/src/components/tasks/QuickAddBar.tsx"
      provides: "Live-parsing quick-add input"
      contains: "parseTaskInput"
    - path: "frontend/src/components/timeline/DueTasksBand.tsx"
      provides: "Pinned Due today band on the timeline"
      contains: "useTaskSummary"
    - path: "frontend/src/components/layout/GlanceRail.tsx"
      provides: "Tasks section (due today + overdue)"
      contains: "overdue"
  key_links:
    - from: "frontend/src/components/tasks/QuickAddBar.tsx"
      to: "frontend/src/utils/parseTaskInput.ts + useCreateTask"
      via: "live parse on keystroke + submit creates task"
      pattern: "parseTaskInput"
    - from: "frontend/src/components/timeline/DueTasksBand.tsx + GlanceRail.tsx"
      to: "useTaskSummary"
      via: "shared react-query key"
      pattern: "useTaskSummary"
---

<objective>
Complete the task UI: quick-add (FAB on phone, `N` keyboard shortcut on desktop) with live token/date resolution via `parseTaskInput`, and the TASK-07 surfacing — a pinned "Due today" band on the Today timeline plus a Tasks section on the glance rail, both reading the deduped `useTaskSummary` hook.

Purpose: Deliver the one-line power-capture Amit asked for (D-10) and surface due/overdue tasks where he sees them (timeline + glance rail, D-11/D-12), closing TASK-03 and TASK-07.
Output: `QuickAddBar.tsx` + `TaskFAB.tsx` (wired into TasksPage), `DueTasksBand.tsx` (into TimelineDay), and the GlanceRail Tasks section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-tasks/27-CONTEXT.md
@.planning/phases/27-tasks/27-UI-SPEC.md
@CLAUDE.md
@.planning/phases/27-tasks/27-04-SUMMARY.md
@.planning/phases/27-tasks/27-05-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

From 27-04: parseTaskInput(raw, refDate?) → {title, due_date, list_name, priority}; useTaskSummary() → {due_today, overdue} (refetch-on-focus, no timer); useCreateTask (optimistic); useTaskLists() for #list fuzzy match.

From 27-05: TasksPage manages active list_id; UndoToast + completion flow already wired; component inline-style conventions established.

Extension points:
- frontend/src/components/timeline/TimelineDay.tsx — insert DueTasksBand AFTER all-day events (Section 3, ~line 239) and BEFORE timed events (Section 4, ~line 241). Band only renders when due_today+overdue > 0 (no empty placeholder).
- frontend/src/components/layout/GlanceRail.tsx — add a "Tasks" card BELOW the Nutrition card (~line 102, after the nutrition div); use useTaskSummary (NOT useToday). Mirror the NutritionRow inline-style pattern.

UI-SPEC anchors (27-UI-SPEC.md):
- FAB: 56px accent #6366F1 circle, fixed bottom:76px (above BottomTabs), aria-label "Add task", Plus icon, phone only
- Desktop N key: opens QuickAddBar inline above the list, only when not focused in an input
- QuickAddBar placeholder "Add a task…  #list  !priority  date"; resolved chips: date "D Mon" (e.g. "18 Jun"), priority "High"/"Medium"/"Low", list chip; submit "Add task" or Enter; Escape/blur dismiss
- DueTasksBand: accent left-border stripe (4px×32px #6366F1), section label "Due today" (label size, textSecondary); compact rows checkbox+title+overdue chip ("Nd overdue" destructive); tapping checkbox = same completion flow; tapping title navigates to /tasks
- GlanceRail Tasks: "N due today" (textPrimary value) + "N overdue" (destructive #EF4444 when >0, hidden when 0); tapping navigates to /tasks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuickAddBar (live parse) + TaskFAB + N-key shortcut</name>
  <read_first>
    - .planning/phases/27-tasks/27-UI-SPEC.md (§ Interaction Contracts — quick-add FAB/keyboard, § Copywriting Contract — placeholder + chip copy)
    - frontend/src/utils/parseTaskInput.ts (from 27-04 — the parser contract)
    - frontend/src/components/tasks/TasksPage.tsx (from 27-05 — where FAB + quick-add mount; active list_id state)
    - frontend/src/hooks/useTasks.ts + useTaskLists.ts (useCreateTask, list fuzzy match)
    - frontend/src/components/layout/BottomTabs.tsx (phone tab bar height for FAB positioning)
  </read_first>
  <behavior>
    - typing "gym tomorrow #health !high" shows: date chip "19 Jun", list chip "health" (fuzzy-matched to a real list or Inbox), priority chip "High"; title becomes "gym"
    - submit (Add task / Enter) → useCreateTask with parsed fields (list_name fuzzy-matched to list_id; unmatched → "inbox"); input clears for another entry
    - Escape / tap-outside / blur dismisses without saving
    - phone: FAB (56px accent, bottom:76px, aria-label "Add task") opens QuickAddBar as a bottom sheet over BottomTabs
    - desktop: pressing N (when not in an input) opens QuickAddBar inline above the list
  </behavior>
  <action>
    Create `components/tasks/QuickAddBar.tsx`: a single-line input (placeholder "Add a task…  #list  !priority  date") that calls `parseTaskInput(value, new Date())` on every keystroke and renders the resolved date/list/priority chips inline per UI-SPEC. On submit, fuzzy-match `list_name` against `useTaskLists()` (unmatched → "inbox") and call `useCreateTask` with `{title, due_date, priority, list_id}`; clear the input. Escape/blur dismisses. Create `components/tasks/TaskFAB.tsx` (phone only: 56px accent Plus FAB fixed at bottom:76px, `aria-label="Add task"`, opens QuickAddBar as a bottom sheet). Wire both into `TasksPage.tsx`: render TaskFAB on phone; add a desktop `keydown` listener for `N` (ignore when `document.activeElement` is an input/textarea) that opens QuickAddBar inline above TaskListView. Show the desktop hint "Press N to add a task." only in the empty-list state (per § Copywriting).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus/frontend && npm run build && npm test -- --run src/utils/parseTaskInput.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds and parseTaskInput tests still green
    - QuickAddBar imports and calls parseTaskInput on input change (grep parseTaskInput in QuickAddBar.tsx)
    - TaskFAB carries `aria-label="Add task"` and is 56px / accent #6366F1 (grep the values)
    - The N-key listener guards against firing while focused in an input (grep activeElement or tagName check)
    - QuickAddBar placeholder is the verbatim "Add a task…  #list  !priority  date"
    - Unmatched #list falls back to "inbox"
  </acceptance_criteria>
  <done>Quick-add works on phone (FAB) and desktop (N); live parse resolves chips; build + parser tests green.</done>
</task>

<task type="auto">
  <name>Task 2: DueTasksBand on the timeline + GlanceRail Tasks section</name>
  <read_first>
    - frontend/src/components/timeline/TimelineDay.tsx (Section 3 all-day ~line 199–239, Section 4 timed ~line 241 — insert DueTasksBand between them)
    - frontend/src/components/layout/GlanceRail.tsx (Nutrition card ~line 57–102 — add Tasks card below; NutritionRow inline-style pattern)
    - frontend/src/hooks/useTaskSummary.ts (from 27-04 — the shared deduped hook)
    - .planning/phases/27-tasks/27-UI-SPEC.md (§ Today Timeline — Due Tasks Band, § Glance Rail — Tasks Section)
    - frontend/src/store/undoStore.ts + components/tasks/UndoToast.tsx (completion flow to reuse)
  </read_first>
  <behavior>
    - DueTasksBand renders only when (due_today + overdue) > 0; positioned after all-day events, before timed events
    - band: accent left-border stripe + "Due today" label; each row = checkbox + title + overdue chip ("Nd overdue" destructive when overdue)
    - tapping a band checkbox runs the same completion micro-animation + undo flow as the Tasks page
    - tapping a band row title navigates to /tasks
    - GlanceRail shows a Tasks card below Nutrition: "N due today" + "N overdue" (overdue destructive #EF4444 when >0, row hidden when 0); tapping navigates to /tasks
    - both read useTaskSummary (one network fetch, react-query dedupes)
  </behavior>
  <action>
    Create `components/timeline/DueTasksBand.tsx` consuming `useTaskSummary()` (and `useTasks` for the actual due/overdue task rows, or extend the summary endpoint usage — read 27-04-SUMMARY.md for whether the summary returns task lists or just counts; if counts-only, fetch the due/overdue task rows via `useTasks` filtered client-side or a small dedicated query). Render the band per UI-SPEC (accent 4px×32px stripe, "Due today" label, compact rows with checkbox + title + overdue chip), reusing the 27-05 completion flow (undoStore + completeTask + micro-animation) for checkbox taps and `useNavigate('/tasks')` for title taps. Insert `<DueTasksBand />` into `TimelineDay.tsx` between Section 3 (all-day) and Section 4 (timed). Extend `GlanceRail.tsx` with a Tasks card below the Nutrition card, consuming `useTaskSummary()`: render "N due today" (textPrimary) and "N overdue" (destructive #EF4444 when >0, hidden when 0) using the existing NutritionRow inline-style idiom; make the card tappable to navigate to /tasks. Both DueTasksBand and GlanceRail MUST use `useTaskSummary` so the fetch is shared (do NOT add a second summary fetch).
  </action>
  <verify>
    <automated>cd /Users/amitgrupper/Desktop/Klaus/frontend && npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds and the full frontend test run is green (existing TimelineDay tests still pass)
    - `grep -n "useTaskSummary" frontend/src/components/timeline/DueTasksBand.tsx frontend/src/components/layout/GlanceRail.tsx` shows both consume the shared hook
    - `grep -n "DueTasksBand" frontend/src/components/timeline/TimelineDay.tsx` shows the band inserted
    - DueTasksBand renders nothing when due_today+overdue == 0 (no empty placeholder — grep a guard condition)
    - GlanceRail overdue row uses destructive #EF4444 and is hidden when count is 0
    - Band section label is verbatim "Due today"; overdue chip is "Nd overdue" format
  </acceptance_criteria>
  <done>Due today band on the timeline + Tasks section on the glance rail, both reading useTaskSummary; build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quick-add keystrokes → task creation | Raw input parsed + submitted as a new task |
| task titles → DOM (band + summary) | Surfaced task content rendered on timeline/glance rail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-TI | Tampering (XSS/injection) | QuickAddBar parse + DueTasksBand title render | mitigate | parseTaskInput strips #/! tokens to safe charset (27-04); title rendered as plain React text children, no dangerouslySetInnerHTML |
| T-27-IV | Tampering | quick-add createTask body | mitigate | server-side Pydantic re-validates (27-02); client parse is convenience only |
| T-27-AC | Info | useTaskSummary fetch | mitigate | /api/tasks/summary is session-gated (27-02) |
| T-27-SC | Tampering | npm installs | accept | no new installs |
</threat_model>

<verification>
- `cd frontend && npm run build` green; `cd frontend && npm test -- --run` green (no TimelineDay/GlanceRail regression)
- Single summary fetch: DueTasksBand + GlanceRail both via useTaskSummary (react-query dedupes)
- No dangerouslySetInnerHTML in any new component
</verification>

<success_criteria>
- Quick-add: FAB (phone) + N-key (desktop), live parse, unparsed→Inbox (TASK-03)
- Due today band on timeline + Tasks section on glance rail, deduped via useTaskSummary (TASK-07)
- Build + frontend tests green
</success_criteria>

<output>
Create `.planning/phases/27-tasks/27-06-SUMMARY.md` when done.
</output>
