---
phase: 32-unified-situation-ambient-memory
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp_tools/memory.py
  - core/tools.py
  - core/reflection.py
  - tests/test_tools.py
  - tests/test_reflection.py
autonomous: true
requirements: [MEM-03]
user_setup: []

must_haves:
  truths:
    - "Amit can deliberately forget a memory: forget_memory deletes a Pinecone vector by id"
    - "A malformed vector_id fails cleanly (validated as a string) — no unhandled exception into tool dispatch"
    - "Nightly reflection flags a memory contradicted by newer facts as a woven note; it never auto-deletes"
  artifacts:
    - path: "mcp_tools/memory.py"
      provides: "forget_memory handler (Pinecone delete-by-id)"
      contains: "forget_memory"
    - path: "core/tools.py"
      provides: "forget_memory tool schema + registration (3-site)"
      contains: "forget_memory"
    - path: "core/reflection.py"
      provides: "contradicted-memory nightly flag"
      contains: "contradict"
  key_links:
    - from: "core/tools.py"
      to: "mcp_tools/memory.py::forget_memory"
      via: "_HANDLERS dispatch"
      pattern: "forget_memory"
    - from: "mcp_tools/memory.py::forget_memory"
      to: "Pinecone index.delete"
      via: "index.delete(ids=[vector_id])"
      pattern: "delete\\(ids"
---

<objective>
Ship the correction/hygiene half of ambient memory IN THIS PHASE (Pitfall 5, non-optional
per D-04): a deliberate `forget_memory` tool (Pinecone delete-by-id) and a reflection
contradiction-flag woven into the nightly narrative. Auto-injecting memory (Plan 06)
without a correction path is unsafe — a stale fact would re-surface every turn forever.

Purpose: MEM-03 — deliberate-only forgetting, no auto-decay. `forget_memory` is Amit's
trigger ("forget that"); reflection only SUGGESTS deletion in the nightly ("I still have
you down as marathon-training — drop that?") and deletes ONLY on Amit's confirmation.
Output: `forget_memory` registered end-to-end + input-validated; contradiction flag woven
into `core/reflection.py`'s nightly digest, mirroring the Phase 31 prune-flag pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md

<interfaces>
From mcp_tools/memory.py: `MemoryTool` wraps a `MemoryStore` with `remember`/`recall`
(lines 23-66). `MemoryStore._get_index()` already constructs the Pinecone index client;
delete-by-id is the stock `index.delete(ids=[vector_id])` call (Don't Hand-Roll).
From core/tools.py: tool registration is a verified 3-site pattern —
schema list (lines ~40-42), tool-schema dict (lines 433/463), and `_HANDLERS`/dispatch
(lines ~1486-1487, handler `_handle_remember` at 1832). `recall`/`remember` are the analogs.
From core/reflection.py: the learning loop already reads `get_recent_window` (line 178) and
weaves prune-flags into the nightly narrative (Phase 31 pattern — DIR-07). The contradiction
flag mirrors that weaving, NOT a new surface.
Security (ASVS V5): validate `vector_id` as a well-formed non-empty string before delete.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: forget_memory handler + 3-site tool registration + vector_id validation</name>
  <read_first>mcp_tools/memory.py (lines 1-70 remember/recall shape); core/tools.py (lines 40-42, 433-490, 1486-1487, 1832 remember/recall registration + handler analog); .planning/phases/32-unified-situation-ambient-memory/32-RESEARCH.md (§Don't Hand-Roll — Pinecone delete-by-id; §Security Domain V5)</read_first>
  <files>mcp_tools/memory.py, core/tools.py, tests/test_tools.py</files>
  <behavior>
    - `forget_memory(vector_id)` calls `index.delete(ids=[vector_id])` on the same MemoryStore-constructed index; returns a structured success dict.
    - A non-string / empty / malformed `vector_id` returns a clean error dict — never raises into tool dispatch (ASVS V5).
    - Registered at all three core/tools.py sites so the brain can call it directly (mirrors recall).
  </behavior>
  <action>Add a `forget_memory` method to `MemoryTool` in `mcp_tools/memory.py` (thin wrapper mirroring `remember`/`recall`), validating `vector_id` is a non-empty string first and returning `{"ok": False, "error": ...}` on malformed input, else calling the stock `index.delete(ids=[vector_id])` via the existing `MemoryStore` index accessor. Register the tool in `core/tools.py` at all three sites (name list, schema dict with a `vector_id` string param + clear description that this is deliberate deletion, `_HANDLERS` dispatch → new `_handle_forget_memory`). Do NOT build a tombstone/soft-delete scheme — Pinecone hard delete-by-id is native.</action>
  <verify>
    <automated>pytest tests/test_tools.py -k forget_memory -x</automated>
  </verify>
  <acceptance_criteria>Tests assert: (a) a valid vector_id triggers `index.delete(ids=[<id>])` (mock the index); (b) a non-string / empty vector_id returns an error dict and does NOT call delete and does NOT raise; (c) `forget_memory` resolves through `core/tools.py` dispatch by name.</acceptance_criteria>
  <done>forget_memory callable end-to-end, input-validated, hard-deletes by id; three registration sites present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reflection contradiction-flag woven into the nightly narrative (never auto-delete)</name>
  <read_first>core/reflection.py (line 178 get_recent_window usage + the prune-flag / nightly-weaving section from Phase 31); prompts/reflection.md; tests/test_reflection.py (existing contradiction/prune-flag test conventions)</read_first>
  <files>core/reflection.py, tests/test_reflection.py</files>
  <behavior>
    - When the day's facts contradict a stored memory (e.g. a superseded goal), reflection surfaces a one-line woven nightly note suggesting Amit drop it — phrased as a question, not an action.
    - Reflection NEVER calls `forget_memory` itself — deletion happens only on Amit's later confirmation (D-04).
    - Absence of any contradiction produces no note (empty-state discipline, mirrors the prune-flag).
  </behavior>
  <action>Extend `core/reflection.py`'s nightly-weaving logic (the same place the Phase 31 prune-flag is woven) to detect a memory contradicted by newer facts from the day and emit a woven suggestion line in the nightly digest — never auto-delete. Reuse the existing recall/day-facts already gathered; keep it best-effort (a failure to detect yields no flag, never breaks the nightly). Add prompt guidance in `prompts/reflection.md` if the model composes the phrasing.</action>
  <verify>
    <automated>pytest tests/test_reflection.py -k contradiction -x</automated>
  </verify>
  <acceptance_criteria>A stale-fact fixture (a fact stored, then contradicted by a later day-fact) makes the nightly digest surface a contradiction suggestion line; the test asserts reflection does NOT call any delete path. A no-contradiction fixture produces no flag.</acceptance_criteria>
  <done>Contradiction flag woven into the nightly; deliberate-only forgetting preserved (reflection never deletes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brain/tool-dispatch → Pinecone | `forget_memory` executes a destructive delete on untrusted `vector_id` input |
| stored memory → agent belief state | auto-injected facts can go stale (ambient poisoning, ASI06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering / Input | `forget_memory` vector_id | mitigate | Validate vector_id as a non-empty string before `index.delete`; malformed → clean error dict, no unhandled exception (ASVS V5) |
| T-32-05 | Tampering (belief state) | Ambient memory poisoning (ASI06) | mitigate | Ship the correction path (forget_memory + reflection contradiction-flag) in the SAME phase as auto-recall (Plan 06); score-thresholding (Plan 06) filters weak matches |
| T-32-06 | Repudiation | Accidental irreversible delete | accept | Single-user system; Pinecone hard delete is the chosen deliberate mechanism (D-04); reflection never auto-deletes, Amit confirms |
</threat_model>

<verification>
- `pytest tests/test_tools.py -k forget_memory -x` and `pytest tests/test_reflection.py -k contradiction -x` green.
- `grep -n "forget_memory" core/tools.py` shows three registration sites.
- `grep -n "delete(ids" mcp_tools/memory.py` confirms native delete-by-id.
</verification>

<success_criteria>
`forget_memory` deletes a Pinecone vector by id with input validation and full 3-site
registration; reflection flags contradicted memories in the nightly without ever
auto-deleting — the deliberate-only correction path lands with (before) auto-recall.
</success_criteria>

<output>
Create `.planning/phases/32-unified-situation-ambient-memory/32-03-SUMMARY.md` when done.
</output>
