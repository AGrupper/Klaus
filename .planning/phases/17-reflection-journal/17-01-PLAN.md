---
phase: 17-reflection-journal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_reflection.py
  - memory/firestore_db.py
  - memory/pinecone_db.py
autonomous: true
requirements: [JOUR-02, JOUR-03, JOUR-04]
must_haves:
  truths:
    - "tests/test_reflection.py exists and is the Wave 0 test scaffold for JOUR-01..06"
    - "JournalStore round-trips date-keyed journal docs (set / get / get_recent)"
    - "A re-run of a date overwrites the journal doc cleanly with no stale keys"
    - "remember_self upserts a Pinecone vector with deterministic id self-{date}"
    - "_VALID_KINDS includes 'self' and recall(kinds=['self']) returns only self vectors"
    - "Default recall (no kinds) behavior is unchanged — still fact+chunk"
  artifacts:
    - path: "tests/test_reflection.py"
      provides: "Wave 0 test scaffold covering JOUR-01..06 + D-03/D-13"
      contains: "test_journal_store_roundtrip"
    - path: "memory/firestore_db.py"
      provides: "JournalStore class — date-keyed journal collection"
      contains: "class JournalStore"
    - path: "memory/pinecone_db.py"
      provides: "'self' kind + remember_self() deterministic-id upsert path"
      contains: "def remember_self"
  key_links:
    - from: "memory/pinecone_db.py remember_self"
      to: "Pinecone index upsert"
      via: "deterministic vector id self-{date}"
      pattern: "self-\\{date"
    - from: "memory/pinecone_db.py recall"
      to: "_VALID_KINDS"
      via: "kinds validation"
      pattern: "_VALID_KINDS"
---

<objective>
Build the Phase 17 data-layer foundation: the Wave 0 test scaffold plus the two
storage primitives the reflection orchestrator depends on — a date-keyed
`JournalStore` in Firestore and a deterministic-ID journal-vector upsert path in
Pinecone (`kind="self"`).

Purpose: `core/reflection.py` (Plan 02) cannot be written until these stores
exist. This plan is Wave 1 — nothing depends on it being parallel-safe, but
everything downstream depends on it being correct.
Output: `tests/test_reflection.py` (test scaffold), `JournalStore` class,
`remember_self()` method, `"self"` added to `_VALID_KINDS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-reflection-journal/17-CONTEXT.md
@.planning/phases/17-reflection-journal/17-RESEARCH.md
@.planning/phases/17-reflection-journal/17-PATTERNS.md
@.planning/phases/17-reflection-journal/17-VALIDATION.md
@docs/CODING_STANDARDS.md

<interfaces>
<!-- Contracts the executor must replicate. Verified from codebase 2026-05-19. -->

memory/firestore_db.py existing helpers (reuse — do NOT re-create):
- `_make_firestore_client(project_id, database)` at line ~24 — credential logic
- module `logger` at line ~21
- `from google.cloud import firestore` at line ~18 (gives `firestore.SERVER_TIMESTAMP`)
- `SelfStateStore` at line ~601 — get/set error-handling shape; `_COLLECTION` class const
- `AttendanceStore` at line ~226 — date-keyed collection pattern

memory/pinecone_db.py existing structures:
- `_VALID_KINDS = frozenset({"fact", "chunk", "chat"})` at line ~29
- `CONTENT_MAX_CHARS = 2000` at line ~28
- `class MemoryStore` at line ~32 (NOTE: class is `MemoryStore`, NOT `PineconeStore`)
- `MemoryStore.remember()` at line ~55 — embed+upsert body to copy; hard-codes `uuid.uuid4()` at ~80
- `MemoryStore.recall()` at line ~96 — already accepts `kinds`; `kinds` default `["fact","chunk"]` at ~112; `$eq` user filter at ~117-119
- `self._embed(content)` — single embed, no burst-sleep needed
- `self._get_index()` — returns the Pinecone index handle

Journal entry dict shape (D-03 + D-04) — what JournalStore.set receives:
```python
{
  "summary": str,            # 2-3 sentence day overview
  "mood": str,               # short string
  "current_focus": str,
  "recent_context": str,
  "highlights": list[str],
  "message_count": int,      # raw metric
  "cost_usd": float,         # raw metric
  "calendar_event_count": int,
  "tasks_completed": int,
  "heartbeat_ok": bool,
}
```

Test mocking patterns (verified from tests/):
- `tests/test_llm_usage_store.py` — `_install_firestore_mock()` installs a mock
  `google.cloud.firestore` into `sys.modules` (`firestore_mock.SERVER_TIMESTAMP = object()`,
  `firestore_mock.Increment` sentinel). Use this exact approach for JournalStore tests.
- `tests/test_proactive_alerts.py` — `os.environ.setdefault("TELEGRAM_ALLOWED_USER_IDS", "123456")`
  before importing the module under test; `@patch` decorators target `core.<module>.<attr>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Wave 0 test scaffold tests/test_reflection.py</name>
  <read_first>
    - tests/test_reflection.py (confirm it does not yet exist)
    - tests/test_proactive_alerts.py (mocking + import-before-patch pattern)
    - tests/test_llm_usage_store.py (`_install_firestore_mock()` sys.modules pattern)
    - .planning/phases/17-reflection-journal/17-VALIDATION.md (the 9 test names — locked)
  </read_first>
  <action>
    Create `tests/test_reflection.py` as the Wave 0 scaffold. It MUST define
    exactly these 9 test function names (from 17-VALIDATION.md, names are LOCKED):
      - test_journal_store_roundtrip          (JOUR-02)
      - test_recall_self_kind                 (JOUR-04)
      - test_remember_self_deterministic_id   (JOUR-03)
      - test_run_reflection_writes_entry      (JOUR-01)
      - test_gather_source_failure_is_isolated(JOUR-01)
      - test_reflection_llm_failure_fallback  (D-13)
      - test_parse_reflection_json_hardened   (D-03)
      - test_cron_reflect_route               (JOUR-05)
      - test_journal_digest_assembly          (JOUR-06)

    For THIS plan, write FULL implementations for the 3 tests that this plan's
    code can satisfy:
      - test_journal_store_roundtrip: install the firestore mock via the
        `_install_firestore_mock()` pattern from test_llm_usage_store.py; build
        a `JournalStore`, call `.set("2026-05-19", {...full entry dict...})`,
        then `.get("2026-05-19")` and assert the 5 LLM fields + 5 raw metrics
        round-trip, and that the returned dict has `date == "2026-05-19"`. Also
        call `.get_recent(3)` on a 4-entry collection and assert newest-first
        ordering, length 3.
      - test_remember_self_deterministic_id: build a `MemoryStore` with mocked
        `_embed` and mocked `_get_index`; call `remember_self(123, "2026-05-19",
        "some content")` twice; assert the upsert vector `id` equals
        `"self-2026-05-19"` BOTH times (deterministic — re-run overwrites).
      - test_recall_self_kind: assert `"self" in pinecone_db._VALID_KINDS`;
        assert `recall(..., kinds=["self"])` passes a `kind` filter of
        `["self"]` to the index query; assert `recall(...)` with NO kinds arg
        still filters on `["fact","chunk"]` (default unchanged).

    For the remaining 6 tests (run_reflection, gather isolation, llm fallback,
    json parse, cron route, digest assembly) write a STUB body that is
    `pytest.skip("implemented in plan 17-02 / 17-03 / 17-04")` — they will be
    fleshed out by the plan that builds the code under test. The scaffold MUST
    collect and run (all 3 real tests pass, 6 skip) so Wave 0 is satisfied.

    Per docs/CODING_STANDARDS.md: module docstring at top explaining the mock
    strategy; `from __future__ import annotations`. No live GCP/Pinecone I/O —
    everything mocked.
  </action>
  <verify>
    <automated>pytest tests/test_reflection.py -q --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_reflection.py` exists
    - `grep -c "^def test_" tests/test_reflection.py` returns exactly `9`
    - All 9 locked test names present: `grep -E "def (test_journal_store_roundtrip|test_recall_self_kind|test_remember_self_deterministic_id|test_run_reflection_writes_entry|test_gather_source_failure_is_isolated|test_reflection_llm_failure_fallback|test_parse_reflection_json_hardened|test_cron_reflect_route|test_journal_digest_assembly)" tests/test_reflection.py` returns 9 lines
    - `pytest tests/test_reflection.py -q --collect-only` exits 0 and collects 9 items
    - `grep -c "pytest.skip" tests/test_reflection.py` returns at least `6`
  </acceptance_criteria>
  <done>Test scaffold collects 9 tests; 3 real (pass after Tasks 2-3), 6 skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JournalStore class to memory/firestore_db.py</name>
  <read_first>
    - memory/firestore_db.py (read SelfStateStore ~601-668 and AttendanceStore ~226-330 in full — these are the templates)
    - .planning/phases/17-reflection-journal/17-PATTERNS.md § "memory/firestore_db.py — add JournalStore class" (the full copy-adapt code block)
    - .planning/phases/17-reflection-journal/17-RESEARCH.md § "Code Examples" → JournalStore block
  </read_first>
  <behavior>
    - JournalStore.set("2026-05-19", entry) then get("2026-05-19") returns entry
      with all 5 LLM fields + 5 raw metrics + date == "2026-05-19"
    - get() on a missing date returns None (never raises)
    - A re-run set() with FEWER keys replaces the doc — no stale keys remain
      (D-12: `.set()` WITHOUT merge=True)
    - get_recent(3) on a 4-doc collection returns 3 docs, newest date first
    - get_recent on a Firestore error returns [] (never raises)
  </behavior>
  <action>
    Add a `JournalStore` class to `memory/firestore_db.py`. Place it after the
    `SelfStateStore` class definition. Reuse the module's existing
    `_make_firestore_client`, `logger`, and `firestore` import — add NO new
    imports.

    Class constant: `_COLLECTION = "journal"` (mirrors `SelfStateStore._COLLECTION`).

    Constructor — copy `AttendanceStore.__init__` shape exactly:
    ```python
    def __init__(self, project_id: str, database: str = "(default)") -> None:
        self._client = _make_firestore_client(project_id, database)
        self._col = self._client.collection(self._COLLECTION)
    ```

    Three methods (full bodies in 17-PATTERNS.md — copy them):
    - `get(self, date_str: str) -> dict | None` — read `self._col.document(date_str).get()`;
      if `not snap.exists` return `None`; else `data = snap.to_dict() or {}`,
      set `data["date"] = snap.id`, return `data`. Wrap in `try/except Exception`
      → `logger.warning("JournalStore.get(%r) failed", date_str, exc_info=True)`
      and return `None`. Reads NEVER raise.
    - `set(self, date_str: str, entry: dict) -> None` — CRITICAL: use
      `self._col.document(date_str).set({**entry, "date": date_str, "updated_at":
      firestore.SERVER_TIMESTAMP})` — `.set()` WITHOUT `merge=True` (D-12:
      overwrite the whole doc so a re-run with fewer fields leaves no stale
      keys; this DIFFERS from `SelfStateStore.set` which uses `merge=True`).
      Wrap in `try/except Exception` → `logger.error(...)` then `raise` (writes
      re-raise so the caller decides).
    - `get_recent(self, n: int) -> list[dict]` — `snaps = list(self._col.stream())`
      inside `try/except` (return `[]` on error); for each snap build
      `data = snap.to_dict() or {}`, `data["date"] = snap.id`; collect, then
      `results.sort(key=lambda d: d.get("date", ""), reverse=True)`; return
      `results[:n]`. Newest-first, no composite index needed (single-user,
      low volume — same `stream()`+sort approach as `LLMUsageStore.summary`).

    Docstrings per docs/CODING_STANDARDS.md; note in `set`'s docstring that it
    overwrites (D-12 idempotency).
  </action>
  <verify>
    <automated>pytest tests/test_reflection.py::test_journal_store_roundtrip -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class JournalStore" memory/firestore_db.py` returns one line
    - `grep -n '_COLLECTION = "journal"' memory/firestore_db.py` returns one line
    - `grep -n "def get_recent" memory/firestore_db.py` returns a line within JournalStore
    - JournalStore.set uses `.set(` with NO `merge=True`: `grep -A3 "def set" memory/firestore_db.py | grep -c "merge=True"` shows the JournalStore.set block has no merge=True (verify by reading the method)
    - `pytest tests/test_reflection.py::test_journal_store_roundtrip -x -q` passes
  </acceptance_criteria>
  <done>JournalStore round-trips date-keyed docs; set overwrites; get_recent newest-first; reads never raise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add "self" kind + remember_self() to memory/pinecone_db.py</name>
  <read_first>
    - memory/pinecone_db.py (read module docstring ~6-13, `_VALID_KINDS` ~29, `CONTENT_MAX_CHARS` ~28, `remember()` ~55-94, `recall()` ~96-130 in full)
    - .planning/phases/17-reflection-journal/17-PATTERNS.md § "memory/pinecone_db.py" (full remember_self code block)
    - .planning/phases/17-reflection-journal/17-RESEARCH.md § Pitfall 2 (content cap) and Security Domain (cross-user leak mitigation)
  </read_first>
  <behavior>
    - "self" is a member of _VALID_KINDS
    - remember_self(user_id, "2026-05-19", content) upserts a vector with
      id == "self-2026-05-19"; a second call with the same date upserts the
      SAME id (deterministic — overwrites, no duplicate)
    - remember_self truncates content longer than CONTENT_MAX_CHARS (2000)
      BEFORE embedding — it does NOT raise (unlike remember())
    - upsert metadata has user_id == str(user_id) and kind == "self"
    - recall(kinds=["self"]) works with no further change to recall()
  </behavior>
  <action>
    Two changes to `memory/pinecone_db.py`:

    (1) D-06 — extend `_VALID_KINDS` at line ~29:
    ```python
    _VALID_KINDS = frozenset({"fact", "chunk", "chat", "self"})
    ```
    Also update the module docstring (~lines 6-13) which enumerates the kinds —
    add "self" to that prose list (journal/self-memory entries).

    (2) D-07 — add a new `remember_self()` method to the `MemoryStore` class.
    `remember()` hard-codes `vector_id = str(uuid.uuid4())` so it CANNOT produce
    a deterministic ID — a new method is mandatory. Copy the embed+upsert body
    of `remember()` but substitute the ID and TRUNCATE instead of raising:
    ```python
    def remember_self(self, user_id: int, date_str: str, content: str) -> str:
        """Upsert a journal entry with a deterministic vector ID (self-{date}).

        A re-run for the same date overwrites the existing vector — no
        duplicates (D-07/D-12). Truncates content over CONTENT_MAX_CHARS
        rather than raising (Pitfall 2).
        """
        if len(content) > CONTENT_MAX_CHARS:
            content = content[:CONTENT_MAX_CHARS]
        vector = self._embed(content)
        vector_id = f"self-{date_str}"
        ts = datetime.now(tz=timezone.utc).isoformat()
        self._get_index().upsert(vectors=[{
            "id": vector_id,
            "values": vector,
            "metadata": {
                "user_id": str(user_id),
                "kind": "self",
                "content": content,
                "ts": ts,
            },
        }])
        return vector_id
    ```
    The `metadata.user_id` MUST be `str(user_id)` — identical to `remember()`'s
    format — so `recall()`'s `$eq` user filter isolates journal vectors per
    user (Security Domain: cross-user leak mitigation). If `datetime`/`timezone`
    are not already imported at the top of `pinecone_db.py`, add
    `from datetime import datetime, timezone` (check first — `remember()` may
    already import them).

    NO change to `recall()` — it already accepts `kinds` and applies
    `{"kind": {"$in": _kinds}}`; once `"self"` is in `_VALID_KINDS`,
    `recall(kinds=["self"])` works.
  </action>
  <verify>
    <automated>pytest tests/test_reflection.py::test_remember_self_deterministic_id tests/test_reflection.py::test_recall_self_kind -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"self"' memory/pinecone_db.py` shows "self" inside the `_VALID_KINDS` frozenset
    - `grep -n "def remember_self" memory/pinecone_db.py` returns one line
    - `grep -n 'f"self-{date_str}"' memory/pinecone_db.py` returns one line (deterministic id)
    - `grep -n "CONTENT_MAX_CHARS" memory/pinecone_db.py` shows remember_self truncating (not raising) — verify by reading the method
    - `pytest tests/test_reflection.py::test_remember_self_deterministic_id tests/test_reflection.py::test_recall_self_kind -x -q` passes
  </acceptance_criteria>
  <done>"self" in _VALID_KINDS; remember_self upserts deterministic id self-{date}, truncates oversized content; recall(kinds=["self"]) works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reflection cron → Pinecone | LLM-generated journal text crosses into the vector store |
| Pinecone recall → any user | `kind="self"` vectors must stay scoped to their owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Denial of Service | `remember_self()` Pinecone upsert | mitigate | Truncate `content` to `CONTENT_MAX_CHARS` (2000) before `_embed()` — Task 3 enforces this; oversized content cannot raise or bloat the index |
| T-17-02 | Information Disclosure | `kind="self"` recall | mitigate | `remember_self` writes `metadata.user_id = str(user_id)` identical to `remember()`; `recall()`'s existing `$eq` user filter isolates journal vectors per owner. Single-user system — LOW severity, but the scoping is enforced not assumed |
| T-17-03 | Tampering | `JournalStore.set` Firestore write | accept | Single-user trusted system; journal docs are read back only into Klaus's own prompt, never executed. No HIGH-severity exposure |
</threat_model>

<verification>
- `pytest tests/test_reflection.py -q` — 3 real tests pass, 6 skip
- `pytest tests/ -q` — full suite still green (no regression in firestore_db / pinecone_db consumers)
</verification>

<success_criteria>
- `tests/test_reflection.py` exists with all 9 locked test names; collects 9 items
- `JournalStore` round-trips date-keyed docs; `set` overwrites (no merge); `get_recent` newest-first
- `"self"` in `_VALID_KINDS`; `remember_self` produces deterministic `self-{date}` id and truncates oversized content
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/17-reflection-journal/17-01-SUMMARY.md`
</output>
