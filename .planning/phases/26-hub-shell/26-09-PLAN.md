---
phase: 26-hub-shell
plan: 09
type: execute
wave: 3
depends_on: ["26-06"]
files_modified:
  - frontend/src/components/shared/Skeleton.tsx
  - frontend/src/components/shared/OfflineIndicator.tsx
  - frontend/src/components/shared/InstallBanner.tsx
  - frontend/src/hooks/useInstallBanner.ts
  - frontend/src/hooks/useOnline.ts
  - frontend/index.html
  - frontend/src/components/shared/InstallBanner.test.tsx
autonomous: true
requirements: [HUB-02, HUB-03]
must_haves:
  truths:
    - "A one-time dismissible iOS install banner shows only when on iOS, not standalone, and not previously dismissed; dismiss persists in localStorage (D-12)"
    - "An offline indicator (amber top strip) appears when the browser is offline and hides when back online (HUB-03)"
    - "A reusable Skeleton shimmer renders for in-flight API fetches, distinct from D-06 placeholders (HUB-03)"
    - "index.html carries the apple-touch-icon link so the installed iOS home-screen icon is correct (HUB-02)"
  artifacts:
    - path: "frontend/src/components/shared/InstallBanner.tsx"
      provides: "iOS Add-to-Home-Screen instruction banner, one-time dismissible (D-12)"
      contains: "InstallBanner"
    - path: "frontend/src/components/shared/OfflineIndicator.tsx"
      provides: "amber offline strip + cached-data notice (HUB-03)"
      contains: "OfflineIndicator"
    - path: "frontend/src/components/shared/Skeleton.tsx"
      provides: "in-flight shimmer (HUB-03)"
      contains: "Skeleton"
  key_links:
    - from: "frontend/src/components/shared/InstallBanner.tsx"
      to: "frontend/src/hooks/useInstallBanner.ts"
      via: "isIOS && !standalone && !dismissed gate"
      pattern: "install-banner-dismissed"
    - from: "frontend/src/components/shared/OfflineIndicator.tsx"
      to: "frontend/src/hooks/useOnline.ts"
      via: "navigator.onLine + online/offline events"
      pattern: "onLine"
---

<objective>
Finish the PWA polish layer: the iOS install onboarding banner (HUB-02 / D-12), the offline indicator, and the reusable in-flight Skeleton (HUB-03 frontend half) — slotting into the shell from 26-06. The service-worker caching contract (network-first index.html, cache-first assets) already ships in 26-01; this plan adds the user-facing degradation + install affordances on top of it.

Purpose: HUB-02 (explicit in-app Add-to-Home-Screen onboarding — iOS has no `beforeinstallprompt`, so a manual-instruction banner per D-12) and HUB-03 (the app shell degrades to skeletons on a bad connection with a visible offline indicator). D-06 placeholders (shipped in 26-07) and HUB-03 skeletons coexist: skeletons shimmer for in-flight network fetches; placeholders are stable text for data that genuinely won't exist yet. This plan supplies the canonical `Skeleton` the timeline plan stubbed locally.

Output: `Skeleton`, `OfflineIndicator`, `InstallBanner` shared components + the `useInstallBanner`/`useOnline` hooks + the `apple-touch-icon` link in `index.html` + a vitest spec covering the banner gate logic and the online/offline toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-hub-shell/26-RESEARCH.md
@.planning/phases/26-hub-shell/26-PATTERNS.md
@.planning/phases/26-hub-shell/26-UI-SPEC.md
@.planning/phases/26-hub-shell/26-VALIDATION.md
@.planning/phases/26-hub-shell/26-01-PLAN.md
@.planning/phases/26-hub-shell/26-06-PLAN.md

<interfaces>
<!-- Greenfield frontend. Source of truth = UI-SPEC component rows + RESEARCH Pattern 4. From 26-01/26-06. -->

From 26-01: the PWA manifest + service worker (network-first index.html, cache-first /assets/) already ship via vite-plugin-pwa;
  frontend/index.html exists (needs the apple-touch-icon link verified/added); placeholder icon PNGs exist (icon-192/512/512-maskable, apple-touch-icon 180x180).
From 26-06: AppShell (mounts OfflineIndicator + InstallBanner); tokens.ts palette (offline #F59E0B amber, skeleton #1F1F1F, accent #6366F1 install CTA).

RESEARCH Pattern 4 — iOS install detection (D-12):
  isIOS = /iphone|ipad|ipod/i.test(navigator.userAgent)
  isStandalone = window.matchMedia('(display-mode: standalone)').matches  (or navigator.standalone)
  dismissed = localStorage.getItem('install-banner-dismissed') === '1'
  showBanner = isIOS && !isStandalone && !dismissed

UI-SPEC Component Inventory → Shared Components:
  Skeleton: animated shimmer for in-flight API fetches (#1F1F1F shimmer over #0A0A0A, animate-pulse).
  OfflineIndicator: amber 4px top strip (#F59E0B) + "Offline — showing cached data" text.
  InstallBanner: iOS-only, bottom of screen, manual Share → Add to Home Screen instruction, dismiss via X (D-12); accent CTA.
UI-SPEC Copywriting Contract (verbatim): offline "Offline — showing cached data"; install heading "Add Klaus to your home screen";
  install body "Tap the Share button below, then choose \"Add to Home Screen\"."; install CTA "How to install";
  dismiss label "Dismiss install prompt"; skeleton SR label "Loading today's timeline…".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeleton + OfflineIndicator + useOnline</name>
  <files>frontend/src/components/shared/Skeleton.tsx, frontend/src/components/shared/OfflineIndicator.tsx, frontend/src/hooks/useOnline.ts</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Component Inventory → Shared Components (Skeleton + OfflineIndicator rows), § Color (skeleton #1F1F1F animate-pulse, offline #F59E0B 4px top strip), § Copywriting Contract (offline string + skeleton SR label)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § Frontend Pattern Assignments (shared components row)
    - frontend/src/tokens.ts (from 26-01 — offline/skeleton/textPrimary tokens); frontend/src/components/timeline/TimelineDay.tsx (from 26-07 — the local Skeleton stub this replaces)
  </read_first>
  <action>Create `frontend/src/components/shared/Skeleton.tsx`: a reusable shimmer block — `#1F1F1F` background with Tailwind `animate-pulse` over the `#0A0A0A` surface; accepts `className` for sizing and an optional `aria-label` (default the SR string "Loading today's timeline…"); render `role="status"` with an `sr-only` label for accessibility. This is the canonical Skeleton the 26-07 timeline plan stubbed locally — note in the SUMMARY that the timeline's local stub should be replaced by importing this. Create `frontend/src/hooks/useOnline.ts`: `useOnline()` returning a boolean, initialized to `navigator.onLine`, updated via `window` `online`/`offline` event listeners (added in a `useEffect`, cleaned up on unmount). Create `frontend/src/components/shared/OfflineIndicator.tsx`: when `useOnline()` is false, render a fixed top strip — a 4px `#F59E0B` (amber) top border + the text "Offline — showing cached data" (exact Copywriting string); render nothing when online. Mount it in `AppShell` (from 26-06) at the top of the layout.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build && test -f dist/index.html && grep -q "Offline — showing cached data" frontend/src/components/shared/OfflineIndicator.tsx && grep -q "onLine" frontend/src/hooks/useOnline.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/shared/Skeleton.tsx` exports `Skeleton` with `animate-pulse` and a `role="status"` + SR label.
    - `frontend/src/hooks/useOnline.ts` reads `navigator.onLine` and subscribes to `online`/`offline` events with cleanup.
    - `OfflineIndicator` renders the exact "Offline — showing cached data" string with the amber strip when offline and nothing when online; it is mounted in `AppShell`.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The in-flight Skeleton + offline indicator (HUB-03 user-facing degradation) exist and mount in the shell.</done>
</task>

<task type="auto">
  <name>Task 2: InstallBanner + useInstallBanner + apple-touch-icon in index.html (D-12 / HUB-02)</name>
  <files>frontend/src/components/shared/InstallBanner.tsx, frontend/src/hooks/useInstallBanner.ts, frontend/index.html</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-RESEARCH.md Pattern 4 (iOS install detection block lines 498–505: isIOS/isStandalone/dismissed/showBanner; iOS has no beforeinstallprompt), Pitfall 6 (iOS EU restriction note)
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Component Inventory → Shared Components (InstallBanner row), § Interaction Contracts → PWA Install Banner (show gate, fixed-bottom position above BottomTabs, dismiss sets localStorage and hides permanently), § Copywriting Contract (install heading/body/CTA/dismiss-label verbatim), § Color (accent CTA)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § Frontend Pattern Assignments (InstallBanner row → Pattern 4 iOS banner; localStorage gate)
    - frontend/index.html (from 26-01 — confirm/add the apple-touch-icon link); frontend/src/components/layout/AppShell.tsx + BottomTabs.tsx (from 26-06 — the banner mounts fixed-bottom above BottomTabs)
  </read_first>
  <action>Create `frontend/src/hooks/useInstallBanner.ts` implementing the RESEARCH Pattern 4 gate: `isIOS = /iphone|ipad|ipod/i.test(navigator.userAgent)`, `isStandalone = window.matchMedia('(display-mode: standalone)').matches || (navigator as any).standalone === true`, `dismissed = localStorage.getItem('install-banner-dismissed') === '1'`; expose `showBanner = isIOS && !isStandalone && !dismissed` and a `dismiss()` that sets `localStorage.setItem('install-banner-dismissed','1')` and hides the banner (D-12 one-time, stays gone). Create `frontend/src/components/shared/InstallBanner.tsx`: when `showBanner`, render a fixed-bottom banner (above `BottomTabs` on phone) on `#1A1A1A` with the heading "Add Klaus to your home screen", the body "Tap the Share button below, then choose \"Add to Home Screen\"." (exact strings), an accent `#6366F1` CTA "How to install" (may expand the instruction), and an X dismiss button with `aria-label="Dismiss install prompt"` calling `dismiss()`; render nothing otherwise. Mount it in `AppShell`. In `frontend/index.html`, ensure the `<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">` is present in `<head>` (add if 26-01 left it as a follow-up) so the installed iOS home-screen icon is correct (HUB-02). Note the iOS-EU-restriction caveat (Pitfall 6) in the SUMMARY: Israel is assumed non-EU so standalone install works; the banner degrades gracefully regardless.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build && test -f dist/index.html && grep -q "Add Klaus to your home screen" frontend/src/components/shared/InstallBanner.tsx && grep -q "install-banner-dismissed" frontend/src/hooks/useInstallBanner.ts && grep -q "apple-touch-icon" frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useInstallBanner.ts` gates on `isIOS && !isStandalone && !dismissed` and persists dismissal to `localStorage` key `install-banner-dismissed`.
    - `InstallBanner` renders the exact heading "Add Klaus to your home screen" + body string + CTA "How to install" + an X with `aria-label="Dismiss install prompt"`.
    - `frontend/index.html` contains an `apple-touch-icon` link (HUB-02 icon).
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The one-time dismissible iOS install banner + apple-touch-icon exist (HUB-02 / D-12); dismissal persists.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest spec — install-banner gate logic + online/offline toggle</name>
  <files>frontend/src/components/shared/InstallBanner.test.tsx</files>
  <read_first>
    - frontend/vitest.config.ts (from 26-01 — jsdom) and frontend/src/App.test.tsx (from 26-06 — render/mock pattern)
    - frontend/src/hooks/useInstallBanner.ts + frontend/src/hooks/useOnline.ts + the two shared components (Tasks 1–2 — surface under test)
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Copywriting Contract (strings to assert); .planning/phases/26-hub-shell/26-VALIDATION.md § Manual-Only Verifications (HUB-02 device test is manual — the unit test covers the gate logic only)
  </read_first>
  <action>Create `frontend/src/components/shared/InstallBanner.test.tsx` (vitest + testing-library; mock `navigator.userAgent`, `window.matchMedia`, `localStorage`, and `navigator.onLine`). Assert: (a) `InstallBanner` renders (heading "Add Klaus to your home screen") when userAgent is iOS, not standalone, and `install-banner-dismissed` is unset; (b) it renders nothing when `install-banner-dismissed === '1'`; (c) it renders nothing on a non-iOS userAgent; (d) calling `dismiss()` sets `localStorage['install-banner-dismissed'] = '1'` and hides the banner (D-12); (e) `OfflineIndicator` renders "Offline — showing cached data" when `navigator.onLine` is false and nothing when true (toggle via dispatching `online`/`offline` events or re-rendering with the mock). Keep network-free. Note in the spec header that the on-device iOS install gesture (HUB-02) is a manual verification per VALIDATION.md — this spec covers only the show/hide gate logic.</action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
    <human-check>HUB-02 (manual, physical iPhone): open the hub in Safari, confirm the install banner appears, follow Share → Add to Home Screen, confirm install, then confirm the banner stays dismissed after reopening. HUB-03 (manual, DevTools offline or airplane mode): reload with the network off and confirm the app shell loads from cache, the offline indicator shows, and sections degrade to skeletons. (Per VALIDATION.md Manual-Only Verifications.)</human-check>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run` exits 0 with the InstallBanner spec running.
    - The spec proves the iOS/standalone/dismissed gate (show when eligible, hide when dismissed or non-iOS) and the online/offline toggle.
    - The on-device HUB-02/HUB-03 checks are recorded as manual `<human-check>` items (not blocking the autonomous build).
  </acceptance_criteria>
  <done>A vitest spec locks the install-banner gate + offline toggle; the on-device install/offline behaviors are flagged for manual verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker cache → rendered shell | A stale cached shell could mask a security fix; mitigated by the network-first index.html SW from 26-01 (HUB-03) |
| localStorage (install-dismissed) → UI | Client-only preference; no security impact, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09-01 | Information Disclosure | Stale cached app shell serving an outdated/insecure UI after a deploy | mitigate | Network-first `index.html` + `registerType:'autoUpdate'` SW from 26-01 (HUB-03); this plan's offline indicator signals when cached data is being shown |
| T-26-09-02 | Tampering | Offline mode masking a failed auth re-check (rendering stale private data while offline) | accept | Offline indicator explicitly labels "showing cached data"; no writes occur offline (no offline write queue in v5.0 — HUBX-05 deferred); on reconnect, `/api/*` re-auths via the session cookie |
| T-26-09-03 | Spoofing | A non-iOS or already-installed user being shown a misleading install prompt | mitigate | The `isIOS && !isStandalone && !dismissed` gate (Pattern 4) prevents showing the banner outside its valid context |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build && test -f dist/index.html` (PWA polish builds, type-clean).
- `cd frontend && npm test -- --run` exits 0 (InstallBanner + offline spec).
- `grep -q "install-banner-dismissed" frontend/src/hooks/useInstallBanner.ts`, `grep -q "Offline — showing cached data" frontend/src/components/shared/OfflineIndicator.tsx`, `grep -q "apple-touch-icon" frontend/index.html`.
- Manual (VALIDATION.md): HUB-02 install gesture on a physical iPhone; HUB-03 offline shell load + skeletons.
- Backend suite unaffected: `pytest -q tests/ -k hub` still green.
</verification>

<success_criteria>
- HUB-02: one-time dismissible iOS install banner with manual Add-to-Home-Screen instructions (D-12) + correct apple-touch-icon.
- HUB-03 (frontend half): offline indicator + reusable in-flight Skeleton; degradation distinct from D-06 placeholders.
- A vitest spec locks the gate/toggle logic; on-device install/offline behavior flagged for manual verification.
</success_criteria>

<output>
Create `.planning/phases/26-hub-shell/26-09-SUMMARY.md` when done.
</output>
