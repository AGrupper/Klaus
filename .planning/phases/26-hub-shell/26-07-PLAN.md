---
phase: 26-hub-shell
plan: 07
type: execute
wave: 3
depends_on: ["26-04", "26-06"]
files_modified:
  - frontend/src/api/today.ts
  - frontend/src/hooks/useToday.ts
  - frontend/src/components/timeline/TimelineDay.tsx
  - frontend/src/components/timeline/TimelineItem.tsx
  - frontend/src/components/timeline/NowLine.tsx
  - frontend/src/components/timeline/TimelineHeader.tsx
  - frontend/src/components/timeline/PlaceholderCard.tsx
  - frontend/src/components/timeline/TimelineDay.test.tsx
autonomous: true
requirements: [TIME-01, TIME-02, TIME-03, TIME-04, TIME-05, TIME-08]
must_haves:
  truths:
    - "The timeline renders today's calendar chronologically with all-day events pinned, the now-line marker, and past items dimmed (D-04)"
    - "The timeline covers strict today, midnight–midnight, with past items still rendered so the day reads whole (D-03)"
    - "The header shows Garmin morning stats + a one-line weather summary (TIME-02)"
    - "Meals render as slot labels with macros and are never shown as eating times (TIME-03)"
    - "The training item shows block context (Week N of 16) and located events show a leave-by / Get Ready chip (TIME-04/05)"
    - "The glance rail shows the day's nutrition running totals (TIME-08); D-06 placeholders render for not-yet-available data (coach note, Garmin) distinct from in-flight skeletons"
  artifacts:
    - path: "frontend/src/hooks/useToday.ts"
      provides: "useQuery for /api/today with refetch-on-mount + refetch-on-focus (D-05)"
      contains: "useToday"
    - path: "frontend/src/components/timeline/NowLine.tsx"
      provides: "accent now-line + scrollIntoView auto-scroll on open (D-04/TIME-04)"
      contains: "scrollIntoView"
    - path: "frontend/src/components/timeline/PlaceholderCard.tsx"
      provides: "D-06 not-ready placeholders (no shimmer)"
      contains: "PlaceholderCard"
  key_links:
    - from: "frontend/src/hooks/useToday.ts"
      to: "/api/today"
      via: "apiFetch in a TanStack useQuery"
      pattern: "today"
    - from: "frontend/src/components/timeline/TimelineDay.tsx"
      to: "frontend/src/hooks/useToday.ts"
      via: "renders the composed timeline data"
      pattern: "useToday"
---

<objective>
Build the Today timeline UI — the home screen of the hub — consuming `/api/today` from 26-04 and rendering inside the shell from 26-06.

Purpose: TIME-01..05 + TIME-08 (rendering). The timeline reads as "where am I in my day" (D-04): calendar chronological with all-day pinned (TIME-01), a now-line marker auto-scrolled-to on open with past items dimmed, a header with Garmin morning stats + a one-line weather summary (TIME-02), meals as slot labels with macros — never eating times (TIME-03), the training item with "Week N of 16" block context (TIME-04), leave-by / Get Ready chips on located events (TIME-05), and the glance rail nutrition totals (TIME-08). Freshness is refetch-on-mount + refetch-on-focus + pull-to-refresh, NOT timer polling (D-05). D-06 "not ready yet" placeholders (coach note before the morning briefing, Garmin before sync) are distinct from HUB-03 in-flight skeletons — placeholders are stable text, skeletons shimmer.

Output: the `today` api client + `useToday` hook + the five timeline components + a vitest spec covering chronological order, all-day pinning, past dimming, slot-label rendering, and D-06 placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-hub-shell/26-RESEARCH.md
@.planning/phases/26-hub-shell/26-PATTERNS.md
@.planning/phases/26-hub-shell/26-UI-SPEC.md
@.planning/phases/26-hub-shell/26-04-PLAN.md
@.planning/phases/26-hub-shell/26-06-PLAN.md

<interfaces>
<!-- Greenfield frontend. Source of truth = UI-SPEC component rows + RESEARCH patterns. From 26-04/26-06. -->

GET /api/today (from 26-04) returns JSON:
  { today, calendar: { all_day: [...], timed: [{id,title,start,end,location?,leave_by?,get_ready_at?}] },
    garmin: {sleep,hrv,body_battery,resting_hr} | null,
    weather: "<one-line>" | null,
    meals: [{slot_label, macros:{kcal,protein_g,carbs_g,fat_g,fiber_g}}],
    training: {item, block_context: "Week N of 16 — Lower Body A"} | null,
    coach_note: "<string>" | null,   // null → D-06 placeholder, NOT a skeleton
    nutrition_totals: {kcal,protein_g,carbs_g,fat_g,fiber_g} }

From 26-06: apiFetch (frontend/src/api/client.ts, credentials:'include'); AppShell main column renders the Today route;
  GlanceRail.tsx has a nutrition-totals slot; tokens.ts palette.

UI-SPEC Interaction Contracts (Today Timeline):
  - Auto-scroll on open: NowLine scrollIntoView({behavior:'smooth', block:'center'}) on mount (D-04).
  - Refresh: useQuery refetchOnMount:true + refetchOnWindowFocus:true; pull-to-refresh invalidates ['today']. NO timer polling (D-05).
  - Past item dimming: opacity-[0.45] on TimelineItem whose end < Date.now() (D-04).
  - Loading: Skeleton replaces each section during initial fetch (HUB-03; Skeleton component arrives in 26-09 — use a local stub class until then).
  - D-06 placeholders (PlaceholderCard, no shimmer): coach_note null → "Coach note coming after your morning briefing."; garmin null → "Sleep stats syncing…"; no meals → "No meals logged yet today."; no training → "No training scheduled today."; no events → "Nothing on the calendar today."
UI-SPEC Copywriting Contract holds the exact placeholder/empty strings — use them verbatim.
Color: accent #6366F1 for the now-line ONLY (per the reserved list); past items dimmed via opacity, not a separate color.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: today api client + useToday hook (refetch-on-mount/focus, no timer polling)</name>
  <files>frontend/src/api/today.ts, frontend/src/hooks/useToday.ts</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-RESEARCH.md Pattern 3 (the /api/today response shape), § Code Examples (apiFetch), § Recommended Project Structure (api/today.ts, hooks/useToday.ts roles)
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Interaction Contracts → Today Timeline (refetchOnMount + refetchOnWindowFocus, pull-to-refresh, NO timer polling — D-05)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § Frontend Pattern Assignments (useToday row — Pattern 3)
    - frontend/src/api/client.ts (from 26-06 — apiFetch); frontend/src/api/today.ts type contract
  </read_first>
  <action>Create `frontend/src/api/today.ts`: a TypeScript `TodayData` interface matching the `/api/today` response shape (calendar.all_day/timed, garmin|null, weather|null, meals[], training|null, coach_note|null, nutrition_totals) and `fetchToday(): Promise<TodayData>` calling `apiFetch('/api/today')`. Create `frontend/src/hooks/useToday.ts`: a `useToday()` wrapping `useQuery({ queryKey: ['today'], queryFn: fetchToday, refetchOnMount: true, refetchOnWindowFocus: true })` — explicitly NO `refetchInterval` (D-05: no timer polling). Export a `useRefreshToday()` helper returning a callback that calls `queryClient.invalidateQueries({ queryKey: ['today'] })` for pull-to-refresh. Keep all date math in Asia/Jerusalem awareness consistent with the server's `today` field (strict today, D-03).</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "refetchOnWindowFocus" frontend/src/hooks/useToday.ts && grep -qv "refetchInterval" frontend/src/hooks/useToday.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useToday.ts` exports `useToday` using `refetchOnMount: true` + `refetchOnWindowFocus: true` and NO `refetchInterval` (D-05).
    - `frontend/src/api/today.ts` exports a `TodayData` type and `fetchToday` calling `apiFetch('/api/today')`.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The Today data layer fetches on mount/focus with pull-to-refresh and no timer polling.</done>
</task>

<task type="auto">
  <name>Task 2: Timeline components — TimelineDay, TimelineItem, NowLine, TimelineHeader, PlaceholderCard</name>
  <files>frontend/src/components/timeline/TimelineDay.tsx, frontend/src/components/timeline/TimelineItem.tsx, frontend/src/components/timeline/NowLine.tsx, frontend/src/components/timeline/TimelineHeader.tsx, frontend/src/components/timeline/PlaceholderCard.tsx</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Component Inventory → Timeline Components (all five rows with notes), § Interaction Contracts → Today Timeline (auto-scroll, dimming, D-06 placeholders), § Copywriting Contract (exact placeholder/empty strings), § Color (now-line accent; past opacity-0.45), § Typography (Heading for date header, Label for metadata/block-context chip), § Spacing Scale
    - .planning/phases/26-hub-shell/26-PATTERNS.md § Frontend Pattern Assignments (NowLine row → scrollIntoView; PlaceholderCard row → no shimmer, fixed strings; TimelineItem → opacity-45 + leave-by chip)
    - frontend/src/hooks/useToday.ts (Task 1 — the data source); frontend/src/tokens.ts (from 26-01 — palette); frontend/src/components/layout/GlanceRail.tsx (from 26-06 — the nutrition-totals slot to fill, TIME-08)
  </read_first>
  <action>Build the five timeline components from `useToday()` data. `TimelineDay.tsx`: renders the full day — all-day events pinned at top (TIME-01), then `timed` events sorted chronologically by start, with the `NowLine` interleaved at the current time position; renders `TimelineHeader` above; passes `nutrition_totals` into the GlanceRail slot (TIME-08); on a `null`/empty section renders the matching `PlaceholderCard`. `TimelineItem.tsx`: one event row card on `#1A1A1A` — title (Body), time (Label `textSecondary`); apply `opacity-[0.45]` when the event end is before `Date.now()` (D-04 past dimming); render a leave-by / Get Ready chip (Label) when `leave_by`/`get_ready_at` is present (TIME-05); render the meal slot LABEL + macros for meal items (TIME-03 — show the slot label and macros, NEVER frame the slot time as an eating time); render the training item with its `block_context` "Week N of 16 — …" as a Label chip (TIME-04). `NowLine.tsx`: a horizontal rule in accent `#6366F1` with the current time label; `useEffect` calls `scrollIntoView({ behavior: 'smooth', block: 'center' })` on mount (D-04 auto-scroll). `TimelineHeader.tsx`: the date heading "Today — {weekday day month}" (Heading 20px/600); Garmin morning stats (sleep/HRV/body battery as Label rows) and the one-line weather summary (TIME-02); collapses to one line on phone; renders a `PlaceholderCard` "Sleep stats syncing…" when `garmin` is null (D-06). `PlaceholderCard.tsx`: a quiet card with fixed text from the Copywriting Contract and NO shimmer animation (distinct from the in-flight Skeleton — D-06); accepts the text string as a prop. Wire the coach-note placeholder ("Coach note coming after your morning briefing.") when `coach_note` is null. Use a temporary local Skeleton stub for in-flight loading (the real `Skeleton` lands in 26-09) — note this in the SUMMARY.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build && test -f dist/index.html && grep -q "scrollIntoView" frontend/src/components/timeline/NowLine.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All five timeline components exist and export their named component.
    - `NowLine.tsx` calls `scrollIntoView({ behavior: 'smooth', block: 'center' })` on mount (D-04 auto-scroll).
    - `TimelineItem.tsx` applies `opacity-[0.45]` (or equivalent) to past items and renders a leave-by chip when present.
    - `PlaceholderCard.tsx` renders the exact Copywriting-Contract strings and has NO shimmer/pulse class (D-06 ≠ skeleton).
    - Meal rows render a slot LABEL + macros and contain no "eaten at"/eating-time framing (TIME-03).
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The Today timeline renders chronologically with all-day pinned, now-line auto-scroll, past dimming, slot-label meals, leave-by chips, block context, header stats/weather, glance-rail totals, and D-06 placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest spec — chronological order, all-day pinning, past dimming, slot labels, D-06 placeholders</name>
  <files>frontend/src/components/timeline/TimelineDay.test.tsx</files>
  <read_first>
    - frontend/vitest.config.ts (from 26-01 — jsdom) and frontend/src/App.test.tsx (from 26-06 — the render-test pattern + mocking approach)
    - frontend/src/components/timeline/TimelineDay.tsx + the other timeline components (Task 2 — surface under test); frontend/src/hooks/useToday.ts (Task 1 — mock its return)
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Copywriting Contract (the exact placeholder strings to assert)
  </read_first>
  <action>Create `frontend/src/components/timeline/TimelineDay.test.tsx` (vitest + testing-library). Mock `useToday` to return a fixture and assert: (a) all-day events render before timed events (TIME-01 pinning); (b) timed events render in chronological order; (c) a timed event whose end is in the past gets the dim treatment (assert the `opacity-[0.45]` class / a `data-past` marker); (d) a meal item renders its slot label + macros and the rendered text contains no "eaten at"/eating-time phrase (TIME-03); (e) when the fixture has `coach_note: null` the "Coach note coming after your morning briefing." placeholder renders, and when `garmin: null` "Sleep stats syncing…" renders (D-06); (f) when `calendar.all_day` and `calendar.timed` are both empty, "Nothing on the calendar today." renders. Mock `scrollIntoView` (jsdom does not implement it) so `NowLine` mount does not throw. Keep the test fast and network-free.</action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run` exits 0 with the TimelineDay spec running.
    - The spec asserts all-day pinning + chronological order, past dimming, slot-label meals (no eating-time framing), and the two D-06 placeholder strings.
    - `scrollIntoView` is mocked so the NowLine mount does not throw.
  </acceptance_criteria>
  <done>A vitest spec proves TIME-01/03 rendering rules, D-04 past dimming, and the D-06 placeholder strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/api/today` JSON → DOM render | Server-composed, already auth-gated and JSON-safe (26-04); the client only renders, never re-fetches secrets |
| refetch-on-focus (D-05) → Routes/Garmin budget | Each focus refetch can trigger upstream calls; the 30-min server cache (26-04) absorbs the cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Tampering | Client re-framing meal slot times as actual eating times | mitigate | Components render the server `slot_label` + macros only; the vitest spec asserts no eating-time phrase (TIME-03 / CLAUDE.md §6 invariant) |
| T-26-07-02 | Denial of Service | refetch-on-focus storm exhausting Routes/Garmin quota (D-05) | mitigate | No timer polling (only mount + focus); the server-side 30-min `_routes_cache` (26-04) bounds upstream calls |
| T-26-07-03 | Information Disclosure | 401 mid-session rendering stale private data instead of re-auth | mitigate | `apiFetch` (26-06) redirects to sign-in on 401; the query errors rather than silently rendering stale data |
| T-26-07-04 | Information Disclosure | D-06 placeholder vs skeleton confusion masking a genuinely failed fetch | accept | Placeholders are for known-absent data (coach note pre-briefing, Garmin pre-sync); fetch errors surface via the query error path, not a placeholder |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build && test -f dist/index.html` (timeline builds, type-clean).
- `cd frontend && npm test -- --run` exits 0 (TimelineDay spec).
- `grep -q "scrollIntoView" frontend/src/components/timeline/NowLine.tsx` (D-04 auto-scroll).
- `grep -q "refetchOnWindowFocus" frontend/src/hooks/useToday.ts` and NO `refetchInterval` (D-05).
- Backend suite unaffected: `pytest -q tests/ -k hub` still green.
</verification>

<success_criteria>
- TIME-01: chronological calendar, all-day pinned. TIME-02: Garmin stats + one-line weather header. TIME-03: meals as slot labels with macros, never eating-time. TIME-04: training item + "Week N of 16" block context, now-line auto-scroll. TIME-05: leave-by / Get Ready chips. TIME-08: glance-rail nutrition totals.
- D-04 now-line + past dimming; D-05 refresh-on-mount/focus + pull-to-refresh (no timer polling); D-06 placeholders distinct from skeletons.
- A vitest spec locks the core rendering rules.
</success_criteria>

<output>
Create `.planning/phases/26-hub-shell/26-07-SUMMARY.md` when done.
</output>
