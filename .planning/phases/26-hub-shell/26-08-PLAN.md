---
phase: 26-hub-shell
plan: 08
type: execute
wave: 4
depends_on: ["26-05", "26-06"]
files_modified:
  - frontend/src/api/chat.ts
  - frontend/src/hooks/useChat.ts
  - frontend/src/hooks/useUnread.ts
  - frontend/src/components/chat/ChatWindow.tsx
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/chat/TypingIndicator.tsx
  - frontend/src/components/chat/ChatInput.tsx
  - frontend/src/components/shared/UnreadBadge.tsx
  - frontend/src/components/layout/BottomTabs.tsx
  - frontend/src/components/layout/DockChat.tsx
  - frontend/src/hooks/useChat.test.tsx
autonomous: true
requirements: [CHAT-03, CHAT-04]
must_haves:
  truths:
    - "A sent message renders optimistically with sending → sent (green) → error states (CHAT-03)"
    - "Klaus's replies arrive via 2.5s polling while the chat is visible, with a 'Klaus is thinking…' indicator while a turn is in flight (CHAT-03)"
    - "The Klaus tab shows an unread badge counting all unseen Klaus messages (incl. Telegram-originated), cleared on scroll-to-bottom (CHAT-04 / D-10/D-11)"
    - "Polling pauses when the chat is not visible (no wasted turns)"
    - "Scroll-to-bottom on a new Klaus message only when the user was already at the bottom"
  artifacts:
    - path: "frontend/src/hooks/useChat.ts"
      provides: "optimistic send (useMutation) + 2.5s polling (useQuery) — CHAT-03"
      contains: "refetchInterval"
    - path: "frontend/src/hooks/useUnread.ts"
      provides: "localStorage last_seen_seq + badge count, clear on scroll-to-bottom (D-10/D-11)"
      contains: "last_seen"
    - path: "frontend/src/components/chat/TypingIndicator.tsx"
      provides: "'Klaus is thinking…' indicator"
      contains: "Klaus is thinking"
  key_links:
    - from: "frontend/src/hooks/useChat.ts"
      to: "/api/chat + /api/chat/messages"
      via: "useMutation POST + useQuery poll"
      pattern: "chat"
    - from: "frontend/src/components/chat/ChatWindow.tsx"
      to: "frontend/src/hooks/useUnread.ts"
      via: "IntersectionObserver on last message clears the badge"
      pattern: "useUnread"
---

<objective>
Build the Klaus chat UI — optimistic send, polling, the thinking indicator, and the unread badge — consuming the chat backend from 26-05 inside the shell from 26-06.

Purpose: CHAT-03/04. Sent messages render optimistically with sending/sent/error status; Klaus's replies arrive via 2.5s polling while the chat is visible, with a "Klaus is thinking…" indicator while a turn is in flight (CHAT-03). The Klaus tab shows an unread badge counting ALL unseen Klaus messages — direct replies, proactive/autonomous-tick outreach, and anything that arrived via Telegram since last-seen (CHAT-04 / D-11) — cleared only when the user scrolls to the bottom and actually sees the newest message (D-10). Polling pauses when the chat isn't visible (TanStack default) so no turns are wasted.

Output: the `chat` api client + `useChat` (optimistic send + polling) + `useUnread` (badge) hooks + the four chat components + the `UnreadBadge` shared component + a vitest spec covering the optimistic→sent transition, the thinking indicator condition, and the unread count math.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-hub-shell/26-RESEARCH.md
@.planning/phases/26-hub-shell/26-PATTERNS.md
@.planning/phases/26-hub-shell/26-UI-SPEC.md
@.planning/phases/26-hub-shell/26-05-PLAN.md
@.planning/phases/26-hub-shell/26-06-PLAN.md

<interfaces>
<!-- Greenfield frontend. Source of truth = UI-SPEC component rows + RESEARCH patterns. From 26-05/26-06. -->

Backend (from 26-05):
  POST /api/chat  body { content: string }  → { ok: true }  (appends user msg + enqueues the agent turn)
  GET  /api/chat/messages  → { messages: [{ role: 'user'|'assistant', content: string, seq?/index }] }  (recent window, D-08)
  Klaus's reply lands in the shared conversation asynchronously; the client discovers it by polling /api/chat/messages.

From 26-06: apiFetch (credentials:'include'); AppShell — DockChat slot (desktop) + the Klaus route /klaus (phone full-screen);
  BottomTabs has an UnreadBadge slot on the Klaus tab; tokens.ts palette.

UI-SPEC Interaction Contracts (Chat):
  - Optimistic send: appears as status 'sending' → on ACK 'sent' (green #22C55E checkmark) → on error 'error' (#EF4444, tap to retry).
  - Polling: refetchInterval 2500 ONLY while ChatWindow mounted/visible; pauses on tab blur (TanStack default — refetchIntervalInBackground false).
  - "Klaus is thinking…": TypingIndicator shows when the last message is role 'user' (a turn is in flight); clears when an assistant message arrives.
  - Scroll-to-bottom on new Klaus message: only if user was already at the bottom; do not yank if reading history.
  - Unread badge clear: IntersectionObserver on the last message → localStorage.last_seen_seq = messages.length (D-10).
  - Unread count = messages.length - last_seen_seq, counting all unseen Klaus messages incl. Telegram-originated (D-11).
  - Send shortcut: desktop Enter sends, Shift+Enter newline; phone send button only.
UI-SPEC Copywriting Contract: send CTA "Send message"; empty "Say hello to Klaus."; thinking "Klaus is thinking…"; error "Couldn't send — tap to retry."
Color: accent #6366F1 for send button + unread badge ONLY; success #22C55E sent check; destructive #EF4444 error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chat api client + useChat (optimistic send + 2.5s polling) + useUnread</name>
  <files>frontend/src/api/chat.ts, frontend/src/hooks/useChat.ts, frontend/src/hooks/useUnread.ts</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-RESEARCH.md Pattern 5 (optimistic send useMutation onMutate/onError/onSettled + useQuery refetchInterval 2500 + the thinking-indicator rule, lines 507–547), Pattern 7 (unread badge via last_seen index = messages.length, lines 594–602)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § Frontend Pattern Assignments (useChat row → Pattern 5; useUnread row → Pattern 7: localStorage.last_seen_seq, badge = messages.length - last_seen_seq, clear on IntersectionObserver)
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Interaction Contracts → Chat (optimistic states, polling-when-visible, thinking condition, unread clear/count)
    - frontend/src/api/client.ts (from 26-06 — apiFetch)
  </read_first>
  <action>Create `frontend/src/api/chat.ts`: a `ChatMessage` type `{ role: 'user'|'assistant'; content: string; status?: 'sending'|'sent'|'error'; id?: string }`, `fetchMessages(): Promise<ChatMessage[]>` calling `apiFetch('/api/chat/messages')` (return the `messages` array), and `postChatMessage(content: string)` calling `apiFetch('/api/chat', { method:'POST', body: JSON.stringify({content}) })`. Create `frontend/src/hooks/useChat.ts` per RESEARCH Pattern 5: a `useQuery({ queryKey:['chat','messages'], queryFn: fetchMessages, refetchInterval: isVisible ? 2500 : false })` (polling only when the chat is visible — accept an `isVisible` arg or use a mounted ref) and a `useMutation` with `onMutate` (cancel in-flight refetch, snapshot, optimistically append `{role:'user',content,status:'sending'}`), `onError` (roll back to snapshot, mark the optimistic message `status:'error'`), `onSettled` (`invalidateQueries(['chat','messages'])`). Expose a derived `isKlausThinking` = the last message has `role === 'user'` (a turn is in flight → show TypingIndicator). Create `frontend/src/hooks/useUnread.ts` per Pattern 7: read `localStorage.last_seen_seq` (default 0), compute `unreadCount = Math.max(0, messages.length - last_seen_seq)` (counts every unseen message incl. Telegram-originated since the shared history is one stream — D-11), and expose `markAllSeen()` setting `localStorage.last_seen_seq = messages.length` (called on scroll-to-bottom — D-10).</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "refetchInterval" frontend/src/hooks/useChat.ts && grep -q "2500" frontend/src/hooks/useChat.ts && grep -q "last_seen" frontend/src/hooks/useUnread.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useChat.ts` uses `useMutation` with `onMutate`/`onError`/`onSettled` (optimistic) and `useQuery` with `refetchInterval: 2500` gated on visibility.
    - `useChat` exposes an `isKlausThinking` derived from "last message is role 'user'".
    - `frontend/src/hooks/useUnread.ts` computes `unreadCount = messages.length - last_seen_seq` and exposes `markAllSeen` writing `localStorage`.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The chat data layer does optimistic send + visibility-gated 2.5s polling + the thinking flag + the unread count math.</done>
</task>

<task type="auto">
  <name>Task 2: Chat components — ChatWindow, MessageBubble, TypingIndicator, ChatInput + UnreadBadge</name>
  <files>frontend/src/components/chat/ChatWindow.tsx, frontend/src/components/chat/MessageBubble.tsx, frontend/src/components/chat/TypingIndicator.tsx, frontend/src/components/chat/ChatInput.tsx, frontend/src/components/shared/UnreadBadge.tsx</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Component Inventory → Chat Components (ChatWindow/MessageBubble/TypingIndicator/ChatInput rows) + Shared Components (UnreadBadge row: accent bg, white label, "9+" for >9, hidden at 0), § Interaction Contracts → Chat (scroll-to-bottom rule, unread clear via IntersectionObserver, send shortcuts), § Copywriting Contract (exact strings), § Color (accent send button + badge; success/destructive states), § Typography, § Spacing (44px touch target on phone send)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § Frontend Pattern Assignments (ChatWindow row → Pattern 5 + Pattern 7: load 50, scroll-to-bottom, IntersectionObserver on last message)
    - frontend/src/hooks/useChat.ts + frontend/src/hooks/useUnread.ts (Task 1 — data); frontend/src/tokens.ts (from 26-01 — palette)
    - frontend/src/components/layout/DockChat.tsx + BottomTabs.tsx (from 26-06 — the slots ChatWindow + UnreadBadge mount into)
  </read_first>
  <action>Build the chat UI from `useChat()` + `useUnread()`. `ChatWindow.tsx`: a scrollable message list (loads the recent window, slicing ~50 for first paint — D-08), renders `MessageBubble` per message and `TypingIndicator` when `isKlausThinking`; auto-scrolls to bottom on a new Klaus message ONLY if the user was already at the bottom (track a "near-bottom" ref on scroll); attaches an `IntersectionObserver` to the last message so that when it becomes visible it calls `markAllSeen()` (D-10 unread clear); renders the empty state "Say hello to Klaus." when there are no messages; renders the `ChatInput` at the bottom. `MessageBubble.tsx`: user messages right-aligned, Klaus messages left-aligned (on `#1A1A1A`); a status icon for user messages — sending (spinner/clock), sent (green `#22C55E` check), error (`#EF4444` + "Couldn't send — tap to retry." with a retry tap that re-sends). `TypingIndicator.tsx`: the "Klaus is thinking…" text with a 3-dot animation, left-aligned like a Klaus bubble. `ChatInput.tsx`: a textarea + send button (accent `#6366F1` bg, ≥44px on phone, label/aria "Send message"); desktop Enter sends + Shift+Enter newlines, phone send button only; disables/clears appropriately on submit. `UnreadBadge.tsx` (shared): accent `#6366F1` background, white Label text, shows the count or "9+" for >9, hidden (renders nothing) at 0 — consumed by `BottomTabs` (Klaus tab) and the DockChat header. Wire `UnreadBadge` into the BottomTabs Klaus-tab slot and the DockChat header from 26-06 using the `useUnread` count.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build && test -f dist/index.html && grep -q "Klaus is thinking" frontend/src/components/chat/TypingIndicator.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All four chat components + `UnreadBadge` exist and export their named component.
    - `MessageBubble` renders sending/sent(green)/error(red, retry) states for user messages (CHAT-03).
    - `TypingIndicator` renders the exact string "Klaus is thinking…".
    - `ChatWindow` attaches an `IntersectionObserver` to the last message that calls `markAllSeen` (D-10) and only auto-scrolls when near the bottom.
    - `UnreadBadge` shows "9+" for counts >9 and renders nothing at 0; it is wired into BottomTabs + DockChat.
    - `cd frontend && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>The chat UI does optimistic send with status, the thinking indicator, smart scroll, and the unread badge cleared on scroll-to-bottom.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest spec — optimistic transition, thinking condition, unread count math</name>
  <files>frontend/src/hooks/useChat.test.tsx</files>
  <read_first>
    - frontend/vitest.config.ts (from 26-01 — jsdom) and frontend/src/App.test.tsx (from 26-06 — render/mock pattern)
    - frontend/src/hooks/useChat.ts + frontend/src/hooks/useUnread.ts (Task 1 — surface under test); frontend/src/components/chat/ChatWindow.tsx (Task 2)
    - .planning/phases/26-hub-shell/26-UI-SPEC.md § Interaction Contracts → Chat (the behaviors to assert) + Copywriting Contract
  </read_first>
  <action>Create `frontend/src/hooks/useChat.test.tsx` (vitest + testing-library, with a `QueryClientProvider` test wrapper and `apiFetch`/network mocked). Assert: (a) calling the mutation optimistically appends a `{role:'user',status:'sending'}` message to the query cache before the server resolves (CHAT-03 optimistic); (b) `isKlausThinking` is true when the last message is role 'user' and false once an assistant message is appended (the thinking-indicator condition); (c) `useUnread`: with `last_seen_seq=2` and 5 messages, `unreadCount === 3`, and after `markAllSeen()` it is 0 and `localStorage.last_seen_seq === 5` (D-10/D-11 count math); (d) the polling query is configured with `refetchInterval` 2500 when visible and `false` when not (assert the hook's option, or assert via a visibility flag). Mock `localStorage` in jsdom. Keep it network-free and fast.</action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run` exits 0 with the useChat spec running.
    - The spec proves the optimistic append, the `isKlausThinking` toggle, and the unread count math (incl. `markAllSeen` clearing to 0).
    - No real network call is made.
  </acceptance_criteria>
  <done>A vitest spec locks the CHAT-03 optimistic/thinking behavior and the CHAT-04 unread count math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChatInput → `POST /api/chat` | User-supplied content crosses into the agent loop; server validates (Pydantic) + auth-gates (26-05); the client must not assume trust |
| polling `GET /api/chat/messages` → DOM | Renders the shared conversation; same-origin, cookie-authed; 401 redirects to sign-in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08-01 | Tampering | Rendering Klaus/Telegram message content as raw HTML (stored XSS) | mitigate | Render message content as text (React escapes by default); never `dangerouslySetInnerHTML` on message content |
| T-26-08-02 | Denial of Service | Polling continuing in the background, wasting agent turns / Routes budget | mitigate | `refetchInterval` gated on chat visibility; TanStack pauses on tab blur (refetchIntervalInBackground false) |
| T-26-08-03 | Information Disclosure | Unread badge leaking count after session expiry | mitigate | `apiFetch` 401 → sign-in redirect (26-06); the badge derives from authed `/api/chat/messages` only |
| T-26-08-04 | Spoofing | Optimistic "sent" state shown when the enqueue actually failed | mitigate | `onError` rolls back + marks `status:'error'` ("Couldn't send — tap to retry."); "sent" only after the POST ACKs |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build && test -f dist/index.html` (chat builds, type-clean).
- `cd frontend && npm test -- --run` exits 0 (useChat spec).
- `grep -q "2500" frontend/src/hooks/useChat.ts` (polling cadence) and `grep -q "last_seen" frontend/src/hooks/useUnread.ts` (badge).
- `grep -q "Klaus is thinking" frontend/src/components/chat/TypingIndicator.tsx`.
- Backend suite unaffected: `pytest -q tests/ -k hub` still green.
</verification>

<success_criteria>
- CHAT-03: optimistic send with sending/sent/error status; 2.5s polling while visible; "Klaus is thinking…" indicator in flight.
- CHAT-04 / D-10/D-11: unread badge counts all unseen Klaus messages (incl. Telegram-originated), cleared on scroll-to-bottom.
- Polling pauses off-screen; scroll-to-bottom only when already at the bottom.
- A vitest spec locks the optimistic/thinking/unread behaviors.
</success_criteria>

<output>
Create `.planning/phases/26-hub-shell/26-08-SUMMARY.md` when done.
</output>
