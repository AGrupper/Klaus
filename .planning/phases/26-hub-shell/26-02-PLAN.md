---
phase: 26-hub-shell
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - memory/firestore_db.py
  - core/morning_briefing.py
  - requirements.txt
  - tests/test_hub_auth.py
  - tests/test_api_today.py
  - tests/test_hub_chat.py
  - tests/test_firestore_hub_fields.py
autonomous: true
requirements: [HUB-01, TIME-07, CHAT-01]
must_haves:
  truths:
    - "SelfStateStore can store and read back a daily_note + daily_note_date (TIME-07 coach-note source)"
    - "UserProfileStore scaffold carries session_version (default 0) and telegram_user_id (default None)"
    - "The morning briefing best-effort-writes a one-line daily_note + daily_note_date after composing"
    - "itsdangerous>=2.2 is pinned in requirements.txt"
    - "Backend Wave 0 test stubs exist and import without error so downstream plans have a Nyquist anchor"
  artifacts:
    - path: "memory/firestore_db.py"
      provides: "session_version + telegram_user_id scaffold fields; daily_note via SelfStateStore.set"
      contains: "session_version"
    - path: "core/morning_briefing.py"
      provides: "daily_note write after compose"
      contains: "daily_note"
    - path: "tests/test_api_today.py"
      provides: "TIME-01..05/07/08 test scaffold"
      contains: "def test_"
    - path: "requirements.txt"
      provides: "itsdangerous pin"
      contains: "itsdangerous"
  key_links:
    - from: "core/morning_briefing.py"
      to: "SelfStateStore.set"
      via: "daily_note write after compose"
      pattern: "daily_note"
    - from: "tests/test_firestore_hub_fields.py"
      to: "memory/firestore_db.py"
      via: "scaffold field assertions"
      pattern: "session_version"
---

<objective>
Lay the backend data + test foundation the Wave 1 backend plans build on, and resolve the two RESEARCH open questions (coach-note source, hub→Telegram identity bridge) by adding fields to the existing Firestore stores.

Purpose: Three Wave 1 backend plans (auth, `/api/today`, chat) all depend on small backend prerequisites: (a) `session_version` on `UserProfileStore` for sign-out-everywhere (D-02), (b) `telegram_user_id` on `UserProfileStore` to bridge the hub's Google identity to the Firestore conversation key (RESEARCH Open Question 2), (c) `daily_note` + `daily_note_date` on `SelfStateStore` plus a write from `core/morning_briefing.py` so TIME-07's coach note has a real source (RESEARCH Coach Note Source — option 1, recommended), and (d) the `itsdangerous>=2.2` pin. It also seeds the Wave 0 test stubs from VALIDATION.md so no downstream task runs 3-deep without an automated verify (Nyquist Dimension 8).

Output: extended `SelfStateStore`/`UserProfileStore`, a best-effort `daily_note` write in the morning briefing, the `itsdangerous` pin, and the three new test files + a store-field test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-hub-shell/26-RESEARCH.md
@.planning/phases/26-hub-shell/26-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hub fields to SelfStateStore + UserProfileStore; pin itsdangerous</name>
  <files>memory/firestore_db.py, requirements.txt, tests/test_firestore_hub_fields.py</files>
  <behavior>
    - test_userprofile_scaffold_has_session_version: a fresh UserProfileStore scaffold contains `session_version` defaulting to 0
    - test_userprofile_scaffold_has_telegram_user_id: scaffold contains `telegram_user_id` defaulting to None
    - test_selfstate_set_accepts_daily_note: `SelfStateStore.set({"daily_note": "x", "daily_note_date": "2026-06-13"})` calls the underlying doc set with those keys merged
  </behavior>
  <read_first>
    - memory/firestore_db.py — `UserProfileStore._SCAFFOLD` (lines 204–220) and `SelfStateStore.set` (lines 523–539); `_jsonsafe_doc` (lines 882–910, read-only)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § `memory/firestore_db.py` (scaffold + set excerpts, new-fields list lines 257–268)
    - .planning/phases/26-hub-shell/26-RESEARCH.md § Coach Note Source (option 1), Open Question 2 (telegram_user_id), Open Question 3 (itsdangerous pin)
    - requirements.txt (line 7 region — google-auth already present)
  </read_first>
  <action>In `memory/firestore_db.py`, add to `UserProfileStore._SCAFFOLD` two new keys: `session_version: 0` (bumped by `/api/auth/revoke-all`, D-02) and `telegram_user_id: None` (Amit's Telegram user_id; the hub keys `FirestoreConversationStore` on this). Confirm `SelfStateStore.set` already merges arbitrary patch keys with `SERVER_TIMESTAMP` (it does — lines 523–539) so `daily_note`/`daily_note_date` need no schema change there; do NOT change the merge logic. Follow the existing lowercase field-naming convention (`session_version`, `telegram_user_id`, `daily_note`). Add `itsdangerous>=2.2` to `requirements.txt` (never rely on transitive availability — RESEARCH Open Question 3). Create `tests/test_firestore_hub_fields.py` covering the three behaviors above, mirroring the firestore store-test patterns already in the repo (mock the Firestore client/doc ref).</action>
  <verify>
    <automated>grep -q "session_version" memory/firestore_db.py && grep -q "telegram_user_id" memory/firestore_db.py && grep -q "itsdangerous" requirements.txt && pytest -q tests/test_firestore_hub_fields.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `memory/firestore_db.py` `_SCAFFOLD` contains `session_version` and `telegram_user_id`.
    - `requirements.txt` contains a line matching `itsdangerous>=2.2`.
    - `pytest -q tests/test_firestore_hub_fields.py -x` exits 0.
  </acceptance_criteria>
  <done>Scaffold carries session_version + telegram_user_id; itsdangerous pinned; store-field tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Write daily_note from the morning briefing (TIME-07 source)</name>
  <files>core/morning_briefing.py</files>
  <read_first>
    - core/morning_briefing.py — the compose step that produces the briefing message (find where the final briefing text is assembled before send)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § `core/morning_briefing.py` (write-after-compose excerpt lines 305–319)
    - .planning/phases/26-hub-shell/26-RESEARCH.md § Coach Note Source (option 1: SelfStateStore daily_note + daily_note_date)
    - memory/firestore_db.py — `SelfStateStore.set` signature
  </read_first>
  <action>In `core/morning_briefing.py`, after the briefing message is composed, add a best-effort write `self_state_store.set({"daily_note": <one_line_coach_note>, "daily_note_date": <today_iso>})` where `<today_iso>` is the Asia/Jerusalem date and `<one_line_coach_note>` is a single-line distillation of the briefing's coaching content (the existing briefing already produces a coach-style note — extract its leading one-liner; do not call the LLM again). The `daily_note_date` guard lets `/api/today` (plan 26-04) serve `None` when the stored note is from a prior day (D-06 placeholder "Coach note coming after your morning briefing"). Wrap the write in `try/except Exception` logged at WARNING so a write failure NEVER blocks the briefing send (the briefing send is the primary effect).</action>
  <verify>
    <automated>grep -q "daily_note" core/morning_briefing.py && grep -q "daily_note_date" core/morning_briefing.py && pytest -q tests/test_morning_briefing.py -x 2>/dev/null || python -c "import ast; ast.parse(open('core/morning_briefing.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `core/morning_briefing.py` contains a `set(` call writing both `daily_note` and `daily_note_date`.
    - The write is inside a `try/except` so the briefing send is unaffected on failure (verify the except logs at WARNING, not ERROR).
    - `core/morning_briefing.py` still parses and any existing `tests/test_morning_briefing.py` stays green.
  </acceptance_criteria>
  <done>Morning briefing best-effort-writes a dated one-line coach note to SelfStateStore; failure cannot block the send.</done>
</task>

<task type="auto">
  <name>Task 3: Seed Wave 0 backend test stubs (auth / api_today / hub_chat)</name>
  <files>tests/test_hub_auth.py, tests/test_api_today.py, tests/test_hub_chat.py</files>
  <read_first>
    - .planning/phases/26-hub-shell/26-VALIDATION.md § Wave 0 Requirements + § Manual-Only Verifications
    - .planning/phases/26-hub-shell/26-RESEARCH.md § Validation Architecture (Phase Requirements → Test Map, exact test names)
    - .planning/phases/26-hub-shell/26-PATTERNS.md § `tests/test_hub_auth.py` / `tests/test_api_today.py` / `tests/test_hub_chat.py` (analog patterns, _ENV dict, _stub_web_server_imports, fake_tasks_v2 fixture, required test-case names)
    - tests/test_task_dispatch.py (fake_tasks_v2 fixture pattern), tests/test_web_server.py (_stub_web_server_imports pattern)
  </read_first>
  <action>Create the three Wave 0 test files as SKELETONS that import cleanly and define the named test functions from VALIDATION/RESEARCH, marked `@pytest.mark.skip(reason="implemented in Wave 1 plan 26-03/04/05")` where the production code does not exist yet — so the files collect and run without error and downstream plans flip them to real assertions. `tests/test_hub_auth.py`: the `_ENV` dict (HUB_SESSION_SECRET, HUB_ALLOWED_EMAIL, GOOGLE_OAUTH_CLIENT_ID, CRON_DEV_BYPASS) + stubs for `test_valid_gis_token_issues_cookie`, `test_allowlist_rejects_other_email`, `test_no_cookie_401`, `test_revoked_session`. `tests/test_api_today.py`: `_stub_web_server_imports` reuse + stubs for `test_today_returns_expected_keys`, `test_no_datetimewithnanoseconds_leak`, `test_meal_slot_time_not_eating_time`, `test_unauthenticated_returns_401`. `tests/test_hub_chat.py`: `fake_tasks_v2`-style fixture + stubs for `test_post_chat_appends_to_firestore`, `test_post_chat_enqueues_hub_message`, `test_get_messages_returns_window`, `test_internal_process_hub_message_oidc_gated`. The skeletons exist so VALIDATION's "no 3 consecutive tasks without an automated verify" holds from the very first downstream task; each downstream plan removes the matching skips as it implements.</action>
  <verify>
    <automated>pytest -q tests/test_hub_auth.py tests/test_api_today.py tests/test_hub_chat.py --collect-only && pytest -q tests/test_hub_auth.py tests/test_api_today.py tests/test_hub_chat.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/test_hub_auth.py tests/test_api_today.py tests/test_hub_chat.py --collect-only` lists all 12 named test functions and exits 0.
    - Running the three files exits 0 (skipped tests count as passing collection — no import/collection errors).
    - Each file references the exact test names from RESEARCH § Validation Architecture.
  </acceptance_criteria>
  <done>Three Wave 0 test files collect cleanly with the named, skip-marked stubs that downstream plans will implement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| morning briefing → SelfStateStore | best-effort write; must never block the user-facing briefing send |
| Firestore scaffold → hub auth | session_version is the server-side control for sign-out-everywhere (D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Denial of Service | `daily_note` write failing and blocking the morning briefing | mitigate | Write wrapped in try/except logged WARNING; briefing send is the primary effect and proceeds regardless (Task 2) |
| T-26-02-02 | Tampering | `itsdangerous` relied on transitively (unpinned) → cookie signing breaks on a dependency change | mitigate | Pin `itsdangerous>=2.2` explicitly in requirements.txt (RESEARCH Open Question 3) |
| T-26-02-03 | Elevation of Privilege | session_version default enabling sign-out-everywhere (D-02) being absent → revoke-all silently no-ops | mitigate | Scaffold defaults `session_version: 0`; field-presence test asserts it (Task 1) |
</threat_model>

<verification>
- `pytest -q tests/test_firestore_hub_fields.py -x` exits 0.
- `grep -c "session_version" memory/firestore_db.py` ≥ 1; `grep -c "telegram_user_id" memory/firestore_db.py` ≥ 1.
- `grep -c "itsdangerous" requirements.txt` == 1.
- `grep -c "daily_note" core/morning_briefing.py` ≥ 1.
- The three Wave 0 test files collect and run: `pytest -q tests/test_hub_auth.py tests/test_api_today.py tests/test_hub_chat.py` exits 0.
- Full suite gate at wave merge: `pytest -q` holds the 1153+ baseline.
</verification>

<success_criteria>
- TIME-07 coach-note source resolved: dated `daily_note` written by the morning briefing, readable by `/api/today`.
- D-02 sign-out-everywhere control (`session_version`) and the hub→Telegram identity bridge (`telegram_user_id`) exist in the scaffold.
- `itsdangerous` pinned.
- Wave 0 test scaffolds exist so every downstream backend task has an automated verify anchor (Nyquist Dimension 8).
</success_criteria>

<output>
Create `.planning/phases/26-hub-shell/26-02-SUMMARY.md` when done.
</output>
