---
phase: 25-progress-projection-benchmark-trend-reporting
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - core/tools.py
  - core/pace_history.py
  - prompts/smart_agent.md
  - tests/test_tool_registration_phase25.py
autonomous: true
requirements: [PROG-02]
must_haves:
  truths:
    - "Klaus answers 'am I on track for my October bench target?' by calling get_goal_projection, which returns a computed trend + gap — not by citing the goal alone"
    - "get_goal_projection is brain-direct (in SMART_AGENT_DIRECT_TOOLS) and excluded from the worker tool set"
    - "The handler validates facet against _BENCHMARK_FACETS before any store access (V5 / T-23-01 pattern) and returns a JSON ProjectionResult"
    - "For threshold_pace the handler sources DENSE Garmin running points from Postgres (D-04), falling back to sparse BenchmarkStore only when no Garmin running data exists; strength facets always use BenchmarkStore"
    - "The handler computes today_iso in Asia/Jerusalem (never date.today()) before calling the helper (Phase-24 CR-01 tz lesson)"
    - "The smart_agent prompt permits citing the computed projection number when get_goal_projection data is available, directional-only otherwise"
  artifacts:
    - path: "core/tools.py"
      provides: "get_goal_projection registration at all four sites + _handle_get_goal_projection handler"
      contains: "_handle_get_goal_projection"
    - path: "core/pace_history.py"
      provides: "fetch_dense_pace_history(today_iso) — Postgres activities → BenchmarkStore-shaped threshold_pace points (D-04 dense path, Pattern 3)"
      contains: "def fetch_dense_pace_history"
    - path: "prompts/smart_agent.md"
      provides: "get_goal_projection tool description + updated projection-language permission"
      contains: "get_goal_projection"
    - path: "tests/test_tool_registration_phase25.py"
      provides: "Registration coverage (direct set, worker exclusion, handlers, schema, handler callable)"
      contains: "get_goal_projection"
  key_links:
    - from: "core/tools.py::_handle_get_goal_projection"
      to: "core/projection.py::project_goal_progress"
      via: "import + call after facet validation and _jsonsafe_doc(profiles.load())"
      pattern: "project_goal_progress"
    - from: "core/tools.py::_handle_get_goal_projection"
      to: "core/pace_history.py::fetch_dense_pace_history"
      via: "threshold_pace branch prefers dense Garmin points over BenchmarkStore (D-04)"
      pattern: "fetch_dense_pace_history"
    - from: "core/tools.py::_handle_get_goal_projection"
      to: "_BENCHMARK_FACETS"
      via: "facet-in-set guard before store access"
      pattern: "_BENCHMARK_FACETS"
    - from: "core/tools.py::_handle_get_goal_projection"
      to: "ZoneInfo(\"Asia/Jerusalem\")"
      via: "today_iso = datetime.now(ZoneInfo('Asia/Jerusalem')).date().isoformat() — never date.today() (CR-01)"
      pattern: "Asia/Jerusalem"
---

<objective>
Surface the Plan-01 projection helper as a reactive brain-direct tool. Register `get_goal_projection(facet)` in `core/tools.py` at all four sites (SMART_AGENT_DIRECT_TOOLS, TOOL_SCHEMAS, WORKER exclusion set, _HANDLERS) with a handler that validates the facet, sources the trend points (DENSE Garmin Postgres history for `threshold_pace` per D-04, sparse BenchmarkStore for the strength facets), calls `project_goal_progress`, and returns JSON. Add the shared `core/pace_history.py::fetch_dense_pace_history` helper so both the reactive tool and the Sunday cron (Plan 03) use the identical D-04 dense-source-selection logic. Update `prompts/smart_agent.md` so the brain cites the computed projection number when available.

Purpose: Closes the reactive half of PROG-02 — "am I on track for X?" computes a trend rather than citing the goal alone (ROADMAP success criterion 1), and honors D-04's locked "two code paths / different confidence per facet" by giving threshold_pace its dense Garmin trend.
Output: get_goal_projection tool + core/pace_history.py dense-pace helper + smart_agent prompt permission + registration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-CONTEXT.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-RESEARCH.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-01-PLAN.md

<interfaces>
<!-- Verified anchors in core/tools.py (grep-confirmed against live source). -->
core/tools.py:
  line 40  SMART_AGENT_DIRECT_TOOLS frozenset opens
  line 66  "get_benchmark_history"  (insert get_goal_projection after this)
  line 963 get_benchmark_history schema in TOOL_SCHEMAS (copy schema shape after it)
  line 1047 get_benchmark_history inside the WORKER exclusion set (add get_goal_projection here)
  line 1708 def _block_stores() -> returns (blocks, benchmarks, profiles)
  line 1790 def _handle_get_benchmark_history(facet, n=10) (place new handler nearby)
  line 1868 "get_benchmark_history": lambda args: ... in _HANDLERS (insert after)

Plan-01 helper (already shipped, do not modify):
  core/projection.py::project_goal_progress(facet, history, dated_goals, today_iso) -> dict
  # facet-agnostic: it just receives a list of points (BenchmarkStore-shaped dicts with
  # date + value) + dated_goals + today_iso. SOURCE SELECTION is the caller's job (D-04).
  # Direction handling already treats threshold_pace as lower-is-better (FACET_DIRECTION).

D-04 dense Garmin pace path (RESEARCH.md Pattern 3 / "Garmin Pace Trend — Data Path"):
  query_health_database(sql) -> list[dict] | str   # mcp_tools/database_tool.py:21
  Postgres `activities` table: date, type, duration_sec, distance_m (NUMERIC(8,2))
  Derive pace as `duration_sec / distance_m * 1000` (sec/km) — RESOLVED Open Question #1;
  do NOT read the ambiguous `avg_pace` column.
  Running types: 'running', 'trail_running', 'treadmill_running'; distance_m >= 3000.
  Existing call shape (core/weekly_training_review.py:120-129): import query_health_database,
  build SQL string with date-arithmetic literals, rows = query_health_database(sql),
  isinstance(rows, list) guard (error returns a str).

Reused helpers:
  _block_stores() -> (blocks, benchmarks, profiles)        # core/tools.py:1708
  benchmarks.get_facet_history(facet, n=10) -> list[dict]
  _jsonsafe_doc(profiles.load()) -> dict                   # strips DatetimeWithNanoseconds
  _BENCHMARK_FACETS frozenset                              # memory/firestore_db.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Phase-25 tool-registration tests</name>
  <read_first>
    - tests/test_tool_registration_phase23.py (exact template — copy structure, _install_tools_mocks, autouse fixture, test class)
    - tests/test_tool_registration_phase25.py (does not exist yet — this task creates it)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (tests/test_tool_registration_phase25.py section — NEW_TOOLS, mock stub must include "core.projection" and "core.pace_history", test class TestPhase25ToolRegistration)
  </read_first>
  <behavior>
    - test_tool_in_direct: "get_goal_projection" in tools.SMART_AGENT_DIRECT_TOOLS
    - test_tool_excluded_from_worker: "get_goal_projection" not in {s["name"] for s in tools.WORKER_TOOL_SCHEMAS}
    - test_tool_in_handlers: "get_goal_projection" in tools._HANDLERS
    - test_tool_has_schema: schema present in TOOL_SCHEMAS with keys >= {name, description, input_schema}; required == ["facet"]; "facet" in properties
    - test_handler_callable: tools._handle_get_goal_projection is callable
  </behavior>
  <action>Copy tests/test_tool_registration_phase23.py to tests/test_tool_registration_phase25.py. Change NEW_TOOLS = ["get_goal_projection"], rename the test class to TestPhase25ToolRegistration, update the module docstring to PROG-02. In the copied _install_tools_mocks(), add both "core.projection" AND "core.pace_history" to the sys.modules.setdefault(m, MagicMock()) stub list so importing core.tools does not fail on the new imports inside the handler. Keep the isolated_modules autouse fixture verbatim. Implement the 5 test methods per the behavior block. Tests MUST fail because get_goal_projection is not yet registered (RED).</action>
  <verify>
    <automated>python3 -m pytest tests/test_tool_registration_phase25.py -x 2>&1 | grep -qE "failed|AssertionError" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tool_registration_phase25.py contains NEW_TOOLS = ["get_goal_projection"] and class TestPhase25ToolRegistration
    - The _install_tools_mocks stub list includes "core.projection" and "core.pace_history"
    - All 5 test methods present (in_direct, excluded_from_worker, in_handlers, has_schema, handler_callable)
    - Running the file FAILS (RED) — registration not yet done
  </acceptance_criteria>
  <done>tests/test_tool_registration_phase25.py exists, mirrors the phase23 template, fails because the tool is unregistered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add core/pace_history.py, register get_goal_projection at all four sites + handler (GREEN)</name>
  <read_first>
    - core/tools.py (the file being modified — read lines 40-69, 960-990, 1040-1052, 1700-1800, 1860-1872 for the four anchor regions + _block_stores + _handle_get_benchmark_history)
    - core/weekly_training_review.py (lines 115-150 — the verified query_health_database call shape: import, SQL string, isinstance(rows, list) error guard — mirror this in fetch_dense_pace_history)
    - core/projection.py (the Plan-01 helper being consumed — confirm project_goal_progress signature is facet-agnostic and takes a points list as `history`)
    - mcp_tools/database_tool.py (query_health_database signature: returns list[dict] | str)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-RESEARCH.md (Pattern 3 + "Garmin Pace Trend — Data Path (D-04)" + RESOLVED Open Question #1: duration_sec/distance_m*1000)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-CONTEXT.md (D-04: threshold_pace dense Garmin, strength sparse BenchmarkStore — two code paths intended)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (core/tools.py section — exact four insertion snippets + handler pattern with facet guard + _jsonsafe_doc + _block_stores)
  </read_first>
  <action>STEP A — create core/pace_history.py with `fetch_dense_pace_history(today_iso: str) -> list[dict]`: import query_health_database from mcp_tools.database_tool; build the running-pace SQL exactly as RESEARCH.md "Recommended gather SQL (safe derivation)" — select date::date, duration_sec, distance_m and the derived pace `ROUND((duration_sec::numeric / distance_m * 1000), 1)` (sec/km, RESOLVED Open Question #1 — do NOT read avg_pace), filtering type IN ('running','trail_running','treadmill_running'), date >= NOW() - INTERVAL '90 days', distance_m >= 3000, duration_sec > 0, ORDER BY date DESC LIMIT 20; call rows = query_health_database(sql); if not isinstance(rows, list) return [] (error string fail-open); map each row to a BenchmarkStore-shaped point dict {"date": <activity_date ISO str>, "facet": "threshold_pace", "value": float(pace_sec_per_km), "unit": "sec_per_km"} skipping rows with a null derived pace; wrap the whole body in try/except returning [] (never raises). today_iso is accepted for signature parity / future use but the SQL uses NOW() server-side; do NOT call date.today() in this module.
  STEP B — Edit core/tools.py at four sites mirroring get_benchmark_history: (Site 1) add "get_goal_projection" to the SMART_AGENT_DIRECT_TOOLS frozenset after "get_benchmark_history" (line ~66); (Site 2) add a get_goal_projection schema object to TOOL_SCHEMAS after the get_benchmark_history schema (line ~985) with description noting deterministic projection + brain-direct + the "am I on track" trigger, input_schema requiring a single "facet" string property whose description lists the 5 valid facets; (Site 3) add "get_goal_projection" to the WORKER_TOOL_SCHEMAS exclusion set (line ~1047) so the worker cannot call it; (Site 4) add "get_goal_projection": lambda args: _handle_get_goal_projection(**args) to _HANDLERS after the get_benchmark_history entry (line ~1868).
  STEP C — Implement _handle_get_goal_projection(facet: str) -> str near _handle_get_benchmark_history. (1) Validate facet against _BENCHMARK_FACETS (import from memory.firestore_db) and return json.dumps({"error": ...}) for unknown facets (T-23-01 / V5 guard, mirror _handle_log_benchmark). (2) Compute today_iso = datetime.now(ZoneInfo("Asia/Jerusalem")).date().isoformat() — import datetime + ZoneInfo; NEVER call date.today() (Phase-24 CR-01: UTC midnight would pass yesterday's date into deadline arithmetic). Mirror the existing Asia/Jerusalem-tz cron pattern in core/tools.py. (3) Call _block_stores() -> (blocks, benchmarks, profiles); load profile = _jsonsafe_doc(profiles.load()); dated_goals = profile.get("dated_goals") or []. (4) SOURCE SELECTION per D-04: if facet == "threshold_pace", import fetch_dense_pace_history from core.pace_history and set history = fetch_dense_pace_history(today_iso); if that dense list is empty (no Garmin running data), fall back to history = benchmarks.get_facet_history(facet, n=10). For all other (strength) facets, history = benchmarks.get_facet_history(facet, n=10). (5) result = project_goal_progress(facet, history, dated_goals, today_iso); return json.dumps(result). Import project_goal_progress from core.projection inside the handler (lazy) to match the phase23 handler style. Keep the projection MATH entirely inside project_goal_progress — the handler only selects the source list and the tz-correct today.</action>
  <verify>
    <automated>python3 -m pytest tests/test_tool_registration_phase25.py -x</automated>
  </verify>
  <acceptance_criteria>
    - core/pace_history.py defines `def fetch_dense_pace_history(` returning a list of {date, facet:"threshold_pace", value, unit:"sec_per_km"} dicts, derives pace via duration_sec/distance_m*1000 (grep confirms NO "avg_pace" read), fails open to [] (never raises)
    - core/tools.py registers get_goal_projection in SMART_AGENT_DIRECT_TOOLS, TOOL_SCHEMAS, the WORKER exclusion set, and _HANDLERS (all four)
    - core/tools.py defines def _handle_get_goal_projection( and it validates facet against _BENCHMARK_FACETS before any store access
    - The handler does NOT call date.today(); it uses ZoneInfo("Asia/Jerusalem") to compute today_iso (grep: no "date.today()" in _handle_get_goal_projection; "Asia/Jerusalem" present)
    - For facet == "threshold_pace" the handler calls fetch_dense_pace_history and prefers its points, falling back to BenchmarkStore only when the dense list is empty (D-04); strength facets use get_facet_history
    - Handler wraps profiles.load() in _jsonsafe_doc before passing dated_goals to project_goal_progress
    - `python3 -m pytest tests/test_tool_registration_phase25.py -x` exits 0 (all 5 GREEN)
    - `python3 -m pytest tests/test_tool_registration_phase23.py -x` still exits 0 (no regression to existing registration tests)
  </acceptance_criteria>
  <done>core/pace_history.py supplies the D-04 dense Garmin pace points; get_goal_projection is registered at all four sites; the handler validates facet, computes today in Asia/Jerusalem, selects the dense-vs-sparse source per D-04, then returns a JSON projection; phase25 + phase23 registration tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update smart_agent.md to permit computed projection + describe the tool</name>
  <read_first>
    - prompts/smart_agent.md (the file being modified — read the Tier A/B contract region ~lines 107-134 and the directional-only restriction at ~line 181, plus the TRAINING & ATHLETIC COACHING section where get_benchmark_history is described)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (prompts/smart_agent.md section — exact line-181 replacement clause + the tool-description bullet to add)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-CONTEXT.md (D-02 framing: number + gap + one rec; on-track does not prescribe)
  </read_first>
  <action>In prompts/smart_agent.md, update the directional-only projection restriction (the line containing "projection — directional only ('Oct pace slips'..."): replace it so the brain cites the computed number and gap when get_goal_projection data is available (e.g. "trend → 98kg by Oct 10, ~7kg behind"), falling back to directional language only when no projection data exists. In the TRAINING & ATHLETIC COACHING section near the existing get_benchmark_history usage description, add a bullet for get_goal_projection(facet): it returns projected_value, gap, on_track, confidence, confidence_label computed server-side (numbers never LLM-invented), called when Sir asks "am I on track for my [goal]?" for any of bench_press_1rm, squat_1rm, push_ups, pull_ups, threshold_pace. Preserve the Tier A/B contract block entirely — do not edit lines 107-134. The "behind" framing must match D-02: number + gap + exactly ONE ranked recommendation + "your call, Sir"; on-track does not prescribe.</action>
  <verify>
    <automated>grep -c "get_goal_projection" prompts/smart_agent.md | { read n; [ "$n" -ge 1 ] && ! grep -q "projection — directional only" prompts/smart_agent.md && echo PROMPT-OK; }</automated>
  </verify>
  <acceptance_criteria>
    - prompts/smart_agent.md contains "get_goal_projection" at least once (tool described)
    - The bare "projection — directional only" restriction no longer appears (it now permits the computed number when data is available)
    - The Tier A/B contract block is unchanged (the strings around the data-presence contract remain present)
    - The added tool description names the 5 valid facets and states numbers are computed server-side / never LLM-invented
  </acceptance_criteria>
  <done>smart_agent.md describes get_goal_projection and permits citing the computed projection number while keeping the Tier A/B contract intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brain (LLM) -> get_goal_projection handler | the facet argument is chosen by the LLM and may be malformed or out-of-set |
| handler -> Firestore (BenchmarkStore / UserProfileStore) | read-only; reuses existing credentials |
| handler -> Postgres (activities table, threshold_pace dense path) | read-only; SQL built from hardcoded strings + server-side NOW() only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering | _handle_get_goal_projection(facet) | mitigate | Validate facet against _BENCHMARK_FACETS frozenset before any store access; return JSON {"error": ...} for unknown facets (mirrors T-23-01 in _handle_log_benchmark) |
| T-25-06 | Information Disclosure | dated_goals JSON serialization | mitigate | Wrap profiles.load() in _jsonsafe_doc so DatetimeWithNanoseconds cannot break json.dumps or leak raw timestamp objects |
| T-25-07 | Information Disclosure | output number to Telegram | mitigate | Handler returns the deterministic project_goal_progress dict verbatim; the brain frames it, never recomputes — no fabricated convergence reaches the user |
| T-25-08 | Elevation of Privilege | worker calling the tool | mitigate | get_goal_projection added to the WORKER exclusion set — brain-direct only; worker tool set never exposes it |
| T-25-13 | Tampering | fetch_dense_pace_history SQL (threshold_pace dense path) | mitigate | SQL uses only hardcoded literals + server-side NOW() interval — no LLM/user input reaches the query; pace derived from duration_sec/distance_m (no avg_pace unit ambiguity); non-list result fails open to [] |
| T-25-14 | Tampering | handler today_iso (deadline arithmetic) | mitigate | today_iso computed via ZoneInfo("Asia/Jerusalem") — never date.today() (UTC); prevents the CR-01 off-by-one-day deadline-gap bug |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (RESEARCH.md: stdlib only) — supply-chain surface is nil |
</threat_model>

<verification>
- `python3 -m pytest tests/test_tool_registration_phase25.py -x` exits 0.
- `python3 -m pytest tests/test_tool_registration_phase23.py -x` exits 0 (no regression).
- `grep -c "get_goal_projection" prompts/smart_agent.md` >= 1; "projection — directional only" no longer present.
- `grep -n "Asia/Jerusalem" core/tools.py` shows the handler's tz-correct today_iso; no "date.today()" in _handle_get_goal_projection.
- `grep -c "avg_pace" core/pace_history.py` returns 0 (pace derived from duration_sec/distance_m per RESOLVED Open Question #1).
</verification>

<success_criteria>
- get_goal_projection is callable by the brain, validates its facet, and returns a JSON projection dict from the shared helper.
- threshold_pace sources DENSE Garmin Postgres points (D-04), falling back to sparse BenchmarkStore only when no Garmin running data exists; strength facets use BenchmarkStore.
- The handler computes today in Asia/Jerusalem (CR-01 lesson satisfied) — never date.today().
- The brain prompt permits citing the computed number + gap (D-02 framing) when projection data is available.
- The worker cannot invoke the tool.
</success_criteria>

<output>
Create `.planning/phases/25-progress-projection-benchmark-trend-reporting/25-02-SUMMARY.md` when done.
</output>
