---
phase: 25-progress-projection-benchmark-trend-reporting
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - core/weekly_training_review.py
  - prompts/weekly_training_review.md
  - tests/test_weekly_training_review.py
  - tests/test_prompts.py
autonomous: true
requirements: [PROG-02]
must_haves:
  truths:
    - "The Sunday weekly review computes a per-facet projection server-side and injects a 'projections' key into the week_data passed to the brain"
    - "The weekly_training_review prompt no longer forbids dated projection; it instructs one consolidated pace-to-deadline block per D-01/D-02 while keeping 'Week N of 16' framing"
    - "Projection topic keys (structural-critique:projection:<facet>) are derived for facets with >=1 data point and written to CoachingTopicStore only after send (COACH-05 dedup)"
    - "The projection gather fails open to {} so the cron always sends"
  artifacts:
    - path: "core/weekly_training_review.py"
      provides: "Gather block #8 (projections) + _derive_structural_topics projection keys"
      contains: "projections"
    - path: "prompts/weekly_training_review.md"
      provides: "Fence lifted at lines 37/47/147 + projection instruction; Week N of 16 preserved"
      contains: "Pace-to-deadline"
    - path: "tests/test_weekly_training_review.py"
      provides: "projections-key, fail-open, and derive-projection-topics coverage"
      contains: "projection"
    - path: "tests/test_prompts.py"
      provides: "Assertion that PHASE 25 FENCE text is gone and Week N of 16 remains"
      contains: "PHASE 25 FENCE"
  key_links:
    - from: "core/weekly_training_review.py::_gather_week_data"
      to: "core/projection.py::project_goal_progress"
      via: "per-facet call in gather block #8, results into data['projections']"
      pattern: "project_goal_progress"
    - from: "core/weekly_training_review.py::_derive_structural_topics"
      to: "CoachingTopicStore (post-send loop in run_weekly_review)"
      via: "structural-critique:projection:<facet> keys in coaching_topics_included"
      pattern: "structural-critique:projection:"
---

<objective>
Surface the Plan-01 projection helper proactively in the Sunday weekly review and lift the Phase-24 fence. Extend `core/weekly_training_review.py` with a fail-open gather block that computes projections for the five dated-goal facets, extend `_derive_structural_topics` to emit projection dedup keys, and lift the three "PHASE 25 FENCE — ABSOLUTELY FORBIDDEN" points in `prompts/weekly_training_review.md` (lines 37/47/147) with the D-01/D-02 projection instruction while preserving "Week N of 16".

Purpose: Closes the proactive half of PROG-02 — the Sunday review reports a pace-to-deadline status for at least one facet (ROADMAP success criteria 2 + 3); reuses the Phase-24 COACH-05 dedup so the same "behind on bench" is not re-raised by the 21:30 cron the same day.
Output: weekly-review gather + topic extension, prompt fence lift, integration + prompt tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-CONTEXT.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-RESEARCH.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-01-PLAN.md

<interfaces>
<!-- Verified anchors. See RESEARCH.md "Weekly Review Integration" + PATTERNS.md weekly_training_review section. -->
core/weekly_training_review.py:
  _gather_week_data          # 7 gather blocks; add fail-open block #8 after the last (~line 250)
  _derive_structural_topics  # lines ~255-272; currently emits structural-critique:session-quality
  run_weekly_review          # post-send loop lines ~375-391 iterates coaching_topics_included and
                             # calls _cts.add_topic(today_iso, topic) AFTER send_and_inject —
                             # projection keys flow through unchanged (Extension point C: no new code)
  today_iso is already in scope in _gather_week_data (Asia/Jerusalem date string)

Plan-01 helper (already shipped, do not modify):
  core/projection.py::project_goal_progress(facet, history, dated_goals, today_iso) -> dict

Reused stores (constructed exactly as existing gather blocks do):
  BenchmarkStore(project_id, database).get_facet_history(facet, n=10)
  UserProfileStore(project_id, database).load()["dated_goals"]
  CoachingTopicStore.add_topic(date_str, topic_str)   # plain string topic; write-after-send only

prompts/weekly_training_review.md fence points (verified):
  line 37  "**PHASE 25 FENCE — ABSOLUTELY FORBIDDEN:** Do NOT compute..." (full paragraph replace)
  line 47  "...never a dated projection:" (clause removal in section heading)
  line 147 "- Never project to a deadline — report current/within-block movement only (Phase 25 fence)" (full replace)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing weekly-review + prompt tests</name>
  <read_first>
    - tests/test_weekly_training_review.py (existing — read its mock/fixture setup for _gather_week_data and run_weekly_review)
    - tests/test_prompts.py (existing — read its style for prompt-content assertions)
    - core/weekly_training_review.py (the functions under test: _gather_week_data, _derive_structural_topics)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-RESEARCH.md (Validation table PROG-02-H, I, M, L)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (_derive_structural_topics extension + gather block #8 shape)
  </read_first>
  <behavior>
    - test_gather_includes_projections (PROG-02-H): _gather_week_data (happy path, BenchmarkStore/UserProfileStore mocked) returns a dict containing a "projections" key whose value is a dict
    - test_projection_gather_fails_open (PROG-02-I): when the projection gather raises (mock get_facet_history to throw), data["projections"] == {} and the function does not raise
    - test_derive_projection_topics (PROG-02-M): _derive_structural_topics on week_data with a facet projection of confidence != "no_data" returns a list containing "structural-critique:projection:<facet>"; a no_data facet does NOT produce a key
    - test_no_phase25_fence (PROG-02-L, in test_prompts.py): the weekly_training_review.md content does NOT contain "PHASE 25 FENCE" and STILL contains "Week" (preserves Week N of 16 framing)
  </behavior>
  <action>In tests/test_weekly_training_review.py add test_gather_includes_projections, test_projection_gather_fails_open, and test_derive_projection_topics, reusing the existing module's mocking approach for Firestore stores (patch BenchmarkStore/UserProfileStore where the gather imports them). Build week_data fixtures with a projections sub-dict to drive _derive_structural_topics (one facet with confidence "low", one with "no_data"). In tests/test_prompts.py add test_no_phase25_fence asserting the loaded weekly_training_review.md prompt text contains neither "PHASE 25 FENCE" nor "ABSOLUTELY FORBIDDEN" while still containing "Week". All four tests MUST fail (RED): the projection gather/topic code and the fence lift do not exist yet.</action>
  <verify>
    <automated>python3 -m pytest tests/test_weekly_training_review.py -k projection tests/test_prompts.py -k phase25 2>&1 | grep -qE "failed|AssertionError|KeyError" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_weekly_training_review.py defines test_gather_includes_projections, test_projection_gather_fails_open, test_derive_projection_topics
    - tests/test_prompts.py defines test_no_phase25_fence asserting absence of "PHASE 25 FENCE" and presence of "Week"
    - All four new tests FAIL (RED) before implementation
  </acceptance_criteria>
  <done>Four new tests exist and fail because the projection gather, topic derivation, and fence lift are not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend weekly_training_review.py — gather block #8 + projection topics (GREEN)</name>
  <read_first>
    - core/weekly_training_review.py (the file being modified — read _gather_week_data blocks 6-7 for the try/except/fail-open shape, _derive_structural_topics for the append+dedup pattern, run_weekly_review post-send loop)
    - core/projection.py (the Plan-01 helper being consumed)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (Extension points A/B/C — exact gather block #8, _derive_structural_topics extension, and the note that point C needs NO new code)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-CONTEXT.md (D-03: all 5 closed-set facets have dated goals → all projected; dedup namespace structural-critique:projection:<facet>)
  </read_first>
  <action>In core/weekly_training_review.py add gather block #8 in _gather_week_data after the last existing gather block: wrapped in try/except mirroring block 6, import project_goal_progress from core.projection and BenchmarkStore/UserProfileStore from memory.firestore_db, build project_id/database from env exactly as existing blocks do, load dated_goals = profile.get("dated_goals") or [], loop over the five facets ["bench_press_1rm","squat_1rm","threshold_pace","push_ups","pull_ups"] calling get_facet_history(facet, n=10) then project_goal_progress(facet, history, dated_goals, today_iso), set data["projections"] = projections; on any exception logger.warning(...) and data["projections"] = {} (fail open to empty dict, never None). Extend _derive_structural_topics: after the existing session-quality append, read projections = week_data.get("projections") or {} and for each (facet, result) where isinstance(result, dict) and result.get("confidence") != "no_data" append f"structural-critique:projection:{facet}"; keep the existing first-seen-order dedup return. Do NOT add a write path in run_weekly_review — the projection keys flow through coaching_topics_included into the existing post-send add_topic loop (Extension point C: verify only, no new code). Do NOT call CoachingTopicStore.add_topic anywhere in _gather_week_data or _derive_structural_topics (write-after-send invariant, D-10).</action>
  <verify>
    <automated>python3 -m pytest tests/test_weekly_training_review.py -x -k projection</automated>
  </verify>
  <acceptance_criteria>
    - core/weekly_training_review.py _gather_week_data sets data["projections"] (a dict) on the happy path and data["projections"] == {} when the gather block raises (fail-open)
    - The projection gather block calls project_goal_progress with the in-scope today_iso (not date.today())
    - _derive_structural_topics emits "structural-critique:projection:<facet>" only for facets with confidence != "no_data"
    - grep confirms no CoachingTopicStore.add_topic call inside _gather_week_data or _derive_structural_topics (write-after-send preserved)
    - python3 -m pytest tests/test_weekly_training_review.py -x -k projection exits 0 (GREEN)
  </acceptance_criteria>
  <done>The Sunday gather computes projections, _derive_structural_topics emits dedup keys for facets with data, and the keys reach the existing post-send write loop unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Lift the Phase-24 fence in weekly_training_review.md</name>
  <read_first>
    - prompts/weekly_training_review.md (the file being modified — read lines 30-50 and 140-150 to confirm the three fence points and the adjacent "Week N of 16" framing)
    - prompts/proactive_alert.md (lines 103-117 — the COACH-04 "exactly one ranked recommendation -> your call, Sir" pattern to mirror in the behind-framing)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (weekly_training_review.md section — exact replacement text for lines 37/47/147)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-CONTEXT.md (D-01 confidence tiers + D-02 framing)
  </read_first>
  <action>Edit prompts/weekly_training_review.md at the three fence points. Line 37: replace the entire "PHASE 25 FENCE — ABSOLUTELY FORBIDDEN" paragraph with a "Pace-to-deadline projection (PROG-02)" instruction block that tells the brain, when projections is present in the data, to include one consolidated progress-toward-goals block after the per-facet scorecard, formatting per D-01 tiers (>=2 points: projected value + target date + gap with the count-naming confidence label; on-track phrasing "trend to 106kg by Oct 10, ahead of the 105kg target"; behind phrasing "trend to 98kg by Oct 10, ~7kg behind. Closer: [one ranked structural recommendation]. Your call, Sir."; 1 point: "baseline only, no trend yet — need another benchmark to project."; 0 points: "no measured data for this facet — log a benchmark.") and stating that on-track does not prescribe, behind triggers exactly ONE ranked recommendation, and Tier A target is always distinguished from Tier B measured trend. Line 47: remove the "— never a dated projection:" clause from the section heading and note that the dated projection block follows the within-block scorecard; keep the block-relative "Week {N} of 16" language for within-block status. Line 147: replace "Never project to a deadline — report current/within-block movement only (Phase 25 fence)" with "Project to deadline per D-01 confidence tiers when projections data is present; on-track does not prescribe; behind = one ranked recommendation + your-call-Sir close". Do NOT remove the "Week N of 16" framing anywhere (Pitfall 5). Also add a note that the November speed goals (3k_time, 400m_time) have no benchmark facet and cannot be projected — acknowledge they exist but state a benchmark facet is required (RESEARCH open question 3).</action>
  <verify>
    <automated>! grep -qE "PHASE 25 FENCE|ABSOLUTELY FORBIDDEN" prompts/weekly_training_review.md && grep -q "Week" prompts/weekly_training_review.md && grep -qiE "pace-to-deadline|project to deadline" prompts/weekly_training_review.md && echo FENCE-LIFTED</automated>
  </verify>
  <acceptance_criteria>
    - prompts/weekly_training_review.md no longer contains "PHASE 25 FENCE" or "ABSOLUTELY FORBIDDEN"
    - prompts/weekly_training_review.md still contains "Week" (Week N of 16 framing preserved — Pitfall 5)
    - prompts/weekly_training_review.md contains a pace-to-deadline / projection instruction (grep -i "pace-to-deadline" or "project to deadline" matches)
    - The behind-framing instruction names exactly ONE ranked recommendation + a your-call-Sir close (D-02)
    - A note states the November speed goals (3k_time, 400m_time) require a benchmark facet and are not projected
    - python3 -m pytest tests/test_prompts.py -x -k phase25 exits 0 (the Task-1 fence test now GREEN)
  </acceptance_criteria>
  <done>The three fence points are lifted, the projection instruction is in place per D-01/D-02, "Week N of 16" is preserved, and test_no_phase25_fence passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Scheduler -> run_weekly_review | server-controlled cron trigger; no user input in the gather facet loop |
| gather -> Firestore (BenchmarkStore / UserProfileStore) | read-only; existing credentials |
| review output -> Telegram | computed numbers framed by the brain, sent via send_and_inject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Tampering | gather block #8 facet loop | mitigate | Facet list is a hardcoded literal of the 5 closed-set facets — no LLM/user input reaches get_facet_history or project_goal_progress |
| T-25-10 | Denial of Service | projection gather failure blocks the cron | mitigate | Block #8 is wrapped in try/except → fail-open to data["projections"] = {}; the Sunday review always sends |
| T-25-11 | Information Disclosure | fabricated convergence in the review | mitigate | Numbers come only from the deterministic project_goal_progress dict; the prompt frames Tier A target vs Tier B trend; 0-point facets say "no measured data" (no invented number) |
| T-25-12 | Repudiation | duplicate same-day nag across crons | mitigate | structural-critique:projection:<facet> written to CoachingTopicStore only after send_and_inject succeeds (COACH-05 / D-10 write-after-send); the 21:30 cron sees the topic and suppresses |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (RESEARCH.md: stdlib only) — supply-chain surface is nil |
</threat_model>

<verification>
- python3 -m pytest tests/test_weekly_training_review.py -x -k projection exits 0.
- python3 -m pytest tests/test_prompts.py -x -k phase25 exits 0.
- grep -E "PHASE 25 FENCE|ABSOLUTELY FORBIDDEN" prompts/weekly_training_review.md returns nothing; grep "Week" matches (Week N of 16 preserved).
- No CoachingTopicStore.add_topic call appears inside _gather_week_data or _derive_structural_topics (write-after-send invariant).
</verification>

<success_criteria>
- The Sunday review computes per-facet projections server-side and injects them into week_data (fail-open to {}).
- The prompt reports one consolidated pace-to-deadline block per D-01/D-02 while keeping Week N of 16 framing.
- Projection dedup keys are written only after send, reusing the Phase-24 COACH-05 gate.
</success_criteria>

<output>
Create `.planning/phases/25-progress-projection-benchmark-trend-reporting/25-03-SUMMARY.md` when done.
</output>
