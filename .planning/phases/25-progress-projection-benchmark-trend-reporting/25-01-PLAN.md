---
phase: 25-progress-projection-benchmark-trend-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/projection.py
  - tests/test_projection.py
autonomous: true
requirements: [PROG-02]
must_haves:
  truths:
    - "A deterministic helper computes a projected facet value toward a dated goal from sparse benchmark history — the brain never invents the number"
    - "0 data points returns a no_data result; 1 point returns baseline_only; >=2 points returns a linear-trend projection with a confidence label that names the count"
    - "Direction-aware on_track: higher-is-better facets are on track when projected >= target; lower-is-better (threshold_pace) when projected <= target"
    - "The helper accepts today_iso as a parameter and never calls date.today() internally (Phase-24 CR-01 tz lesson)"
    - "The half-marathon time string converts to a sec/km target for threshold_pace comparison"
  artifacts:
    - path: "core/projection.py"
      provides: "project_goal_progress pure-function helper + FACET_DIRECTION + GOAL_METRIC_TO_FACET lookup tables"
      contains: "def project_goal_progress"
      min_lines: 80
    - path: "tests/test_projection.py"
      provides: "Unit coverage of 0/1/2/3-point cases, both directions, HM conversion, same-date dedup"
      contains: "from core.projection import project_goal_progress"
  key_links:
    - from: "core/projection.py::project_goal_progress"
      to: "today_iso parameter"
      via: "date.fromisoformat(today_iso) — never date.today()"
      pattern: "today_iso"
    - from: "tests/test_projection.py"
      to: "core/projection.py"
      via: "direct import, no Firestore mock"
      pattern: "from core.projection import"
---

<objective>
Build the deterministic projection helper that is the foundation of PROG-02. `core/projection.py` is a pure-function module (no I/O, no LLM calls, never raises) that takes a benchmark history list + dated goals + today's date string and returns a JSON-serializable `ProjectionResult` dict. Both Wave-2 surfaces (the reactive `get_goal_projection` tool and the Sunday weekly-review block) call this single helper so the math is identical and auditable.

Purpose: Numbers are computed server-side, never LLM-invented (D-13 anti-fabrication / Tier-A-vs-Tier-B "no fabricated convergence" contract). This is the unfabricatable core.
Output: `core/projection.py` + `tests/test_projection.py` (Wave-0 test scaffold for the whole phase's pure-function coverage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-CONTEXT.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-RESEARCH.md
@.planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md

<interfaces>
<!-- Verified contracts the executor needs. From RESEARCH.md "Data Reality — Verified Signatures". -->

BenchmarkStore facet history entry (from get_facet_history, date-desc):
  {"date": "2026-07-18", "facet": "bench_press_1rm", "value": 92.5,
   "unit": "kg", "block_id": "...", "notes": "...", "doc_id": "2026-07-18_bench_press_1rm"}

UserProfileStore.dated_goals (list of dicts):
  [{"target_date": "2026-10-31", "goal_label": "...",
    "metrics": {"bench_press_kg": 100, "squat_kg": 120, "half_marathon_time": "1:25:00"}},
   {"target_date": "2026-11-30", "goal_label": "...",
    "metrics": {"push_ups": 125, "pull_ups": 35, "3k_time": "9:30", "400m_time": "55s"}}]

5-facet closed set (memory/firestore_db.py _BENCHMARK_FACETS):
  {"bench_press_1rm", "squat_1rm", "push_ups", "pull_ups", "threshold_pace"}

Facet -> dated_goals metric key + direction (RESEARCH.md Pattern 2 table):
  bench_press_1rm -> bench_press_kg     (kg,  target 100, 2026-10-31, higher-is-better)
  squat_1rm       -> squat_kg           (kg,  target 120, 2026-10-31, higher-is-better)
  threshold_pace  -> half_marathon_time (str "1:25:00" -> ~241.7 sec/km, lower-is-better)
  push_ups        -> push_ups           (reps, target 125, 2026-11-30, higher-is-better)
  pull_ups        -> pull_ups           (reps, target 35,  2026-11-30, higher-is-better)
  3k_time, 400m_time -> NO benchmark facet — not projectable (silently ignored)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing pure-function tests for project_goal_progress</name>
  <read_first>
    - tests/test_projection.py (does not exist yet — this task creates it)
    - tests/test_pricing.py (analog: pure-function unit tests, direct import, no mocks, no fixtures)
    - tests/test_compute_acwr.py (analog: synthetic-data builder helper, today passed as parameter, edge-case assertions) — if absent, mirror the test_pricing.py shape
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-RESEARCH.md (Validation Architecture table PROG-02-A..G,N; HM conversion 85*60/21.1 ~= 241.7)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (tests/test_projection.py section — exact test names + _build_history helper shape)
  </read_first>
  <behavior>
    - test_project_0_points: empty history -> result["confidence"] == "no_data", result["projected_value"] is None
    - test_project_1_point: one entry -> result["confidence"] == "baseline_only", projected_value is None, confidence_label mentions "baseline"
    - test_project_2_points: two entries (improving) -> projected_value is a float, on_track is a bool, confidence_label names the count "2"
    - test_project_3_points: three entries -> least-squares fit; projected_value reflects the trend slope, data_point_count == 3
    - test_higher_is_better: bench_press_1rm projected >= target -> on_track True; projected < target -> on_track False
    - test_lower_is_better: threshold_pace projected <= target -> on_track True; projected > target -> on_track False
    - test_hm_time_conversion: dated_goals half_marathon_time "1:25:00" yields target_value ~241.7 sec/km (assert within 0.5)
    - test_dedup_same_date: two history entries with identical date collapse to one before the LSQ fit (no ZeroDivisionError; finite projected_value)
  </behavior>
  <action>Create tests/test_projection.py importing project_goal_progress, FACET_DIRECTION, GOAL_METRIC_TO_FACET from core.projection. Add a module-level _build_history(values, dates, facet, unit) helper that returns date-desc list[dict] matching the BenchmarkStore entry shape (keys: date, facet, value, unit, block_id, notes). Implement the 8 test functions named exactly per 25-PATTERNS.md. Build dated_goals fixtures matching the verified shape in the interfaces block. Pass today_iso="2026-08-01" explicitly to every call (never rely on the system clock). Tests MUST fail at this point because core/projection.py does not exist yet (collection error is an acceptable RED state).</action>
  <verify>
    <automated>python3 -m pytest tests/test_projection.py -x 2>&1 | grep -qE "ModuleNotFoundError|No module named 'core.projection'|collected 8" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_projection.py defines exactly the 8 functions: test_project_0_points, test_project_1_point, test_project_2_points, test_project_3_points, test_higher_is_better, test_lower_is_better, test_hm_time_conversion, test_dedup_same_date
    - File contains "from core.projection import project_goal_progress"
    - Every project_goal_progress call site passes a literal today_iso string (grep: no "date.today" or "datetime.now" in the test file)
    - Running the file errors on the missing core.projection import (RED) — implementation does not exist yet
  </acceptance_criteria>
  <done>tests/test_projection.py exists with 8 named tests; collection fails on missing core.projection module (RED state confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement core/projection.py pure-function helper (GREEN)</name>
  <read_first>
    - core/projection.py (does not exist yet — this task creates it)
    - core/pricing.py (analog: module-level lookup dict, pure function, never raises, fail-open default)
    - mcp_tools/garmin_tool.py (analog: compute_acwr(activities, today=today) — today passed as parameter, returns typed dict)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-RESEARCH.md (Pattern 1 signature + _linear_project LSQ math + Pitfall 2 same-date dedup + Pitfall 6 tz)
    - .planning/phases/25-progress-projection-benchmark-trend-reporting/25-PATTERNS.md (core/projection.py section — imports, lookup tables, return shape, error-handling wrapper)
  </read_first>
  <action>Create core/projection.py with `from __future__ import annotations`, stdlib-only imports (logging, dataclasses asdict/dataclass, datetime.date, typing.Optional), module logger. Define FACET_DIRECTION dict (bench_press_1rm/squat_1rm/push_ups/pull_ups=True, threshold_pace=False) and GOAL_METRIC_TO_FACET dict per the interfaces table. Add a private _hm_to_sec_per_km(hms: str) -> float helper that parses "H:MM:SS" to total seconds and divides by 21.1. Add a private _linear_project(points, target_t) -> float doing least-squares slope with a `den != 0` guard returning 0.0 slope on degenerate input (per RESEARCH _linear_project). Define a ProjectionResult dataclass with fields facet, confidence, data_point_count, projected_value, target_value, target_date, gap, on_track, unit, confidence_label. Implement project_goal_progress(facet, history, dated_goals, today_iso) -> dict: (1) resolve target_value + target_date by scanning dated_goals.metrics for the metric key that maps to this facet (threshold_pace via _hm_to_sec_per_km on half_marathon_time); (2) dedup history by date keeping the most recent value per date (Pitfall 2); (3) branch on point count — 0 -> no_data, 1 -> baseline_only (projected_value None, confidence_label "baseline only, no trend yet"), >=2 -> compute t_i = (date_i - earliest_date).days, call _linear_project to target_t = (target_date - earliest_date).days, set projected_value, gap (direction-aware projected vs target), on_track via FACET_DIRECTION, and confidence_label naming the count ("from N benchmarks" / "from only 2 benchmarks — low confidence"); set confidence to "low" for exactly 2 points, "medium"/"high" for 3+. Parse today_iso via date.fromisoformat — NEVER call date.today() or datetime.now(). Wrap the entire body in try/except returning the no_data fail-open dict (per 25-PATTERNS.md error-handling block). Return asdict(ProjectionResult(...)) so callers get a plain JSON-serializable dict.</action>
  <verify>
    <automated>python3 -m pytest tests/test_projection.py -x</automated>
  </verify>
  <acceptance_criteria>
    - core/projection.py defines `def project_goal_progress(` and returns a dict (asdict of ProjectionResult)
    - Module defines FACET_DIRECTION and GOAL_METRIC_TO_FACET as module-level dicts
    - grep confirms NO "date.today" and NO "datetime.now" anywhere in core/projection.py (use: grep -v '^#' core/projection.py | grep -c 'date.today\|datetime.now' returns 0)
    - The function body is wrapped in try/except returning a no_data dict on any exception (never raises)
    - `python3 -m pytest tests/test_projection.py -x` exits 0 — all 8 tests pass (GREEN), covering 0/1/2/3-point cases and both metric directions
  </acceptance_criteria>
  <done>All 8 tests in tests/test_projection.py pass; core/projection.py is pure, never raises, never reads the system clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> project_goal_progress | facet string and today_iso string arrive from a tool handler (LLM-influenced) or a cron (server-controlled) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | project_goal_progress(today_iso) | mitigate | Wrap date.fromisoformat(today_iso) inside the function's try/except; a malformed date returns a no_data result rather than raising |
| T-25-02 | Tampering | _linear_project denominator | mitigate | `den != 0` guard + same-date dedup prevents division-by-zero / NaN reaching the result (Pitfall 2) |
| T-25-03 | Information Disclosure | projected_value fabrication | mitigate | Number is computed by deterministic LSQ from real history only; 0-point facets return projected_value=None (no invented convergence) |
| T-25-04 | Tampering | facet not in FACET_DIRECTION | accept | Helper is the inner layer; the tool handler (Plan 02) validates facet against _BENCHMARK_FACETS before this is reached. An unmapped facet here resolves to no target_value and a no_data/baseline result — fails safe, no store access in this module |
</threat_model>

<verification>
- `python3 -m pytest tests/test_projection.py -x` exits 0 (all 8 pass).
- `grep -v '^#' core/projection.py | grep -c 'date.today\|datetime.now'` returns 0.
- core/projection.py imports only stdlib (no firestore/garmin/network imports — confirms pure-function).
</verification>

<success_criteria>
- project_goal_progress returns the three documented confidence shapes (no_data / baseline_only / projected) keyed on data-point count per D-01.
- Direction-aware on_track is correct for both higher-is-better and lower-is-better facets.
- HM time string converts to a sec/km target within 0.5 of 241.7.
- The helper accepts today_iso and never reads the system clock (CR-01 lesson satisfied).
</success_criteria>

<output>
Create `.planning/phases/25-progress-projection-benchmark-trend-reporting/25-01-SUMMARY.md` when done.
</output>
