---
phase: 22-expert-coaching-knowledge-d-13-release
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - prompts/morning_briefing.md
  - prompts/proactive_alert.md
  - prompts/autonomous.md
  - core/morning_briefing.py
  - core/proactive_alerts.py
  - tests/test_main_render_smart_system.py
autonomous: true
requirements: [COACH-01, COACH-02]
must_haves:
  truths:
    - "morning_briefing.md, proactive_alert.md, and autonomous.md each contain a {coaching_guide} placeholder"
    - "_compose_briefing and _compose_alert inject the slim core so no literal {coaching_guide} survives"
    - "autonomous Layer-2 receives the slim core via its existing render_smart_system call"
    - "Crons are biased toward staying on the cheap slim core (deep lookup only when warranted)"
  artifacts:
    - path: "core/morning_briefing.py"
      provides: "{coaching_guide} injection at compose time"
      contains: "coaching_guide"
    - path: "core/proactive_alerts.py"
      provides: "{coaching_guide} injection at compose time"
      contains: "coaching_guide"
    - path: "prompts/autonomous.md"
      provides: "{coaching_guide} placeholder resolved via render_smart_system"
      contains: "{coaching_guide}"
  key_links:
    - from: "core/morning_briefing.py:_compose_briefing"
      to: "_get_orchestrator()._coaching_guide_content"
      via: ".replace('{coaching_guide}', ...)"
      pattern: "coaching_guide"
    - from: "core/proactive_alerts.py:_compose_alert"
      to: "_get_orchestrator()._coaching_guide_content"
      via: ".replace('{coaching_guide}', ...)"
      pattern: "coaching_guide"
---

<objective>
Make the proactive crons coaching-aware (COACH-01 in the crons; supports COACH-02/SC-2). The
morning briefing and evening alert compose their own prompts and do NOT pass through
`render_smart_system`, so their `{coaching_guide}` placeholders would render literally unless
injected at compose time. Add a `{coaching_guide}` placeholder to each cron template and inject
the orchestrator's cached slim core in `_compose_briefing` / `_compose_alert`. The autonomous
Layer-2 path already calls `render_smart_system`, so it only needs the placeholder in
`prompts/autonomous.md`. Express D-05's cost bias (high-frequency crons stay on the cheap core;
deep `read_coaching_guide` only when a topic genuinely warrants it) in the cron prompt text.

Purpose: every proactive coaching touchpoint carries the slim core; SC-2 (briefing/alert name
the specific session + load) is unblocked by the guide reaching the cron compose path.
Output: three updated cron templates + two updated compose functions + a no-literal-placeholder test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-expert-coaching-knowledge-d-13-release/22-RESEARCH.md
@.planning/phases/22-expert-coaching-knowledge-d-13-release/22-PATTERNS.md

<interfaces>
<!-- Compose-time injection contracts. Extracted from PATTERNS.md / RESEARCH Regression Surface. -->

core/morning_briefing.py:280-289 — _compose_briefing(today_data, today_iso): reads prompts/morning_briefing.md,
  does .replace("{today_date}", today_iso). Add .replace("{coaching_guide}", _get_orchestrator()._coaching_guide_content) BEFORE the today_date replace.
core/proactive_alerts.py:369-379 — _compose_alert(alerts_context): reads prompts/proactive_alert.md,
  does .replace("{today_date}", today_str). Same injection (coaching_guide before today_date).
core/main.py:_get_orchestrator() — process-wide singleton (already imported in autonomous.py). Exposes ._coaching_guide_content.
core/autonomous.py:568-579 — _compose_layer2() already calls orchestrator.render_smart_system(smart_system_template).
  NO code change in autonomous.py; only prompts/autonomous.md needs the {coaching_guide} placeholder.

Stable-prefix ordering: place {coaching_guide} near the TOP of each .md template, before any volatile {today_date}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add {coaching_guide} placeholders to the three cron templates</name>
  <files>prompts/morning_briefing.md, prompts/proactive_alert.md, prompts/autonomous.md</files>
  <read_first>
    - prompts/morning_briefing.md (full — find a top-of-prompt position before any {today_date})
    - prompts/proactive_alert.md (full — same)
    - prompts/autonomous.md (full — same; this one resolves via render_smart_system)
    - .planning/phases/22-expert-coaching-knowledge-d-13-release/22-RESEARCH.md (lines 717-755 — Pitfall 6 + Regression Surface table + cron cost-discipline wording)
  </read_first>
  <action>
    Insert a `{coaching_guide}` placeholder near the TOP of each of `prompts/morning_briefing.md`,
    `prompts/proactive_alert.md`, and `prompts/autonomous.md` — before any `{today_date}` token to
    keep the stable prefix first. Immediately after the placeholder in `morning_briefing.md` and
    `autonomous.md` (the two high-frequency / low-stakes crons per D-05), add one cost-bias line in
    Klaus's voice: "You already have the coaching guide core above. Only call read_coaching_guide(topic)
    if Sir asks 'why?' or a precise protocol isn't covered by the core." In `proactive_alert.md`
    (evening check-in) add the same placeholder; the Sunday weekly review tuning is out of scope here.
    Do NOT add the placeholder anywhere a volatile token already precedes it.
  </action>
  <verify>
    <automated>for f in prompts/morning_briefing.md prompts/proactive_alert.md prompts/autonomous.md; do grep -q '{coaching_guide}' "$f" || { echo "MISSING in $f"; exit 1; }; done; echo "all three templates have placeholder"</automated>
  </verify>
  <acceptance_criteria>
    - All three `.md` files contain a `{coaching_guide}` token.
    - In each file, `{coaching_guide}` appears before the first `{today_date}` (source assertion via line-order check).
    - `morning_briefing.md` and `autonomous.md` contain the cost-bias instruction line.
  </acceptance_criteria>
  <done>Three cron templates carry the placeholder; high-frequency crons carry the cost-bias steer.</done>
</task>

<task type="auto">
  <name>Task 2: Inject the slim core at cron compose time + no-literal-placeholder test</name>
  <files>core/morning_briefing.py, core/proactive_alerts.py, tests/test_main_render_smart_system.py</files>
  <read_first>
    - core/morning_briefing.py (lines 280-289 _compose_briefing; the run_morning_briefing call site)
    - core/proactive_alerts.py (lines 369-379 _compose_alert)
    - core/autonomous.py (lines 568-579 — confirm render_smart_system pass-through; NO edit here)
    - tests/test_main_render_smart_system.py (briefing-no-literal-placeholder test slot; reuse _make_orchestrator)
    - .planning/phases/22-expert-coaching-knowledge-d-13-release/22-PATTERNS.md (lines 471-547 exact compose-time injection edits; lines 705-719 shared compose pattern)
  </read_first>
  <action>
    In `core/morning_briefing.py:_compose_briefing`, before the existing `.replace("{today_date}",
    today_iso)`, fetch the cached slim core via `from core.main import _get_orchestrator` →
    `_get_orchestrator()._coaching_guide_content` and chain `.replace("{coaching_guide}",
    coaching_guide_content)` (stable before volatile). Keep the existing OSError plain-text fallback
    intact. Apply the identical pattern to `core/proactive_alerts.py:_compose_alert`. Do NOT modify
    `core/autonomous.py` (its render_smart_system call already resolves the placeholder once Task 1
    adds it to `autonomous.md`). Add a `briefing_no_literal_placeholder` unit test to
    `tests/test_main_render_smart_system.py`: render the morning briefing prompt through the compose
    path (or assert via a focused unit on the replace logic) and assert the produced system prompt
    contains NO literal `{coaching_guide}` substring.
  </action>
  <verify>
    <automated>python -m pytest tests/test_main_render_smart_system.py -x -k "briefing_no_literal_placeholder or coaching_guide" && python -m pytest tests/test_proactive_alerts.py tests/test_morning_briefing.py -x 2>/dev/null; true</automated>
  </verify>
  <acceptance_criteria>
    - `_compose_briefing` and `_compose_alert` each chain `.replace("{coaching_guide}", ...)` before `.replace("{today_date}", ...)` (source assertion).
    - The OSError plain-text fallback paths are preserved (not removed).
    - `core/autonomous.py` is unchanged (no diff).
    - `briefing_no_literal_placeholder` test asserts no literal `{coaching_guide}` survives; it passes.
    - Existing morning-briefing / proactive-alert tests still pass.
  </acceptance_criteria>
  <done>Both compose paths inject the slim core; autonomous untouched; no literal placeholder leaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coaching-guide slim core → cron system prompts | The slim core is interpolated into briefing/alert/autonomous prompts that drive autonomous outbound messages to Amit. |
| orchestrator singleton → cron compose | Crons read `_get_orchestrator()._coaching_guide_content` cross-module. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Information Disclosure | unresolved `{coaching_guide}` literal leaking into an outbound cron message | mitigate | `briefing_no_literal_placeholder` test asserts no literal token survives; compose injection runs before send. Pitfall 6 directly addressed. |
| T-22-09 | Denial of Service / cost | crons calling deep `read_coaching_guide` on every high-frequency tick | mitigate | D-05 cost-bias instruction in `morning_briefing.md` / `autonomous.md` steers the LLM to stay on the cheap slim core; deep lookup reserved for "why?" / precise-protocol needs. Prompt-level, not a hard block (per D-05). |
| T-22-10 | Tampering | missing orchestrator attribute if Plan 02 incomplete | accept | depends_on 22-02 guarantees `_coaching_guide_content` exists; if absent the wave gate fails before this plan runs. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (prompt + compose edits only). |
</threat_model>

<verification>
- `python -m pytest tests/test_main_render_smart_system.py -x -k "briefing_no_literal_placeholder or coaching_guide"` green.
- SC-2 manual smoke: `python -m core.morning_briefing --dry-run --date <tomorrow>` names the scheduled session type + plan target (not generic "strength session").
- `core/autonomous.py` shows no diff.
</verification>

<success_criteria>
- COACH-01 reaches the crons: morning briefing, evening alert, and autonomous tick all carry the slim core; no literal placeholder leaks; cost bias expressed; SC-2 specificity unblocked.
</success_criteria>

<output>
Create `.planning/phases/22-expert-coaching-knowledge-d-13-release/22-03-SUMMARY.md` when done.
</output>
