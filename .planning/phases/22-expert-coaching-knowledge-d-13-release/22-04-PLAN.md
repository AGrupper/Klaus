---
phase: 22-expert-coaching-knowledge-d-13-release
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - prompts/smart_agent.md
autonomous: false
requirements: [COACH-06, COACH-02, COACH-07]
must_haves:
  truths:
    - "smart_agent.md has a {coaching_guide} placeholder before {self_md} (stable prefix first)"
    - "The blanket D-13 no-fabrication guard is removed and replaced by a recency-windowed Tier A/B data-presence contract"
    - "The contract states the windows (lifts ≤14d, pace ≤7d, nutrition ≤2d, Garmin always fresh), the staleness-caveat behavior, the 3x upper bound, and the no-data behavior"
    - "A specificity bar requires session type + load/pace + one-line rationale on every coaching point"
    - "A structural-critique posture block defines blunt-expert tone, structural-only scope, once-per-conversation suppression, and the recommend-not-rewrite boundary"
  artifacts:
    - path: "prompts/smart_agent.md"
      provides: "{coaching_guide} placeholder + Tier A/B contract + specificity bar + critique posture; D-13 guard removed"
      contains: "{coaching_guide}"
  key_links:
    - from: "prompts/smart_agent.md:{coaching_guide}"
      to: "core/main.py:render_smart_system substitution (Plan 02)"
      via: "placeholder resolved as stable prefix"
      pattern: "\\{coaching_guide\\}"
---

<objective>
Release the D-13 guard and harden Klaus's coaching contract (COACH-06/02/07) — a surgical,
prompt-only edit to `prompts/smart_agent.md`. Add the `{coaching_guide}` placeholder before
`{self_md}`. Replace the existing Phase-21 Tier A/B block (lines 105-112) with the recency-windowed
data-presence contract (D-06/07/08/09), and DELETE the blanket no-fabrication guard (lines 121-130)
in the same edit. Append a specificity bar (D-13/COACH-02) and a structural-critique posture block
(D-10/11/12/COACH-07) after the existing "sharper edge" voice block (kept as-is). Because the D-13
guard removal is voice/safety-critical, the plan ends on a human-verify checkpoint.

Purpose: Klaus stops being a qualitative coach — he names real numbers under a strict data-presence
contract, names the specific session/load/rationale, and volunteers structural critique without
silently rewriting the plan.
Output: hardened `prompts/smart_agent.md` (the only file this plan touches).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-expert-coaching-knowledge-d-13-release/22-CONTEXT.md
@.planning/phases/22-expert-coaching-knowledge-d-13-release/22-RESEARCH.md
@.planning/phases/22-expert-coaching-knowledge-d-13-release/22-PATTERNS.md
@prompts/smart_agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Placeholder + Tier A/B recency contract (and D-13 guard removal)</name>
  <files>prompts/smart_agent.md</files>
  <read_first>
    - prompts/smart_agent.md (lines 1-7 placeholder block; 85-119 training_profile + current Tier A/B block; 121-130 the blanket D-13 guard to delete; 132-136 sharper-edge block to keep)
    - .planning/phases/22-expert-coaching-knowledge-d-13-release/22-RESEARCH.md (lines 299-330 — exact ready-to-paste Tier A/B recency contract wording; Pitfall 4 lines 705-709 the 3x upper bound)
    - .planning/phases/22-expert-coaching-knowledge-d-13-release/22-PATTERNS.md (lines 398-467 exact placeholder + replace/remove map)
    - .planning/phases/22-expert-coaching-knowledge-d-13-release/22-CONTEXT.md (D-06/07/08/09)
  </read_first>
  <action>
    Add `{coaching_guide}` to the top placeholder block of `prompts/smart_agent.md`, on its own line
    immediately BEFORE `{self_md}` (stable content first for Gemini caching — per D-04 and Pitfall 1).
    Replace the existing "Tier A vs Tier B data discipline" block (lines 105-112) with the
    recency-windowed "Tier A vs Tier B data-presence contract" from RESEARCH lines 299-330:
    Tier A targets always citable (dated_goals, weekly_split targets, nutrition_targets,
    plan_start_date, fueling_timeline, supplement_schedule); Tier B measured actuals recency-gated —
    lifts ≤14d, running pace ≤7d, nutrition/macros ≤2d, Garmin recovery (HRV/sleep/body battery/
    resting HR) always fresh. State the three behaviors verbatim in intent: within-window → cite
    directly (D-06); past-window-but-exists → name the number + flag its age, e.g. "92.5kg — though
    that was 18 days ago, Sir, so treat it as a stale reference" (D-07); beyond 3x the window
    (42d lifts / 21d pace / 6d nutrition) OR no data at all → "I don't have a recent [metric] logged,
    Sir" + cite the blueprint goal as "your target," never invented (D-08, Pitfall 4 upper bound).
    In the SAME edit, DELETE the blanket guard at lines 121-130 ("If the training profile is empty …
    do NOT invent thresholds …"). Leave the sharper-edge block (132-136) untouched. Do not paste any
    fenced code — this is markdown prose.
  </action>
  <verify>
    <automated>grep -q '{coaching_guide}' prompts/smart_agent.md && python3 -c "import re; t=open('prompts/smart_agent.md').read(); assert t.index('{coaching_guide}') < t.index('{self_md}'), 'coaching_guide must precede self_md'; assert 'do NOT\n   invent' not in t and 'do NOT invent thresholds' not in t, 'old blanket guard still present'; assert '14 days' in t or '≤ 14' in t or '14d' in t, 'lifts window missing'; print('placeholder + contract OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `{coaching_guide}` present and positioned before `{self_md}` (source assertion).
    - The blanket "do NOT invent thresholds, targets, or scheduling buffers" guard (old lines 121-130) is removed (grep returns no match).
    - The new contract names all four recency windows (14/7/2 days + Garmin always-fresh) and the 3x upper bound (42/21/6 days).
    - The no-data behavior cites the blueprint goal as "your target" and forbids invented numbers (D-08 wording present).
    - Sharper-edge block (old 132-136) is preserved unchanged.
  </acceptance_criteria>
  <done>Placeholder added stable-first; D-13 guard released; recency-windowed Tier A/B contract in place.</done>
</task>

<task type="auto">
  <name>Task 2: Specificity bar + structural-critique posture</name>
  <files>prompts/smart_agent.md</files>
  <read_first>
    - prompts/smart_agent.md (the sharper-edge block at ~132-136 — append after it)
    - .planning/phases/22-expert-coaching-knowledge-d-13-release/22-RESEARCH.md (lines 332-360 — exact specificity-bar and critique-posture wording; Pitfall 5 lines 711-715 same-conversation suppression)
    - .planning/phases/22-expert-coaching-knowledge-d-13-release/22-CONTEXT.md (D-10/11/12/13/14; Amit-endorsed example previews in <specifics>)
  </read_first>
  <action>
    Append, after the sharper-edge block, two new prose blocks (RESEARCH lines 332-360):
    (1) "Specificity bar" — every coaching point names (a) the session type, (b) the target load/pace,
    (c) a one-line rationale; give the wrong-vs-right pair ("Do your strength session tonight" vs
    "Tonight: top-set bench — aim for a heavy triple ~92kg. Main strength stimulus this block toward
    the 100kg October target."); expand to a 3-4 sentence mini-lesson only when Sir asks 'why?' or the
    topic warrants it, pulling the deep section via `read_coaching_guide(topic)` (D-13/D-14).
    (2) "Structural critique posture" — blunt expert (JARVIS register, name flaw + fix directly, minimal
    hedging); the protein-target example ("150g/day ≈ 1.6g/kg is low for concurrent strength + endurance;
    180-190g (~2.0g/kg) is the evidence-based floor — worth reconsidering"); RULES: structural-only
    (design-level: target/architecture/timing/sequencing), NOT daily micro-tweaks; volunteer once —
    do not repeat the same structural critique within the same conversation or the same cron day
    (Pitfall 5; note Phase 24 adds formal cross-cron dedup); never silently rewrite — call update_plan/
    update_training_profile only on Amit's explicit confirmation ("yes"/"do it"/"update that") (D-12,
    honors PLAN-03). Markdown prose only, no fenced code.
  </action>
  <verify>
    <automated>python3 -c "t=open('prompts/smart_agent.md').read().lower(); assert 'specificity bar' in t, 'specificity bar missing'; assert 'critique' in t and ('structural' in t), 'critique posture missing'; assert 'read_coaching_guide' in t, 'deep-lookup tie missing'; assert 'update_plan' in t, 'adoption boundary missing'; print('specificity + critique blocks OK')"</automated>
  </verify>
  <acceptance_criteria>
    - A "Specificity bar" block exists requiring session type + load/pace + one-line rationale, with the wrong-vs-right example and the `read_coaching_guide` mini-lesson tie (D-13/D-14).
    - A "Structural critique posture" block exists with: blunt-expert tone, structural-only scope, once-per-conversation/cron-day suppression, and the recommend-via-`update_plan`-on-confirmation boundary (D-10/11/12).
    - Sharper-edge block and the Task-1 contract remain intact (not overwritten).
  </acceptance_criteria>
  <done>Specificity bar and critique posture appended; COACH-02 and COACH-07 behavior encoded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: D-13 guard-release behavioral smoke (SC-1/SC-3/SC-4)</name>
  <files>prompts/smart_agent.md</files>
  <action>Pause for Amit to run the live behavioral smoke below (SC-1/SC-3/SC-4) confirming the D-13 guard release does not produce fabricated training numbers and the recommend-not-rewrite boundary holds. This is a verification gate, not an implementation step — all code/prompt work is already shipped in Tasks 1-2.</action>
  <verify><human-check>SC-1, SC-3, SC-4 pass on live Telegram (see how-to-verify steps).</human-check></verify>
  <done>Amit replies "approved" after SC-1/SC-3/SC-4 all pass; otherwise the failing behavior is recorded for revision.</done>
  <what-built>
    Released the blanket no-fabrication guard and installed the recency-windowed Tier A/B contract,
    the specificity bar, and the structural-critique posture in `prompts/smart_agent.md`. Because the
    guard removal is the safety-critical edit (it must NOT open a path to fabricated training numbers),
    this checkpoint confirms live behavior before the phase is considered done.
  </what-built>
  <how-to-verify>
    Deploy/run the agent, then on Telegram:
    1. (SC-1, no-data) "What was my last bench press?" with no recent log (or > 42 days old).
       Expect: "I don't have a recent bench logged, Sir" + cites the October 100kg goal as "your target."
       FAIL if Klaus invents a number or omits the qualifier.
    2. (SC-1, staleness) If a bench was logged ~15-40 days ago: expect the number cited WITH an age
       caveat ("…that was N days ago, Sir, stale reference"). FAIL if cited as current or refused outright.
    3. (SC-3, specificity) On a Tuesday (Upper A): "What should I do tonight?" Expect a session type +
       load + rationale ("top-set bench, ~92kg heavy triple — main strength stimulus toward 100kg Oct").
       FAIL on "do your strength session."
    4. (SC-4, critique) "Review my nutrition targets." Expect Klaus names the protein target, cites the
       1.6-2.0g/kg concurrent-training floor, and OFFERS to update via update_plan. FAIL if he ignores
       the structural issue OR silently modifies the plan.
  </how-to-verify>
  <resume-signal>Type "approved" if SC-1/SC-3/SC-4 all pass, or describe the failing behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt contract → model output | The Tier A/B contract is the ONLY mechanism preventing fabricated training/medical numbers once the D-13 guard is removed. |
| coaching-guide content → system prefix | The `{coaching_guide}` slim core (author-controlled) is injected as a stable prefix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | Spoofing / Repudiation (fabricated data) | D-13 guard removal opening a path to invented training numbers | mitigate (HIGH — blocking) | The recency-windowed Tier A/B contract replaces the blanket guard IN THE SAME EDIT (never removed alone). No-data → "I don't have a recent X logged, Sir" + Tier-A target only; past-window → staleness caveat; beyond 3x → no-data. Task-3 human-verify SC-1 is the BLOCKING gate confirming no fabrication live before phase close. |
| T-22-12 | Tampering | autonomous/silent plan rewrite under the new critique posture | mitigate | Critique posture explicitly forbids silent rewrites; `update_plan`/`update_training_profile` fire only on explicit confirmation (D-12, honors PLAN-03). Task-3 SC-4 verifies the recommend-not-rewrite boundary. |
| T-22-13 | Spoofing (prompt injection) | coaching-guide content in the system prefix steering behavior | accept | Content is author-controlled and PR-reviewed; no untrusted runtime data is interpolated into the contract or guide. Low risk; documented. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (prompt-only edit). |
</threat_model>

<verification>
- `prompts/smart_agent.md` has `{coaching_guide}` before `{self_md}`; old blanket guard removed; recency contract + specificity bar + critique posture present (automated greps in tasks 1-2).
- BLOCKING human-verify Task 3: SC-1/SC-3/SC-4 pass live (no fabricated numbers; specific sessions; recommend-not-rewrite).
- Full prompt still renders cleanly via render_smart_system (covered by Plan 02 no-unresolved-placeholders test).
</verification>

<success_criteria>
- COACH-06: D-13 guard released under the data-presence contract; no invented numbers (SC-1 green).
- COACH-02: specificity bar enforced — session + load + rationale (SC-3 green).
- COACH-07: structural critique volunteered, recommend-not-rewrite boundary held (SC-4 green).
</success_criteria>

<output>
Create `.planning/phases/22-expert-coaching-knowledge-d-13-release/22-04-SUMMARY.md` when done.
</output>
