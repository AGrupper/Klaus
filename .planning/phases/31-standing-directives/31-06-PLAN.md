---
phase: 31-standing-directives
plan: 06
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03"]
files_modified:
  - core/reflection.py
  - prompts/reflection.md
  - core/nightly_review.py
  - prompts/nightly_review.md
  - tests/test_reflection.py
  - tests/test_nightly_review.py
autonomous: true
requirements: [DIR-02, DIR-06, DIR-07]
must_haves:
  truths:
    - "Nightly reflection reads the full 24h conversation via get_recent_window, not the empty 6h session window"
    - "Each Klaus-initiated outreach is paired with Amit's reaction (replied / ignored-topic / ignored)"
    - "A single strong signal (one pushback OR one ignore) can ground a self-directive proposal, active immediately"
    - "A vetoed proposal is kept (status vetoed) and never re-proposed"
    - "Event-based directive conditions are judged nightly and expired when ended; uncertain conditions stay active"
    - "Directive items (proposals, expiries, prune-flags, scope-questions) are woven into the nightly narrative, not a fixed section"
  artifacts:
    - path: "core/reflection.py"
      provides: "get_recent_window read + reaction-pairing input + directive-proposal/expiry/prune writes"
      contains: "get_recent_window"
    - path: "prompts/reflection.md"
      provides: "reaction-pairing task + directive_proposals/prune_flags/expiry_notes schema"
      contains: "directive_proposals"
    - path: "core/nightly_review.py"
      provides: "interim directive context + directive_items woven into _compose_nightly"
      contains: "standing_directives"
  key_links:
    - from: "_gather_day"
      to: "get_recent_window"
      via: "replaces conv_store.get(user_id)"
      pattern: "get_recent_window"
    - from: "run_reflection"
      to: "StandingDirectiveStore.add(origin='klaus_self')"
      via: "self-directive write, active immediately (D-09)"
      pattern: "klaus_self"
    - from: "reflection directive_items"
      to: "_compose_nightly"
      via: "woven into the nightly narrative (D-19/D-20)"
      pattern: "directive_items"
---

<objective>
Fix the reflection learning loop and its nightly announcement channel. Replace the stale 6h
conversation read with `get_recent_window(24h)`, pair each Klaus-initiated outreach with Amit's
reaction, let reflection propose self-directives (active immediately) and judge event-based
directive expiry, and weave directive items into the nightly narrative.

Purpose: This is the novel core of the phase (DIR-06/07) and fixes live bug "B3" (Pitfall 6 — the
reflection loop is otherwise structurally blind to the day's conversation). Klaus genuinely
self-corrects fast (single signal, ignore counts, active immediately, no cap — D-09..D-14), with
the nightly announcement + one-line veto + list/cancel as the entire control surface. Also
completes DIR-02's judged-condition expiry (D-05/D-08) and D-04's prune-flag accumulation guard.

Output: learning-loop rewrite in `core/reflection.py` + `prompts/reflection.md`, nightly weaving in
`core/nightly_review.py` + `prompts/nightly_review.md`, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standing-directives/31-CONTEXT.md
@.planning/phases/31-standing-directives/31-PATTERNS.md
@.planning/phases/31-standing-directives/31-01-SUMMARY.md
@.planning/phases/31-standing-directives/31-02-SUMMARY.md
@.planning/phases/31-standing-directives/31-03-SUMMARY.md

<interfaces>
<!-- From Plan 02: get_recent_window. From Plan 01: StandingDirectiveStore. From Plan 03: formatter. -->
FirestoreConversationStore.get_recent_window(user_id, hours=24, max_messages=60) -> list[dict].
StandingDirectiveStore(project_id, database): add(text, origin="klaus_self", context_quote=...,
  expires_at=..., condition_text=...), list_active(), expire(id), and (from Plan 03/D-13) a way to
  mark a proposal vetoed — reuse cancel()/a status write; vetoed proposals are kept (never re-proposed).
OutreachLogStore.get_today(date_str) -> list[dict]; each entry: {topic_key, time, draft, final, tick_index}
  (memory/firestore_db.py:1918-1936). Append is send-gated — entries are already-delivered-only.

core/reflection.py anchors (verified live):
- _gather_day at line 123; THE bug line 159: gathered["conversation"] = conv_store.get(user_id) or []
- _parse_reflection_json at line 51 (_REQUIRED_STR_KEYS + highlights isinstance-guard discipline —
  RE-READ IN FULL before extending; confirm it does not reject unknown keys, per RESEARCH Assumption A3)
- _brain_reflect at line 280; brain_input dict at ~line 393; user_message = json.dumps(brain_input) at 408
- run_reflection at line 357; isolated per-write-target try/except (JournalStore/Pinecone/SelfStateStore)
  at lines 428-489 — mirror this discipline for the new directive writes (non-fatal to the journal)

core/nightly_review.py anchors: _gather_tomorrow at line 165; _compose_nightly at line 223;
run_nightly at line 351; _ensure_reflection at line 87 (calls run_reflection). Nightly is EXEMPT
from veto (D-21) — it only injects directive CONTEXT and weaves directive_items into the narrative.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the stale read + assemble reaction-pairing input</name>
  <files>core/reflection.py, tests/test_reflection.py</files>
  <read_first>
    - core/reflection.py lines 123-197 (_gather_day incl. the bug at 159), 280-410 (_brain_reflect + brain_input assembly)
    - memory/firestore_db.py lines 1918-1936 (OutreachLogStore.get_today) + the entry schema at 1841-1867
    - tests/test_reflection.py (existing _gather_day / _brain_reflect test structure to extend)
    - .planning/phases/31-standing-directives/31-RESEARCH.md (§ Pitfall 6; § Code Examples reflection; § Open Questions #3 reflection-read-time limitation)
  </read_first>
  <action>
    Replace `gathered["conversation"] = conv_store.get(user_id) or []` (reflection.py:159) with
    `conv_store.get_recent_window(user_id, hours=24) or []` — THE single most concrete fix in this
    phase (Pitfall 6 / bug "B3"). In `run_reflection`/`_brain_reflect`'s input assembly, also read
    `OutreachLogStore.get_today(target_date)` and pass BOTH the 24h windowed conversation and the
    outreach entries into the `brain_input` JSON payload so the SAME existing brain-reflect call can
    classify each outreach's reaction per D-11: topic-engaging reply = "replied"; subject-changing
    reply = "ignored-topic"; no reply by reflection read-time = "ignored" (deterministic). Do NOT add a
    second LLM call. Add a plan comment noting the accepted once-per-night read-time limitation
    (RESEARCH Open Questions #3 — a reply landing after the organic trigger but before the 01:00
    backstop is a rare accepted edge case, not special-cased).
    Add tests: (a) seed a conversation doc whose session updated_at is >6h old but with in-24h
    messages → assert `_gather_day`'s conversation list is NON-EMPTY (current behavior returns []);
    (b) assert the brain_input payload includes both the windowed conversation and the outreach entries.
  </action>
  <verify>
    <automated>pytest tests/test_reflection.py -k "recent_window or gather_day or reaction" -x</automated>
  </verify>
  <acceptance_criteria>
    - core/reflection.py contains `get_recent_window(user_id, hours=24` and no longer calls `conv_store.get(user_id)` in _gather_day
    - core/reflection.py reads `OutreachLogStore.get_today` and includes both sources in brain_input
    - A test seeds a >6h-stale session doc and asserts _gather_day conversation is non-empty
    - `pytest tests/test_reflection.py -k "recent_window or gather_day or reaction" -x` exits 0
  </acceptance_criteria>
  <done>Reflection sees the full day's conversation and has the outreach entries needed to pair reactions.</done>
</task>

<task type="auto">
  <name>Task 2: Self-directive proposals, judged expiry, prune-flags (schema + writes)</name>
  <files>core/reflection.py, prompts/reflection.md, tests/test_reflection.py</files>
  <read_first>
    - core/reflection.py lines 51-116 (_parse_reflection_json — RE-READ IN FULL; extend, do not replace), 428-489 (the isolated per-write-target try/except discipline)
    - prompts/reflection.md lines 23-41 (the existing 5-key Output Format schema to extend)
    - .planning/phases/31-standing-directives/31-CONTEXT.md (D-04, D-05, D-08, D-09..D-14, D-19/D-20)
    - .planning/phases/31-standing-directives/31-PATTERNS.md (§ core/reflection.py — JSON-schema-extension + self-directive write)
  </read_first>
  <action>
    Extend `_parse_reflection_json` with three NEW OPTIONAL keys following the exact
    isinstance-guard-then-default-`[]` discipline used for `highlights` (never raise; default empty on
    missing/wrong-typed): `directive_proposals` (list of {text, condition_text?/expires_at?,
    rationale}), `prune_flags` (list of {directive_id, reason}), `expiry_notes` (list of
    {directive_id, reason}). Update `prompts/reflection.md`'s Output Format from "EXACTLY these 5 keys"
    to "these 5 required keys, plus 3 optional keys when applicable", documenting each new key's shape
    in the existing highlights-style prose, and add the reaction-pairing task description: propose
    `origin="klaus_self"` directives from a SINGLE strong signal — one pushback/frustration OR one
    ignored outreach (D-10/D-11/D-12; an ignore-grounded proposal may go as far as a full stop, form
    is per-case judgment); do NOT re-propose a directive matching a previously vetoed one (D-13); flag
    stale/contradicted active directives as prune_flags (D-04); for event-based conditions, judge
    against calendar+conversation whether the condition ended — expire if clearly ended, STAY ACTIVE if
    unsure (D-08). No cap on proposals per night (D-14).
    In `run_reflection`, after the brain call, apply the outputs using the SAME isolated-per-write
    try/except discipline as the existing JournalStore/Pinecone/SelfStateStore writes (each directive
    write is NON-FATAL to the journal write): for each proposal call
    `StandingDirectiveStore.add(origin="klaus_self", ...)` (D-09 active immediately); for each
    expiry_note call `store.expire(directive_id)` (D-05/D-08); prune_flags are carried forward as
    narrative items (surfaced by Task 3), not auto-actioned. Return/emit the assembled `directive_items`
    (proposals + expiries + prune_flags + scope-questions) so Task 3's nightly compose can weave them.
    Add tests: (a) a single ignored-outreach fixture yields a directive_proposal and a
    StandingDirectiveStore.add(origin="klaus_self") call; (b) a proposal matching an existing vetoed
    directive is NOT re-proposed (D-13); (c) an expiry_note triggers store.expire; (d) the 3 new keys
    default to [] when absent and _parse_reflection_json still returns the 5 required keys.
  </action>
  <verify>
    <automated>pytest tests/test_reflection.py -k "proposal or expiry or prune or veto or reflection_json" -x</automated>
  </verify>
  <acceptance_criteria>
    - core/reflection.py _parse_reflection_json handles `directive_proposals`, `prune_flags`, `expiry_notes` as optional [] defaults
    - core/reflection.py run_reflection calls `StandingDirectiveStore.add(origin="klaus_self"` inside an isolated try/except and calls `store.expire` for expiry_notes
    - prompts/reflection.md contains `directive_proposals` and no longer claims "EXACTLY these 5 keys"
    - A test asserts a single ignored/pushback signal grounds a proposal AND a vetoed directive is not re-proposed
    - `pytest tests/test_reflection.py -x` exits 0
  </acceptance_criteria>
  <done>Reflection proposes self-directives from a single signal (active immediately), judges expiry, flags prunes, and never re-proposes a vetoed one — all non-fatal to the journal write.</done>
</task>

<task type="auto">
  <name>Task 3: Weave directive items into the nightly narrative (nightly exempt from veto)</name>
  <files>core/nightly_review.py, prompts/nightly_review.md, tests/test_nightly_review.py</files>
  <read_first>
    - core/nightly_review.py lines 87-160 (_ensure_reflection → run_reflection handoff), 165-291 (_gather_tomorrow + _compose_nightly incl. payload build at ~251-267)
    - prompts/nightly_review.md (full — placeholder ordering; add a directive_items block consistent with the existing {coaching_guide}/{today_date} pattern)
    - .planning/phases/31-standing-directives/31-CONTEXT.md (D-19 weave-into-narrative, D-20 heavy-night triage, D-21 nightly EXEMPT)
    - .planning/phases/31-standing-directives/31-PATTERNS.md (§ core/nightly_review.py — interim injection + payload key)
  </read_first>
  <action>
    In `_gather_tomorrow`, add a best-effort `standing_directives` read
    (`StandingDirectiveStore.list_active()`, sentinel shape) for interim directive CONTEXT — this
    feeds the nightly narrative; the nightly is EXEMPT from veto (D-21) and must NEVER emit a skip
    verdict. Add a `directive_items` key to `_compose_nightly`'s payload carrying the reflection
    output from Task 2 (proposals with their one-line veto option, dated/judged expiry notes,
    prune-flags, and any D-15 scope-clarification questions). Update `prompts/nightly_review.md` to weave
    these into the narrative per D-19 (NO fixed "Directives:" section, NO separate message) and D-20
    (heavy-night triage: activations and expiries MUST always be stated because behavior is changing;
    prune-flags and scope-questions MAY wait a night if the message runs long). Each self-directive
    proposal must carry a one-line veto option (DIR-07). Wire the reflection→nightly handoff so the
    `directive_items` produced in Task 2 reach `_compose_nightly` (via the `_ensure_reflection` result
    or a shared read of the just-written reflection output — match whichever the existing handoff uses).
    Add tests: (a) a night with a proposal renders the proposal text + a veto option in the nightly
    payload/output; (b) an expiry note is always stated; (c) the nightly NEVER emits a skip verdict
    even when a covering directive is active (exemption); (d) no directive activity → no directive
    section clutter (narrative unchanged).
  </action>
  <verify>
    <automated>pytest tests/test_nightly_review.py -k "directive or veto or expiry" -x</automated>
  </verify>
  <acceptance_criteria>
    - core/nightly_review.py _gather_tomorrow reads `standing_directives` and _compose_nightly payload contains `directive_items`
    - prompts/nightly_review.md weaves directive items into the narrative (no fixed "Directives:" section) and states activations/expiries always
    - A test asserts a proposal renders with a one-line veto option and the nightly never emits a skip verdict
    - `pytest tests/test_nightly_review.py -x` exits 0
  </acceptance_criteria>
  <done>Directive proposals/expiries/prune-flags/scope-questions are woven into the nightly narrative with veto options; nightly stays exempt from veto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 24h conversation window → reflection brain call | Day's chat content (Anthropic backend) drives reaction classification |
| reaction classification (n=1) → immediate behavior change | A single misread reaction can swing next-day behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Tampering | Fast adaptation from a single ambiguous signal (delayed reply misread as ignored) swings behavior | accept | Locked user decision (D-10/D-11 — Amit chose adaptation speed); the safety net is D-13 (vetoed = durable anti-lesson, never re-proposed) + D-17/D-18 (list/cancel). Do NOT reintroduce a multi-day consistency guard (Pitfall 5) |
| T-31-10 | Denial of Service | A directive-write failure aborts the journal write | mitigate | Each directive write is in its own try/except, non-fatal to JournalStore/Pinecone/SelfStateStore writes (mirrors the existing per-write-target discipline) |
| T-31-03 | Information Disclosure | 24h window may contain a pasted secret forwarded to the reflection brain | accept | Anthropic backend (not Groq); milestone-accepted residual per RESEARCH.md Security Domain |
| T-31-SC | Tampering | Package installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `pytest tests/test_reflection.py tests/test_nightly_review.py -x` green.
- Live-behavior check: seed a >6h-stale session doc; `_gather_day` conversation is non-empty (proves bug "B3" fixed).
- Grep confirms `_gather_day` no longer calls `conv_store.get(user_id)`.
</verification>

<success_criteria>
- DIR-06: reflection reads the 24h window and pairs each outreach with a reaction (replied/ignored-topic/ignored).
- DIR-07: reflection proposes self-directives from a single signal, active immediately, surfaced with a one-line veto in the woven nightly narrative; vetoed proposals are never re-proposed.
- DIR-02 judged-condition expiry (D-05/D-08) and D-04 prune-flags are live; all directive writes are non-fatal to the journal.
</success_criteria>

<output>
Create `.planning/phases/31-standing-directives/31-06-SUMMARY.md` when done.
</output>
