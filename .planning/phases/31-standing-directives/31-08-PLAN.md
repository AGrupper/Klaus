---
phase: 31-standing-directives
plan: 08
type: execute
gap_closure: true
wave: 2
depends_on: [31-07]
files_modified:
  - memory/firestore_db.py
  - core/tools.py
  - tests/test_firestore_db.py
  - tests/test_tools.py
  - tests/test_reflection.py
autonomous: true
requirements: [DIR-07]

must_haves:
  truths:
    - "A klaus_self self-directive rejected via cancel_standing_directive is persisted status='vetoed' (durable anti-lesson), not 'cancelled' — DIR-07/D-13"
    - "A directive vetoed via the real reject path is NOT re-proposed on the next reflection run — the D-13 anti-lesson guard actually fires"
  artifacts:
    - path: "memory/firestore_db.py"
      provides: "StandingDirectiveStore.veto(did) writes status='vetoed' (get-then-update, cache-invalidate, never hard-delete); a single-doc read (get(did)) exposes origin for routing"
      contains: "vetoed"
    - path: "core/tools.py"
      provides: "_handle_cancel_standing_directive routes origin=='klaus_self' directives to store.veto(); user_chat directives still cancel()"
      contains: "veto"
  key_links:
    - from: "memory/firestore_db.py:StandingDirectiveStore.veto"
      to: "core/reflection.py vetoed_texts guard (status=='vetoed')"
      via: "veto() is the writer the reflection guard reads back"
      pattern: "\"vetoed\""
    - from: "core/tools.py:_handle_cancel_standing_directive"
      to: "memory/firestore_db.py:StandingDirectiveStore.veto"
      via: "origin=='klaus_self' → veto(id)"
      pattern: "veto\\("
---

<objective>
Close verification gap 2 (DIR-07 / D-13): make the reflection anti-lesson guard REACHABLE.
`core/reflection.py` already filters `status=="vetoed"` to skip re-proposing rejected
self-directives — but NO production path writes `"vetoed"`. The only undo surface,
`cancel_standing_directive`, writes `"cancelled"`, so `vetoed_texts` stays empty forever and a
`klaus_self` directive Amit explicitly rejected can be re-proposed verbatim (active immediately)
on the next reflection run — defeating the D-10 single-signal safety net. The green test
`test_reflection_vetoed_directive_is_not_re_proposed` seeds a `status="vetoed"` doc no code can
create, masking the real cancel→re-propose scenario.

Purpose: D-13 LOCKS "Veto = durable anti-lesson — vetoed proposals are kept (status `vetoed`,
never hard-deleted) and reflection must not re-propose the same or near-same directive." Honor it
by giving `vetoed` a real writer reached through the chat reject path the verification names
(`cancel_standing_directive`).

Output: a `StandingDirectiveStore.veto()` writer, origin-aware routing in the cancel handler
(rejecting a `klaus_self` directive durably vetoes it), and a test that exercises the REAL
reject→persist→no-re-propose path end to end (not a hand-seeded status). `core/reflection.py`'s
guard is intentionally left unchanged — it already reads `status=="vetoed"`; this plan supplies the
writer it was waiting for.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-standing-directives/31-CONTEXT.md
@.planning/phases/31-standing-directives/31-VERIFICATION.md
@.planning/phases/31-standing-directives/31-06-SUMMARY.md
@.planning/phases/31-standing-directives/31-REVIEW.md

<interfaces>
<!-- Existing code — modify/extend, do not re-explore. -->

memory/firestore_db.py::StandingDirectiveStore (lines 1841-2051):
  status enum already documents 'vetoed' (line 1850) — schema is ready, only the writer is missing.
  cancel(did) -> bool     # writes status='cancelled'  (get-then-update, cache-invalidate, re-raise-on-error)
  expire(did) -> bool     # writes status='expired'    (same shape)
  supersede(old_id, new_directive_id) -> bool  # writes status='superseded' + superseded_by
  # NO get(did) single-doc read exists yet; list_active()/list_all() are the only reads.
  add(text, origin="user_chat", ...) -> dict   # origin distinguishes user_chat vs klaus_self

core/tools.py::_handle_cancel_standing_directive(id) -> str — lines 2247-2261
  # currently: builds store, calls store.cancel(id), returns json.dumps({"ok": bool(ok)})
  cancel_standing_directive schema — lines 914-928

core/reflection.py (DO NOT MODIFY — guard already correct) lines 511-531:
  vetoed_texts = { str(d.get("text","")).strip().lower()
                   for d in directive_store.list_all() if d.get("status") == "vetoed" }
  # proposals whose lowercased text is in vetoed_texts are skipped. This fires ONCE 'vetoed' has a writer.

tests/test_reflection.py:
  test_reflection_vetoed_directive_is_not_re_proposed (lines ~650-683) — the current test SEEDS an
  unreachable status='vetoed' doc. The new test must instead produce the vetoed state via the real
  reject path and then assert no-re-propose.
</interfaces>

**Environment (executors):** run pytest via `.venv/bin/python -m pytest` (Python 3.13 venv;
system Python 3.14 segfaults on grpc/protobuf; run per-file — full-suite one-process runs have a known
grpc teardown artifact). Full suite green baseline: 2013 passed / 3 skipped.
Invariants (CLAUDE.md): NEVER hard-delete a directive — veto is a status transition; Firestore names lowercase.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add StandingDirectiveStore.veto() writer + get() single-doc read</name>
  <files>memory/firestore_db.py, tests/test_firestore_db.py</files>
  <read_first>
    - memory/firestore_db.py lines 1841-2051 (StandingDirectiveStore — add/list_active/list_all/cancel/supersede/expire; the get-then-update+cache-invalidate write shape to copy)
    - tests/test_firestore_db.py — TestStandingDirectiveStore (the analog test class for cancel/supersede/expire)
    - .planning/phases/31-standing-directives/31-CONTEXT.md D-13 (veto = durable anti-lesson, status 'vetoed', never hard-delete)
  </read_first>
  <behavior>
    - Test: veto(did) on an existing directive flips status to 'vetoed' (never .delete), returns True, and invalidates the ("standing_directives",) cache prefix.
    - Test: veto(did) on a non-existent id returns False (does not raise).
    - Test: get(did) returns the stored doc dict (jsonsafe) for an existing id, and None for a missing id, never raising.
    - Test: after veto(), list_all() contains the doc with status='vetoed' (so reflection's list_all()-based guard can read it).
  </behavior>
  <action>
    Add `veto(did: str) -> bool` to StandingDirectiveStore, modeled EXACTLY on cancel()/expire()
    (get snapshot → if not exists return False → update({"status": "vetoed"}) → re-raise on other errors
    → _cache_invalidate_prefix(("standing_directives",)) → return True). Never hard-delete. Add a
    `get(did: str) -> dict | None` never-raises single-doc read (document(did).get(); return
    _jsonsafe_doc(snap.to_dict()) if exists else None; return None on Firestore error) so the cancel
    handler in Task 2 can read `origin` cheaply without a full-collection scan. Extend
    TestStandingDirectiveStore in tests/test_firestore_db.py with the behavior tests above.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_firestore_db.py -k "veto or standing" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def veto" memory/firestore_db.py` and `grep -n "def get" memory/firestore_db.py` (within StandingDirectiveStore) both present.
    - veto() writes status='vetoed' via update() (no `.delete(` in the method), invalidates the cache prefix, returns True on existing / False on missing.
    - get() returns the doc dict for an existing id and None for a missing id without raising.
    - `.venv/bin/python -m pytest tests/test_firestore_db.py -k "veto or standing" -q` exits 0.
  </acceptance_criteria>
  <done>StandingDirectiveStore has a real status='vetoed' writer (the reflection guard's missing dependency) plus a cheap origin lookup, both test-covered and never hard-deleting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route cancel_standing_directive of a klaus_self directive to veto()</name>
  <files>core/tools.py, tests/test_tools.py</files>
  <read_first>
    - core/tools.py lines 914-928 (cancel_standing_directive schema) and 2247-2261 (_handle_cancel_standing_directive)
    - memory/firestore_db.py StandingDirectiveStore.veto()/get() (from Task 1)
    - tests/test_tools.py — _FakeStandingDirectiveStore + fake_directive_store fixture + TestStandingDirectiveTools
    - .planning/phases/31-standing-directives/31-VERIFICATION.md gap 2 (the spec) and 31-REVIEW.md CR-01
  </read_first>
  <behavior>
    - Test: _handle_cancel_standing_directive(id) on a directive whose origin=='klaus_self' calls store.veto(id) (status→'vetoed'), NOT store.cancel(id).
    - Test: _handle_cancel_standing_directive(id) on a directive whose origin=='user_chat' calls store.cancel(id) (status→'cancelled') — Amit cancelling his own directive is not an anti-lesson.
    - Test: a non-existent id returns {"ok": false} and does not raise.
    - Both branches remain idempotent and return {"ok": bool}.
  </behavior>
  <action>
    In `_handle_cancel_standing_directive`, before deciding the write, look up the directive via
    `store.get(id)`. If the doc exists and its `origin == "klaus_self"`, call `store.veto(id)` (D-13:
    rejecting a self-proposed directive durably records it as an anti-lesson so reflection never
    re-proposes it); otherwise call `store.cancel(id)` (user's own directives cancel normally). If
    get() returns None, return {"ok": False}. Preserve the idempotent {"ok": bool} return contract.
    Update the cancel_standing_directive schema description to note that rejecting a directive Klaus
    proposed himself (origin klaus_self) durably vetoes it so it will not be proposed again — keep the
    existing "Call this directly — do NOT delegate to the worker." phrasing. Extend
    _FakeStandingDirectiveStore with a veto() method + origin-carrying get() and add the tests above to
    TestStandingDirectiveTools.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tools.py -k standing -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_handle_cancel_standing_directive` calls store.veto(id) when the fetched directive's origin=='klaus_self', and store.cancel(id) otherwise.
    - `grep -n "veto" core/tools.py` shows the routing in the cancel handler.
    - A missing id returns {"ok": false} without raising; both branches stay idempotent.
    - `.venv/bin/python -m pytest tests/test_tools.py -k standing -q` exits 0.
  </acceptance_criteria>
  <done>The chat reject path the verification names (cancel_standing_directive) now writes status='vetoed' for self-proposed directives, feeding the reflection anti-lesson guard.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the seeded-status reflection test with a real reject→no-re-propose test</name>
  <files>tests/test_reflection.py</files>
  <read_first>
    - tests/test_reflection.py test_reflection_vetoed_directive_is_not_re_proposed (~lines 650-683) — the current test that seeds an unreachable status='vetoed' doc
    - core/reflection.py lines 495-574 (run_reflection's directive-proposal processing + the status=='vetoed' guard at 511-531)
    - core/tools.py _handle_cancel_standing_directive (Task 2 routing) and memory/firestore_db.py veto() (Task 1)
    - .planning/phases/31-standing-directives/31-VERIFICATION.md gap 2 second `missing` bullet
  </read_first>
  <action>
    Add a test that exercises the REAL production path end to end (do NOT hand-seed status='vetoed').
    Steps: (1) create a klaus_self directive in the (mock/fake) store via the real add(origin='klaus_self')
    path; (2) reject it through the real reject surface — call `_handle_cancel_standing_directive(id)` (or,
    if the harness can't reach the tool handler, `store.veto(id)` — but prefer the handler so origin-routing
    is proven), leaving the doc at status='vetoed'; (3) drive reflection's proposal processing with a
    directive_proposals entry whose text matches the vetoed directive (same as the current test's shape);
    (4) assert the matching proposal is SKIPPED — store.add is NOT called again for that text (no new active
    klaus_self directive is created). Keep/repurpose the existing test name or add a clearly-named new test;
    if the old seeded-status test is now redundant, update it to use the real path rather than leaving a test
    that passes on an unreachable state. Reuse the existing reflection test harness (mock StandingDirectiveStore
    / monkeypatched brain result) already in the file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_reflection.py -k "vetoed or re_propose" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The vetoed state in the test is produced by the real veto/reject path (via veto() writing 'vetoed'), NOT by hand-seeding a status='vetoed' doc.
    - The test asserts a proposal matching a vetoed directive's text is not re-added on the reflection run (add not called for that text).
    - `.venv/bin/python -m pytest tests/test_reflection.py -k "vetoed or re_propose" -q` exits 0.
    - No production change to core/reflection.py's guard is required (guard already reads status=='vetoed').
  </acceptance_criteria>
  <done>The reflection anti-lesson is proven end to end: reject a self-directive via the real path, and it is not re-proposed next run — the D-13 durable anti-lesson is live, not dead code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Amit chat reject → cancel_standing_directive | Amit's rejection of a self-proposal becomes durable adaptation signal |
| reflection loop → StandingDirectiveStore.add(klaus_self) | single-signal self-directive activation (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08-01 | Repudiation | vetoed directive lost / re-proposed | mitigate | veto() writes a durable status transition (never hard-delete, D-13); reflection guard reads it back via list_all(); Task 3 proves no-re-propose end to end. |
| T-31-08-02 | Tampering | user_chat cancel mis-routed to veto | mitigate | Routing keys strictly on origin=='klaus_self' from store.get(); user_chat directives still cancel(). Test 2 asserts both branches. |
| T-31-08-03 | Denial of Service | Firestore get() failure in cancel handler | accept | get() never raises (returns None → {"ok": False}); consistent with the store's fail-open read discipline. |
</threat_model>

<verification>
- `grep -rn '"vetoed"' core/ memory/` now shows a real WRITER (memory/firestore_db.py veto()) in addition to the reflection read comparison — the gap's "dead guard / no writer" finding is resolved.
- `.venv/bin/python -m pytest tests/test_firestore_db.py -q` and `.venv/bin/python -m pytest tests/test_tools.py -q` and `.venv/bin/python -m pytest tests/test_reflection.py -q` each exit 0 (per-file, per the repo's grpc test-isolation note).
</verification>

<success_criteria>
- Rejecting a klaus_self directive via cancel_standing_directive persists status='vetoed' (durable anti-lesson), closing verification gap 2 / DIR-07 / D-13.
- The reflection guard (status=='vetoed') now has a real production writer and fires — a vetoed self-directive is not re-proposed on the next run, proven by a test exercising the real reject path (not a seeded status).
- Amit cancelling his own (user_chat) directive still writes 'cancelled' — veto semantics apply only to self-proposals.
</success_criteria>

<output>
Create `.planning/phases/31-standing-directives/31-08-SUMMARY.md` when done.
</output>
