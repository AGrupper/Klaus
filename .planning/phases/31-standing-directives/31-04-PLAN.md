---
phase: 31-standing-directives
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-03"]
files_modified:
  - core/autonomous.py
  - prompts/autonomous_triage.md
  - tests/test_autonomous.py
autonomous: true
requirements: [DIR-03]
must_haves:
  truths:
    - "Active standing directives reach the tick triage prompt as a Step-0 STANDING ORDERS veto above all other logic"
    - "A directive vetoes only topics its stated scope plausibly covers (topic-scoped, not blanket)"
    - "Active directives reach Layer-2 compose and follow-up compose"
    - "The new standing_directives gather never flips _is_empty_signals from empty to non-empty"
    - "Triage reasoning names which directive it applied and why"
  artifacts:
    - path: "core/autonomous.py"
      provides: "_gather_standing_directives job + directives block wired into triage + both composes"
      contains: "def _gather_standing_directives"
    - path: "prompts/autonomous_triage.md"
      provides: "Step-0 STANDING ORDERS veto instruction"
      contains: "STANDING ORDERS"
  key_links:
    - from: "gather_situation jobs dict"
      to: "_gather_standing_directives"
      via: "standing_directives job entry"
      pattern: "standing_directives"
    - from: "_build_triage_prompt"
      to: "render_standing_directives_block"
      via: "lazy import of the shared formatter"
      pattern: "render_standing_directives_block"
    - from: "_is_empty_signals"
      to: "standing_directives"
      via: "deliberate absence (documented comment, context-only)"
      pattern: "standing_directives is CONTEXT only"
---

<objective>
Inject standing directives into the autonomous engine's three reasoning surfaces: the tick triage
prompt (as a Step-0 STANDING ORDERS veto above all other logic), Layer-2 compose, and follow-up
compose. Add the `_gather_standing_directives` context-only gather job.

Purpose: Realizes the tick/compose sites of DIR-03. This is where a directive like "stop nagging
about training while I'm in France" actually silences the relevant proactive outreach — but only
the relevant outreach (topic-scoped veto, Pitfall 5), never as a blanket gate. The gather must be
context-only so it never defeats the free-tier cost gate that is Klaus's entire cost model
(Pitfall 4).

Output: `_gather_standing_directives` + triage/compose wiring in `core/autonomous.py`, the Step-0
veto in `prompts/autonomous_triage.md`, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standing-directives/31-CONTEXT.md
@.planning/phases/31-standing-directives/31-PATTERNS.md
@.planning/phases/31-standing-directives/31-01-SUMMARY.md
@.planning/phases/31-standing-directives/31-03-SUMMARY.md

<interfaces>
<!-- From Plan 01: StandingDirectiveStore.list_active(). From Plan 03: the shared formatter. -->
render_standing_directives_block(directives, *, style="prose"|"json") — import lazily inside each
function (`from core.tools import render_standing_directives_block`) to avoid an import cycle.
StandingDirectiveStore(project_id, database).list_active() -> list[dict].

core/autonomous.py anchors (verified live):
- _gather_due_followups at line 320 (the gather-job template: try/except → [] sentinel)
- _is_empty_signals at line 175 (the empty-gate; training_status/acwr are context-only precedent)
- gather_situation jobs dict at ~lines 588-615 (fanned out via ThreadPoolExecutor)
- _build_triage_prompt at line 686 (builds the JSON snapshot + prose blocks)
- _compose_layer2 at line 839, _compose_followup_layer2 at line 899 (both call
  orchestrator.render_smart_system — so the chat {standing_directives} placeholder from Plan 03 is
  ALREADY picked up for the system prompt of these two; add the snapshot key for parity)
- _format_now_block at line 669 (the "one helper, three call sites" precedent)

prompts/autonomous_triage.md: "Decision procedure" header with existing "Step 1 — vetoes" (~lines
163-168) — insert a new Step 0 immediately above it; Inputs block (~lines 43-60).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Context-only _gather_standing_directives job (never a trigger)</name>
  <files>core/autonomous.py, tests/test_autonomous.py</files>
  <read_first>
    - core/autonomous.py lines 320-328 (_gather_due_followups), 175-220 (_is_empty_signals + the training_status/acwr context-only comment block), 555-626 (gather_situation jobs dict + empty assignment)
    - tests/test_autonomous.py lines 341-445 (_is_empty_signals + due_followups fixtures to mirror)
    - .planning/phases/31-standing-directives/31-PATTERNS.md (§ core/autonomous.py — gather job + empty-signal exclusion; Pitfall 4)
  </read_first>
  <action>
    Add `_gather_standing_directives(project_id, database) -> list` following the
    `_gather_due_followups` try/except → `[]` sentinel shape (lazy `from memory.firestore_db import
    StandingDirectiveStore`, return `sds.list_active()`, `[]` on Exception). Register it in
    `gather_situation`'s jobs dict as `"standing_directives": lambda: _gather_standing_directives(
    project_id, database)`. CRITICAL (Pitfall 4): do NOT add any check for `standing_directives` to
    `_is_empty_signals` — its mere presence must NEVER flip empty=False. Add an explicit code comment
    at the same location as the existing training_status/acwr context-only note, documenting that
    `standing_directives` is deliberately absent from the empty gate (context-only, mirrors
    training_status/acwr). Add tests: (a) an otherwise-empty situation that contains active directives
    still returns `_is_empty_signals == True`; (b) `_gather_standing_directives` returns `[]` on store
    failure without raising; (c) the job key appears in the assembled situation dict.
  </action>
  <verify>
    <automated>pytest tests/test_autonomous.py -k "standing_directive or empty_signal" -x</automated>
  </verify>
  <acceptance_criteria>
    - core/autonomous.py contains `def _gather_standing_directives`
    - core/autonomous.py gather_situation jobs dict contains a `standing_directives` entry
    - core/autonomous.py contains a comment stating standing_directives is CONTEXT only / not a trigger, at the _is_empty_signals site
    - A test asserts a situation with active directives but no other signals is still empty (empty=True)
    - `pytest tests/test_autonomous.py -k "standing_directive or empty_signal" -x` exits 0
  </acceptance_criteria>
  <done>Directives are gathered as context and provably cannot wake the free tier on their own.</done>
</task>

<task type="auto">
  <name>Task 2: Step-0 STANDING ORDERS veto + Layer-2/follow-up compose injection</name>
  <files>core/autonomous.py, prompts/autonomous_triage.md, tests/test_autonomous.py</files>
  <read_first>
    - core/autonomous.py lines 686-737 (_build_triage_prompt snapshot + prose blocks), 839-939 (_compose_layer2 / _compose_followup_layer2 — the inline snap dicts at ~870-887 and ~922-928)
    - prompts/autonomous_triage.md (full file — the "Decision procedure" section and Inputs block)
    - .planning/phases/31-standing-directives/31-RESEARCH.md (§ Code Examples "Step-0 STANDING ORDERS veto insertion"; § Pitfall 1 topic-scoping)
    - .planning/phases/31-standing-directives/31-PATTERNS.md (§ core/autonomous.py wiring points)
  </read_first>
  <action>
    In `prompts/autonomous_triage.md`, add a `Step 0 — standing orders` block immediately above the
    existing `Step 1 — vetoes` in the Decision procedure. It must instruct: before anything else,
    check the active standing directives block; a directive vetoes THIS tick's topic ONLY if its
    stated scope plausibly covers what is about to be raised (co-occurring in time is NOT enough —
    topic-scoped, not blanket, per Pitfall 1); if a directive covers the topic, do not act and record
    which directive was applied and why in the reasoning; if genuinely unsure whether scope covers the
    topic, suppress for now but flag the ambiguity so tonight's reflection can ask Amit to clarify
    scope (D-15). Add a rendered "Active standing directives:" line to the triage Inputs block.
    In `core/autonomous.py::_build_triage_prompt`, lazily import `render_standing_directives_block`,
    add a `"standing_directives"` key to the JSON snapshot (style="json") AND append a prose
    "Active standing directives:\n{block}" section to the returned prompt (style="prose"). In
    `_compose_layer2` and `_compose_followup_layer2`, add a `"standing_directives"` key to their inline
    snap dicts for parity (the system-prompt injection for these two already flows automatically
    through render_smart_system per Plan 03 — no separate system-prompt wiring needed).
    Add tests: (a) the triage prompt contains the directive text when a directive is active; (b) the
    triage prompt omits the block when there are no active directives; (c) _compose_layer2 and
    _compose_followup_layer2 include a standing_directives key in their snapshot.
  </action>
  <verify>
    <automated>pytest tests/test_autonomous.py -k "standing_directive or triage or compose" -x</automated>
  </verify>
  <acceptance_criteria>
    - prompts/autonomous_triage.md contains `Step 0` and `STANDING ORDERS` and a topic-scope instruction ("stated scope plausibly covers")
    - core/autonomous.py _build_triage_prompt references `render_standing_directives_block` and adds a `standing_directives` snapshot key
    - core/autonomous.py _compose_layer2 and _compose_followup_layer2 snap dicts contain `standing_directives`
    - A test asserts the triage prompt contains directive text when active and omits it when none
    - `pytest tests/test_autonomous.py -x` exits 0 (no regression across the 1808-line suite file)
  </acceptance_criteria>
  <done>Directives reach triage (topic-scoped Step-0 veto), Layer-2 compose, and follow-up compose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored directive → free-tier tick cost gate | A directive is context for judgment, not a reason to spend the paid tier |
| Groq per-request token budget | The rendered triage prompt + max_tokens must stay within the 8K TPM/request admission budget |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Denial of Service | New standing_directives gather flips _is_empty_signals, spending Groq/Sonnet budget on formerly-free ticks | mitigate | Explicitly exclude standing_directives from _is_empty_signals with a documented comment (Pitfall 4); test asserts an active-directive-only situation stays empty |
| T-31-07 | Tampering | Blanket veto over-suppresses unrelated proactive speech for a directive's full duration | mitigate | Step-0 veto is topic-scoped ("stated scope plausibly covers"), records which directive applied; D-15 scope-uncertainty queues a nightly clarification instead of blanket silence (Pitfall 1) |
| T-31-BUDGET | Denial of Service | Rendered triage prompt grows past Groq per-request budget | accept | Formal token-budget guard test is Phase 32 MEM-05; this phase keeps the directives block terse (json style in the snapshot) and does not blow the budget |
| T-31-SC | Tampering | Package installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `pytest tests/test_autonomous.py -x` green.
- Grep confirms `_is_empty_signals` contains no `standing_directives` check and carries the context-only comment.
</verification>

<success_criteria>
- The tick/compose sites of DIR-03 are live: Step-0 topic-scoped veto in triage, directives in both composes.
- The new gather is provably context-only (cannot wake the free tier).
</success_criteria>

<output>
Create `.planning/phases/31-standing-directives/31-04-SUMMARY.md` when done.
</output>
