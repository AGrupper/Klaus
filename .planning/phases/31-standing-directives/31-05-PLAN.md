---
phase: 31-standing-directives
plan: 05
type: execute
wave: 3
depends_on: ["31-01", "31-03"]
files_modified:
  - core/morning_briefing.py
  - prompts/morning_briefing.md
  - core/weekly_training_review.py
  - prompts/weekly_training_review.md
  - tests/test_morning_briefing.py
  - tests/test_weekly_training_review.py
autonomous: true
requirements: [DIR-03]
must_haves:
  truths:
    - "The morning briefing and weekly review each see active standing directives at compose time"
    - "A directive whose scope covers the cron can make that composer emit a skip verdict instead of a message"
    - "A directive-skipped send is logged as skipped_by_directive — distinguishable from an infra failure"
    - "A directive-skipped morning briefing does NOT write the structured snapshot / daily_note (hub /api/today falls back to its existing placeholder)"
  artifacts:
    - path: "core/morning_briefing.py"
      provides: "directive gather + skip-verdict parse + skipped_by_directive path"
      contains: "skipped_by_directive"
    - path: "core/weekly_training_review.py"
      provides: "directive gather + skip-verdict parse"
      contains: "standing_directives"
  key_links:
    - from: "run_morning_briefing"
      to: "_parse_briefing_skip"
      via: "fenced-JSON skip verdict after _compose_briefing"
      pattern: "_parse_briefing_skip"
    - from: "directive-skip"
      to: "hub /api/today contract"
      via: "no structured/daily_note write on skip"
      pattern: "skipped_by_directive"
---

<objective>
Give standing directives interim veto power over the two legacy proactive crons that are safe to
silence — the morning briefing and the Sunday weekly review — until Phase 33's unified cascade
replaces this machinery. Each composer's own LLM call may output a skip verdict instead of a
message when an active directive's scope covers it (D-21/D-22). The nightly review is EXEMPT
(handled in Plan 06 — it is the veto/announcement channel).

Purpose: A directive now has real teeth over legacy surfaces immediately, not just chat. Realizes
the interim-cron site of DIR-03. Skips are logged distinctly (`skipped_by_directive`) so silence
stays distinguishable from failure.

Output: directive gather + skip-verdict parsing + prompt injection in `core/morning_briefing.py`
and `core/weekly_training_review.py`, with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standing-directives/31-CONTEXT.md
@.planning/phases/31-standing-directives/31-PATTERNS.md
@.planning/phases/31-standing-directives/31-01-SUMMARY.md
@.planning/phases/31-standing-directives/31-03-SUMMARY.md

<interfaces>
<!-- From Plan 01: StandingDirectiveStore.list_active(). From Plan 03: the shared formatter. -->
render_standing_directives_block(directives, *, style="prose") — lazy import to format the block.
StandingDirectiveStore(project_id, database).list_active() -> list[dict].

The fenced-JSON-trailer precedent (reuse this EXACT convention for the skip verdict, do not invent
a new sentinel): core/autonomous.py:799-831 `_parse_followup_action` searches a trailing
```json {"action": ...}``` block, parses it out, returns (verdict, polished_message).

core/morning_briefing.py anchors (verified live):
- run_morning_briefing at line 122; text = _compose_briefing(today_data, today_iso) at line 137;
  send_and_inject at line 141
- _set_state at line 46; the structured/daily_note write block at lines 161-197
- _gather_data at ~line 265; _compose_briefing at line 471

core/weekly_training_review.py anchors (verified live):
- _gather_week_data at line 43; _compose_review at line 372; run_weekly_review at line 487;
  send_and_inject at lines 515-522
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Morning briefing directive veto (D-21/D-22) + skipped_by_directive</name>
  <files>core/morning_briefing.py, prompts/morning_briefing.md, tests/test_morning_briefing.py</files>
  <read_first>
    - core/morning_briefing.py lines 122-197 (run_morning_briefing incl. send + the structured/daily_note write block), 265-330 (_gather_data), 471+ (_compose_briefing)
    - core/autonomous.py lines 799-831 (_parse_followup_action — the fenced-JSON-trailer pattern to mirror)
    - prompts/morning_briefing.md (full — find the placeholder/section ordering to add a directives block + skip instruction)
    - tests/test_morning_briefing.py (existing tests — mirror their compose/state-mock style)
    - .planning/phases/31-standing-directives/31-RESEARCH.md (§ Open Questions #2 — do NOT write structured/daily_note on a directive skip; § Pitfall — hub contract)
  </read_first>
  <action>
    In `_gather_data`, add a best-effort directive read (`data["standing_directives"] =
    StandingDirectiveStore(...).list_active()`) using the try/except-per-source sentinel shape already
    in that function. Inject the rendered directives block into `prompts/morning_briefing.md` with an
    instruction (D-22) that the compose MAY output a fenced JSON trailer of the form {"skip": true, "reason": "..."}
    instead of a briefing when an active directive's scope covers the morning briefing.
    Add `_parse_briefing_skip(text) -> tuple[bool, str, str]` modeled EXACTLY on `_parse_followup_action`
    (search the trailing fenced-JSON block for `{"skip": true, "reason": "..."}`, return
    (skip_bool, reason, polished_text); default to (False, "", text) when absent/malformed).
    Wire it into `run_morning_briefing` immediately after `text = _compose_briefing(...)` (line 137):
    if skip is true — log `skipped_by_directive` (distinct from a send failure; use the existing
    logger + a _set_state status distinct from "sent"/"failed", e.g. "skipped_by_directive"),
    RETURN without calling `send_and_inject`, WITHOUT the structured write and WITHOUT the
    daily_note/daily_note_date write (per RESEARCH Open Questions #2 — keep the hub /api/today falling
    back to its existing "coach note coming" placeholder rather than surfacing stale structured data).
    Add tests: (a) a covering directive produces a skip → send_and_inject NOT called, status
    "skipped_by_directive", no structured/daily_note write; (b) a non-covering / no directive → normal
    send + normal structured/daily_note write; (c) _parse_briefing_skip parses the fenced trailer and
    tolerates its absence.
  </action>
  <verify>
    <automated>pytest tests/test_morning_briefing.py -k "directive or skip" -x</automated>
  </verify>
  <acceptance_criteria>
    - core/morning_briefing.py contains `def _parse_briefing_skip` and the string `skipped_by_directive`
    - core/morning_briefing.py _gather_data reads `standing_directives`
    - prompts/morning_briefing.md contains a directives block and a `skip` verdict instruction
    - A test asserts a directive-skip path does NOT call send_and_inject and does NOT write structured/daily_note
    - `pytest tests/test_morning_briefing.py -x` exits 0
  </acceptance_criteria>
  <done>An active covering directive silences the morning briefing, logged distinctly, hub contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: Weekly review directive veto (D-21/D-22)</name>
  <files>core/weekly_training_review.py, prompts/weekly_training_review.md, tests/test_weekly_training_review.py</files>
  <read_first>
    - core/weekly_training_review.py lines 43-120 (_gather_week_data), 372-460 (_compose_review), 487-522 (run_weekly_review incl. send_and_inject)
    - core/autonomous.py lines 799-831 (_parse_followup_action)
    - prompts/weekly_training_review.md (full — section ordering for the directives block + skip instruction)
    - tests/test_weekly_training_review.py (existing tests — mirror the compose/send-mock style)
  </read_first>
  <action>
    In `_gather_week_data`, add a best-effort `standing_directives` read via
    `StandingDirectiveStore.list_active()` (same sentinel shape). Inject the rendered directives block
    into `prompts/weekly_training_review.md` with the same D-22 skip instruction (the composer MAY emit
    a fenced JSON trailer of the form {"skip": true, "reason": "..."} when a directive's scope covers the
    weekly review). Add a skip-verdict parse (reuse the same fenced-JSON convention — either import a
    shared helper or add a local `_parse_review_skip` mirroring `_parse_followup_action`). Wire it into
    `run_weekly_review` after `_compose_review(...)`: on skip, log `skipped_by_directive` and RETURN
    without calling `send_and_inject`. (No structured/daily_note hub contract applies to the weekly
    review — a plain skip-and-log is sufficient.) Preserve the existing D-24 send/retry discipline for
    the non-skip path unchanged. Also update the module docstring at core/weekly_training_review.py
    line 12 ("send_and_inject — always sends (D-24)") to note the D-21/D-22 exception — a covering
    standing directive now skips the send (logged skipped_by_directive).
    Add tests: (a) a covering directive → skip, send_and_inject NOT called, logged skipped_by_directive;
    (b) no/non-covering directive → normal send path unchanged.
  </action>
  <verify>
    <automated>pytest tests/test_weekly_training_review.py -k "directive or skip" -x</automated>
  </verify>
  <acceptance_criteria>
    - core/weekly_training_review.py _gather_week_data reads `standing_directives`
    - core/weekly_training_review.py contains a fenced-JSON skip parse and the string `skipped_by_directive`
    - prompts/weekly_training_review.md contains a directives block and a `skip` verdict instruction
    - A test asserts a directive-skip path does NOT call send_and_inject
    - core/weekly_training_review.py line-12 docstring no longer claims send_and_inject "always sends" without the directive-skip caveat
    - `pytest tests/test_weekly_training_review.py -x` exits 0
  </acceptance_criteria>
  <done>An active covering directive silences the Sunday weekly review, logged distinctly from failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| directive skip verdict → outreach silence | Silence must be distinguishable from an infra failure in the logs |
| directive skip → hub /api/today contract | A skipped morning briefing must not corrupt the hub's Today snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08 | Repudiation | A directive-skipped send is indistinguishable from a crashed cron | mitigate | Skips logged as `skipped_by_directive` (a distinct status, never "failed"), reusing the write-after-send discipline — the skip is a recorded decision, not a swallowed error |
| T-31-09 | Tampering | A skipped morning briefing writes stale structured/daily_note data to the hub | mitigate | On a directive skip, the structured + daily_note writes are deliberately skipped; hub /api/today falls back to its existing placeholder (RESEARCH Open Questions #2) |
| T-31-SC | Tampering | Package installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `pytest tests/test_morning_briefing.py tests/test_weekly_training_review.py -x` green.
- Grep confirms `skipped_by_directive` appears in both crons and is never conflated with the "failed" status.
</verification>

<success_criteria>
- The interim-cron site of DIR-03 is live: morning briefing and weekly review can be vetoed by a covering directive, via each composer's own LLM call (no new LLM call, no cascade machinery).
- Skips are logged distinctly; the morning-briefing skip keeps the hub /api/today contract unbroken.
</success_criteria>

<output>
Create `.planning/phases/31-standing-directives/31-05-SUMMARY.md` when done.
</output>
