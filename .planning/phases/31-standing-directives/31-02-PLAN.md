---
phase: 31-standing-directives
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/firestore_conversation.py
  - tests/test_firestore_conversation.py
autonomous: true
requirements: [DIR-06]
must_haves:
  truths:
    - "get_recent_window returns messages from the last 24h regardless of the 6h session-idle timeout"
    - "Every newly appended message carries a per-message ISO ts"
    - "Legacy messages without a ts field are tolerated (kept by position, never KeyError'd or dropped)"
    - "get_recent_window respects a max_messages cap"
  artifacts:
    - path: "memory/firestore_conversation.py"
      provides: "get_recent_window() + per-message ts in _txn_append"
      contains: "def get_recent_window"
    - path: "tests/test_firestore_conversation.py"
      provides: "get_recent_window coverage"
      contains: "recent_window"
  key_links:
    - from: "_txn_append"
      to: "message dict"
      via: "adds ts=datetime.now(timezone.utc).isoformat()"
      pattern: "\"ts\""
    - from: "get_recent_window"
      to: "per-message ts"
      via: "24h cutoff filter tolerating missing ts"
      pattern: "timedelta\\(hours=hours\\)"
---

<objective>
Add `get_recent_window()` to `FirestoreConversationStore` plus a per-message `ts` timestamp in
`_txn_append`. This is a genuine shared dependency built once here and reused by Phase 32's
ambient-recall tail-prepend and `conversation_tail` gather — building it three times risks three
divergent truncation bugs.

Purpose: Fixes live, already-diagnosed bug "B3": `core/reflection.py::_gather_day` reads
`conv_store.get()` (the 6h-timeout-bounded ACTIVE session), which is empty at nightly-reflection
time on most nights — so the DIR-06/07 learning loop would be structurally blind to the day's
conversation. `get_recent_window(user_id, hours=24)` returns the day's messages independent of
the session-idle timeout. Realizes the DIR-06 window primitive (consumed by Plan 06).

Output: `get_recent_window()` + `ts` stamping in `memory/firestore_conversation.py`, with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standing-directives/31-CONTEXT.md
@.planning/phases/31-standing-directives/31-PATTERNS.md

<interfaces>
<!-- The exact analogs. Do not explore — copy the get_full try/except-then-empty shape. -->
memory/firestore_conversation.py:
- get_full(user_id) at lines 146-166 — the try/except GoogleAPICallError → [] shape to model on;
  returns list((snapshot.to_dict() or {}).get("messages", []))
- get(user_id) at line 120 — the current session-bounded read that reflection wrongly uses
- _txn_append at lines 33-67; line 54 currently: messages.append({"role": role, "content": content})
- datetime/timezone already imported at the top of the file (no new import needed for ts stamping)

Test helpers already in tests/test_firestore_conversation.py (lines 20-93):
- _install_firestore_mock(), _store_with_doc(doc), the isolated_modules fixture — reuse verbatim
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing get_recent_window + ts tests</name>
  <files>tests/test_firestore_conversation.py</files>
  <read_first>
    - tests/test_firestore_conversation.py (full file, ~93 lines — reuse _install_firestore_mock / _store_with_doc / isolated_modules verbatim; PATTERNS.md § memory/firestore_conversation.py has the exact fixture excerpt)
    - .planning/phases/31-standing-directives/31-RESEARCH.md (§ Code Examples get_recent_window for the reference implementation)
  </read_first>
  <behavior>
    - Seed messages where some carry a ts within 24h and some carry a ts older than 24h; assert only
      the in-window ones are returned, EVEN when the doc's session-level updated_at is >6h old
      (i.e. get() would return [] but get_recent_window returns the day's messages)
    - Seed messages with NO ts field (legacy): they are kept by array position, not dropped, not KeyError'd
    - Seed >max_messages in-window messages; assert the return is truncated to the last max_messages
    - A malformed/unparseable ts string is tolerated (kept, not crashed)
    - After append via _txn_append, the stored message dict contains a "ts" ISO string
  </behavior>
  <action>
    Add tests named around `recent_window` (so `-k recent_window` selects them) plus one asserting
    `_txn_append` stamps `ts`. Seed docs with mixed ts-present / ts-absent / stale-updated_at cases
    via `_store_with_doc`. These MUST fail initially (method + ts stamping do not yet exist).
  </action>
  <verify>
    <automated>pytest tests/test_firestore_conversation.py -k "recent_window or ts" -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_firestore_conversation.py contains test names matching `recent_window`
    - A test asserts the 24h window returns messages when the 6h session window would be empty
    - A test asserts legacy (no-ts) message tolerance and a test asserts the max_messages cap
    - The selected tests fail (RED) — method not yet implemented
  </acceptance_criteria>
  <done>Failing tests encode the 24h-window, ts-stamping, legacy-tolerance, and cap behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement get_recent_window + per-message ts stamping</name>
  <files>memory/firestore_conversation.py</files>
  <read_first>
    - memory/firestore_conversation.py lines 33-67 (_txn_append) and 146-166 (get_full)
    - .planning/phases/31-standing-directives/31-RESEARCH.md (§ Code Examples — get_recent_window body + the _txn_append ts one-liner)
  </read_first>
  <action>
    In `_txn_append`, change the `messages.append({...})` at line 54 to also set
    `"ts": datetime.now(timezone.utc).isoformat()` (datetime/timezone already imported).
    Add method `get_recent_window(self, user_id: int, hours: int = 24, max_messages: int = 60) -> list[dict]`:
    read the doc (same try/except `GoogleAPICallError` → `[]` shape as `get_full`), return `[]` if
    not `snapshot.exists`; compute `cutoff = datetime.now(timezone.utc) - timedelta(hours=hours)`;
    iterate `messages`, keeping any message whose parsed `ts` (via `datetime.fromisoformat`) is
    `>= cutoff`, and keeping (by position) any message with a missing or unparseable `ts` (legacy
    tolerance — never KeyError, never drop). Return `windowed[-max_messages:]`. Do NOT consult
    `session_start_index` or the idle timeout — this method is deliberately timeout-independent.
  </action>
  <verify>
    <automated>pytest tests/test_firestore_conversation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - memory/firestore_conversation.py contains `def get_recent_window(self, user_id`
    - memory/firestore_conversation.py line in _txn_append contains `"ts"` set to an ISO timestamp
    - Source contains `timedelta(hours=hours)` and returns `windowed[-max_messages:]`
    - `pytest tests/test_firestore_conversation.py -x` exits 0 (GREEN, including the 3 pre-existing tests)
  </acceptance_criteria>
  <done>get_recent_window returns the 24h window independent of session timeout; new messages carry ts; existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation doc → reflection brain call (Plan 06) | The 24h window content is later forwarded to the Anthropic reflection call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Information Disclosure | 24h conversation window may contain an accidentally-pasted secret forwarded to the reflection brain | accept | Reflection uses the Anthropic backend (not Groq) — smaller blast radius than the Phase 32 Groq-triage case; RESEARCH.md flags this as a milestone-accepted residual, not a Phase 31 blocker |
| T-31-SC | Tampering | Package installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `pytest tests/test_firestore_conversation.py -x` green (including the 3 pre-existing tests).
- Manual sanity: `get_recent_window` does not reference `session_start_index` or `SESSION_TIMEOUT_HOURS`.
</verification>

<success_criteria>
- get_recent_window returns the last-24h messages regardless of the 6h idle timeout, tolerates legacy no-ts messages, and caps at max_messages.
- Newly appended messages carry a per-message ISO ts. DIR-06 window primitive ready for Plan 06.
</success_criteria>

<output>
Create `.planning/phases/31-standing-directives/31-02-SUMMARY.md` when done.
</output>
