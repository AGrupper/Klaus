---
phase: 28-habits-supplements
plan: 05
type: execute
wave: 4
depends_on: ["28-04-habits-supplements"]
files_modified:
  - frontend/src/components/timeline/HabitsBand.tsx
  - frontend/src/components/timeline/TimelineDay.tsx
  - frontend/src/components/layout/GlanceRail.tsx
autonomous: true
requirements: [TIME-06, HABIT-02]
must_haves:
  # CONTEXT.md decisions implemented by this plan: D-05, D-08
  truths:
    - "Today's scheduled habits/supplements appear as a band on the Today timeline (TIME-06)"
    - "A single tap on a timeline habit toggles it done/undone; tapping a timeline supplement opens the dose-edit sheet"
    - "The band renders nothing when no habit/supplement is scheduled today (no empty placeholder)"
    - "The desktop GlanceRail shows a Habits streaks card below the Tasks card with up to 4 streak leaders"
    - "Both the timeline band and the streaks card show/hide responsively via Tailwind classes only — never inline style display"
  artifacts:
    - path: "frontend/src/components/timeline/HabitsBand.tsx"
      provides: "Today-timeline band of habits/supplements scheduled today with one-tap check-off (mirrors DueTasksBand)"
      contains: "useCheckOffHabit"
    - path: "frontend/src/components/timeline/TimelineDay.tsx"
      provides: "mounts HabitsBand on the Today timeline after DueTasksBand"
      contains: "HabitsBand"
    - path: "frontend/src/components/layout/GlanceRail.tsx"
      provides: "Habits streaks card (desktop) following the Tasks card block"
      contains: "useHabitSummary"
  key_links:
    - from: "frontend/src/components/timeline/HabitsBand.tsx"
      to: "useHabits / useCheckOffHabit (hooks from 28-04)"
      via: "client-side filter on scheduled_today + optimistic check-off"
      pattern: "useCheckOffHabit"
    - from: "frontend/src/components/timeline/TimelineDay.tsx"
      to: "components/timeline/HabitsBand"
      via: "mounted after <DueTasksBand /> (line ~243)"
      pattern: "<HabitsBand"
    - from: "frontend/src/components/layout/GlanceRail.tsx"
      to: "GET /api/habits/summary via useHabitSummary"
      via: "streaks card mirroring the Tasks card block (lines 123-189)"
      pattern: "useHabitSummary"
---

> **CONTEXT.md decisions implemented by this plan:** D-05, D-08

<objective>
Surface habits on the two read-only Hub surfaces that Plan 04 (Habits tab) does not
own: the **Today timeline band** (`HabitsBand.tsx`, mounted in `TimelineDay.tsx`) and the
**desktop GlanceRail streaks card** (`GlanceRail.tsx`). This completes the TIME-06 frontend
contract and the D-08 "check-off lives on BOTH the Today timeline AND the Habits tab"
locked decision. Both surfaces reuse the data layer and optimistic check-off hook built in
Plan 04 — this plan adds no new hooks or API calls.

Purpose: 28-04 delivered one-tap check-off in the Habits tab; D-08 requires the same
one-tap action on the Today timeline (TIME-06), and CONTEXT.md's canonical reference
("glance rail gains a streaks summary") + UI-SPEC § GlanceRail Streaks Card require the
desktop rail to show streak leaders. These are the two missing surfaces.

Output: `HabitsBand.tsx` (new), `TimelineDay.tsx` (mounts the band), `GlanceRail.tsx`
(streaks card added) — all rendering habits via the Plan-04 hooks, all responsive
show/hide driven by Tailwind classes (never inline `style={{ display }}`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-habits-supplements/28-CONTEXT.md
@.planning/phases/28-habits-supplements/28-PATTERNS.md
@.planning/phases/28-habits-supplements/28-UI-SPEC.md
@.planning/phases/28-habits-supplements/28-04-PLAN.md

<interfaces>
<!-- CONSUMED from Plan 04 (frontend/src/hooks/useHabits.ts + api/habits.ts) — use directly,
     do NOT redefine these hooks or types. They are a cross-plan data contract: -->
useHabits()            -> react-query list of Habit + {scheduled_today, done_today, dose_taken, streak}
useCheckOffHabit()     -> optimistic mutation; call with {habitId, date, done, doseTaken?}
useHabitSummary()      -> {pending_today: number, streak_leaders: [{id, name, streak}]}
type Habit, HabitSlot, HabitType   (from frontend/src/api/habits.ts)

<!-- CONSUMED from the existing codebase (analogs, verified present): -->
frontend/src/components/timeline/DueTasksBand.tsx   (band header, guard, row, check-off + tap-to-/tasks)
frontend/src/components/timeline/TimelineDay.tsx    (mount site — <DueTasksBand /> at line ~243)
frontend/src/components/layout/GlanceRail.tsx       (Tasks card block lines 123-189 — exact analog)
frontend/src/components/habits/DoseEditSheet.tsx    (from Plan 04 — opened on supplement check-off)
frontend/src/tokens.ts                              (accent/border/textPrimary/textSecondary/typography/fontFamily)

<!-- DIVERGENCES from DueTasksBand (28-PATTERNS § HabitsBand): -->
- NO title-tap navigation — the habit name is NOT a link (do not import/use useNavigate for the title row).
- Supplements show a dose pill ("5g") at Label 13px in `border` background beside the name.
- Tapping a supplement opens DoseEditSheet (D-09); tapping a habit toggles immediately (D-07).
- Slot-group order: Morning → Noon → Evening → Bedtime → "any time" (same as HabitsPage).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HabitsBand.tsx + mount in TimelineDay.tsx</name>
  <files>frontend/src/components/timeline/HabitsBand.tsx, frontend/src/components/timeline/TimelineDay.tsx</files>
  <read_first>
    - frontend/src/components/timeline/DueTasksBand.tsx (entire file — band header lines 246-277, guard `if (length === 0) return null` line ~236, row + check-off + completion micro-anim; the exact analog)
    - frontend/src/components/timeline/TimelineDay.tsx (lines 40-50 imports + line ~243 `<DueTasksBand />` mount site)
    - frontend/src/hooks/useHabits.ts (Plan 04 — useHabits + useCheckOffHabit; do NOT redefine)
    - frontend/src/components/habits/DoseEditSheet.tsx (Plan 04 — opened on supplement check-off)
    - .planning/phases/28-habits-supplements/28-PATTERNS.md (§ HabitsBand — header copy-exact, guard, divergences)
    - .planning/phases/28-habits-supplements/28-UI-SPEC.md (§ HabitsBand (Today timeline) lines 275-290 + Spacing band-header exception line 54)
  </read_first>
  <action>
    Create `frontend/src/components/timeline/HabitsBand.tsx` mirroring `DueTasksBand.tsx`.
    Data: call `useHabits()` and filter client-side to items where `scheduled_today === true`
    (the flag is enriched by GET /api/habits in Plan 02 — TIME-06 API side); guard
    `if (items.length === 0) return null` (no empty placeholder, mirroring DueTasksBand
    line ~236). Section header: "Habits" label, accent 4px × 32px left-border stripe,
    padding `10px 14px 6px` — copy the DueTasksBand header (lines 246-277) verbatim, change
    only the label text (named band-header spacing exception, UI-SPEC line 54). Order rows by
    slot Morning → Noon → Evening → Bedtime → "any time". Per row: 44px CheckButton
    (filled accent circle when `done_today`, open circle when pending; same 150ms completion
    micro-anim as DueTasksBand, NO row collapse), habit name (Body 16px, plain React text —
    NOT a link, no `useNavigate` on the title, T-28-xss), slot chip, and for supplements a
    dose pill ("5g") at Label 13px in `border` background beside the name. Tap a habit-type
    item → call `useCheckOffHabit` immediately with `{habitId, date: todayISO, done: !done_today}`
    (toggle, D-07). Tap a supplement-type item → open `DoseEditSheet` (D-09) rather than
    toggling inline. Import all colors from `tokens.ts` (accent/border/textPrimary/
    textSecondary/typography/fontFamily) — never hardcode hex. Then modify
    `frontend/src/components/timeline/TimelineDay.tsx`: import `HabitsBand` and mount
    `<HabitsBand />` immediately after `<DueTasksBand />` (line ~243), before the timed
    calendar events (UI-SPEC: band renders after DueTasksBand, before timed events).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "error TS" ; test $? -ne 0 && grep -q "HabitsBand" frontend/src/components/timeline/TimelineDay.tsx && grep -q "useCheckOffHabit" frontend/src/components/timeline/HabitsBand.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/timeline/HabitsBand.tsx` exists; frontend typechecks with zero TS errors (`npx tsc --noEmit` clean).
    - `TimelineDay.tsx` imports and mounts `<HabitsBand />` after `<DueTasksBand />` (grep confirms both the import and the JSX mount).
    - HabitsBand reuses `useHabits` + `useCheckOffHabit` from Plan 04 (no redefined hook bodies; grep shows the imports, not new `useMutation`/`useQuery` declarations for habits).
    - Guard present: returns null when no item is scheduled today.
    - `grep -rn "style={{ *display" frontend/src/components/timeline/HabitsBand.tsx` returns nothing (Pitfall 2 — class-driven display).
    - `grep -rn "dangerouslySetInnerHTML" frontend/src/components/timeline/HabitsBand.tsx` returns nothing (T-28-xss); habit name + dose rendered as plain React text.
    - `cd frontend && npx vitest run src/components/timeline 2>/dev/null || true` — no pre-existing timeline test regresses (TimelineDay.test.tsx).
  </acceptance_criteria>
  <done>HabitsBand renders today's scheduled habits/supplements with one-tap toggle + supplement dose sheet, mounted in TimelineDay after DueTasksBand; frontend typechecks clean (TIME-06 frontend side, D-08 timeline).</done>
</task>

<task type="auto">
  <name>Task 2: GlanceRail Habits streaks card (desktop)</name>
  <files>frontend/src/components/layout/GlanceRail.tsx</files>
  <read_first>
    - frontend/src/components/layout/GlanceRail.tsx (Tasks card block lines 123-189 — exact analog; responsive wrapper class line ~64 `hidden md:flex md:flex-col`; the 14px font warning at line 166)
    - frontend/src/hooks/useHabits.ts (Plan 04 — useHabitSummary; do NOT redefine)
    - .planning/phases/28-habits-supplements/28-PATTERNS.md (§ GlanceRail.tsx (modify) — copy Tasks card block, replace nav/aria/heading/rows/empty)
    - .planning/phases/28-habits-supplements/28-UI-SPEC.md (§ GlanceRail Streaks Card lines 159-166 + § Modified: GlanceRail lines 292-300 + Typography note line 71: streak value 13px/600, NOT 14px)
  </read_first>
  <action>
    Modify `frontend/src/components/layout/GlanceRail.tsx`: add a "Habits" card immediately
    below the existing "Tasks" card by copying the Tasks card block (lines 123-189) and
    replacing — navigation `navigate('/habits')`; `aria-label="Habits overview — navigate to
    habits"`; heading "Habits" (Heading 20px/600 textPrimary); body = one row per streak
    leader from `useHabitSummary().streak_leaders` (max 4 rows), label = habit name
    (`typography.label.fontSize` 13px / weight 400, textSecondary), value = `"[N]-day streak"`
    (`typography.label.fontSize` 13px / `fontWeight: 600`, textPrimary) — use the 13px Label
    token, NOT 14px (UI-SPEC Typography line 71); empty state "No habits defined."
    (13px textSecondary) when `streak_leaders` is empty. Consume `useHabitSummary` from Plan
    04 (do NOT redefine it). The whole card lives inside the rail's existing desktop-only
    wrapper (`hidden md:flex md:flex-col`, line ~64) — do NOT add any inline `style={{ display }}`
    to show/hide it (Pitfall 2, the Phase-27 leak). Import all colors from `tokens.ts`.
    Render the habit name as plain React text (T-28-xss).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "error TS" ; test $? -ne 0 && grep -q "useHabitSummary" frontend/src/components/layout/GlanceRail.tsx && grep -q "/habits" frontend/src/components/layout/GlanceRail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `GlanceRail.tsx` contains a Habits streaks card that calls `useHabitSummary` and navigates to `/habits` (grep confirms both).
    - Frontend typechecks with zero TS errors.
    - Card renders up to 4 streak-leader rows with "[N]-day streak" values at 13px/600 (no 14px literal introduced for the streak value).
    - Empty state "No habits defined." present.
    - `grep -n "style={{ *display" frontend/src/components/layout/GlanceRail.tsx` shows no NEW responsive display use for the Habits card (display is class-driven via the existing rail wrapper).
    - `grep -rn "dangerouslySetInnerHTML" frontend/src/components/layout/GlanceRail.tsx` returns nothing.
    - `cd frontend && npx vitest run 2>/dev/null || true` — no pre-existing test regresses.
  </acceptance_criteria>
  <done>Desktop GlanceRail shows a Habits streaks card below the Tasks card, fed by useHabitSummary, navigates to /habits; frontend typechecks clean (CONTEXT canonical ref + UI-SPEC § GlanceRail Streaks Card).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/habits + /api/habits/summary → React render | habit name + dose strings rendered into the timeline band and rail card |
| phone vs desktop wrappers | inline `display` leaks phone-only UI to desktop (Phase 27 UAT, 4 incidents) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-xss | Tampering | HabitsBand / GlanceRail name + dose render | mitigate | Dose and name rendered as plain React text only; no `dangerouslySetInnerHTML` (grep gate in both tasks). API length-validates upstream (Plan 02). |
| T-28-display | (UX correctness) | timeline band + rail card responsive visibility | mitigate | Show/hide via Tailwind classes only (band inherits TimelineDay layout; card sits in the rail's `hidden md:flex` wrapper). No inline `style={{ display }}` (grep gate, Pitfall 2). |
| T-28-auth | Elevation of Privilege | GET /api/habits + /api/habits/summary reads | mitigate | Reads go through the Plan-04 hooks → apiFetch (session cookie); routes session-gated server-side behind `require_hub_session` (Plan 02). No new endpoints or token handling added here. |
| T-28-SC | Tampering | npm installs | accept | No new npm packages (28-RESEARCH § Package Legitimacy Audit: none; react-query/lucide already present). |
</threat_model>

<verification>
Frontend (no pytest concern here):
- `cd frontend && npx tsc --noEmit -p tsconfig.json` → zero errors
- `grep -q "HabitsBand" frontend/src/components/timeline/TimelineDay.tsx` → band is mounted
- `grep -q "useHabitSummary" frontend/src/components/layout/GlanceRail.tsx` → streaks card present
- `grep -rn "style={{ *display" frontend/src/components/timeline/HabitsBand.tsx` → empty (Pitfall 2)
- `grep -rn "dangerouslySetInnerHTML" frontend/src/components/timeline/ frontend/src/components/layout/GlanceRail.tsx` → empty (T-28-xss)
- `cd frontend && npx vitest run` → no regression (TimelineDay.test.tsx green)
- `cd frontend && npx vite build` → builds clean
- Manual (28-VALIDATION): tap a due habit on the phone timeline → instant check + tap-again un-check; supplement tap → dose sheet; desktop rail shows streak leaders.
</verification>

<success_criteria>
- Today's scheduled habits/supplements render as a timeline band with one-tap check-off (TIME-06 frontend, D-08 timeline side).
- Supplement check-off in the band opens the dose-edit sheet (D-09); habit check-off toggles immediately (D-07).
- Desktop GlanceRail shows a Habits streaks card below Tasks with up to 4 streak leaders.
- Band + card reuse the Plan-04 hooks (no new endpoints/hooks); responsive display is class-driven; frontend typechecks and builds clean.
</success_criteria>

<output>
Create `.planning/phases/28-habits-supplements/28-05-SUMMARY.md` when done.
</output>
