---
phase: 30-health-pages
plan: 04
type: execute
wave: 3
depends_on: [30-02]
files_modified:
  - frontend/src/api/health.ts
  - frontend/src/hooks/useHealth.ts
  - frontend/src/components/health/SubTabs.tsx
  - frontend/src/components/health/RangeToggle.tsx
  - frontend/src/components/health/SubTabs.test.tsx
autonomous: true
requirements: [HLTH-01, HLTH-02, HLTH-03]
user_setup: []

must_haves:
  truths:
    - "Typed fetch functions call each /api/health/* endpoint via apiFetch (credentials + 401 handling inherited)"
    - "react-query hooks key on ['health', <tab>, <range>] with 5-min staleTime and refetch-on-focus"
    - "SubTabs (Training/Nutrition/Sleep) persists the last-visited tab in localStorage, default Training (D-01, D-02)"
    - "RangeToggle (7d/30d/90d/1y) is NOT persisted — defaults to 30d on every mount (D-04, D-05, D-06)"
  artifacts:
    - path: "frontend/src/api/health.ts"
      provides: "TrainingHistoryData/NutritionDetailData/SleepRecoveryData types + fetchers"
      contains: "fetchTrainingHistory"
    - path: "frontend/src/hooks/useHealth.ts"
      provides: "useTrainingHistory/useNutritionDetail/useSleepRecovery"
      contains: "useTrainingHistory"
    - path: "frontend/src/components/health/SubTabs.tsx"
      provides: "3-way persisted segmented control"
      contains: "health-tab"
    - path: "frontend/src/components/health/RangeToggle.tsx"
      provides: "4-way non-persisted range control"
      contains: "RangeToggle"
  key_links:
    - from: "frontend/src/api/health.ts"
      to: "/api/health/training|nutrition|sleep"
      via: "apiFetch"
      pattern: "apiFetch"
    - from: "frontend/src/components/health/SubTabs.tsx"
      to: "localStorage['health-tab']"
      via: "getItem/setItem"
      pattern: "health-tab"
---

<objective>
Build the frontend data-fetching layer and the two segmented controls that every health
sub-page depends on: typed `api/health.ts` fetchers, `hooks/useHealth.ts` react-query
hooks, the persisted `SubTabs`, and the non-persisted `RangeToggle`.

Purpose: These match the backend route contracts from Plan 30-02 and the established
`api/today.ts` + `useToday` + `SortGroupControl` patterns. Building them once here keeps
the three page plans (05/06/07) parallelizable and consistent.
Output: 2 data-layer files + 2 control components + a SubTabs persistence test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-health-pages/30-UI-SPEC.md
@.planning/phases/30-health-pages/30-PATTERNS.md
@.planning/phases/30-health-pages/30-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

frontend/src/api/today.ts — the type-contract + fetch-fn shape to mirror:
  export interface TodayData {...}
  export async function fetchToday(): Promise<TodayData> { return apiFetch<TodayData>('/api/today') }
frontend/src/api/client.ts — apiFetch<T>(path) sets credentials:'include' + handles 401 redirect.
  ALWAYS use apiFetch, never raw fetch() (Security Domain).
frontend/src/hooks/useToday.ts — useQuery pattern (queryKey, queryFn, refetchOnWindowFocus).
frontend/src/components/tasks/SortGroupControl.tsx — SegmentedGroup helper (~48-102):
  accent active bg, secondary inactive bg, 32px height, border separators, 44px min touch width,
  aria-pressed. Copy this button-row shape for both controls.

Backend response shape (from Plan 30-02, confirm against 30-02-SUMMARY.md):
  /api/health/training → { entries: [...tagged by modality], blocks: [...], strength_volume: {x,y}[], run_trend: {x,y}[] }
  /api/health/nutrition → { series per macro, missing_dates, averages, targets, avg_protein_g_per_kg, slot_adherence, calories_target_derived? }
  /api/health/sleep → { series per metric, header_stats, pipeline_active }
  RangeKey = '7d' | '30d' | '90d' | '1y'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api/health.ts + hooks/useHealth.ts</name>
  <files>frontend/src/api/health.ts, frontend/src/hooks/useHealth.ts</files>
  <read_first>
    - frontend/src/api/today.ts (full — type contract + fetchToday shape, JSDoc-null-semantics convention)
    - frontend/src/api/client.ts (apiFetch signature + credentials/401 handling)
    - frontend/src/hooks/useToday.ts (full — useQuery config)
    - .planning/phases/30-health-pages/30-02-SUMMARY.md (the actual JSON keys the routes return — the source of truth for the TS interfaces)
    - .planning/phases/30-health-pages/30-PATTERNS.md (api/health.ts + useHealth.ts Pattern Assignment; the exact staleTime value)
  </read_first>
  <behavior>
    - fetchTrainingHistory(range) calls apiFetch<TrainingHistoryData>(`/api/health/training?range=${range}`); same for nutrition + sleep.
    - RangeKey type is the closed set '7d'|'30d'|'90d'|'1y'.
    - Interfaces mirror the 30-02 response keys exactly, with JSDoc noting y:number|null gap semantics (D-08) and slot-label-not-time semantics (nutrition).
    - useTrainingHistory/useNutritionDetail/useSleepRecovery use queryKey ['health', <tab>, range], queryFn the matching fetcher, staleTime 5*60*1000, refetchOnWindowFocus true, and NO refetchInterval / NO refetchOnMount override.
  </behavior>
  <action>
    Create frontend/src/api/health.ts exporting a `RangeKey` union type, the three response
    interfaces (`TrainingHistoryData`, `NutritionDetailData`, `SleepRecoveryData`) documented
    field-by-field to match the 30-02-SUMMARY response keys (calling out `y: number | null`
    gap semantics and slot-LABEL-only semantics inline as JSDoc), and three thin fetchers
    `fetchTrainingHistory(range)` / `fetchNutritionDetail(range)` / `fetchSleepRecovery(range)`
    as `apiFetch<T>(\`/api/health/<area>?range=${range}\`)` wrappers — never raw fetch().
    Create frontend/src/hooks/useHealth.ts exporting `useTrainingHistory(range)`,
    `useNutritionDetail(range)`, `useSleepRecovery(range)` using useQuery with the queryKey /
    staleTime (5 min) / refetchOnWindowFocus config from the interfaces block; do not add
    refetchInterval or refetchOnMount.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>api/health.ts exports typed fetchers over apiFetch; useHealth.ts exports the three range-scoped hooks with 5-min staleTime; typecheck clean.</done>
  <acceptance_criteria>
    - `grep -n "apiFetch<" frontend/src/api/health.ts` shows all three fetchers use apiFetch (no raw `fetch(`).
    - `grep -n "5 \* 60 \* 1000\|300000" frontend/src/hooks/useHealth.ts` confirms the 5-min staleTime.
    - `grep -n "refetchInterval\|refetchOnMount" frontend/src/hooks/useHealth.ts` returns nothing.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubTabs (persisted) + RangeToggle (non-persisted)</name>
  <files>frontend/src/components/health/SubTabs.tsx, frontend/src/components/health/RangeToggle.tsx, frontend/src/components/health/SubTabs.test.tsx</files>
  <read_first>
    - frontend/src/components/tasks/SortGroupControl.tsx (SegmentedGroup ~48-102 — the button-row visual pattern to copy)
    - frontend/src/tokens.ts (accent, secondary, border, textSecondary, typography, fontFamily)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory SubTabs/RangeToggle rows; Interaction § page structure D-01/D-02/D-06)
    - frontend/src/components/habits/ContributionGrid.test.tsx (component-test convention)
  </read_first>
  <behavior>
    - SubTabs renders 3 buttons Training/Nutrition/Sleep; active button uses accent bg + weight 600, inactive uses secondary bg + textSecondary.
    - SubTabs reads its initial value from localStorage['health-tab'] (default 'training' when absent) and writes localStorage['health-tab'] on every change (D-02).
    - RangeToggle renders 4 buttons 7d/30d/90d/1y; it is controlled by the parent's useState and does NOT read or write localStorage — default 30d is owned by the parent page (D-06).
    - Both meet the 44px min touch-target width and use aria-pressed on the active button.
  </behavior>
  <action>
    Create frontend/src/components/health/SubTabs.tsx by adapting SortGroupControl's
    SegmentedGroup button-row (full width, drop the label span). Manage the active tab with
    `useState(() => localStorage.getItem('health-tab') ?? 'training')` and a `useEffect` (or
    on-click handler) that calls `localStorage.setItem('health-tab', value)` on change; expose
    the active value + onChange to the parent (controlled or via callback prop, per the parent
    HealthPage contract in Plan 30-08). Create RangeToggle.tsx as a controlled 4-way segmented
    control (props: `value: RangeKey`, `onChange: (r: RangeKey) => void`) using the same
    button-row visuals — NO localStorage. Write SubTabs.test.tsx: assert it defaults to
    Training with no localStorage key, restores the persisted tab when the key is set, and
    writes localStorage on tab change.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/health/SubTabs.test.tsx</automated>
  </verify>
  <done>SubTabs persists the active tab in localStorage (default Training); RangeToggle is a controlled non-persisted 4-way control; SubTabs persistence tests pass.</done>
  <acceptance_criteria>
    - `grep -n "health-tab" frontend/src/components/health/SubTabs.tsx` shows both getItem and setItem.
    - `grep -n "localStorage" frontend/src/components/health/RangeToggle.tsx` returns nothing (not persisted).
    - `cd frontend && npx vitest run src/components/health/SubTabs.test.tsx` exits 0 with a default-Training test and a persistence test.
    - `grep -n "aria-pressed\|44" frontend/src/components/health/SubTabs.tsx` confirms the accessibility + touch-target convention.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend fetch → /api/health/* | Session cookie must accompany every request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Spoofing | health fetchers | mitigate | All three use the shared `apiFetch` wrapper (credentials:'include' + uniform 401 redirect) — never raw `fetch()` (RESEARCH.md Security Domain) |
| T-30-04-02 | Tampering | range query value | mitigate | RangeKey is a closed TS union; only the 4 valid strings are constructible in the UI; the server also allowlists (defense in depth, T-30-02-01) |
| T-30-04-SC | Tampering | npm installs | accept | No new dependencies (@tanstack/react-query already present) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean
- `cd frontend && npx vitest run src/components/health/SubTabs.test.tsx` green
</verification>

<success_criteria>
- Typed fetchers + react-query hooks exist over apiFetch with 5-min staleTime + refetch-on-focus
- SubTabs persists last tab (default Training); RangeToggle is controlled + non-persisted (default 30d owned by parent)
</success_criteria>

<output>
Create `.planning/phases/30-health-pages/30-04-SUMMARY.md` when done
</output>
