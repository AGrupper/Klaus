---
phase: 30-health-pages
plan: 07
type: execute
wave: 4
depends_on: [30-02, 30-03, 30-04]
files_modified:
  - frontend/src/components/health/sleep/SleepRecoveryPage.tsx
  - frontend/src/components/health/sleep/HeaderStatRow.tsx
  - frontend/src/components/health/sleep/HRVChart.tsx
  - frontend/src/components/health/sleep/SleepChart.tsx
  - frontend/src/components/health/sleep/BodyBatteryChart.tsx
  - frontend/src/components/health/sleep/SleepRecoveryPage.test.tsx
autonomous: true
requirements: [HLTH-03]

must_haves:
  truths:
    - "The Sleep sub-tab shows a header stat row (last night's HRV, sleep score, body battery, resting HR, readiness) (D-19)"
    - "Three stacked charts render: HRV (overnight + 7-day baseline overlay), Sleep (score line + duration bars), Body Battery (D-17, D-18)"
    - "When the biometric pipeline has never run, a distinct 'isn't syncing yet' placeholder shows instead of empty charts"
    - "Missing days render as gaps, never zero (watch-not-worn ≠ HRV of 0)"
  artifacts:
    - path: "frontend/src/components/health/sleep/SleepRecoveryPage.tsx"
      provides: "Sleep sub-tab root: RangeToggle + header stats + 3 stacked charts + pipeline guard"
      contains: "SleepRecoveryPage"
    - path: "frontend/src/components/health/sleep/HRVChart.tsx"
      provides: "Dual-series HRV overlay (overnight solid + baseline dashed)"
      contains: "HRVChart"
    - path: "frontend/src/components/health/sleep/HeaderStatRow.tsx"
      provides: "Last-night stat strip"
      contains: "HeaderStatRow"
  key_links:
    - from: "SleepRecoveryPage.tsx"
      to: "useSleepRecovery(range)"
      via: "react-query hook"
      pattern: "useSleepRecovery"
    - from: "SleepRecoveryPage.tsx"
      to: "pipeline_active flag"
      via: "renders pipeline-not-live placeholder when false"
      pattern: "pipeline_active"
---

<objective>
Build the Sleep & Recovery sub-page (HLTH-03): a `RangeToggle`, a `HeaderStatRow` of last
night's numbers, and three stacked `ChartCard`s — HRV (overnight vs 7-day baseline
overlay, D-18), Sleep (score line + duration bars in one card, D-17), Body Battery — plus
the pipeline-not-live guard for the biometric-sync deploy dependency.

Purpose: Show HRV/sleep/body-battery trends with visible patterns over days and weeks
(HLTH-03 success criterion); the overnight-vs-baseline overlay mirrors the exact recovery
signal Klaus's coaching already uses. Consumes `/api/health/sleep` (30-02), the chart
toolkit (30-03), and the data layer/controls (30-04).
Output: 5 sleep components + a SleepRecoveryPage pipeline-guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-health-pages/30-UI-SPEC.md
@.planning/phases/30-health-pages/30-PATTERNS.md
@.planning/phases/30-health-pages/30-02-SUMMARY.md
@.planning/phases/30-health-pages/30-03-SUMMARY.md
@.planning/phases/30-health-pages/30-04-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + prior plans — use directly. -->

Consumes (from prior plans):
  frontend/src/hooks/useHealth.ts::useSleepRecovery(range)
  frontend/src/components/health/RangeToggle.tsx
  frontend/src/components/charts/{LineChart,BarChart,ChartCard,ChartEmptyState}.tsx
  frontend/src/api/health.ts::SleepRecoveryData (series per metric, header_stats, pipeline_active)

Analogs to copy:
  frontend/src/components/timeline/TimelineHeader.tsx — NutritionStrip (~119-199): horizontal
    scroll stat strip on phone / inline row on desktop; each stat Body(16px/600) value over
    Label(13px/400 textSecondary) label. Defensive null-coalescing (?? ) — when pipeline_active
    is false all 5 stats may be null → render nothing rather than dashes/.toFixed() crashes.
    RESPONSIVE GOTCHA: use className="flex md:hidden" etc. — NEVER style={{ display }} (bit 4×).
  frontend/src/components/timeline/PlaceholderCard.tsx — the pipeline-not-live placeholder shape
    (stable "not available yet", NO shimmer — distinct from a loading skeleton).

UI-SPEC series colors (pass as props): HRV overnight #38BDF8 solid · HRV baseline #A78BFA dashed
  (stroke-dasharray 2 3) · Sleep score #38BDF8 line · Sleep duration #2DD4BF bars · Body battery
  #4ADE80 (intentionally NOT success #22C55E). Copy strings in 30-UI-SPEC Copywriting § Sleep.
  Charts stack VERTICALLY (D-17, not side by side).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeaderStatRow + the three chart components</name>
  <files>frontend/src/components/health/sleep/HeaderStatRow.tsx, frontend/src/components/health/sleep/HRVChart.tsx, frontend/src/components/health/sleep/SleepChart.tsx, frontend/src/components/health/sleep/BodyBatteryChart.tsx</files>
  <read_first>
    - frontend/src/components/timeline/TimelineHeader.tsx (NutritionStrip ~119-199 stat-strip pattern + defensive null-coalescing + responsive class-driven display)
    - frontend/src/components/charts/{ChartCard,LineChart,BarChart,ChartEmptyState}.tsx (from 30-03)
    - frontend/src/api/health.ts (SleepRecoveryData series + header_stats fields)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory § Sleep; Color § Sleep series; Copywriting § Sleep header labels / chart headings / HRV legend / per-chart empty)
    - .planning/phases/30-health-pages/30-PATTERNS.md (HeaderStatRow Pattern Assignment + defensive null coercion)
  </read_first>
  <behavior>
    - HeaderStatRow shows 5 stats from header_stats: HRV, Sleep score, Body battery, Resting HR, Readiness — Body(16px/600) value over Label(13px textSecondary) label. Horizontal scroll strip on phone / inline row on desktop, class-driven display only. Any null stat renders safely (no dashes/.toFixed crash); if all 5 null, renders nothing.
    - HRVChart wraps ChartCard+LineChart with TWO series: overnight (#38BDF8 solid) + 7-day baseline (#A78BFA dashed) — the gap between them is the coaching signal (D-18). Legend "Overnight" / "7-day baseline".
    - SleepChart combines a LineChart (score, #38BDF8) and BarChart (duration, #2DD4BF bars) in one ChartCard (D-17). Heading "Sleep".
    - BodyBatteryChart wraps ChartCard+LineChart single series (#4ADE80). Heading "Body Battery".
    - Each chart shows its per-chart empty copy ("No {HRV/sleep/body battery} data for this range.") when its series is empty. Null days render as gaps (LineChart/BarChart handle), never zero.
  </behavior>
  <action>
    Create HeaderStatRow.tsx adapting NutritionStrip (5 stats, null-coalescing, class-driven
    phone-scroll vs desktop-inline — never inline `display`). Create HRVChart.tsx passing the
    overnight + baseline series (colors + dashed flag) into LineChart with the legend. Create
    SleepChart.tsx composing LineChart(score) + BarChart(duration) inside one ChartCard. Create
    BodyBatteryChart.tsx single-series LineChart. Each renders ChartEmptyState with its per-chart
    empty copy when empty.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>HeaderStatRow safely renders 5 last-night stats; HRVChart overlays overnight+baseline; SleepChart combines score line + duration bars; BodyBatteryChart single series; typecheck clean.</done>
  <acceptance_criteria>
    - `grep -n "38BDF8\|A78BFA\|dashed\|Overnight\|7-day baseline" frontend/src/components/health/sleep/HRVChart.tsx` confirms the dual-series overlay + legend.
    - `grep -n "4ADE80" frontend/src/components/health/sleep/BodyBatteryChart.tsx` and `grep -n "22C55E\|success" frontend/src/components/health/sleep/BodyBatteryChart.tsx` (latter returns nothing — body battery is NOT the success green).
    - `grep -nE "style=\{\{[^}]*display" frontend/src/components/health/sleep/HeaderStatRow.tsx` returns no responsive inline `display`.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SleepRecoveryPage compose + pipeline-not-live guard</name>
  <files>frontend/src/components/health/sleep/SleepRecoveryPage.tsx, frontend/src/components/health/sleep/SleepRecoveryPage.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useHealth.ts (useSleepRecovery from 30-04)
    - frontend/src/components/health/RangeToggle.tsx (from 30-04)
    - frontend/src/components/timeline/PlaceholderCard.tsx (pipeline-not-live placeholder shape, no shimmer)
    - frontend/src/components/shared/Skeleton.tsx (loading state)
    - frontend/src/api/health.ts (SleepRecoveryData.pipeline_active)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Interaction § pipeline-not-live guard; Responsive § Sleep desktop inline row + vertical charts; Copywriting § pipeline-not-live placeholder)
    - .planning/phases/30-health-pages/30-CONTEXT.md (biometric-sync deploy dependency)
  </read_first>
  <behavior>
    - SleepRecoveryPage owns useState<RangeKey>('30d'), renders RangeToggle, HeaderStatRow, then the three charts stacked vertically (D-17).
    - When pipeline_active is false (biometric pipeline has NEVER populated a row), render the PlaceholderCard copy "Sleep & recovery data isn't syncing yet." INSTEAD of the header stats + empty charts — distinct from the normal per-chart empty state (which shows when pipeline_active is true but the selected range has no rows).
    - Skeleton on loading; error copy "Couldn't load sleep & recovery data — pull to refresh."
  </behavior>
  <action>
    Create SleepRecoveryPage.tsx wiring RangeToggle → useSleepRecovery(range) → HeaderStatRow +
    HRVChart + SleepChart + BodyBatteryChart (vertical stack). Branch on `pipeline_active`: when
    false, render the PlaceholderCard "isn't syncing yet" copy in place of the stats+charts; when
    true, render the normal content (per-chart empty states handle a data-less range). Skeleton on
    loading, error copy on error. Write SleepRecoveryPage.test.tsx: assert that pipeline_active
    false renders the "isn't syncing yet" placeholder and NOT the charts, and that pipeline_active
    true with empty series renders the charts with their per-chart empty states (the two states are
    visibly distinct).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/health/sleep/SleepRecoveryPage.test.tsx</automated>
  </verify>
  <done>SleepRecoveryPage stacks header stats + 3 charts, and shows the distinct pipeline-not-live placeholder when pipeline_active is false; tests prove the two empty states differ.</done>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/health/sleep/SleepRecoveryPage.test.tsx` exits 0 with a pipeline_active=false placeholder test and a pipeline_active=true empty-range test.
    - `grep -n "pipeline_active\|isn't syncing yet" frontend/src/components/health/sleep/SleepRecoveryPage.tsx` confirms the guard + copy.
    - `grep -n "useSleepRecovery\|HRVChart\|SleepChart\|BodyBatteryChart\|HeaderStatRow" frontend/src/components/health/sleep/SleepRecoveryPage.tsx` shows all wired.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API series → chart render | Pre-aggregated biometrics rendered verbatim |
| pipeline_active flag → UI state | Distinguishes "cron never ran" from "no data in range" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07-01 | Information Disclosure | missing biometric days | mitigate | Null days render as gaps via LineChart/BarChart (D-08); watch-not-worn never drawn as HRV/body-battery of 0 |
| T-30-07-02 | Information Disclosure | pipeline-never-ran state | mitigate | `pipeline_active=false` shows a distinct "isn't syncing yet" placeholder — never silently implies zero data (guards the biometric-sync deploy dependency) |
| T-30-07-03 | Denial of Service (self) | null header stats | mitigate | Defensive `?? ` null-coalescing — no `.toFixed()` crash when a stat is null (pipeline-not-live case) |
| T-30-07-SC | Tampering | npm installs | accept | No new dependencies |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/health/sleep/SleepRecoveryPage.test.tsx` green
- `cd frontend && npx tsc --noEmit` clean
</verification>

<success_criteria>
- Sleep sub-page shows a last-night header stat row + three stacked charts (HRV overlay, sleep score+duration, body battery) (D-17/D-18/D-19)
- pipeline-not-live placeholder shows distinctly from empty-range state (biometric-sync deploy guard)
- Missing days render as gaps, never zeros (D-08)
</success_criteria>

<output>
Create `.planning/phases/30-health-pages/30-07-SUMMARY.md` when done
</output>
