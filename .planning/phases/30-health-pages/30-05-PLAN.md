---
phase: 30-health-pages
plan: 05
type: execute
wave: 4
depends_on: [30-02, 30-03, 30-04]
files_modified:
  - frontend/src/components/health/training/TrainingHistoryPage.tsx
  - frontend/src/components/health/training/TrainingTrendCharts.tsx
  - frontend/src/components/health/training/TrainingLog.tsx
  - frontend/src/components/health/training/TrainingLogEntry.tsx
  - frontend/src/components/health/training/BlockDivider.tsx
  - frontend/src/components/health/training/StrengthDrilldownSheet.tsx
  - frontend/src/components/health/training/RunDrilldownSheet.tsx
  - frontend/src/components/health/training/BenchmarkDrilldownSheet.tsx
  - frontend/src/components/health/training/TrainingLog.test.tsx
autonomous: true
requirements: [HLTH-01]

must_haves:
  truths:
    - "The Training sub-tab shows one reverse-chronological log interleaving strength/run/benchmark entries, each modality color-coded (D-09)"
    - "Block boundaries render as labeled dividers; benchmark entries are highlighted in the stream (D-12)"
    - "Two trend charts (Weekly Volume, Pace & Distance) render above the log, respecting the range toggle (D-11)"
    - "Tapping an entry opens its drill-down: strength per-set table, run lap table, benchmark measured-vs-previous (D-10)"
  artifacts:
    - path: "frontend/src/components/health/training/TrainingHistoryPage.tsx"
      provides: "Training sub-tab root: RangeToggle + trend charts + mixed log"
      contains: "TrainingHistoryPage"
    - path: "frontend/src/components/health/training/TrainingLog.tsx"
      provides: "Reverse-chronological mixed log with block dividers"
      contains: "TrainingLog"
    - path: "frontend/src/components/health/training/StrengthDrilldownSheet.tsx"
      provides: "Per-set drill-down sheet"
      contains: "DrilldownSheet"
  key_links:
    - from: "TrainingHistoryPage.tsx"
      to: "useTrainingHistory(range)"
      via: "react-query hook"
      pattern: "useTrainingHistory"
    - from: "TrainingLogEntry.tsx"
      to: "Strength/Run/Benchmark DrilldownSheet"
      via: "onTap opens matching sheet"
      pattern: "DrilldownSheet"
---

<objective>
Build the Training History sub-page (HLTH-01): a `RangeToggle`, two trend `ChartCard`s
(Weekly Volume, Pace & Distance), and one mixed reverse-chronological log interleaving
color-coded strength/run/benchmark entries with block dividers and benchmark highlights —
each entry tappable into a full drill-down sheet.

Purpose: Amit's explicit ask — one color-coded stream that "reads like the periodized
plan" (D-09, D-12) with full per-set / per-lap drill-down (D-10). Consumes the
`/api/health/training` contract (30-02), the chart toolkit (30-03), and the data
layer/controls (30-04).
Output: 8 training components + a TrainingLog behavior test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-health-pages/30-UI-SPEC.md
@.planning/phases/30-health-pages/30-PATTERNS.md
@.planning/phases/30-health-pages/30-02-SUMMARY.md
@.planning/phases/30-health-pages/30-03-SUMMARY.md
@.planning/phases/30-health-pages/30-04-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + prior plans — use directly. -->

Consumes (from prior plans):
  frontend/src/hooks/useHealth.ts::useTrainingHistory(range)
  frontend/src/components/health/RangeToggle.tsx (controlled: value/onChange)
  frontend/src/components/charts/{LineChart,BarChart,ChartCard,ChartEmptyState,ChartTooltip}.tsx
  frontend/src/api/health.ts::TrainingHistoryData (entries/blocks/strength_volume/run_trend)

Analogs to copy:
  frontend/src/components/tasks/TaskDetailSheet.tsx — sheet/modal chrome: scrim z:190, sheet
    z:191, phone bottom-sheet vs desktop centered modal (maxWidth 480px, up to 560px for the
    strength/run tables), 250ms ease-out slide, scroll-lock (document.body.style.overflow),
    onMouseDown={preventDefault} on close button. Health sheets have NO text inputs → use
    bottom:0 (not keyboardInset), no useVisualViewport.
  frontend/src/components/tasks/{DueTasksBand}.tsx — 4px accent left-border stripe convention
    (recolor per modality) + band-header padding (block divider 10px 14px).
  frontend/src/components/habits/ContributionGrid.test.tsx — component-test convention.

UI-SPEC colors (pass into charts/badges/stripes as props — NOT hardcoded in shared primitives):
  Strength #FB923C · Run #38BDF8 · Benchmark #A78BFA (+ row tint #A78BFA14) · BlockDivider bg #111118.
  Log entry minHeight 64px. Copy strings in 30-UI-SPEC Copywriting Contract § Training.
Responsive: every phone-vs-desktop wrapper uses a Tailwind class for display (md:hidden /
  hidden md:flex) — NEVER style={{ display }} (Phase 27 UAT lesson, bit 4×).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrainingLog + TrainingLogEntry + BlockDivider</name>
  <files>frontend/src/components/health/training/TrainingLog.tsx, frontend/src/components/health/training/TrainingLogEntry.tsx, frontend/src/components/health/training/BlockDivider.tsx, frontend/src/components/health/training/TrainingLog.test.tsx</files>
  <read_first>
    - frontend/src/components/tasks/DueTasksBand.tsx (left-border stripe + band-header padding convention)
    - frontend/src/api/health.ts (TrainingHistoryData entry/block shape from 30-04)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory § Training; Color § modality; Spacing § log row minHeight 64px + block divider padding; Copywriting § Training badges/dividers)
    - .planning/phases/30-health-pages/30-PATTERNS.md (TrainingLog/BlockDivider Pattern Assignment)
    - frontend/src/components/habits/ContributionGrid.test.tsx (test convention)
  </read_first>
  <behavior>
    - TrainingLog renders entries in reverse-chronological order, interleaving strength/run/benchmark.
    - Block boundaries (from the blocks array, resolved by date overlap) render as a BlockDivider row "Block {N} — {label}" between the entries that fall in different blocks. N is the server-derived `block_number` and the name is the block's `label` field (both come from the /api/health/training payload — there is NO `block_name` field).
    - Each TrainingLogEntry has: a 4px left-border stripe in its modality color (strength #FB923C / run #38BDF8 / benchmark #A78BFA), a modality badge (Strength/Run/Benchmark), a Body(16px) title, a Label(13px textSecondary) summary line, a trailing chevron, minHeight 64px.
    - Benchmark entries additionally get an {benchmarkColor}14 (8% opacity) tinted row background (D-12).
    - Tapping a TrainingLogEntry invokes an onSelect callback with the entry (drill-down wired in Task 2); BlockDivider rows are NOT tappable.
  </behavior>
  <action>
    Create TrainingLogEntry.tsx (single row per the visual spec above; modality color chosen by
    a small map, passed to the stripe/badge; benchmark rows get the tint background; calls
    `onSelect(entry)` on tap). Create BlockDivider.tsx (full-width row, #111118 bg, 10px 14px
    padding, Label(13px) textSecondary "Block {block_number} — {label}" using the block's server-derived `block_number` and its `label` field — NOT a nonexistent `block_name`). Create TrainingLog.tsx that
    takes `entries` + `blocks`, resolves which block each entry belongs to by date overlap,
    inserts a BlockDivider whenever the block changes walking newest→oldest, and renders the
    ChartEmptyState/empty copy "No training logged in this range." when entries is empty. Write
    TrainingLog.test.tsx: assert reverse-chronological interleave of the three modalities,
    modality color-coding present per row (assert the stripe color differs by modality), a
    block divider appears at a block boundary, and tapping an entry fires onSelect.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/health/training/TrainingLog.test.tsx</automated>
  </verify>
  <done>TrainingLog interleaves color-coded strength/run/benchmark entries reverse-chronologically with block dividers + benchmark highlights; tapping fires onSelect; tests pass.</done>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/health/training/TrainingLog.test.tsx` exits 0 with a color-coding test and a block-divider test.
    - `grep -n "64" frontend/src/components/health/training/TrainingLogEntry.tsx` confirms minHeight 64.
    - `grep -n "FB923C\|38BDF8\|A78BFA" frontend/src/components/health/training/TrainingLogEntry.tsx` confirms the three modality colors are applied.
    - `grep -nE "style=\{\{[^}]*display" frontend/src/components/health/training/TrainingLog.tsx` returns no responsive `display` inline style.
    - `grep -n "block_name" frontend/src/components/health/training/BlockDivider.tsx frontend/src/components/health/training/TrainingLog.tsx` returns nothing; the divider reads `block_number` and `label`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Strength/Run/Benchmark drill-down sheets</name>
  <files>frontend/src/components/health/training/StrengthDrilldownSheet.tsx, frontend/src/components/health/training/RunDrilldownSheet.tsx, frontend/src/components/health/training/BenchmarkDrilldownSheet.tsx</files>
  <read_first>
    - frontend/src/components/tasks/TaskDetailSheet.tsx (full — scrim z:190 / sheet z:191, phone-sheet vs desktop-modal split, scroll-lock, onMouseDown preventDefault; adapt: no text inputs → bottom:0, no useVisualViewport)
    - frontend/src/api/health.ts (per-set / lap / benchmark fields on the entry payload from 30-02)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory § drill-down sheets; Copywriting § Training drill-down titles + table columns)
    - .planning/phases/30-health-pages/30-PATTERNS.md (drill-down sheets Pattern Assignment — z-index/positional split)
  </read_first>
  <behavior>
    - StrengthDrilldownSheet shows a per-set table: columns "Exercise" / "Sets × Reps" / "Weight", title "{date} — Strength".
    - RunDrilldownSheet shows a lap table: columns "Lap" / "Pace" / "HR", title "{date} — Run".
    - BenchmarkDrilldownSheet shows "Measured: {value}" / "Previous: {previous_value}", title "{date} — Benchmark: {facet}". `previous_value` is supplied on the benchmark entry by the /api/health/training payload (30-02, derived from the prior same-facet result); when it is null the sheet shows "Previous: —".
    - All three: phone bottom-sheet (slides up 250ms) / desktop centered modal (maxWidth 480px, up to 560px for strength/run tables); scrim z:190, sheet z:191; scroll-lock while open; close button uses onMouseDown={preventDefault}.
    - No responsive wrapper uses inline style={{ display }}.
  </behavior>
  <action>
    Create the three drill-down sheets by adapting TaskDetailSheet's chrome (scrim + positional
    split + scroll-lock + close-button trap) into a small shared sheet shell (either a local
    helper or repeated per file — prefer a tiny shared shell to avoid drift), each rendering its
    respective table/body from the entry payload the log passed in. Use the exact copy strings
    from the UI-SPEC. Since these sheets contain no text inputs, use `bottom: 0` on phone (not
    keyboardInset) and omit useVisualViewport. Drive all phone-vs-desktop visibility with
    Tailwind classes, never inline `display`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Three drill-down sheets exist with the correct tables/copy and the shared sheet chrome (z:190/191, scroll-lock, close-trap); typecheck clean.</done>
  <acceptance_criteria>
    - `grep -n "z-\|zIndex" frontend/src/components/health/training/StrengthDrilldownSheet.tsx` confirms the z:190/191 scheme (via shared shell or inline).
    - `grep -n "Sets × Reps\|Weight\|Exercise" frontend/src/components/health/training/StrengthDrilldownSheet.tsx` and `grep -n "Lap\|Pace\|HR" frontend/src/components/health/training/RunDrilldownSheet.tsx` confirm the table columns.
    - `grep -n "previous_value\|Previous" frontend/src/components/health/training/BenchmarkDrilldownSheet.tsx` confirms the sheet reads the entry's `previous_value` (with a "—" fallback when null).
    - `grep -nE "style=\{\{[^}]*display" frontend/src/components/health/training/*.tsx` returns no responsive inline `display`.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: TrainingHistoryPage + TrainingTrendCharts compose</name>
  <files>frontend/src/components/health/training/TrainingHistoryPage.tsx, frontend/src/components/health/training/TrainingTrendCharts.tsx</files>
  <read_first>
    - frontend/src/hooks/useHealth.ts (useTrainingHistory from 30-04)
    - frontend/src/components/health/RangeToggle.tsx (controlled control from 30-04)
    - frontend/src/components/charts/{ChartCard,LineChart,BarChart,ChartEmptyState}.tsx (from 30-03)
    - frontend/src/components/shared/Skeleton.tsx (loading state)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Interaction § range toggle → fetching, loading skeletons; Responsive § Training desktop 2-col vs phone stacked; Copywriting § trend headings + empty states)
  </read_first>
  <behavior>
    - TrainingHistoryPage owns `useState<RangeKey>('30d')`, renders RangeToggle at top, TrainingTrendCharts, then TrainingLog.
    - Changing the range triggers a new useTrainingHistory query; Skeleton blocks replace charts + log rows during the initial fetch for a range.
    - TrainingTrendCharts renders "Weekly Volume" (strength) + "Pace & Distance" (running) as ChartCard+LineChart/BarChart pairs — side-by-side 2-column grid on desktop, stacked on phone (class-driven display, not inline).
    - Entry taps route to the matching drill-down sheet (one sheet-open state at the page level).
    - Global error → "Couldn't load training history — pull to refresh."
  </behavior>
  <action>
    Create TrainingTrendCharts.tsx rendering the two ChartCards with the strength (#FB923C) and
    run (#38BDF8) series colors passed as props to LineChart/BarChart, 2-col grid on desktop
    (Tailwind md: classes) / stacked on phone, ChartEmptyState per chart when its series is
    empty. Create TrainingHistoryPage.tsx wiring RangeToggle → useTrainingHistory(range) →
    TrainingTrendCharts + TrainingLog, holding the selected-entry + open-sheet state and
    rendering the correct drill-down sheet, Skeleton on loading, the error copy on error.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>TrainingHistoryPage composes RangeToggle + trend charts + mixed log + drill-downs with loading/error states; typecheck clean.</done>
  <acceptance_criteria>
    - `grep -n "useTrainingHistory\|RangeToggle\|TrainingLog\|TrainingTrendCharts" frontend/src/components/health/training/TrainingHistoryPage.tsx` shows all wired.
    - `grep -n "Weekly Volume\|Pace & Distance" frontend/src/components/health/training/TrainingTrendCharts.tsx` confirms the trend headings.
    - `grep -nE "md:|hidden" frontend/src/components/health/training/TrainingTrendCharts.tsx` confirms class-driven responsive layout (no inline `display`).
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API series → chart/log render | Pre-aggregated data rendered verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | Tampering | trend chart values | mitigate | Charts render server-computed `{x,y}` verbatim — no client-side tonnage/pace re-derivation (server-computes/client-renders invariant) |
| T-30-05-02 | Information Disclosure | data gaps | mitigate | Empty ranges show explicit empty-state copy; null trend points render as breaks via LineChart (D-08), never fabricated zeros |
| T-30-05-SC | Tampering | npm installs | accept | No new dependencies |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/health/training/TrainingLog.test.tsx` green
- `cd frontend && npx tsc --noEmit` clean
</verification>

<success_criteria>
- Training sub-page shows a color-coded mixed reverse-chronological log with block dividers + benchmark highlights (D-09/D-12)
- Two range-respecting trend charts render above the log (D-11)
- Tapping any entry opens the correct drill-down (per-set / lap / measured-vs-previous) (D-10)
</success_criteria>

<output>
Create `.planning/phases/30-health-pages/30-05-SUMMARY.md` when done
</output>
