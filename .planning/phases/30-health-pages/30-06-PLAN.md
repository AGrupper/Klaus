---
phase: 30-health-pages
plan: 06
type: execute
wave: 4
depends_on: [30-02, 30-03, 30-04]
files_modified:
  - frontend/src/components/health/nutrition/NutritionDetailPage.tsx
  - frontend/src/components/health/nutrition/MacroChipRow.tsx
  - frontend/src/components/health/nutrition/MacroTrendChart.tsx
  - frontend/src/components/health/nutrition/SlotAdherenceGrid.tsx
  - frontend/src/components/health/nutrition/DayDrilldownSheet.tsx
  - frontend/src/components/health/nutrition/SlotAdherenceGrid.test.tsx
autonomous: true
requirements: [HLTH-02]

must_haves:
  truths:
    - "The Nutrition sub-tab shows one macro trend chart defaulting to calories, with a chip row toggling protein/carbs/fat/fiber"
    - "The selected metric's target renders as a dashed reference line; a summary row shows range average vs target (protein adds g/kg)"
    - "A contribution-style slot-adherence grid marks whether a meal was logged per fueling slot per day, keyed on slot LABEL only"
    - "Tapping a bar/point or grid cell opens that day's meal-by-meal breakdown by slot — never presented as a clock time"
    - "Unlogged days render as gaps, never zeros (D-08)"
  artifacts:
    - path: "frontend/src/components/health/nutrition/NutritionDetailPage.tsx"
      provides: "Nutrition sub-tab root: RangeToggle + chip row + trend chart + slot grid"
      contains: "NutritionDetailPage"
    - path: "frontend/src/components/health/nutrition/SlotAdherenceGrid.tsx"
      provides: "Slot-label-keyed hit/miss contribution grid"
      contains: "SlotAdherenceGrid"
    - path: "frontend/src/components/health/nutrition/DayDrilldownSheet.tsx"
      provides: "Per-slot meal breakdown for a day"
      contains: "DayDrilldownSheet"
  key_links:
    - from: "NutritionDetailPage.tsx"
      to: "useNutritionDetail(range)"
      via: "react-query hook"
      pattern: "useNutritionDetail"
    - from: "SlotAdherenceGrid.tsx"
      to: "slot LABEL cells (no derived time)"
      via: "grid cells keyed on slot label"
      pattern: "aria-label"
---

<objective>
Build the Nutrition Detail sub-page (HLTH-02): a `RangeToggle`, a 5-way `MacroChipRow`
(default Calories), a `MacroTrendChart` (selected metric + dashed target line + summary
row), a contribution-style `SlotAdherenceGrid`, and a `DayDrilldownSheet` — all upholding
the slot-label-is-never-an-eating-time invariant (CLAUDE.md §6, D-13/D-16).

Purpose: Distinguish calories/protein/carbs/fat/fiber with a slot view (HLTH-02 success
criterion). Consumes `/api/health/nutrition` (30-02), the chart toolkit (30-03), and the
data layer/controls (30-04).
Output: 5 nutrition components + a SlotAdherenceGrid slot-label test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-health-pages/30-UI-SPEC.md
@.planning/phases/30-health-pages/30-PATTERNS.md
@.planning/phases/30-health-pages/30-02-SUMMARY.md
@.planning/phases/30-health-pages/30-03-SUMMARY.md
@.planning/phases/30-health-pages/30-04-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + prior plans — use directly. -->

Consumes (from prior plans):
  frontend/src/hooks/useHealth.ts::useNutritionDetail(range)
  frontend/src/components/health/RangeToggle.tsx
  frontend/src/components/charts/{LineChart,ChartCard,ChartEmptyState,ChartTooltip}.tsx
  frontend/src/api/health.ts::NutritionDetailData (per-macro series, missing_dates, averages,
    targets, avg_protein_g_per_kg, slot_adherence, calories_target_derived?)

Analogs to copy:
  frontend/src/components/habits/ContributionGrid.tsx (full, 147 lines) — EXPLICITLY directed
    reuse: role="grid"/role="gridcell", 12px cells / 2px gap (named exception — do NOT round to
    4px), gridAutoFlow: column, overflowX auto + auto-scroll-to-newest (scrollLeft = scrollWidth).
    ADAPT: rows = fueling slots (gridTemplateRows: repeat(numSlots,12px)), columns = days-in-range
    (no weekday/mondayIndex/leadingPad logic). 2-state fill: hit #38BDF8 / miss #1F1F1F.
  frontend/src/components/habits/ContributionGrid.test.tsx — test convention.
  frontend/src/components/tasks/TaskDetailSheet.tsx — DayDrilldownSheet chrome (z:190/191,
    bottom:0 no keyboardInset, scroll-lock, onMouseDown close-trap).
  frontend/src/components/tasks/SortGroupControl.tsx — SegmentedGroup shape for MacroChipRow
    (adapt to 5-way, active chip uses its OWN metric color, never accent).

UI-SPEC macro colors (pass as props): Calories #38BDF8 · Protein #F87171 · Carbs #FBBF24 ·
  Fat #A78BFA · Fiber #2DD4BF · Target line #9CA3AF dashed (stroke-dasharray 4 4).
INVARIANT (CLAUDE.md §6): slot LABELS only ("Post-lift"/"Evening"/…) — NEVER emit or render a
  clock time derived from canonical 08:00/12:00/20:00 slot timestamps. Copy strings in
  30-UI-SPEC Copywriting § Nutrition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MacroChipRow + MacroTrendChart</name>
  <files>frontend/src/components/health/nutrition/MacroChipRow.tsx, frontend/src/components/health/nutrition/MacroTrendChart.tsx</files>
  <read_first>
    - frontend/src/components/tasks/SortGroupControl.tsx (SegmentedGroup shape to adapt to a 5-way chip toggle)
    - frontend/src/components/charts/{ChartCard,LineChart,ChartEmptyState}.tsx (from 30-03)
    - frontend/src/components/timeline/TimelineHeader.tsx (NutritionStrip/GarminStatsRows label/value styling for the summary row)
    - frontend/src/api/health.ts (NutritionDetailData series/averages/targets shape)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory § Nutrition MacroChipRow/MacroTrendChart; Color § macro series; Copywriting § Nutrition chip labels / summary row / target legend; Interaction D-14/D-15)
  </read_first>
  <behavior>
    - MacroChipRow renders 5 single-select chips (Calories/Protein/Carbs/Fat/Fiber); the active chip uses ITS OWN metric color (never accent — respects the nutrition-macro accent carve-out), inactive uses border bg + textSecondary. Default selection Calories (D-14). Chip row scrolls horizontally on narrow phones (overflow-x auto, flex-shrink 0 per chip).
    - MacroTrendChart renders the selected metric's series in that metric's color, a dashed target reference line (#9CA3AF, from targets[metric]; for calories use the derived target when calories_target_derived is set), and a summary row "Avg {metric}: {value} · Target {target_value}" — the protein row additionally appends "· {value} g/kg bodyweight".
    - Unlogged days are gaps (LineChart handles null); never zero-filled.
    - Empty range → "No nutrition data logged in this range."
  </behavior>
  <action>
    Create MacroChipRow.tsx (controlled: `metric` + `onChange`; 5 chips each carrying its own
    color from a metric→color map; horizontal-scroll wrapper for phone). Create
    MacroTrendChart.tsx wrapping ChartCard+LineChart: pass the selected metric's series +
    color + the dashed target referenceLine, and render a summary row below the chart using the
    NutritionStrip label/value styling (avg vs target; protein appends g/kg from
    avg_protein_g_per_kg). Render ChartEmptyState with the nutrition empty copy when the series
    is empty. Tapping a bar/point invokes an onDaySelect(date) callback (day drilldown wired at
    the page level in Task 3).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>MacroChipRow toggles the 5 metrics (own colors, default Calories); MacroTrendChart shows the selected series + dashed target + avg-vs-target summary (protein g/kg); typecheck clean.</done>
  <acceptance_criteria>
    - `grep -n "F87171\|FBBF24\|2DD4BF\|38BDF8\|A78BFA" frontend/src/components/health/nutrition/MacroChipRow.tsx` confirms per-metric colors (not accent #6366F1).
    - `grep -n "6366F1\|accent" frontend/src/components/health/nutrition/MacroChipRow.tsx` returns nothing (accent carve-out respected).
    - `grep -n "g/kg\|Target\|Avg" frontend/src/components/health/nutrition/MacroTrendChart.tsx` confirms the summary row.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SlotAdherenceGrid + DayDrilldownSheet (slot-label invariant)</name>
  <files>frontend/src/components/health/nutrition/SlotAdherenceGrid.tsx, frontend/src/components/health/nutrition/DayDrilldownSheet.tsx, frontend/src/components/health/nutrition/SlotAdherenceGrid.test.tsx</files>
  <read_first>
    - frontend/src/components/habits/ContributionGrid.tsx (full — 12px/2px-gap cell convention, role=grid/gridcell, gridAutoFlow column, auto-scroll-to-newest; adapt rows=slots, columns=days)
    - frontend/src/components/habits/ContributionGrid.test.tsx (test convention)
    - frontend/src/components/tasks/TaskDetailSheet.tsx (DayDrilldownSheet chrome)
    - frontend/src/api/health.ts (slot_adherence matrix shape from 30-02)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory § SlotAdherenceGrid/DayDrilldownSheet; Color § slot grid hit/miss; Copywriting § slot section heading / day drilldown per-meal row; Interaction D-13/D-16 slot-label-never-time)
    - CLAUDE.md §6 (HealthKit/Lifesum slot-time invariant)
  </read_first>
  <behavior>
    - SlotAdherenceGrid rows = distinct fueling-slot LABELS present in the range (from slot_adherence), columns = days-in-range. Cell fill: hit #38BDF8 (a meal logged in that slot that day), miss #1F1F1F. 12px cells, 2px gap (do NOT round to 4px). role="grid", cells role="gridcell" aria-label="{date}, {slot}: {logged/not logged}". Auto-scrolls to the newest day on mount. Horizontal-scroll within its card.
    - Grid cells and labels key on the slot LABEL only — NO clock time anywhere (aria-label uses the slot name, not a derived HH:MM).
    - Tapping a cell invokes onDaySelect(date).
    - DayDrilldownSheet shows that day's meals grouped/labeled by slot: "{slot label} — {kcal} kcal, {protein}g protein, {carbs}g carbs, {fat}g fat, {fiber}g fiber" — slot label is the fueling-slot NAME, never a time. Empty → "No meals logged this day."
    - Slot-adherence empty state → "No meals logged yet in this range."
  </behavior>
  <action>
    Create SlotAdherenceGrid.tsx by adapting ContributionGrid's grid CSS: gridTemplateRows
    repeat(numSlots,12px), gridAutoFlow column, columns = days, 2px gap, hit/miss 2-state fill,
    role/aria as above (slot LABEL in the aria-label, never a time), overflowX auto +
    scrollLeft=scrollWidth on mount. Create DayDrilldownSheet.tsx adapting TaskDetailSheet chrome
    (z:190/191, bottom:0, scroll-lock, close-trap) rendering the per-slot meal breakdown with the
    exact copy string, slot labels only. Write SlotAdherenceGrid.test.tsx: assert cells are keyed
    on the slot label (aria-label contains the slot name), assert NO cell/label contains an HH:MM
    or clock-time string, assert hit vs miss fill colors differ, and tapping a cell fires
    onDaySelect.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/health/nutrition/SlotAdherenceGrid.test.tsx</automated>
  </verify>
  <done>SlotAdherenceGrid renders a slot-label-keyed hit/miss grid (no clock times) with 12px/2px cells + auto-scroll; DayDrilldownSheet shows per-slot meals labeled by slot only; tests pass.</done>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/health/nutrition/SlotAdherenceGrid.test.tsx` exits 0 with a test asserting no HH:MM/clock-time string appears and a test asserting the aria-label carries the slot name.
    - `grep -n "gap: '2px'\|gap: 2\|2px" frontend/src/components/health/nutrition/SlotAdherenceGrid.tsx` confirms the 2px gap convention.
    - `grep -n "38BDF8\|1F1F1F\|skeleton" frontend/src/components/health/nutrition/SlotAdherenceGrid.tsx` confirms hit/miss fills.
    - `grep -nE "[0-2][0-9]:[0-5][0-9]" frontend/src/components/health/nutrition/DayDrilldownSheet.tsx` returns nothing (no clock time rendered).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: NutritionDetailPage compose</name>
  <files>frontend/src/components/health/nutrition/NutritionDetailPage.tsx</files>
  <read_first>
    - frontend/src/hooks/useHealth.ts (useNutritionDetail from 30-04)
    - frontend/src/components/health/RangeToggle.tsx (from 30-04)
    - frontend/src/components/shared/Skeleton.tsx (loading state)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Interaction § nutrition macro chip + chart D-14/15/16; Responsive § Nutrition; Copywriting § Nutrition error/empty)
  </read_first>
  <behavior>
    - NutritionDetailPage owns useState<RangeKey>('30d') and useState<metric>('calories'), renders RangeToggle, MacroChipRow, MacroTrendChart, and SlotAdherenceGrid (heading "Fueling Slot Adherence").
    - Both MacroTrendChart bar/point taps and SlotAdherenceGrid cell taps open a single DayDrilldownSheet for the selected date.
    - Skeleton on loading; error copy "Couldn't load nutrition data — pull to refresh."
  </behavior>
  <action>
    Create NutritionDetailPage.tsx wiring RangeToggle → useNutritionDetail(range) →
    MacroChipRow (controls the selected metric) → MacroTrendChart (selected metric) →
    SlotAdherenceGrid, holding the open-day + DayDrilldownSheet state shared by both the chart
    and the grid. Skeleton on loading, error copy on error.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>NutritionDetailPage composes range toggle + chip row + trend chart + slot grid + shared day drilldown with loading/error states; typecheck clean.</done>
  <acceptance_criteria>
    - `grep -n "useNutritionDetail\|MacroChipRow\|MacroTrendChart\|SlotAdherenceGrid\|DayDrilldownSheet" frontend/src/components/health/nutrition/NutritionDetailPage.tsx` shows all wired.
    - `grep -n "Fueling Slot Adherence" frontend/src/components/health/nutrition/NutritionDetailPage.tsx` confirms the section heading.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slot data → grid/drilldown render | Slot labels must never be surfaced as eating times |
| API series → chart render | Pre-aggregated data rendered verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06-01 | Information Disclosure | slot timestamps | mitigate | Grid + drilldown render slot LABELS only; test asserts no HH:MM/clock-time string is rendered (CLAUDE.md §6, D-13/D-16) |
| T-30-06-02 | Tampering | macro averages | mitigate | Summary row renders server-computed averages/targets/g-per-kg verbatim — no client re-derivation |
| T-30-06-03 | Information Disclosure | unlogged days | mitigate | Null macro days render as gaps via LineChart (D-08); grid miss ≠ a fabricated zero-calorie day |
| T-30-06-SC | Tampering | npm installs | accept | No new dependencies |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/health/nutrition/SlotAdherenceGrid.test.tsx` green
- `cd frontend && npx tsc --noEmit` clean
</verification>

<success_criteria>
- Nutrition sub-page shows a calories-default macro trend with chip toggle (D-14), dashed target + avg-vs-target summary incl. protein g/kg (D-15)
- Slot-adherence grid + day drill-down key on slot LABEL only — no eating-time inference (D-13/D-16, CLAUDE.md §6)
- Unlogged days render as gaps, never zeros (D-08)
</success_criteria>

<output>
Create `.planning/phases/30-health-pages/30-06-SUMMARY.md` when done
</output>
