---
phase: 30-health-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/LineChart.tsx
  - frontend/src/components/charts/BarChart.tsx
  - frontend/src/components/charts/ChartTooltip.tsx
  - frontend/src/components/charts/ChartCard.tsx
  - frontend/src/components/charts/ChartEmptyState.tsx
  - frontend/src/components/charts/LineChart.test.tsx
  - frontend/src/components/charts/BarChart.test.tsx
autonomous: true
requirements: [HLTH-01, HLTH-02, HLTH-03]
user_setup: []

must_haves:
  truths:
    - "LineChart renders a visible break (not a zero, not an interpolated bridge) at every null point (D-08)"
    - "BarChart renders no bar (not a zero-height bar) for a null value"
    - "Tapping/hovering a point shows a tooltip with the exact value; tapping a gap shows a 'No data' tooltip"
    - "Charts render whatever {x,y} points arrive (daily or weekly) without client-side re-bucketing"
    - "No new npm dependency is added — hand-rolled SVG only"
  artifacts:
    - path: "frontend/src/components/charts/LineChart.tsx"
      provides: "Generic SVG line chart with gap-split paths, dual-series, dashed reference line, tooltips"
      contains: "LineChart"
    - path: "frontend/src/components/charts/BarChart.tsx"
      provides: "Generic SVG bar chart with gap semantics"
      contains: "BarChart"
    - path: "frontend/src/components/charts/ChartTooltip.tsx"
      provides: "Shared tooltip bubble (secondary bg, border, 8px radius)"
      contains: "ChartTooltip"
    - path: "frontend/src/components/charts/ChartCard.tsx"
      provides: "Card wrapper matching GlanceRail/HabitRow convention"
      contains: "ChartCard"
    - path: "frontend/src/components/charts/ChartEmptyState.tsx"
      provides: "Centered empty-state message inside a ChartCard"
      contains: "ChartEmptyState"
  key_links:
    - from: "LineChart.tsx"
      to: "series points with y === null"
      via: "SVG path split into multiple <path> segments at each null"
      pattern: "null"
---

<objective>
Build the shared, hand-rolled inline-SVG chart toolkit — `LineChart`, `BarChart`,
`ChartTooltip`, `ChartCard`, `ChartEmptyState` — the first charting capability in the
Klaus Hub. Zero new dependencies (D-04 discretion resolved in 30-UI-SPEC), matching the
`ContributionGrid` "pure hand-rolled visuals, no library" precedent. All three health
pages consume these primitives.

Purpose: The single highest-value implementation detail is D-08 gap rendering — no
charting library treats "never interpolate, never zero-fill a null" as a first-class
primitive without fighting its API. Hand-rolling gives full control over the SVG path
splitting that makes gaps honest.
Output: 5 chart primitives + gap-rendering unit tests. No consumption yet (pages come
in later plans) — this plan ships the toolkit standalone with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-health-pages/30-UI-SPEC.md
@.planning/phases/30-health-pages/30-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

frontend/src/tokens.ts exports: dominant #0A0A0A, secondary #1A1A1A, accent #6366F1,
  textPrimary #F9FAFB, textSecondary #9CA3AF, border #2A2A2A, skeleton #1F1F1F,
  fontFamily, typography (Body 16px/1.5, Label 13px/1.4), fontWeightSemibold 600.
frontend/src/components/habits/ContributionGrid.tsx — the "hand-rolled SVG/CSS, no library"
  philosophy precedent + its .test.tsx sibling shows the component-test convention (vitest +
  @testing-library/react).
frontend/src/components/timeline/PlaceholderCard.tsx — empty-state shape analog.
frontend/src/components/tasks/TaskDetailSheet.tsx — tooltip chrome values only
  (bg #1A1A1A, border 1px #2A2A2A, borderRadius 8px).

LineChart props contract (from 30-UI-SPEC Component Inventory):
  series: { label: string; color: string; points: { x: string; y: number | null }[]; dashed?: boolean }[]
  referenceLine?: { value: number; label: string }
  height: number   // 160 phone / 220 desktop
Chart colors are passed IN by callers (from the UI-SPEC modality/macro/sleep color tables) —
  the primitives themselves import only neutral chrome tokens, never a hardcoded series hex.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LineChart + BarChart with gap semantics</name>
  <files>frontend/src/components/charts/LineChart.tsx, frontend/src/components/charts/BarChart.tsx, frontend/src/components/charts/ChartTooltip.tsx, frontend/src/components/charts/LineChart.test.tsx, frontend/src/components/charts/BarChart.test.tsx</files>
  <read_first>
    - frontend/src/components/habits/ContributionGrid.tsx (hand-rolled SVG/CSS pattern, token imports, header-comment philosophy)
    - frontend/src/components/habits/ContributionGrid.test.tsx (vitest component-test convention to mirror)
    - frontend/src/tokens.ts (chrome tokens + typography)
    - frontend/src/components/tasks/TaskDetailSheet.tsx (tooltip chrome values only: bg #1A1A1A, border 1px #2A2A2A, radius 8px)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory chart props + ChartTooltip row; Color § Chart chrome + Missing-data gap row; Interaction § Chart tap/hover tooltip (D-04) + Missing-data gaps)
    - .planning/phases/30-health-pages/30-PATTERNS.md (chart primitives Pattern Assignment — the gap-split discipline)
  </read_first>
  <behavior>
    - LineChart with points [{x:"d1",y:10},{x:"d2",y:null},{x:"d3",y:12}] renders TWO separate <path> segments (a visible break at d2), NOT one path bridging d1→d3 and NOT a point at y=0 for d2.
    - LineChart supports multiple series (dual-series overlay, D-18) and a dashed second series (stroke-dasharray for baseline).
    - LineChart supports an optional dashed reference line (target, D-15) drawn in textSecondary.
    - LineChart x-axis is time-scaled from the point index/date spacing — renders daily and weekly point sets identically (no client re-bucketing; renders whatever arrives).
    - BarChart with a null value renders no bar for that x (not a zero-height bar).
    - LineChart and BarChart render ChartTooltip THEMSELVES (internally) via nearest-point/nearest-bar hit-testing: hovering (desktop) or tapping (phone, persists until tapping elsewhere) near a point/bar shows a tooltip with that point's exact value + its x label (D-04).
    - Hovering/tapping an x-position that falls on a gap (y === null) shows a tooltip reading "No data" in textSecondary (no value line) — the tooltip still appears so gaps are discoverable (D-08).
    - Both import only neutral chrome tokens; series colors come from props.
  </behavior>
  <action>
    Create frontend/src/components/charts/LineChart.tsx with a header comment
    "Hand-rolled inline SVG — no chart library (D-04, resolved in 30-UI-SPEC), matching the
    ContributionGrid precedent." Accept the props contract from the interfaces block. Compute
    an SVG viewBox from `height` and the point count; map each series to path data by walking
    its points and STARTING A NEW `<path>` element every time `y === null` (literal path
    splitting — never a single path with moveTo gaps, which some renderers visually connect).
    Apply `strokeDasharray` when `series.dashed` is true. Draw the optional `referenceLine` as
    a horizontal dashed line at its y-value using `textSecondary`. Render point markers so the
    tooltip layer (Task 2 / consumer) can hit-test; expose an `onPointHover`/`onPointSelect`
    style callback OR render nearest-point hit-testing internally per the UI-SPEC interaction
    contract (tap on phone persists, hover on desktop). Create BarChart.tsx with the same gap
    rule (skip rendering a rect for null). Import colors only from tokens for chrome; series
    colors come from props. Write LineChart.test.tsx and BarChart.test.tsx (mirror
    ContributionGrid.test.tsx): the highest-value test — a `-t gap` test asserting a null
    point produces a broken line (assert ≥2 path elements, or that no path segment spans the
    null x) and that no zero-value marker/bar is rendered at the null x.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/charts/LineChart.test.tsx src/components/charts/BarChart.test.tsx</automated>
  </verify>
  <done>LineChart splits its path at nulls (no interpolation, no zero-fill), supports dual-series + dashed reference line, and renders ChartTooltip internally on hover/tap ("No data" at gaps); BarChart skips null bars with the same internal tooltip; gap + tooltip + nodata tests pass.</done>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/charts/LineChart.test.tsx -t gap` exits 0 with a test proving a null point breaks the line.
    - `grep -n "no chart library\|ContributionGrid precedent" frontend/src/components/charts/LineChart.tsx` confirms the philosophy header.
    - `grep -rn "recharts\|visx\|d3\|chart.js" frontend/package.json` returns nothing (no dependency added).
    - `grep -n "referenceLine\|dashed\|strokeDasharray" frontend/src/components/charts/LineChart.tsx` confirms reference-line + dashed-series support.
    - `grep -n "ChartTooltip" frontend/src/components/charts/LineChart.tsx frontend/src/components/charts/BarChart.tsx` confirms the charts render ChartTooltip internally (not left to consumers).
    - `cd frontend && npx vitest run src/components/charts/LineChart.test.tsx -t tooltip` exits 0 (a hover/tap near a real point shows a tooltip with that point's exact value).
    - `cd frontend && npx vitest run src/components/charts/LineChart.test.tsx -t nodata` exits 0 (a hover/tap at a gapped x shows a "No data" tooltip with no value).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ChartCard + ChartEmptyState</name>
  <files>frontend/src/components/charts/ChartCard.tsx, frontend/src/components/charts/ChartEmptyState.tsx</files>
  <read_first>
    - frontend/src/components/timeline/PlaceholderCard.tsx (empty-state shape)
    - frontend/src/components/charts/ChartTooltip.tsx (created in Task 1 — the tooltip these cards will contain; no change here, just context)
    - frontend/src/tokens.ts (secondary, border, textPrimary, textSecondary, typography)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Component Inventory: ChartCard/ChartEmptyState rows; Color § Chart chrome; Spacing § chart card padding 16px, radius 10px)
  </read_first>
  <behavior>
    - ChartCard wraps children: secondary bg, border, borderRadius 10px, padding 16px, an optional Body(16px/600) title.
    - ChartEmptyState renders a centered Label(13px textSecondary) message inside a ChartCard, replacing the chart entirely (no empty axes).
  </behavior>
  <action>
    Create ChartCard.tsx (card wrapper matching the GlanceRail/HabitRow convention:
    secondary bg, border, 10px radius, 16px padding, optional title slot). Create
    ChartEmptyState.tsx (centered textSecondary Label message; render it in place of the chart
    when a series has zero points). All import styling only from tokens.ts — no hardcoded hex
    beyond what tokens provide. These are presentational; no dedicated test file required
    beyond the smoke coverage the page plans add, but they must compile and typecheck.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>ChartCard/ChartEmptyState exist, typecheck, and use only token styling.</done>
  <acceptance_criteria>
    - Both files exist and `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -n "borderRadius: 10\|borderRadius: '10" frontend/src/components/charts/ChartCard.tsx` and `padding: 16` confirm the card convention.
    - The "No data" gap tooltip is covered by Task 1's ChartTooltip + nodata test (not re-owned here).
    - No hardcoded series hex in these three files (`grep -nE "#[0-9A-Fa-f]{6}" frontend/src/components/charts/ChartCard.tsx frontend/src/components/charts/ChartEmptyState.tsx` returns nothing — colors come from tokens).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API data → chart render | Pre-aggregated points rendered as-is; charts must not re-derive/alter values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Tampering | client-side aggregation | mitigate | Charts render `{x,y}` points verbatim — no summing/averaging/bucketing in TS (server-computes/client-renders invariant, the 2026-06-09 drifting-numbers lesson) |
| T-30-03-02 | Information Disclosure | gap rendering | mitigate | Null → visible break + "No data" tooltip; never a fabricated zero that misrepresents a watch-not-worn / unlogged day (D-08) |
| T-30-03-SC | Tampering | npm installs | accept | Zero new dependencies — hand-rolled SVG (RESEARCH.md audit not applicable); `frontend/package.json` unchanged |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/charts/LineChart.test.tsx src/components/charts/BarChart.test.tsx` green
- `cd frontend && npx tsc --noEmit` clean
- `frontend/package.json` unchanged (no new dependency)
</verification>

<success_criteria>
- Five chart primitives exist, typecheck, and use only token-based styling
- Gap rendering (D-08) proven by test: null → broken line / skipped bar / "No data" tooltip, never a zero
- No npm dependency added
</success_criteria>

<output>
Create `.planning/phases/30-health-pages/30-03-SUMMARY.md` when done
</output>
