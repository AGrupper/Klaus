---
phase: 30-health-pages
plan: 08
type: execute
wave: 5
depends_on: [30-05, 30-06, 30-07]
files_modified:
  - frontend/src/components/health/HealthPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/health/HealthPage.test.tsx
autonomous: false
requirements: [HLTH-01, HLTH-02, HLTH-03]

must_haves:
  truths:
    - "The /health route renders the real HealthPage (SubTabs + active sub-page), not the ComingSoon placeholder"
    - "Switching sub-tabs swaps the sub-page without a route change; the last tab is remembered on reload"
    - "All three sub-pages render inside the standard center column (sidebar + glance rail + docked chat intact)"
    - "The full frontend + backend test suites hold their baselines"
  artifacts:
    - path: "frontend/src/components/health/HealthPage.tsx"
      provides: "Health root: SubTabs + active sub-page switch"
      contains: "HealthPage"
    - path: "frontend/src/App.tsx"
      provides: "/health route wired to the real HealthPage"
      contains: "HealthPage"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "components/health/HealthPage"
      via: "route element swap (ComingSoon → HealthPage)"
      pattern: "HealthPage"
    - from: "HealthPage.tsx"
      to: "SubTabs + Training/Nutrition/Sleep pages"
      via: "active-tab switch"
      pattern: "SubTabs"
---

<objective>
Wire the three sub-pages together behind the `/health` route: a `HealthPage` root that
renders `SubTabs` and switches between `TrainingHistoryPage` / `NutritionDetailPage` /
`SleepRecoveryPage`, and the `App.tsx` swap replacing the `ComingSoon` placeholder. Then
run the full-suite phase gate and a device UAT checkpoint (SPA/auth paths have no CI
coverage — prior v5.0 phases hit deploy-only bugs).

Purpose: This is the integration seam that turns three independently-built sub-pages into
the shipped Health tab (D-01/D-02/D-03). It closes HLTH-01/02/03 at the route level.
Output: HealthPage root + App.tsx wiring + a HealthPage switch test + a human UAT gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-health-pages/30-UI-SPEC.md
@.planning/phases/30-health-pages/30-05-SUMMARY.md
@.planning/phases/30-health-pages/30-06-SUMMARY.md
@.planning/phases/30-health-pages/30-07-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + prior plans — use directly. -->

frontend/src/App.tsx:
  - Current: function HealthPage() { return <ComingSoon label="Health" /> }  (~line 95)
  - Route: <Route path="/health" element={<HealthPage />} />  (~line 192) — path stays, only the element body changes.
  - Sidebar.tsx / BottomTabs.tsx already have the Health nav entry — NO changes there.

Consumes (from prior plans):
  frontend/src/components/health/SubTabs.tsx (persisted tab, default 'training')
  frontend/src/components/health/training/TrainingHistoryPage.tsx (30-05)
  frontend/src/components/health/nutrition/NutritionDetailPage.tsx (30-06)
  frontend/src/components/health/sleep/SleepRecoveryPage.tsx (30-07)

D-03: Health renders in the SAME center column as Today/Tasks (no full-width exception) —
  do NOT alter the app-shell layout; HealthPage is just the center-column content.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HealthPage root + App.tsx route swap</name>
  <files>frontend/src/components/health/HealthPage.tsx, frontend/src/App.tsx, frontend/src/components/health/HealthPage.test.tsx</files>
  <read_first>
    - frontend/src/App.tsx (the HealthPage placeholder ~95 + the /health route ~192 + how other pages like HabitsPage are imported/wired)
    - frontend/src/components/health/SubTabs.tsx (from 30-04 — its active-value/onChange contract)
    - frontend/src/components/health/training/TrainingHistoryPage.tsx, nutrition/NutritionDetailPage.tsx, sleep/SleepRecoveryPage.tsx (the three sub-page root exports)
    - frontend/src/components/habits/HabitsPage.tsx (root page owning sub-nav + persisted tab — the closest structural analog)
    - .planning/phases/30-health-pages/30-UI-SPEC.md (Interaction § page structure D-01/D-02/D-03; Component Inventory § Modified App.tsx)
  </read_first>
  <behavior>
    - HealthPage renders SubTabs at the top of the center column and, below it, exactly one of TrainingHistoryPage / NutritionDetailPage / SleepRecoveryPage based on the active tab.
    - Switching sub-tabs is pure client state (no route change) and persists via SubTabs' localStorage['health-tab']; first-ever visit defaults to Training.
    - App.tsx's /health route renders <HealthPage /> (real) instead of ComingSoon; the route path is unchanged; Sidebar/BottomTabs unchanged.
    - The layout stays inside the standard center column (no full-width exception, D-03).
  </behavior>
  <action>
    Create frontend/src/components/health/HealthPage.tsx: a root component holding the active-tab
    state via SubTabs (SubTabs manages localStorage; HealthPage consumes the active value) and
    rendering the matching sub-page. Modify frontend/src/App.tsx: import HealthPage from
    './components/health/HealthPage' and change the `HealthPage()` placeholder function body (or
    the route element) to render the real component instead of `<ComingSoon label="Health" />` —
    do NOT change the route path or touch Sidebar/BottomTabs. Write HealthPage.test.tsx: assert
    it renders the Training sub-page by default (no localStorage key), renders the persisted
    sub-page when localStorage['health-tab'] is set, and switches the rendered sub-page when the
    tab changes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/health/HealthPage.test.tsx</automated>
  </verify>
  <done>/health renders the real HealthPage with SubTabs switching between the three sub-pages, default Training, persisted across reload; ComingSoon removed for Health; tests pass.</done>
  <acceptance_criteria>
    - `grep -n "ComingSoon" frontend/src/App.tsx` no longer shows it wrapping the Health route (Health renders the real component).
    - `grep -n "HealthPage" frontend/src/App.tsx` shows the import + usage.
    - `cd frontend && npx vitest run src/components/health/HealthPage.test.tsx` exits 0 with a default-Training test and a tab-switch test.
    - `git diff frontend/src/components/layout/Sidebar.tsx frontend/src/components/layout/BottomTabs.tsx` is empty (nav untouched).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suites + build</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - .planning/phases/30-health-pages/30-VALIDATION.md (Sampling Rate § phase gate — both baselines must hold)
    - .planning/STATE.md (Notes — per-file pytest, 1720+ backend / 122+ frontend baselines)
  </read_first>
  <action>
    Run the frontend full suite (`cd frontend && npm test`) and confirm it holds the 122+
    baseline. Run the frontend production build (`cd frontend && npm run build`) to confirm the
    new health tree + charts compile into the bundle with no type/build errors (SPA build is
    absent from CI — this is the only pre-deploy build check). Run the backend health test files
    per-file (`pytest tests/test_health_training_api.py tests/test_health_nutrition_api.py
    tests/test_health_sleep_api.py tests/test_benchmark_store.py tests/test_health_reads.py -x`
    invoked per-file, NOT as one segfault-prone `pytest tests/` run) and confirm green. Fix any
    regressions surfaced (this task owns no new source beyond regression fixes).
  </action>
  <verify>
    <automated>cd frontend && npm test && npm run build</automated>
  </verify>
  <done>Frontend suite holds the 122+ baseline, the production build succeeds, and all backend health test files pass per-file.</done>
  <acceptance_criteria>
    - `cd frontend && npm test` exits 0 with ≥122 tests passing.
    - `cd frontend && npm run build` exits 0 (no type/build error; health tree bundled).
    - Each backend health test file passes when run individually (`-x`, per-file).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device UAT — deployed health pages on iPhone Safari</name>
  <what-built>
    The three Health sub-pages (Training / Nutrition / Sleep) behind /health, with range
    toggles, hand-rolled charts (gap-honest), drill-down sheets, the slot-adherence grid, and
    the sleep pipeline-not-live guard. All backend routes are session-authed and event-loop-safe.
  </what-built>
  <how-to-verify>
    1. Deploy the frontend + backend to Cloud Run (per docs/DEPLOYMENT.md). SPA/auth paths have
       NO CI coverage — the deployed URL is the only real test surface (prior v5.0 phases hit
       deploy-only bugs).
    2. On the installed iPhone PWA, open the Health tab. Confirm SubTabs (Training/Nutrition/Sleep)
       and that reloading returns to the last-visited sub-tab; first visit lands on Training.
    3. Training: confirm the mixed color-coded log (strength orange / run blue / benchmark violet),
       block dividers, both trend charts, and that tapping an entry opens the correct drill-down
       (per-set / lap / measured-vs-previous). Switch the range toggle (7d/30d/90d/1y) and confirm
       the charts + log update.
    4. Nutrition: confirm the calories-default macro chart, chip toggle to protein/carbs/fat/fiber,
       the dashed target line + avg-vs-target summary (protein shows g/kg), the slot-adherence grid
       (slot LABELS only — verify NO clock times appear anywhere), and that tapping a bar/cell opens
       the day's per-slot meal breakdown.
    5. Sleep: confirm the header stat row, the HRV overnight+baseline overlay, sleep score+duration,
       body battery. If the biometric-sync cron is not yet live, confirm the "Sleep & recovery data
       isn't syncing yet." placeholder shows (distinct from an empty range). OPERATOR STEP: check
       `gcloud scheduler jobs list` for `klaus-biometric-sync` and `daily_biometrics` row recency.
    6. Confirm a missing day renders as a visible gap (not a zero dip) on any chart with a gap.
    7. Confirm the responsive layout: desktop keeps sidebar + glance rail + docked chat; no phone-only
       UI leaks to desktop or vice versa (the inline-display responsive gotcha).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (rendering, gaps, slot-time leak, layout leak, auth)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated → /health SPA + /api/health/* | Auth gate (existing require_hub_session) must hold post-deploy |
| deployed build → device | SPA/auth paths have no CI coverage; deploy-only bugs are the documented v5.0 risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08-01 | Spoofing | /api/health/* post-deploy | mitigate | Device UAT confirms an unauthenticated/logged-out state cannot reach the health data (existing HUB-01 gate) |
| T-30-08-02 | Information Disclosure | slot-time leak on device | mitigate | UAT step 4 explicitly verifies NO clock times render in the nutrition slot views (CLAUDE.md §6) |
| T-30-08-03 | Information Disclosure | responsive layout leak | mitigate | UAT step 7 verifies no phone-only UI leaks to desktop (inline-display gotcha, bit 4× in prior phases) |
| T-30-08-SC | Tampering | npm installs | accept | No new dependencies added across the phase (RESEARCH.md audit: not applicable) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/health/HealthPage.test.tsx` green
- `cd frontend && npm test && npm run build` green
- Backend health test files pass per-file
- Human UAT approved on the deployed iPhone PWA
</verification>

<success_criteria>
- /health renders the real HealthPage (SubTabs + active sub-page); ComingSoon removed for Health
- Full frontend suite (122+) + production build green; backend health tests green per-file
- Device UAT confirms all three pages render correctly, no slot-time leak, no responsive leak, auth holds
</success_criteria>

<output>
Create `.planning/phases/30-health-pages/30-08-SUMMARY.md` when done
</output>
