---
phase: 30.5-brain-upgrade-sonnet-5
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - core/self_manifest.py
  - docs/SELF.md
  - prompts/smart_agent.md
  - scripts/measure_prompt_tokens.py
autonomous: true
requirements: [BRAIN-06]
user_setup: []

must_haves:
  truths:
    - "The generated SELF.md manifest is measurably smaller — tools collapse to category one-liners, crons to one summary line"
    - "smart_agent.md has a light de-prescription pass with the Tier A/B contract and persona voice preserved verbatim"
    - "The token reduction is measured with the real claude-sonnet-5 tokenizer (count_tokens), not char estimates, and before/after numbers are recorded"
  artifacts:
    - path: core/self_manifest.py
      provides: "Compact manifest generator (grouped tool one-liners + cron summary line)"
      contains: "def"
    - path: scripts/measure_prompt_tokens.py
      provides: "count_tokens before/after measurement tool for the slimming acceptance number"
      contains: "count_tokens"
    - path: prompts/smart_agent.md
      provides: "Light de-prescription pass, Tier A/B + persona untouched"
  key_links:
    - from: "core/self_manifest.py"
      to: "docs/SELF.md"
      via: "generator writes the file"
      pattern: "SELF.md"
---

<objective>
Measurably slim Klaus's always-on system prompt (BRAIN-06) before Sonnet takes live traffic —
mandatory ordering per Pitfall 3 (Sonnet follows prescriptive ALWAYS/NEVER rules more literally,
so de-prescription must land before the flip). Two surfaces: (1) compact the `SELF.md` generator
(`core/self_manifest.py`) so tools collapse to per-category one-liners and crons to a single
summary line (D-07), regenerating `docs/SELF.md`; (2) a LIGHT de-prescription pass on
`prompts/smart_agent.md` (D-06) that softens Gemini-era hand-holding imperatives while keeping the
Tier A/B data-presence contract and the JARVIS/C-3PO persona voice VERBATIM (D-05). Measure the
reduction with the real `claude-sonnet-5` tokenizer via `count_tokens` and record before/after.

Purpose: Cheaper, less over-literal always-on prompt before the flip.
Output: Compact generator + regenerated SELF.md, de-prescribed smart_agent.md, measurement script + numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact the SELF.md generator (grouped tool one-liners + cron summary line)</name>
  <files>core/self_manifest.py, docs/SELF.md</files>
  <read_first>
    - core/self_manifest.py (_render_manifest; §Tools table loop at 364-375; §Cron Jobs table at 378-394; §Model Map at 349-362; _load_tool_data at line 105; the "do not edit manually" front-matter it emits)
    - CLAUDE.md §4 (live directory layout — canonical tool list by category) and §5 (current 10 cron jobs — the truthed list)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"core/self_manifest.py::_render_manifest"
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md D-07 (grouped one-liners)
  </read_first>
  <action>
    In `_render_manifest`, replace the per-tool `## Tools` markdown table (one row per tool,
    lines 364-375) with grouped one-liner lines by category (e.g. "Calendar: list/create/
    free-busy/delete…", "Memory: remember/recall/forget…") derived from grouping the existing
    `tool_rows` (from `_load_tool_data`) by a category key (tool-name prefix or the existing
    `routing` field). Preserve `_load_tool_data`'s schema introspection so the manifest can never
    drift — change ONLY the rendering loop, not the data source. Collapse the `## Cron Jobs`
    table (378-394) to a single summary line; while here, true the cron list up against CLAUDE.md
    §5 (the hardcoded table is stale: proactive-alerts/reflect retired, backstop/autonomous-tick/
    strength-sync/run-sync missing). Do NOT change the Model Map default strings here — the
    post-flip default updates belong to Plan 06 (avoid a self_manifest.py conflict with the flip
    plan by leaving Model Map defaults alone). Regenerate `docs/SELF.md` by running the generator
    so the committed artifact reflects the compaction (docs/SELF.md is generated output, never
    hand-edited).
  </action>
  <verify>
    <automated>python -c "import core.self_manifest as m; print(hasattr(m,'_render_manifest'))" && python -m pytest tests/ -x -q -k "self_manifest or manifest" 2>/dev/null; echo done</automated>
  </verify>
  <acceptance_criteria>
    - core/self_manifest.py `## Tools` output is grouped category one-liners, not a per-tool table row loop
    - core/self_manifest.py `## Cron Jobs` output is a single summary line
    - `_load_tool_data` introspection is unchanged (only the rendering loop changed)
    - docs/SELF.md is regenerated (no per-tool table; contains grouped category lines)
    - Model Map default model strings in _render_manifest are unchanged (Plan 06 owns those)
    - docs/SELF.md line count is lower than before the change (record before/after in SUMMARY)
  </acceptance_criteria>
  <done>SELF.md generator emits a compact capability map (category tool lines + one cron line); docs/SELF.md regenerated smaller; introspection preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Light de-prescription pass on smart_agent.md (Tier A/B + persona verbatim)</name>
  <files>prompts/smart_agent.md</files>
  <read_first>
    - prompts/smart_agent.md (full 360 lines; Tier A/B data-presence contract ~lines 167-201 and the persona voice sections — both OFF LIMITS this phase)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md D-05 (untouchable) + D-06 (light pass only)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"prompts/smart_agent.md" and §"Common Pitfalls" Pitfall 3
  </read_first>
  <action>
    Grep the file for `ALWAYS`/`NEVER`/`MUST` OUTSIDE the Tier A/B region (~167-201) and the
    persona voice sections. For each hit, apply D-06's test: if it is a genuine hard invariant
    (delivery idempotency, cost gating, secret denylist) keep it; if it is a Gemini-era
    hand-holding imperative or redundant phrasing, soften it to values/guidance framing or delete
    the redundancy. This is a LIGHT pass — do NOT do the deep behavior-shaping rewrite (that is
    Phases 31-33; double-rewriting is wasteful). Do NOT modify a single character of the Tier A/B
    data-presence (no-fabrication) contract or the JARVIS/C-3PO persona voice — those stay
    verbatim (D-05, battle-tested against real regressions). Keep the file semantically
    equivalent in behavior contracts; only the prescriptive tone of non-invariant sections
    changes.
  </action>
  <verify>
    <automated>grep -c "Tier A" prompts/smart_agent.md</automated>
  </verify>
  <acceptance_criteria>
    - The Tier A/B data-presence contract region is byte-identical to before (verify with a saved diff of lines ~167-201: no changes there)
    - The JARVIS/C-3PO persona voice sections are unchanged
    - At least one non-invariant ALWAYS/NEVER imperative outside the protected regions was softened or removed (documented in SUMMARY)
    - `grep -c "Tier A" prompts/smart_agent.md` outputs >= 1 (contract still present)
  </acceptance_criteria>
  <done>smart_agent.md non-invariant prescriptive sections softened for Sonnet's literal following; Tier A/B contract + persona preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the slimming with count_tokens (real Sonnet-5 tokenizer)</name>
  <files>scripts/measure_prompt_tokens.py</files>
  <read_first>
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md §"Code Examples" (count_tokens snippet — the primary source; renders the ACTUAL rendered system template, not the raw file)
    - core/main.py render_smart_system (how the system template is assembled/rendered) — reference only for what to feed count_tokens
    - scripts/eval_tick_brain.py (measurement-only runner — directory/style convention for scripts)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md D-08 (directional + measured, no fixed threshold; never char estimates)
  </read_first>
  <action>
    Create `scripts/measure_prompt_tokens.py`: a one-off script that renders the actual smart
    system prompt (SELF.md + smart_agent.md as assembled by render_smart_system, using
    placeholder values for volatile sections) and calls
    `anthropic.Anthropic(api_key=os.environ["SMART_AGENT_API_KEY"]).messages.count_tokens(
    model="claude-sonnet-5", system=rendered, messages=[{"role":"user","content":"placeholder"}])`,
    printing `count.input_tokens`. It must NOT use char-count estimation or tiktoken (wrong
    tokenizer family). This is a live-API measurement tool, not a pytest fixture. Run it against
    the pre-slim and post-slim prompt (git-stash or a saved copy of the old rendered prompt for
    the "before" number) and record both numbers in the plan SUMMARY as the BRAIN-06 acceptance
    evidence. Guard for a missing API key with a clear error message (do not crash opaquely).
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('scripts/measure_prompt_tokens.py').read()); print('parse-ok')" && grep -c "count_tokens" scripts/measure_prompt_tokens.py</automated>
  </verify>
  <acceptance_criteria>
    - scripts/measure_prompt_tokens.py exists and parses (valid Python)
    - It calls `client.messages.count_tokens(model="claude-sonnet-5"` on the rendered system prompt
    - It does NOT import tiktoken or estimate tokens from character count
    - Before/after `input_tokens` numbers are recorded in 30.5-04-SUMMARY.md and the after < before
    - `grep -c "count_tokens" scripts/measure_prompt_tokens.py` outputs >= 1
  </acceptance_criteria>
  <done>A count_tokens-based script produces the real Sonnet-5 before/after token numbers proving measurable reduction; numbers recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator → docs/SELF.md | Generated artifact overwritten on deploy; must be a code change, not a hand-edit |
| Prompt content → Sonnet behavior | Over-literal following of surviving ALWAYS/NEVER rules |
| count_tokens → Anthropic API | Uses SMART_AGENT_API_KEY; read-only token count, no generation cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30.5-10 | Tampering | Manual edit of generated docs/SELF.md drifting from generator | mitigate | Compaction lands in the generator only; regenerate the file; introspection preserved |
| T-30.5-11 | Repudiation / Integrity | Slimming claimed but unmeasured (char estimate) | mitigate | count_tokens against real claude-sonnet-5; before/after recorded (no tiktoken/char estimate) |
| T-30.5-12 | Tampering | Accidental edit to Tier A/B contract or persona (regression surface) | mitigate | Protected regions preserved verbatim; acceptance asserts byte-identical Tier A/B block |
| T-30.5-SC | Tampering | pip installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- Generator emits compact tool/cron sections; docs/SELF.md regenerated smaller
- Tier A/B + persona verbatim in smart_agent.md
- scripts/measure_prompt_tokens.py produces real Sonnet-5 before/after numbers (recorded in SUMMARY)
</verification>

<success_criteria>
BRAIN-06 satisfied: always-on prompt measurably slimmer via count_tokens on the real model, with Tier A/B contract and persona voice preserved — landed before the Plan 06 flip.
</success_criteria>

<output>
Create `.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-04-SUMMARY.md` when done (include before/after token numbers).
</output>
