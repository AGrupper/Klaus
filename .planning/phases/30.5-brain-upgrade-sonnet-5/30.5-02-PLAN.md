---
phase: 30.5-brain-upgrade-sonnet-5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/firestore_db.py
  - tests/test_firestore_db.py
autonomous: true
requirements: [BRAIN-02, BRAIN-04, BRAIN-07]
user_setup: []

must_haves:
  truths:
    - "LLMUsageStore.record accepts and accumulates cache-read/cache-write tokens and per-purpose cost"
    - "A 'yesterday' spend total (with per-purpose cost breakdown) is readable for the daily tripwire"
    - "UserProfileStore.load() is served from the module TTL cache on repeat calls within TTL"
    - "A per-date cost-tripwire suppression record can be checked and set (once-per-day gating)"
  artifacts:
    - path: memory/firestore_db.py
      provides: "LLMUsage cache-token + per-purpose-cost fields, yesterday/date summary, UserProfileStore TTL cache, CostTripwireLog suppression"
      contains: "cache_read_tokens"
    - path: tests/test_firestore_db.py
      provides: "Cache-field record, per-date summary, UserProfileStore cache, tripwire suppression tests"
      contains: "user_profile"
  key_links:
    - from: "UserProfileStore.load"
      to: "_READ_CACHE"
      via: "_cache_get / _cache_put"
      pattern: "_cache_get\\(.*user_profile"
    - from: "LLMUsageStore.record"
      to: "firestore.Increment cache-token fields"
      via: "merge=True set"
      pattern: "cache_write_tokens.*Increment"
---

<objective>
Build the storage-layer foundation the metering (Plan 03) and daily-spend tripwire (Plan 05)
plans consume, all inside `memory/firestore_db.py`: (1) extend `LLMUsageStore` to accumulate
cache-read/cache-write tokens and per-purpose cost, plus a per-date/"yesterday" summary that
returns per-purpose cost drivers; (2) TTL-cache `UserProfileStore.load()` using the existing
`SelfStateStore` pattern (BRAIN-07); (3) add a per-date cost-tripwire suppression store
(BRAIN-04's once-per-day gate). Owning all `firestore_db.py` edits in one plan keeps parallel
waves conflict-free.

Purpose: Truthful, cache-aware, per-purpose cost metering + a cached profile read + tripwire gate.
Output: Extended LLMUsageStore, cached UserProfileStore, CostTripwireLog store, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend LLMUsageStore — cache-token fields, per-purpose cost, per-date summary</name>
  <files>memory/firestore_db.py, tests/test_firestore_db.py</files>
  <behavior>
    - record(model, purpose, in_tokens, out_tokens, cost, cache_read_tokens=0, cache_write_tokens=0) increments total_cache_read_tokens, total_cache_write_tokens, and {purpose}_cost_usd alongside the existing totals
    - Existing 5-arg callers (no cache kwargs) keep working unchanged (defaults 0)
    - summary_for_date("YYYY-MM-DD") returns that day's doc dict ({} if absent), never raises
    - The tripwire can derive per-purpose cost drivers from {purpose}_cost_usd fields
  </behavior>
  <read_first>
    - memory/firestore_db.py LLMUsageStore (class at line 401; record() at 423-443; summary() at 445-480; schema comment 410-415)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"memory/firestore_db.py::LLMUsageStore" and §"Never raises store discipline"
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md §"Open Questions" #2 (per-purpose cost gap; add {purpose}_cost_usd, not just {purpose}_calls)
  </read_first>
  <action>
    Extend `LLMUsageStore.record` with two new keyword args `cache_read_tokens: int = 0` and
    `cache_write_tokens: int = 0` (defaults keep all existing call sites source-compatible).
    Add `total_cache_read_tokens` and `total_cache_write_tokens` `firestore.Increment` fields to
    the merged `set()` payload. Also add a `{purpose}_cost_usd: firestore.Increment(cost)` field
    (built from the same `purpose_key` prefix as the existing `{purpose}_calls`), so D-03's "top
    2-3 cost drivers by purpose" has per-purpose COST, not just call counts. Update the schema
    docstring (lines 410-415) to list the new fields. Add a `summary_for_date(self, date_str:
    str) -> dict` method that reads `llm_usage/{date_str}` and returns `snap.to_dict() or {}` (or
    `{}` if absent), wrapped in the same try/except-returns-{} "never raises" discipline as
    `summary()`. This is additive — Firestore is schemaless, no migration. Preserve the existing
    write-never-raises behavior (log warning, no propagation).
    Add tests to tests/test_firestore_db.py (mock the Firestore client / doc set) asserting the
    Increment payload includes the new cache-token and per-purpose-cost keys, and that
    summary_for_date returns {} for a missing doc without raising.
  </action>
  <verify>
    <automated>python -m pytest tests/test_firestore_db.py -x -q -k "usage or cache or summary_for_date"</automated>
  </verify>
  <acceptance_criteria>
    - memory/firestore_db.py `LLMUsageStore.record` signature contains `cache_read_tokens` and `cache_write_tokens` with default 0
    - record()'s Firestore payload contains `total_cache_read_tokens`, `total_cache_write_tokens`, and a `{purpose}_cost_usd` Increment field
    - memory/firestore_db.py defines `def summary_for_date(self, date_str` returning {} for a missing document
    - Existing 5-positional-arg `record(...)` calls still type-check (defaults applied)
    - `python -m pytest tests/test_firestore_db.py -x -q -k "usage or cache or summary_for_date"` exits 0
  </acceptance_criteria>
  <done>LLMUsageStore accumulates cache tokens + per-purpose cost and exposes a per-date summary; existing callers unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TTL-cache UserProfileStore.load() (BRAIN-07)</name>
  <files>memory/firestore_db.py, tests/test_firestore_db.py</files>
  <behavior>
    - Two load() calls within the 600s TTL trigger exactly one Firestore read
    - update() invalidates the cache so the next load() re-reads
    - load() still returns {} on any error and never raises (existing contract preserved)
  </behavior>
  <read_first>
    - memory/firestore_db.py UserProfileStore (__init__ + load() at 226-252; update() re-raises on failure)
    - memory/firestore_db.py SelfStateStore.get()/set() (lines 503-543) and __init__ _cache_key (line 501) — the exact pattern to replicate
    - memory/firestore_db.py module cache primitives `_READ_CACHE`, `_cache_get`, `_cache_put`, `_cache_invalidate_prefix` (lines 61-86; TTL 600s)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"memory/firestore_db.py::UserProfileStore"
  </read_first>
  <action>
    Mirror `SelfStateStore` exactly. In `UserProfileStore.__init__`, set
    `self._cache_key = ("user_profile", project_id, database)`. In `load()`, before the Firestore
    read, do the `cache_key = getattr(self, "_cache_key", None)` guard → `_cache_get(cache_key)`
    → return `dict(cached)` on hit; on miss, after a successful read, `_cache_put(cache_key,
    dict(result))` before returning. Keep the existing try/except-returns-{} error path intact
    (do not cache on the error path). In `update()`, after the write succeeds (and after the
    existing re-raise-on-failure logic), call `_cache_invalidate_prefix(cache_key)` — invalidate
    only on the success path, matching `SelfStateStore.set`'s ordering. Reuse the module-level
    cache helpers; do NOT build a new cache mechanism. Default 600s TTL is correct (profile data
    changes rarely).
    Add a test to tests/test_firestore_db.py asserting two consecutive load() calls hit Firestore
    once, and that update() forces a re-read (mirror any existing SelfStateStore cache test).
  </action>
  <verify>
    <automated>python -m pytest tests/test_firestore_db.py -x -q -k "user_profile"</automated>
  </verify>
  <acceptance_criteria>
    - memory/firestore_db.py `UserProfileStore.__init__` sets `self._cache_key = ("user_profile"`
    - `UserProfileStore.load()` calls `_cache_get` and `_cache_put`
    - `UserProfileStore.update()` calls `_cache_invalidate_prefix` on the success path only
    - A test asserts two load() calls within TTL produce one Firestore read
    - `python -m pytest tests/test_firestore_db.py -x -q -k "user_profile"` exits 0
  </acceptance_criteria>
  <done>UserProfileStore.load() is TTL-cached via the shared _READ_CACHE; update() invalidates; error contract preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add per-date cost-tripwire suppression store (BRAIN-04 gate)</name>
  <files>memory/firestore_db.py, tests/test_firestore_db.py</files>
  <read_first>
    - memory/firestore_db.py OutreachLogStore (lines 1709-1784) — per-date document-ID pattern
    - memory/firestore_db.py LLMUsageStore (llm_usage/{date} doc-per-day read shape) and JournalStore (journal/{date})
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"Analog 3 — per-date suppression store" and §"Per-date document-ID Firestore collections"
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md §"Code Examples" (OutreachLog-pattern per-date suppression snippet)
  </read_first>
  <action>
    Add a small `CostTripwireLogStore` class (collection `cost_tripwire_log`, document ID = the
    date string, lowercase-only collection name per project invariant) with two methods:
    `already_fired(self, date_str: str) -> bool` returning `snap.exists` (try/except returns
    False, never raises), and `mark_fired(self, date_str: str, summary: dict) -> None` that
    `set()`s `{"date": date_str, "fired_at": firestore.SERVER_TIMESTAMP, **summary}` (log-and-
    re-raise on write failure, matching OutreachLogStore.append discipline). This is a plain
    existence-check + set — no ArrayUnion needed (at most one alert per date). Do NOT wire it
    into heartbeat here; Plan 05 consumes it.
    Add a test asserting already_fired() is False before mark_fired() and True after (mock the
    Firestore doc get/set).
  </action>
  <verify>
    <automated>python -m pytest tests/test_firestore_db.py -x -q -k "tripwire"</automated>
  </verify>
  <acceptance_criteria>
    - memory/firestore_db.py defines `class CostTripwireLogStore` using collection `cost_tripwire_log`
    - It exposes `already_fired(self, date_str` and `mark_fired(self, date_str`
    - The collection name is all-lowercase (project casing invariant)
    - already_fired() returns False on read failure without raising
    - `python -m pytest tests/test_firestore_db.py -x -q -k "tripwire"` exits 0
  </acceptance_criteria>
  <done>CostTripwireLogStore provides per-date once-only suppression, ready for Plan 05's heartbeat wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App code → Firestore | All writes are server-side; no untrusted external input reaches these stores |
| LLMUsage cost data | Sensitive spend data; read only by heartbeat/tripwire on the same service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30.5-04 | Tampering / Integrity | LLMUsage cost fields | mitigate | firestore.Increment for concurrency-safe accumulation; additive schema, no migration; per-purpose cost added truthfully |
| T-30.5-05 | Denial of Service | Store read/write raising into a turn or cron | mitigate | Every new read returns {}/False on error and never raises; writes log-and-re-raise per existing store discipline |
| T-30.5-06 | Information Disclosure | Cost data collection casing (silent 404 on uppercase) | accept | Collection `cost_tripwire_log` is lowercase; single-user service, no cross-tenant exposure |
| T-30.5-SC | Tampering | pip installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_firestore_db.py -x -q` green
- `grep -n "cache_read_tokens\|_cache_key = (\"user_profile\"\|class CostTripwireLogStore" memory/firestore_db.py` shows all three
</verification>

<success_criteria>
Storage foundation ready: cache-aware + per-purpose-cost LLMUsage with per-date summary (feeds BRAIN-02/04), TTL-cached UserProfileStore (BRAIN-07), per-date tripwire suppression (BRAIN-04 gate).
</success_criteria>

<output>
Create `.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-02-SUMMARY.md` when done.
</output>
