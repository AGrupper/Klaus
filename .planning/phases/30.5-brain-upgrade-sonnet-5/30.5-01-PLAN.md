---
phase: 30.5-brain-upgrade-sonnet-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/tick_brain.py
  - tests/test_tick_brain.py
  - .github/workflows/deploy.yml
autonomous: false
requirements: [BRAIN-03]
user_setup: []

must_haves:
  truths:
    - "TickBrain builds its fallback client from TICK_BRAIN_FALLBACK_* env, never from SMART_AGENT_*"
    - "A forced Groq primary failure routes to gemini-3.5-flash, never claude-sonnet-5"
    - "The decoupling is deployed and live-verified BEFORE the brain model flip (Plan 06)"
  artifacts:
    - path: core/tick_brain.py
      provides: "TICK_BRAIN_FALLBACK_* decoupled fallback client construction"
      contains: "TICK_BRAIN_FALLBACK_BACKEND"
    - path: .github/workflows/deploy.yml
      provides: "TICK_BRAIN_FALLBACK_* env + secret wiring"
      contains: "TICK_BRAIN_FALLBACK_MODEL=gemini-3.5-flash"
    - path: tests/test_tick_brain.py
      provides: "Decoupled-fallback assertions (rewritten off SMART_AGENT_*)"
      contains: "TICK_BRAIN_FALLBACK"
  key_links:
    - from: core/tick_brain.py
      to: "TICK_BRAIN_FALLBACK_MODEL env"
      via: "os.getenv in __init__"
      pattern: "TICK_BRAIN_FALLBACK_MODEL"
---

<objective>
Decouple the tick-brain's Groq-failure fallback from the brain's `SMART_AGENT_*` env vars by
introducing explicit `TICK_BRAIN_FALLBACK_*` vars (default `gemini`/`gemini-3.5-flash`), and
deploy + live-verify this BEFORE the Plan 06 brain flip. This is the milestone's central
cost-safety fix (BRAIN-03): once `SMART_AGENT_MODEL` becomes `claude-sonnet-5`, every transient
Groq 429/500 would otherwise silently bill Sonnet rates (~10-30x Gemini Flash) on the free
always-on tick.

Purpose: Prevent silent Sonnet-rate billing on the free tick-brain path.
Output: Decoupled fallback client, rewritten tests, deploy.yml wiring, live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decouple TickBrain fallback onto TICK_BRAIN_FALLBACK_* env</name>
  <files>core/tick_brain.py, tests/test_tick_brain.py</files>
  <read_first>
    - core/tick_brain.py (current fallback construction at lines 116-129 reads SMART_AGENT_BACKEND/MODEL/API_KEY; the think() try/primary/except/fallback structure at lines 164-200; defensive env-parse pattern at lines 95-106; purpose-layering at 159-162)
    - tests/test_tick_brain.py (TestTickBrainConstructor at ~line 95 and TestTickBrainThink at ~line 171 currently assert the SMART_AGENT_*-coupled behavior — these must be rewritten, not merely extended)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md §"Code Examples" (verbatim test_forced_groq_failure_routes_to_decoupled_fallback shape) and §"Common Pitfalls" Pitfall 1
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"core/tick_brain.py::TickBrain.__init__"
  </read_first>
  <action>
    In TickBrain.__init__, replace the three fallback env reads (`SMART_AGENT_BACKEND`,
    `SMART_AGENT_MODEL`, `SMART_AGENT_API_KEY`) with `TICK_BRAIN_FALLBACK_BACKEND`,
    `TICK_BRAIN_FALLBACK_MODEL`, `TICK_BRAIN_FALLBACK_API_KEY`, plus an optional
    `TICK_BRAIN_FALLBACK_BASE_URL`. Default backend to `gemini` and model to `gemini-3.5-flash`
    when the vars are unset (so a fresh deploy is safe), but only construct the fallback client
    when a usable API key is present — preserve the existing `if backend and model and key: ...
    else: self._fallback_client = None / self._fallback_model = None` optional-client shape
    (the same shape used in AgentOrchestrator and reused in Plan 06). Keep `_fallback_model` set
    to the resolved model string so `think()`'s log line and the tests can assert it. Do NOT read
    any `SMART_AGENT_*` var in this module anymore. Update the module docstring at line ~29 that
    currently documents `SMART_AGENT_*` as the fallback source to name `TICK_BRAIN_FALLBACK_*`
    instead. Leave the primary Groq client, `_max_tokens`/`_temperature` parsing, and the
    think() primary/except/fallback control flow untouched.
    Then rewrite the affected tests in tests/test_tick_brain.py: constructor tests must set
    `TICK_BRAIN_FALLBACK_*` (not `SMART_AGENT_*`) and assert `_fallback_model == "gemini-3.5-flash"`;
    add a forced-failure test that patches the primary client's `chat` to raise `LLMError` and
    asserts the fallback client is invoked and `_fallback_model` is `gemini-3.5-flash` and never
    `claude-sonnet-5` (follow the RESEARCH.md Code Examples snippet exactly).
  </action>
  <verify>
    <automated>python -m pytest tests/test_tick_brain.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SMART_AGENT" core/tick_brain.py` returns no matches (module no longer reads brain env)
    - core/tick_brain.py contains `TICK_BRAIN_FALLBACK_BACKEND`, `TICK_BRAIN_FALLBACK_MODEL`, `TICK_BRAIN_FALLBACK_API_KEY`
    - core/tick_brain.py defaults the fallback model to `gemini-3.5-flash` when the env var is unset
    - tests/test_tick_brain.py contains a test asserting the forced-Groq-failure fallback model is `gemini-3.5-flash` and asserting it is not `claude-sonnet-5`
    - `python -m pytest tests/test_tick_brain.py -x -q` exits 0
  </acceptance_criteria>
  <done>TickBrain fallback reads only TICK_BRAIN_FALLBACK_* (default Gemini); rewritten tests prove a forced Groq failure routes to gemini-3.5-flash, never a Claude model.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TICK_BRAIN_FALLBACK_* into deploy.yml (ship-first, before the flip)</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (line 100 `--set-env-vars` single string; line 101 `--update-secrets` single string — live secret names are `klaus-gemini-key`, `klaus-anthropic-key`, `klaus-deepseek-key`)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §".github/workflows/deploy.yml" and §"Common Pitfalls" Pitfall 5 (clobber gotcha)
    - $HOME/.claude/projects/-Users-amitgrupper-Desktop-Klaus/memory/klaus_web_push_gotchas.md (deploy.yml --set-env-vars clobbers out-of-band Cloud Run env vars)
  </read_first>
  <action>
    In the `--set-env-vars` string on line 100, append
    `TICK_BRAIN_FALLBACK_BACKEND=gemini,TICK_BRAIN_FALLBACK_MODEL=gemini-3.5-flash` (place it
    adjacent to the existing `TICK_BRAIN_*` vars for readability). In the `--update-secrets`
    string on line 101, append `TICK_BRAIN_FALLBACK_API_KEY=klaus-gemini-key:latest` — the same
    secret that already backs `SMART_AGENT_API_KEY` today (Gemini's key), confirming RESEARCH
    Assumption A3. Do NOT touch `SMART_AGENT_*` in this plan — the brain flip and the
    `SMART_AGENT_FALLBACK_*` rename happen in Plan 06 only. This env addition must land in these
    two strings (never console-only) or the next deploy clobbers it.
  </action>
  <verify>
    <automated>grep -c "TICK_BRAIN_FALLBACK_MODEL=gemini-3.5-flash" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml line 100 `--set-env-vars` string contains `TICK_BRAIN_FALLBACK_BACKEND=gemini` and `TICK_BRAIN_FALLBACK_MODEL=gemini-3.5-flash`
    - .github/workflows/deploy.yml line 101 `--update-secrets` string contains `TICK_BRAIN_FALLBACK_API_KEY=klaus-gemini-key:latest`
    - `grep -n "SMART_AGENT_MODEL=claude-sonnet-5" .github/workflows/deploy.yml` returns no match (flip is Plan 06, not here)
    - `grep -c "TICK_BRAIN_FALLBACK_MODEL=gemini-3.5-flash" .github/workflows/deploy.yml` outputs 1
  </acceptance_criteria>
  <done>deploy.yml carries the TICK_BRAIN_FALLBACK_* env + secret; no SMART_AGENT flip present yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-verify forced Groq failure routes to Gemini (before the flip)</name>
  <what-built>Tick-brain fallback decoupled onto TICK_BRAIN_FALLBACK_* (Gemini), wired into deploy.yml, unit-verified that a forced Groq failure routes to gemini-3.5-flash and never a Claude model.</what-built>
  <how-to-verify>
    1. Merge/deploy this plan (push triggers the deploy.yml workflow → new klaus-agent-00NNN revision). Confirm the new revision came up and reads the new env (Cloud Run console → revision → Variables shows TICK_BRAIN_FALLBACK_MODEL=gemini-3.5-flash and the TICK_BRAIN_FALLBACK_API_KEY secret mapping).
    2. Force a Groq primary failure (per D-13, mechanism is your choice — e.g. a staging revision with an invalid TICK_BRAIN_API_KEY, or an injected error) and trigger an autonomous/heartbeat tick.
    3. Inspect logs / LLMUsage: the tick_fallback purpose entry must show `gemini-3.5-flash`, NEVER `claude-sonnet-5` or any Claude model.
    4. Confirm this is verified BEFORE running Plan 06 (the brain flip). This ordering is the phase's hard constraint (ROADMAP success criterion 3, D-13).
  </how-to-verify>
  <resume-signal>Type "approved" once the forced Groq failure logs a gemini-3.5-flash fallback, or describe what surfaced instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.yml → Cloud Run env | Env/secret mapping controls which model+key backs the tick fallback |
| Groq API → TickBrain | Untrusted provider availability; failure must route to the cheap tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30.5-01 | Tampering | tick-brain fallback env coupling | mitigate | Decouple onto TICK_BRAIN_FALLBACK_*; unit test asserts fallback model is gemini-3.5-flash, never a Claude model |
| T-30.5-02 | Information Disclosure / Cost | Wrong secret backing the fallback tier | mitigate | TICK_BRAIN_FALLBACK_API_KEY maps to klaus-gemini-key (Gemini's own key); verify live Cloud Run revision config post-deploy, not just YAML (clobber gotcha) |
| T-30.5-03 | Denial of Service (cost) | Silent Sonnet-rate billing on transient Groq errors post-flip | mitigate | Decouple + live-verify BEFORE the Plan 06 flip (D-13 ordering) |
| T-30.5-SC | Tampering | pip installs | mitigate | No new packages this plan; anthropic floor bump is Plan 03 (audited/approved in RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `python -m pytest tests/test_tick_brain.py -x -q` green
- `grep -n "SMART_AGENT" core/tick_brain.py` empty
- deploy.yml carries TICK_BRAIN_FALLBACK_* env + secret
- Live: forced Groq failure logs gemini-3.5-flash fallback (checkpoint)
</verification>

<success_criteria>
BRAIN-03 satisfied: tick-brain fallback is decoupled onto TICK_BRAIN_FALLBACK_*, deployed, and live-verified to route Groq failures to gemini-3.5-flash before any brain model flip.
</success_criteria>

<output>
Create `.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-01-SUMMARY.md` when done.
</output>
