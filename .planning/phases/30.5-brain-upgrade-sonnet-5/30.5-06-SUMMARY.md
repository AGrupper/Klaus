---
phase: 30.5-brain-upgrade-sonnet-5
plan: 06
subsystem: llm-integration
tags: [anthropic, claude-sonnet-5, brain-flip, fallback-chain, deploy.yml, self-manifest]

# Dependency graph
requires:
  - phase: 30.5-01
    provides: "TICK_BRAIN_FALLBACK_* decoupling, deployed + live-verified 2026-07-18 (D-13 hard-ordering satisfied)"
  - phase: 30.5-02
    provides: "LLMUsageStore.summary_for_date(); CostTripwireLogStore"
  - phase: 30.5-03
    provides: "claude-sonnet-5 pricing, cache_control + cache-token metering, MAX_TOKENS=16000"
  - phase: 30.5-04
    provides: "Slimmed SELF.md generator + de-prescribed smart_agent.md"
  - phase: 30.5-05
    provides: "Heartbeat daily-spend tripwire (check_daily_spend / _send_daily_spend_alert)"
provides:
  - "3-tier Sonnet->Gemini->Haiku fallback chain in AgentOrchestrator (smart_agent_tertiary, purpose=smart_fallback_2)"
  - "D-12 Klaus-voiced backup-reasoning disclosure line on the Gemini-fallback branch only"
  - "SMART_AGENT_FALLBACK_* Gemini compose fallback in morning_briefing._compose_briefing and nightly_review._compose_nightly"
  - "deploy.yml flipped: SMART_AGENT_BACKEND=anthropic/MODEL=claude-sonnet-5 (brain); SMART_AGENT_FALLBACK_*=gemini (2nd tier); new SMART_AGENT_TERTIARY_*=anthropic/claude-haiku-4-5 (3rd tier); KLAUS_DAILY_COST_ALERT=5"
  - "core/self_manifest.py Model Map defaults updated to post-flip truth + tick-brain-fallback row bug fix"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Optional-client-from-env shape reused a 4th time for smart_agent_tertiary (SMART_AGENT_TERTIARY_BACKEND/MODEL/API_KEY)"
    - "D-12 disclosure appended to response['text'] immediately after the Gemini-fallback .chat() call succeeds, before the tertiary branch is ever reached"
    - "2-tier Sonnet->Gemini compose-with-deterministic-fallback replicated verbatim from core/reflection.py::_brain_reflect into morning_briefing._compose_briefing and nightly_review._compose_nightly"

key-files:
  created: []
  modified:
    - core/main.py
    - core/morning_briefing.py
    - core/nightly_review.py
    - core/self_manifest.py
    - docs/SELF.md
    - .github/workflows/deploy.yml
    - tests/test_main.py
    - tests/test_morning_briefing.py
    - tests/test_nightly_review.py

key-decisions:
  - "Tertiary env-var naming resolved per RESEARCH.md Open Question 1: SMART_AGENT_FALLBACK_* keeps meaning '2nd tier' (now Gemini); a new SMART_AGENT_TERTIARY_* env set backs Haiku as the 3rd tier — matches the deploy.yml wiring exactly"
  - "D-12 disclosure text is appended directly after the successful smart_agent_fallback.chat() call, so it flows through into whatever response.text ultimately becomes (single-turn final answer or a tool-preamble) — never on the tertiary Haiku hop"
  - "core/self_manifest.py's stale 'Tick-brain fallback' Model Map row (previously rendered brain_model/brain_backend, a pre-existing Plan-01 coupling bug this row never got updated for) was fixed as a Rule-1 auto-fix: post-flip it would have falsely rendered 'claude-sonnet-5' as Klaus's own tick-brain fallback, directly contradicting Plan 01's TICK_BRAIN_FALLBACK_* decoupling. Now reads TICK_BRAIN_FALLBACK_MODEL/BACKEND"
  - "docs/SELF.md was regenerated via `python core/self_manifest.py` (never hand-edited) so the manifest can't drift from the generator's new defaults"

requirements-completed: [BRAIN-01]

# Metrics
duration: ~45min (Tasks 1-3; Task 4 is an open checkpoint, not executed)
completed: 2026-07-18
---

# Phase 30.5 Plan 06: Sonnet-5 Brain Flip Summary

**Sonnet-5 is now the configured brain across every conversation turn and paid proactive compose, with a verified-in-tests 3-tier Sonnet->Gemini->Haiku fallback chain (D-12 disclosure on the Gemini tier), a matching 2-tier Gemini fallback added to the morning-briefing and nightly-review composes, and deploy.yml/self_manifest.py flipped to the post-flip truth — Task 4's live production validation checklist is an open blocking checkpoint, not yet run.**

## Performance

- **Duration:** ~45 min (Tasks 1-3)
- **Completed:** 2026-07-18
- **Tasks:** 3 of 4 completed (Task 4 is `checkpoint:human-verify` gate="blocking" — see below)
- **Files modified:** 9

## Accomplishments

- `AgentOrchestrator.__init__` now constructs an optional third client `smart_agent_tertiary` from `SMART_AGENT_TERTIARY_BACKEND/MODEL/API_KEY`, using the identical env-optional-client shape already used 3x elsewhere (`smart_agent_fallback`, `TickBrain._fallback_client`).
- `_run_smart_loop`'s except-block chain now nests a `purpose="smart_fallback_2"` attempt on the tertiary client inside the existing `except LLMError as fallback_exc` branch, before returning `CONNECTIVITY_ERROR_TEXT`; `increment_fallback_counter()` stays in its own silent try/except; purpose labels layer `smart` -> `smart_fallback` -> `smart_fallback_2`.
- D-12: when the Gemini fallback (`purpose="smart_fallback"`) succeeds, a Klaus-voiced backup-reasoning disclosure line (`FALLBACK_DISCLOSURE_TEXT`) is appended to the response text — only on that branch, never on the tertiary Haiku hop.
- `core/morning_briefing.py::_compose_briefing` and `core/nightly_review.py::_compose_nightly` each gained a second `try/except` — a `SMART_AGENT_FALLBACK_*` Gemini compose attempted before the deterministic `_plain_text_fallback`, replicating `core/reflection.py::_brain_reflect`'s 2-tier shape exactly. `nightly_review` preserves `purpose="nightly_review"` on both the brain and fallback calls. Both fallback attempts are silent-try/except — a Gemini-fallback failure can never crash either cron.
- `.github/workflows/deploy.yml` flipped: `SMART_AGENT_BACKEND=anthropic,SMART_AGENT_MODEL=claude-sonnet-5` (brain); `SMART_AGENT_FALLBACK_BACKEND=gemini,SMART_AGENT_FALLBACK_MODEL=gemini-3.5-flash` (2nd tier, was Haiku); new `SMART_AGENT_TERTIARY_BACKEND=anthropic,SMART_AGENT_TERTIARY_MODEL=claude-haiku-4-5` (3rd tier); `KLAUS_DAILY_COST_ALERT=5`. Secrets remapped: `SMART_AGENT_API_KEY=klaus-anthropic-key:latest`, `SMART_AGENT_FALLBACK_API_KEY=klaus-gemini-key:latest`, new `SMART_AGENT_TERTIARY_API_KEY=klaus-anthropic-key:latest`.
- `core/self_manifest.py`'s Model Map default strings updated to post-flip truth (`SMART_AGENT_MODEL` default `claude-sonnet-5`, fallback-row default `gemini-3.5-flash`); `docs/SELF.md` regenerated via the generator (never hand-edited).

## Task Commits

Each task was committed atomically. Tasks 1 and 2 followed the plan's `tdd="true"` RED/GREEN cycle:

1. **Task 1 RED: failing tests for 3-tier fallback + D-12 disclosure** - `be63ae7` (test)
2. **Task 1 GREEN: extend AgentOrchestrator to 3-tier Sonnet->Gemini->Haiku + disclosure** - `287f720` (feat)
3. **Task 2 RED: failing tests for SMART_AGENT_FALLBACK_* Gemini compose fallback** - `9ad44bd` (test)
4. **Task 2 GREEN: extend Gemini fallback to morning/nightly composes** - `eb8390b` (feat)
5. **Task 3: flip deploy.yml env/secrets + true up self_manifest defaults** - `1b462cd` (feat)

Task 4 is a `checkpoint:human-verify` (`gate="blocking"`) — no commit; see below.

## Files Created/Modified

- `core/main.py` - New `FALLBACK_DISCLOSURE_TEXT` constant; `AgentOrchestrator.__init__` builds `smart_agent_tertiary` from `SMART_AGENT_TERTIARY_*` env (None-safe); `_run_smart_loop` nests a tertiary Haiku attempt (`purpose="smart_fallback_2"`) inside the existing fallback except-block, appends the D-12 disclosure on Gemini-fallback success only.
- `core/morning_briefing.py` - `_compose_briefing` gains a second try/except attempting a `SMART_AGENT_FALLBACK_*` Gemini compose before `_plain_text_fallback`.
- `core/nightly_review.py` - `_compose_nightly` gains the identical second try/except, preserving `purpose="nightly_review"`.
- `core/self_manifest.py` - Model Map defaults updated (`SMART_AGENT_MODEL` default `claude-sonnet-5`, fallback default `gemini-3.5-flash`); fixed the stale "Tick-brain fallback" row to read `TICK_BRAIN_FALLBACK_MODEL/BACKEND` instead of `brain_model/brain_backend` (Rule 1 auto-fix — see Deviations).
- `docs/SELF.md` - Regenerated via `python core/self_manifest.py`; Model Map now shows `claude-sonnet-5` brain / `gemini-3.5-flash` fallback / `gemini-3.5-flash` tick-brain-fallback.
- `.github/workflows/deploy.yml` - `--set-env-vars` and `--update-secrets` strings flipped per the plan's exact spec (brain -> Anthropic/Sonnet-5, fallback -> Gemini, new tertiary -> Haiku, `KLAUS_DAILY_COST_ALERT=5` added).
- `tests/test_main.py` - Added `TestThreeTierFallbackChain` (6 tests): Sonnet-error->Gemini-with-disclosure, Sonnet+Gemini-error->Haiku (`smart_fallback_2`, no disclosure), all-three-fail->`CONNECTIVITY_ERROR_TEXT`, tertiary-None->`CONNECTIVITY_ERROR_TEXT` after Gemini fails, tertiary client constructed from env, tertiary None when env absent.
- `tests/test_morning_briefing.py` - Added 2 tests: brain-fail->Gemini-fallback text (not the plain template), brain+fallback-fail->plain template.
- `tests/test_nightly_review.py` - Added 2 tests: same brain-fail->Gemini-fallback / brain+fallback-fail->plain-template pair for `_compose_nightly`.

## Decisions Made

- Resolved RESEARCH.md's Open Question 1 (3-tier env-var naming): kept `SMART_AGENT_FALLBACK_*` meaning "2nd tier" (now Gemini) and introduced a new `SMART_AGENT_TERTIARY_*` env set for Haiku — matches the plan's own recommendation and the deploy.yml wiring exactly.
- D-12 disclosure is appended immediately after the successful `smart_agent_fallback.chat()` call (mutating `response["text"]` in place) rather than at the point of final return, so it flows correctly through both the "final text, no tool calls" path and the "text + tool_calls" preamble path without a second code branch.
- Morning-briefing's fallback compose call omits an explicit `purpose=` kwarg to match the primary brain call's existing (unlabeled, `purpose=""`) behavior exactly — this plan's scope was adding the fallback tier, not introducing new LLMUsage labeling for a call site that didn't have one before.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed a pre-existing staleness bug in self_manifest.py's "Tick-brain fallback" Model Map row**
- **Found during:** Task 3 (Model Map defaults update)
- **Issue:** The row rendered `brain_model`/`brain_backend` (i.e. whatever backs `SMART_AGENT_*`) as the tick-brain's fallback model — a coupling bug left over from before Plan 01's `TICK_BRAIN_FALLBACK_*` decoupling. Post-flip, this would have caused `docs/SELF.md` to falsely claim Klaus's tick-brain fallback is `claude-sonnet-5`/Anthropic, directly contradicting Plan 01's verified decoupling (tick-brain fallback is actually `gemini-3.5-flash` via `TICK_BRAIN_FALLBACK_MODEL`). This is squarely within this task's blast radius — the flip is exactly what would have exposed the bug's visible symptom.
- **Fix:** Added `tick_fallback_model`/`tick_fallback_backend` reading `TICK_BRAIN_FALLBACK_MODEL`/`TICK_BRAIN_FALLBACK_BACKEND` (defaulting to `gemini-3.5-flash`/`gemini`), and pointed the Model Map row at those instead of `brain_model`/`brain_backend`.
- **Files modified:** `core/self_manifest.py`, `docs/SELF.md` (regenerated)
- **Verification:** `docs/SELF.md`'s Model Map now shows `| Tick-brain fallback | gemini-3.5-flash | Gemini (AI Studio) |` — correct and no longer coupled to the brain model.
- **Committed in:** `1b462cd` (part of Task 3's commit)

No other deviations — Tasks 1-3 executed per their `<action>` blocks, with the two D-16/D-12 implementation judgment calls (tertiary env-var naming, disclosure placement) resolved as described above under Decisions Made, both already anticipated as "Claude's Discretion" / "Open Question" territory in RESEARCH.md.

## Issues Encountered

- Confirmed via `/Users/amitgrupper/Desktop/Klaus/.venv/bin/python -m pytest tests/test_main.py tests/test_morning_briefing.py tests/test_nightly_review.py tests/test_pricing.py tests/test_llm_client.py tests/test_heartbeat.py tests/test_tick_brain.py -q` (all green, per-file per the project's documented full-suite-segfault caveat) — no regressions in any file this plan or its prior siblings (Plans 01/03/04/05) touched.
- Both TDD tasks followed the full RED/GREEN discipline: tests committed first with the implementation reverted (`git stash`), confirmed to fail for the expected reason (`ImportError`/wrong-branch-taken assertion failures), then the implementation was restored and tests re-run green before the GREEN commit.

## User Setup Required

**External live deployment + validation required — Task 4 is an open blocking checkpoint (see below).** Per the plan's `user_setup` frontmatter: `claude-sonnet-5` must be confirmed enabled on the Anthropic account backing `klaus-anthropic-key` before this plan's deploy.yml changes are merged and deployed.

## Next Phase Readiness

- Tasks 1-3 (code + deploy wiring + manifest truth) are complete, committed, and unit-tested. All new/modified tests pass; the full per-file regression sweep above shows no breakage in any sibling-plan file.
- **Task 4 is NOT executed** — see Checkpoint below. This is the phase's final gate; once it passes, Phase 30.5 (BRAIN-01..07, all 7 requirements) is complete.
- No blockers beyond the open checkpoint itself.

---
*Phase: 30.5-brain-upgrade-sonnet-5*
*Completed: 2026-07-18 (Tasks 1-3 only; Task 4 checkpoint open)*

## Task 4 Checkpoint (D-14 live validation) — OPEN, NOT EXECUTED

**Type:** human-verify
**Gate:** blocking

This task requires an actual merged deploy against the real `klaus-agent` Cloud Run service and
live production checks — it cannot be executed or meaningfully auto-approved from an unmerged
worktree. Per the plan:

**Precondition already satisfied:** Plan 01's `TICK_BRAIN_FALLBACK_*` decoupling was deployed and
live-verified 2026-07-18 (evidence: `30.5-01-SUMMARY.md` "Task 3 Checkpoint Closure" section —
Cloud Run rev `klaus-agent-00156-7hv` env diff clean, forced Groq failure at two ticks routed to
Gemini with zero anthropic/claude mentions in the log window, key restored via revs 00159-00160).
D-13's hard-ordering constraint is satisfied — this plan's flip is cleared to deploy.

**What must happen next (left for the orchestrator/user to execute after merge):**
1. Confirm `claude-sonnet-5` access is enabled on the Anthropic account backing
   `klaus-anthropic-key` (a single scratch `messages.create` against `claude-sonnet-5` returning
   200). If access is missing, STOP — this blocks the flip.
2. Merge and deploy this plan; confirm the live Cloud Run revision's Variables show
   `SMART_AGENT_MODEL=claude-sonnet-5` and the remapped secrets (not just the YAML — clobber
   gotcha).
3. Run the D-14 live checklist on production:
   - One Telegram turn — Klaus answers on Sonnet (LLMUsage shows `purpose=smart` on
     `claude-sonnet-5`).
   - One hub turn via `/internal/process-hub-message` — same.
   - One tool-heavy coaching question — tools fire, no temperature/thinking 400 errors, no
     max_tokens truncation. Record the observed end-to-end turn latency and confirm it stays
     under `LLM_TIMEOUT_SECONDS=120s`.
   - One forced Anthropic failure (e.g. staging bad key) — confirm the Gemini fallback answers a
     conversation turn AND the D-12 backup-reasoning disclosure line appears.
   - Next paid compose window (morning briefing / nightly review) with the same
     forced-Anthropic-failure condition — confirm the compose is served by the Gemini fallback
     (LLMUsage shows `gemini-3.5-flash` on that purpose), NOT the deterministic plain-text
     template.
   - Next day: compare LLMUsage day totals against the Anthropic console (within ~10%, BRAIN-02),
     confirm `cache_read_tokens` are non-zero on turn 2+ of a conversation.
4. Rollback if needed is an env-var flip back to Gemini (D-15, Amit's judgment call) — no
   predefined auto-rollback thresholds.

**Resume signal:** Type "approved" once the live checklist passes, or describe what surfaced.

## Self-Check: PASSED

- FOUND: core/main.py
- FOUND: core/morning_briefing.py
- FOUND: core/nightly_review.py
- FOUND: core/self_manifest.py
- FOUND: docs/SELF.md
- FOUND: .github/workflows/deploy.yml
- FOUND: tests/test_main.py
- FOUND: tests/test_morning_briefing.py
- FOUND: tests/test_nightly_review.py
- FOUND: .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-06-SUMMARY.md
- FOUND commit: be63ae7 (Task 1 RED)
- FOUND commit: 287f720 (Task 1 GREEN)
- FOUND commit: 9ad44bd (Task 2 RED)
- FOUND commit: eb8390b (Task 2 GREEN)
- FOUND commit: 1b462cd (Task 3)
