---
phase: 30.5-brain-upgrade-sonnet-5
plan: 05
type: execute
wave: 3
depends_on: ["30.5-02", "30.5-03"]
files_modified:
  - core/heartbeat.py
  - prompts/cost_tripwire.md
  - tests/test_heartbeat.py
autonomous: true
requirements: [BRAIN-04]
user_setup: []

must_haves:
  truths:
    - "The first heartbeat after midnight computes yesterday's total LLM cost and, if over KLAUS_DAILY_COST_ALERT, alerts Amit once"
    - "The alert is Klaus-summarized and brain-composed (Sonnet frames deterministic numbers; template fallback if compose fails)"
    - "A malformed KLAUS_DAILY_COST_ALERT never crashes the heartbeat cron"
    - "The alert fires at most once per date (per-date suppression)"
  artifacts:
    - path: core/heartbeat.py
      provides: "check_daily_spend tripwire: yesterday summary read, threshold compare, per-date suppression, brain-compose + template fallback"
      contains: "KLAUS_DAILY_COST_ALERT"
    - path: prompts/cost_tripwire.md
      provides: "Klaus-voiced cost-summary compose prompt (numbers supplied, never computed by the model)"
    - path: tests/test_heartbeat.py
      provides: "Tripwire once-per-day, threshold, template-fallback tests"
      contains: "tripwire"
  key_links:
    - from: "core/heartbeat.py check_daily_spend"
      to: "LLMUsageStore.summary_for_date(yesterday)"
      via: "store read"
      pattern: "summary_for_date"
    - from: "core/heartbeat.py check_daily_spend"
      to: "CostTripwireLogStore.already_fired/mark_fired"
      via: "per-date suppression"
      pattern: "already_fired"
---

<objective>
Add the heartbeat daily-spend tripwire (BRAIN-04): the first heartbeat tick after midnight reads
yesterday's total LLM cost from `LLMUsageStore.summary_for_date`, and if it exceeds
`KLAUS_DAILY_COST_ALERT` (default $5, env-tunable, defensively parsed), sends Amit a single
Klaus-summarized alert — total, top 2-3 cost drivers by purpose, cache-hit rate, and Klaus's own
read on whether it looks like normal usage or something broken (D-03). The alert text is
brain-composed by one Sonnet call that only phrases the deterministically-computed numbers (D-04),
with a deterministic plain-text template fallback if the compose fails. Per-date suppression
(CostTripwireLogStore from Plan 02) enforces once-per-day (D-01).

Purpose: Cost safety net + transparency, matching Klaus's cost-transparent identity.
Output: check_daily_spend tripwire wired into heartbeat, compose prompt, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-RESEARCH.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-02-SUMMARY.md
@.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-03-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 02 (memory/firestore_db.py) — consume directly -->
LLMUsageStore.summary_for_date(date_str) -> dict   # day doc incl. total_cost_usd, {purpose}_cost_usd, cache-token totals
CostTripwireLogStore.already_fired(date_str) -> bool
CostTripwireLogStore.mark_fired(date_str, summary: dict) -> None
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prompts/cost_tripwire.md + check_daily_spend composer with template fallback</name>
  <files>core/heartbeat.py, prompts/cost_tripwire.md</files>
  <behavior>
    - check_daily_spend reads yesterday's summary; if total_cost_usd > threshold and not already fired for that date, it composes + returns an alert and marks fired
    - If total_cost_usd <= threshold, no alert and no fired-mark
    - If already fired for the date, no second alert
    - A malformed KLAUS_DAILY_COST_ALERT falls back to the $5 default without raising
    - If the Sonnet compose call fails, a deterministic plain-text template alert is used instead
  </behavior>
  <read_first>
    - core/heartbeat.py (check_degradation checker shape ~379-406; _compose_message/_plain_text_fallback LLM-compose-with-fallback pattern ~654-700; _collect_signals registration ~717-731; the stale-streak gotcha note)
    - core/tick_brain.py lines 95-106 (defensive int/float env parse try/except ValueError — mirror for KLAUS_DAILY_COST_ALERT)
    - prompts/heartbeat.md (sibling compose prompt — style reference for prompts/cost_tripwire.md)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"Daily-spend tripwire" (Analogs 1-3) and §"LLM-compose-with-deterministic-fallback"
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-CONTEXT.md D-01..D-04
  </read_first>
  <action>
    Create `prompts/cost_tripwire.md`: a Klaus-voiced (JARVIS register) system prompt that takes a
    JSON payload of PRE-COMPUTED numbers (yesterday's total cost, top 2-3 purposes by cost with
    amounts, cache-hit rate, the threshold) and asks Klaus to phrase a short alert plus his own
    read on whether it looks like normal heavy usage or something broken — explicitly instruct
    that the numbers are supplied and must NOT be recomputed or invented.
    In core/heartbeat.py, add `check_daily_spend()`: defensively parse `KLAUS_DAILY_COST_ALERT`
    (float, try/except → default 5.0, mirroring tick_brain's env-parse), compute yesterday's date,
    read `LLMUsageStore.summary_for_date(yesterday)`; if `total_cost_usd` <= threshold, return None
    (no alert). Otherwise, if `CostTripwireLogStore.already_fired(yesterday)` is True, return None.
    Else derive the deterministic payload (total, top-N `{purpose}_cost_usd` drivers, cache-hit
    rate = cache_read_tokens / (in_tokens + cache_read_tokens) or similar), compose via a helper
    that mirrors `_compose_message`/`_plain_text_fallback` but points at `SMART_AGENT_*` (Sonnet)
    with `prompts/cost_tripwire.md` and `purpose="cost_tripwire"` (so the alert's own cost is
    auditable), falling back to a deterministic plain-text template if the compose fails. The
    whole function is wrapped so it never raises into the cron. Do NOT send the message here —
    return the composed text (Task 2 wires sending + suppression marking), OR mark-fired + send
    within this function if that matches the existing heartbeat structure; keep the mark-fired
    strictly gated on successful send (mirror D-10 OutreachLog gating).
  </action>
  <verify>
    <automated>python -m pytest tests/test_heartbeat.py -x -q -k "tripwire or spend"</automated>
  </verify>
  <acceptance_criteria>
    - prompts/cost_tripwire.md exists and instructs the model to phrase (not compute) the supplied numbers
    - core/heartbeat.py defines `def check_daily_spend` reading `LLMUsageStore.summary_for_date` and comparing against a defensively-parsed `KLAUS_DAILY_COST_ALERT` (default 5.0)
    - A malformed `KLAUS_DAILY_COST_ALERT` value does not raise (test covers it)
    - The composer uses `purpose="cost_tripwire"` and falls back to a deterministic template on compose failure
    - `python -m pytest tests/test_heartbeat.py -x -q -k "tripwire or spend"` exits 0
  </acceptance_criteria>
  <done>check_daily_spend computes yesterday's spend, brain-composes a Klaus-voiced alert with template fallback, defensively parses the threshold, and never raises.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the tripwire into the heartbeat cron with once-per-day suppression + deploy.yml threshold</name>
  <files>core/heartbeat.py, tests/test_heartbeat.py</files>
  <read_first>
    - core/heartbeat.py (the hourly cron entrypoint / where checkers and send actions are invoked; _collect_signals ~717-731; the send path used by heartbeat alerts)
    - .planning/phases/30.5-brain-upgrade-sonnet-5/30.5-PATTERNS.md §"Wiring into the cron entrypoint" (tripwire is NOT a Signal checker — it is a separate guarded once-daily action gated by per-date suppression)
    - core/scheduled_message.py (send_and_inject — the Telegram send + conversation injection the alert should use, gating mark_fired on send success per D-10)
  </read_first>
  <action>
    Invoke `check_daily_spend()` as a SEPARATE guarded call inside the heartbeat cron handler
    (not inside `_collect_signals` — it is not a Signal-emitting ops checker; it is a once-daily
    paid-compose-and-send action). Because per-date suppression via `CostTripwireLogStore` makes
    the exact trigger hour irrelevant, it may be checked on every hourly tick — the first tick
    after yesterday's data is final sends; subsequent ticks no-op via `already_fired`. Send the
    composed alert through the same send path heartbeat already uses (send_and_inject), and call
    `CostTripwireLogStore.mark_fired(yesterday, summary)` ONLY after a successful send (mirror the
    OutreachLog D-10 gating — no suppression record if delivery failed, so it retries next tick).
    Add `KLAUS_DAILY_COST_ALERT=5` to the deploy.yml `--set-env-vars` string — WAIT: deploy.yml is
    owned by Plans 01 and 06 to avoid conflicts; instead, add a note in this plan's SUMMARY that
    Plan 06 must include `KLAUS_DAILY_COST_ALERT=5` in the deploy.yml env string (the code defaults
    to 5.0 so a missing env var is safe, but the env var should be present for tunability). Do NOT
    edit deploy.yml in this plan.
    Add tests: once-per-day suppression (second invocation after mark_fired sends nothing),
    mark_fired only on successful send (a send failure leaves already_fired False).
  </action>
  <verify>
    <automated>python -m pytest tests/test_heartbeat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - check_daily_spend is invoked from the heartbeat cron handler outside _collect_signals
    - The alert is sent via the existing send path and mark_fired is called only after a successful send
    - A second heartbeat tick on the same date sends no second alert (test asserts)
    - A simulated send failure leaves already_fired False (retry-safe; test asserts)
    - This plan does NOT modify .github/workflows/deploy.yml (Plan 06 adds KLAUS_DAILY_COST_ALERT=5)
    - `python -m pytest tests/test_heartbeat.py -x -q` exits 0
  </acceptance_criteria>
  <done>The tripwire fires at most once per date, gated on successful delivery, wired into the hourly heartbeat; deploy.yml env addition deferred to Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KLAUS_DAILY_COST_ALERT env → heartbeat | Untrusted-shaped numeric threshold read via os.getenv |
| Tripwire compose → Anthropic (paid) | The alert itself costs money; must be bounded |
| LLMUsage cost data → Telegram | Spend numbers surfaced to Amit (owner-only channel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30.5-13 | Denial of Service | Malformed KLAUS_DAILY_COST_ALERT crashing the heartbeat cron | mitigate | Defensive float-parse with $5 default (mirror tick_brain env-parse); test covers malformed value |
| T-30.5-14 | Denial of Service (cost) | Tripwire compose becoming an unbounded paid loop | mitigate | Once-per-date suppression (CostTripwireLogStore) bounds to <=1 paid compose/day; deterministic template fallback avoids retry storms; mark_fired gated on send success |
| T-30.5-15 | Repudiation | Alert cost invisible in metering | mitigate | Compose call uses purpose="cost_tripwire" so its own cost is recorded/auditable |
| T-30.5-16 | Information Disclosure | Spend data leakage | accept | Sent only to Amit's allowlisted Telegram (owner-only), no new surface |
| T-30.5-SC | Tampering | pip installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_heartbeat.py -x -q` green
- `grep -n "KLAUS_DAILY_COST_ALERT\|summary_for_date\|already_fired" core/heartbeat.py` present
- prompts/cost_tripwire.md exists
</verification>

<success_criteria>
BRAIN-04 satisfied: a once-daily, per-date-suppressed, defensively-thresholded, brain-composed (with template fallback) spend tripwire fires when yesterday's cost exceeds KLAUS_DAILY_COST_ALERT.
</success_criteria>

<output>
Create `.planning/phases/30.5-brain-upgrade-sonnet-5/30.5-05-SUMMARY.md` when done (flag the KLAUS_DAILY_COST_ALERT=5 deploy.yml addition for Plan 06).
</output>
