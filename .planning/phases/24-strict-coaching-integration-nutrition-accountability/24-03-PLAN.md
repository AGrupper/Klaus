---
phase: 24-strict-coaching-integration-nutrition-accountability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core/tools.py
  - core/main.py
  - tests/test_tools.py
  - tests/test_main.py
autonomous: true
requirements: [COACH-03, COACH-04, COACH-05]
must_haves:
  truths:
    - "read_coaching_guide returns the not-found JSON when a query word matches more than one section (no wrong-section fed as authoritative)"
    - "read_coaching_guide returns the correct section only when exactly one section matches an unambiguous (≥4-char) word"
    - "A data-heavy coaching query returns a single substantive message, not the fallback-plus-answer double-send"
    - "The smart-loop iteration cap is high enough (12) for a 6-tool data-heavy coaching turn"
  artifacts:
    - path: "core/tools.py"
      provides: "hardened _handle_read_coaching_guide fuzzy match (WR-02)"
      contains: "candidate_anchors"
    - path: "core/main.py"
      provides: "MAX_TOOL_ITERATIONS=12 + last-substantive-text fallback (double-send fix)"
      contains: "last_response_text"
  key_links:
    - from: "_run_smart_loop loop exhaustion"
      to: "return last_response_text"
      via: "substantive (>100 char) text suppresses the sentinel fallback"
      pattern: "last_response_text"
    - from: "_handle_read_coaching_guide fuzzy fallback"
      to: "not-found JSON"
      via: "len(candidate_anchors) != 1 → skip word"
      pattern: "len(candidate_anchors)"
---

<objective>
Land the two folded-todo fixes that Phase 24's far-more-data-heavy coaching paths would otherwise regress: harden `read_coaching_guide` fuzzy matching so an ambiguous word never feeds a wrong section to the brain as authoritative (WR-02), and fix the smart-loop double-send by raising the tool-iteration cap to 12 and returning the last substantive text instead of the fallback at exhaustion.

Purpose: Phase 24 makes both crons and reactive chat materially more data-heavy (macro adherence, fueling-slot math, skip-deficit math, benchmark facets) — 6+ tool calls per coaching turn. Without these fixes the cap-8 double-send regresses further and the strict-coaching contexts call `read_coaching_guide` more often, raising the wrong-section risk. These are infrastructure correctness fixes that the Wave-2 cron+chat coaching behavior depends on; anti-fabrication (SC-1) must still hold.

Output: hardened `_handle_read_coaching_guide` in `core/tools.py`; `MAX_TOOL_ITERATIONS=12` + last-substantive-text fallback in `core/main.py`; tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md
@.planning/todos/pending/coaching-query-iteration-cap-double-send.md
@.planning/todos/pending/phase-22-code-review-advisory.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-02 — read_coaching_guide unambiguous fuzzy match</name>
  <files>core/tools.py, tests/test_tools.py</files>
  <read_first>
    - core/tools.py lines 1500-1543 (_handle_read_coaching_guide — slug normalization at 1506-1509 which is T-22-04 mitigation and must NOT change; fuzzy fallback loop 1531-1541; not-found return 1543)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md Finding 7 (current bug + count-of-candidates fix shape) + Security Domain (T-22-04 preserved)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md § "_handle_read_coaching_guide (MODIFY — WR-02 hardening)" full replacement excerpt
    - .planning/todos/pending/phase-22-code-review-advisory.md (WR-02 detail)
  </read_first>
  <behavior>
    - querying a slug whose word matches exactly one section anchor returns that section's content JSON
    - querying a slug whose word ("set" from "top-set-strength") matches multiple section anchors returns the not-found JSON (NOT the first hit)
    - short words (< 4 chars) are skipped (do not over-match)
    - when no word yields a unique match, the not-found JSON is returned so the brain falls back to the slim core
  </behavior>
  <action>
    Replace the fuzzy-fallback loop body (core/tools.py lines 1531-1541) with a count-of-candidates approach: for each `word in slug.split("-")`, skip if `not word or len(word) < 4`; compile an anchor-only regex `<!-- SECTION: [^>]*{escaped word}[^>]* -->` (IGNORECASE), `findall` it, and ONLY proceed when `len(candidate_anchors) == 1` (unambiguous). On a unique match, compile the full section-capture regex (DOTALL|IGNORECASE) and return `{"topic": slug, "content": <captured>}`. If a word matches multiple or zero anchors, skip it and try the next word. Leave the existing not-found return at line 1543 unchanged (`{"error": "Section '...' not found in COACHING_GUIDE.md"}`). Do NOT touch the slug normalization at lines 1506-1509 (T-22-04 path-traversal mitigation). Reuse the module's existing `re` import alias (`_re`).

    Add tests to `tests/test_tools.py`: an ambiguous-word case (asserts not-found JSON, not a section), a short-word-skip case, and an unambiguous-match case (asserts correct section content). Build the guide content as an in-test string with multiple `<!-- SECTION: ... -->` anchors so no file dependency.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tools.py -x -q -k "coaching_guide"</automated>
  </verify>
  <acceptance_criteria>
    - `core/tools.py` fuzzy fallback uses a candidate count (verify: `grep -n 'candidate_anchors\|findall' core/tools.py`) and a `len(word) < 4` skip (verify: `grep -n 'len(word) < 4' core/tools.py`)
    - the slug normalization block at lines ~1506-1509 (T-22-04) is unchanged
    - an ambiguous word returns the not-found JSON (asserted in test); an unambiguous word returns the correct section (asserted)
    - `pytest tests/test_tools.py -x -q -k "coaching_guide"` exits 0
  </acceptance_criteria>
  <done>Ambiguous fuzzy queries return not-found (brain falls back to slim core); only an unambiguous ≥4-char word returns a section; T-22-04 preserved; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Double-send fix — cap 12 + last-substantive-text fallback</name>
  <files>core/main.py, tests/test_main.py</files>
  <read_first>
    - core/main.py line 44 (MAX_TOOL_ITERATIONS = 8) and the smart-loop body lines 488-668 (loop entry 543, response_text extraction ~579, no-tool-call early return 582-583, exhaustion fallback 661-668)
    - core/main.py lines 51-54 comment + CLAUDE.md §6 — the exact fallback sentinel string is asserted by tests/test_autonomous.py::test_sentinel_substring_matches_main_constant and MUST NOT change
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md Finding 8 (cap raise rationale + substantive-suppression fix) + Pitfall list
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md § "_run_smart_loop (MODIFY)" full excerpt incl. the IMPORTANT note that the sentinel string stays intact
    - .planning/todos/pending/coaching-query-iteration-cap-double-send.md (full root-cause + acceptance: single correct message, SC-1 still holds)
  </read_first>
  <behavior>
    - MAX_TOOL_ITERATIONS is 12
    - when the loop hits the cap and the last iteration produced a substantive (> 100 char) response_text alongside tool calls, that text is returned (single message), NOT the apologetic fallback
    - when the loop hits the cap with no substantive text produced, the exact existing sentinel fallback string is still returned (test_sentinel_substring_matches_main_constant stays green)
    - the no-tool-call early-return path is unchanged
  </behavior>
  <action>
    Change `MAX_TOOL_ITERATIONS = 8` to `MAX_TOOL_ITERATIONS = 12` at core/main.py line 44 (rationale comment: data-heavy Phase-24 coaching queries need up to ~6 tool calls). Introduce a `last_response_text = ""` tracker before the `for iteration in range(MAX_TOOL_ITERATIONS)` loop; inside the loop, after `response_text = response["text"]`, set `if response_text: last_response_text = response_text`. At loop exhaustion (lines 661-668), BEFORE returning the sentinel fallback string, add: `if last_response_text and len(last_response_text) > 100: logger.warning(...); return last_response_text`. The existing apologetic sentinel string return stays exactly as-is after that guard (it is asserted verbatim by test_autonomous; do not edit its text). This suppresses the double-send because the substantive brain-produced answer is returned instead of discarding it and re-emitting the fallback. Anti-fabrication (SC-1) holds because the returned text was produced by the brain, not synthesized here.

    Add tests to `tests/test_main.py`: assert `MAX_TOOL_ITERATIONS == 12`; a cap-exhaustion-with-substantive-text case returning the text (mock the loop to produce >100-char text + tool calls each iteration); a cap-exhaustion-with-no-text case returning the unchanged sentinel string. Confirm `tests/test_autonomous.py::test_sentinel_substring_matches_main_constant` still passes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_main.py -x -q -k "tool_iterations or double_send or smart_loop" && python -m pytest tests/test_autonomous.py -x -q -k "sentinel"</automated>
  </verify>
  <acceptance_criteria>
    - `core/main.py` has `MAX_TOOL_ITERATIONS = 12` (verify: `grep -n 'MAX_TOOL_ITERATIONS = 12' core/main.py`)
    - the exhaustion path tracks and can return `last_response_text` guarded by `len(...) > 100` (verify: `grep -n 'last_response_text' core/main.py` returns ≥ 3 hits)
    - the apologetic sentinel string text is byte-identical to before (verify: `python -m pytest tests/test_autonomous.py -x -q -k "sentinel"` exits 0)
    - `pytest tests/test_main.py -x -q -k "tool_iterations or double_send or smart_loop"` exits 0
  </acceptance_criteria>
  <done>Cap is 12; a substantive answer at exhaustion is returned once (no double-send); the sentinel string is unchanged and its guard test passes; SC-1 holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram (allowlisted user) → smart loop / read_coaching_guide | User query reaches tool-iteration loop + coaching-guide section lookup |
| read_coaching_guide → COACHING_GUIDE.md filesystem | Section slug used to select content fed to the brain as authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Tampering | read_coaching_guide path traversal (T-22-04) | mitigate | the existing slug normalization + regex-only matching (lines 1506-1509) is preserved unchanged; the WR-02 fix only narrows the fuzzy fallback, no filesystem path concat introduced |
| T-24-09 | Tampering | wrong-section fed as authoritative coaching source | mitigate | WR-02: only an unambiguous single-anchor match returns content; ambiguous/zero → not-found JSON → brain falls back to slim core (prevents a misleading section being cited as fact, supporting SC-1 anti-fabrication) |
| T-24-10 | Elevation of Privilege | MAX_TOOL_ITERATIONS raise increases LLM cost surface | accept | brain is already gated by TELEGRAM_ALLOWED_USER_IDS; 8→12 is a marginal per-turn cost increase for a single allowlisted user; no external-facing cost amplification |
| T-24-11 | Spoofing | returning last_response_text could surface a fabricated number | mitigate | returned text is produced by the brain under the Tier A/B recency contract (Phase 22), not synthesized in the fallback path; the >100-char guard returns the brain's own answer, not invented content — SC-1 preserved |
| T-24-SC | Tampering | npm/pip/cargo installs | N/A | no new packages introduced in this plan (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `python -m pytest tests/test_tools.py tests/test_main.py -x -q` green
- `python -m pytest tests/test_autonomous.py -x -q -k "sentinel"` green (sentinel string unchanged)
- `grep -n 'MAX_TOOL_ITERATIONS = 12' core/main.py` and `grep -n 'candidate_anchors' core/tools.py` both return a hit
</verification>

<success_criteria>
- A data-heavy coaching query returns one correct message, no double-send (folded todo acceptance)
- An ambiguous read_coaching_guide query never feeds a wrong section (WR-02; supports SC-1)
- Anti-fabrication SC-1 preserved; sentinel-string guard test green; baseline preserved
</success_criteria>

<output>
Create `.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-03-SUMMARY.md` when done.
</output>
