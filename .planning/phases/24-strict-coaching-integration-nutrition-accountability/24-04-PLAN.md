---
phase: 24-strict-coaching-integration-nutrition-accountability
plan: 04
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - core/proactive_alerts.py
  - prompts/proactive_alert.md
  - prompts/smart_agent.md
  - tests/test_proactive_alerts.py
autonomous: true
requirements: [COACH-03, COACH-04, COACH-05, NUTR-01, NUTR-02, NUTR-03]
must_haves:
  truths:
    - "The 21:30 check-in flags nutrition (macro gap + fueling-slot structural miss + supplement rider) using the Wave-1 helpers, gathered from MealStore + anchors"
    - "A coaching topic already raised today is filtered out of the 21:30 compose context (cross-cron dedup), and new topics are written to CoachingTopicStore only after send succeeds"
    - "Skip pushback names the session + concrete-unit deficit + directional consequence with no softening; recovery conflict gives exactly one ranked rec + 'your call, Sir'"
    - "Reactive chat always answers fully and never burns a cron topic"
  artifacts:
    - path: "core/proactive_alerts.py"
      provides: "_gather_nutrition_data + dedup-gate wiring + post-send topic write in run_proactive_alerts"
      contains: "def _gather_nutrition_data"
    - path: "prompts/proactive_alert.md"
      provides: "strict pushback + recovery single-rec + nutrition accountability + dedup semantics"
    - path: "prompts/smart_agent.md"
      provides: "reactive strict-pushback format + reactive-never-suppressed clarification"
  key_links:
    - from: "run_proactive_alerts"
      to: "CoachingTopicStore.has_topic / add_topic"
      via: "filter before compose, write after send"
      pattern: "CoachingTopicStore"
    - from: "_gather_nutrition_data"
      to: "_macro_gap_check / _detect_slot_misses / _resolve_anchor_times"
      via: "Wave-1 pure helpers on MealStore + Garmin data"
      pattern: "_detect_slot_misses"
---

<objective>
Wire the Wave-1 primitives into the 21:30 proactive-alerts cron and author the strict-coaching prompt behavior: nutrition + fueling-slot + supplement accountability gathered and composed (NUTR-01/02/03), the cross-cron dedup gate enforced in the 21:30 compose path with post-send write discipline (COACH-05), and the strict skip-pushback / recovery single-ranked-rec format (COACH-03/04) in `proactive_alert.md` plus the reactive-chat counterpart in `smart_agent.md`.

Purpose: The 21:30 check-in is the primary accountability cron (D-08) where the day's sessions resolve — strict pushback and nutrition accountability fire here first. The dedup gate must live in this compose path so the same topic isn't repeated by the morning briefing / weekly review.

Output: `_gather_nutrition_data` helper + dedup-gate filter + post-send `add_topic` write in `core/proactive_alerts.py`; strict-coaching + nutrition + dedup semantics in `prompts/proactive_alert.md`; reactive strict-pushback format in `prompts/smart_agent.md`; integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-01-SUMMARY.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-02-SUMMARY.md

<interfaces>
<!-- Consumed from Wave 1 — do not redefine, import and call. -->
From memory/firestore_db.py (Plan 01): CoachingTopicStore(project_id, database).has_topic(date,key) / add_topic(date,key) / topics_today(date)
From core/proactive_alerts.py (Plan 02): MACRO_THRESHOLDS, SLOT_SUPPLEMENTS, _resolve_anchor_times(today_iso, garmin_activities, calendar_events), _map_meals_to_slots(meals, am, pm), _detect_slot_misses(meals, am, pm, today_date), _macro_gap_check(totals, day_type, targets)
From memory/firestore_db.py: MealStore.get_day(date) / get_day_aggregate(date)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _gather_nutrition_data + dedup gate wiring in run_proactive_alerts</name>
  <files>core/proactive_alerts.py, tests/test_proactive_alerts.py</files>
  <read_first>
    - core/proactive_alerts.py: run_proactive_alerts (line 151), the gather block + alerts_context build (to ~283), compute_recovery_concern call (276-278), _compose_alert call (285), _mark_processed (289 + def 308), _already_sent (197 + def 297)
    - core/morning_briefing.py lines 174-301 (_gather_data best-effort gather-each-source pattern to mirror for _gather_nutrition_data)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md § "core/proactive_alerts.py → NEW _gather_nutrition_data helper + run_proactive_alerts integration" (gather pattern, dedup-gate insertion point, post-send write) + § "Shared Patterns" (best-effort gather, Asia/Jerusalem date key, post-send write discipline, _make_firestore_client factory)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md Finding 6 (exact integration points + run_training_checkin-before-_already_sent note) + Pitfall 1 (write-after-send) + Pitfall 7 (get_day returns []) + Open Question 2 (reuse fetched garmin to avoid second API call)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md D-01..D-04 (topic-key vocabulary, hard-suppress + one escalation, proactive-only, daily reset) + D-08..D-12
  </read_first>
  <behavior>
    - _gather_nutrition_data returns a dict with meals, macro_totals, macro_gaps, slot_misses, am_anchor, pm_anchor; on a no-meal day (get_day returns []) it returns a missing-data context, not a crash (Pitfall 7)
    - run_proactive_alerts attaches nutrition_data to alerts_context and detects topic keys (protein-miss, carb-miss[:long-run-day], fueling-miss:{slot}, skipped-session:{type}, recovery-conflict:{subject})
    - a topic already in CoachingTopicStore.topics_today(today_il) is excluded from the new-topics list fed to compose; un-raised topics are included
    - CoachingTopicStore.add_topic is called ONLY after send_and_inject succeeds (write-after-send, Pitfall 1) — never before
    - the Jerusalem-time date key is used for all CoachingTopicStore calls
    - dedup gate failure (store raises) does not crash the cron (fail-open: topics fire)
  </behavior>
  <action>
    Add `_gather_nutrition_data(today_iso, garmin_activities=None) -> dict` to `core/proactive_alerts.py`, mirroring `morning_briefing._gather_data`'s best-effort wrapped-source style. Fetch `MealStore.get_day(today_iso)` (Pitfall 7: returns [], guard `if not meals`) + `get_day_aggregate(today_iso)["totals"]`; resolve anchors via `_resolve_anchor_times(today_iso, garmin_activities, calendar_events)` reusing the garmin list already fetched in the cron (Open Question 2 — do NOT make a second `fetch_garmin_activities` call); compute `_detect_slot_misses(...)` and `_macro_gap_check(totals, day_type, targets)` where `day_type` is derived from the garmin long-run check (distance>16000 or running+duration>4200) and the weekly_split rest-day check from `UserProfileStore.load()`, and `targets` come from `UserProfileStore.load()['nutrition_targets']` (150g/350g blueprint). Return the dict shape in the interfaces block.

    In `run_proactive_alerts`: after the existing gather (after ~line 283) and before `_compose_alert` (line 285), (1) call `_gather_nutrition_data(today_iso, garmin_activities=<already-fetched list>)` wrapped best-effort and attach to `alerts_context["nutrition"]`; (2) build a `_detected_topics` list from the nutrition gaps/slot-misses + the existing skip/recovery context (skip → `skipped-session:{type}`, recovery → `recovery-conflict:{subject}`, fueling → `fueling-miss:{slot}`, macro → `protein-miss`/`carb-miss[:long-run-day]`); (3) construct `CoachingTopicStore` via the local `_make_firestore_client` factory, compute `_today_il = datetime.now(_TZ).date().isoformat()`, filter to `coaching_topics_new = [t for t in _detected_topics if not cts.has_topic(_today_il, t)]` and set `alerts_context["coaching_topics_new"]` + `coaching_topics_already_raised` (wrap fail-open — on error let all topics fire). After `send_and_inject` succeeds and before/with `_mark_processed`, loop `cts.add_topic(_today_il, t)` for each topic in `coaching_topics_new` (Pitfall 1: write-after-send only), wrapped best-effort (non-fatal). Do NOT move or alter the `_already_sent` gate (line 197) — the new topic gate is finer-grained and sits in the compose path. Do NOT construct a new AgentOrchestrator (use the singleton via existing `_get_orchestrator()` for the coaching guide, already injected by `_compose_alert`).

    Add integration tests to `tests/test_proactive_alerts.py`: a no-meal-day _gather_nutrition_data case (missing-data, no crash); a dedup case asserting an already-raised topic is excluded from `coaching_topics_new`; a write-after-send case asserting `add_topic` is NOT called when send fails and IS called after a successful send. Mock MealStore / CoachingTopicStore / send_and_inject.
  </action>
  <verify>
    <automated>python -m pytest tests/test_proactive_alerts.py -x -q -k "nutrition or dedup or gather or topic"</automated>
  </verify>
  <acceptance_criteria>
    - `core/proactive_alerts.py` defines `_gather_nutrition_data` and references `CoachingTopicStore`, `_macro_gap_check`, `_detect_slot_misses`, `_resolve_anchor_times` (verify: `grep -n 'def _gather_nutrition_data\|CoachingTopicStore\|_detect_slot_misses' core/proactive_alerts.py`)
    - CoachingTopicStore.add_topic is invoked only on the post-send path (verify: the `add_topic` call is lexically after the `send_and_inject` success branch; test asserts no add_topic on send failure)
    - the date key uses `datetime.now(_TZ)` Jerusalem time, not UTC (verify: `grep -n '_TZ' core/proactive_alerts.py` around the CoachingTopicStore calls)
    - `_already_sent` gate at line ~197 is unchanged
    - `pytest tests/test_proactive_alerts.py -x -q -k "nutrition or dedup or gather or topic"` exits 0
  </acceptance_criteria>
  <done>21:30 cron gathers nutrition + fueling + supplement context, enforces the cross-cron dedup gate in the compose path, and writes topics only after a successful send; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict-pushback + nutrition + dedup prompt behavior (proactive_alert.md + smart_agent.md)</name>
  <files>prompts/proactive_alert.md, prompts/smart_agent.md, tests/test_proactive_alerts.py</files>
  <read_first>
    - prompts/proactive_alert.md (current content — Phase 22 {coaching_guide} integration + Phase 23 benchmark framing already present)
    - prompts/smart_agent.md (current content — Tier A/B recency contract + specificity bar + critique posture from Phase 22 D-13)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md § "Prompt files (MODIFY — 4 files)" (the proactive_alert.md + smart_agent.md additions)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md D-05 (directional consequence, no dated projection), D-06 (escalate on repeat misses), D-07 (exactly one ranked rec + "your call, Sir", never a menu), D-09/D-10/D-11 (nutrition framing), D-12 (structural critique distinct), D-03 (reactive never suppressed) + <specifics> example registers
    - .planning/REQUIREMENTS.md Out of Scope rows (no dated projection — Phase 25; no daily macro micro-optimization; advise-never-override)
  </read_first>
  <behavior>
    - the prompt instructs: skip pushback names the session + concrete-unit deficit (km/sets) + a directional blueprint-anchored consequence, with NO softening/hedging and NO dated "N weeks behind" projection (that is Phase 25)
    - recovery-vs-plan conflict produces exactly ONE ranked recommendation + explicit "your call, Sir" — never a menu, never dictating
    - nutrition flags use the gathered macro_gaps / slot_misses + supplement riders (slot #2 → D3+K2/Omega-3; #5 → Creatine; #6 pre-bed standalone), framed as structural misses not daily micro-tweaks
    - dedup: topics in coaching_topics_already_raised are not repeated unless materially worsened (one escalation referencing the earlier flag — D-02)
    - smart_agent.md: reactive coaching uses the same strict format AND reactive answers are never suppressed by cron topics and do not burn a topic (D-03)
  </behavior>
  <action>
    Edit `prompts/proactive_alert.md` to add, in the existing coaching section style: (a) a strict skip-pushback block — name the session, state the deficit in concrete units from the gathered data, give a directional blueprint-anchored consequence; explicitly forbid softening/hedging and forbid any dated "N weeks behind" projection (Phase 25 scope); escalate tone on repeated misses (D-06). (b) a recovery-conflict block — cite the biometric fact + number, state the plan conflict, give EXACTLY ONE ranked recommendation, end with "your call, Sir"; forbid menus and forbid dictating (D-07). (c) a nutrition-accountability block — surface `nutrition.macro_gaps` and `nutrition.slot_misses` as structural misses with their supplement riders from SLOT_SUPPLEMENTS framing (D-10/D-11); keep the structural target-critique (e.g. "150g protein is low for your volume") distinct and pattern-triggered, not a nightly nag (D-12); never do daily macro micro-optimization (out of scope). (d) dedup semantics — "if `coaching_topics_already_raised` is non-empty, do not repeat those topics; reference one only if its condition has materially worsened with new data (D-02)". Use the verbatim example registers from CONTEXT <specifics> as the target tone.

    Edit `prompts/smart_agent.md` to add the reactive counterpart: the same strict-pushback + single-ranked-rec format for in-chat coaching queries (D-05/06/07), and an explicit clarification that reactive chat always answers fully, is never suppressed by what a cron already said, and does not burn a cron topic (D-03). Anchor numbers to the Tier A/B recency contract already in the file — Klaus may only state measured numbers within the recency window.

    Add a prompt-content assertion test to `tests/test_proactive_alerts.py` (read the prompt file, assert the strict-pushback / single-rec / "your call, Sir" / dedup-semantics markers are present and that no dated-projection language was introduced).
  </action>
  <verify>
    <automated>python -m pytest tests/test_proactive_alerts.py -x -q -k "prompt or skip_pushback or recovery or supplement"</automated>
  </verify>
  <acceptance_criteria>
    - `prompts/proactive_alert.md` contains the "your call, Sir" single-ranked-rec instruction and a no-softening skip-pushback instruction (verify: `grep -ni 'your call, Sir' prompts/proactive_alert.md`; `grep -ni 'one' prompts/proactive_alert.md` near the recovery block)
    - `prompts/proactive_alert.md` references the supplement riders and forbids daily macro micro-optimization (verify: `grep -ni 'D3\|Creatine\|Mg-Glycinate\|structural' prompts/proactive_alert.md`)
    - `prompts/proactive_alert.md` includes dedup semantics for already-raised topics (verify: `grep -ni 'already' prompts/proactive_alert.md`)
    - `prompts/smart_agent.md` states reactive chat is never suppressed by cron topics (verify: `grep -ni 'reactive\|never suppress\|always answer' prompts/smart_agent.md`)
    - no dated-projection language introduced (verify: `grep -ni 'weeks behind\|on track for' prompts/proactive_alert.md` returns nothing, or only in a "do NOT do this" prohibition line)
    - `pytest tests/test_proactive_alerts.py -x -q -k "prompt or skip_pushback or recovery or supplement"` exits 0
  </acceptance_criteria>
  <done>The 21:30 prompt enforces strict pushback, single-ranked recovery rec, structural nutrition + supplement framing, and dedup semantics; smart_agent.md mirrors the reactive format and the never-suppress rule; no dated projection leaked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MealStore / Garmin / UserProfileStore → 21:30 compose context | Server-side data assembled into alerts_context JSON sent to the brain |
| brain compose → Telegram (allowlisted user) | Strict-coaching message delivered; CoachingTopicStore written after send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-12 | Tampering | false dedup block (write-before-send) | mitigate | Pitfall 1: CoachingTopicStore.add_topic called only after send_and_inject succeeds — a crash between write and send cannot create a topic key with no corresponding message |
| T-24-13 | Spoofing | strict pushback states a fabricated deficit/number | mitigate | numbers come only from the gathered MealStore/Garmin/blueprint data under the Tier A/B recency contract; the prompt forbids inventing numbers (SC-1); deficit units are derived in the gather layer, not invented at compose |
| T-24-14 | Denial of Service | nutrition/dedup gather failure crashes the 21:30 cron | mitigate | every new gather + gate call is best-effort wrapped (fail-open) mirroring the existing cron discipline — a single store/Garmin failure degrades to silent-omit, never crashes |
| T-24-15 | Information Disclosure | meal/biometric data in compose context logged | mitigate | error logs reference topic keys + dates only; the alerts_context JSON is sent to the brain (already the existing trust model), not written to logs |
| T-24-16 | Repudiation | over-suppression silently drops a real worsening issue | mitigate | D-02 one-escalation rule in the prompt: a materially-worsened already-raised topic gets a single escalation reference rather than total silence |
| T-24-SC | Tampering | npm/pip/cargo installs | N/A | no new packages introduced in this plan (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `python -m pytest tests/test_proactive_alerts.py -x -q` green (all Phase-24 proactive tests)
- `grep -n 'def _gather_nutrition_data' core/proactive_alerts.py` and `grep -ni 'your call, Sir' prompts/proactive_alert.md` both return a hit
- Manual (UAT, per VALIDATION.md): live 21:30 with a skipped session shows named session + concrete deficit + directional consequence, no hedging; an HRV<baseline-vs-top-set conflict yields one ranked rec + "your call, Sir"
</verification>

<success_criteria>
- 21:30 flags structural fueling-slot misses from MealStore timestamps mapped to the 6 slots (SC-4)
- Skip pushback is strict with concrete-unit deficit + directional consequence, no softening (SC-1/COACH-03)
- Recovery conflict = one ranked rec + "your call, Sir", never a menu (SC-2/COACH-04)
- The same topic does not repeat across crons same-day (SC-3 substrate enforced here)
- Reactive chat always answers fully and never burns a cron topic (COACH-05/D-03)
</success_criteria>

<output>
Create `.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-04-SUMMARY.md` when done.
</output>
