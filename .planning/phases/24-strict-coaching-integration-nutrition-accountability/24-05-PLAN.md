---
phase: 24-strict-coaching-integration-nutrition-accountability
plan: 05
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - core/morning_briefing.py
  - core/weekly_training_review.py
  - prompts/morning_briefing.md
  - prompts/weekly_training_review.md
  - tests/test_morning_briefing.py
  - tests/test_weekly_training_review.py
autonomous: true
requirements: [COACH-05, PROG-01, PROG-03, PROG-04]
must_haves:
  truths:
    - "The morning briefing frames today's named session + recovery state + relevant fueling reminder as ONE integrated block, not three labeled lines"
    - "The morning briefing recaps an unresolved prior-day miss using yesterday's coaching topics, at reduced priority, subject to dedup"
    - "The Sunday weekly review reports per-facet within-block status (strength top-set trend, threshold volume vs target, ACWR) with block-relative framing and a session-quality trend from the annotated log — NO dated projection"
    - "Both crons honor the cross-cron dedup gate: a topic already raised today is not repeated, and topics are written only after send succeeds"
  artifacts:
    - path: "core/morning_briefing.py"
      provides: "coaching_topics_today/yesterday gather + post-send topic write"
      contains: "coaching_topics_yesterday"
    - path: "core/weekly_training_review.py"
      provides: "dedup gate gather/write + {coaching_guide} injection in _compose_review"
      contains: "CoachingTopicStore"
    - path: "prompts/morning_briefing.md"
      provides: "D-18 integrated block + prior-day recap framing"
    - path: "prompts/weekly_training_review.md"
      provides: "per-facet within-block status + quality trend framing (no projection)"
  key_links:
    - from: "morning_briefing._gather_data"
      to: "CoachingTopicStore.topics_today(today) + topics_today(yesterday)"
      via: "dedup + prior-day recap"
      pattern: "coaching_topics_yesterday"
    - from: "weekly_training_review._compose_review"
      to: "_get_orchestrator()._coaching_guide_content"
      via: "{coaching_guide} injection (new for this cron)"
      pattern: "coaching_guide"
---

<objective>
Fold strict, specific coaching into the morning briefing and the Sunday weekly review: the morning briefing weaves today's named session + recovery + fueling reminder into one integrated block and recaps an unresolved prior-day miss (PROG-03, D-18, D-08); the weekly review reports per-facet within-block status + a session-quality trend with block-relative framing (PROG-01, PROG-04, D-17). Both crons honor the cross-cron dedup gate (COACH-05) with post-send write discipline.

Purpose: These two crons complete the "coaching is folded into every touchpoint" goal. The morning briefing and weekly review must not repeat topics the 21:30 check-in already raised (dedup), must surface the derived session-quality field (Plan 01), and must stay within-block only — no dated projection (Phase 25).

Output: dedup gather/write in both crons + `{coaching_guide}` injection in `_compose_review`; D-18 integrated block + prior-day recap in `prompts/morning_briefing.md`; per-facet + quality-trend framing in `prompts/weekly_training_review.md`; tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-01-SUMMARY.md

<interfaces>
<!-- Consumed from Wave 1 — import and call, do not redefine. -->
From memory/firestore_db.py (Plan 01): CoachingTopicStore(project_id, database).has_topic(date,key) / add_topic(date,key) / topics_today(date)
training_log entries now carry a `quality` field ("strong"|"neutral"|"grind"|None) written by Plan 01.
Existing: morning_briefing._gather_data (line 174), _compose_briefing (308); weekly_training_review._gather_week_data (42), _compose_review (227); _get_orchestrator()._coaching_guide_content
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Morning briefing — dedup + prior-day recap gather + post-send write + D-18 prompt</name>
  <files>core/morning_briefing.py, prompts/morning_briefing.md, tests/test_morning_briefing.py</files>
  <read_first>
    - core/morning_briefing.py: _gather_data (line 174) incl. the Phase-23 block gather block (~278-299) as the best-effort analog, return data (301); run_morning_briefing (122) + send_and_inject success path (~140); _compose_briefing (308) incl. {coaching_guide} injection (~317-328)
    - prompts/morning_briefing.md (current content — block framing + coaching guide already present)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md § "core/morning_briefing.py → _gather_data (MODIFY)" (the new gather blocks, post-send write, D-18 note) + § "Shared Patterns" (Asia/Jerusalem date key, post-send discipline)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md D-18 (one integrated block) + D-08 (prior-day unresolved-miss recap) + D-02 (dedup hard-suppress + one escalation) + D-03 (proactive-only gate)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md Finding 9 (today vs yesterday topics; prior-day recap reads YESTERDAY's doc)
  </read_first>
  <behavior>
    - _gather_data adds `coaching_topics_today` (from topics_today(today_iso)) and `coaching_topics_yesterday` (from topics_today(yesterday_iso)) — yesterday's drives the D-08 prior-day recap; both fail-open to [] on error
    - run_morning_briefing writes any included coaching topics to CoachingTopicStore AFTER send succeeds (write-after-send)
    - the morning_briefing.md prompt frames today's named session + recovery state + fueling reminder as ONE integrated block (not three labeled lines) and surfaces an unresolved prior-day miss at reduced priority (one contextual line, no nag)
    - the prompt does not repeat a topic already in coaching_topics_today unless materially worsened (D-02)
  </behavior>
  <action>
    In `core/morning_briefing.py` `_gather_data`, after the existing block gather and before `return data`, add a best-effort wrapped block (mirror the Phase-23 block-gather style) that constructs `CoachingTopicStore` via env vars (`GCP_PROJECT_ID` + `FIRESTORE_DATABASE` default "(default)"), computes `yesterday_iso = (date.fromisoformat(today_iso) - timedelta(days=1)).isoformat()`, and sets `data["coaching_topics_today"] = cts.topics_today(today_iso)` and `data["coaching_topics_yesterday"] = cts.topics_today(yesterday_iso)`; fail-open both to []. Ensure `timedelta` is imported.

    In `run_morning_briefing`, after `send_and_inject` succeeds (~line 140), add a best-effort wrapped loop that writes any `today_data.get("coaching_topics_included")` topic keys via `cts.add_topic(today_iso, t)` (write-after-send discipline, Pitfall 1). Use the Jerusalem-day key consistent with the cron's existing date handling.

    Edit `prompts/morning_briefing.md`: instruct the brain to weave today's named session (from calendar/block), the recovery state (recovery_concern/garmin), and the single most relevant fueling reminder into ONE integrated block — not three separate labeled sections (D-18). Add a prior-day recap instruction: if `coaching_topics_yesterday` contains an unresolved miss still relevant today, surface it as one low-priority contextual line, never a nag (D-08); and a dedup instruction: do not repeat a topic in `coaching_topics_today` unless materially worsened (D-02).

    Add tests to `tests/test_morning_briefing.py`: assert `_gather_data` returns `coaching_topics_today` and `coaching_topics_yesterday` keys (mock CoachingTopicStore); assert the post-send write path calls add_topic only after a successful send; a prompt-content assertion that the integrated-block + prior-day-recap instructions are present.
  </action>
  <verify>
    <automated>python -m pytest tests/test_morning_briefing.py -x -q -k "integrated or coaching or topic or prior"</automated>
  </verify>
  <acceptance_criteria>
    - `core/morning_briefing.py` `_gather_data` sets `coaching_topics_today` and `coaching_topics_yesterday` (verify: `grep -n 'coaching_topics_today\|coaching_topics_yesterday' core/morning_briefing.py`)
    - add_topic is on the post-send path in run_morning_briefing (verify: the add_topic loop is lexically after the send_and_inject success branch; test asserts no write on send failure)
    - `prompts/morning_briefing.md` contains an integrated-block instruction and a prior-day recap instruction (verify: `grep -ni 'integrated\|one block\|prior\|yesterday' prompts/morning_briefing.md`)
    - `pytest tests/test_morning_briefing.py -x -q -k "integrated or coaching or topic or prior"` exits 0
  </acceptance_criteria>
  <done>Morning briefing gathers today+yesterday coaching topics, recaps unresolved prior-day misses at reduced priority, frames one integrated block, and writes topics only after send; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weekly review — per-facet + quality trend + dedup + {coaching_guide}; prompt framing</name>
  <files>core/weekly_training_review.py, prompts/weekly_training_review.md, tests/test_weekly_training_review.py</files>
  <read_first>
    - core/weekly_training_review.py: _gather_week_data (line 42) incl. training_log via get_range + block_benchmarks gather (~200-222); _compose_review (227) incl. prompt read + meal_audit append (242-264, note it does NOT currently inject {coaching_guide}); run_weekly_review (299) + send_and_inject (~312)
    - prompts/weekly_training_review.md (current content — Phase 23 block/benchmark framing present)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md § "core/weekly_training_review.py → _gather_week_data (MODIFY)" (no new gather code for quality; dedup gather/write; add {coaching_guide} injection) + § "Shared Patterns" → "LLM composition with {coaching_guide} injection"
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md D-17 (within-block status + trend ONLY, no deadline projection) + D-12 (structural critique dedup) + D-02/D-03
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md Finding 10 (training_log.quality already present after Plan 01; block_benchmarks per-facet already gathered; only prompt + dedup + guide-injection changes) + Open Question 3 (week distribution + block trend framing)
    - .planning/REQUIREMENTS.md Out of Scope (no dated projection — Phase 25 PROG-02)
  </read_first>
  <behavior>
    - _gather_week_data / _compose_review surface `coaching_topics_today` for dedup and the training_log list now carries `quality` (no new gather code needed for quality — it is present in the doc)
    - _compose_review injects `{coaching_guide}` (new for this cron — mirror morning_briefing._compose_briefing)
    - run_weekly_review writes included coaching topics to CoachingTopicStore after send succeeds
    - the weekly_training_review.md prompt reports per-facet within-block status (strength top-set trend, threshold volume vs target, ACWR) with block-relative framing AND a session-quality trend (e.g. "this week: 3 strong, 2 neutral, 1 grind") from training_log[].quality, gracefully handling null quality; it does NOT compute any dated "on track / N weeks behind" projection (Phase 25)
  </behavior>
  <action>
    In `core/weekly_training_review.py`: in `_gather_week_data` (or at the top of `_compose_review`/`run_weekly_review`) add a best-effort wrapped CoachingTopicStore construction and set `week_data["coaching_topics_today"] = cts.topics_today(today_iso)` (fail-open []). No new gather code for quality — `get_range` already returns training_log entries that now include the `quality` field (Finding 10). In `_compose_review`, add `{coaching_guide}` injection mirroring `morning_briefing._compose_briefing` (try `_get_orchestrator()._coaching_guide_content`, fail-open to "" with a warning, then `.replace("{coaching_guide}", ...)`); keep the existing `{today_date}` replace and the meal_audit append intact. In `run_weekly_review` after `send_and_inject` succeeds, write `week_data.get("coaching_topics_included")` topics via `cts.add_topic(today_iso, t)` best-effort (write-after-send).

    Edit `prompts/weekly_training_review.md`: instruct per-facet within-block reporting — strength top-set trend, threshold volume vs target, ACWR — all block-relative ("Week N of 16, Block X"); add a session-quality-trend instruction using `training_log[].quality` (count distribution this week + block trend when block data exists; ignore null-quality sessions gracefully — Open Question 3). EXPLICITLY forbid any dated deadline projection / "on track for Oct" / "N weeks behind" framing (that is Phase 25 PROG-02) — Phase 24 reports current/within-block movement only. Add dedup semantics for `structural-critique:*` topics (don't repeat a topic already raised today; D-12/D-02). Now that `{coaching_guide}` is injected, the prompt may reference it for the strict per-facet framing.

    Add tests to `tests/test_weekly_training_review.py`: assert `coaching_topics_today` is gathered; assert `_compose_review` injects coaching_guide (the `{coaching_guide}` placeholder is replaced); a prompt-content assertion that per-facet + quality-trend instructions are present and that no dated-projection language was introduced.
  </action>
  <verify>
    <automated>python -m pytest tests/test_weekly_training_review.py -x -q -k "facet or quality or coaching or guide or topic"</automated>
  </verify>
  <acceptance_criteria>
    - `core/weekly_training_review.py` references `CoachingTopicStore` and injects `{coaching_guide}` in compose (verify: `grep -n 'CoachingTopicStore\|coaching_guide' core/weekly_training_review.py`)
    - add_topic is on the post-send path (verify: lexically after send_and_inject success; test asserts no write on send failure)
    - `prompts/weekly_training_review.md` contains per-facet + session-quality-trend instructions (verify: `grep -ni 'facet\|quality\|top-set\|ACWR' prompts/weekly_training_review.md`)
    - no dated-projection language introduced (verify: `grep -ni 'weeks behind\|on track for' prompts/weekly_training_review.md` returns nothing, or only in a "do NOT do this" prohibition line)
    - `pytest tests/test_weekly_training_review.py -x -q -k "facet or quality or coaching or guide or topic"` exits 0
  </acceptance_criteria>
  <done>Weekly review reports per-facet within-block status + session-quality trend (block-relative, no projection), injects the coaching guide, and honors the dedup gate with post-send writes; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoachingTopicStore / TrainingLogStore / BenchmarkStore → cron compose context | Server-side data assembled into the briefing/review JSON sent to the brain |
| brain compose → Telegram (allowlisted user) | Coaching message delivered; CoachingTopicStore written after send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-17 | Tampering | false dedup block (write-before-send) | mitigate | Pitfall 1: add_topic only after send_and_inject succeeds in both crons — no orphan topic keys |
| T-24-18 | Denial of Service | coaching-topic / guide gather failure crashes a cron | mitigate | all new gather + guide-injection calls are best-effort wrapped (fail-open to [] / "") mirroring existing cron discipline |
| T-24-19 | Spoofing | weekly review states a fabricated per-facet trend or projects to a deadline | mitigate | trends computed from training_log/BenchmarkStore data under the Tier A/B contract; prompt explicitly forbids dated projection (Phase 25 fence) and forbids inventing numbers (SC-1); null quality handled gracefully, never fabricated |
| T-24-20 | Information Disclosure | quality/biometric data in logs | mitigate | error logs reference topic keys + dates only; quality is a 3-value enum; biometrics already part of the existing review trust model, not newly logged |
| T-24-21 | Repudiation | morning briefing silently drops an unresolved prior-day miss | mitigate | D-08 prior-day recap instruction surfaces an unresolved yesterday topic as one low-priority line rather than total silence |
| T-24-SC | Tampering | npm/pip/cargo installs | N/A | no new packages introduced in this plan (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `python -m pytest tests/test_morning_briefing.py tests/test_weekly_training_review.py -x -q` green
- `grep -n 'coaching_topics_yesterday' core/morning_briefing.py` and `grep -n 'coaching_guide' core/weekly_training_review.py` both return a hit
- Manual (UAT, per VALIDATION.md): morning briefing shows session+recovery+fueling as one block; weekly review shows per-facet within-block status + quality distribution with no dated projection
</verification>

<success_criteria>
- Morning briefing frames one integrated block + prior-day recap (SC-5/PROG-03, D-18/D-08)
- Weekly review reports per-facet within-block status + session-quality trend, block-relative, no projection (SC-6/PROG-01/PROG-04, D-17)
- Both crons honor the cross-cron dedup gate with post-send writes (SC-3/COACH-05)
- Baseline preserved (911+); no dated-projection language leaked (Phase 25 fence held)
</success_criteria>

<output>
Create `.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-05-SUMMARY.md` when done.
</output>
