---
phase: 24-strict-coaching-integration-nutrition-accountability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/proactive_alerts.py
  - tests/test_proactive_alerts.py
autonomous: true
requirements: [NUTR-01, NUTR-02, NUTR-03]
must_haves:
  truths:
    - "Protein below the meaningful-gap threshold produces a macro-gap flag; a marginal shortfall does not (no daily micro-optimization)"
    - "A missed post-AM-run reload slot is detected from MealStore timestamps mapped to the AM-run anchor — only when an AM run actually happened"
    - "Slot misses do NOT fire on a rest day with no resolved training anchor (Pitfall 2)"
    - "A missed structural slot carries its supplement rider context (slot #2 → D3+K2/Omega-3; slot #6 pre-bed → Mg/Zn/Cu standalone)"
  artifacts:
    - path: "core/proactive_alerts.py"
      provides: "MACRO_THRESHOLDS dict + _resolve_anchor_times + _map_meals_to_slots + _detect_slot_misses + _macro_gap_check pure helpers"
      contains: "def _detect_slot_misses"
    - path: "tests/test_proactive_alerts.py"
      provides: "NUTR-01/02/03 pure-function unit tests"
  key_links:
    - from: "_detect_slot_misses"
      to: "MealStore meal timestamps + AM/PM anchors"
      via: "slot window membership check"
      pattern: "post-am-run"
    - from: "_macro_gap_check"
      to: "MACRO_THRESHOLDS"
      via: "protein/carb threshold comparison"
      pattern: "MACRO_THRESHOLDS"
---

<objective>
Build the Wave-1 nutrition-accountability pure functions (no I/O) that the 21:30 cron integration (Plan 04) will call: macro-gap detection on a meaningful-shortfall threshold (NUTR-01), meal→fueling-slot mapping anchored to actual training times with structural-miss detection (NUTR-02), and supplement-rider context tied to the carrier slot (NUTR-03).

Purpose: All slot/macro logic is rule-based datetime + threshold arithmetic. Implementing it as pure functions first makes it unit-testable in isolation (no Firestore/Garmin mocks) and prevents I/O failures from masking logic bugs. Plan 04 then wires these into `_gather_nutrition_data` + compose.

Output: `MACRO_THRESHOLDS`, `_resolve_anchor_times`, `_map_meals_to_slots`, `_detect_slot_misses`, `_macro_gap_check` in `core/proactive_alerts.py`; Wave-0 tests. No cron wiring in this plan (that is Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md
@.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md
@docs/hybrid_athlete_blueprint.md

<interfaces>
<!-- Pure-fn contracts Plan 04 will call. No I/O inside these. -->

MACRO_THRESHOLDS (module-level dict): protein/carb floors by day_type
def _resolve_anchor_times(today_iso, garmin_activities, calendar_events) -> tuple[datetime|None, datetime|None]   # (am_run_anchor, pm_lift_anchor)
def _map_meals_to_slots(meals: list[dict], am_anchor, pm_anchor) -> dict   # {slot_name: [meals]}
def _detect_slot_misses(meals: list[dict], am_anchor, pm_anchor, today_date: str) -> list[str]   # ["post-am-run","pm-post-lift","pre-bed"]
def _macro_gap_check(totals: dict, day_type: str, targets: dict) -> list[dict]   # [{topic_key, description, severity}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Macro-gap check + thresholds</name>
  <files>core/proactive_alerts.py, tests/test_proactive_alerts.py</files>
  <read_first>
    - core/proactive_alerts.py lines 325-420 (existing pure detectors _detect_weather_conflicts / _detect_overloaded_day — the pure-fn-no-I/O style to mirror)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md Finding 4 (macro thresholds table + day_type rules + pattern-triggered structural critique) + Don't Hand-Roll (MACRO_THRESHOLDS as module dict)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md D-09 (meaningful-gap only, no micro-optimization) + D-12 (structural critique distinct from daily flag)
    - docs/hybrid_athlete_blueprint.md §6 (150g protein / 350g carb targets)
  </read_first>
  <behavior>
    - protein 110g (< 120g floor) on a normal day → one flag with topic_key "protein-miss"
    - protein 145g → no flag (marginal shortfall is NOT flagged — D-09)
    - carbs 240g on a normal day (< 250g) → flag "carb-miss"; carbs 290g on a long_run day (< 300g) → flag "carb-miss:long-run-day"; carbs 290g on a normal day → no flag (long-run floor only applies to long_run day_type)
    - a rest/deload day uses the relaxed carb floor (200g)
    - returned flags are dicts with topic_key + description + severity; an all-met day returns []
  </behavior>
  <action>
    Add a module-level `MACRO_THRESHOLDS` dict to `core/proactive_alerts.py` encoding the Finding-4 floors: protein floor 120g (80% of 150g target); carb floors by day_type — normal 250g, long_run 300g, deload 200g. Reference the 150g/350g targets from the blueprint as the Tier-A source.

    Add pure function `_macro_gap_check(totals: dict, day_type: str, targets: dict) -> list[dict]`. `totals` carries `protein_g` / `carbs_g`; `day_type` is one of `"long_run" | "normal" | "deload" | "rest"`. Return a list of flag dicts `{"topic_key", "description", "severity"}` — `protein-miss` when protein < floor; `carb-miss` (subject `long-run-day` when day_type == "long_run") when carbs < the day_type floor. Description names the real number and the target (Tier-A target citable always; the measured total is Tier-B, citable because it is today's data ≤2d per the recency contract). Do NOT emit marginal-shortfall flags (D-09 — only structurally meaningful floors). Keep it pure — no Firestore, no Garmin calls.

    Add tests to `tests/test_proactive_alerts.py` covering all behaviors above.
  </action>
  <verify>
    <automated>python -m pytest tests/test_proactive_alerts.py -x -q -k "macro"</automated>
  </verify>
  <acceptance_criteria>
    - `core/proactive_alerts.py` contains `MACRO_THRESHOLDS` and `def _macro_gap_check` (verify: `grep -n 'MACRO_THRESHOLDS\|def _macro_gap_check' core/proactive_alerts.py`)
    - `_macro_gap_check` performs no I/O (verify: no `MealStore`, `fetch_garmin`, `firestore`, or `.get(` Firestore call inside the function body)
    - protein 145g on a normal day returns no flag; protein 110g returns a `protein-miss` flag (asserted in tests)
    - `pytest tests/test_proactive_alerts.py -x -q -k "macro"` exits 0
  </acceptance_criteria>
  <done>Meaningful protein/carb shortfalls (day-type aware) produce flags; marginal shortfalls do not; pure and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anchor resolution + meal→slot mapping + structural-miss detection + supplement riders</name>
  <files>core/proactive_alerts.py, tests/test_proactive_alerts.py</files>
  <read_first>
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-RESEARCH.md Finding 3 (anchor priority order, slot-window table, _detect_slot_misses algorithm, supplement inference) + Pitfall 2 (anchor None on rest day) + Pitfall 7 (get_day returns [] not None)
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-PATTERNS.md § "Slot-miss detection (new pure function)" code excerpt + § "Pure function / no I/O for testability"
    - .planning/phases/24-strict-coaching-integration-nutrition-accountability/24-CONTEXT.md D-10 (training-anchored windows, flag slots #2/#5/#6) + D-11 (supplements ride carrier slot; #6 standalone)
    - docs/hybrid_athlete_blueprint.md §6 (6 named slots + embedded supplements: #2 D3+K2/Omega-3, #5 Creatine, #6 Mg-Glycinate/Zinc/Copper)
  </read_first>
  <behavior>
    - _resolve_anchor_times returns (am, pm) datetimes from a Garmin running activity start (priority 1) / calendar run event (priority 2) / fixed 07:30 fallback for AM; strength activity / gym event / fixed 19:00 for PM; returns None for a slot when neither data nor a training event exists and no fallback is warranted on a rest day
    - _detect_slot_misses returns "post-am-run" only when am_anchor is not None and no meal falls in [anchor+15min, anchor+90min]
    - on a rest day (am_anchor None, pm_anchor None) only "pre-bed" can fire — "post-am-run"/"pm-post-lift" do NOT (Pitfall 2)
    - "pre-bed" fires when no meal timestamp falls in the fixed 21:00–23:59 window
    - _map_meals_to_slots groups meals into the 6 named slots using the anchor windows
  </behavior>
  <action>
    Add three pure functions to `core/proactive_alerts.py`:

    `_resolve_anchor_times(today_iso, garmin_activities, calendar_events) -> (am_anchor, pm_anchor)`: takes already-fetched data as args (NO new Garmin/calendar I/O inside — keep pure and testable; Plan 04 passes the fetched lists). AM priority: Garmin activity start where type in {"running","trail_running","treadmill_running"} → calendar event with "run" in summary → fixed 07:30. PM priority: Garmin type in {"strength_training","fitness_equipment"} → calendar event with "gym"/"lower body"/"upper body" in summary → fixed 19:00. Return None for a slot when neither data nor event exists (do not fabricate an anchor on a true rest day — let the caller widen via fallback only when appropriate per D-10 discretion; document the fallback choice in a comment).

    `_map_meals_to_slots(meals, am_anchor, pm_anchor) -> dict`: parse each meal `timestamp` (ISO string; guard missing) and bucket into the 6 slots using windows from Finding 3 table (#1 pre-AM-run anchor−90..−15; #2 post-AM-run anchor+15..+90; #3 midday fixed 12:00–14:30; #4 pre-lift anchor−90..−15; #5 post-lift anchor+15..+90; #6 pre-bed fixed 21:00–23:59).

    `_detect_slot_misses(meals, am_anchor, pm_anchor, today_date) -> list[str]`: return the missed HARD slots only (#2 "post-am-run", #5 "pm-post-lift", #6 "pre-bed"). CRITICAL (Pitfall 2): only evaluate "post-am-run" when `am_anchor is not None`, only "pm-post-lift" when `pm_anchor is not None`; "pre-bed" is always evaluated (fixed window). Use `<=` inclusive window membership. Topic-key form is `fueling-miss:{slot}` when surfaced by the caller; this fn returns the bare slot names.

    Supplement riders are surfaced at compose time (Plan 04), but expose the mapping as a module constant here: a `SLOT_SUPPLEMENTS` dict — `"post-am-run": "D3+K2/Omega-3"`, `"pm-post-lift": "Creatine"`, `"pre-bed": "Mg-Glycinate/Zinc/Copper"` (D-11) — so Plan 04 can build the rider copy without re-deriving it.

    Add tests to `tests/test_proactive_alerts.py`: a resolved-AM-anchor missed-reload case, a rest-day no-anchor case asserting "post-am-run"/"pm-post-lift" do NOT fire (Pitfall 2), a pre-bed-miss case, and a meal-present-in-slot no-miss case. Build meals as plain dicts with `timestamp` ISO strings — no Firestore.
  </action>
  <verify>
    <automated>python -m pytest tests/test_proactive_alerts.py -x -q -k "slot or anchor or supplement"</automated>
  </verify>
  <acceptance_criteria>
    - `core/proactive_alerts.py` defines `_resolve_anchor_times`, `_map_meals_to_slots`, `_detect_slot_misses`, and `SLOT_SUPPLEMENTS` (verify: `grep -n 'def _resolve_anchor_times\|def _map_meals_to_slots\|def _detect_slot_misses\|SLOT_SUPPLEMENTS' core/proactive_alerts.py`)
    - `_detect_slot_misses` guards `am_anchor is not None` / `pm_anchor is not None` before evaluating slots #2/#5 (verify: `grep -n 'am_anchor' core/proactive_alerts.py` shows an `is not None` / truthy guard around the post-am-run branch; rest-day test asserts no false slot miss)
    - the three slot functions perform no Firestore/Garmin I/O (data passed as args)
    - `SLOT_SUPPLEMENTS` maps post-am-run → D3+K2/Omega-3, pm-post-lift → Creatine, pre-bed → Mg-Glycinate/Zinc/Copper
    - `pytest tests/test_proactive_alerts.py -x -q -k "slot or anchor or supplement"` exits 0
  </acceptance_criteria>
  <done>Training-anchored slot mapping flags hard-slot misses only when the training anchor resolved; rest days produce no false slot miss; supplement riders are mapped; pure and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MealStore / Garmin data → pure nutrition functions | Already-fetched, server-side-aggregated data passed as args; no untrusted parse of external input here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | meal `timestamp` parse in _map_meals_to_slots | mitigate | timestamps originate from the server-side HealthKit aggregation pipeline (MealStore), not free user text; guard missing/malformed `timestamp` (skip the meal) so a bad value cannot raise inside a cron |
| T-24-06 | Information Disclosure | flag descriptions (meal data in messages) | accept | macro/slot descriptions surface only the user's own numbers to the allowlisted Telegram user; no third-party PII; consistent with existing nutrition coaching |
| T-24-07 | Denial of Service | rest-day false slot miss → nightly noise | mitigate | Pitfall-2 guard: slots #2/#5 only evaluated when their anchor resolved — prevents spurious flags that would erode trust and over-fire the dedup gate |
| T-24-SC | Tampering | npm/pip/cargo installs | N/A | no new packages introduced in this plan (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `python -m pytest tests/test_proactive_alerts.py -x -q` green (macro + slot + anchor + supplement)
- `grep -n 'def _detect_slot_misses\|def _macro_gap_check\|SLOT_SUPPLEMENTS\|MACRO_THRESHOLDS' core/proactive_alerts.py` returns all four
- No I/O inside the new pure functions (manual review + tests run without Firestore/Garmin mocks)
</verification>

<success_criteria>
- Macro-gap flags fire only on meaningful shortfalls, day-type aware (NUTR-01)
- Hard-slot structural misses detected from anchored windows, no rest-day false positives (NUTR-02, substrate for SC-4)
- Supplement riders mapped to carrier slots, #6 standalone (NUTR-03)
- New helpers pure and fully unit-tested; baseline preserved
</success_criteria>

<output>
Create `.planning/phases/24-strict-coaching-integration-nutrition-accountability/24-02-SUMMARY.md` when done.
</output>
