---
phase: 20-accountability-crons-recovery-briefing
plan: 05
type: tdd
wave: 4
depends_on: [20-04]
files_modified:
  - tests/test_recovery_concern.py
  - core/training_checkin.py
  - core/morning_briefing.py
  - prompts/morning_briefing.md
  - prompts/proactive_alert.md
autonomous: true
requirements: [RECOVERY-01, RECOVERY-02, RECOVERY-03]
must_haves:
  truths:
    - "compute_recovery_concern returns None on no-trigger inputs and a {level: mild|strong, ...} dict on threshold crossings"
    - "RECOVERY_THRESHOLDS is a module-level dict with all required keys and a v0-heuristic docstring"
    - "Morning briefing _gather_data sets data['recovery_concern'] best-effort, silently omitting on failure (no 'all clear' placeholder)"
    - "Both morning_briefing.md and proactive_alert.md read recovery_concern and shift to a suggesting, metric-anchored tone with no invented numeric targets"
  artifacts:
    - path: "core/training_checkin.py"
      provides: "compute_recovery_concern + RECOVERY_THRESHOLDS"
      contains: "RECOVERY_THRESHOLDS"
    - path: "core/morning_briefing.py"
      provides: "recovery_concern computation in _gather_data"
      contains: "recovery_concern"
    - path: "prompts/morning_briefing.md"
      provides: "recovery_concern tone-shift section"
      contains: "recovery_concern"
    - path: "prompts/proactive_alert.md"
      provides: "recovery_concern framing"
      contains: "recovery_concern"
  key_links:
    - from: "core/morning_briefing.py::_gather_data"
      to: "core/training_checkin.py::compute_recovery_concern"
      via: "best-effort Pattern-C call"
      pattern: "compute_recovery_concern"
---

<objective>
Add recovery awareness: `compute_recovery_concern` + the `RECOVERY_THRESHOLDS` v0 heuristic dict (in `core/training_checkin.py`, where the morning-briefing block imports it per PATTERNS), wire it into `morning_briefing._gather_data` as a best-effort Pattern-C block, and extend both `prompts/morning_briefing.md` and `prompts/proactive_alert.md` to read `recovery_concern` and shift tone — direct, metric-anchored, suggesting (not commanding), with no invented numeric targets when the profile is empty.

Purpose: This turns Klaus's recovery data (ACWR / HRV / sleep + today's planned intensity) into a tone shift that surfaces overreach equally in the morning briefing and the evening alert (D-16), with severity levels (D-12) and the no-fabrication guardrail (D-13).
Output: compute_recovery_concern + RECOVERY_THRESHOLDS; morning_briefing wiring; two prompt edits; RED→GREEN test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-accountability-crons-recovery-briefing/20-PATTERNS.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-RESEARCH.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-UI-SPEC.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- From Plan 03/04:
     mcp_tools.calendar_tool.GoogleCalendarManager().list_training_events(...) → today's Training events
       (for classifying today's planned intensity). -->
<!-- From garmin_tool.py: fetch_garmin_today() → dict including sleep score, hrv status; compute_acwr_from_db()
     → {"acute","chronic","ratio"} (ratio None when insufficient baseline). -->
<!-- Consecutive low-sleep (D-15, RESEARCH Open Q3): read daily_biometrics via Postgres, NOT a 2nd Garmin call:
     SELECT date, sleep_score FROM daily_biometrics WHERE date >= (today-2) ORDER BY date DESC LIMIT 2.
     Mirror compute_acwr_from_db's psycopg2 lazy-import + never-raises sentinel pattern. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED→GREEN RECOVERY_THRESHOLDS + compute_recovery_concern</name>
  <read_first>
    - core/training_checkin.py — from Plan 04 (module exists); add the recovery block here
    - mcp_tools/garmin_tool.py:339 compute_acwr, :396 compute_acwr_from_db (psycopg2 lazy-import, never-raises sentinel); fetch_garmin_today shape
    - 20-RESEARCH.md Finding 10 (lines 523–562) — RECOVERY_THRESHOLDS dict + severity determination; Open Question 3 (consecutive-sleep Postgres read, lines 801–804)
    - 20-CONTEXT.md D-12 (mild/strong), D-13 (no fabrication), D-14 (intensity by keyword), D-15 (consecutive low-sleep), D-16
    - 20-UI-SPEC.md § Recovery Concern copy (lines 226–244) — mild/strong examples, guardrail
  </read_first>
  <behavior>
    - Test test_thresholds_dict_shape: RECOVERY_THRESHOLDS exists with keys acwr_mild, acwr_strong, sleep_low, consecutive_low_sleep_nights, intensity_keywords_high, intensity_keywords_moderate, hrv_flag_values; module/dict carries a docstring noting v0 heuristics to be tuned after ~2 weeks
    - Test: compute_recovery_concern returns None when ACWR is low, sleep is fine, HRV is fine, and no intense session today (no "all clear" object — actual None)
    - Test: ACWR >= acwr_strong (1.8) + a high-intensity keyword event today → {"level": "strong", ...}
    - Test: ACWR >= acwr_mild (1.5) + high-intensity → {"level": "mild", ...}
    - Test (D-15): 2 consecutive nights sleep_score < 70 + an intense session today → at least "mild"
    - Test (D-13/strong): HRV flagged ("unbalanced"/"low") + sleep_score < 70 + heavy lifting → "strong"
    - Test: the returned dict carries the contributing metrics (acwr ratio, hrv status, sleep score) so the prompt can be metric-anchored, but NO prescriptive numeric targets
  </behavior>
  <action>
Create `tests/test_recovery_concern.py` FIRST (RED). Mock `compute_acwr_from_db`, `fetch_garmin_today`, the Postgres sleep read, and `list_training_events`. Commit RED: `test(20-05): add failing recovery_concern tests`.

Then add to `core/training_checkin.py` (it is the import target in morning_briefing per PATTERNS lines 448–460):

**RECOVERY_THRESHOLDS** — copy verbatim from RESEARCH Finding 10 lines 545–557, with the docstring:
```python
RECOVERY_THRESHOLDS = {
    # v0 heuristics — tune after ~2 weeks of journaled training_log + biometrics data.
    # Keys map to severity levels ("mild" or "strong"). RECOVERY-02.
    "acwr_mild":   1.5,   # ACWR >= 1.5 + any high-intensity session today → mild
    "acwr_strong": 1.8,   # ACWR >= 1.8 + high-intensity → strong
    "sleep_low":   70,    # Garmin sleep score < 70 (D-15)
    "consecutive_low_sleep_nights": 2,        # 2 consecutive nights below sleep_low (D-15)
    "intensity_keywords_high": ("heavy", "intervals", "speed", "long run", "hiit"),
    "intensity_keywords_moderate": ("gym", "run", "bike", "five fingers"),
    "hrv_flag_values": ("unbalanced", "low"),  # Garmin HRV status strings (A4 assumption)
}
```

**`def compute_recovery_concern(garmin_data: dict | None, today_iso: str) -> dict | None`** implementing the D-12 severity logic from RESEARCH lines 559–562:
1. Classify today's intensity (D-14): fetch today's Training events via `list_training_events`; if any event title contains a `intensity_keywords_high` substring → high; else if any matches `intensity_keywords_moderate` → moderate; else moderate (unknown → moderate per D-14). Best-effort: on calendar failure, treat as unknown/moderate.
2. Read ACWR ratio (`compute_acwr_from_db()` → ratio; None when insufficient — treat as not-a-trigger).
3. Read HRV status + today sleep score from `garmin_data` (passed in from morning_briefing); read the prior 2 nights' sleep scores via a Postgres helper `_recent_sleep_scores(today_iso, n=2)` (lazy psycopg2, never-raises, returns list; Open Q3 — do NOT make a 2nd Garmin call).
4. Determine severity:
   - **strong** if (ratio is not None and ratio >= acwr_strong and intensity == "high") OR (hrv flagged AND sleep_score < sleep_low AND intensity == "high")
   - **mild** if (ratio is not None and ratio >= acwr_mild and intensity in ("high","moderate")) OR (>=2 consecutive nights sleep < sleep_low AND intensity in ("high","moderate"))
   - else **None**
5. Return None when no trigger; else `{"level": "mild"|"strong", "acwr": ratio, "hrv_status": <status>, "sleep_score": <score>, "intensity": <class>}`. No numeric prescriptions in the dict (D-13).

Commit GREEN: `feat(20-05): RECOVERY_THRESHOLDS + compute_recovery_concern`.
  </action>
  <verify>
    <automated>pytest tests/test_recovery_concern.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RECOVERY_THRESHOLDS = {" core/training_checkin.py` returns a match with all 7 documented keys present
    - `grep -n "tune after" core/training_checkin.py` shows the v0-heuristic docstring (RECOVERY-02)
    - `grep -n "def compute_recovery_concern" core/training_checkin.py` returns a match
    - `grep -n "_recent_sleep_scores\|daily_biometrics" core/training_checkin.py` shows the Postgres consecutive-sleep read (Open Q3, no 2nd Garmin call)
    - returns None on no-trigger; "mild"/"strong" on the documented crossings; no numeric prescription keys in the dict
    - `pytest tests/test_recovery_concern.py -x -q` passes (incl. test_thresholds_dict_shape)
  </acceptance_criteria>
  <done>RECOVERY_THRESHOLDS v0 dict + compute_recovery_concern with D-12 mild/strong severity, D-14 keyword intensity, D-15 consecutive-sleep (Postgres), D-13 no-fabrication; green tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire recovery_concern into morning_briefing._gather_data</name>
  <read_first>
    - core/morning_briefing.py — _gather_data (lines ~174–); the Garmin fetch + Postgres writeback block; the existing Pattern-C try/except blocks; data["garmin"] shape
    - 20-PATTERNS.md § "core/morning_briefing.py :: _gather_data (recovery_concern)" (lines 430–460) — exact best-effort block placement
    - 20-CONTEXT.md D-13 (silent omit, no "all clear" placeholder)
  </read_first>
  <action>
Add a best-effort Pattern-C block to `core/morning_briefing.py::_gather_data`, placed AFTER the Garmin + Postgres writeback block and BEFORE the TickTick tasks block (copy 20-PATTERNS.md lines 448–460 verbatim):
```python
# Phase 20 — RECOVERY-01: compute recovery concern from ACWR + HRV + sleep + today's intensity
try:
    from core.training_checkin import compute_recovery_concern
    rc = compute_recovery_concern(
        garmin_data=data.get("garmin"),
        today_iso=today_iso,
    )
    if rc:
        data["recovery_concern"] = rc
except Exception:
    logger.warning("morning_briefing: recovery_concern computation failed", exc_info=True)
    # silent omit — no "all clear" placeholder (D-13 guardrail)
```
Only set `data["recovery_concern"]` when `rc` is truthy (mirrors the NUTR-07 silent-omit precondition already in this file — the prompt's tone-shift depends on the KEY being absent when there is no concern). Do not add an else branch. Use the actual `today_iso` variable name the function already uses (read the signature first).
  </action>
  <verify>
    <automated>python -c "src=open('core/morning_briefing.py').read(); assert 'compute_recovery_concern' in src and 'data[\"recovery_concern\"]' in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "compute_recovery_concern" core/morning_briefing.py` shows the import + call inside _gather_data
    - `grep -n 'data\["recovery_concern"\]' core/morning_briefing.py` shows the conditional set (only when truthy)
    - No `else` branch sets an "all clear" value (D-13 silent omit)
    - the block is wrapped in try/except (best-effort) and placed after the Garmin/Postgres block
    - Full suite green: `pytest tests/ -q`
  </acceptance_criteria>
  <done>_gather_data computes recovery_concern best-effort and sets data["recovery_concern"] only when present (silent omit on no-concern/failure per D-13).</done>
</task>

<task type="auto">
  <name>Task 3: Extend morning_briefing.md + proactive_alert.md prompts</name>
  <read_first>
    - prompts/morning_briefing.md — full file; existing section structure + `---` separators + JARVIS voice; the nutrition recap block (NUTR-07) as the silent-omit precedent
    - prompts/proactive_alert.md — full file; alert body structure + tone
    - 20-UI-SPEC.md § Message Copy — Recovery Concern (lines 226–244): mild/strong examples, 🔴 strong prefix in alert (not in briefing prose), guardrail copy; § Typography (no exclamation marks, "sir", no emoji in prose)
    - 20-CONTEXT.md D-13 (no invented numbers), D-16 (both paths equal weight)
  </read_first>
  <action>
Edit both prompt files to read the `recovery_concern` data key and shift tone (RECOVERY-03, D-16 — equal weight in both).

**prompts/morning_briefing.md** — add a recovery-concern instruction section (using the file's existing `---` separator + bold-label convention). Instruct: "When `recovery_concern` is present in the data, weave ONE metric-anchored, suggesting (not commanding) sentence into the summary. Name the actual signals (e.g. 'ACWR is at 1.6 and sleep was below par'). With an empty training profile, suggest only qualitative modifications ('keep today's session submaximal', 'favour aerobic over anaerobic') — never invent specific weights, HR zones, or paces. For `level: strong`, be more direct and explicitly name the combination of signals. When `recovery_concern` is absent, add NO recovery framing and NO 'all clear' line." Use the exact mild/strong example sentences from UI-SPEC lines 232–237 as guidance. No emoji in the briefing prose (UI-SPEC); no exclamation marks; address as "sir".

**prompts/proactive_alert.md** — add the same recovery-concern framing with equal weight (D-16), plus: for `level: strong`, prefix the recovery note with 🔴 (UI-SPEC line 238 — strong prefix in the alert, NOT in the morning briefing prose). Same guardrail (no invented numbers), same JARVIS voice, suggesting not commanding.

Both files: keep all existing content intact; only ADD the recovery-concern section.
  </action>
  <verify>
    <automated>grep -l "recovery_concern" prompts/morning_briefing.md prompts/proactive_alert.md | wc -l | grep -q 2 && grep -q "🔴" prompts/proactive_alert.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recovery_concern" prompts/morning_briefing.md` >= 1 AND `grep -c "recovery_concern" prompts/proactive_alert.md` >= 1 (D-16 both)
    - `grep -n "submaximal\|aerobic\|qualitative\|never invent\|no.*numeric" prompts/morning_briefing.md` shows the D-13 no-fabrication guardrail
    - `grep -n "🔴\|strong" prompts/proactive_alert.md` shows the strong-severity framing/prefix (UI-SPEC)
    - both prompts instruct "absent → no recovery framing, no all-clear" (silent omit)
    - no exclamation marks added; "sir" address preserved; no emoji added to morning_briefing prose
  </acceptance_criteria>
  <done>Both prompts read recovery_concern with equal weight (D-16), shift to suggesting metric-anchored tone, apply mild/strong differentiation + 🔴 strong prefix in the alert, enforce the D-13 no-invented-numbers guardrail, and add no framing when absent.</done>
</task>

</tasks>

<threat_model>
No new external/auth surface — recovery computation reads existing Garmin + Postgres sources behind the morning-briefing cron's existing OIDC boundary; prompt edits are static content.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Information disclosure | recovery prompt fabricating medical/numeric advice | mitigate | D-13 guardrail encoded in both prompts: qualitative modifications only, never invented weights/HR/paces while profile empty. |

Reuses morning-briefing-tick + proactive-alerts OIDC cron auth — no new threat surface beyond the prompt-content guardrail above.
</threat_model>

<verification>
- `pytest tests/test_recovery_concern.py -q` green (thresholds shape, None on no-trigger, mild/strong crossings, D-15 consecutive sleep)
- `grep` checks confirm both prompts read recovery_concern + carry the D-13 guardrail + 🔴 strong prefix in the alert
- Full suite green: `pytest tests/ -q`
</verification>

<success_criteria>
- RECOVERY_THRESHOLDS v0 dict (RECOVERY-02) + compute_recovery_concern (RECOVERY-01) with mild/strong severity
- morning_briefing._gather_data sets recovery_concern best-effort, silent-omit on no-concern
- Both prompts shift tone equally (RECOVERY-03, D-16) with the no-fabrication guardrail
- recovery_concern test file green; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/20-accountability-crons-recovery-briefing/20-05-SUMMARY.md`
</output>
