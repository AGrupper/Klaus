---
phase: 20-accountability-crons-recovery-briefing
plan: 06
type: execute
wave: 4
depends_on: [20-01]
files_modified:
  - tests/test_web_server.py
  - tests/test_heartbeat.py
  - tests/test_docs.py
  - core/weekly_training_review.py
  - prompts/weekly_training_review.md
  - interfaces/web_server.py
  - core/heartbeat.py
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-04]
must_haves:
  truths:
    - "POST /cron/weekly-training-review verifies OIDC, returns 200 when app present, 500 when app absent, and re-raises (logging ok=False) on handler exception"
    - "The weekly review composes a brain-generated scorecard + narrative from training_log, activities, daily_biometrics, MealStore 7-day totals, and athletic_goals, and always sends (even on a sparse week)"
    - "prompts/weekly_training_review.md exists with the required placeholders and scorecard/narrative/suggestion structure"
    - "The heartbeat staleness map carries a weekly-training-review key (~170h)"
  artifacts:
    - path: "interfaces/web_server.py"
      provides: "/cron/weekly-training-review OIDC route"
      contains: "weekly-training-review"
    - path: "core/weekly_training_review.py"
      provides: "run_weekly_review + gather + brain compose"
      contains: "async def run_weekly_review"
    - path: "prompts/weekly_training_review.md"
      provides: "review prompt template"
      contains: "{today_date}"
    - path: "core/heartbeat.py"
      provides: "weekly-training-review staleness key"
      contains: "weekly-training-review"
  key_links:
    - from: "interfaces/web_server.py::cron_weekly_training_review"
      to: "core/weekly_training_review.py::run_weekly_review"
      via: "lazy import + await inside the route"
      pattern: "run_weekly_review"
    - from: "core/weekly_training_review.py::_gather_week_data"
      to: "memory/firestore_db.py::TrainingLogStore.get_range"
      via: "Sun–Sat week window read"
      pattern: "get_range|get_recent"
---

<objective>
Build the Sunday-10:00 weekly training review: the `/cron/weekly-training-review` OIDC route (mirroring `/cron/autonomous-tick`), the `core/weekly_training_review.py` brain-composed module (gather 7-day training_log + activities + daily_biometrics + MealStore totals + athletic_goals → compose scorecard + narrative + one suggestion → always send), the `prompts/weekly_training_review.md` template, and the heartbeat staleness key (~170h).

Purpose: The weekly review is the reflective coaching ritual (D-17 brain-composed, D-18 emoji scorecard, D-19 richer narrative, D-20 one data-grounded suggestion, D-24 always send). It reads `TrainingLogStore` from Plan 01.
Output: cron route + heartbeat key + weekly_training_review.py + prompt file; extended web_server/heartbeat/docs test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-accountability-crons-recovery-briefing/20-PATTERNS.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-RESEARCH.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-UI-SPEC.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01: TrainingLogStore(project_id, database).get_range(start_date, end_date) / get_recent(days). -->
<!-- Reusable web_server helpers: _verify_cron_request(request) (line 232); _log_cron_run(job_id, ok) (line 338);
     _application module global; lazy module import inside route (autonomous-tick at line 398 is the template). -->
<!-- Brain LLM (D-17): LLMClient(backend=os.environ["SMART_AGENT_BACKEND"], model=os.environ["SMART_AGENT_MODEL"],
     api_key=os.environ["SMART_AGENT_API_KEY"]).chat(messages=[...], system=<prompt>, purpose="weekly_review").
     reflection.py:301–322 is the exact analog. -->
<!-- MealStore.get_day_aggregate(date_str) → {} when empty, else totals incl. fiber (D-21, sum 7 days). -->
<!-- fetch_garmin_activities(14) → trend data this week vs last (D-22). compute consecutive Sun–Sat (D-23). -->
<!-- send_and_inject(bot, text, inject_into_conversation=True) (Plan 03 extended; weekly review IS injected). -->
<!-- heartbeat staleness dict: core/heartbeat.py:108 _CRON_MAX_STALENESS_HOURS, "healthkit-sync": 48 at line 115. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompts/weekly_training_review.md + weekly_training_review.py module</name>
  <read_first>
    - core/reflection.py:288–322 — brain-composed cron: prompt_path.read_text + {today_date} substitution + LLMClient(SMART_AGENT_*) + chat(purpose=...)
    - core/morning_briefing.py:~283 — appending meal_audit.md guidance into the compose prompt; _gather_data Pattern-C best-effort blocks
    - prompts/reflection.md, prompts/proactive_alert.md — {today_date} placeholder + JARVIS voice template shape
    - prompts/meal_audit.md — reused for nutrition critique (D-21; no change)
    - 20-PATTERNS.md § "core/weekly_training_review.py" (lines 226–269), § "prompts/weekly_training_review.md" (lines 647–658), § "Brain LLM Call" (lines 775–791), § "Pattern-C Best-Effort Block"
    - 20-RESEARCH.md Finding 12 (lines 581–595), Pitfall 8 (DST week boundary, lines 663–665)
    - 20-UI-SPEC.md § Message Copy — Weekly Training Review (lines 194–222), § Empty / Error States (lines 248–258)
    - 20-CONTEXT.md D-17..D-24
  </read_first>
  <action>
**Create `prompts/weekly_training_review.md`** — system prompt for the brain. Include the `{today_date}` placeholder. Instruct the brain to produce (UI-SPEC lines 196–222):
1. Greeting: "Good morning, sir. Here is your training review for the week ending {week_end_date}."
2. An emoji/bullet scorecard — one line per planned workout: `✅ {Day} — {Type} — RPE {N}` (logged), `❌ {Day} — {Type} — {skip reason}` (skipped), `⚠️ {Day} — {Type} — no log entry` (planned, no record). NO monospace tables (D-18).
3. 2–4 paragraphs of reflective coaching narrative (D-19) wrapping the scorecard, including HRV / RHR / sleep trend lines this-week-vs-last with ↑/↓ arrows (D-22).
4. ONE suggestion grounded in this week's actual data, JARVIS voice, no invented numeric targets (D-20, D-13 guardrail).
5. Nutrition: weave raw MealStore 7-day totals (calories/protein/carbs/fiber) into the narrative using meal_audit.md critique guidance, not a separate section (D-21).
6. Sparse-week behavior (D-24): if the log/calendar is empty, still produce the "Quiet week — not enough data to review yet, sir." note. Error-state copy per UI-SPEC lines 254–256 when Garmin/log data is unavailable.
Voice rules: "sir", no exclamation marks, no emoji in prose (only scorecard status emoji), suggesting not commanding.

**Create `core/weekly_training_review.py`** mirroring `reflection.py`:
```python
async def run_weekly_review(bot: Bot, today_iso: str) -> None:
    week_data = _gather_week_data(today_iso)
    message = _compose_review(week_data, today_iso)
    from core.scheduled_message import send_and_inject
    await send_and_inject(bot, message, inject_into_conversation=True)   # D-24 always send; injected
```
- `_gather_week_data(today_iso)` — compute the previous Sun–Sat window (D-23) using `datetime(..., tzinfo=ZoneInfo("Asia/Jerusalem"))` not naive dates (Pitfall 8). Best-effort Pattern-C blocks for: `TrainingLogStore.get_range(week_start, week_end)`; `fetch_garmin_activities(14)` (this + last week trend); `daily_biometrics` HRV/RHR/sleep this-week-vs-last; MealStore 7-day totals (sum `get_day_aggregate` across the 7 dates, fiber included); `UserProfileStore.load().get("athletic_goals")` (skip if empty). Each block on failure sets the key to a sentinel + logs (UI-SPEC error copy is the brain's job from the data state).
- `_compose_review(week_data, today_iso)` — load `prompts/weekly_training_review.md`, substitute `{today_date}`, append `prompts/meal_audit.md` content for nutrition critique (mirror morning_briefing.py:~283), call `LLMClient(SMART_AGENT_*).chat(messages=[{"role":"user","content": json.dumps(week_data, ensure_ascii=False, indent=2)}], system=system_prompt, purpose="weekly_review")`, return `(response.get("text") or "").strip()`. If the LLM returns empty, fall back to a minimal data-derived scorecard string (still send — D-24).
NOTE the caller (`_log_cron_run`) is handled in the route (Task 2), not here.
  </action>
  <verify>
    <automated>pytest tests/test_docs.py::test_weekly_training_review_prompt_exists -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f prompts/weekly_training_review.md && grep -q "{today_date}" prompts/weekly_training_review.md`
    - `grep -n "✅\|❌\|⚠️" prompts/weekly_training_review.md` shows the scorecard emoji set (D-18)
    - `grep -n "Quiet week" prompts/weekly_training_review.md` shows the D-24 sparse-week copy
    - `grep -n "async def run_weekly_review\|def _gather_week_data\|def _compose_review" core/weekly_training_review.py` returns all three
    - `grep -n "SMART_AGENT_BACKEND\|SMART_AGENT_MODEL" core/weekly_training_review.py` shows brain composition (D-17)
    - `grep -n "ZoneInfo(\"Asia/Jerusalem\")" core/weekly_training_review.py` shows tz-aware week boundary (Pitfall 8)
    - `grep -n "meal_audit" core/weekly_training_review.py` shows nutrition-critique reuse (D-21)
    - `grep -n "inject_into_conversation=True" core/weekly_training_review.py` (weekly review IS injected)
  </acceptance_criteria>
  <done>weekly_training_review.py composes the brain scorecard+narrative+suggestion from the Sun–Sat window (tz-aware), reuses meal_audit.md for nutrition, always sends; prompt file exists with required structure + placeholders + D-24 sparse copy.</done>
</task>

<task type="auto">
  <name>Task 2: Add /cron/weekly-training-review route + extend web_server tests</name>
  <read_first>
    - interfaces/web_server.py:398–435 — @app.post("/cron/autonomous-tick") route (copy verbatim, rename): _verify_cron_request first, _application None guard → 500, lazy import, _log_cron_run on success AND exception, raise on exception
    - interfaces/web_server.py:232 _verify_cron_request, :338 _log_cron_run
    - tests/test_web_server.py:43–90 — _stub_web_server_imports helper + TestCronAutonomousTick (the 200/500/401 + _log_cron_run assertions)
    - 20-PATTERNS.md § "/cron/weekly-training-review" (lines 335–360), § "Test — Web Server Cron Route" (lines 826–841)
    - 20-RESEARCH.md Finding 8 (lines 484–507)
  </read_first>
  <action>
Add the route to `interfaces/web_server.py` (place after the autonomous-tick route), copying the autonomous-tick shape verbatim and substituting names:
```python
@app.post("/cron/weekly-training-review")
async def cron_weekly_training_review(request: Request) -> JSONResponse:
    """Weekly training review — Sunday 10:00 Asia/Jerusalem. Phase 20 — REVIEW-01."""
    await _verify_cron_request(request)
    if _application is None:
        raise HTTPException(status_code=500, detail={"error": "Not initialised"})
    import core.weekly_training_review as _review   # lazy import — keeps /health cold-start fast
    try:
        today = datetime.now(ZoneInfo("Asia/Jerusalem")).date().isoformat()
        await _review.run_weekly_review(_application.bot, today)
        _log_cron_run("weekly-training-review", ok=True)
    except Exception:
        _log_cron_run("weekly-training-review", ok=False)
        raise
    return JSONResponse(content={"ok": True})
```
Confirm `ZoneInfo` + `datetime` are already imported at module top (they are, per the autonomous-tick route); do not re-import.

Extend `tests/test_web_server.py` with `class TestCronWeeklyTrainingReview` mirroring `TestCronAutonomousTick` (20-PATTERNS.md lines 826–841): patch `core.weekly_training_review.run_weekly_review` with an AsyncMock; assert POST returns 200 with app present + dev-bypass; 500 when `ws._application = None`; `_log_cron_run` called with ok=True on success and ok=False on exception (patch run_weekly_review to raise and assert the route re-raises / 500). Cover REVIEW-01 + REVIEW-04.
  </action>
  <verify>
    <automated>pytest tests/test_web_server.py::TestCronWeeklyTrainingReview -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '/cron/weekly-training-review' interfaces/web_server.py` returns the route
    - the route calls `_verify_cron_request` first, guards `_application is None` → 500, lazy-imports the module, calls `_log_cron_run("weekly-training-review", ok=True/False)` on both paths, and re-raises on exception
    - `grep -n "class TestCronWeeklyTrainingReview" tests/test_web_server.py` returns a match
    - `pytest tests/test_web_server.py::TestCronWeeklyTrainingReview -x -q` passes (200 / 500 / ok-flag both paths)
  </acceptance_criteria>
  <done>/cron/weekly-training-review OIDC route mirrors autonomous-tick (verify → guard → lazy import → log both paths → re-raise); REVIEW-01 + REVIEW-04 covered by green tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add heartbeat staleness key + extend heartbeat tests</name>
  <read_first>
    - core/heartbeat.py:108–116 — _CRON_MAX_STALENESS_HOURS dict; "healthkit-sync": 48 at line 115; "autonomous-tick": 1
    - tests/test_heartbeat.py — test_autonomous_tick_staleness_threshold_is_one_hour + test_all_cron_jobs_have_staleness_entry (the staleness-key test pattern)
    - 20-PATTERNS.md § "core/heartbeat.py :: _CRON_MAX_STALENESS_HOURS" (lines 464–479)
    - 20-RESEARCH.md Finding 9 (lines 509–521)
  </read_first>
  <action>
Add one entry to `_CRON_MAX_STALENESS_HOURS` in `core/heartbeat.py`, after the `"healthkit-sync": 48` line (copy 20-PATTERNS.md lines 468–477):
```python
    "weekly-training-review": 170,  # Phase 20 — Sunday 10:00; 170h = 7d + 2h slack
```
Rationale: weekly job; one missed Sunday plus 2h tolerance avoids spurious alerts while catching a broken job within the second weekly window.

Extend `tests/test_heartbeat.py` with `test_weekly_training_review_staleness_threshold` asserting `_CRON_MAX_STALENESS_HOURS["weekly-training-review"] == 170` (mirror `test_autonomous_tick_staleness_threshold_is_one_hour`). The existing `test_all_cron_jobs_have_staleness_entry` continues to pass.
  </action>
  <verify>
    <automated>pytest tests/test_heartbeat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"weekly-training-review": 170' core/heartbeat.py` returns a match in the staleness dict
    - `grep -n "def test_weekly_training_review_staleness_threshold" tests/test_heartbeat.py` returns a match
    - `pytest tests/test_heartbeat.py -x -q` passes
  </acceptance_criteria>
  <done>Heartbeat monitors the weekly-review cron with a 170h staleness threshold; green tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Scheduler → /cron/weekly-training-review | OIDC-authenticated cron trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Spoofing | /cron/weekly-training-review unauthenticated call | mitigate | `_verify_cron_request(request)` runs first (same OIDC SA + audience as existing crons); CRON_DEV_BYPASS only in local dev. |
| T-20-14 | Repudiation | silent cron failure | mitigate | `_log_cron_run` on success AND exception (ok flag); heartbeat staleness key (170h) raises an alarm if the job stops running. |
| T-20-15 | Info disclosure | brain composing fabricated numeric targets | mitigate | Prompt enforces D-13/D-20 no-invented-targets guardrail (qualitative suggestion only). |
</threat_model>

<verification>
- `pytest tests/test_web_server.py::TestCronWeeklyTrainingReview tests/test_heartbeat.py tests/test_docs.py::test_weekly_training_review_prompt_exists -q` green
- Full suite green: `pytest tests/ -q`
- Route mirrors autonomous-tick invariants (verify-first, guard, lazy import, log both paths, re-raise)
</verification>

<success_criteria>
- /cron/weekly-training-review OIDC route (REVIEW-01) with _log_cron_run on both paths (REVIEW-04)
- weekly_training_review.py brain-composed scorecard+narrative+suggestion from training_log/activities/biometrics/MealStore/goals, always sends (REVIEW-02, D-17..D-24)
- prompts/weekly_training_review.md exists (REVIEW-03)
- heartbeat 170h staleness key
- new/extended tests green; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/20-accountability-crons-recovery-briefing/20-06-SUMMARY.md`
</output>
