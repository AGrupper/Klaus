---
phase: 20-accountability-crons-recovery-briefing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CHECKIN-01, CHECKIN-06, REVIEW-02, CRON-01]
must_haves:
  truths:
    - "REQUIREMENTS.md no longer claims a separate /cron/training-checkin endpoint or klaus-training-checkin job"
    - "REQUIREMENTS.md REVIEW-02 no longer implies a persisted meal_audits store"
    - "ROADMAP.md Phase 20 success criteria reflect the folded check-in + single new scheduler job"
    - "The Traceability table still lists all 19 Phase 20 REQ-IDs (no ID removed)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled CHECKIN-01/06, REVIEW-02, CRON-01 text matching D-09 and D-21"
      contains: "folds into"
    - path: ".planning/ROADMAP.md"
      provides: "Reconciled Phase 20 SC#1/#2/#5"
      contains: "klaus-weekly-training-review"
  key_links: []
---

<objective>
Reconcile the source-of-truth planning docs so they stop contradicting the locked decisions. D-09 eliminates the separate training-checkin endpoint and scheduler job (the check-in folds into the existing 21:30 proactive-alerts cron). D-21 establishes there is no persisted `MealAuditStore` — REVIEW-02's "meal_audits" source resolves to live `MealStore` 7-day totals + the runtime `meal_audit.md` guidance.

Purpose: Downstream verification (`/gsd-verify-work`) reads REQUIREMENTS.md and ROADMAP.md as the contract. If their text still describes a deleted endpoint/job and a non-existent store, verification will flag false gaps. These edits are in-scope deliverables, not optional notes.
Output: Reconciled REQUIREMENTS.md (CHECKIN-01, CHECKIN-06, REVIEW-02, CRON-01) and ROADMAP.md (Phase 20 SC#1/#2/#5). Traceability REQ-IDs unchanged (still 19).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile REQUIREMENTS.md (CHECKIN-01, CHECKIN-06, REVIEW-02, CRON-01)</name>
  <read_first>
    - .planning/REQUIREMENTS.md — § "Training Check-in Cron (Phase 20)" (CHECKIN-01..06, lines 73–80), § "Weekly Training Review Cron" (REVIEW-02, line 85), § "Cloud Scheduler Bootstrap" (CRON-01, line 97), § Traceability (lines 121–179)
    - 20-CONTEXT.md § decisions D-09 (lines 40–44) and D-21 (line 60)
    - 20-RESEARCH.md § Phase Requirements table (lines 70–94) — the reconciled wording already drafted there
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` in place. Change ONLY the descriptive text of these four bullets; do NOT remove any REQ-ID and do NOT touch the Traceability table rows (all 19 Phase 20 IDs stay).

- **CHECKIN-01** — replace the current text with: "**CHECKIN-01** (RECONCILED per D-09): No separate `/cron/training-checkin` endpoint. The check-in logic folds into the existing 21:30 `proactive-alerts` cron via a new `core/training_checkin.py` module invoked from `core/proactive_alerts.py`."
- **CHECKIN-06** — replace with: "**CHECKIN-06** (RECONCILED per D-09): The `0 21` schedule is moot. The check-in runs at 21:30 Asia/Jerusalem inside `proactive-alerts` (no separate scheduler trigger)."
- **REVIEW-02** — replace with: "**REVIEW-02** (RECONCILED per D-21): Weekly review composes from 7-day `training_log`, `activities`, `daily_biometrics`, live `MealStore` 7-day totals (calories/protein/carbs/fiber, no persisted `MealAuditStore`) interpreted at review time via the runtime `prompts/meal_audit.md` guidance, and `UserProfileStore.athletic_goals` (skipped if empty)."
- **CRON-01** — replace with: "**CRON-01** (RECONCILED per D-09): `scripts/bootstrap_shifu_crons.sh` creates ONLY `klaus-weekly-training-review` (the `klaus-training-checkin` job is eliminated) using the existing `CLOUD_SCHEDULER_SA_EMAIL` OIDC service account."

Leave CHECKIN-02, CHECKIN-03, CHECKIN-04, CHECKIN-05, REVIEW-01, REVIEW-03, REVIEW-04 unchanged. Leave the Traceability table fully intact (19 Phase 20 rows). The Coverage footer line ("53/53 requirements mapped … 19 → Phase 20") stays correct.
  </action>
  <verify>
    <automated>grep -q "folds into" .planning/REQUIREMENTS.md && grep -q "no persisted .MealAuditStore." .planning/REQUIREMENTS.md && grep -c "| 20 " .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RECONCILED per D-09" .planning/REQUIREMENTS.md` returns CHECKIN-01, CHECKIN-06, CRON-01
    - `grep -n "RECONCILED per D-21" .planning/REQUIREMENTS.md` returns REVIEW-02
    - `grep -c "Pending" .planning/REQUIREMENTS.md` still shows 19 Phase 20 traceability rows present (no ID removed)
    - The strings "/cron/training-checkin" and "klaus-training-checkin" no longer appear as *active* requirements (only inside the RECONCILED notes describing what was eliminated)
  </acceptance_criteria>
  <done>CHECKIN-01, CHECKIN-06, REVIEW-02, CRON-01 text matches D-09/D-21; all 19 Phase 20 REQ-IDs intact in Traceability.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP.md Phase 20 success criteria</name>
  <read_first>
    - .planning/ROADMAP.md — § "Phase 20: Accountability Crons & Recovery Briefing" (lines 93–104), specifically SC#1 (line 98), SC#2 (line 99), SC#5 (line 102)
    - 20-CONTEXT.md § D-09 (lines 40–44)
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` § Phase 20 Success Criteria in place. Reconcile the three criteria that reference the eliminated 21:00 cron + two-job design:

- **SC#1** (currently "the 21:00 training check-in cron sends zero Telegram messages…") — change "21:00 training check-in cron" to "21:30 training check-in (folded into the proactive-alerts cron)". Keep the rest (zero messages on a fully-covered day; Garmin activities appear in `training_log` with `source="garmin"`).
- **SC#2** (currently "the 21:00 cron sends an inline-keyboard prompt…") — change "21:00 cron" to "21:30 check-in". Keep the inline-keyboard prompt + button-write + notes-reply behavior.
- **SC#5** (currently "creates `klaus-training-checkin` and `klaus-weekly-training-review`… alongside the existing 9 jobs") — change to: "Operator running `scripts/bootstrap_shifu_crons.sh` creates the single `klaus-weekly-training-review` Cloud Scheduler job with OIDC auth (the training check-in needs no new job — it folds into proactive-alerts per D-09); the new job appears alongside the existing jobs in `docs/DEPLOYMENT.md` Phase Shifu section."

Leave SC#3 (recovery concern) and SC#4 (weekly review content) unchanged. Do not change the Phase 20 Goal, Depends-on, or Requirements lines.
  </action>
  <verify>
    <automated>grep -q "klaus-weekly-training-review" .planning/ROADMAP.md && ! grep -q "klaus-training-checkin" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "21:30 training check-in" .planning/ROADMAP.md` matches (folded design referenced in SC#1/#2)
    - `grep -c "klaus-training-checkin" .planning/ROADMAP.md` returns 0 (the eliminated job is gone from SC#5)
    - `grep -n "klaus-weekly-training-review" .planning/ROADMAP.md` returns the single-job SC#5
    - The Phase 20 Requirements line (LOG-01..CRON-02, 19 IDs) is unchanged
  </acceptance_criteria>
  <done>ROADMAP Phase 20 SC#1/#2/#5 describe the folded 21:30 check-in + single new scheduler job; SC#3/#4 + Goal + Requirements untouched.</done>
</task>

</tasks>

<threat_model>
No new threat surface — documentation-only edits to planning markdown. Reuses no runtime code path, no auth surface, no network boundary.
</threat_model>

<verification>
- `grep` checks above pass
- REQUIREMENTS.md Coverage footer still reads "53/53 … 19 → Phase 20"
- No source code touched; no test impact
</verification>

<success_criteria>
- D-09 reconciliation visible in CHECKIN-01, CHECKIN-06, CRON-01 (REQUIREMENTS) + SC#1/#2/#5 (ROADMAP)
- D-21 reconciliation visible in REVIEW-02
- All 19 Phase 20 REQ-IDs preserved in Traceability
</success_criteria>

<output>
After completion, create `.planning/phases/20-accountability-crons-recovery-briefing/20-02-SUMMARY.md`
</output>
