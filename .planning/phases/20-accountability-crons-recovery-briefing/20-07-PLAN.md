---
phase: 20-accountability-crons-recovery-briefing
plan: 07
type: execute
wave: 5
depends_on: [20-01, 20-06]
files_modified:
  - tests/test_docs.py
  - scripts/bootstrap_shifu_crons.sh
  - docs/DEPLOYMENT.md
  - docs/SELF.md
autonomous: true
requirements: [CRON-01, CRON-02]
user_setup:
  - service: cloud-scheduler
    why: "Create the weekly-training-review scheduler job + re-register the Telegram webhook for callback_query"
    dashboard_config:
      - task: "Run scripts/bootstrap_shifu_crons.sh (creates klaus-weekly-training-review)"
        location: "gcloud CLI with PROJECT_ID / REGION / SERVICE_URL / CLOUD_SCHEDULER_SA_EMAIL exported"
      - task: "Re-run setWebhook with allowed_updates=[\"message\",\"callback_query\"] (else button taps are silently dropped)"
        location: "Telegram Bot API setWebhook (curl block documented in DEPLOYMENT.md Phase Shifu section)"
must_haves:
  truths:
    - "scripts/bootstrap_shifu_crons.sh is re-runnable (describe-or-create/update) and creates ONLY klaus-weekly-training-review with OIDC auth"
    - "docs/DEPLOYMENT.md has a Phase Shifu section documenting the single new job + the allowed_updates callback_query re-registration step"
    - "docs/SELF.md (regenerated) lists the new tools log_training + get_training_history and the /cron/weekly-training-review route"
  artifacts:
    - path: "scripts/bootstrap_shifu_crons.sh"
      provides: "re-runnable single-job bootstrap"
      contains: "klaus-weekly-training-review"
    - path: "docs/DEPLOYMENT.md"
      provides: "Phase Shifu section + allowed_updates step"
      contains: "Phase Shifu"
  key_links:
    - from: "scripts/bootstrap_shifu_crons.sh"
      to: "CLOUD_SCHEDULER_SA_EMAIL / SERVICE_URL/cron/weekly-training-review"
      via: "gcloud scheduler jobs create/update http with OIDC"
      pattern: "weekly-training-review"
---

<objective>
Ship the operations layer: a re-runnable `scripts/bootstrap_shifu_crons.sh` that creates ONLY `klaus-weekly-training-review` (D-09/D-25/CRON-01), a `docs/DEPLOYMENT.md` Phase Shifu section documenting the one new job + the mandatory `allowed_updates` callback_query webhook re-registration (Pitfall 1), and a regenerated `docs/SELF.md` surfacing the new tools + cron route.

Purpose: This makes Phase 20 deployable and self-documenting. The bootstrap is the operator's single command; the allowed_updates step is the silent-failure trap (button taps dropped without it). SELF.md regen keeps Klaus's self-knowledge from drifting.
Output: bootstrap script; DEPLOYMENT.md Phase Shifu section; regenerated SELF.md; extended docs test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-accountability-crons-recovery-briefing/20-PATTERNS.md
@.planning/phases/20-accountability-crons-recovery-briefing/20-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- /cron/weekly-training-review route exists (Plan 06). log_training + get_training_history tools registered
     in core/tools.py (Plan 01). self_manifest.py auto-greps tool "name": entries + /cron/ routes. -->
<!-- DEPLOYMENT.md §14e is the existing gcloud scheduler create block analog (lines ~1100–1109);
     §19 is the Cloud Scheduler Full Job Inventory table; line ~489 has allowed_updates=["message"]. -->
<!-- Existing schedule string for the weekly review: "0 10 * * 0" Asia/Jerusalem (REVIEW-04). -->
<!-- Env vars the script reads (mirror §14e): PROJECT_ID, REGION (me-west1), SERVICE_URL, CLOUD_SCHEDULER_SA_EMAIL. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create re-runnable scripts/bootstrap_shifu_crons.sh</name>
  <read_first>
    - docs/DEPLOYMENT.md §14e — the `gcloud scheduler jobs create http` block (lines ~1100–1109): flag set, OIDC SA, audience, location, project
    - 20-PATTERNS.md § "scripts/bootstrap_shifu_crons.sh" (lines 661–701) — exact re-runnable describe-or-create script body
    - 20-RESEARCH.md Finding 13 (lines 597–623)
    - 20-CONTEXT.md D-09 (only the weekly-review job), D-25 (re-runnable, existing OIDC SA), CRON-01
  </read_first>
  <action>
Create `scripts/bootstrap_shifu_crons.sh` by copying the body from 20-PATTERNS.md lines 666–699 verbatim. It MUST:
- Start with `#!/usr/bin/env bash` and `set -euo pipefail`.
- Read `PROJECT_ID`, `REGION` (default `me-west1`), `SERVICE_URL`, `CLOUD_SCHEDULER_SA_EMAIL` from env with `:?`/`:-` guards.
- `gcloud scheduler jobs describe "klaus-weekly-training-review" --location="${REGION}" --project="${PROJECT_ID}" &>/dev/null` → if present, `gcloud scheduler jobs update http`; else `gcloud scheduler jobs create http` (D-25 re-runnable describe-or-create).
- Schedule string EXACTLY `--schedule="0 10 * * 0"` with `--time-zone="Asia/Jerusalem"` (REVIEW-04).
- `--uri="${SERVICE_URL}/cron/weekly-training-review"`, `--http-method=POST` (create branch), `--oidc-service-account-email="${CLOUD_SCHEDULER_SA_EMAIL}"`, `--oidc-token-audience="${SERVICE_URL}"`, `--location="${REGION}"`, `--project="${PROJECT_ID}"`.
- Create ONLY this one job (D-09 — NO `klaus-training-checkin`).
- Lowercase `klaus-` name per CLAUDE.md invariant.
- End with `echo "Done."`.
- After writing, mark it executable: `chmod +x scripts/bootstrap_shifu_crons.sh`.
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap_shifu_crons.sh && grep -q "0 10 \* \* 0" scripts/bootstrap_shifu_crons.sh && grep -q "klaus-weekly-training-review" scripts/bootstrap_shifu_crons.sh && ! grep -q "klaus-training-checkin" scripts/bootstrap_shifu_crons.sh && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/bootstrap_shifu_crons.sh` reports no syntax errors
    - `grep -n "describe" scripts/bootstrap_shifu_crons.sh` AND `grep -n "jobs update http\|jobs create http" scripts/bootstrap_shifu_crons.sh` confirm the re-runnable describe-or-create/update pattern (D-25)
    - `grep -c "klaus-weekly-training-review" scripts/bootstrap_shifu_crons.sh` >= 1 and `grep -c "klaus-training-checkin" scripts/bootstrap_shifu_crons.sh` == 0 (D-09/CRON-01)
    - `grep -q '\-\-schedule="0 10 \* \* 0"' scripts/bootstrap_shifu_crons.sh` and `grep -q 'Asia/Jerusalem' scripts/bootstrap_shifu_crons.sh` (REVIEW-04)
    - `grep -q "CLOUD_SCHEDULER_SA_EMAIL" scripts/bootstrap_shifu_crons.sh` (existing OIDC SA)
    - file is executable (`test -x scripts/bootstrap_shifu_crons.sh`)
  </acceptance_criteria>
  <done>Re-runnable bootstrap creates exactly one OIDC job (klaus-weekly-training-review, 0 10 * * 0 Asia/Jerusalem) using the existing scheduler SA; no training-checkin job (D-09/D-25/CRON-01).</done>
</task>

<task type="auto">
  <name>Task 2: Add DEPLOYMENT.md Phase Shifu section + extend docs test</name>
  <read_first>
    - docs/DEPLOYMENT.md — §19 Cloud Scheduler Full Job Inventory table (column shape: `# | Job ID | Schedule | Endpoint | Phase`); §14e gcloud block; the `setWebhook` block (~line 489) with `allowed_updates=["message"]`; §21/§22 section style
    - 20-RESEARCH.md Finding 4 (allowed_updates, lines 338–350), Pitfall 1 (lines 633–637)
    - tests/test_docs.py — TestDeploymentCompleteness grep-style assertions (the existing completeness-test pattern)
    - 20-CONTEXT.md D-09, CRON-02
  </read_first>
  <action>
Edit `docs/DEPLOYMENT.md`:

1. **Add the new job to the §19 Cloud Scheduler Full Job Inventory table** — append a row for `klaus-weekly-training-review | 0 10 * * 0 (Asia/Jerusalem) | /cron/weekly-training-review | Phase 20 (Shifu)`. Do NOT add a `klaus-training-checkin` row (D-09 — the check-in folds into proactive-alerts; note in the row or nearby that the 21:30 check-in needs no separate job).

2. **Add a new "## Phase Shifu (Phase 20) — Accountability Crons" section** containing:
   - The `gcloud scheduler jobs create http klaus-weekly-training-review` block (mirror §14e exactly: `0 10 * * 0`, `Asia/Jerusalem`, `/cron/weekly-training-review`, OIDC SA + audience, `me-west1`, project) — and a one-line pointer that `scripts/bootstrap_shifu_crons.sh` runs this idempotently.
   - A note that the training check-in is folded into the existing 21:30 `proactive-alerts` cron (D-09) — no new scheduler job.
   - **A REQUIRED `setWebhook` re-registration step** (Pitfall 1 / Finding 4): a curl block updating `allowed_updates` from `["message"]` to `["message","callback_query"]`, with a warning that WITHOUT this, all inline-keyboard button taps are silently dropped by Telegram. Mark it as a one-time operator step after deploy.

3. Keep all existing DEPLOYMENT.md content intact; only append.

Then extend `tests/test_docs.py::TestDeploymentCompleteness` with grep-style assertions:
- `test_phase_shifu_section_present` — DEPLOYMENT.md contains "Phase Shifu" and "klaus-weekly-training-review".
- `test_allowed_updates_callback_query_documented` — DEPLOYMENT.md contains `"callback_query"` within an allowed_updates context.
- `test_no_separate_training_checkin_job` — DEPLOYMENT.md does NOT register a `klaus-training-checkin` scheduler job (assert the folded-design note instead).
  </action>
  <verify>
    <automated>pytest tests/test_docs.py -x -q && grep -q "Phase Shifu" docs/DEPLOYMENT.md && grep -q "callback_query" docs/DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase Shifu" docs/DEPLOYMENT.md` returns the new section header
    - `grep -n "klaus-weekly-training-review" docs/DEPLOYMENT.md` appears in BOTH the §19 inventory row and the new section gcloud block
    - `grep -n 'allowed_updates.*callback_query\|"callback_query"' docs/DEPLOYMENT.md` shows the re-registration step (Pitfall 1)
    - `grep -n "folds into\|proactive-alerts" docs/DEPLOYMENT.md` near the Phase Shifu section documents the D-09 fold (no separate check-in job)
    - new tests in `tests/test_docs.py::TestDeploymentCompleteness` pass
    - `pytest tests/test_docs.py -x -q` passes
  </acceptance_criteria>
  <done>DEPLOYMENT.md Phase Shifu section documents the single new OIDC job, the D-09 folded check-in, and the mandatory allowed_updates callback_query re-registration; §19 inventory updated; docs tests green (CRON-02).</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate docs/SELF.md</name>
  <read_first>
    - core/self_manifest.py:49–80 — how SELF.md is generated (greps tool "name": entries in core/tools.py + /cron/ routes in web_server.py); the regeneration entry point
    - docs/SELF.md — current generated content (tool list + cron route list)
    - 20-RESEARCH.md Finding 14 (lines 625–627)
  </read_first>
  <action>
Regenerate `docs/SELF.md` by running the manifest generator: `python core/self_manifest.py` (the same command CI runs on deploy). This will auto-include the new `log_training` + `get_training_history` tools (registered in Plan 01) and the `/cron/weekly-training-review` route (added in Plan 06), since the generator greps `core/tools.py` tool names and `interfaces/web_server.py` `/cron/` routes. Commit the regenerated file. Do NOT hand-edit SELF.md — it is auto-generated and hand-edits drift on the next deploy.

If the generator requires env vars (model strings) that are not set locally, set the minimal `SMART_AGENT_MODEL` / `WORKER_AGENT_MODEL` / `TICK_BRAIN_MODEL` env vars from `.env.example` defaults for the generation run only (the manifest reads model strings at generate-time per CLAUDE.md §3).
  </action>
  <verify>
    <automated>grep -q "log_training" docs/SELF.md && grep -q "get_training_history" docs/SELF.md && grep -q "weekly-training-review" docs/SELF.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "log_training" docs/SELF.md` returns a match
    - `grep -n "get_training_history" docs/SELF.md` returns a match
    - `grep -n "weekly-training-review" docs/SELF.md` returns a match (new cron route surfaced)
    - SELF.md was regenerated via `python core/self_manifest.py` (not hand-edited)
  </acceptance_criteria>
  <done>docs/SELF.md regenerated to list log_training + get_training_history tools and the /cron/weekly-training-review route; no hand-edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → gcloud / Telegram setWebhook | privileged ops commands run by the operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-16 | Spoofing | weekly-review job without OIDC | mitigate | bootstrap script sets `--oidc-service-account-email` + `--oidc-token-audience` (same SA as existing jobs); route enforces `_verify_cron_request`. |
| T-20-17 | Tampering | webhook delivers callback_query to wrong handler | accept | allowed_updates only widens the update types Telegram delivers; the router (Plan 03) enforces the user allow-list on every callback. |

No new application-code surface — ops scripts + docs + auto-generated manifest. Reuses existing OIDC cron auth + Telegram allow-list boundaries.
</threat_model>

<verification>
- `pytest tests/test_docs.py -q` green
- `bash -n scripts/bootstrap_shifu_crons.sh` clean; grep checks confirm single-job + correct schedule + no training-checkin job
- SELF.md contains the new tools + route
- Full suite green: `pytest tests/ -q`
</verification>

<success_criteria>
- bootstrap_shifu_crons.sh re-runnable, creates ONLY klaus-weekly-training-review with OIDC (CRON-01, D-09/D-25)
- DEPLOYMENT.md Phase Shifu section + §19 inventory row + allowed_updates callback_query step (CRON-02, Pitfall 1)
- SELF.md regenerated with new tools + cron route
- docs tests green; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/20-accountability-crons-recovery-briefing/20-07-SUMMARY.md`
</output>
