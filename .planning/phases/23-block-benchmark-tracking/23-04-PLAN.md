---
phase: 23-block-benchmark-tracking
plan: 04
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - core/morning_briefing.py
  - core/weekly_training_review.py
  - prompts/morning_briefing.md
  - prompts/weekly_training_review.md
  - tests/test_morning_briefing.py
  - tests/test_weekly_training_review.py
autonomous: true
requirements: [BLOCK-01, BLOCK-03]
must_haves:
  truths:
    - "The morning briefing surfaces 'Week N of 16, [phase name]' when an active block exists, and a pre-cycle countdown before 2026-06-21"
    - "The Sunday weekly review surfaces the current block + this-block benchmarks (raw block-over-block deltas only, no projection)"
    - "Both cron gather steps are best-effort: a None block or empty store degrades silently and never crashes the cron"
    - "Week number is derived from plan_start_date 2026-06-21 at gather time, never read from a stored field (D-03)"
  artifacts:
    - path: "core/morning_briefing.py"
      provides: "best-effort BlockStore.get_current() gather + pre-cycle countdown in _gather_data"
      contains: "BlockStore"
    - path: "core/weekly_training_review.py"
      provides: "best-effort BlockStore + BenchmarkStore gather in _gather_week_data"
      contains: "BenchmarkStore"
    - path: "prompts/morning_briefing.md"
      provides: "block / pre_cycle_countdown conditional rendering section"
      contains: "Week"
    - path: "prompts/weekly_training_review.md"
      provides: "current_block + block_benchmarks data-key rendering"
      contains: "current_block"
  key_links:
    - from: "_gather_data / _gather_week_data"
      to: "BlockStore.get_current()"
      via: "best-effort try/except with if block: guard"
      pattern: "BlockStore"
    - from: "_gather_week_data"
      to: "BenchmarkStore.get_block_benchmarks"
      via: "block_id from get_current()"
      pattern: "get_block_benchmarks"
---

<objective>
Surface block context in the two remaining coaching crons (morning briefing + Sunday weekly review) and their prompt files. Add best-effort `BlockStore.get_current()` gather to both, plus `BenchmarkStore.get_block_benchmarks()` to the weekly review, with derived week-number framing and a pre-cycle countdown — all silent-omit on None/empty. No new behavior in the 21:30 cron (that is Plan 03).

Purpose: BLOCK-01 SC#1 ("cron messages include 'Week N of 16, [phase name]' framing") and the D-04 pre-cycle countdown across the morning briefing and weekly review; BLOCK-03 surfacing of this-block benchmarks (raw deltas only) in the weekly review.
Output: two cron gather modifications, two prompt-file rendering sections, and gather tests for both crons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-block-benchmark-tracking/23-CONTEXT.md
@.planning/phases/23-block-benchmark-tracking/23-RESEARCH.md
@.planning/phases/23-block-benchmark-tracking/23-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/23-block-benchmark-tracking/23-01-SUMMARY.md
@core/morning_briefing.py
@core/weekly_training_review.py

<interfaces>
<!-- Stores from Plan 01. Cron anchors VERIFIED live by the planner. -->

Stores (Plan 01):
  BlockStore(project_id, database): get_current() -> dict|None (fields: block_id, label, start_date, end_date, focus_facets, status, benchmark_due, doc_id)
  BenchmarkStore(project_id, database): get_block_benchmarks(block_id) -> list[dict] (date-desc)
  get_week_num(plan_start_date, today) -> int|None  (returns None when today < start, D-03)

morning_briefing.py anchors (VERIFIED live):
  _gather_data(today_iso) builds `data` dict; MealStore best-effort gather at lines 256-272 (the exact silent-omit pattern to mirror); `return data` at line 273.
  Env construction: os.environ["GCP_PROJECT_ID"], os.environ.get("FIRESTORE_DATABASE", "(default)")
  Already imports: from datetime import date, timedelta

weekly_training_review.py anchors (VERIFIED live):
  _gather_week_data(today_iso) at line 42; `today = date.fromisoformat(today_iso)` at line 59; numbered gather sections 1-5; UserProfileStore.athletic_goals gather at lines 181-191 (the pattern to mirror).
  Tests use a `patched_sources` fixture and call wtr._gather_week_data("2026-06-07") directly.

Plan start date constant: "2026-06-21" (from UserProfileStore.plan_start_date; hardcode the literal per PATTERNS.md gather examples — it matches the milestone anchor).
</interfaces>

<closed_sets>
Block phase labels (for "Week N of 16, [label]" framing — derived; the label comes from the active block doc):
  Block 1 "Aerobic Base" · Block 2 "Capacity Build" · Block 3 "Deep Waters → Peak Engine" · Block 4 "Race Specificity → Taper → Race Week"
Gather output keys:
  morning_briefing data["block"] = {label, week_num, benchmark_due, end_date, block_id} when active; data["pre_cycle_countdown"] = N when today < 2026-06-21; NEITHER key when post-cycle with no active block (silent omit).
  weekly_training_review data["current_block"] = {**block, week_num} when active (else None); data["block_benchmarks"] = list (else []); data["pre_cycle_countdown"] = N when pre-cycle.
Scope guard: weekly review shows RAW block-over-block deltas only — NO trend projection / pace-to-deadline (that is Phase 25 / PROG-02). Do not compute trajectories here.
</closed_sets>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cron block-gather tests (RED first)</name>
  <files>tests/test_morning_briefing.py, tests/test_weekly_training_review.py</files>
  <read_first>
    - tests/test_morning_briefing.py (existing patch/mock style; test_prompt_omits_section_when_no_nutrition for the silent-omit assertion pattern)
    - tests/test_weekly_training_review.py (patched_sources fixture; wtr._gather_week_data direct-call tests like test_weekly_review_athletic_goals_*)
    - core/morning_briefing.py (_gather_data, MealStore gather 256-272), core/weekly_training_review.py (_gather_week_data, athletic_goals gather 181-191)
  </read_first>
  <behavior>
    test_morning_briefing.py (mock BlockStore.get_current via patch on memory.firestore_db.BlockStore):
    - test_gather_data_includes_block_state: active block (label "Aerobic Base", end_date in range) + today 2026-06-28 → data["block"] present with week_num==2 (28-21=7 days // 7 +1), label, benchmark_due, end_date
    - test_gather_data_precycle_countdown: today 2026-06-12 (before anchor), get_current → None → data["pre_cycle_countdown"]==9, no data["block"] key
    - test_gather_data_block_failure_silent: BlockStore raises → neither data["block"] nor data["pre_cycle_countdown"] set, no exception (Pitfall 4)
    - test_gather_data_postcycle_no_active_silent: today after anchor, get_current None → neither key set (silent omit)
    test_weekly_training_review.py (extend patched_sources to stub BlockStore/BenchmarkStore):
    - test_gather_week_includes_current_block: active block + today 2026-07-19 (a Sunday) → data["current_block"] has week_num, data["block_benchmarks"] is the stubbed list
    - test_gather_week_precycle: get_current None + pre-cycle date → data["pre_cycle_countdown"] set, data["current_block"] None
    - test_gather_week_block_failure_sets_defaults: store raises → data["current_block"] None, data["block_benchmarks"] == [], no exception
  </behavior>
  <action>Add the block-gather tests to both existing test files using their established patch/fixture styles. In test_morning_briefing.py, patch memory.firestore_db.BlockStore so get_current returns the stub or raises; call core.morning_briefing._gather_data with a fixed today_iso and assert the data keys per the behavior list. In test_weekly_training_review.py, extend the patched_sources fixture to also stub BlockStore.get_current and BenchmarkStore.get_block_benchmarks, then call wtr._gather_week_data with a Sunday date and assert current_block / block_benchmarks / pre_cycle_countdown. Tests fail until Task 2/3 (RED).</action>
  <verify>
    <automated>python -m pytest tests/test_morning_briefing.py tests/test_weekly_training_review.py -k "block or current_block or precycle or pre_cycle" -q 2>&1 | grep -qE "failed|error|Error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - test_morning_briefing.py contains test_gather_data_includes_block_state, test_gather_data_precycle_countdown, test_gather_data_block_failure_silent
    - test_weekly_training_review.py contains test_gather_week_includes_current_block and test_gather_week_block_failure_sets_defaults
    - The -k block selection exits non-zero (RED) before Task 2
  </acceptance_criteria>
  <done>Cron block-gather tests exist and fail because the gather steps are absent.</done>
</task>

<task type="auto">
  <name>Task 2: block-state gather in morning_briefing + weekly_training_review (GREEN)</name>
  <files>core/morning_briefing.py, core/weekly_training_review.py</files>
  <read_first>
    - core/morning_briefing.py (MealStore best-effort gather lines 256-272 — copy the try/except + `if truthy:` silent-omit shape; return data line 273)
    - core/weekly_training_review.py (athletic_goals gather lines 181-191; numbered-section convention; `today = date.fromisoformat(today_iso)` line 59)
    - .planning/phases/23-block-benchmark-tracking/23-PATTERNS.md (morning_briefing section lines 362-413; weekly_training_review section lines 417-460 — verbatim gather code)
    - memory/firestore_db.py (BlockStore.get_current, get_week_num, BenchmarkStore.get_block_benchmarks from Plan 01)
  </read_first>
  <action>In core/morning_briefing.py _gather_data, insert a best-effort block gather AFTER the nutrition block (after line 269) and BEFORE `return data`: import BlockStore, construct with the env vars, call get_current(); if block: compute week_num = (date.fromisoformat(today_iso) - date.fromisoformat("2026-06-21")).days // 7 + 1 and set data["block"] = {"label": block.get("label"), "week_num": week_num, "benchmark_due": block.get("benchmark_due", False), "end_date": block.get("end_date"), "block_id": block.get("block_id") or block.get("doc_id")}; else compute days_until = (date.fromisoformat("2026-06-21") - date.fromisoformat(today_iso)).days and set data["pre_cycle_countdown"] = days_until ONLY when days_until > 0 (post-cycle with no active block: set neither key — silent omit). Guard ALL field access with `if block:` (Pitfall 4). Wrap in try/except logger.warning("morning_briefing: block state fetch failed", exc_info=True) — never crash the briefing.

In core/weekly_training_review.py _gather_week_data, add a new numbered section "6. BlockStore + BenchmarkStore" after the UserProfileStore section (after line 191): import BlockStore and BenchmarkStore, construct with env vars, call get_current(); if block: week_num = (today - date.fromisoformat("2026-06-21")).days // 7 + 1, set data["current_block"] = {**block, "week_num": week_num}, then bench_store.get_block_benchmarks(block.get("block_id") or block.get("doc_id")) into data["block_benchmarks"] (or [] if no id); else compute pre-cycle days_until and set data["pre_cycle_countdown"] when > 0. On exception: set data["current_block"] = None and data["block_benchmarks"] = [] (matching the file's defensive-default convention) and logger.warning. Derive week_num from plan_start_date (D-03) — never read a stored week field. No fenced code; follow PATTERNS.md verbatim.</action>
  <verify>
    <automated>python -m pytest tests/test_morning_briefing.py tests/test_weekly_training_review.py -q</automated>
  </verify>
  <acceptance_criteria>
    - core/morning_briefing.py contains a BlockStore gather with an `if block:` guard and a pre_cycle_countdown branch
    - core/weekly_training_review.py contains a BlockStore + BenchmarkStore gather section setting current_block + block_benchmarks
    - `grep -c "2026-06-21" core/morning_briefing.py core/weekly_training_review.py` shows the derived-week formula in both (no stored week_num field read)
    - Both crons' block gather is inside try/except (best-effort, never crashes) — `grep -A1 "block state fetch failed\|BlockStore/BenchmarkStore fetch failed" <files>` shows logger.warning
    - `python -m pytest tests/test_morning_briefing.py tests/test_weekly_training_review.py -q` exits 0
  </acceptance_criteria>
  <done>Both crons gather block state best-effort, derive week_num from the anchor, and degrade silently pre-cycle and on store failure.</done>
</task>

<task type="auto">
  <name>Task 3: block rendering in both prompt files</name>
  <files>prompts/morning_briefing.md, prompts/weekly_training_review.md</files>
  <read_first>
    - prompts/morning_briefing.md (nutrition conditional section ~line 68 + recovery_concern section ~line 125 — the "only when key present / OMIT when absent" pattern)
    - prompts/weekly_training_review.md (data-key list lines 10-18 — the `- \`key\` — description` convention)
    - .planning/phases/23-block-benchmark-tracking/23-PATTERNS.md (prompt files section lines 464-504)
  </read_first>
  <action>In prompts/morning_briefing.md, add a conditional section "## Current Training Block (when `block` key is present in data)": when data["block"] is present, open the briefing with a single line "Week {block.week_num} of 16 — {block.label}, Sir." and, if block.benchmark_due is true, add a brief note that a benchmark is due this week; when `pre_cycle_countdown` is present instead, render "Pre-cycle, Sir — your 16-week build begins in {pre_cycle_countdown} days (Sun 2026-06-21)."; when BOTH keys are absent, OMIT block framing entirely (no placeholder). In prompts/weekly_training_review.md, add two data-key lines to the data-key list: "- `current_block` — active BlockStore doc with `label`, `week_num`, `benchmark_due`, `end_date` (None pre-cycle)" and "- `block_benchmarks` — list of BenchmarkStore docs for this block (may be empty)"; add a rendering rule: when current_block is present, frame the review with "Week N of 16, {label}"; when block_benchmarks is non-empty, include a brief per-facet RAW delta vs the prior block where available — NO trend projection (Phase 25). Keep the JARVIS/C-3PO voice in both.</action>
  <verify>
    <automated>grep -l "Week.*of 16" prompts/morning_briefing.md prompts/weekly_training_review.md && grep -c "current_block\|block_benchmarks" prompts/weekly_training_review.md</automated>
  </verify>
  <acceptance_criteria>
    - prompts/morning_briefing.md contains a "## Current Training Block" conditional section with the "Week N of 16" line, the pre_cycle_countdown line, and an explicit "omit when both absent" rule
    - prompts/weekly_training_review.md data-key list includes `current_block` and `block_benchmarks`
    - prompts/weekly_training_review.md instructs RAW block-over-block deltas only (no projection language)
    - Both files mention "Week" / "of 16" framing
  </acceptance_criteria>
  <done>Both prompts render block framing when present, the pre-cycle countdown, and omit cleanly when absent; the weekly review shows raw deltas only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore (BlockStore/BenchmarkStore) → cron gather | persisted block/benchmark state read into best-effort gather dicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-13 | Denial of Service | block gather crashing a cron (pre-cycle None / empty store) | mitigate | Both gathers are best-effort try/except with `if block:` guards (Pitfall 4); on failure the key is unset (morning) or defaulted to None/[] (weekly); the cron always completes. |
| T-23-14 | Tampering | stale/wrong week number surfaced in coaching | mitigate | week_num is derived at gather time from plan_start_date 2026-06-21 (D-03); never read from a stored field, so it cannot drift. |
| T-23-15 | Information Disclosure | benchmark/lift numbers in Telegram briefing | accept | Single recipient (Amit); surfacing measured numbers is the intended UX (BLOCK-03). LOW. |
</threat_model>

<verification>
- `python -m pytest tests/test_morning_briefing.py tests/test_weekly_training_review.py -q` exits 0
- both crons' block gather wrapped in try/except (best-effort)
- week_num derived from 2026-06-21 in both crons (no stored week field)
- `python -m pytest -q` full suite green
</verification>

<success_criteria>
- Morning briefing surfaces "Week N of 16, [label]" when active and the pre-cycle countdown before 2026-06-21 (BLOCK-01 SC#1 / D-04)
- Weekly review surfaces current_block + this-block benchmarks as raw deltas (BLOCK-01 / BLOCK-03; projection deferred to Phase 25)
- Both gathers degrade silently on None block, empty store, or store failure — never crash the cron (Pitfall 4)
- Week number derived from plan_start_date at gather time, never stored (D-03)
</success_criteria>

<output>
Create `.planning/phases/23-block-benchmark-tracking/23-04-SUMMARY.md` when done
</output>
