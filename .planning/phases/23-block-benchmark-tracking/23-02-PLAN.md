---
phase: 23-block-benchmark-tracking
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - core/tools.py
  - tests/test_tool_registration_phase23.py
autonomous: true
requirements: [BLOCK-01, BLOCK-03]
must_haves:
  truths:
    - "Brain can call get_plan, get_block_status, log_benchmark, get_benchmark_history, start_block, end_block"
    - "All 6 new tools are brain-direct (in SMART_AGENT_DIRECT_TOOLS) and excluded from WORKER_TOOL_SCHEMAS"
    - "update_plan is NOT re-added (already exists from Phase 21)"
    - "get_block_status returns current block (resolved by date range) + its benchmarks + raw delta vs prior block"
    - "get_plan and get_block_status surface the active block via date-range get_current(), never depending on a manual start_block call (D-01)"
    - "log_benchmark supports Epley estimation for bench/squat top-sets"
  artifacts:
    - path: "core/tools.py"
      provides: "6 new brain-direct tool schemas + handlers + _HANDLERS dispatch"
      contains: "get_block_status"
    - path: "tests/test_tool_registration_phase23.py"
      provides: "Tool registration coverage for the 6 new tools"
  key_links:
    - from: "_HANDLERS"
      to: "BlockStore / BenchmarkStore"
      via: "handler functions importing stores"
      pattern: "BenchmarkStore|BlockStore"
    - from: "SMART_AGENT_DIRECT_TOOLS"
      to: "WORKER_TOOL_SCHEMAS exclusion"
      via: "the 6 new tool names"
      pattern: "get_block_status"
---

<objective>
Add the 6 new brain-direct tools to `core/tools.py`: `get_plan`, `get_block_status`, `log_benchmark`, `get_benchmark_history`, `start_block`, `end_block`. Register them in `SMART_AGENT_DIRECT_TOOLS`, exclude them from `WORKER_TOOL_SCHEMAS`, and dispatch them in `_HANDLERS`. `update_plan` already exists (Phase 21) and must NOT be re-added.

Purpose: Give the brain reactive (chat-path) access to block state and benchmark recording/comparison so Amit can ask "what block am I in?" / "log my bench at 90kg" / "bench history across blocks?" and Klaus answers from real stored data (BLOCK-01, BLOCK-03).
Output: 6 tool schemas + handlers + dispatch entries, plus a tool-registration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-block-benchmark-tracking/23-CONTEXT.md
@.planning/phases/23-block-benchmark-tracking/23-RESEARCH.md
@.planning/phases/23-block-benchmark-tracking/23-PATTERNS.md
@.planning/phases/23-block-benchmark-tracking/23-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (memory/firestore_db.py) and existing tools.py anchors. -->

Stores (from Plan 01):
  BlockStore(project_id, database):
    get_current(today=None)->dict|None — resolves the active block by DATE RANGE (start_date <= today <= end_date)
      across all seeded blocks; defaults today to current ISO date; NOT a status==active filter; transitions
      Block 1 -> Block 2 automatically with no start_block call (D-01).
    get_all()->list, upsert(block)
    start_block(id)/end_block(id) — bookkeeping ONLY: set the status field + drive the current_block_id FK;
      they do NOT gate get_current() resolution (which is purely date-driven).
  BenchmarkStore(project_id, database): log_benchmark(date,facet,value,unit,block_id,notes=""), get_facet_history(facet,n=10)->list, get_block_benchmarks(block_id)->list
  get_week_num(plan_start_date, today)->int|None
  UserProfileStore: load()->dict, update(patch) ; scaffold has current_block_id
  _jsonsafe_doc(d), epley helper if needed

tools.py anchors (VERIFIED live):
  SMART_AGENT_DIRECT_TOOLS frozenset at line 40 (update_plan already at line 57)
  TOOL_SCHEMAS list (get_training_profile schema lines 657-669; update_plan schema line 724; read_coaching_guide schema lines 672-696)
  WORKER_TOOL_SCHEMAS comprehension at line 894 (excludes update_plan at line 913)
  _HANDLERS dict at line 1551 (update_plan handler line 1576)
  _handle_get_training_profile lines 1353-1365 (read-handler pattern: import store, env-var construct, json.dumps)
  _handle_update_training_profile lines 1414-1425 (write-handler pattern: try/except → {"error": str(exc)})
</interfaces>

<closed_sets>
The 6 NEW tools (update_plan EXCLUDED — already registered, Pitfall 2):
  get_plan (no args) — UserProfileStore.load() + BlockStore.get_current() merged (block resolved by date range)
  get_block_status (no args) — current block (date-range resolved) + its benchmarks + raw per-facet delta vs prior block
  log_benchmark (args: date, facet, value, unit, block_id; optional notes) — BenchmarkStore.log_benchmark
  get_benchmark_history (args: facet; optional n) — BenchmarkStore.get_facet_history
  start_block (args: block_id) — BlockStore.start_block + UserProfileStore.update current_block_id (bookkeeping)
  end_block (args: block_id) — BlockStore.end_block + UserProfileStore.update current_block_id=None (bookkeeping)
Closed facet set for log_benchmark validation (handler defers to store ValueError): bench_press_1rm, squat_1rm, push_ups, pull_ups, threshold_pace
Epley: 1RM ≈ weight × (1 + reps/30), round to 1 decimal.
</closed_sets>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_tool_registration_phase23.py (RED first)</name>
  <files>tests/test_tool_registration_phase23.py</files>
  <read_first>
    - tests/test_tool_registration_phase20.py (exact registration-test structure to mirror)
    - core/tools.py (SMART_AGENT_DIRECT_TOOLS line 40, WORKER_TOOL_SCHEMAS line 894, _HANDLERS line 1551)
  </read_first>
  <behavior>
    - test_six_new_tools_in_direct: each of {get_plan, get_block_status, log_benchmark, get_benchmark_history, start_block, end_block} is in SMART_AGENT_DIRECT_TOOLS
    - test_six_new_tools_excluded_from_worker: none of the 6 appear in WORKER_TOOL_SCHEMAS names (V4 access control, T-23-05)
    - test_six_new_tools_in_handlers: each of the 6 is a key in _HANDLERS
    - test_update_plan_not_duplicated: "update_plan" appears exactly once in SMART_AGENT_DIRECT_TOOLS and once in _HANDLERS (Pitfall 2)
    - test_six_new_tools_have_schemas: each of the 6 has a schema entry in TOOL_SCHEMAS with name + description + input_schema
  </behavior>
  <action>Create tests/test_tool_registration_phase23.py mirroring tests/test_tool_registration_phase20.py. Import the registry objects from core.tools. Assert membership of the 6 names in SMART_AGENT_DIRECT_TOOLS and _HANDLERS, absence from WORKER_TOOL_SCHEMAS (build the worker name set from `{s["name"] for s in WORKER_TOOL_SCHEMAS}`), single occurrence of update_plan, and presence of a TOOL_SCHEMAS entry per new tool. Tests fail until the tools are added (RED).</action>
  <verify>
    <automated>python -m pytest tests/test_tool_registration_phase23.py -q 2>&1 | grep -qE "failed|error|Error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tool_registration_phase23.py exists and references all 6 tool names
    - Running it exits non-zero (RED) before Task 2
    - Includes the update_plan-not-duplicated assertion
  </acceptance_criteria>
  <done>Registration test exists and fails because the 6 tools are not yet wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 6 brain-direct tools — schemas, handlers, dispatch (GREEN)</name>
  <files>core/tools.py</files>
  <read_first>
    - core/tools.py (get_training_profile schema 657-669; read_coaching_guide schema 672-696; _handle_get_training_profile 1353-1365; _handle_update_training_profile 1414-1425; SMART_AGENT_DIRECT_TOOLS 40-62; WORKER_TOOL_SCHEMAS 894-917; _HANDLERS 1551-1578)
    - .planning/phases/23-block-benchmark-tracking/23-PATTERNS.md (tools.py section lines 216-290)
    - memory/firestore_db.py (BlockStore/BenchmarkStore/get_week_num signatures from Plan 01 — get_current resolves by date range)
  </read_first>
  <action>Add 6 TOOL_SCHEMAS entries. Zero-arg schemas (copy get_training_profile shape) for `get_plan` and `get_block_status`. Arg schemas (copy read_coaching_guide shape) for: `log_benchmark` required ["date","facet","value","unit","block_id"], optional "notes"; `get_benchmark_history` required ["facet"], optional "n"; `start_block` required ["block_id"]; `end_block` required ["block_id"]. Descriptions name the closed facet set and note brain-direct. Add the 6 names to SMART_AGENT_DIRECT_TOOLS after line 61 (DO NOT add update_plan — Pitfall 2). Add the same 6 names to the WORKER_TOOL_SCHEMAS exclusion set after line 913. Write handlers mirroring _handle_get_training_profile (read) and _handle_update_training_profile (write, try/except → {"error": str(exc)}): `_handle_get_plan()` merges UserProfileStore.load() and BlockStore.get_current() (the block is resolved by date range — no start_block dependency; compute week_num via get_week_num against plan_start_date "2026-06-21"), json.dumps; `_handle_get_block_status()` returns current block (from date-range get_current) + BenchmarkStore.get_block_benchmarks(block_id) + a `facet_deltas` dict of raw (current_value - prior_block_value) per facet computed from get_facet_history (prior block's most recent value per facet) — NO trend projection (out of scope, Phase 25), just raw deltas; `_handle_log_benchmark(date, facet, value, unit, block_id, notes="")` calls BenchmarkStore.log_benchmark (store raises ValueError on bad facet → caught, returned as {"error": ...}); `_handle_get_benchmark_history(facet, n=10)`; `_handle_start_block(block_id)` calls BlockStore.start_block + UserProfileStore.update({"current_block_id": block_id}) (bookkeeping — get_current still resolves by date); `_handle_end_block(block_id)` calls BlockStore.end_block + UserProfileStore.update({"current_block_id": None}) (bookkeeping). Add an `epley_1rm(weight, reps)` helper (round(weight*(1+reps/30),1)) usable when logging bench/squat from a top-set — the brain passes the computed value, but expose the helper for handler use if value derivation is requested. Add 6 _HANDLERS dispatch entries after line 1578 (no-arg: `lambda args: _handle_x()`; arg: `lambda args: _handle_x(**args)`). Apply _jsonsafe_doc to dict results. No fenced code in this action; follow PATTERNS.md.</action>
  <verify>
    <automated>python -m pytest tests/test_tool_registration_phase23.py -q</automated>
  </verify>
  <acceptance_criteria>
    - core/tools.py contains handlers _handle_get_plan, _handle_get_block_status, _handle_log_benchmark, _handle_get_benchmark_history, _handle_start_block, _handle_end_block
    - `python -c "import core.tools"` exits 0 (no duplicate-key/KeyError at module load — Pitfall 2)
    - All 6 names present in SMART_AGENT_DIRECT_TOOLS and _HANDLERS, absent from WORKER_TOOL_SCHEMAS
    - update_plan appears exactly once in SMART_AGENT_DIRECT_TOOLS
    - get_plan/get_block_status surface the active block via date-range get_current() (no start_block precondition)
    - get_block_status payload includes block + benchmarks + facet_deltas (raw, no projection)
    - `python -m pytest tests/test_tool_registration_phase23.py -q` exits 0
  </acceptance_criteria>
  <done>All 6 tools registered, dispatched, brain-direct, worker-excluded; module imports cleanly; registration test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brain → tool handlers | LLM-chosen tool args (facet, value, block_id) cross into store writes |
| worker → tools | worker must NOT reach block/benchmark mutation tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Elevation of Privilege | start_block/end_block/log_benchmark via worker | mitigate | All 6 tools excluded from WORKER_TOOL_SCHEMAS; access control is structural. Tested by test_six_new_tools_excluded_from_worker. |
| T-23-06 | Tampering | log_benchmark value/facet injection | mitigate | facet validated against closed 5-set in BenchmarkStore (Plan 01); handler catches ValueError and returns {"error":...} instead of writing garbage. |
| T-23-07 | Denial of Service | duplicate update_plan key crashing module load | mitigate | update_plan NOT re-added; test_update_plan_not_duplicated + `import core.tools` gate. |
| T-23-08 | Information Disclosure | benchmark/lift numbers returned to brain | accept | Single-user agent, brain is Amit's own; no external exposure. LOW. |
</threat_model>

<verification>
- `python -m pytest tests/test_tool_registration_phase23.py -q` exits 0
- `python -c "import core.tools"` exits 0
- `python -m pytest -q` full suite green
</verification>

<success_criteria>
- 6 brain-direct tools callable, worker-excluded, dispatched in _HANDLERS (BLOCK-01, BLOCK-03)
- get_plan surfaces current block (date-range resolved) + week number; get_block_status surfaces benchmarks + raw cross-block deltas
- log_benchmark records per-facet results; get_benchmark_history serves cross-block facet history
- update_plan untouched (single registration), module imports cleanly
</success_criteria>

<output>
Create `.planning/phases/23-block-benchmark-tracking/23-02-SUMMARY.md` when done
</output>
