---
phase: 33-occasion-cascade
plan: 08
type: execute
wave: 3
depends_on: [33-04]
files_modified:
  - core/weekly_training_review.py
  - tests/test_weekly_training_review.py
autonomous: true
requirements: [OCC-03, OCC-06]
must_haves:
  truths:
    - "The Sunday weekly review runs as occasion=\"weekly_review\" through the shared cascade when OCCASION_CASCADE is on (OCC-03)"
    - "The weekly review never self-skips — Layer 1's verdict shapes emphasis and which topics lead, never whether it fires (D-03)"
    - "A standing directive can still veto the weekly, and that veto is recorded distinctly from a failure (D-03, Phase 31 D-21/D-22)"
    - "With OCCASION_CASCADE off, the weekly composes exactly as it does in production today (OCC-06, D-30)"
    - "_derive_structural_topics and the post-send CoachingTopicStore write survive the composer replacement untouched"
    - "The weekly's compose is not truncated mid-thought by a default completion budget"
  artifacts:
    - path: "core/weekly_training_review.py"
      provides: "Cascade-routed weekly with advisory-only triage and flag branching"
      contains: "advisory_only=True"
    - path: "core/weekly_training_review.py"
      provides: "Weekly occasion state doc for heartbeat anomaly detection"
      contains: "weekly_reviews"
  key_links:
    - from: "core/weekly_training_review.py::run_weekly_review"
      to: "core/autonomous.py::run_occasion_cascade"
      via: "occasion=\"weekly_review\", advisory_only=True, max_tokens=32000"
      pattern: "run_occasion_cascade"
    - from: "core/weekly_training_review.py::_gather_week_data"
      to: "memory/firestore_db.py::CoachingTopicStore"
      via: "coaching_topics_included derived pre-send, written post-send (unchanged)"
      pattern: "_derive_structural_topics"
---

<objective>
Route the Sunday weekly training review through the cascade — with the one asymmetry that
defines it: it never self-skips.

D-03 is precise about what changes. The weekly's triage judgment governs **shape and
emphasis** — what this week is about, which topics lead, whether it is a scorecard week or
a "you've been sick, here's the reset" week — and never whether it fires. A missing Sunday
leaves a hole in the training record; a fixed cadence with judged content is the real win
over the template. The only thing that can still silence it is the Phase 31 Step-0
standing-directive veto, which this plan must preserve exactly.

Plan 33-04 implements this as `advisory_only=True`: Layer 1 still runs and its
draft/reason still shape the compose, but a `should_act=False` verdict does not stop the
send.

Purpose: OCC-03 — the last legacy composer moves onto the shared cascade.
Output: a cascade-routed `run_weekly_review` behind `OCCASION_CASCADE`, a `weekly_reviews`
state doc so the heartbeat can tell "not fired" from "vetoed", and the surviving
deterministic topic-derivation machinery left alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/CODING_STANDARDS.md
@.planning/phases/33-occasion-cascade/33-CONTEXT.md
@.planning/phases/33-occasion-cascade/33-PATTERNS.md
@.planning/phases/33-occasion-cascade/33-VALIDATION.md
@.planning/phases/33-occasion-cascade/33-04-SUMMARY.md

<interfaces>
<!-- Contract this plan CREATES/PRESERVES. Plan 33-10 (/internal/process-occasion) and
     plan 33-11 (heartbeat anomaly #3) depend on these exactly. -->

core/weekly_training_review.py:

    _STATE_COLLECTION = "weekly_reviews"     # NEW — date-keyed, mirrors nightly_reviews

    async def run_weekly_review(bot, today_iso: str) -> None
        # signature UNCHANGED — /cron/weekly-training-review calls it as-is

    def _get_state(today_iso: str) -> dict           # NEW, copied from nightly_review.py
    def _set_state(today_iso: str, fields: dict) -> None   # NEW, same pattern

    # SURVIVING UNTOUCHED (33-RESEARCH § Pitfall 9):
    def _gather_week_data(today_iso: str) -> dict
    def _derive_structural_topics(week_data: dict) -> list[str]
    # and the post-send CoachingTopicStore.add_topic loop in run_weekly_review

    # SURVIVING for the flag-off arm (deleted in Phase 35, not here):
    def _compose_review(week_data, today_iso) -> str
    def _parse_review_skip(text) -> tuple[bool, str, str]

    # weekly_reviews/{date} document fields:
    #   status:       "sent" | "skipped_by_directive"     (NEVER "skipped_by_judgment" — D-03)
    #   composed_via: "llm" | "draft_fallback"            (absent when vetoed)
    #   trigger:      "cron"
    #   sent_at / judged_at: ISO-8601
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route the weekly through the cascade in advisory-only mode</name>
  <files>core/weekly_training_review.py, tests/test_weekly_training_review.py</files>
  <read_first>
    - core/weekly_training_review.py lines 548-617 — `run_weekly_review` in full: the two
      `run_in_executor` offloads, the `_parse_review_skip` veto branch, the `TimedOut`
      single-retry around `send_and_inject`, and the post-send `CoachingTopicStore` loop
    - core/weekly_training_review.py lines 389-520 — `_compose_review`, especially the
      `max_tokens=32000` argument at line 472 and the comment explaining the 2026-07-19
      mid-thought truncation incident
    - core/weekly_training_review.py lines 340 and 362-386 — `_derive_structural_topics` and
      its call site inside `_gather_week_data`; 33-RESEARCH § Pitfall 9 warns these are NOT
      part of the composer being replaced
    - core/nightly_review.py lines 55-76 — the `_get_state`/`_set_state` pair to copy verbatim
      (same `_make_firestore_client` + `collection(...).document(date).set(..., merge=True)`)
    - core/autonomous.py::run_occasion_cascade — the `advisory_only` and `max_tokens`
      keywords from plan 33-04
    - .planning/phases/33-occasion-cascade/33-CONTEXT.md § D-03, D-30
  </read_first>
  <action>
    Add the flag branch and the cascade call to `run_weekly_review`.

    Read the flag once:
    `_cascade_on = os.getenv("OCCASION_CASCADE", "false").lower() == "true"`.

    **Flag OFF** — the existing `_compose_review` → `_parse_review_skip` → send →
    `CoachingTopicStore` sequence, unchanged except for the state-doc writes added in Task 2.

    **Flag ON**:
      1. `week_data = await loop.run_in_executor(None, _gather_week_data, today_iso)` —
         unchanged, and it still sets `data["coaching_topics_included"] =
         _derive_structural_topics(data)` internally. Do not touch either.
      2. `decision = await run_occasion_cascade(bot, occasion="weekly_review",
         target_date=today_iso, occasion_data=week_data,
         occasion_prompt=<prompts/weekly_occasion.md>, advisory_only=True,
         max_tokens=32000)`.
         `advisory_only=True` is D-03: Layer 1 runs, its `reason` and `draft` reach Layer 2
         as shape guidance, and a `should_act=False` verdict does not stop the send.
         `max_tokens=32000` carries forward `_compose_review`'s existing budget through the
         `_run_smart_loop` passthrough added by plan 33-02 — the weekly is the largest
         compose in the system and Sonnet's internal thinking previously exhausted the
         default 16K mid-thought (2026-07-19 incident).
      3. Keep the send inside `run_occasion_cascade` (it already sends with
         `message_class="review"`). The `TimedOut` single-retry currently wrapping
         `send_and_inject` in this module protects against a known Telegram flake on this
         surface — move that retry behaviour into the cascade call site by catching
         `telegram.error.TimedOut` around the `run_occasion_cascade` await, sleeping 2s and
         retrying **once**, and note in the code comment that the retry may re-run Layer 2.
         Guard the re-run against a double send by checking `decision.get("sent")` from the
         first attempt before retrying — only retry when the first attempt raised before
         `sent` became True.
      4. Post-send `CoachingTopicStore.add_topic` loop: keep it, gated on
         `decision.get("sent")` being True, exactly the write-after-send discipline it has today.

    Do not delete `_compose_review`, `_parse_review_skip`, `_derive_structural_topics`, or
    `prompts/weekly_training_review.md`. Composer deletion is Phase 35.
  </action>
  <acceptance_criteria>
    - `grep -c "advisory_only=True" core/weekly_training_review.py` returns `1`
    - `grep -c "max_tokens=32000" core/weekly_training_review.py` returns `2` (the surviving
      `_compose_review` call and the new cascade call)
    - `grep -c "def _derive_structural_topics" core/weekly_training_review.py` returns `1`
      and `grep -c "_derive_structural_topics(data)" core/weekly_training_review.py` returns `1`
      — Pitfall 9 guard: the function and its call site both survive
    - Test `never_self_skips`: with `TickBrain.think` returning
      `{"should_act": False, "reason": "quiet week", "draft": "..."}`, the flag-ON weekly
      still reaches Layer 2 compose and still calls `send_and_inject`; the returned/stored
      status is `"sent"` and is never `"skipped_by_judgment"`
    - Test `occasion_cascade_flag`: flag off ⇒ `_compose_review` called,
      `run_occasion_cascade` not; flag on ⇒ the inverse
    - Test: `CoachingTopicStore.add_topic` is called once per entry in
      `week_data["coaching_topics_included"]` after a successful cascade send, and not at all
      when the send failed
    - Test: a `TimedOut` raised by the first cascade attempt triggers exactly one retry, and
      a `TimedOut` raised after `sent` became True does not
    - `pytest tests/test_weekly_training_review.py -x` green
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_weekly_training_review.py -k "never_self_skips or occasion_cascade_flag" -x -q</automated>
  </verify>
  <done>The weekly runs through the cascade in advisory-only mode with its large completion budget preserved; Layer 1 shapes it and can never silence it; the deterministic topic machinery and the post-send topic write are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Weekly state doc and the surviving standing-directive veto</name>
  <files>core/weekly_training_review.py, tests/test_weekly_training_review.py</files>
  <read_first>
    - core/weekly_training_review.py lines 519-547 — `_parse_review_skip`, the current
      directive-veto mechanism (a fenced-JSON trailer from the composer)
    - core/autonomous.py — where the Step-0 standing-directive veto lives in the shared
      triage prompt path, and `_gather_standing_directives` (line 399) which puts active
      directives into the situation dict
    - core/tools.py::render_standing_directives_block — how directive scope is rendered
    - core/nightly_review.py lines 55-76 — the `_get_state`/`_set_state` pattern to copy
    - core/heartbeat.py lines 108-118 — `_CRON_MAX_STALENESS_HOURS["weekly-training-review"] = 170`,
      the existing staleness check that plan 33-11 extends; it needs a state doc to read
    - .planning/phases/33-occasion-cascade/33-CONTEXT.md § D-03 and 31-CONTEXT.md § D-21/D-22
      (the Phase 31 legacy-cron veto invariant that must survive intact)
  </read_first>
  <action>
    **Add a `weekly_reviews/{date}` state doc.** Copy `_get_state` / `_set_state` from
    `core/nightly_review.py` verbatim (same `_make_firestore_client` helper, same
    `collection(_STATE_COLLECTION).document(today_iso).set(fields, merge=True)`, same
    never-raises posture), with `_STATE_COLLECTION = "weekly_reviews"`. No store class —
    33-PATTERNS is explicit that these date-keyed occasion state docs use direct collection
    access, matching nightly and morning.

    Write on both branches:
      - after a successful send → `{"status": "sent", "trigger": "cron", "sent_at": <ISO now>,
        "judged_at": <ISO now>, "composed_via": decision["composed_via"]}`
      - on a directive veto → `{"status": "skipped_by_directive", "trigger": "cron",
        "judged_at": <ISO now>, "skip_reason": <reason>}` with **no** `composed_via` key
      - `"skipped_by_judgment"` must be structurally unreachable for this occasion (D-03).

    **Preserve the directive veto on the cascade path.** On the flag-ON path there is no
    `_parse_review_skip` trailer to parse, because Layer 1 owns the Step-0 standing-orders
    check. Before calling `run_occasion_cascade`, read the active standing directives from
    the situation the cascade will build — or, more simply and with less coupling, check
    them directly here via the same `_gather_standing_directives`-backed accessor
    `core/autonomous.py` uses, and short-circuit to the `skipped_by_directive` state write
    when a directive's scope covers the weekly review. Do NOT rely on Layer 1's
    `should_act=False` to express a veto: `advisory_only=True` deliberately ignores that
    field, so a directive expressed only through the verdict would be silently overridden.
    This is the load-bearing detail of D-03 — the one thing that can silence the weekly must
    be checked upstream of the advisory-only cascade call.

    Log the veto at INFO with the same `weekly_review: skipped_by_directive for %s (%s)`
    message shape the current code uses, so existing log greps and the Phase 31 behaviour
    are unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c '_STATE_COLLECTION = "weekly_reviews"' core/weekly_training_review.py` returns `1`
    - `grep -c "skipped_by_judgment" core/weekly_training_review.py` returns `0` — the status
      is structurally impossible for this occasion
    - Test `directive_veto`: with an active standing directive whose scope covers the weekly
      review, the flag-ON path writes `status == "skipped_by_directive"` with a `skip_reason`,
      never calls `run_occasion_cascade`, and never calls `send_and_inject`
    - Test: with `advisory_only=True` and NO directive, a `should_act=False` verdict still
      results in `status == "sent"` — proving the veto is upstream, not derived from the verdict
    - Test: the flag-OFF path's existing `_parse_review_skip` veto still writes
      `status == "skipped_by_directive"` (same state contract on both arms, so the heartbeat
      check works during the A/B window)
    - Test: a successful send writes `composed_via` and a veto does not — one field
      distinguishes "vetoed" from "ran"
    - `pytest tests/test_weekly_training_review.py -x` green
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_weekly_training_review.py -k "directive_veto or state" -x -q</automated>
  </verify>
  <done>A `weekly_reviews/{date}` doc records every Sunday's outcome on both flag arms; a standing directive is checked upstream of the advisory-only cascade so it can still veto; a judgment skip is unreachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Week-window training/biometric data (Garmin, Postgres, Firestore) → paid tool-enabled compose | The largest data volume of any compose in the system crosses into an agentic loop |
| Standing directives → weekly send/no-send | The only control that can silence a fixed-cadence message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-25 | Elevation of Privilege | `advisory_only=True` overriding a directive-expressed veto | mitigate | The standing-directive check is performed upstream of the cascade call and short-circuits to `skipped_by_directive`; a test asserts a `should_act=False` verdict alone never silences the weekly, so the two mechanisms cannot be confused (D-03, Phase 31 D-21/D-22) |
| T-33-26 | Denial of Service | weekly compose truncated or starving the event loop | mitigate | `max_tokens=32000` carried through the plan 33-02 passthrough prevents the 2026-07-19 mid-thought truncation; the gather stays on `run_in_executor` and the cascade's own Layer-2 executor offload preserves the fix for the three-Sunday 500 incident |
| T-33-27 | Repudiation | a missing Sunday indistinguishable from a vetoed one | mitigate | `weekly_reviews/{date}` records `sent` vs `skipped_by_directive` with `composed_via` present only on the former; heartbeat anomaly #3 (plan 33-11) reads it, so "the weekly did not fire" is always attributable |
| T-33-28 | Tampering | duplicate weekly send from the `TimedOut` retry | mitigate | The retry only fires when the first attempt raised before `decision["sent"]` became True; the shared OutreachLog `weekly:<date>` topic key gives per-day repeat suppression as a second layer (D-18) |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (33-RESEARCH.md § Standard Stack) |
</threat_model>

<verification>
- `pytest tests/test_weekly_training_review.py -x -q` green
- `pytest tests/test_coaching_topic_store.py -x -q` green (Pitfall 9 regression guard)
- `grep -c "skipped_by_judgment" core/weekly_training_review.py` returns `0`
- `OCCASION_CASCADE` unset ⇒ `git diff core/weekly_training_review.py` shows the legacy compose path unmodified apart from the state-doc writes
</verification>

<success_criteria>
The Sunday weekly runs through the shared cascade with judged shape and a guaranteed send,
a standing directive can still veto it through a mechanism that advisory mode cannot
override, the flag-off arm is production-identical, and the deterministic topic machinery
survived the composer replacement.
</success_criteria>

<output>
Create `.planning/phases/33-occasion-cascade/33-08-SUMMARY.md` when done
</output>
